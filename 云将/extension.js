import { lib, game, ui, get, ai, _status } from '../../noname.js';
game.import('extension', function (lib, game, ui, get, ai, _status) {
    //写入(改自手杀ui和群英荟萃)
    const zhuwrite = function (lists, thisname) {
        if (lists && lists.length) {
            var SRr = "<html><head><meta charset='utf-8'><style tybe='text/css'>body {background-image: url(beijing.png);background-size: 100% 100%;background-position: center;--w: 560px;--h: calc(var(--w) * 610/1058);width: var(--w);height: var(--h);background-repeat: no-repeat;background-attachment: fixed;}h1{text-shadow:1px 1px 1PX #000000,1px -1px 1PX #000000,-1px 1px 1PX #000000,-1px -1px 1PX #000000;font-size:20px}div {width: 160vmin;height: 63vmin;border: 0px solid black;border-radius: 9px;padding: 35px;margin-top: 5.5vmin;margin-bottom: 5.5vmin;margin-left: 10.5vmin;margin-right: 10.5vmin;position: center;}div.ex1 {width: 160vmin;/*改变内框宽度*/height: 63vmin;overflow: auto;}</style></head><body><div class='ex1'>";
            for (var i = 0; i < lists.length; i++) {
                SRr += '〖';
                if (lib.translate[lists[i]]) SRr += lib.translate[lists[i]] + '(' + lists[i] + ')〗';
                else SRr += lists[i] + '〗';
                if (lib.translate[lists[i] + '_info']) SRr += lib.translate[lists[i] + '_info'];
            }
            SRr += '</div></body></html>';
        } else {
            var SRr = "<html><head><meta charset='utf-8'><style tybe='text/css'>body {background-image: url(beijing.png);background-size: 100% 100%;background-position: center;--w: 560px;--h: calc(var(--w) * 610/1058);width: var(--w);height: var(--h);background-repeat: no-repeat;background-attachment: fixed;}h1{text-shadow:1px 1px 1PX #000000,1px -1px 1PX #000000,-1px 1px 1PX #000000,-1px -1px 1PX #000000;font-size:20px}div {width: 160vmin;height: 63vmin;border: 0px solid black;border-radius: 9px;padding: 35px;margin-top: 5.5vmin;margin-bottom: 5.5vmin;margin-left: 10.5vmin;margin-right: 10.5vmin;position: center;}div.ex1 {width: 160vmin;/*改变内框宽度*/height: 63vmin;overflow: auto;}</style></head><body><div class='ex1'>亲～您尚未禁将</div></body></html>";
        }
        game.writeFile(SRr, 'extension/云将/shenfen', 'banZhu.html', function () { });
    };
    const zhongwrite = function (lists, thisname) {
        if (lists && lists.length) {
            var SRr = "<html><head><meta charset='utf-8'><style tybe='text/css'>body {background-image: url(beijing.png);background-size: 100% 100%;background-position: center;--w: 560px;--h: calc(var(--w) * 610/1058);width: var(--w);height: var(--h);background-repeat: no-repeat;background-attachment: fixed;}h1{text-shadow:1px 1px 1PX #000000,1px -1px 1PX #000000,-1px 1px 1PX #000000,-1px -1px 1PX #000000;font-size:20px}div {width: 160vmin;height: 63vmin;border: 0px solid black;border-radius: 9px;padding: 35px;margin-top: 5.5vmin;margin-bottom: 5.5vmin;margin-left: 10.5vmin;margin-right: 10.5vmin;position: center;}div.ex1 {width: 160vmin;/*改变内框宽度*/height: 63vmin;overflow: auto;}</style></head><body><div class='ex1'>";
            for (var i = 0; i < lists.length; i++) {
                SRr += '〖';
                if (lib.translate[lists[i]]) SRr += lib.translate[lists[i]] + '(' + lists[i] + ')〗';
                else SRr += lists[i] + '〗';
                if (lib.translate[lists[i] + '_info']) SRr += lib.translate[lists[i] + '_info'];
            }
            SRr += '</div></body></html>';
        } else {
            var SRr = "<html><head><meta charset='utf-8'><style tybe='text/css'>body {background-image: url(beijing.png);background-size: 100% 100%;background-position: center;--w: 560px;--h: calc(var(--w) * 610/1058);width: var(--w);height: var(--h);background-repeat: no-repeat;background-attachment: fixed;}h1{text-shadow:1px 1px 1PX #000000,1px -1px 1PX #000000,-1px 1px 1PX #000000,-1px -1px 1PX #000000;font-size:20px}div {width: 160vmin;height: 63vmin;border: 0px solid black;border-radius: 9px;padding: 35px;margin-top: 5.5vmin;margin-bottom: 5.5vmin;margin-left: 10.5vmin;margin-right: 10.5vmin;position: center;}div.ex1 {width: 160vmin;/*改变内框宽度*/height: 63vmin;overflow: auto;}</style></head><body><div class='ex1'>亲～您尚未禁将</div></body></html>";
        }
        game.writeFile(SRr, 'extension/云将/shenfen', 'banZhong.html', function () { });
    };
    const fanwrite = function (lists, thisname) {
        if (lists && lists.length) {
            var SRr = "<html><head><meta charset='utf-8'><style tybe='text/css'>body {background-image: url(beijing.png);background-size: 100% 100%;background-position: center;--w: 560px;--h: calc(var(--w) * 610/1058);width: var(--w);height: var(--h);background-repeat: no-repeat;background-attachment: fixed;}h1{text-shadow:1px 1px 1PX #000000,1px -1px 1PX #000000,-1px 1px 1PX #000000,-1px -1px 1PX #000000;font-size:20px}div {width: 160vmin;height: 63vmin;border: 0px solid black;border-radius: 9px;padding: 35px;margin-top: 5.5vmin;margin-bottom: 5.5vmin;margin-left: 10.5vmin;margin-right: 10.5vmin;position: center;}div.ex1 {width: 160vmin;/*改变内框宽度*/height: 63vmin;overflow: auto;}</style></head><body><div class='ex1'>";
            for (var i = 0; i < lists.length; i++) {
                SRr += '〖';
                if (lib.translate[lists[i]]) SRr += lib.translate[lists[i]] + '(' + lists[i] + ')〗';
                else SRr += lists[i] + '〗';
                if (lib.translate[lists[i] + '_info']) SRr += lib.translate[lists[i] + '_info'];
            }
            SRr += '</div></body></html>';
        } else {
            var SRr = "<html><head><meta charset='utf-8'><style tybe='text/css'>body {background-image: url(beijing.png);background-size: 100% 100%;background-position: center;--w: 560px;--h: calc(var(--w) * 610/1058);width: var(--w);height: var(--h);background-repeat: no-repeat;background-attachment: fixed;}h1{text-shadow:1px 1px 1PX #000000,1px -1px 1PX #000000,-1px 1px 1PX #000000,-1px -1px 1PX #000000;font-size:20px}div {width: 160vmin;height: 63vmin;border: 0px solid black;border-radius: 9px;padding: 35px;margin-top: 5.5vmin;margin-bottom: 5.5vmin;margin-left: 10.5vmin;margin-right: 10.5vmin;position: center;}div.ex1 {width: 160vmin;/*改变内框宽度*/height: 63vmin;overflow: auto;}</style></head><body><div class='ex1'>亲～您尚未禁将</div></body></html>";
        }
        game.writeFile(SRr, 'extension/云将/shenfen', 'banFan.html', function () { });
    };
    const neiwrite = function (lists, thisname) {
        if (lists && lists.length) {
            var SRr = "<html><head><meta charset='utf-8'><style tybe='text/css'>body {background-image: url(beijing.png);background-size: 100% 100%;background-position: center;--w: 560px;--h: calc(var(--w) * 610/1058);width: var(--w);height: var(--h);background-repeat: no-repeat;background-attachment: fixed;}h1{text-shadow:1px 1px 1PX #000000,1px -1px 1PX #000000,-1px 1px 1PX #000000,-1px -1px 1PX #000000;font-size:20px}div {width: 160vmin;height: 63vmin;border: 0px solid black;border-radius: 9px;padding: 35px;margin-top: 5.5vmin;margin-bottom: 5.5vmin;margin-left: 10.5vmin;margin-right: 10.5vmin;position: center;}div.ex1 {width: 160vmin;/*改变内框宽度*/height: 63vmin;overflow: auto;}</style></head><body><div class='ex1'>";
            for (var i = 0; i < lists.length; i++) {
                SRr += '〖';
                if (lib.translate[lists[i]]) SRr += lib.translate[lists[i]] + '(' + lists[i] + ')〗';
                else SRr += lists[i] + '〗';
                if (lib.translate[lists[i] + '_info']) SRr += lib.translate[lists[i] + '_info'];
            }
            SRr += '</div></body></html>';
        } else {
            var SRr = "<html><head><meta charset='utf-8'><style tybe='text/css'>body {background-image: url(beijing.png);background-size: 100% 100%;background-position: center;--w: 560px;--h: calc(var(--w) * 610/1058);width: var(--w);height: var(--h);background-repeat: no-repeat;background-attachment: fixed;}h1{text-shadow:1px 1px 1PX #000000,1px -1px 1PX #000000,-1px 1px 1PX #000000,-1px -1px 1PX #000000;font-size:20px}div {width: 160vmin;height: 63vmin;border: 0px solid black;border-radius: 9px;padding: 35px;margin-top: 5.5vmin;margin-bottom: 5.5vmin;margin-left: 10.5vmin;margin-right: 10.5vmin;position: center;}div.ex1 {width: 160vmin;/*改变内框宽度*/height: 63vmin;overflow: auto;}</style></head><body><div class='ex1'>亲～您尚未禁将</div></body></html>";
        }
        game.writeFile(SRr, 'extension/云将/shenfen', 'banNei.html', function () { });
    };
    return {
        name: '云将',
        content(config, pack) {
            if (lib.rank) {
                lib.rank.rarity.rare.addArray(['yunyuanshu', 'yundengai', 'yunyuanshao', 'yunhuaman', 'yunyujin', 'yunlejin', 'yundianwei', 'yunliuxie', 'yunhuangzhong', 'yunzhangchunhua', 'yundongzuo', 'db_yundaxiaoqiao', 'yunsunce', 'yunsunjian', 'yuncaiwenji', 'yunhuangyueying', 'yunzuoci', 'yuncaishi', 'yundiaochan', 'yundongbai', 'yunwangji', 'yungongsunzhan', 'yunjiangwei', 'yunlusu', 'yunxushi', 'db_yunmachao', 'yuntaishici', 'yunzhenfu', 'yunzhangjiao', 'yunxuhuang', 'yunliru', 'yunmaliang', 'yunliushan']);
                lib.rank.rarity.epic.addArray(['yuncaopi', 'yunzhaoxiang', 'yunfazheng', 'yunluxun', 'yuncaoying', 'yunsunquan', 'yunzhanghe', 'yunxunyu', 'db_yunjiaxu', 'yunsunshangxiang', 'yunzhugeliang', 'yunlvbu', 'yunguanyu', 'yunguojia', 'yunpangtong', 'yunzhangliao', 'yuncaocao', 'yunzhouyu', 'yunlvmeng', 'yunliubei', 'yunzhaoyun', 'yunzhangfei', 'yunsimayi', 'yunlukang', 'yunlvlingqi', 'yunganning']);
                lib.rank.rarity.legend.addArray(['yunyouzi', 'yunhanyun', 'yunjihanyun', 'yunshenhanyun']);
            }

            lib.skill._yjzwyy = {
                trigger: {
                    global: 'dieAfter',
                },
                forced: true,
                _priority: 2,
                forced: true,
                forceDie: true,
                lastDo: true,
                filter(event, player) {
                    return !event.player.isAlive();
                },
                content() {
                    game.playAudio('../extension/云将/audio', trigger.player.name);
                },
            };

            if (lib.config.extension_云将_tianxiasanfen == 'on') {
                lib.skill._sanfentianxia_wei = {
                    trigger: {
                        player: 'damageEnd',
                    },
                    usable: 1,
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mode: ['identity'],
                    filter(event, player) {
                        return player.group == 'wei';
                    },
                    content() {
                        var cards = get.cards();
                        player.showCards(cards, get.translation(player));
                        if ((event.bool = get.color(cards[0]) == 'black')) {
                            player.gain(cards, 'draw2');
                        }
                    },
                };
                lib.skill._sanfentianxia_shu = {
                    trigger: {
                        player: 'shaMiss',
                    },
                    usable: 1,
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mode: ['identity'],
                    filter(event, player) {
                        return player.group == 'shu' && get.itemtype(event.cards) == 'cards' && get.position(event.cards[0]) == 'd' && !player.hasSkill('kongcheng');
                    },
                    content() {
                        player.gain(trigger.cards);
                        player.$gain2(trigger.cards);
                    },
                };
                lib.skill._sanfentianxia_wu = {
                    trigger: {
                        player: 'taoEnd',
                    },
                    usable: 1,
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mode: ['identity'],
                    filter(event, player) {
                        return _status.currentPhase == player && player.group == 'wu';
                    },
                    content() {
                        'step 0';
                        var card = get.cardPile2(function (card) {
                            return [card.name == 'huogong', card.name == 'tiesuo'].randomGet();
                        });
                        if (card) player.gain(card, 'gain2');
                        ('step 1');
                        game.updateRoundNumber();
                    },
                };
                lib.skill._sanfentianxia_qun = {
                    trigger: {
                        player: 'phaseZhunbeiBefore',
                    },
                    usable: 1,
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mode: ['identity'],
                    filter(event, player) {
                        if (player.hp != 1) return false;
                        return player.group == 'qun' && Math.ceil(game.players.length) > 4;
                    },
                    content() {
                        player.chooseDrawRecover(true);
                    },
                };
                lib.skill._sanfentianxia_jin = {
                    trigger: {
                        player: 'phaseJieshuBegin',
                    },
                    usable: 1,
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mode: ['identity'],
                    filter(event, player) {
                        return player.group == 'jin' && player.countCards('h') > 0;
                    },
                    content() {
                        'step 0';
                        player.chooseToDiscard('h', '是否弃置一张手牌观看牌堆顶一张牌,将其置于牌堆顶或牌堆底并摸一张牌').set('ai', function (card) {
                            if (get.attitude(player, trigger.target) > 0) return true;
                            return 6 - get.value(card);
                        });
                        ('step 1');
                        if (result.bool) {
                            player.chooseToGuanxing(1);
                        } else {
                            event.finish();
                        }
                        ('step 2');
                        player.draw();
                    },
                };
                lib.skill._sanfentianxia_shen = {
                    trigger: {
                        player: 'phaseZhunbeiBegin',
                    },
                    usable: 1,
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mode: ['identity'],
                    filter(event, player) {
                        return player.group == 'shen' && player.countCards('j') && player.countCards('he') > 1;
                    },
                    content() {
                        'step 0';
                        player.chooseToDiscard('he', 2, '是否弃置两张牌来移除自己判定区的一张牌？').ai = function (card) {
                            if (player.countCards('j') <= 1 && (player.hasSkillTag('rejudge') || player.hasSkillTag('nodamage') || player.hasSkillTag('nothunder')) && (player.hasJudge('shandian') || player.hasJudge('fulei'))) return false;
                            return 6 - get.value(card);
                        };
                        ('step 1');
                        if (result.bool) {
                            player.discardPlayerCard(player, 'j', true).ai = function (button) {
                                if (player.countCards('h') < 2 && (!player.hasJudge('shandian') || !player.hasJudge('fulei'))) {
                                    return -get.value(button.link);
                                }
                                return get.value(button.link);
                            };
                        }
                    },
                };
            }

            if (lib.config.extension_云将_shanheyonggu == 'on') {
                lib.skill._fenghuyunlong = {
                    trigger: {
                        global: 'gameStart',
                    },
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mode: ['identity'],
                    filter(event, player) {
                        return player.identity == 'zhu' && _status.mode == 'normal';
                    },
                    content() {
                        if (
                            game.countPlayer(function (current) {
                                return current.identity == 'zhong';
                            }) > 0
                        ) {
                            var list = [];
                            for (var i = 0; i < game.players.length; i++) {
                                if (game.players[i].identity == 'zhong') {
                                    list.push(game.players[i]);
                                }
                            }
                            var target = list.randomGet();
                            player.storage.mingzhong = target;
                            player.storage.dongcha = target;
                            if (!_status.connectMode) {
                                if (player == game.me) {
                                    target.setIdentity('zhong');
                                    target.node.identity.classList.remove('guessing');
                                    target.zhongfixed = true;
                                }
                            } else {
                                player.chooseControl('ok').set('dialog', [get.translation(target) + '是忠臣', [[target.name], 'character']]);
                            }
                        }
                        player.addTempSkill('shanheyonggu_mz');
                    },
                };
                lib.skill.shanheyonggu_mz = {
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mode: ['identity'],
                    ai: {
                        effect: {
                            player(card, player, target, current) {
                                if (get.tag(card, 'damage') && get.attitude(player, target) <= 0) {
                                    if (card.name == 'huogong' || card.name == 'juedou') {
                                        return [0.05, 0];
                                    }
                                    if (card.name == 'sha' && !card.nature) {
                                        if ((target.hasSkill('tengjia3') || target.hasSkill('rw_tengjia4')) && !(player.getEquip('qinggang') || player.getEquip('zhuque'))) {
                                            return 'zeroplayertarget';
                                        }
                                    }
                                    if (card.name == 'sha' && get.color(card) == 'black' && target.hasSkill('renwang_skill')) {
                                        if (!player.getEquip('qinggang')) {
                                            return 'zeroplayertarget';
                                        }
                                    }
                                    if (player.storage.mingzhong != target) {
                                        return [0.05, 0.05];
                                    }
                                }
                                if (card.name == 'guohe' || card.name == 'shunshou' || card.name == 'lebu' || card.name == 'bingliang' || card.name == 'caomu' || card.name == 'zhujinqiyuan' || card.name == 'caochuanjiejian' || card.name == 'toulianghuanzhu') {
                                    if (get.attitude(player, target) <= 0 && player.storage.mingzhong != target) {
                                        return [0.05, 0.05];
                                    }
                                }
                            },
                        },
                    },
                };

                lib.skill._tianxiayitong = {
                    trigger: {
                        global: ['dieAfter'],
                    },
                    silent: true,
                    forced: true,
                    popup: false,
                    charlotte: true,
                    mode: ['identity'],
                    filter(event, player) {
                        if (get.mode() == 'identity') {
                            var numf = game.countPlayer(function (current) {
                                return current.identity == 'fan';
                            });
                        }
                        return _status.mode == 'normal' && (player.identity == 'zhu' || player.identity == 'zhong') && numf == 0 && !player.isUnderControl(true);
                    },
                    content() {
                        var func = function () {
                            game.countPlayer(function (current) {
                                current.setIdentity();
                            });
                        };
                        if (player == game.me) func();
                        else if (player.isOnline()) player.send(func);
                        if (!player.storage.zhibi) player.storage.zhibi = [];
                        player.storage.zhibi.addArray(game.players);
                        player.addSkill('shanheyonggu_smz');
                    },
                };
                lib.skill.shanheyonggu_smz = {
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mode: ['identity'],
                    ai: {
                        effect: {
                            player(card, player, target, current) {
                                if (get.tag(card, 'damage')) {
                                    if (target.identity == 'nei') {
                                        return [0.5, 0.05];
                                    }
                                    if (card.name == 'nanman' || card.name == 'wanjian') {
                                        if (
                                            game.countPlayer(function (current) {
                                                return current.identity == 'nei' && (current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1'));
                                            })
                                        ) {
                                            return 'zeroplayertarget';
                                        }
                                    }
                                }
                                if (card.name == 'guohe' || card.name == 'shunshou' || card.name == 'lebu' || card.name == 'bingliang' || card.name == 'caomu' || card.name == 'zhujinqiyuan' || card.name == 'caochuanjiejian' || card.name == 'toulianghuanzhu') {
                                    if (target.identity == 'zhong' || target.identity == 'zhu') {
                                        return 0;
                                    }
                                    if (target.identity == 'nei') {
                                        if (card.name == 'guohe' || card.name == 'shunshou') {
                                            if (target.countCards('he') == 0 && (target.hasJudge('bingliang') || target.hasJudge('lebu'))) {
                                                return 0;
                                            }
                                        }
                                        return [0.5, 0.05];
                                    }
                                }
                            },
                        },
                    },
                };

                lib.skill._yingshilanggu = {
                    trigger: {
                        global: ['changeHp', 'dieAfter', 'useSkillAfter', 'phaseZhunbeiAfter', 'phaseBefore', 'phaseJieshuAfter', 'useCard', 'respond'],
                    },
                    silent: true,
                    forced: true,
                    popup: false,
                    charlotte: true,
                    mode: ['identity'],
                    filter(event, player) {
                        return _status.mode == 'normal' && player.identity == 'nei';
                    },
                    content() {
                        'step 0';
                        event.cfz = 0;
                        event.cff = 0;
                        player.removeSkill('shanheyonggu_tz');
                        player.removeSkill('shanheyonggu_tf');
                        ('step 1');
                        if (get.mode() == 'identity') {
                            var numz = game.countPlayer(function (current) {
                                return current.identity == 'zhong';
                            });
                            var numf = game.countPlayer(function (current) {
                                return current.identity == 'fan';
                            });
                            if (
                                game.countPlayer(function (current) {
                                    return (current.identity == 'zhu' || current.identity == 'zhong') && game.getRarity(current.name) == 'rare';
                                })
                            ) {
                                event.cfz += 1;
                            }
                            if (
                                game.countPlayer(function (current) {
                                    return (current.identity == 'zhu' || current.identity == 'zhong') && game.getRarity(current.name) == 'epic';
                                })
                            ) {
                                event.cfz += 2;
                            }
                            if (
                                game.countPlayer(function (current) {
                                    return (current.identity == 'zhu' || current.identity == 'zhong') && game.getRarity(current.name) == 'legend';
                                })
                            ) {
                                event.cfz += 3;
                            }
                            if (
                                game.countPlayer(function (current) {
                                    return current.identity == 'fan' && game.getRarity(current.name) == 'rare';
                                })
                            ) {
                                event.cff += 1;
                            }
                            if (
                                game.countPlayer(function (current) {
                                    return current.identity == 'fan' && game.getRarity(current.name) == 'epic';
                                })
                            ) {
                                event.cff += 2;
                            }
                            if (
                                game.countPlayer(function (current) {
                                    return current.identity == 'fan' && game.getRarity(current.name) == 'legend';
                                })
                            ) {
                                event.cff += 3;
                            }
                            if (Math.abs(event.cff - event.cfz) <= 3) {
                                if (numz > 0 && numf > 0) {
                                    if (numf == 1 && numz == 1) {
                                        player.addTempSkill('shanheyonggu_tf');
                                        event.finish();
                                    } else {
                                        if (game.zhu.hp >= 2 && numf <= numz + 1) {
                                            player.addTempSkill('shanheyonggu_tf');
                                            event.finish();
                                        } else {
                                            player.addTempSkill('shanheyonggu_tz');
                                            event.finish();
                                        }
                                    }
                                }
                                if (numf == 0 && numz > 0) {
                                    player.addTempSkill('shanheyonggu_tf');
                                    event.finish();
                                }
                                if (numf > 0 && numz == 0) {
                                    player.addTempSkill('shanheyonggu_tz');
                                    event.finish();
                                }
                                if (numz == 0 && numf == 0) {
                                    player.removeSkill('shanheyonggu_tz');
                                    player.removeSkill('shanheyonggu_tf');
                                    event.finish();
                                }
                            } else {
                                if (event.cff >= event.cfz || game.zhu.hp < 2) {
                                    player.addTempSkill('shanheyonggu_tz');
                                    event.finish();
                                }
                                if (event.cff < event.cfz && game.zhu.hp >= 2) {
                                    player.addTempSkill('shanheyonggu_tf');
                                    event.finish();
                                }
                            }
                        }
                    },
                };

                lib.skill.shanheyonggu_tz = {
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mode: ['identity'],
                    ai: {
                        effect: {
                            player(card, player, target, current) {
                                if (get.tag(card, 'damage')) {
                                    if (player.hasSkill('juesheng') && game.roundNumber < 2) {
                                        if (card.name == 'juedou') {
                                            return 0;
                                        }
                                    }
                                    if (target.identity == 'zhu') {
                                        return [0, -0.5];
                                    }
                                    if (target.identity == 'fan') {
                                        return [0.05, 0.05];
                                    }
                                    if (target.identity == 'zhong') {
                                        return [0.005, 0.005];
                                    }
                                }
                                if (card.name == 'guohe' || card.name == 'lebu' || card.name == 'bingliang' || card.name == 'caomu' || card.name == 'zhujinqiyuan' || card.name == 'caochuanjiejian' || card.name == 'toulianghuanzhu') {
                                    if (target.identity == 'zhu') {
                                        return 0;
                                    }
                                    if (target.identity == 'fan') {
                                        return [0.05, 0.05];
                                    }
                                    if (target.identity == 'zhong') {
                                        return [0.005, 0.005];
                                    }
                                }
                            },
                        },
                    },
                };

                lib.skill.shanheyonggu_tf = {
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mode: ['identity'],
                    ai: {
                        effect: {
                            player(card, player, target, current) {
                                if (get.tag(card, 'damage')) {
                                    if (player.hasSkill('juesheng') && game.roundNumber < 2) {
                                        if (card.name == 'juedou') {
                                            return 0;
                                        }
                                    }
                                    if (target.identity == 'zhu') {
                                        return [0, -0.5];
                                    }
                                    if (target.identity == 'zhong') {
                                        return [0.05, 0.05];
                                    }
                                    if (target.identity == 'fan') {
                                        return [0.005, 0.005];
                                    }
                                }
                                if (card.name == 'guohe' || card.name == 'lebu' || card.name == 'bingliang' || card.name == 'caomu' || card.name == 'zhujinqiyuan' || card.name == 'caochuanjiejian' || card.name == 'toulianghuanzhu') {
                                    if (game.zhu.hp > 3 && target.identity == 'zhu') {
                                        return [0.05, 0.05];
                                    }
                                    if (target.identity == 'zhong') {
                                        return [0.005, 0.005];
                                    }
                                    if (target.identity == 'fan') {
                                        return [0.05, 0.05];
                                    }
                                }
                            },
                        },
                    },
                };

                lib.skill._wanjunpiyi = {
                    ai: {
                        threaten(player, target) {
                            if (game.getRarity(player.name) == 'common') return 2;
                            if (game.getRarity(player.name) == 'rare') return 4;
                            if (game.getRarity(player.name) == 'epic') return 6;
                            if (game.getRarity(player.name) == 'legend') return 8;
                        },
                    },
                };

                lib.skill._shanheyonggu = {
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mode: ['identity'],
                    ai: {
                        effect: {
                            player(card, player, target, current) {
                                var numz = game.countPlayer(function (current) {
                                    return current.identity == 'zhong';
                                });
                                var numf = game.countPlayer(function (current) {
                                    return current.identity == 'fan';
                                });
                                var numn = game.countPlayer(function (current) {
                                    return current.identity == 'nei';
                                });

                                if (_status.mode == 'normal' && card.name == 'taoyuan') {
                                    if ((player.identity == 'zhong' || player.identity == 'zhu') && game.zhu.hp == 1 && get.recoverEffect(game.zhu, player, player) > 0) {
                                        return [2.5, 2.5];
                                    }
                                }

                                if (player.identity == 'zhu' && _status.mode == 'normal' && get.tag(card, 'damage') && numz == 0 && numf > 0) {
                                    if (target.identity == 'nei') {
                                        return 0;
                                    }
                                }
                                if (player.identity == 'nei' && _status.mode == 'normal' && get.tag(card, 'damage') && numz >= 2 && numf == 1) {
                                    if (target.identity == 'fan') {
                                        return 0;
                                    }
                                }
                                if (player.identity == 'fan' && _status.mode == 'normal' && get.tag(card, 'damage')) {
                                    if (numz >= 2 && numf == 1) {
                                        if (target.identity == 'nei') {
                                            return 0;
                                        }
                                    }
                                }

                                if (player.isUnderControl(true) && player.identity == 'fan' && get.tag(card, 'damage') && get.attitude(target, player) > 0) {
                                    if (target.identity == 'fan' && target.hp == 1 && target.countCards('h') < 3 && (numz > 0 || numn > 0)) {
                                        return 'zeroplayertarget';
                                    }
                                }
                            },
                        },
                    },
                };

                lib.skill._shanheyonggu_cs = {
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    mod: {
                        aiUseful(player, card, num) {
                            if (card.name == 'tao') {
                                return num + 5;
                            }
                            if (card.name == 'jiu') {
                                return num + 3;
                            }
                        },
                        aiValue(player, card, num) {
                            if (player.isPhaseUsing()) {
                                if (
                                    player.countCards('hes', { name: 'qinggang' }) >= 1 &&
                                    game.hasPlayer(function (current) {
                                        return current != player && current.getEquip(2) && get.attitude(player, current) < 0;
                                    }) == 1
                                ) {
                                    if (card.name == 'qinggang') {
                                        return num + 15;
                                    } else {
                                        if (get.type(card) == 'equip' && get.subtype(card) == 'equip1') return num - 15;
                                    }
                                }
                                if (
                                    player.countCards('hes', { name: 'qilin' }) >= 1 &&
                                    game.hasPlayer(function (current) {
                                        return current != player && current.getEquip(3) && get.attitude(player, current) < 0;
                                    }) == 1
                                ) {
                                    if (card.name == 'qilin') {
                                        return num + 15;
                                    } else {
                                        if (get.type(card) == 'equip' && get.subtype(card) == 'equip1') return num - 15;
                                    }
                                }
                                if (
                                    player.countCards('hes', { name: 'zhuque' }) >= 1 &&
                                    game.hasPlayer(function (current) {
                                        return current != player && current.hasSkill('tengjia3') && get.attitude(player, current) < 0;
                                    }) == 1
                                ) {
                                    if (card.name == 'zhuque') {
                                        return num + 15;
                                    } else {
                                        if (get.type(card) == 'equip' && get.subtype(card) == 'equip1') return num - 15;
                                    }
                                }
                                if (
                                    player.countCards('hes', { name: 'guding' }) >= 1 &&
                                    game.hasPlayer(function (current) {
                                        return current != player && current.countCards('h') <= 0 && get.attitude(player, current) < 0;
                                    }) == 1
                                ) {
                                    if (card.name == 'guding') {
                                        return num + 15;
                                    } else {
                                        if (get.type(card) == 'equip' && get.subtype(card) == 'equip1') return num - 15;
                                    }
                                }
                            }
                        },
                        aiOrder(player, card, num) {
                            if (
                                game.countPlayer(function (current) {
                                    return get.attitude(player, current) > 0 && current.hasSkill('xinfu_guanwei') && current.countCards('he') > 0 && !current.hasSkill('xinfu_guanwei_off');
                                })
                            ) {
                                if (typeof card == 'object' && player.isPhaseUsing()) {
                                    var evt = player.getLastUsed();
                                    if (evt && evt.card && evt.card.suit && evt.card.suit == card.suit) {
                                        return num + 10;
                                    }
                                }
                            }

                            if (
                                player.isPhaseUsing() &&
                                player.countCards('hes', { name: 'qinggang' }) >= 1 &&
                                game.hasPlayer(function (current) {
                                    return current != player && current.getEquip(2) && get.distance(player, current) <= 2 && get.attitude(player, current) < 0;
                                }) == 1
                            ) {
                                if (card.name == 'qinggang') {
                                    return num + 20;
                                } else {
                                    if (get.type(card) == 'equip' && get.subtype(card) == 'equip1') return num - 20;
                                }
                            }
                            if (
                                player.isPhaseUsing() &&
                                player.countCards('hes', { name: 'qilin' }) >= 1 &&
                                game.hasPlayer(function (current) {
                                    return current != player && current.getEquip(3) && get.distance(player, current) <= 5 && get.attitude(player, current) < 0;
                                }) == 1
                            ) {
                                if (card.name == 'qilin') {
                                    return num + 10;
                                } else {
                                    if (get.type(card) == 'equip' && get.subtype(card) == 'equip1') return num - 10;
                                }
                            }
                            if (
                                player.isPhaseUsing() &&
                                player.countCards('hes', { name: 'zhuque' }) >= 1 &&
                                game.hasPlayer(function (current) {
                                    return current != player && current.hasSkill('tengjia3') && get.distance(player, current) <= 4 && get.attitude(player, current) < 0;
                                }) == 1
                            ) {
                                if (card.name == 'zhuque') {
                                    return num + 25;
                                } else {
                                    if (get.type(card) == 'equip' && get.subtype(card) == 'equip1') return num - 25;
                                }
                            }
                            if (
                                player.isPhaseUsing() &&
                                player.countCards('hes', { name: 'guding' }) >= 1 &&
                                game.hasPlayer(function (current) {
                                    return current != player && current.countCards('h') <= 0 && get.distance(player, current) <= 2 && get.attitude(player, current) < 0;
                                }) == 1
                            ) {
                                if (card.name == 'guding') {
                                    return num + 25;
                                } else {
                                    if (get.type(card) == 'equip' && get.subtype(card) == 'equip1') return num - 25;
                                }
                            }
                        },
                    },
                    ai: {
                        effect: {
                            player(card, player, target, current) {
                                if (!card) return; //QQQ
                                var numz = game.countPlayer(function (current) {
                                    return current.identity == 'zhong';
                                });
                                var numf = game.countPlayer(function (current) {
                                    return current.identity == 'fan';
                                });
                                var hzdr = game.countPlayer(function (current) {
                                    return current.isLinked() && !current.hasSkillTag('nodamage') && get.attitude(current, player) <= 0;
                                });
                                var hzdyf = game.countPlayer(function (current) {
                                    return current.isLinked() && !(current.hasSkillTag('nodamage') || current.hasSkillTag('nofire')) && get.attitude(current, player) > 0;
                                });
                                var hzdrf = game.countPlayer(function (current) {
                                    return current.isLinked() && !(current.hasSkillTag('nodamage') || current.hasSkillTag('nofire')) && get.attitude(current, player) <= 0;
                                });
                                var hzdyt = game.countPlayer(function (current) {
                                    return current.isLinked() && !(current.hasSkillTag('nodamage') || current.hasSkillTag('nothunder')) && get.attitude(current, player) > 0;
                                });
                                var hzdrt = game.countPlayer(function (current) {
                                    return current.isLinked() && !(current.hasSkillTag('nodamage') || current.hasSkillTag('nothunder')) && get.attitude(current, player) <= 0;
                                });
                                var cxhzdyf = game.countPlayer(function (current) {
                                    return current.isLinked() && !(current.hasSkillTag('nodamage') || current.hasSkillTag('nofire')) && current.hp <= 1 && get.attitude(current, player) > 0;
                                });
                                var cxhzdyt = game.countPlayer(function (current) {
                                    return current.isLinked() && !(current.hasSkillTag('nodamage') || current.hasSkillTag('nothunder')) && current.hp <= 1 && get.attitude(current, player) > 0;
                                });
                                if (!target) return; //QQQ
                                if (player.isUnderControl(true) && get.tag(card, 'damage') && card.nature && get.attitude(target, player) > 0) {
                                    if (target.isLinked() && hzdr > 0) {
                                        return 'zeroplayertarget';
                                    }
                                }
                                if (get.tag(card, 'damage') && card.nature && card.nature == 'fire') {
                                    if (target.isLinked()) {
                                        if (hzdyf >= hzdrf && cxhzdyf >= 1) {
                                            return 'zeroplayertarget';
                                        }
                                        if (get.mode() == 'identity') {
                                            if (player.identity == 'zhong' && game.zhu.isLinked() && game.zhu.hp <= 3 && !(game.zhu.hasSkillTag('nodamage') || game.zhu.hasSkillTag('nofire'))) {
                                                return 'zeroplayertarget';
                                            }
                                            if (player.identity == 'zhu' && cxhzdyf >= 1) {
                                                return 'zeroplayertarget';
                                            }
                                            if (player.identity == 'nei' && game.zhu.isLinked() && game.zhu.hp <= 3 && (numz > 0 || numf > 0)) {
                                                return 'zeroplayertarget';
                                            }
                                        }
                                    }
                                }
                                if (get.tag(card, 'damage') && card.nature && card.nature == 'thunder') {
                                    if (target.isLinked()) {
                                        if (hzdyt >= hzdrt && cxhzdyt >= 1) {
                                            return 'zeroplayertarget';
                                        }
                                        if (get.mode() == 'identity') {
                                            if (player.identity == 'zhong' && game.zhu.isLinked() && game.zhu.hp <= 3 && !(game.zhu.hasSkillTag('nodamage') || game.zhu.hasSkillTag('nothunder'))) {
                                                return 'zeroplayertarget';
                                            }
                                            if (player.identity == 'zhu' && cxhzdyt >= 1) {
                                                return 'zeroplayertarget';
                                            }
                                            if (player.identity == 'nei' && game.zhu.isLinked() && game.zhu.hp <= 3 && (numz > 0 || numf > 0)) {
                                                return 'zeroplayertarget';
                                            }
                                        }
                                    }
                                }

                                if (card.name == 'sha') {
                                    if ((target.hasSkill('tengjia3') || target.hasSkill('rw_tengjia4')) && !card.nature) {
                                        if (!(player.getEquip('zhuque') || player.getEquip('qinggang') || player.hasSkillTag('unequip'))) {
                                            return 'zeroplayertarget';
                                        }
                                    }
                                    if (target.hasSkill('tengjia2') && card.nature && get.attitude(player, target) < 0) {
                                        return [0.5, 0.5];
                                    }
                                    if (target.hasSkill('tengjia2') && player.getEquip('zhuque') && get.attitude(player, target) < 0) {
                                        return [0.5, 0.5];
                                    }
                                    if (target.hasSkill('renwang_skill')) {
                                        if (get.color(card) == 'black') {
                                            if (!(player.getEquip('qinggang') || player.hasSkillTag('unequip'))) {
                                                return 'zeroplayertarget';
                                            }
                                        }
                                    }
                                    if (target.hasSkillTag('useShan') && get.attitude(player, target) > 0) {
                                        if (player.hasSkill('jiu')) {
                                            return [-0.5, -0.5];
                                        }
                                        if (target.getEquip('bagua') && player.getEquip('qinggang')) {
                                            return [-0.5, -0.5];
                                        }
                                        if (card.nature && target.hasSkill('tengjia2')) {
                                            return [-0.5, -0.5];
                                        }
                                        if (!card.nature && target.hasSkill('tengjia2')) {
                                            return 'zeroplayertarget';
                                        }
                                        if (target.hasSkill('renwang_skill')) {
                                            if (get.color(card) == 'black') {
                                                return 'zeroplayertarget';
                                            }
                                        }
                                        if (target.getEquip('bagua')) {
                                            return [0.5, 0.5];
                                        }
                                        if (target.countCards('hs', { name: 'shan' }) <= 1) {
                                            return [-0.5, -0.5];
                                        }
                                    }
                                    if (player.hasSkill('jiu')) {
                                        if ((target.getEquip('baiyin') || target.getEquip('rewrite_baiyin')) && get.attitude(target, player) < 0) {
                                            if (!(player.getEquip('qinggang') || player.hasSkillTag('unequip'))) {
                                                return [0.5, -0.5];
                                            }
                                        }
                                        if (get.attitude(target, player) > 0) {
                                            if (target.hasSkillTag('maixie')) {
                                                return [-1, -1];
                                            }
                                            if (target.hasSkillTag('useShan')) {
                                                return [-1, -1];
                                            }
                                        }
                                    }
                                    if (game.roundNumber == 1) {
                                        if (player.identity == 'fan' && target.identity == 'fan') {
                                            return 0;
                                        }
                                        if (get.attitude(player, target) < 0) {
                                            return [0.5, 0.5];
                                        }
                                        if (get.attitude(player, target) <= 0) {
                                            return [0.05, 0.05];
                                        }
                                    }
                                }

                                if (card.name == 'jiu') {
                                    if (!player.hasSkill('yunxiongxu') && !player.hasSkill('xinfu_tushe') && !player.hasSkill('hengwu')) {
                                        if (
                                            player.hp > 0 &&
                                            target == player &&
                                            player.countCards('hs', 'sha') > 0 &&
                                            !game.hasPlayer(function (current) {
                                                return current != player && player.inRange(current) && get.attitude(player, current) < 0 && !current.hasSkill('baiyin_skill') && !current.hasSkill('rw_baiyin_skill');
                                            })
                                        ) {
                                            return 'zeroplayertarget';
                                        }
                                        if (player.hp > 0 && target == player && player.countCards('hs', 'sha') == 0) {
                                            return 'zeroplayertarget';
                                        }
                                    }
                                }

                                if (card.name == 'guohe' || card.name == 'shunshou') {
                                    if (get.attitude(player, target) < 0 && target.getDamagedHp() && target.countCards('h') == 0 && target.countCards('e') == 1 && (target.getEquip('baiyin') || target.getEquip('rewrite_baiyin'))) {
                                        return 'zeroplayertarget';
                                    }
                                    if (get.attitude(player, target) < 0 && target.countCards('he') == 0 && (target.hasJudge('bingliang') || target.hasJudge('lebu'))) {
                                        return 'zeroplayertarget';
                                    }
                                    if (get.attitude(player, target) > 0 && (target.hasJudge('bingliang') || target.hasJudge('lebu'))) {
                                        return [2.5, 2.5];
                                    }
                                    if (get.attitude(player, target) > 0 && (target.hasJudge('shandian') || target.hasJudge('fulei')) && (target.hasSkillTag('nodamage') || target.hasSkillTag('nothunder') || target.hasSkillTag('rejudge'))) {
                                        return 'zeroplayertarget';
                                    }
                                    if (game.roundNumber == 1) {
                                        if (player.identity == 'fan' && target.identity == 'fan') {
                                            return 0;
                                        }
                                        if (get.attitude(player, target) < 0) {
                                            return [0.5, 0.5];
                                        }
                                        if (get.attitude(player, target) <= 0) {
                                            return [0.05, 0.05];
                                        }
                                    }
                                }

                                var gpdy = game.countPlayer(function (current) {
                                    return (current.hasSkillTag('rejudge') || current.hasSkill('yundunshu')) && get.attitude(current, player) > 0;
                                });
                                if (card.name == 'shandian' && gpdy <= 0) {
                                    if (!player.hasSkill('hengwu')) {
                                        return 'zeroplayertarget';
                                    }
                                }
                            },
                            target(card, player, target, current) {
                                if (!card) return; //QQQ

                                if (get.tag(card, 'damage') && !card.nature) {
                                    if (target.hasSkillTag('nodamage')) return 'zeroplayertarget';
                                }
                                if (get.tag(card, 'damage') && card.nature == 'thunder') {
                                    if (target.hasSkillTag('nodamage') || target.hasSkillTag('nothunder')) return 'zeroplayertarget';
                                }
                                if (get.tag(card, 'damage') && card.nature == 'fire') {
                                    if (target.hasSkillTag('nodamage') || target.hasSkillTag('nofire')) return 'zeroplayertarget';
                                }

                                if (get.attitude(player, target) < 0 && get.tag(card, 'damage') && target.hp == 1) {
                                    return [1.5, -1.5];
                                }

                                if (get.tag(card, 'damage') && target.hasSkillTag('maixie')) {
                                    if (get.attitude(player, target) > 0 && (target.hasSkill('lingren_jianxiong') || target.hasSkill('new_rejianxiong') || target.hasSkill('jianxiong') || target.hasSkill('huituo') || target.hasSkill('qianlong') || target.hasSkill('reandong') || target.hasSkill('fenyong2') || target.hasSkillTag('nodamage') || target.hasSkill('fenyong2') || target.hasSkill('hunzi') || target.hasSkill('rehunzi') || target.hasSkill('olhunzi'))) {
                                        return false;
                                    }
                                    if (!target.hasSkillTag('maixie_defend')) {
                                        if (get.attitude(player, target) > 0) {
                                            if (player.hasSkillTag('jueqing', false, target)) {
                                                return [0, -2.5];
                                            }
                                            if (card.name == 'sha' && player.hasSkill('jiu')) {
                                                return [0, -2.5];
                                            }
                                            if (card.name == 'huogong' && target.getEquip('tengjia')) {
                                                return [0, -2.5];
                                            }
                                            if (card.name == 'huogong' && target.hp <= 3) {
                                                return [0, -2.5];
                                            }
                                            if (card.name == 'huogong' && player.countCards('h') <= 5) {
                                                return [0, -2.5];
                                            }
                                            if (card.name == 'sha' && target.countCards('h') == 0 && player.getEquip('guding')) {
                                                return [0, -2.5];
                                            }
                                            if (card.name == 'sha' && card.nature && target.hasSkill('tengjia2')) {
                                                return [0, -2.5];
                                            }
                                        }
                                        if (player.hasSkillTag('jueqing', false, target)) return [1, -2];
                                        if (player.hasSkillTag('ignoreSkill', false, target)) return [1, -1];
                                        if (!target.hasFriend()) return;
                                        if (get.attitude(player, target) > 0) {
                                            if (target.hp >= 3 && target.countCards('hs', 'tao') >= 1) {
                                                return [0.5, 1];
                                            }
                                            if (target.hp >= 3) {
                                                return [0.5, 0.5];
                                            }
                                            if (target.hp == 2) {
                                                return [0.5, 0.25];
                                            }
                                        }
                                        if (get.attitude(player, target) < 0) {
                                            if (target.hp >= 3) {
                                                return [0.5, 0.5];
                                            }
                                            if (target.hp == 2) {
                                                return [0.5, 0.25];
                                            }
                                            if (target.hp == 1) {
                                                return [0.5, -0.5];
                                            }
                                        }
                                    }
                                }
                                if (get.tag(card, 'damage') && get.attitude(player, target) < 0 && target.hasSkillTag('maixie_defend')) {
                                    if (!target.hasFriend()) return;
                                    if (get.mode() == 'identity' && player.identity == 'zhu' && player != target && player.hp <= 2) {
                                        return [0.5, 0.5, 1, -1.5];
                                    }
                                    if (player != target && target.hasSkillTag('maixie_defend')) {
                                        return [0.5, 0.5, 1, -1];
                                    }
                                }
                            },
                        },
                    },
                };

                lib.skill._shanheyonggu_wuxie = {
                    trigger: {
                        global: 'useCardToPlayered',
                    },
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    filter(event, player) {
                        return get.type(event.card) == 'trick';
                    },
                    content() {
                        if (player.countCards('hs', { name: 'wuxie' }) == 1 && (player.hasJudge('lebu') || player.hasJudge('bingliang')) && player != _status.currentPhase) {
                            if (get.attitude(player, trigger.target) > 0 && get.tag(trigger.card, 'damage')) {
                                if (get.mode() == 'identity' && player.identity == 'zhong' && trigger.target == game.zhu) {
                                    if (game.zhu.hp > 2) {
                                        player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                                    } else {
                                        event.finish();
                                    }
                                }
                                if (trigger.target.hp >= 2) {
                                    player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                                }
                            }
                            if (get.attitude(player, trigger.target) > 0 && !get.tag(trigger.card, 'damage')) {
                                player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                            }
                        }
                        if ((trigger.card.name == 'guohe' || trigger.card.name == 'shunshou') && get.attitude(player, trigger.player) > 0 && get.attitude(player, trigger.target) > 0) {
                            player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                        }
                        if (trigger.card.name == 'guohe' && get.attitude(player, trigger.target) > 0 && get.attitude(player, trigger.player) <= 0) {
                            if (!trigger.target.getEquip(2) && !trigger.target.getEquip(3)) {
                                player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                            }
                        }
                        if (trigger.card.name == 'guohe' && trigger.target.countCards('h', { name: 'wuxie' }) == 1 && trigger.target.countCards('h') == 1) {
                            if (trigger.target != player) {
                                player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                            }
                        }
                        if (trigger.card.name == 'huogong') {
                            if (get.attitude(player, trigger.target) > 0 && get.attitude(player, trigger.player) <= 0 && player.countCards('hs', { name: 'wuxie' }) == 1) {
                                if (!trigger.target.hasSkill('tengjia2') && (trigger.target.hp >= 2 || trigger.target.hasSkillTag('nofire') || trigger.target.hasSkillTag('nodamage'))) {
                                    player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                                }
                            }
                            if (get.attitude(player, trigger.player) > 0) {
                                player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                            }
                        }
                        if (trigger.card.name == 'juedou') {
                            if (get.attitude(player, trigger.player) > 0 && get.attitude(player, trigger.target) <= 0) {
                                player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                            }
                            if (get.attitude(player, trigger.player) <= 0 && get.attitude(player, trigger.target) > 0 && trigger.target.hp > 2 && trigger.target != player) {
                                player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                            }
                        }
                        if (trigger.card.name == 'tiesuo') {
                            var hz = game.countPlayer(function (current) {
                                return current.isLinked() && !current.hasSkillTag('nodamage') && get.attitude(current, player) > 0;
                            });
                            if (get.attitude(player, trigger.target) > 0 && !trigger.target.isLinked() && (!trigger.target.hasSkill('tengjia2') || trigger.target.hp > 2 || hz <= 1)) {
                                player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                            }
                        }
                        if (trigger.card.name == 'nanman' || trigger.card.name == 'wanjian') {
                            if (get.attitude(player, trigger.target) <= 0 && trigger.target != player) {
                                player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                            }
                        }
                        if (trigger.card.name == 'nanman') {
                            if (get.attitude(player, trigger.target) > 0 && trigger.target != player && (trigger.target.hp > 2 || trigger.target.hasSkillTag('nodamage') || trigger.target.countCards('hs', { name: 'sha' }) >= 1)) {
                                player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                            }
                        }
                        if (trigger.card.name == 'wanjian') {
                            if (get.attitude(player, trigger.target) > 0 && trigger.target != player && (trigger.target.hp > 2 || trigger.target.hasSkillTag('nodamage') || trigger.target.countCards('hs', { name: 'shan' }) >= 1)) {
                                player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardToAfter');
                            }
                        }
                    },
                };
                lib.skill._shanheyonggu_wuxie_jiedao = {
                    trigger: {
                        global: 'useCard',
                    },
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    filter(event, player, card) {
                        return player.countCards('hs', { name: 'wuxie' }) == 1 && event.card.name == 'jiedao';
                    },
                    content() {
                        player.addTempSkill('shanheyonggu_wuxie_fy', 'useCardAfter');
                    },
                };
                lib.skill._shanheyonggu_wuxie_lei = {
                    trigger: {
                        global: 'phaseJudgeBefore',
                    },
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    filter(event, player, card) {
                        return event.player.countCards('j') <= 1 && (event.player.hasJudge('shandian') || event.player.hasJudge('fulei')) && (player.hasSkillTag('rejudge') || event.player.hasSkillTag('nodamage') || event.player.hasSkillTag('nothunder'));
                    },
                    content() {
                        player.addTempSkill('shanheyonggu_wuxie_fy', 'phaseJudgeAfter');
                    },
                };
                lib.skill.shanheyonggu_wuxie_fy = {
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    ai: {
                        effect: {
                            player(card, player, target) {
                                if (card.name == 'wuxie') {
                                    return 'zeroplayertarget';
                                }
                            },
                        },
                    },
                };

                lib.skill._shanheyonggu_wugu = {
                    trigger: {
                        global: 'wuguBegin',
                    },
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    content() {
                        if (player.getDamagedHp()) {
                            player.addTempSkill('shanheyonggu_wugu_ss_pai', 'wuguAfter');
                        }
                        if (!player.getDamagedHp()) {
                            player.addTempSkill('shanheyonggu_wugu_wss_pai', 'wuguAfter');
                        }
                    },
                };
                lib.skill.shanheyonggu_wugu_ss_pai = {
                    mod: {
                        aiValue(player, card, num) {
                            if (card.name == 'tao') return num + 10;
                            if (card.name == 'wuzhong') return num + 9;
                            if (card.name == 'jiu') return num + 8;
                            if (card.name == 'wuxie') return num + 7;
                            if (card.name == 'shunshou') return num + 6;
                            if (card.name == 'guohe') return num + 5;
                            if (card.name == 'lebu') return num + 4;
                            if (card.name == 'bingliang') return num + 3;
                        },
                    },
                };
                lib.skill.shanheyonggu_wugu_wss_pai = {
                    mod: {
                        aiValue(player, card, num) {
                            if (card.name == 'wuzhong') return num + 10;
                            if (card.name == 'wuxie') return num + 9;
                            if (card.name == 'shunshou') return num + 8;
                            if (card.name == 'tao') return num + 7;
                            if (card.name == 'guohe') return num + 6;
                            if (card.name == 'lebu') return num + 5;
                            if (card.name == 'bingliang') return num + 4;
                            if (card.name == 'jiu') return num + 3;
                        },
                    },
                };

                lib.skill._shanheyonggu_Discard = {
                    trigger: {
                        player: 'chooseToDiscardBefore',
                    },
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    filter(event, player, card) {
                        if (
                            game.countPlayer(function (current) {
                                return current.hp == 1 && get.attitude(current, player) < 0;
                            }) > 0
                        )
                            return false;
                        var evt = event.getParent('phaseDiscard');
                        return evt && evt.name != 'phaseDiscard';
                    },
                    content() {
                        player.addTempSkill('shanheyonggu_Discard_key', 'chooseToDiscardAfter');
                    },
                };
                lib.skill.shanheyonggu_Discard_key = {
                    mod: {
                        aiValue(player, card, num) {
                            if (player.hp > 2 && player.countCards('h') <= 2) {
                                if (card.name == 'wuzhong') return num + 3;
                            }
                            if (player.hasJudge('lebu') || player.hasJudge('bingliang')) {
                                if (card.name == 'wuxie') return num + 3;
                            }
                            if (player.hp <= 2) {
                                if (card.name == 'tao') return num + 3;
                            }
                            if (player.hp <= 1) {
                                if (card.name == 'tao') return num + 3;
                                if (card.name == 'jiu') return num + 3;
                                if (card.name == 'du') return num + 3;
                            }
                        },
                        aiUseful(player, card, num) {
                            if (player.hp > 2 && player.countCards('h') <= 2) {
                                if (card.name == 'wuzhong') return num + 3;
                            }
                            if (player.hasJudge('lebu') || player.hasJudge('bingliang')) {
                                if (card.name == 'wuxie') return num + 3;
                            }
                            if (player.hp <= 2) {
                                if (card.name == 'tao') return num + 3;
                            }
                            if (player.hp <= 1) {
                                if (card.name == 'tao') return num + 3;
                                if (card.name == 'jiu') return num + 3;
                                if (card.name == 'du') return num + 3;
                            }
                        },
                    },
                };
                lib.skill._shanheyonggu_Discard_du = {
                    trigger: {
                        player: 'phaseDiscardBefore',
                    },
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    filter(event, player, card) {
                        return player.countCards('h', { name: 'du' }) >= player.hp;
                    },
                    content() {
                        player.addTempSkill('shanheyonggu_Discard_du_useful', 'phaseDiscardAfter');
                    },
                };
                lib.skill.shanheyonggu_Discard_du_useful = {
                    aiUseful(player, card, num) {
                        if (card.name == 'du') {
                            return get.useful({ name: 'jiu' }) - 0.5;
                        }
                    },
                };

                lib.skill._shanheyonggu_doudizhu = {
                    trigger: {
                        target: 'useCardToTargeted',
                    },
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    filter(event, player, card) {
                        return event.card && event.card.name == 'sha' && _status.currentPhase != player;
                    },
                    content() {
                        'step 0';
                        var hzdy = game.countPlayer(function (current) {
                            return current.isLinked() && get.attitude(current, player) > 0;
                        });
                        var hzdr = game.countPlayer(function (current) {
                            return current.isLinked() && get.attitude(current, player) <= 0;
                        });
                        var hzcxdy = game.countPlayer(function (current) {
                            return current.isLinked() && get.attitude(current, player) > 0 && current != player && current.hp <= 1;
                        });
                        var hzcxdr = game.countPlayer(function (current) {
                            return current.isLinked() && get.attitude(current, player) <= 0 && current != player && current.hp <= 1;
                        });
                        if (trigger.player.hasSkill('yunjiwu_damage') || trigger.player.hasSkill('luoyi2') || trigger.player.hasSkill('reluoyi2') || trigger.player.hasSkill('repojun') || trigger.player.hasSkill('hanbing_skill') || trigger.player.hasSkill('xinfu_zengdao2') || trigger.player.hasSkill('xinliegong') || trigger.player.hasSkill('anjian') || trigger.player.hasSkill('reanjian') || trigger.player.hasSkill('xinanjian') || trigger.player.hasSkill('lkzhongzhuang') || trigger.player.hasSkill('decadexianzhen2') || trigger.player.hasSkill('twxuhe_damage') || trigger.player.hasSkill('twchuanshu_effect') || trigger.player.hasSkill('xinqingxi') || trigger.player.hasSkill('twqingxi') || trigger.player.hasSkill('nsshijun') || trigger.player.hasSkill('liyong2') || trigger.player.hasSkill('wangong2') || trigger.player.hasSkill('pyzhuren_diamond') || trigger.player.hasSkill('tuxing2') || trigger.player.hasSkill('spjiedao') || trigger.player.hasSkill('yunyijue') || trigger.player.hasSkill('yunwusheng') || trigger.player.hasSkill('yunposhi') || trigger.player.hasSkill('dcyiyong') || player.hasSkill('zhenlie') || player.hasSkill('bazhen') || player.hasSkill('juezhen') || player.hasSkill('yunlingren_damage') || player.hasSkill('cuijin') || player.hasSkill('shouli_thunder') || player.hasSkill('twyiju') || player.hasSkill('twzhongchi_effect') || player.hasSkill('reyanzhu2') || player.hasSkill('twqiongji') || player.hasSkill('yise_damage') || player.hasSkill('dcshizhao_effect') || player.getEquip('bagua') || player.hasSkillTag('useShan') || player.hasSkillTag('respondShan') || (trigger.card.nature == 'fire' && player.hasSkillTag('tengjia3')) || player.countMark('xionghuo') > 0 || player.countMark('dcfudao_deadmark') > 0) {
                            event.finish();
                        }
                        ('step 1');
                        if (trigger.player.countCards('hs', { name: 'sha' }) >= 1 && trigger.player.getCardUsable('sha') >= 1 && trigger.getParent(1).jiu != true && player.countCards('hs', { name: 'shan' }) == 1 && player.hp > 1) {
                            player.addTempSkill('shanheyonggu_shan_fy', 'shaAfter');
                        }
                    },
                };
                lib.skill.shanheyonggu_shan_fy = {
                    forced: true,
                    popup: false,
                    silent: true,
                    charlotte: true,
                    ai: {
                        effect: {
                            player(card, player, target) {
                                if (card.name == 'shan') {
                                    return 'zeroplayertarget';
                                }
                            },
                        },
                    },
                };

                lib.skill.zhengsu = {
                    trigger: {
                        player: 'phaseDiscardEnd',
                    },
                    forced: true,
                    charlotte: true,
                    filter(event, player) {
                        return player.storage.zhengsu_leijin || player.storage.zhengsu_bianzhen || player.storage.zhengsu_mingzhi;
                    },
                    content() {
                        player.chooseDrawRecover(2, '整肃奖励:摸两张牌或回复1点体力');
                    },
                    subSkill: {
                        leijin: {
                            mark: true,
                            trigger: {
                                player: 'useCard1',
                            },
                            lastDo: true,
                            charlotte: true,
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return player.isPhaseUsing() && player.storage.zhengsu_leijin !== false;
                            },
                            content() {
                                var list = player.getHistory('useCard', function (evt) {
                                    return evt.isPhaseUsing(player);
                                });
                                var goon = true;
                                for (var i = 0; i < list.length; i++) {
                                    var num = list[i].card.number;
                                    if (typeof num != 'number') {
                                        goon = false;
                                        break;
                                    }
                                    if (i > 0) {
                                        var num2 = list[i - 1].card.number;
                                        if (typeof num2 != 'number' || num2 >= num) {
                                            goon = false;
                                            break;
                                        }
                                    }
                                }
                                if (!goon) {
                                    game.broadcastAll(function (player) {
                                        player.storage.zhengsu_leijin = false;
                                        if (player.marks.zhengsu_leijin) player.marks.zhengsu_leijin.firstChild.innerHTML = '╳';
                                        delete player.storage.zhengsu_leijin_markcount;
                                    }, player);
                                } else {
                                    if (list.length > 2) {
                                        game.broadcastAll(
                                            function (player, num) {
                                                if (player.marks.zhengsu_leijin) player.marks.zhengsu_leijin.firstChild.innerHTML = '○';
                                                player.storage.zhengsu_leijin = true;
                                                player.storage.zhengsu_leijin_markcount = num;
                                            },
                                            player,
                                            num,
                                        );
                                    } else
                                        game.broadcastAll(
                                            function (player, num) {
                                                player.storage.zhengsu_leijin_markcount = num;
                                            },
                                            player,
                                            num,
                                        );
                                }
                                player.markSkill('zhengsu_leijin');
                            },
                            intro: {
                                content: '<li>条件:回合内所有于出牌阶段使用的牌点数递增且不少于三张',
                            },
                            mod: {
                                aiOrder(player, card, num) {
                                    if (typeof card.number != 'number') return;
                                    var history = player.getHistory('useCard', function (evt) {
                                        return evt.isPhaseUsing();
                                    });
                                    if (history.length == 0) return num + 10 * (14 - card.number);
                                    var num = history[0].card.number;
                                    if (!num) return;
                                    for (var i = 1; i < history.length; i++) {
                                        var num2 = history[i].card.number;
                                        if (!num2 || num2 <= num) return;
                                        num = num2;
                                    }
                                    if (card.number > num) return num + 10 * (14 - card.number);
                                },
                            },
                        },
                        bianzhen: {
                            mark: true,
                            trigger: {
                                player: 'useCard1',
                            },
                            firstDo: true,
                            charlotte: true,
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return player.isPhaseUsing() && player.storage.zhengsu_bianzhen !== false;
                            },
                            content() {
                                var list = player.getHistory('useCard', function (evt) {
                                    return evt.isPhaseUsing();
                                });
                                var goon = true,
                                    suit = list[0].card.suit;
                                if (suit == 'none') {
                                    goon = false;
                                } else {
                                    for (var i = 1; i < list.length; i++) {
                                        if (list[i].suit != suit) {
                                            goon = false;
                                            break;
                                        }
                                    }
                                }
                                if (!goon) {
                                    game.broadcastAll(function (player) {
                                        player.storage.zhengsu_bianzhen = false;
                                        if (player.marks.zhengsu_bianzhen) player.marks.zhengsu_bianzhen.firstChild.innerHTML = '╳';
                                    }, player);
                                } else {
                                    if (list.length > 1) {
                                        game.broadcastAll(function (player) {
                                            if (player.marks.zhengsu_bianzhen) player.marks.zhengsu_bianzhen.firstChild.innerHTML = '○';
                                            player.storage.zhengsu_bianzhen = true;
                                        }, player);
                                    } else
                                        game.broadcastAll(
                                            function (player, suit) {
                                                if (player.marks.zhengsu_bianzhen) player.marks.zhengsu_bianzhen.firstChild.innerHTML = get.translation(suit);
                                            },
                                            player,
                                            suit,
                                        );
                                }
                                player.markSkill('zhengsu_bianzhen');
                            },
                            intro: {
                                content: '<li>条件:回合内所有于出牌阶段使用的牌花色相同且不少于两张',
                            },
                            mod: {
                                aiOrder(player, card, num) {
                                    if (typeof card == 'object' && player.isPhaseUsing()) {
                                        var evt = player.getLastUsed();
                                        if (evt && evt.card && evt.card.suit && evt.card.suit == card.suit) {
                                            return num + 10;
                                        }
                                    }
                                },
                            },
                        },
                        mingzhi: {
                            mark: true,
                            trigger: {
                                player: 'loseAfter',
                            },
                            firstDo: true,
                            charlotte: true,
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                if (player.storage.zhengsu_mingzhi === false || event.type != 'discard') return false;
                                var evt = event.getParent('phaseDiscard');
                                return evt && evt.player == player;
                            },
                            content() {
                                var goon = true,
                                    list = [];
                                player.getHistory('lose', function (event) {
                                    if (!goon || event.type != 'discard') return false;
                                    var evt = event.getParent('phaseDiscard');
                                    if (evt && evt.player == player) {
                                        for (var i of event.cards2) {
                                            var suit = i.suit;
                                            if (list.includes(suit)) {
                                                goon = false;
                                                break;
                                            } else list.push(suit);
                                        }
                                    }
                                });
                                if (!goon) {
                                    game.broadcastAll(function (player) {
                                        player.storage.zhengsu_mingzhi = false;
                                        if (player.marks.zhengsu_mingzhi) player.marks.zhengsu_mingzhi.firstChild.innerHTML = '╳';
                                        delete player.storage.zhengsu_mingzhi_list;
                                    }, player);
                                } else {
                                    if (list.length > 1) {
                                        game.broadcastAll(
                                            function (player, list) {
                                                if (player.marks.zhengsu_mingzhi) player.marks.zhengsu_mingzhi.firstChild.innerHTML = '○';
                                                player.storage.zhengsu_mingzhi = true;
                                                player.storage.zhengsu_mingzhi_list = list;
                                                player.storage.zhengsu_mingzhi_markcount = list.length;
                                            },
                                            player,
                                            list,
                                        );
                                    } else
                                        game.broadcastAll(
                                            function (player, list) {
                                                player.storage.zhengsu_mingzhi_list = list;
                                                player.storage.zhengsu_mingzhi_markcount = list.length;
                                            },
                                            player,
                                            list,
                                        );
                                }
                                player.markSkill('zhengsu_mingzhi');
                            },
                            intro: {
                                content: '<li>条件:回合内所有于弃牌阶段弃置的牌花色均不相同且不少于两张',
                            },
                        },
                    },
                };

                lib.skill.feiyang = {
                    trigger: {
                        player: 'phaseJudgeBegin',
                    },
                    charlotte: true,
                    forced: true,
                    filter(event, player) {
                        return _status.mode != 'online' && _status.mode != 'binglin' && player == game.zhu && player.countCards('j') && player.countCards('h') > 1;
                    },
                    content() {
                        'step 0';
                        player.chooseToDiscard('h', 2, '是否发动【飞扬】,弃置两张手牌并弃置自己判定区的一张牌？').ai = function (card) {
                            if (player.countCards('j') <= 1 && (player.hasSkillTag('rejudge') || player.hasSkillTag('nodamage') || player.hasSkillTag('nothunder')) && (player.hasJudge('shandian') || player.hasJudge('fulei'))) return false;
                            return 6 - get.value(card);
                        };
                        ('step 1');
                        if (result.bool) {
                            player.discardPlayerCard(player, 'j', true).ai = function (button) {
                                if (player.countCards('h') < 2 && (!player.hasJudge('shandian') || !player.hasJudge('fulei'))) {
                                    return -get.value(button.link);
                                }
                                return get.value(button.link);
                            };
                        }
                    },
                };

                lib.skill.dbzhuifeng = {
                    audio: 2,
                    groupSkill: true,
                    enable: 'phaseUse',
                    usable: 2,
                    viewAsFilter(player) {
                        return player.group == 'wei' && player.hp > 0;
                    },
                    viewAs: { name: 'juedou' },
                    filterCard: () => false,
                    selectCard: -1,
                    log: false,
                    precontent() {
                        player.loseHp();
                    },
                    group: 'dbzhuifeng_self',
                    subSkill: {
                        self: {
                            trigger: { player: 'damageBegin2' },
                            forced: true,
                            filter(event, player) {
                                var evt = event.parent;
                                return evt.skill == 'dbzhuifeng' && evt.player == player;
                            },
                            content() {
                                trigger.cancel();
                                player.getStat().skill.dbzhuifeng = 2;
                            },
                        },
                    },
                    ai: {
                        order() {
                            return get.order({ name: 'juedou' }) - 0.5;
                        },
                        result: {
                            player(player, target) {
                                if (player.hp == 1) return -999;
                            },
                        },
                    },
                };

                lib.skill.olxiuhao = {
                    audio: 2,
                    trigger: {
                        player: 'damageBegin4',
                        source: 'damageBegin2',
                    },
                    usable: 1,
                    filter(event, player) {
                        return event.source && event.source.isIn() && event.source != event.player;
                    },
                    logTarget(event, player) {
                        return player == event.player ? event.source : event.player;
                    },
                    check(event, player) {
                        _status.olxiuhao_judging = true;
                        var bool = false;
                        if (player == event.source) {
                            if (get.attitude(player, event.player) > 0) bool = true;
                            if (get.damageEffect(event.player, player, player, event.nature) <= 0) bool = true;
                        } else {
                            if (get.attitude(player, event.source) > 0) bool = true;
                            if (get.damageEffect(player, event.source, player, event.nature) < 0) {
                                if (event.source.hasSkillTag('nogain')) bool = true;
                                if (event.num >= player.hp + player.countCards('hs', { name: ['tao', 'jiu'] }) && (!player.hasFriend() || player == get.zhu(player))) bool = true;
                            }
                        }
                        delete _status.olxiuhao_judging;
                        return bool;
                    },
                    content() {
                        trigger.cancel();
                        trigger.source.draw(2);
                    },
                    ai: {
                        effect: {
                            target(card, player, target) {
                                if (target != player && !_status.olxiuhao_judging && get.tag(card, 'damage') && get.attitude(target, player) > 0 && !target.getStat('triggerSkill').olxiuhao) return [0, 0.5, 0, 0.5];
                            },
                            player(card, player, target) {
                                if (target != player && !_status.olxiuhao_judging && get.tag(card, 'damage') && get.attitude(player, target) > 0 && !player.getStat('triggerSkill').olxiuhao) return [0, 0.5, 0, 0.5];
                            },
                        },
                    },
                };

                lib.skill.xinfu_zengdao = {
                    audio: 2,
                    init(player) {
                        player.storage.xinfu_zengdao = false;
                    },
                    limited: true,
                    enable: 'phaseUse',
                    usable: 1,
                    filterTarget(card, player, target) {
                        return player != target;
                    },
                    filter(event, player) {
                        if (player.storage.xinfu_zengdao == true) return false;
                        return player.countCards('e') > 0;
                    },
                    position: 'e',
                    filterCard: true,
                    selectCard: [1, Infinity],
                    discard: false,
                    lose: true,
                    content() {
                        player.awakenSkill('xinfu_zengdao');
                        player.$give(cards, target, false);
                        target.storage.xinfu_zengdao2 = cards.slice(0);
                        target.addSkill('xinfu_zengdao2');
                    },
                    ai: {
                        order: 1,
                        result: {
                            target(card, player, target) {
                                if (player.countCards('e') >= 2) {
                                    return 1;
                                }
                            },
                        },
                    },
                    mark: true,
                    intro: {
                        content: 'limited',
                    },
                };

                lib.skill.recuorui = {
                    audio: 'cuorui',
                    enable: 'phaseUse',
                    limited: true,
                    filter(event, player) {
                        return (
                            player.hp > 0 &&
                            game.hasPlayer(function (current) {
                                return current != player && current.countGainableCards(player, 'h') > 0;
                            })
                        );
                    },
                    filterTarget(card, player, target) {
                        return target != player && target.countGainableCards(player, 'h') > 0;
                    },
                    selectTarget() {
                        return [1, _status.event.player.hp];
                    },
                    content() {
                        if (num == 0) player.awakenSkill('recuorui');
                        player.gainPlayerCard(target, true, 'h');
                    },
                    mark: true,
                    intro: {
                        content: 'limited',
                    },
                    init(player, skill) {
                        player.storage[skill] = false;
                    },
                    ai: {
                        order: 9,
                        result: {
                            player: 1,
                            target: -1,
                        },
                    },
                };

                lib.skill.yinju = {
                    audio: 2,
                    enable: 'phaseUse',
                    limited: true,
                    filterTarget(card, player, target) {
                        return player != target;
                    },
                    content() {
                        player.awakenSkill('yinju');
                        player.storage.yinju2 = target;
                        player.addTempSkill('yinju2');
                    },
                    mark: true,
                    intro: {
                        content: 'limited',
                    },
                    init(player, skill) {
                        player.storage[skill] = false;
                    },
                    ai: {
                        order: 10,
                        result: {
                            target(card, player, target) {
                                var nm = player.getCards('hs', 'nanman');
                                var wj = player.getCards('hs', 'wanjian');
                                if (nm + wj >= 2) {
                                    return 1;
                                }
                            },
                        },
                    },
                };
                lib.skill.yinju2 = {
                    trigger: {
                        player: 'useCardToPlayered',
                        source: 'damageBefore',
                    },
                    forced: true,
                    filter(event, player, name) {
                        if (name == 'useCardToPlayered') return event.target == player.storage.yinju2;
                        return event.player == player.storage.yinju2;
                    },
                    logTarget(event) {
                        return event[event.name == 'damage' ? 'player' : 'target'];
                    },
                    content() {
                        'step 0';
                        if (trigger.name == 'damage') {
                            trigger.cancel();
                            trigger.player.recover(trigger.num);
                            event.finish();
                        } else {
                            game.asyncDraw([player, trigger.target]);
                        }
                        ('step 1');
                    },
                    ai: {
                        effect: {
                            player(card, player, target) {
                                if (card.name == 'nanman' || card.name == 'wanjian') {
                                    return [1, 1];
                                }
                            },
                        },
                    },
                };

                lib.skill.youlong = {
                    enable: 'chooseToUse',
                    audio: 2,
                    audioname: ['key_sakuya'],
                    zhuanhuanji: true,
                    marktext: '☯',
                    mark: true,
                    intro: {
                        content(storage, player) {
                            return storage ? '每轮限一次,你可以废除你的一个装备栏,视为使用一张未以此法使用过的基本牌.' : '每轮限一次,你可以废除你的一个装备栏,视为使用一张未以此法使用过的普通锦囊牌.';
                        },
                    },
                    init(player) {
                        player.storage.youlong = false;
                        if (!player.storage.youlong2) player.storage.youlong2 = [];
                    },
                    hiddenCard(player, name) {
                        if (player.storage.youlong2.includes(name) || player.countDisabled() >= 5) return false;
                        if (player.hasSkill('youlong_' + (player.storage.youlong || false))) return false;
                        var type = get.type(name);
                        if (player.storage.youlong) return type == 'basic';
                        return type == 'trick';
                    },
                    filter(event, player) {
                        if (player.storage.youlong2.includes(name) || player.countDisabled() >= 5) return false;
                        if (player.hasSkill('youlong_' + (player.storage.youlong || false))) return false;
                        var type = player.storage.youlong ? 'basic' : 'trick';
                        for (var name of lib.inpile) {
                            if (player.storage.youlong2.includes(name)) continue;
                            if (get.type(name) != type) continue;
                            if (event.filterCard && event.filterCard({ name: name }, player, event)) return true;
                        }
                        return false;
                    },
                    chooseButton: {
                        dialog(event, player) {
                            var dialog = ui.create.dialog('游龙', 'hidden');
                            var table = document.createElement('div');
                            table.classList.add('add-setting');
                            table.style.margin = '0';
                            table.style.width = '100%';
                            table.style.position = 'relative';
                            for (var i = 1; i < 6; i++) {
                                if (player.isDisabled(i)) continue;
                                var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
                                td.innerHTML = '<span>' + get.translation('equip' + i) + '</span>';
                                td.link = i;
                                td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
                                Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
                                table.appendChild(td);
                                dialog.buttons.add(td);
                            }
                            dialog.content.appendChild(table);
                            var type = player.storage.youlong ? 'basic' : 'trick';
                            var list = [];
                            for (var name of lib.inpile) {
                                if (player.storage.youlong2.includes(name)) continue;
                                if (get.type(name) != type) continue;
                                if (event.filterCard && event.filterCard({ name: name }, player, event)) {
                                    list.push([type, '', name]);
                                    if (name == 'sha') {
                                        for (var j of lib.inpile_nature) list.push(['基本', '', 'sha', j]);
                                    }
                                }
                            }
                            dialog.add([list, 'vcard']);
                            return dialog;
                        },
                        filter(button) {
                            if (ui.selected.buttons.length && typeof button.link == typeof ui.selected.buttons[0].link) return false;
                            return true;
                        },
                        select: 2,
                        check(button) {
                            var player = _status.event.player;
                            if (typeof button.link == 'number') {
                                var card = player.getEquip(button.link);
                                if (card) {
                                    var val = get.value(card);
                                    if (val > 0) return 0;
                                    return 5 - val;
                                }
                                switch (button.link) {
                                    case 3:
                                        return 4.5;
                                        break;
                                    case 4:
                                        return 4.4;
                                        break;
                                    case 5:
                                        return 4.3;
                                        break;
                                    case 2:
                                        return (3 - player.hp) * 1.5;
                                        break;
                                    case 1: {
                                        if (
                                            game.hasPlayer(function (current) {
                                                return (get.realAttitude || get.attitude)(player, current) < 0 && get.distance(player, current) > 1;
                                            })
                                        )
                                            return 0;
                                        return 3.2;
                                    }
                                }
                            }
                            var name = button.link[2];
                            var evt = _status.event.parent;
                            if (get.type(name) == 'basic') {
                                if (name == 'shan') return 2;
                                if (evt.type == 'dying') {
                                    if (get.attitude(player, evt.dying) < 2) return false;
                                    if (player.hp < 1 && name == 'jiu') return 2.1;
                                    return 1.9;
                                }
                                if (evt.type == 'phase') return player.getUseValue({ name: name, nature: button.link[3] });
                                return 1;
                            }
                            if (!['chuqibuyi', 'shuiyanqijunx', 'juedou', 'nanman', 'wanjian', 'shunshou', 'zhujinqiyuan'].includes(name)) return 0;
                            var card = { name: name };
                            if (['shunshou', 'zhujinqiyuan'].includes(card.name)) {
                                if (
                                    !game.hasPlayer(function (current) {
                                        return get.attitude(player, current) != 0 && get.distance(player, current) <= 1 && player.canUse(card, current) && get.effect(current, card, player, player) > 0;
                                    })
                                )
                                    return 0;
                                return player.getUseValue(card) - 7;
                            }
                            return player.getUseValue(card) - 4;
                        },
                        backup(links, player) {
                            if (typeof links[1] == 'number') links.reverse();
                            var equip = links[0];
                            var name = links[1][2];
                            var nature = links[1][3];
                            return {
                                filterCard() {
                                    return false;
                                },
                                selectCard: -1,
                                equip: equip,
                                viewAs: {
                                    name: name,
                                    nature: nature,
                                },
                                popname: true,
                                precontent() {
                                    player.disableEquip(lib.skill.youlong_backup.equip);
                                    player.addTempSkill('youlong_' + (player.storage.youlong || false), 'roundStart');
                                    player.changeZhuanhuanji('youlong');
                                    player.storage.youlong2.add(event.result.card.name);
                                },
                            };
                        },
                        prompt(links, player) {
                            if (typeof links[1] == 'number') links.reverse();
                            var equip = 'equip' + links[0];
                            var name = links[1][2];
                            var nature = links[1][3];
                            return '废除自己的' + get.translation(equip) + '栏,视为使用' + (get.translation(nature) || '') + get.translation(name);
                        },
                    },
                    ai: {
                        respondSha: true,
                        respondShan: true,
                        skillTagFilter(player, tag, arg) {
                            if (arg == 'respond') return false;
                            if (!player.storage.youlong || player.hasSkill('youlong_true')) return false;
                            var name = tag == 'respondSha' ? 'sha' : 'shan';
                            return !player.storage.youlong2.includes(name);
                        },
                        order: 1,
                        result: {
                            player: 1,
                        },
                    },
                };

                lib.skill.zhafu = {
                    audio: 2,
                    enable: 'phaseUse',
                    limited: true,
                    filterTarget(card, player, target) {
                        return player != target;
                    },
                    content() {
                        player.awakenSkill('zhafu');
                        target.addSkill('zhafu_hf');
                        target.storage.zhafu_hf = player;
                    },
                    ai: {
                        order: 1,
                        result: {
                            target(player, target) {
                                if (get.attitude(player, target) < 0) {
                                    var num = target.countCards('h');
                                    return -num;
                                    if (target.hasJudge('lebu')) return -num * 2;
                                }
                                return 0;
                            },
                        },
                    },
                    subSkill: {
                        hf: {
                            trigger: {
                                player: 'phaseDiscardBegin',
                            },
                            forced: true,
                            popup: false,
                            charlotte: true,
                            content() {
                                'step 0';
                                if (player.countCards('h') <= 1 || player.storage.zhafu_hf.isDead()) event.finish();
                                ('step 1');
                                player.chooseCard('h', true, '选择保留一张手牌,将其余的手牌交给' + get.translation(player.storage.zhafu_hf)).ai = get.value;
                                ('step 2');
                                var cards = player.getCards('h');
                                cards.remove(result.cards[0]);
                                player.storage.zhafu_hf.gain(cards, player, 'giveAuto');
                                ('step 3');
                                player.removeSkill('zhafu_hf');
                            },
                        },
                    },
                    mark: true,
                    intro: {
                        content: 'limited',
                    },
                    init(player, skill) {
                        player.storage[skill] = false;
                    },
                };

                lib.skill.wulie = {
                    trigger: {
                        player: 'phaseJieshuBegin',
                    },
                    audio: 2,
                    forced: true,
                    limited: true,
                    filter(event, player) {
                        return player.hp > 0;
                    },
                    content() {
                        'step 0';
                        player.chooseTarget([1, player.hp], get.prompt2('wulie'), lib.filter.notMe).set('ai', function (target) {
                            if (player.hp <= 3) return false;
                            if (player.hp >= 4) {
                                if (get.mode() == 'identity') {
                                    if (player.identity == 'zhu') {
                                        if (player.hp <= 4) {
                                            return false;
                                        }
                                        if (player.hp >= 5) {
                                            return get.attitude(player, target);
                                        }
                                    }
                                }
                                if (player.hp >= 4) {
                                    return get.attitude(player, target);
                                }
                            } else {
                                return false;
                            }
                        });
                        ('step 1');
                        if (result.bool) {
                            var targets = result.targets.sortBySeat();
                            player.awakenSkill('wulie');
                            player.loseHp(targets.length);
                            while (targets.length) {
                                targets[0].addSkill('wulie2');
                                targets.shift().addMark('wulie2');
                            }
                        }
                    },
                    mark: true,
                    intro: {
                        content: 'limited',
                    },
                    init(player, skill) {
                        player.storage[skill] = false;
                    },
                };

                lib.skill.jiang = {
                    shaRelated: true,
                    audio: 2,
                    preHidden: true,
                    audioname: ['sp_lvmeng', 're_sunben', 're_sunce'],
                    trigger: {
                        player: 'useCardToPlayered',
                        target: 'useCardToTargeted',
                    },
                    filter(event, player) {
                        if (!(event.card.name == 'juedou' || (event.card.name == 'sha' && get.color(event.card) == 'red'))) return false;
                        return player == event.target || event.parent.triggeredTargets3.length == 1;
                    },
                    forced: true,
                    content() {
                        player.draw();
                    },
                    ai: {
                        effect: {
                            target(card, player, target) {
                                if (card.name == 'sha' && get.color(card) == 'red') return [1, 0.6];
                            },
                            player(card, player, target) {
                                if (card.name == 'sha' && get.color(card) == 'red') return [1, 0.5];
                            },
                        },
                    },
                };

                lib.skill.yinghun = {
                    audio: 2,
                    audioname: ['re_sunjian', 'sunce', 're_sunben', 're_sunce', 'ol_sunjian', 're_sunyi'],
                    trigger: {
                        player: 'phaseZhunbeiBegin',
                    },
                    forced: true,
                    preHidden: true,
                    content() {
                        'step 0';
                        player
                            .chooseTarget(get.prompt2('yinghun'), function (card, player, target) {
                                return player != target;
                            })
                            .set('ai', function (target) {
                                var player = _status.event.player;
                                if (player.getDamagedHp() == 1 && target.countCards('he') == 0) {
                                    return 0;
                                }
                                if (get.attitude(_status.event.player, target) > 0) {
                                    return 10 + get.attitude(_status.event.player, target);
                                }
                                if (player.getDamagedHp() == 1) {
                                    return -1;
                                }
                                return 1;
                            })
                            .setHiddenSkill(event.name);
                        ('step 1');
                        if (result.bool) {
                            event.num = player.getDamagedHp();
                            event.target = result.targets[0];
                            if (event.num == 1) {
                                event.directcontrol = true;
                            } else {
                                var str1 = '摸' + get.cnNumber(event.num, true) + '弃一';
                                var str2 = '摸一弃' + get.cnNumber(event.num, true);
                                player
                                    .chooseControl(str1, str2, function (event, player) {
                                        if (player.isHealthy()) return 1 - _status.event.choice;
                                        return _status.event.choice;
                                    })
                                    .set('choice', get.attitude(player, event.target) > 0 ? 0 : 1);
                                event.str = str1;
                            }
                        } else {
                            event.finish();
                        }
                        ('step 2');
                        if (event.directcontrol || result.control == event.str) {
                            if (event.num > 0) event.target.draw(event.num);
                            event.target.chooseToDiscard(true, 'he');
                        } else {
                            event.target.draw();
                            if (event.num > 0) event.target.chooseToDiscard(event.num, true, 'he');
                        }
                    },
                    ai: {
                        effect: {
                            target(card, player, target) {
                                if (get.tag(card, 'damage') && player.hp == player.maxHp) return 1.5;
                                if (get.tag(card, 'recover') && player.hp >= player.maxHp - 1) return 'zeroplayertarget';
                            },
                        },
                    },
                };
                lib.skill.gzyinghun = {
                    audio: 'yinghun',
                    audioname: ['re_sunjian', 'sunce', 're_sunben', 're_sunce', 'ol_sunjian', 're_sunyi'],
                    trigger: {
                        player: 'phaseZhunbeiBegin',
                    },
                    filter(event, player) {
                        return player.getDamagedHp() > 0;
                    },
                    forced: true,
                    preHidden: true,
                    content() {
                        'step 0';
                        player
                            .chooseTarget(get.prompt2('gzyinghun'), function (card, player, target) {
                                return player != target;
                            })
                            .set('ai', function (target) {
                                var player = _status.event.player;
                                if (player.getDamagedHp() == 1 && target.countCards('he') == 0) {
                                    return 0;
                                }
                                if (get.attitude(_status.event.player, target) > 0) {
                                    return 10 + get.attitude(_status.event.player, target);
                                }
                                if (player.getDamagedHp() == 1) {
                                    return -1;
                                }
                                return 1;
                            })
                            .setHiddenSkill(event.name);
                        ('step 1');
                        if (result.bool) {
                            event.num = player.getDamagedHp();
                            event.target = result.targets[0];
                            if (event.num == 1) {
                                event.directcontrol = true;
                            } else {
                                var str1 = '摸' + get.cnNumber(event.num, true) + '弃一';
                                var str2 = '摸一弃' + get.cnNumber(event.num, true);
                                player
                                    .chooseControl(str1, str2, function (event, player) {
                                        return _status.event.choice;
                                    })
                                    .set('choice', get.attitude(player, event.target) > 0 ? str1 : str2);
                                event.str = str1;
                            }
                        } else {
                            event.finish();
                        }
                        ('step 2');
                        if (event.directcontrol || result.control == event.str) {
                            event.target.draw(event.num);
                            event.target.chooseToDiscard(true, 'he');
                        } else {
                            event.target.draw();
                            event.target.chooseToDiscard(event.num, true, 'he');
                        }
                    },
                    ai: {
                        effect: {
                            target(card, player, target) {
                                if (get.tag(card, 'damage') && player.hp == player.maxHp) return 1.5;
                                if (get.tag(card, 'recover') && player.hp >= player.maxHp - 1) return 'zeroplayertarget';
                            },
                        },
                        threaten(player, target) {
                            if (target.hp == target.maxHp) return 0.5;
                            if (target.hp == 1) return 2;
                            if (target.hp == 2) return 1.5;
                            return 0.5;
                        },
                        maixie: true,
                    },
                };

                lib.skill.xinzhaofu = {
                    mark: false,
                    init(player) {
                        if (player.hasZhuSkill('xinzhaofu')) {
                            player.markSkill('xinzhaofu');
                            player.storage.xinzhaofu = false;
                        }
                    },
                    audio: 'zhaofu',
                    enable: 'phaseUse',
                    usable: 1,
                    filter(event, player) {
                        return player.hasZhuSkill('xinzhaofu');
                    },
                    limited: true,
                    selectTarget: [1, 2],
                    filterTarget(card, player, target) {
                        return player != target;
                    },
                    contentBefore() {
                        player.awakenSkill('xinzhaofu');
                    },
                    content() {
                        target.addSkill('xinzhaofu_effect');
                        target.markAuto('xinzhaofu_effect', [player]);
                    },
                    ai: {
                        order: 6,
                        result: {
                            target: -1,
                        },
                    },
                    subSkill: {
                        effect: {
                            charlotte: true,
                            mark: true,
                            intro: {
                                content: '已视为在其他吴势力角色的攻击范围内',
                            },
                            mod: {
                                inRangeOf(from, to) {
                                    if (from.group != 'wu') return;
                                    var list = to.getStorage('xinzhaofu_effect');
                                    for (var i of list) {
                                        if (i != from) return true;
                                    }
                                },
                            },
                        },
                    },
                    intro: {
                        content: 'limited',
                    },
                };

                lib.skill.drlt_xiongluan = {
                    audio: 2,
                    enable: 'phaseUse',
                    mark: true,
                    limited: true,
                    init(player) {
                        player.storage.drlt_xiongluan = false;
                    },
                    filter(event, player) {
                        if (player.storage.drlt_xiongluan) return false;
                        return true;
                    },
                    filterTarget(card, player, target) {
                        return target != player;
                    },
                    content() {
                        player.awakenSkill('drlt_xiongluan');
                        player.storage.drlt_xiongluan = true;
                        player.disableEquip('equip1');
                        player.disableEquip('equip2');
                        player.disableEquip('equip3');
                        player.disableEquip('equip4');
                        player.disableEquip('equip5');
                        player.disableJudge();
                        player.addTempSkill('drlt_xiongluan_effect');
                        player.storage.drlt_xiongluan_effect = [target];
                        target.addSkill('drlt_xiongluan_ban');
                        target.markSkillCharacter('drlt_xiongluan_effect', player, '雄乱', '无法使用或打出任何手牌');
                    },
                    ai: {
                        order: 13,
                        result: {
                            target(player, target) {
                                if (target.getEquip('bagua') || target.getEquip('rewrite_bagua')) return 0;
                                var hs = player.countCards('h', function (card) {
                                    return ['sha', 'juedou'].includes(card.name) && get.effect(target, card, player, player) != 0;
                                });
                                var ts = target.hp;
                                if (hs >= ts && ts > 1) return -1;
                                return 0;
                            },
                        },
                    },
                    intro: {
                        content: 'limited',
                    },
                };
                lib.skill.drlt_xiongluan_effect = {
                    onremove(player) {
                        player.storage.drlt_xiongluan_effect.removeSkill('drlt_xiongluan_ban');
                        player.storage.drlt_xiongluan_effect.unmarkSkill('drlt_xiongluan_effect');
                        delete player.storage.drlt_xiongluan_effect;
                    },
                    mod: {
                        targetInRange(card, player, target) {
                            if (target.hasSkill('drlt_xiongluan_ban')) {
                                return true;
                            }
                        },
                        cardUsableTarget(card, player, target) {
                            if (target.hasSkill('drlt_xiongluan_ban')) return true;
                        },
                    },
                    charlotte: true,
                    ai: {
                        effect: {
                            player(card, player, target) {
                                if (get.tag(card, 'damage') && target.hasSkill('drlt_xiongluan_ban')) return [99, 99];
                            },
                        },
                    },
                };
                lib.skill.drlt_xiongluan_ban = {
                    mod: {
                        cardEnabled2(card, player) {
                            if (get.position(card) == 'h') return false;
                        },
                    },
                    charlotte: true,
                };

                lib.skill.xinfu_tunjun = {
                    limited: true,
                    enable: 'phaseUse',
                    audio: 2,
                    filter(event, player) {
                        if (player.storage.xinfu_tunjun) return false;
                        return player.storage.xinfu_lveming && player.storage.xinfu_lveming > 0;
                    },
                    filterTarget: true,
                    selectTarget: 1,
                    content() {
                        'step 0';
                        player.awakenSkill('xinfu_tunjun');
                        event.num = player.storage.xinfu_lveming;
                        event.toequip = [];
                        ('step 1');
                        var equip = get.cardPile(function (card) {
                            var bool1 = true;
                            for (var i = 0; i < event.toequip.length; i++) {
                                if (get.type(card) == 'equip' && get.subtype(card) == get.subtype(event.toequip[i])) bool1 = false;
                            }
                            return get.type(card) == 'equip' && !event.toequip.includes(card) && target.isEmpty(get.subtype(card)) && bool1;
                        });
                        if (equip) event.toequip.push(equip);
                        else event.num = 0;
                        event.num--;
                        ('step 2');
                        if (event.num > 0) event.goto(1);
                        ('step 3');
                        for (var i = 0; i < event.toequip.length; i++) {
                            target.chooseUseTarget(event.toequip[i], true).set('animate', false).set('nopopup', true);
                        }
                    },
                    ai: {
                        order: 1,
                        result: {
                            target(card, player, target) {
                                if (player.storage.xinfu_lveming >= 2) {
                                    return 1;
                                }
                            },
                        },
                    },
                    mark: true,
                    intro: {
                        content: 'limited',
                    },
                    init(player) {
                        player.storage.xinfu_tunjun = false;
                    },
                };

                lib.skill.xinfu_kaikang = {
                    audio: 2,
                    trigger: {
                        global: 'useCardToTargeted',
                    },
                    filter(event, player) {
                        return event.card.name == 'sha' && get.distance(player, event.target) <= 1 && event.target.isIn();
                    },
                    check(event, player) {
                        return get.attitude(player, event.target) > 0;
                    },
                    logTarget: 'target',
                    content() {
                        'step 0';
                        player.draw();
                        if (trigger.target != player) {
                            player.chooseCard(true, 'he', '交给' + get.translation(trigger.target) + '一张牌').set('ai', function (card) {
                                if (get.position(card) == 'e') return -1;
                                if (card.name == 'shan') return 1;
                                if (get.type(card) == 'equip') return 0.5;
                                return 0;
                            });
                        } else {
                            event.finish();
                        }
                        ('step 1');
                        trigger.target.gain(result.cards, player, 'give');
                        event.card = result.cards[0];
                        ('step 2');
                        if (trigger.target.getCards('h').includes(card) && get.type(card) == 'equip') {
                            trigger.target.chooseUseTarget(card);
                        }
                    },
                    ai: {
                        threaten: 1.1,
                    },
                };

                lib.skill.xinfu_limu = {
                    mod: {
                        targetInRange(card, player, target) {
                            if (player.countCards('j') && player.inRange(target)) {
                                return true;
                            }
                        },
                        cardUsableTarget(card, player, target) {
                            if (player.countCards('j') && player.inRange(target)) return true;
                        },
                        aiValue(player, card, num) {
                            if (card.name == 'zhangba') return 30;
                            if (player.getEquip('zhangba') && player.countCards('hs') > 1 && ['shan', 'tao', 'jiu'].includes(card.name)) return 0;
                            if (card.name == 'shan' || card.name == 'tao' || card.name == 'jiu') return num / 3;
                        },
                    },
                    audio: 2,
                    enable: 'phaseUse',
                    discard: false,
                    filter(event, player) {
                        if (player.hasJudge('lebu')) return false;
                        return player.countCards('hes', { suit: 'diamond' }) > 0;
                    },
                    viewAs: {
                        name: 'lebu',
                    },
                    position: 'hes',
                    filterCard(card, player, event) {
                        return card.suit == 'diamond' && player.canAddJudge({ name: 'lebu', cards: [card] });
                    },
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return player == target;
                    },
                    check(card) {
                        var player = _status.event.player;
                        if (!player.getEquip('zhangba') && player.countCards('hs', 'sha') < 2) {
                            if (
                                player.countCards('h', function (cardx) {
                                    return cardx != card && cardx.name == 'shan';
                                }) > 0
                            )
                                return 0;
                            var damaged = player.maxHp - player.hp - 1;
                            var ts = player.countCards('h', function (cardx) {
                                return cardx != card && cardx.name == 'tao';
                            });
                            if (ts > 0 && ts > damaged) return 0;
                        }
                        if (card.name == 'shan') return 15;
                        if (card.name == 'tao') return 10;
                        return 9 - get.value(card);
                    },
                    onuse(links, player) {
                        var next = game.createEvent('limu_recover', false, _status.event.parent);
                        next.player = player;
                        next.setContent(function () {
                            player.recover();
                        });
                    },
                    ai: {
                        result: {
                            target: 1,
                            ignoreStatus: true,
                        },
                        order(item, player) {
                            if (
                                player.countCards('hs', 'zhangba') ||
                                (player.countCards('h', function (card) {
                                    return card.suit == 'diamond' && get.type(card) == 'basic';
                                }) == 1 &&
                                    player.countCards('h', function (card) {
                                        return card.name != 'sha' && get.type(card) == 'basic';
                                    }) == 1 &&
                                    player.countCards('h', { type: 'trick' }) > 0)
                            ) {
                                return get.order({ name: 'sha' }) + 1;
                            }
                            if (player.getDamagedHp() >= 2) return 5;
                            if (
                                game.hasPlayer(function (current) {
                                    return current != player && player.inRange(current) && get.attitude(player, current) <= 0;
                                })
                            ) {
                                if (player.countCards('hs', 'sha') > 1) return 3;
                                if (player.countCards('h', 'sha') > 0 && player.countCards('h', 'tao') == 0 && player.countCards('h', 'shan') == 0 && player.countCards('h', 'jiu') == 0) return 3;
                                if (
                                    player.countCards('h', function (card) {
                                        return card.suit == 'diamond' && get.type(card) == 'basic';
                                    }) == 1 &&
                                    player.countCards('h', function (card) {
                                        return card.name != 'sha' && get.type(card) == 'basic';
                                    }) == 1 &&
                                    (player.countCards('h', 'taoyuan') > 0 || player.countCards('h', 'wugu') > 0 || player.countCards('h', 'tiesuo') > 0 || player.countCards('h', 'nanman') > 0 || player.countCards('h', 'wanjian') > 0)
                                )
                                    return 12;
                            }
                            if (player.getEquip('zhangba')) return 15;
                            return 0;
                        },
                        effect: {
                            player(card, player, target) {
                                if (card.name == 'zhangba' && player.hasSkill('xinfu_tushe')) {
                                    return [3.5, 3.5];
                                }
                                if (player.countCards('h', 'jiu') + player.countCards('h', 'tao') == 1 && player.countCards('h', { type: 'basic' }) == 1 && player.hasSkill('xinfu_tushe')) {
                                    if (card.name == 'tao') {
                                        return [2.5, 2.5];
                                    }
                                    if (card.name == 'jiu') {
                                        return [2.5, 2.5];
                                    }
                                }
                                if (player.getEquip('zhangba') && player.hasSkill('xinfu_tushe')) {
                                    if (get.attitude(player, target) <= 0 && card.name == 'sha') {
                                        return [2.5, 2.5];
                                    }
                                    if (card.name == 'jiu') {
                                        return [2.5, 2.5];
                                    }
                                    if (!player.countCards('h', { type: 'basic' })) {
                                        if (get.type2(card) == 'trick') {
                                            return [3, 3];
                                        }
                                    }
                                }
                                if (!player.countCards('h', { type: 'basic' }) && player.countCards('j') && player.hasSkill('xinfu_tushe')) {
                                    if (card.name == 'wugu' || card.name == 'taoyuan' || card.name == 'tiesuo') {
                                        return [1, 1.5];
                                    }
                                }
                            },
                        },
                        basic: {
                            order: 1,
                            useful: 1,
                            value: 8,
                        },
                        tag: {
                            skip: 'phaseUse',
                        },
                    },
                };

                lib.skill.pytianjiang_move = {
                    audio: 'pytianjiang',
                    prompt: '将装备区里的一张牌移动至其他角色的装备区',
                    enable: 'phaseUse',
                    position: 'e',
                    filter(event, player) {
                        return player.countCards('e') > 0;
                    },
                    check() {
                        return 1;
                    },
                    filterCard: true,
                    filterTarget(event, player, target) {
                        return target != player && target.canEquip(ui.selected.cards[0], true);
                    },
                    prepare: 'give',
                    discard: false,
                    lose: false,
                    content() {
                        target.equip(cards[0]);
                        if (cards[0].name.indexOf('pyzhuren_') == 0) player.draw(2);
                    },
                    ai: {
                        order(item, player) {
                            return get.order({ name: 'sha' }) - 0.1;
                        },
                        expose: 0.2,
                        result: {
                            target(player, target) {
                                if (ui.selected.cards.length) {
                                    var card = ui.selected.cards[0];
                                    if (target.getEquip(card) || target.countCards('h', { subtype: get.subtype(card) })) return 1;
                                    return get.effect(target, card, player, target);
                                }
                                return 0;
                            },
                        },
                    },
                };

                lib.skill.xinfu_jianjie = {
                    derivation: ['jianjie_faq'],
                    group: ['xinfu_jianjie1', 'xinfu_jianjie2'],
                    audio: 3,
                    trigger: {
                        player: 'phaseZhunbeiBegin',
                    },
                    forced: true,
                    filter(event, player) {
                        if (player.phaseNumber > 1) return false;
                        return !game.hasPlayer(function (current) {
                            return current.hasSkill('smh_huoji') || current.hasSkill('smh_lianhuan');
                        });
                    },
                    content() {
                        'step 0';
                        player
                            .chooseTarget('请将「龙印」交给一名角色', true, function (card, player, target) {
                                return target != player;
                            })
                            .set('ai', function (target) {
                                var player = _status.event.player;
                                return get.attitude(player, target);
                            });
                        ('step 1');
                        if (result.bool && result.targets && result.targets.length) {
                            var target = result.targets[0];
                            player.line(target, 'fire');
                            target.addSkill('smh_huoji');
                        }
                        if (
                            game.hasPlayer(function (current) {
                                return !current.hasSkill('smh_huoji') && current != player;
                            })
                        ) {
                            player
                                .chooseTarget('请将「凤印」交给一名角色', true, function (card, player, target) {
                                    return target != player && !target.hasSkill('smh_huoji');
                                })
                                .set('ai', function (target) {
                                    var player = _status.event.player;
                                    return get.attitude(player, target);
                                });
                        } else event.finish();
                        ('step 2');
                        if (result.bool && result.targets && result.targets.length) {
                            var target = result.targets[0];
                            player.line(target, 'green');
                            target.addSkill('smh_lianhuan');
                        }
                    },
                };
                lib.skill.xinfu_jianjie1 = {
                    audio: 3,
                    prompt: '你的第一个准备阶段,你令两名不同的角色分别获得龙印与凤印;出牌阶段限一次(你的第一个回合除外),或当拥有龙印、凤印的角色死亡时,你可以转移龙印、凤印',
                    enable: 'phaseUse',
                    usable: 1,
                    filter(event, player) {
                        if (player.phaseNumber == 1) return false;
                        if (
                            !game.hasPlayer(function (current) {
                                return current.hasSkill('smh_huoji') || current.hasSkill('smh_lianhuan');
                            })
                        )
                            return false;
                        return true;
                    },
                    filterTarget(card, player, target) {
                        if (ui.selected.targets.length == 1) {
                            return true;
                        } else {
                            return target.hasSkill('smh_huoji') || target.hasSkill('smh_lianhuan');
                        }
                    },
                    targetprompt: ['移走印', '得到印'],
                    selectTarget: 2,
                    multitarget: true,
                    content() {
                        'step 0';
                        if (targets[0].hasSkill('smh_huoji') && targets[0].hasSkill('smh_lianhuan')) {
                            player.chooseControl('龙印', '凤印').set('prompt', '请选择要移动的印');
                        } else {
                            if (targets[0].hasSkill('smh_huoji')) event._result = { control: '龙印' };
                            else event._result = { control: '凤印' };
                        }
                        ('step 1');
                        if (result.control == '龙印') {
                            targets[0].removeSkill('smh_huoji');
                            targets[1].addSkill('smh_huoji');
                        } else {
                            targets[0].removeSkill('smh_lianhuan');
                            targets[1].addSkill('smh_lianhuan');
                        }
                    },
                    ai: {
                        order: 8,
                        result: {
                            target(player, target) {
                                var att = get.attitude(player, target);
                                if (ui.selected.targets.length == 0) {
                                    return get.attitude(player, target) < 0 ? -999 : -3;
                                    return get.attitude(player, target) >= 0 ? 0 : att;
                                    return target == player && get.attitude(player, target) >= 0 ? 0 : 1;
                                    return target != player && get.attitude(player, target) >= 0 ? 0 : 2;
                                } else {
                                    if (target != player) {
                                        if (target.hasSkill('smh_huoji') || target.hasSkill('smh_lianhuan')) return target.countCards('h') + 2;
                                        return target.countCards('h') + 1;
                                    }
                                    if (target == player) {
                                        if (target.hasSkill('smh_huoji') || target.hasSkill('smh_lianhuan')) return target.countCards('h') + 1;
                                        return target.countCards('h') + 0.5;
                                    }
                                }
                            },
                        },
                        expose: 0.4,
                        threaten: 3,
                    },
                };

                lib.skill.xinfu_yinshi = {
                    audio: 2,
                    trigger: {
                        player: 'damageBegin4',
                    },
                    forced: true,
                    filter(event, player) {
                        if (player.hasSkill('smh_huoji') || player.hasSkill('smh_lianhuan')) return false;
                        if (!player.isEmpty(2)) return false;
                        if (event.nature) return true;
                        return get.type(event.card, 'trick') == 'trick';
                    },
                    content() {
                        trigger.cancel();
                    },
                    ai: {
                        notrick: true,
                        nofire: true,
                        nothunder: true,
                        effect: {
                            player(card, player, target) {
                                if (!player.hasSkill('smh_huoji') && !player.hasSkill('smh_lianhuan')) {
                                    if (get.type(card) == 'equip' && get.subtype(card) == 'equip2') return 'zeroplayertarget';
                                }
                            },
                            target(card, player, target, current) {
                                if (target.hasSkill('smh_huoji') || target.hasSkill('smh_lianhuan')) return;
                                if (player == target && get.subtype(card) == 'equip2') {
                                    if (get.equipValue(card) <= 8) return 0;
                                }
                                if (!target.isEmpty(2)) return;
                                if (card.name == 'tiesuo') return 'zeroplayertarget';
                                if (get.tag(card, 'natureDamage')) return 'zerotarget';
                                if (get.type(card) == 'trick' && get.tag(card, 'damage')) {
                                    return 'zeroplayertarget';
                                }
                            },
                        },
                    },
                };

                lib.skill.xinfu_guanwei = {
                    audio: 2,
                    usable: 1,
                    trigger: {
                        global: 'phaseUseEnd',
                    },
                    filter(event, player) {
                        var history = event.player.getHistory('useCard', function (evt) {
                            return evt.getParent('phaseUse') == event;
                        });
                        var num = 0;
                        var suit = false;
                        for (var i = 0; i < history.length; i++) {
                            var suit2 = history[i].card.suit;
                            if (!suit2) continue;
                            if (suit && suit != suit2) return false;
                            suit = suit2;
                            num++;
                        }
                        return num > 1;
                    },
                    forced: true,
                    content() {
                        'step 0';
                        player.chooseToDiscard('he', get.prompt('xinfu_guanwei', trigger.player), '弃置一张牌,令其摸两张牌并进行一个额外的出牌阶段.').set('ai', function (card) {
                            if (get.attitude(_status.event.player, _status.currentPhase) < 1) return 0;
                            return 9 - get.value(card);
                        })('step 1');
                        if (result.bool) {
                            player.addTempSkill('xinfu_guanwei_off');
                            player.line(trigger.player, 'green');
                            trigger.player.draw(2);
                        } else {
                            event.finish();
                        }
                        ('step 2');
                        var next = trigger.player.phaseUse();
                        event.next.remove(next);
                        trigger.getParent('phase').next.push(next);
                    },
                    ai: {
                        expose: 0.5,
                    },
                    subSkill: {
                        off: {
                            charlotte: true,
                        },
                    },
                };

                lib.skill.kongcheng = {
                    mod: {
                        targetEnabled(card, player, target, now) {
                            if (target.countCards('h') == 0) {
                                if (card.name == 'sha' || card.name == 'juedou') return false;
                            }
                        },
                        aiUseful(player, card, num) {
                            if (card.name == 'sha') {
                                return num / 2;
                            }
                        },
                    },
                    group: 'kongcheng1',
                    audio: 'kongcheng1',
                    audioname: ['re_zhugeliang'],
                    ai: {
                        noh: true,
                        skillTagFilter(player, tag) {
                            if (tag == 'noh') {
                                if (player.countCards('h') != 1) return false;
                            }
                        },
                    },
                };

                lib.skill.duanchang = {
                    audio: 2,
                    audioname: ['re_caiwenji'],
                    forbid: ['boss'],
                    trigger: {
                        player: 'die',
                    },
                    forced: true,
                    forceDie: true,
                    filter(event, player) {
                        return event.source && event.source.isIn();
                    },
                    content() {
                        trigger.source.clearSkills();
                    },
                    logTarget: 'source',
                    ai: {
                        threaten(player, target) {
                            if (target.hp == 1) return 0.2;
                            return 1.5;
                        },
                        effect: {
                            target(card, player, target, current) {
                                if (!target.hasFriend()) return;
                                if (target.hp <= 1 && get.tag(card, 'damage')) return [1, 0, -1, -2.5];
                            },
                        },
                    },
                };

                lib.skill.biaozhao2 = {
                    trigger: {
                        global: ['loseAsyncAfter', 'loseAfter', 'cardsDiscardAfter'],
                    },
                    forced: true,
                    audio: 'biaozhao',
                    filter(event, player) {
                        if (event.name == 'loseAsyncAfter' && event.type != 'discard') return false;
                        if (event.name == 'lose' && (event.getlx === false || event.position != ui.discardPile)) return false;
                        var cards = player.getExpansions('biaozhao');
                        if (!cards.length) return false;
                        var suit = cards[0].suit;
                        var num = cards[0].number;
                        if (event.cards && event.cards.length) {
                            for (var i = 0; i < event.cards.length; i++) {
                                if (event.cards[i].suit == suit && event.cards[i].number == num) return true;
                            }
                        }
                        return false;
                    },
                    content() {
                        'step 0';
                        var card = player.getExpansions('biaozhao')[0];
                        if (trigger.parent.name == 'discard') {
                            trigger.player.gain(card, player, 'give', 'bySelf');
                        } else {
                            player.loseToDiscardpile(card);
                        }
                        ('step 1');
                        player.loseHp();
                    },
                };

                lib.skill.yechou = {
                    audio: 2,
                    trigger: {
                        player: 'die',
                    },
                    forced: true,
                    forceDie: true,
                    content() {
                        'step 0';
                        player
                            .chooseTarget(get.prompt2('yechou'), function (card, player, target) {
                                return player != target && target.getDamagedHp() > 1;
                            })
                            .set('forceDie', true)
                            .set('ai', function (target) {
                                var num = get.attitude(_status.event.player, target);
                                return -num;
                            });
                        ('step 1');
                        if (result.bool) {
                            var target = result.targets[0];
                            player.line(target, 'green');
                            target.addTempSkill('yechou2', { player: 'phaseZhunbeiBegin' });
                        }
                    },
                    ai: {
                        maixie_defend: true,
                        effect: {
                            target(card, player, target) {
                                if (player.hasSkillTag('jueqing', false, target)) return [1, -1];
                                if (target.hp <= 1 && get.attitude(player, target) <= 0 && (get.tag(card, 'damage') || get.damageEffect(target, player, player) > 0) && target.hasFriend()) return [1, 0, 0, -9];
                                // if(get.tag(card,'damage')&&get.damageEffect(target,player,player)>0) return [1,0,0,-1.5];
                            },
                        },
                    },
                };

                lib.skill.twwuhun = {
                    trigger: {
                        player: 'die',
                    },
                    forceDie: true,
                    check(event, player) {
                        return game.hasPlayer(function (current) {
                            return current != player && current.hasMark('twwuhun') && get.attitude(player, current) < 0;
                        });
                    },
                    content() {
                        'step 0';
                        player
                            .judge(function (card) {
                                var name = card.name;
                                if (name == 'tao' || name == 'taoyuan') return -25;
                                return 15;
                            })
                            .set('forceDie', true).judge2 = function (result) {
                                return result.bool;
                            };
                        ('step 1');
                        var num = game.countPlayer(function (current) {
                            return current != player && current.hasMark('twwuhun');
                        });
                        if (result.bool && num > 0) {
                            player
                                .chooseTarget('请选择【武魂】的目标', '选择至少一名拥有<梦魇>标记的角色.令这些角色各自失去X点体力(X为其<梦魇>标记数)', true, [1, num], function (card, player, target) {
                                    return target != player && target.hasMark('twwuhun');
                                })
                                .set('forceDie', true)
                                .set('ai', function (target) {
                                    return -get.attitude(_status.event.player, target);
                                });
                        } else event.finish();
                        ('step 2');
                        var targets = result.targets.sortBySeat();
                        player.line(targets, 'fire');
                        event.targets = targets;
                        ('step 3');
                        var target = targets.shift();
                        var num = target.countMark('twwuhun');
                        if (num > 0) target.loseHp(num);
                        if (targets.length) event.redo();
                    },
                    marktext: '魇',
                    intro: {
                        name: '梦魇',
                        content: 'mark',
                    },
                    group: 'twwuhun_gain',
                    subSkill: {
                        gain: {
                            trigger: {
                                player: 'damageEnd',
                                source: 'damageSource',
                            },
                            forced: true,
                            filter(event, player, name) {
                                if (event.player == event.source) return false;
                                var target = lib.skill.twwuhun_gain.logTarget(event, player);
                                if (!target || !target.isAlive()) return false;
                                return name == 'damageEnd' || target.hasMark('twwuhun');
                            },
                            logTarget(event, player) {
                                if (player == event.player) return event.source;
                                return event.player;
                            },
                            content() {
                                var target = lib.skill.twwuhun_gain.logTarget(trigger, player);
                                target.addMark('twwuhun', player == trigger.source ? 1 : trigger.num);
                            },
                            ai: {
                                maixie_defend: true,
                                effect: {
                                    target(card, player, target) {
                                        if (player.hasSkillTag('jueqing', false, target)) return [1, -1];
                                        if (target.hp <= 1 && get.attitude(player, target) <= 0 && (get.tag(card, 'damage') || get.damageEffect(target, player, player) > 0) && target.hasFriend()) return [1, 0, 0, -9];
                                        // if(get.tag(card,'damage')&&get.damageEffect(target,player,player)>0) return [1,0,0,-1.5];
                                    },
                                },
                            },
                        },
                    },
                };
                lib.skill.new_wuhun = {
                    group: ['new_wuhun_mark', 'new_wuhun_die', 'wuhun22', 'wuhun23'],
                    trigger: {
                        player: 'damageEnd',
                    },
                    forced: true,
                    filter(event, player) {
                        return event.source != undefined;
                    },
                    content() {
                        trigger.source.addMark('new_wuhun_mark', trigger.num);
                    },
                    ai: {
                        maixie_defend: true,
                        effect: {
                            target(card, player, target) {
                                if (player.hasSkillTag('jueqing', false, target)) return [1, -1];
                                if (target.hp <= 1 && get.attitude(player, target) <= 0 && (get.tag(card, 'damage') || get.damageEffect(target, player, player) > 0) && target.hasFriend()) return [1, 0, 0, -9];
                                // if(get.tag(card,'damage')&&get.damageEffect(target,player,player)>0) return [1,0,0,-1.5];
                            },
                        },
                    },
                    subSkill: {
                        die: {
                            trigger: {
                                player: 'die',
                            },
                            forced: true,
                            forceDie: true,
                            forced: true,
                            filter(event, player) {
                                return game.hasPlayer(function (current) {
                                    return current != player && current.hasMark('new_wuhun_mark');
                                });
                            },
                            content() {
                                'step 0';
                                var num = 0;
                                for (var i = 0; i < game.players.length; i++) {
                                    var current = game.players[i];
                                    if (current != player && current.countMark('new_wuhun_mark') > num) {
                                        num = current.countMark('new_wuhun_mark');
                                    }
                                }
                                player
                                    .chooseTarget(true, '请选择【武魂】的目标', function (card, player, target) {
                                        return target != player && target.countMark('new_wuhun_mark') == _status.event.num;
                                    })
                                    .set('ai', function (target) {
                                        return -get.attitude(_status.event.player, target);
                                    })
                                    .set('forceDie', true)
                                    .set('num', num);
                                ('step 1');
                                if (result.bool && result.targets && result.targets.length) {
                                    var target = result.targets[0];
                                    event.target = target;
                                    player.line(target, { color: [255, 255, 0] });
                                }
                                ('step 2');
                                target.judge(function (card) {
                                    if (['tao', 'taoyuan'].includes(card.name)) return 10;
                                    return -10;
                                }).judge2 = function (result) {
                                    return result.bool == false ? true : false;
                                };
                                ('step 3');
                                if (!result.bool) {
                                    const next = game.createEvent('diex', false);
                                    next.source = player;
                                    next.player = target;
                                    next._triggered = null;
                                    next.restMap = { type: null, count: null, audio: null };
                                    next.excludeMark = [];
                                    next.setContent('die');
                                }
                            },
                        },
                        mark: {
                            marktext: '魇',
                            intro: {
                                name: '梦魇',
                                content: 'mark',
                            },
                        },
                    },
                    ai: {
                        threaten: 0.01,
                        notemp: true,
                    },
                };

                lib.skill.huomo = {
                    audio: 2,
                    autoname: ['huzhao'],
                    enable: 'chooseToUse',
                    onChooseToUse(event) {
                        if (game.online || event.huomo_list) return;
                        var list = lib.skill.huomo.getUsed(event.player);
                        event.set('huomo_list', list);
                    },
                    getUsed(player) {
                        var list = [];
                        player.getHistory('useCard', function (evt) {
                            if (get.type(evt.card, null, false) == 'basic') list.add(evt.card.name);
                        });
                        return list;
                    },
                    hiddenCard(player, name) {
                        if (get.type(name) != 'basic') return false;
                        var list = lib.skill.huomo.getUsed(player);
                        if (list.includes(name)) return false;
                        return player.hasCard(function (card) {
                            return get.color(card) == 'black' && get.type(card) != 'basic';
                        }, 'eh');
                    },
                    filter(event, player) {
                        if (
                            event.type == 'wuxie' ||
                            !player.hasCard(function (card) {
                                return get.color(card) == 'black' && get.type(card) != 'basic';
                            }, 'eh')
                        )
                            return false;
                        var list = event.huomo_list || lib.skill.huomo.getUsed(player);
                        for (var name of lib.inpile) {
                            if (get.type(name) != 'basic' || list.includes(name)) continue;
                            var card = { name: name };
                            if (event.filterCard(card, player, event)) return true;
                            if (name == 'sha') {
                                for (var nature of lib.inpile_nature) {
                                    card.nature = nature;
                                    if (event.filterCard(card, player, event)) return true;
                                }
                            }
                        }
                        return false;
                    },
                    chooseButton: {
                        dialog(event, player) {
                            var vcards = [];
                            var list = event.huomo_list || lib.skill.huomo.getUsed(player);
                            for (var name of lib.inpile) {
                                if (get.type(name) != 'basic' || list.includes(name)) continue;
                                var card = { name: name };
                                if (event.filterCard(card, player, event)) vcards.push(['基本', '', name]);
                                if (name == 'sha') {
                                    for (var nature of lib.inpile_nature) {
                                        card.nature = nature;
                                        if (event.filterCard(card, player, event)) vcards.push(['基本', '', name, nature]);
                                    }
                                }
                            }
                            return ui.create.dialog('活墨', [vcards, 'vcard'], 'hidden');
                        },
                        check(button) {
                            var player = _status.event.player;
                            var card = { name: button.link[2], nature: button.link[3] };
                            if (
                                _status.event.parent.type != 'phase' ||
                                game.hasPlayer(function (current) {
                                    return player.canUse(card, current) && get.effect(current, card, player, player) > 0;
                                })
                            ) {
                                switch (button.link[2]) {
                                    case 'tao':
                                    case 'shan':
                                        return 5;
                                    case 'jiu': {
                                        if (player.countCards('he', { color: 'black' }) > 0) return 3;
                                    }
                                    case 'sha':
                                        if (button.link[3] == 'fire') return 2.95;
                                        else if (button.link[3] == 'thunder' || button.link[3] == 'ice') return 2.92;
                                        else return 2.9;
                                }
                            }
                            return 0;
                        },
                        backup(links, player) {
                            return {
                                check(card, player, target) {
                                    if (!ui.selected.cards.length && get.color(card) == 'black') return 10;
                                    else return 8 - get.value(card);
                                },
                                filterCard(card) {
                                    return get.type(card) != 'basic' && get.color(card) == 'black';
                                },
                                viewAs: {
                                    name: links[0][2],
                                    nature: links[0][3],
                                    suit: 'none',
                                    number: null,
                                },
                                position: 'he',
                                popname: true,
                                ignoreMod: true,
                                precontent() {
                                    'step 0';
                                    var card = event.result.cards[0];
                                    event.card = card;
                                    player.$throw(card, 1000);
                                    game.log(player, '将', card, '置于牌堆顶');
                                    event.result.card = { name: event.result.card.name, nature: event.result.card.nature };
                                    event.result.cards = [];
                                    player.lose(card, ui.cardPile, 'visible', 'insert');
                                    ('step 1');
                                },
                            };
                        },
                        prompt(links, player) {
                            return '将一张黑色非基本牌置于牌堆顶并视为使用一张' + get.translation(links[0][3] || '') + get.translation(links[0][2]);
                        },
                    },
                    ai: {
                        order() {
                            var player = _status.event.player;
                            var event = _status.event;
                            if (event.filterCard && event.filterCard({ name: 'jiu' }, player, event) && get.effect(player, { name: 'jiu' }) > 0 && player.countCards('he', { color: 'black' })) {
                                return 3.3;
                            }
                            return 3.1;
                        },
                        skillTagFilter(player, tag, arg) {
                            if (tag == 'fireAttack') return true;
                            if (player.countCards('he', { color: 'black' })) return false;
                            if (
                                !player.hasCard(function (card) {
                                    return get.color(card) == 'black';
                                }, 'hes')
                            ) {
                                return false;
                            }
                        },
                        result: {
                            player: 1,
                        },
                        respondSha: true,
                        respondShan: true,
                        fireAttack: true,
                    },
                };

                lib.skill.zhaosong = {
                    trigger: {
                        global: 'phaseDrawAfter',
                    },
                    logTarget: 'player',
                    check(event, player) {
                        return get.attitude(player, event.player) > 0 || (get.attitude(player, event.player) <= 0 && event.player.countCards('h') <= 2);
                    },
                    filter(event, player) {
                        if (player == event.player || !event.player.countCards('h')) return false;
                        var types = ['basic', 'trick', 'equip'];
                        for (var i of types) {
                            if (event.player.hasMark('zhaosong_' + i)) return false;
                        }
                        return true;
                    },
                    prompt2: '令其交给你一张手牌,并根据类型获得对应的标记',
                    content() {
                        'step 0';
                        event.target = trigger.player;
                        event.target.chooseCard('h', true, get.translation(player) + '发动了【诏颂】;请交给其一张手牌');
                        ('step 1');
                        if (result.bool) {
                            var card = result.cards[0];
                            player.gain(card, target, 'give');
                            var type = get.type2(card, target);
                            if (lib.skill['zhaosong_' + type]) {
                                target.addSkill('zhaosong_' + type);
                                target.addMark('zhaosong_' + type);
                            }
                        }
                    },
                    subSkill: {
                        basic: {
                            marktext: '颂',
                            intro: {
                                name: '诏颂(颂)',
                                name2: '颂',
                                content: '当你使用【杀】选择唯一目标时,你可移去<颂>,并为此【杀】增加至多两个目标',
                            },
                            trigger: {
                                player: 'useCard2',
                            },
                            forced: true,
                            charlotte: true,
                            filter(event, player) {
                                return (
                                    player.hasMark('zhaosong_basic') &&
                                    event.card.name == 'sha' &&
                                    event.targets.length == 1 &&
                                    game.hasPlayer(function (current) {
                                        return current != player && current != event.targets[0] && lib.filter.targetEnabled2(event.card, player, current);
                                    })
                                );
                            },
                            content() {
                                'step 0';
                                player
                                    .chooseTarget([1, 2], '是否弃置<颂>标记？', '为' + get.translation(trigger.card) + '增加至多两个目标', function (card, player, target) {
                                        var evt = _status.event.getTrigger();
                                        return target != player && target != evt.targets[0] && lib.filter.targetEnabled2(evt.card, player, target);
                                    })
                                    .set('ai', function (target) {
                                        var evt = _status.event.getTrigger();
                                        return get.effect(target, evt.card, evt.player, evt.player);
                                    });
                                ('step 1');
                                if (result.bool) {
                                    if (player != event.player && !player.isOnline()) game.delayx();
                                    //player.addTempSkill('zhaosong_shaloss');
                                } else event.finish();
                                ('step 2');
                                var targets = result.targets;
                                player.removeMark('zhaosong_basic', 1);
                                player.removeSkill('zhaosong_basic');
                                trigger.targets.addArray(targets);
                                trigger.zhaosong_basic = true;
                            },
                        },
                        trick: {
                            marktext: '诔',
                            intro: {
                                name: '诏颂(诔)',
                                name2: '诔',
                                content: '当你进入濒死状态时,你可移去<诔>,将体力回复至1点并摸一张牌',
                            },
                            trigger: {
                                player: 'dying',
                            },
                            prompt: '是否弃置<诔>标记？',
                            prompt2: '回复体力至1点并摸一张牌',
                            charlotte: true,
                            filter(event, player) {
                                return player.hasMark('zhaosong_trick') && player.hp < 1;
                            },
                            check(event, player) {
                                if (
                                    player.maxHp < 2 ||
                                    player.countCards('h', function (card) {
                                        var mod2 = game.checkMod(card, player, 'unchanged', 'cardEnabled2', player);
                                        if (mod2 != 'unchanged') return mod2;
                                        var mod = game.checkMod(card, player, event.player, 'unchanged', 'cardSavable', player);
                                        if (mod != 'unchanged') return mod;
                                        var savable = get.info(card).savable;
                                        if (typeof savable == 'function') savable = savable(card, player, event.player);
                                        return savable;
                                    }) >=
                                    1 + event.num - event.player.hp
                                )
                                    return false;
                                return true;
                            },
                            content() {
                                player.removeMark('zhaosong_trick', 1);
                                player.removeSkill('zhaosong_trick');
                                //player.loseMaxHp();
                                if (player.hp < 1) player.recover(1 - player.hp);
                                player.draw();
                            },
                        },
                        equip: {
                            marktext: '赋',
                            intro: {
                                name: '诏颂(赋)',
                                name2: '赋',
                                content: '出牌阶段开始时,你可移去<赋>并弃置一名角色区域内的至多两张牌',
                            },
                            trigger: {
                                player: 'phaseUseBegin',
                            },
                            forced: true,
                            charlotte: true,
                            filter(event, player) {
                                return (
                                    player.hasMark('zhaosong_equip') &&
                                    game.hasPlayer(function (current) {
                                        return current.hasCard(function (card) {
                                            return lib.filter.canBeDiscarded(card, player, current);
                                        }, 'hej');
                                    })
                                );
                            },
                            content() {
                                'step 0';
                                player
                                    .chooseTarget('是否弃置<赋>标记？', '弃置一名角色区域内的至多两张牌', function (card, player, current) {
                                        return current.hasCard(function (card) {
                                            return lib.filter.canBeDiscarded(card, player, current);
                                        }, 'hej');
                                    })
                                    .set('ai', function (target) {
                                        var player = _status.event.player,
                                            att = get.attitude(player, target) > 0 ? 2 : 1;
                                        return get.effect(target, { name: 'guohe_copy' }, player, player) * att;
                                    });
                                ('step 1');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    event.target = target;
                                    player.removeMark('zhaosong_equip', 1);
                                    player.removeSkill('zhaosong_equip');
                                    player.discardPlayerCard(target, true, 'hej', [1, 2]);
                                }
                            },
                        },
                    },
                };

                lib.skill.xinfu_fuyin = {
                    trigger: {
                        target: 'useCardToTargeted',
                    },
                    forced: true,
                    audio: 2,
                    filter(event, player) {
                        if (event.player.countCards('h') < player.countCards('h')) return false;
                        if (event.card.name != 'sha' && event.card.name != 'juedou') return false;
                        return !game.hasPlayer2(function (current) {
                            return current.getHistory('useCard', function (evt) {
                                return evt != event.parent && evt.card && ['sha', 'juedou'].includes(evt.card.name) && evt.targets.includes(player);
                            }).length;
                        });
                    },
                    content() {
                        trigger.parent.excluded.add(player);
                        player.addTempSkill('xinfu_fuyin_off');
                    },
                    ai: {
                        effect: {
                            target(card, player, target, current) {
                                if ((card.name == 'sha' || card.name == 'juedou') && target.countCards('h') < player.countCards('h') && !target.hasSkill('xinfu_fuyin_off')) {
                                    return 'zeroplayertarget';
                                }
                            },
                        },
                    },
                };
                lib.skill.xinfu_fuyin_off = {
                    charlotte: true,
                };

                lib.skill.fangquan = {
                    audio: 2,
                    trigger: {
                        player: 'phaseUseBefore',
                    },
                    filter(event, player) {
                        return player.countCards('h') > 0 && !player.hasSkill('fangquan3');
                    },
                    forced: true,
                    preHidden: true,
                    content() {
                        'step 0';
                        var fang = player.countMark('fangquan2') == 0 && player.hp >= 2 && player.countCards('h') <= player.hp + 2;
                        player
                            .chooseBool(get.prompt2('fangquan'))
                            .set('ai', function () {
                                if (!_status.event.fang) return false;
                                return game.hasPlayer(function (target) {
                                    if (target.hasJudge('lebu') || target.classList.contains('turnedover') || target == player) return false;
                                    if (get.attitude(player, target) > 4) {
                                        return get.threaten(target) / Math.sqrt(target.hp + 1) / Math.sqrt(target.countCards('h') + 1) > 0;
                                    }
                                    return false;
                                });
                            })
                            .set('fang', fang)
                            .setHiddenSkill(event.name);
                        ('step 1');
                        if (result.bool) {
                            trigger.cancel();
                            player.addTempSkill('fangquan2');
                            player.addMark('fangquan2', 1, false);
                            //player.storage.fangquan=result.targets[0];
                        }
                    },
                };
                lib.skill.olfangquan = {
                    audio: 2,
                    audioname: ['shen_caopi'],
                    trigger: {
                        player: 'phaseUseBefore',
                    },
                    filter(event, player) {
                        return player.countCards('h') > 0 && !player.hasSkill('olfangquan3');
                    },
                    forced: true,
                    content() {
                        'step 0';
                        var fang = player.countMark('olfangquan2') == 0 && player.hp >= 2 && player.countCards('h') <= player.hp + 2;
                        player
                            .chooseBool(get.prompt2('olfangquan'))
                            .set('ai', function () {
                                if (!_status.event.fang) return false;
                                return game.hasPlayer(function (target) {
                                    if (target.hasJudge('lebu') || target.classList.contains('turnedover') || target == player) return false;
                                    if (get.attitude(player, target) > 4) {
                                        return get.threaten(target) / Math.sqrt(target.hp + 1) / Math.sqrt(target.countCards('h') + 1) > 0;
                                    }
                                    return false;
                                });
                            })
                            .set('fang', fang);
                        ('step 1');
                        if (result.bool) {
                            trigger.cancel();
                            player.addTempSkill('olfangquan2');
                            player.addMark('olfangquan2', 1, false);
                        }
                    },
                };
                lib.skill.refangquan = {
                    audio: 2,
                    trigger: {
                        player: 'phaseUseBefore',
                    },
                    filter(event, player) {
                        return player.countCards('h') > 0 && !player.hasSkill('fangquan3');
                    },
                    forced: true,
                    content() {
                        'step 0';
                        var fang = player.countMark('fangquan2') == 0 && player.hp >= 2 && player.countCards('h') <= player.hp + 2;
                        player
                            .chooseBool(get.prompt2('refangquan'))
                            .set('ai', function () {
                                if (!_status.event.fang) return false;
                                return game.hasPlayer(function (target) {
                                    if (target.hasJudge('lebu') || target.classList.contains('turnedover') || target == player) return false;
                                    if (get.attitude(player, target) > 4) {
                                        return get.threaten(target) / Math.sqrt(target.hp + 1) / Math.sqrt(target.countCards('h') + 1) > 0;
                                    }
                                    return false;
                                });
                            })
                            .set('fang', fang);
                        ('step 1');
                        if (result.bool) {
                            trigger.cancel();
                            player.addTempSkill('fangquan2', 'phaseAfter');
                            player.addMark('fangquan2', 1, false);
                            player.addTempSkill('refangquan2');
                            //player.storage.fangquan=result.targets[0];
                        }
                    },
                };
                lib.skill.fangquan2 = {
                    trigger: {
                        player: 'phaseEnd',
                    },
                    forced: true,
                    popup: false,
                    content() {
                        'step 0';
                        event.count = player.countMark(event.name);
                        player.removeMark(event.name, event.count);
                        ('step 1');
                        event.count--;
                        player.chooseToDiscard('是否弃置一张牌并令一名其他角色进行一个额外回合？').ai = function (card) {
                            return 20 - get.value(card);
                        };
                        ('step 2');
                        if (result.bool) {
                            player.chooseTarget(true, '请选择进行额外回合的目标角色', lib.filter.notMe).ai = function (target) {
                                if (target.hasJudge('lebu') || target.classList.contains('turnedover')) return -1;
                                if (get.attitude(player, target) > 0) {
                                    return get.threaten(target) / Math.sqrt(target.hp + 1) / Math.sqrt(target.countCards('h') + 1);
                                    if (target.isUnderControl(true)) {
                                        return 2 * (get.threaten(target) / Math.sqrt(target.hp + 1) / Math.sqrt(target.countCards('h') + 1) > 0);
                                    }
                                }
                                return -1;
                            };
                        } else event.finish();
                        ('step 3');
                        var target = result.targets[0];
                        player.line(target, 'fire');
                        target.markSkillCharacter('fangquan', player, '放权', '进行一个额外回合');
                        target.phase('nodelay');
                        target.addSkill('fangquan3');
                        if (event.count > 0) event.goto(1);
                    },
                };
                lib.skill.olfangquan2 = {
                    trigger: {
                        player: 'phaseDiscardBegin',
                    },
                    forced: true,
                    popup: false,
                    content() {
                        'step 0';
                        event.count = player.countMark(event.name);
                        player.removeMark(event.name, event.count, false);
                        ('step 1');
                        event.count--;
                        player.chooseToDiscard('是否弃置一张牌并令一名其他角色进行一个额外回合？').ai = function (card) {
                            return 20 - get.value(card);
                        };
                        ('step 2');
                        if (result.bool) {
                            player.chooseTarget(true, '请选择进行额外回合的目标角色', lib.filter.notMe).ai = function (target) {
                                if (target.hasJudge('lebu') || target.classList.contains('turnedover')) return -1;
                                if (get.attitude(player, target) > 0) {
                                    return get.threaten(target) / Math.sqrt(target.hp + 1) / Math.sqrt(target.countCards('h') + 1);
                                    if (target.isUnderControl(true)) {
                                        return 2 * (get.threaten(target) / Math.sqrt(target.hp + 1) / Math.sqrt(target.countCards('h') + 1) > 0);
                                    }
                                }
                                return -1;
                            };
                        } else event.finish();
                        ('step 3');
                        var target = result.targets[0];
                        player.line(target, 'fire');
                        target.markSkillCharacter('olfangquan', player, '放权', '进行一个额外回合');
                        target.phase('nodelay');
                        target.addSkill('olfangquan3');
                        if (event.count > 0) event.goto(1);
                    },
                };

                lib.skill.jijiu = {
                    mod: {
                        aiValue(player, card, num) {
                            if (card.name != 'tao' && get.color(card) != 'red') return;
                            var cards = player.getCards('hs', function (card) {
                                return card.name == 'tao' || get.color(card) == 'red';
                            });
                            cards.sort(function (a, b) {
                                return (a.name == 'tao' ? 1 : 2) - (b.name == 'tao' ? 1 : 2);
                            });
                            var geti = function () {
                                if (cards.includes(card)) {
                                    return cards.indexOf(card);
                                }
                                return cards.length;
                            };
                            return Math.max(num, [6.5, 4, 3, 2][Math.min(geti(), 2)]);
                        },
                        aiUseful() {
                            return lib.skill.jijiu.mod.aiValue.apply(this, arguments);
                        },
                    },
                    audio: 2,
                    audioname: ['re_huatuo'],
                    enable: 'chooseToUse',
                    viewAsFilter(player) {
                        return player != _status.currentPhase && player.countCards('hes', { color: 'red' }) > 0;
                    },
                    filterCard(card) {
                        return get.color(card) == 'red';
                    },
                    position: 'hes',
                    viewAs: {
                        name: 'tao',
                    },
                    prompt: '将一张红色牌当桃使用',
                    check(card) {
                        return 15 - get.value(card);
                    },
                    ai: {
                        threaten: 1.5,
                        basic: {
                            order(card, player) {
                                if (player.hasSkillTag('pretao')) return 5;
                                return 2;
                            },
                            useful: [6.5, 4, 3, 2],
                            value: [6.5, 4, 3, 2],
                        },
                        result: {
                            target: 2,
                            target_use(player, target) {
                                var numtt = player.countCards('he', { color: 'red' });
                                if (target.hp < 0 && target != player && get.attitude(player, target) > 0) {
                                    if (player.countCards('he', { color: 'red' }) + target.hp > 0) {
                                        return 1;
                                    }
                                }
                                // if(player==target&&player.hp<=0) return 2;
                                if (player.hasSkillTag('nokeep', true, null, true)) return 2;
                                var nd = player.needsToDiscard();
                                var keep = false;
                                if (nd <= 0) {
                                    keep = true;
                                } else if (nd == 1 && target.hp >= 2 && target.countCards('h', 'tao') <= 1) {
                                    keep = true;
                                }
                                var mode = get.mode();
                                if (target.hp >= 2 && keep && target.hasFriend()) {
                                    if (target.hp > 2 || nd == 0) return 0;
                                    if (target.hp == 2) {
                                        if (
                                            game.hasPlayer(function (current) {
                                                if (target != current && get.attitude(target, current) >= 3) {
                                                    if (current.hp <= 1) return true;
                                                    if ((mode == 'identity' || mode == 'versus' || mode == 'chess') && current.identity == 'zhu' && current.hp <= 2) return true;
                                                }
                                            })
                                        ) {
                                            return 0;
                                        }
                                    }
                                }
                                var att = get.attitude(player, target);
                                if (att < 3 && att >= 0 && player != target) return 0;
                                var tri = _status.event.getTrigger();
                                if (mode == 'identity' && player.identity == 'fan' && target.identity == 'fan') {
                                    if (tri && tri.name == 'dying' && tri.source && tri.source.identity == 'fan' && tri.source != target) {
                                        var num = game.countPlayer(function (current) {
                                            if (current.identity == 'fan') {
                                                return current.countCards('h', 'tao');
                                            }
                                        });
                                        if (num > 1 && player == target) return 2;
                                        return 0;
                                    }
                                }
                                if (mode == 'identity' && player.identity == 'zhu' && target.identity == 'nei') {
                                    if (tri && tri.name == 'dying' && tri.source && tri.source.identity == 'zhong') {
                                        return 0;
                                    }
                                }
                                if (mode == 'stone' && target.isMin() && player != target && tri && tri.name == 'dying' && player.side == target.side && tri.source != target.getEnemy()) {
                                    return 0;
                                }
                                return 2;
                            },
                        },
                        tag: {
                            recover: 1,
                            save: 1,
                        },
                    },
                };

                lib.skill.spzhenting = {
                    audio: 2,
                    trigger: {
                        global: 'useCardToTarget',
                    },
                    usable: 1,
                    filter(event, player) {
                        return (event.card.name == 'sha' || get.type(event.card, false) == 'delay') && event.player != player && !event.targets.includes(player) && player.inRange(event.target);
                    },
                    logTarget: 'target',
                    check(event, player) {
                        var target = event.target,
                            source = event.player;
                        if (get.attitude(player, event.target) <= 0 || (event.card.name == 'sha' && player.hp == 1 && player.countCards('h', 'shan') == 0)) return false;
                        var eff2 = get.effect(player, event.card, source, player);
                        if (eff2 >= 0) return true;
                        var eff1 = get.effect(target, event.card, source, player);
                        if (eff1 >= 0) return false;
                        if (eff1)
                            if (event.card.name == 'sha') {
                                if (player.hasShan()) return true;
                                if (eff1 > eff2) return false;
                                if (player.hp > 2) return true;
                                if (player.hp == 2) return eff2 > eff1 / 3;
                                if (player.hp == 1) return eff2 > eff1 / 5;
                                return false;
                            }
                        if (event.card.name == 'shandian' || event.card.name == 'bingliang') return true;
                        if (event.card.name == 'lebu') return !player.needsToDiscard() && target.needsToDiscard();
                        return false;
                    },
                    content() {
                        'step 0';
                        var target = trigger.target,
                            evt = trigger.parent;
                        evt.triggeredTargets2.remove(target);
                        evt.targets.remove(target);
                        evt.triggeredTargets2.add(player);
                        evt.targets.add(player);
                        game.log(trigger.card, '的目标被改为了', player);
                        trigger.untrigger();
                        ('step 1');
                        if (!trigger.player.countDiscardableCards(player, 'h')) event._result = { index: 0 };
                        else player.chooseControl().set('choiceList', ['摸一张牌', '弃置' + get.translation(trigger.player) + '的一张手牌']);
                        ('step 2');
                        if (result.index == 0) player.draw();
                        else {
                            player.line(trigger.player, 'fire');
                            player.discardPlayerCard(trigger.player, true, 'h');
                        }
                    },
                    ai: {
                        threaten: 1.4,
                    },
                };

                lib.skill.baiyi = {
                    enable: 'phaseUse',
                    usable: 1,
                    filterTarget(card, player, target) {
                        return player != target;
                    },
                    selectTarget: 2,
                    limited: true,
                    filter(event, player) {
                        return player.isDamaged() && game.players.length > 2;
                    },
                    multitarget: true,
                    multiline: true,
                    changeSeat: true,
                    content() {
                        player.awakenSkill('baiyi');
                        game.broadcastAll(
                            function (target1, target2) {
                                game.swapSeat(target1, target2);
                            },
                            targets[0],
                            targets[1],
                        );
                    },
                    ai: {
                        order: 1,
                        result: {
                            target(player, target) {
                                if (player.hasUnknown() && target != player.next && target != player.previous) return 0;
                                var distance = Math.pow(get.distance(player, target, 'absolute'), 2);
                                if (!ui.selected.targets.length) return distance;
                                var distance2 = Math.pow(get.distance(player, ui.selected.targets[0], 'absolute'), 2);
                                return Math.min(0, distance - distance2);
                            },
                        },
                    },
                    mark: true,
                    intro: {
                        content: 'limited',
                    },
                    init(player, skill) {
                        player.storage[skill] = false;
                    },
                };

                lib.skill.sbyaoming = {
                    audio: 2,
                    chargeSkill: true,
                    enable: 'phaseUse',
                    filter(event, player) {
                        return player.countMark('charge') > 0;
                    },
                    filterTarget: true,
                    prompt() {
                        var num = _status.event.player.storage.sbyaoming_status;
                        var list = ['弃置一名手牌数不小于你的角色的一张牌', ';或令一名手牌数不大于你的角色摸一张牌'];
                        if (typeof num == 'number') list[num] += '(上次选择)';
                        return list[0] + list[1];
                    },
                    content() {
                        'step 0';
                        player.removeMark('charge', 1);
                        var num = target.countCards('h'),
                            num2 = player.countCards('h');
                        if (num == num2 && target.countCards('he') > 0) {
                            var choice = get.attitude(player, target) > 0 ? 1 : 0;
                            var str = get.translation(target),
                                choiceList = ['弃置' + str + '的一张牌', '令' + str + '摸一张牌'];
                            if (typeof player.storage.sbyaoming_status == 'number') choiceList[player.storage.sbyaoming_status] += '(上次选择)';
                            var next = player.chooseControl().set('choiceList', choiceList);
                            next.set('ai_choice', choice);
                            next.set('ai', () => _status.event.ai_choice);
                        } else event._result = { index: num > num2 ? 0 : 1 };
                        ('step 1');
                        if (result.index == 0) {
                            player.discardPlayerCard(target, true, 'he');
                        } else target.draw();
                        if (typeof player.storage.sbyaoming_status == 'number' && result.index != player.storage.sbyaoming_status) {
                            player.addMark('charge', 1);
                            delete player.storage.sbyaoming_status;
                        } else {
                            player.storage.sbyaoming_status = result.index;
                        }
                    },
                    ai: {
                        order: 6,
                        result: {
                            target(player, target) {
                                var att = get.attitude(player, target),
                                    eff = [0, 0];
                                var hs = player.countCards('h'),
                                    ht = target.countCards('h');
                                if (hs >= ht) {
                                    eff[0] = get.effect(target, { name: 'wuzhong' }, player, player) / 2;
                                    if (player.storage.sbyaoming_status == 0) eff[0] *= 1.2;
                                }
                                if (hs < ht) {
                                    eff[1] = get.effect(target, { name: 'guohe_copy2' }, player, player);
                                    if (player.storage.sbyaoming_status == 1) eff[1] *= 1.2;
                                }
                                return Math.max.apply(Math, eff);
                            },
                        },
                    },
                    group: ['sbyaoming_damage', 'sbyaoming_init'],
                    subSkill: {
                        damage: {
                            trigger: {
                                player: 'damageEnd',
                            },
                            forced: true,
                            content() {
                                'step 0';
                                var num = Math.min(trigger.num, 4 - player.countMark('charge'));
                                if (num > 0) {
                                    player.addMark('charge', num);
                                }
                                ('step 1');
                                player.chooseTarget(get.prompt('sbyaoming'), lib.skill.sbyaoming.prompt()).set('ai', function (target) {
                                    var player = _status.event.player;
                                    return get.effect(target, 'sbyaoming', player, player);
                                });
                                ('step 2');
                                if (result.bool) {
                                    player.useSkill('sbyaoming', result.targets);
                                }
                            },
                        },
                        init: {
                            trigger: {
                                global: 'phaseBefore',
                                player: 'enterGame',
                            },
                            forced: true,
                            filter(event, player) {
                                return (event.name != 'phase' || game.phaseNumber == 0) && player.countMark('charge') < 4;
                            },
                            content() {
                                player.addMark('charge', Math.min(2, 4 - player.countMark('charge')));
                            },
                        },
                    },
                };

                lib.skill.fangzhu = {
                    audio: 2,
                    trigger: {
                        player: 'damageEnd',
                    },
                    forced: true,
                    preHidden: true,
                    content() {
                        'step 0';
                        player
                            .chooseTarget(get.prompt('fangzhu'), '令一名其他角色将武将牌翻面并摸' + get.cnNumber(player.getDamagedHp()) + '张牌', function (card, player, target) {
                                return player != target;
                            })
                            .setHiddenSkill('fangzhu').ai = function (target) {
                                if (target.hasSkillTag('noturn')) return 0;
                                var player = _status.event.player;
                                if (get.attitude(_status.event.player, target) > 0 && target.classList.contains('turnedover')) return 10 * player.getDamagedHp();
                                if (target.classList.contains('turnedover')) return -1;
                                if (_status.currentPhase == target && player.getDamagedHp() < 2 && get.attitude(_status.event.player, _status.currentPhase) < 0) return -get.attitude(_status.event.player, target);
                                if (_status.currentPhase == target && player.getDamagedHp() >= 2 && get.attitude(_status.event.player, _status.currentPhase) > 0) return get.attitude(_status.event.player, target);
                                if (_status.currentPhase != target) return 2 * -get.attitude(_status.event.player, target);
                                return 0;
                            };
                        ('step 1');
                        if (result.bool) {
                            result.targets[0].draw(player.getDamagedHp());
                            result.targets[0].turnOver();
                        }
                    },
                    ai: {
                        maixie: true,
                        maixie_hp: true,
                        effect: {
                            target(card, player, target) {
                                if (get.tag(card, 'damage')) {
                                    if (player.hasSkillTag('jueqing', false, target)) return [1, -2];
                                    if (target.hp <= 1) return;
                                    if (!target.hasFriend()) return;
                                    var hastarget = false;
                                    var turnfriend = false;
                                    var players = game.filterPlayer();
                                    for (var i = 0; i < players.length; i++) {
                                        if (get.attitude(target, players[i]) < 0 && !players[i].isTurnedOver()) {
                                            hastarget = true;
                                        }
                                        if (get.attitude(target, players[i]) > 0 && players[i].isTurnedOver()) {
                                            hastarget = true;
                                            turnfriend = true;
                                        }
                                    }
                                    if (get.attitude(player, target) > 0 && !hastarget) return;
                                    if (turnfriend || target.hp == target.maxHp) return [0.5, 1];
                                    if (target.hp > 1) return [1, 0.5];
                                }
                            },
                        },
                    },
                };
                lib.skill.refangzhu = {
                    audio: 2,
                    trigger: {
                        player: 'damageEnd',
                    },
                    forced: true,
                    content() {
                        'step 0';
                        player.chooseTarget(get.prompt2('refangzhu'), function (card, player, target) {
                            return player != target;
                        }).ai = function (target) {
                            if (target.hasSkillTag('noturn')) return 0;
                            var player = _status.event.player;
                            if (get.attitude(_status.event.player, target) > 0 && target.classList.contains('turnedover')) return 10 * player.getDamagedHp();
                            if (target.classList.contains('turnedover')) return -1;
                            if (_status.currentPhase == target && player.getDamagedHp() < 3) return -get.attitude(_status.event.player, target);
                            if (_status.currentPhase != target) return 2 * -get.attitude(_status.event.player, target);
                            return 0;
                        };
                        ('step 1');
                        if (result.bool) {
                            event.target = result.targets[0];
                            if (player.isHealthy()) event._result = { bool: false };
                            else
                                event.target
                                    .chooseToDiscard('he', player.getDamagedHp())
                                    .set('ai', function (card) {
                                        var player = _status.event.player;
                                        if (player.isTurnedOver() || _status.event.getTrigger().player.getDamagedHp() > 2) return -1;
                                        return player.hp * player.hp - get.value(card);
                                    })
                                    .set('prompt', '弃置' + get.cnNumber(player.getDamagedHp()) + '张牌并失去一点体力;或选择不弃置,将武将牌翻面并摸' + get.cnNumber(player.getDamagedHp()) + '张牌.');
                        } else event.finish();
                        ('step 2');
                        if (result.bool) {
                            event.target.loseHp();
                        } else {
                            if (player.isDamaged()) event.target.draw(player.getDamagedHp());
                            event.target.turnOver();
                        }
                    },
                    ai: {
                        maixie: true,
                        maixie_hp: true,
                        effect: {
                            target(card, player, target) {
                                if (get.tag(card, 'damage')) {
                                    if (player.hasSkillTag('jueqing', false, target)) return [1, -1.5];
                                    if (target.hp <= 1) return;
                                    if (!target.hasFriend()) return;
                                    var hastarget = false;
                                    var turnfriend = false;
                                    var players = game.filterPlayer();
                                    for (var i = 0; i < players.length; i++) {
                                        if (get.attitude(target, players[i]) < 0 && !players[i].isTurnedOver()) {
                                            hastarget = true;
                                        }
                                        if (get.attitude(target, players[i]) > 0 && players[i].isTurnedOver()) {
                                            hastarget = true;
                                            turnfriend = true;
                                        }
                                    }
                                    if (get.attitude(player, target) > 0 && !hastarget) return;
                                    if (turnfriend || target.hp == target.maxHp) return [0.5, 1];
                                    if (target.hp > 1) return [1, 0.5];
                                }
                            },
                        },
                    },
                };

                lib.skill.jilue_fangzhu = {
                    audio: 1,
                    trigger: {
                        player: 'damageEnd',
                    },
                    forced: true,
                    filter(event, player) {
                        return player.hasMark('renjie');
                    },
                    content() {
                        'step 0';
                        player.chooseTarget('是否弃置一枚<忍>,并发动【放逐】？', function (card, player, target) {
                            return player != target;
                        }).ai = function (target) {
                            if (target.hasSkillTag('noturn')) return 0;
                            if (target.hasSkillTag('noturn')) return 0;
                            var player = _status.event.player;
                            if (get.attitude(_status.event.player, target) > 0 && target.classList.contains('turnedover')) return 10 * player.getDamagedHp();
                            if (target.classList.contains('turnedover')) return -1;
                            if (_status.currentPhase == target && player.getDamagedHp() < 2 && get.attitude(_status.event.player, _status.currentPhase) < 0) return -get.attitude(_status.event.player, target);
                            if (_status.currentPhase == target && player.getDamagedHp() >= 2 && get.attitude(_status.event.player, _status.currentPhase) > 0) return get.attitude(_status.event.player, target);
                            if (_status.currentPhase != target) return 2 * -get.attitude(_status.event.player, target);
                            return 0;
                        };
                        ('step 1');
                        if (result.bool) {
                            player.removeMark('renjie', 1);
                            result.targets[0].draw(player.maxHp - player.hp);
                            result.targets[0].turnOver();
                        }
                    },
                };

                lib.skill.jilue_wansha = {
                    audio: 'wansha',
                    audioname: ['shen_simayi'],
                    enable: 'phaseUse',
                    usable: 1,
                    filter(event, player) {
                        return player.hasMark('renjie');
                    },
                    content() {
                        player.removeMark('renjie', 1);
                        player.addTempSkill('rewansha');
                    },
                    ai: {
                        order(item, player) {
                            return get.order({ name: 'sha' }) + 1.5;
                        },
                        result: {
                            player(player, target) {
                                var cx = game.hasPlayer(function (current) {
                                    return current != player && current.hp == 1 && player.inRange(current) && get.attitude(player, current) < 0;
                                });
                                if (
                                    cx > 0 &&
                                    ((player.countCards('hs', 'sha') > 0 && player.getCardUsable('sha') > 0) ||
                                        player.countCards('hs', function (card) {
                                            return card.name != 'sha' && get.tag(card, 'damage');
                                        }) > 0)
                                ) {
                                    return 1;
                                }
                                return 0;
                            },
                        },
                        effect: {
                            player(card, player, target, current) {
                                if (player.hasSkill('rewansha')) {
                                    if (get.tag(card, 'damage')) {
                                        if (get.attitude(player, target) < 0 && target.hp == 1) {
                                            return [1, 1.5];
                                        }
                                    }
                                }
                            },
                        },
                    },
                };

                lib.skill.juesheng = {
                    audio: 2,
                    enable: 'phaseUse',
                    limited: true,
                    viewAs: {
                        name: 'juedou',
                    },
                    filterCard: () => false,
                    selectCard: -1,
                    precontent() {
                        player.awakenSkill('juesheng');
                        player.addTempSkill('juesheng_counter');
                    },
                    ai: {
                        result: {
                            player(player, target) {
                                if (game.roundNumber < 2) return 'zeroplayertarget';
                            },
                            target(player, target) {
                                if (game.roundNumber < 2) return 'zeroplayertarget';
                                var num = target.getAllHistory('useCard', function (evt) {
                                    return evt.card.name == 'sha';
                                }).length;
                                if (get.attitude(player, target) < 0) {
                                    if (num < target.hp) {
                                        return 'zeroplayertarget';
                                    } else {
                                        return num * lib.card.juedou.ai.result.target;
                                    }
                                }
                            },
                        },
                        wuxie(target, card, player, viewer) {
                            if (player == game.me && get.attitude(viewer, player) > 0) {
                                return 0;
                            }
                        },
                        basic: {
                            order: 5,
                            useful: 1,
                            value: 5.5,
                        },
                        tag: {
                            respond: 2,
                            respondSha: 2,
                            damage: 1,
                        },
                    },
                    subSkill: {
                        counter: {
                            trigger: {
                                global: 'damageBegin1',
                            },
                            forced: true,
                            charlotte: true,
                            filter(event, player) {
                                var evt = event.parent;
                                return evt.skill == 'juesheng' && evt.player == player;
                            },
                            content() {
                                var target = trigger.parent.target;
                                trigger.num = target.getAllHistory('useCard', (evt) => evt.card.name == 'sha').length;
                                target.addTempSkill('juesheng', { player: 'phaseAfter' });
                            },
                        },
                    },
                    mark: true,
                    intro: {
                        content: 'limited',
                    },
                    init(player, skill) {
                        player.storage[skill] = false;
                    },
                };

                lib.skill.hfjieying2 = {
                    mod: {
                        cardEnabled(card, player) {
                            if (player.storage.hfjieying2) return false;
                        },
                        cardSavable(card, player) {
                            if (player.storage.hfjieying2) return false;
                        },
                        targetInRange(card, player) {
                            if (player == _status.currentPhase && (card.name == 'sha' || get.type(card) == 'trick')) return true;
                        },
                        aiOrder(player, card, num) {
                            var info = get.info(card);
                            if (!get.tag(card, 'damage') && (!info || !info.toself)) return num + 20;
                        },
                    },
                    trigger: {
                        player: 'useCard2',
                    },
                    forced: true,
                    filter(event, player) {
                        if (player != _status.currentPhase) return false;
                        var card = event.card;
                        if (card.name != 'sha' && get.type(card) != 'trick') return false;
                        var info = get.info(card);
                        if (info.allowMultiple == false) return false;
                        if (event.targets && !info.multitarget) {
                            if (
                                game.hasPlayer(function (current) {
                                    return !event.targets.includes(current) && lib.filter.targetEnabled2(card, player, current);
                                })
                            ) {
                                return true;
                            }
                        }
                        return false;
                    },
                    content() {
                        'step 0';
                        var prompt2 = '为' + get.translation(trigger.card) + '增加一个目标';
                        player
                            .chooseTarget(get.prompt('hfjieying2'), function (card, player, target) {
                                var player = _status.event.player;
                                return !_status.event.targets.includes(target) && lib.filter.targetEnabled2(_status.event.card, player, target);
                            })
                            .set('prompt2', prompt2)
                            .set('ai', function (target) {
                                var trigger = _status.event.getTrigger();
                                var player = _status.event.player;
                                return get.effect(target, trigger.card, player, player);
                            })
                            .set('card', trigger.card)
                            .set('targets', trigger.targets);
                        ('step 1');
                        if (result.bool) {
                            if (!event.isMine() && !event.isOnline()) game.delayx();
                            event.targets = result.targets;
                        } else {
                            event.finish();
                        }
                        ('step 2');
                        if (event.targets) {
                            trigger.targets.addArray(event.targets);
                        }
                    },
                    group: 'hfjieying3',
                    mark: true,
                    intro: {
                        content(player) {
                            if (player) return '不能使用牌直到回合结束';
                            return '使用【杀】或普通锦囊牌时无距离限制且可以多指定一个目标';
                        },
                    },
                };

                lib.skill.tuntian = {
                    audio: 2,
                    audioname: ['gz_dengai'],
                    trigger: {
                        player: 'loseAfter',
                        global: ['equipAfter', 'addJudgeAfter', 'gainAfter', 'loseAsyncAfter', 'addToExpansionAfter'],
                    },
                    forced: true,
                    preHidden: true,
                    filter(event, player) {
                        if (player == _status.currentPhase) return false;
                        if (event.name == 'gain' && event.player == player) return false;
                        var evt = event.getl(player);
                        return evt && evt.cards2 && evt.cards2.length;
                    },
                    content() {
                        'step 0';
                        var next = player.judge(function (card) {
                            if (card.suit == 'heart') return -1;
                            return 1;
                        });
                        next.judge2 = function (result) {
                            return result.bool;
                        };
                        if (get.mode() != 'guozhan') {
                            next.callback = lib.skill.tuntian.callback;
                            event.finish();
                        }
                        ('step 1');
                        if (!result.bool || get.position(result.card) != 'd') {
                            //game.cardsDiscard(card);
                            event.finish();
                            return;
                        }
                        event.card = result.card;
                        player.chooseBool('是否将' + get.translation(event.card) + '作为【田】置于武将牌上？').ai = function () {
                            return true;
                        };
                        ('step 2');
                        if (!result.bool && !event.directbool) {
                            return;
                        }
                        player.addToExpansion(event.card, 'gain2').gaintag.add('tuntian');
                    },
                    callback() {
                        if (!event.judgeResult.bool) {
                            event.finish();
                            return;
                        }
                        player.addToExpansion(event.judgeResult.card, 'gain2').gaintag.add('tuntian');
                    },
                    marktext: '田',
                    intro: {
                        content: 'expansion',
                        markcount: 'expansion',
                    },
                    onremove(player, skill) {
                        var cards = player.getExpansions(skill);
                        if (cards.length) player.loseToDiscardpile(cards);
                    },
                    group: 'tuntian_dist',
                    subSkill: {
                        dist: {
                            mod: {
                                globalFrom(from, to, distance) {
                                    var num = distance - from.getExpansions('tuntian').length;
                                    if (_status.event.skill == 'jixi_backup' || _status.event.skill == 'gzjixi_backup') num++;
                                    return num;
                                },
                            },
                        },
                    },
                    ai: {
                        effect: {
                            target(card, player, target, current) {
                                if (card.name == 'sha' && get.attitude(player, target) > 0 && target.countCards('hs', { name: 'shan' }) <= 0) {
                                    return 'zeroplayertarget';
                                }
                                if (card.name == 'juedou' && get.attitude(player, target) > 0) {
                                    return 'zeroplayertarget';
                                }
                                if (!target.hasFriend() && !player.hasUnknown()) return;
                                if (_status.currentPhase == target) return;
                                if (card.name != 'shuiyanqijunx' && get.tag(card, 'loseCard') && target.countCards('he')) {
                                    if (target.hasSkill('ziliang')) return 0.7;
                                    return [0.5, Math.max(2, target.countCards('h'))];
                                }
                                if (target.isUnderControl(true, player)) {
                                    if ((get.tag(card, 'respondSha') && target.countCards('h', 'sha')) || (get.tag(card, 'respondShan') && target.countCards('h', 'shan'))) {
                                        if (target.hasSkill('ziliang')) return 0.7;
                                        return [0.5, 1];
                                    }
                                } else if (get.tag(card, 'respondSha') || get.tag(card, 'respondShan')) {
                                    if (get.attitude(player, target) > 0 && card.name == 'juedou') return;
                                    if (get.tag(card, 'damage') && target.hasSkillTag('maixie')) return;
                                    if (target.countCards('h') == 0) return 2;
                                    if (target.hasSkill('ziliang')) return 0.7;
                                    if (get.mode() == 'guozhan') return 0.5;
                                    return [0.5, Math.max(target.countCards('h') / 4, target.countCards('h', 'sha') + target.countCards('h', 'shan'))];
                                }
                            },
                        },
                        threaten(player, target) {
                            if (target.countCards('h') == 0) return 2;
                            return 0.5;
                        },
                        nodiscard: true,
                    },
                };
                lib.skill.oltuntian = {
                    inherit: 'tuntian',
                    filter(event, player) {
                        if (player == _status.currentPhase) {
                            if (event.type != 'discard') return false;
                            var evt = event.getl(player);
                            return (
                                evt &&
                                evt.cards2 &&
                                evt.cards2.filter(function (i) {
                                    return get.name(i, evt.hs.includes(i) ? player : false) == 'sha';
                                }).length
                            );
                        }
                        if (event.name == 'gain' && event.player == player) return false;
                        var evt = event.getl(player);
                        return evt && evt.cards2 && evt.cards2.length;
                    },
                    audio: 2,
                    audioname: ['gz_dengai'],
                    trigger: {
                        player: 'loseAfter',
                        global: ['equipAfter', 'addJudgeAfter', 'gainAfter', 'loseAsyncAfter', 'addToExpansionAfter'],
                    },
                    forced: true,
                    preHidden: true,
                    content() {
                        'step 0';
                        var next = player.judge(function (card) {
                            if (card.suit == 'heart') return -1;
                            return 1;
                        });
                        next.judge2 = function (result) {
                            return result.bool;
                        };
                        if (get.mode() != 'guozhan') {
                            next.callback = lib.skill.tuntian.callback;
                            event.finish();
                        }
                        ('step 1');
                        if (!result.bool || get.position(result.card) != 'd') {
                            //game.cardsDiscard(card);
                            event.finish();
                            return;
                        }
                        event.card = result.card;
                        player.chooseBool('是否将' + get.translation(event.card) + '作为【田】置于武将牌上？').ai = function () {
                            return true;
                        };
                        ('step 2');
                        if (!result.bool && !event.directbool) {
                            return;
                        }
                        player.addToExpansion(event.card, 'gain2').gaintag.add('tuntian');
                    },
                    callback() {
                        if (!event.judgeResult.bool) {
                            event.finish();
                            return;
                        }
                        player.addToExpansion(event.judgeResult.card, 'gain2').gaintag.add('tuntian');
                    },
                    marktext: '田',
                    intro: {
                        content: 'expansion',
                        markcount: 'expansion',
                    },
                    onremove(player, skill) {
                        var cards = player.getExpansions(skill);
                        if (cards.length) player.loseToDiscardpile(cards);
                    },
                    group: 'tuntian_dist',
                    subSkill: {
                        dist: {
                            mod: {
                                globalFrom(from, to, distance) {
                                    var num = distance - from.getExpansions('tuntian').length;
                                    if (_status.event.skill == 'jixi_backup' || _status.event.skill == 'gzjixi_backup') num++;
                                    return num;
                                },
                            },
                        },
                    },
                    ai: {
                        effect: {
                            target(card, player, target, current) {
                                if (card.name == 'sha' && get.attitude(player, target) > 0 && target.countCards('hs', { name: 'shan' }) <= 0) {
                                    return 'zeroplayertarget';
                                }
                                if (card.name == 'juedou' && get.attitude(player, target) > 0) {
                                    return 'zeroplayertarget';
                                }
                                if (!target.hasFriend() && !player.hasUnknown()) return;
                                if (_status.currentPhase == target) return;
                                if (card.name != 'shuiyanqijunx' && get.tag(card, 'loseCard') && target.countCards('he')) {
                                    if (target.hasSkill('ziliang')) return 0.7;
                                    return [0.5, Math.max(2, target.countCards('h'))];
                                }
                                if (target.isUnderControl(true, player)) {
                                    if ((get.tag(card, 'respondSha') && target.countCards('h', 'sha')) || (get.tag(card, 'respondShan') && target.countCards('h', 'shan'))) {
                                        if (target.hasSkill('ziliang')) return 0.7;
                                        return [0.5, 1];
                                    }
                                } else if (get.tag(card, 'respondSha') || get.tag(card, 'respondShan')) {
                                    if (get.attitude(player, target) > 0 && card.name == 'juedou') return;
                                    if (get.tag(card, 'damage') && target.hasSkillTag('maixie')) return;
                                    if (target.countCards('h') == 0) return 2;
                                    if (target.hasSkill('ziliang')) return 0.7;
                                    if (get.mode() == 'guozhan') return 0.5;
                                    return [0.5, Math.max(target.countCards('h') / 4, target.countCards('h', 'sha') + target.countCards('h', 'shan'))];
                                }
                            },
                        },
                        threaten(player, target) {
                            if (target.countCards('h') == 0) return 2;
                            return 0.5;
                        },
                        nodiscard: true,
                    },
                };
                lib.skill.retuntian = {
                    audio: 2,
                    trigger: {
                        player: 'loseAfter',
                        global: ['equipAfter', 'addJudgeAfter', 'gainAfter', 'loseAsyncAfter', 'addToExpansionAfter'],
                    },
                    forced: true,
                    filter(event, player) {
                        if (player == _status.currentPhase) return false;
                        if (event.name == 'gain' && event.player == player) return false;
                        var evt = event.getl(player);
                        return evt && evt.cards2 && evt.cards2.length;
                    },
                    content() {
                        player.judge(function (card) {
                            return 1;
                        }).callback = lib.skill.retuntian.callback;
                    },
                    callback() {
                        'step 0';
                        if (event.judgeResult.suit == 'heart') {
                            player.gain(card, 'gain2');
                            event.finish();
                        } else if (get.mode() == 'guozhan') {
                            player.chooseBool('是否将' + get.translation(card) + '作为【田】置于武将牌上？').set('frequentSkill', 'retuntian').ai = function () {
                                return true;
                            };
                        } else event.directbool = true;
                        ('step 1');
                        if (!result.bool && !event.directbool) {
                            //game.cardsDiscard(card);
                            return;
                        }
                        player.addToExpansion(card, 'gain2').gaintag.add('tuntian');
                    },
                    group: 'tuntian_dist',
                    ai: {
                        effect: {
                            target(card, player, target, current) {
                                if (card.name == 'sha' && get.attitude(player, target) > 0 && target.countCards('hs', { name: 'shan' }) <= 0) {
                                    return 'zeroplayertarget';
                                }
                                if (card.name == 'juedou' && get.attitude(player, target) > 0) {
                                    return 'zeroplayertarget';
                                }
                                return lib.skill.tuntian.ai.effect.target.apply(this, arguments);
                            },
                        },
                        threaten(player, target) {
                            if (target.countCards('h') == 0) return 2;
                            return 0.5;
                        },
                        nodiscard: true,
                    },
                };

                lib.skill.chaofeng = {
                    audio: 2,
                    trigger: {
                        source: 'damageBegin1',
                    },
                    forced: true,
                    filter(event, player) {
                        return player.countCards('he') > 0 && player.isPhaseUsing() && !player.hasSkill('chaofeng2');
                    },
                    content() {
                        'step 0';
                        var str = '弃置一张牌并摸一张牌',
                            color,
                            type;
                        if (trigger.card) {
                            type = get.type2(trigger.card, false);
                            color = get.color(trigger.card, false);
                            if (color != 'none') str += ';若弃置' + get.translation(color) + '牌则改为摸两张牌';
                            if (type) str += ';若弃置类型为' + get.translation(type) + '的牌则伤害+1';
                        }
                        var next = player.chooseToDiscard('he', get.prompt('chaofeng', trigger.player), str);
                        next.set('ai', function (card) {
                            var player = _status.event.player,
                                suit = _status.event.color,
                                number = _status.event.type;
                            var val = 4 - get.value(card);
                            if (get.color(card) == suit) val += 3;
                            if (get.attitude(player, trigger.player) <= 0 && get.type2(card) == number) val += 4;
                            if (get.attitude(player, trigger.player) > 0 && get.type2(card) == number) val = 0;
                            return val;
                        });
                        if (color != 'none') {
                            event.color = color;
                            next.set('color', color);
                        }
                        if (type) {
                            event.type = type;
                            next.set('type', type);
                        }
                        ('step 1');
                        if (result.bool) {
                            player.addTempSkill('chaofeng2', 'phaseUseEnd');
                            var card = result.cards[0];
                            player.draw(event.color && get.color(card, card.original == 'h' ? player : false) == event.color ? 2 : 1);
                            if (event.type && get.type2(card, card.original == 'h' ? player : false) == event.type) trigger.num++;
                        }
                    },
                };

                lib.skill.jugong = {
                    audio: ['jingong', 2],
                    trigger: {
                        global: 'damageEnd',
                    },
                    usable: 1,
                    forced: true,
                    notemp: true,
                    marktext: '功',
                    init(player) {
                        player.storage.jugong = [];
                    },
                    filter(event, player) {
                        return event.card && (event.card.name == 'sha' || event.card.name == 'juedou') && event.notLink() && _status.currentPhase != player;
                    },
                    content() {
                        'step 0';
                        player.draw();
                        ('step 1');
                        if (player.countCards('h')) {
                            player.chooseCard('将' + get.cnNumber(1) + '张手牌置于武将牌上作为<功>', 1, true);
                        } else {
                            event.finish();
                        }
                        ('step 2');
                        if (result.cards && result.cards.length) {
                            player.lose(result.cards, ui.special);
                            player.storage.jugong = player.storage.jugong.concat(result.cards);
                            player.markSkill('jugong');
                            game.log(player, '将', result.cards, '置于武将牌上作为<功>');
                        }
                    },
                    intro: {
                        content: 'cards',
                    },
                    group: 'jugong_1',
                    subSkill: {
                        1: {
                            trigger: {
                                player: 'damageBegin',
                            },
                            filter(event, player) {
                                return player.storage.jugong.length > 1;
                            },
                            content() {
                                'step 0';
                                player.chooseCardButton('移去两张<功>', 2, player.storage.jugong, true);
                                ('step 1');
                                if (event.directresult || result.bool) {
                                    var links = event.directresult || result.links;
                                    for (var i = 0; i < links.length; i++) {
                                        player.storage.jugong.remove(links[i]);
                                    }
                                    if (!player.storage.jugong.length) {
                                        player.unmarkSkill('jugong');
                                    } else {
                                        player.markSkill('jugong');
                                    }
                                    player.$throw(links);
                                    game.log(player, '被移去了', links);
                                    for (var i = 0; i < links.length; i++) {
                                        ui.discardPile.appendChild(links[i]);
                                    }
                                }
                                ('step 2');
                                trigger.cancel();
                            },
                        },
                    },
                };

                lib.skill.xindanshou = {
                    audio: 2,
                    trigger: {
                        global: 'phaseJieshuBegin',
                        target: 'useCardToTargeted',
                    },
                    forced: true,
                    filter(event, player, name) {
                        return ((name == 'phaseJieshuBegin' && event.player != player && player.countCards('he') >= event.player.countCards('h')) || (event.targets && event.targets.includes(player) && ['basic', 'trick'].includes(get.type(event.card, 'trick')))) && !player.hasSkill('xindanshou_as');
                    },
                    content() {
                        'step 0';
                        if (event.triggername == 'phaseJieshuBegin') {
                            var num = trigger.player.countCards('h');
                            if (num > 0)
                                player.chooseToDiscard(get.prompt('xindanshou', trigger.player), num, '弃置' + get.cnNumber(num) + '张牌并对' + get.translation(trigger.player) + '造成1点伤害', 'he').set('ai', function (card) {
                                    if (num > 3 && trigger.player.hp > 1) return false;
                                    if (get.damageEffect(_status.event.getTrigger().player, _status.event.player, _status.event.player) > 0) return Math.max(5.5, 8 - _status.event.selectTarget) - get.value(card);
                                    return -1;
                                });
                            else
                                player.chooseBool(get.prompt('xindanshou', trigger.player), '对' + get.translation(trigger.player) + '造成1点伤害').ai = function () {
                                    return get.damageEffect(trigger.player, player, player) > 0;
                                };
                        } else {
                            var num = 0;
                            game.countPlayer2(function (current) {
                                var history = current.getHistory('useCard');
                                for (var j = 0; j < history.length; j++) {
                                    if (['basic', 'trick'].includes(get.type(history[j].card, 'trick')) && history[j].targets && history[j].targets.includes(player)) num++;
                                }
                            });
                            event.num = num;
                            player
                                .chooseBool(get.prompt('xindanshou') + '(可摸' + get.cnNumber(num) + '张牌)', get.translation('xindanshou_info'))
                                .set('ai', function () {
                                    return _status.event.choice;
                                })
                                .set(
                                    'choice',
                                    (function () {
                                        if (player.isPhaseUsing()) {
                                            if (
                                                player.countCards('h', function (card) {
                                                    return ['basic', 'trick'].includes(get.type(card, 'trick')) && player.canUse(card, player, null, true) && get.effect(player, card, player) > 0 && player.getUseValue(card, null, true) > 0;
                                                })
                                            )
                                                return false;
                                            return true;
                                        }
                                        if (num > 2) return true;
                                        var card = trigger.card;
                                        if (get.tag(card, 'damage') && player.hp <= trigger.parent.baseDamage && (!get.tag(card, 'respondShan') || !player.hasShan()) && (!get.tag(card, 'respondSha') || !player.hasSha())) return true;
                                        var source = _status.currentPhase,
                                            todis = source.countCards('h') - Math.max(0, source.needsToDiscard());
                                        if (
                                            todis <=
                                            Math.max(
                                                Math.min(
                                                    2 + (source.hp <= 1 ? 1 : 0),
                                                    player.countCards('he', function (card) {
                                                        return get.value(card, player) < Math.max(5.5, 8 - todis);
                                                    }),
                                                ),
                                                player.countCards('he', function (card) {
                                                    return get.value(card, player) <= 0;
                                                }),
                                            ) &&
                                            get.damageEffect(source, player, player) > 0
                                        )
                                            return false;
                                        if (!source.isPhaseUsing() || get.attitude(player, source) > 0) return true;
                                        if (card.name == 'sha' && !source.getCardUsable('sha')) return true;
                                        return Math.random() < num / 3;
                                    })(),
                                );
                        }
                        ('step 1');
                        if (result.bool) {
                            if (!result.cards || !result.cards.length) {
                            }
                            if (event.triggername == 'useCardToTargeted') {
                                player.draw(num);
                                player.addTempSkill('xindanshou_as');
                            } else {
                                trigger.player.damage('nocard');
                            }
                        }
                    },
                    subSkill: {
                        as: {},
                    },
                    ai: {
                        threaten: 0.6,
                        effect: {
                            target(card, player, target, current) {
                                if (get.attitude(player, target) <= 0) {
                                    if (typeof card != 'object' || target.hasSkill('xindanshou_as') || !['basic', 'trick'].includes(get.type(card, 'trick'))) return;
                                    var num = 0;
                                    game.countPlayer2(function (current) {
                                        var history = current.getHistory('useCard');
                                        for (var j = 0; j < history.length; j++) {
                                            if (['basic', 'trick'].includes(get.type(history[j].card, 'trick')) && history[j].targets && history[j].targets.includes(player)) num++;
                                        }
                                    });
                                    if (player == target && current > 0) return [1.1, num];
                                    return [0.9, num];
                                }
                            },
                        },
                    },
                };

                lib.skill.yongdi = {
                    audio: 2,
                    audioname: ['xinping'],
                    limited: true,
                    trigger: {
                        player: 'phaseZhunbeiBegin',
                    },
                    filter(event, player) {
                        return !player.storage.yongdi;
                    },
                    init(player) {
                        player.storage.yongdi = false;
                    },
                    mark: true,
                    intro: {
                        content: 'limited',
                    },
                    forced: true,
                    content() {
                        'step 0';
                        player
                            .chooseTarget(get.prompt2('yongdi'), function (card, player, target) {
                                return (target.hasSex('male') || target.name == 'key_yuri') && target != player;
                            })
                            .set('ai', function (target) {
                                if (!_status.event.goon) return 0;
                                var player = _status.event.player;
                                var att = get.attitude(player, target);
                                if (att <= 0) return 0;
                                var mode = get.mode();
                                if (mode == 'identity' || (mode == 'versus' && _status.mode == 'four')) {
                                    if (target.name && lib.character[target.name]) {
                                        for (var i = 0; i < lib.character[target.name][3].length; i++) {
                                            if (lib.skill[lib.character[target.name][3][i]].zhuSkill) {
                                                return att * 2;
                                            }
                                        }
                                    }
                                }
                                return get.attitude(player, target);
                            })
                            .set('goon', !player.hasUnknown());
                        ('step 1');
                        if (result.bool) {
                            player.awakenSkill('yongdi');
                            player.storage.yongdi = true;
                            var target = result.targets[0];
                            target.gainMaxHp(true);
                            target.recover();
                            var mode = get.mode();
                            if (mode == 'identity' || (mode == 'versus' && _status.mode == 'four')) {
                                if (target.name && lib.character[target.name]) {
                                    var skills = lib.character[target.name][3];
                                    target.storage.zhuSkill_yongdi = [];
                                    for (var i = 0; i < skills.length; i++) {
                                        var info = lib.skill[skills[i]];
                                        if (info.zhuSkill) {
                                            target.storage.zhuSkill_yongdi.push(skills[i]);
                                            if (info.init) {
                                                info.init(target);
                                            }
                                            if (info.init2) {
                                                info.init2(target);
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    },
                    ai: {
                        expose: 0.2,
                    },
                };
                lib.skill.dcyongdi = {
                    audio: 'yongdi',
                    audioname: ['xinping'],
                    limited: true,
                    enable: 'phaseUse',
                    filterTarget(card, player, target) {
                        return target.hasSex('male');
                    },
                    mark: true,
                    intro: {
                        content: 'limited',
                    },
                    forced: true,
                    content() {
                        'step 0';
                        player.awakenSkill('dcyongdi');
                        if (!game.hasPlayer((current) => current.maxHp < target.maxHp)) {
                            target.gainMaxHp();
                        }
                        ('step 1');
                        if (target.isMinHp()) {
                            target.recover();
                        }
                        ('step 2');
                        if (target.isMinHandcard()) {
                            target.draw(Math.min(5, target.maxHp));
                        }
                        ('step 3');
                    },
                    ai: {
                        expose: 0.3,
                        order: 1,
                        result: {
                            target(player, target) {
                                if (get.attitude(player, target) > 0) {
                                    var val = 0;
                                    var bool1 = !game.hasPlayer((current) => current.maxHp < target.maxHp),
                                        bool2 = target.isMinHp(),
                                        bool3 = target.isMinHandcard();
                                    if (bool1) val += 5;
                                    if (bool2) {
                                        if (bool1) target.maxHp++;
                                        val += get.recoverEffect(target, player, player);
                                        if (bool1) target.maxHp--;
                                    }
                                    if (bool3) {
                                        var num = Math.min(5, target.maxHp + (bool1 ? 1 : 0));
                                        val += 5 * num;
                                    }
                                    return val;
                                }
                            },
                        },
                    },
                    init(player, skill) {
                        player.storage[skill] = false;
                    },
                };

                lib.skill.spshanxi = {
                    audio: 2,
                    trigger: {
                        player: 'phaseUseBegin',
                    },
                    forced: true,
                    filter(event, player) {
                        return game.hasPlayer(function (current) {
                            return current != player && !current.hasMark('spshanxi');
                        });
                    },
                    content() {
                        'step 0';
                        var eff = 0;
                        var target = game.findPlayer(function (current) {
                            return current != player && !current.hasMark('spshanxi');
                        });
                        if (target) eff = -get.attitude(player, target) / Math.sqrt(Math.max(1, target.hp));
                        player
                            .chooseTarget(get.prompt('spshanxi'), '令一名其他角色获得<檄>', function (card, player, target) {
                                return target != player && !target.hasMark('spshanxi');
                            })
                            .set('ai', function (target) {
                                return -get.attitude(_status.event.player, target) / Math.sqrt(Math.max(1, target.hp)) - _status.event.eff;
                            })
                            .set('eff', eff);
                        ('step 1');
                        if (result.bool) {
                            var target = result.targets[0];
                            game.countPlayer(function (current) {
                                if (current == target) {
                                    current.addMark('spshanxi', 1);
                                    current.addSkill('spshanxi_bj');
                                } else {
                                    var num = current.countMark('spshanxi');
                                    if (num > 0) {
                                        current.removeMark('spshanxi', num);
                                        current.removeSkill('spshanxi_bj');
                                    }
                                }
                            });
                        }
                    },
                    marktext: '檄',
                    intro: {
                        name2: '檄',
                        content: '已被设下索命檄文',
                    },
                    group: 'spshanxi_suoming',
                    ai: {
                        threaten: 3.3,
                    },
                };
                lib.skill.spshanxi_bj = {
                    charlotte: true,
                };

                lib.skill.twxiaolian = {
                    audio: 2,
                    trigger: {
                        global: 'useCardToTarget',
                    },
                    logTarget: 'target',
                    filter(event, player) {
                        return event.card && event.card.name == 'sha' && event.player != player && event.targets.length == 1 && event.targets[0] != player;
                    },
                    check(event, player) {
                        return player.hp > 1 && get.attitude(player, event.target) > 0;
                    },
                    content() {
                        trigger.parent.twxiaolian = trigger.targets[0];
                        trigger.targets.length = 0;
                        trigger.parent.triggeredTargets2.length = 0;
                        trigger.targets.push(player);
                    },
                    group: 'twxiaolian_damage',
                    subSkill: {
                        distance: {
                            charlotte: true,
                            init(player, skill) {
                                if (!player.storage[skill]) player.storage[skill] = [];
                            },
                            mark: true,
                            marktext: '马',
                            intro: {
                                content: 'cards',
                                onunmark: 'throw',
                            },
                            mod: {
                                globalTo(from, to, distance) {
                                    if (from != to && to.storage.twxiaolian_distance) return distance + to.storage.twxiaolian_distance.length;
                                },
                            },
                        },
                        damage: {
                            trigger: {
                                player: 'damageEnd',
                            },
                            forced: true,
                            filter(event, player) {
                                return event.getParent(2).twxiaolian != undefined;
                            },
                            content() {
                                'step 0';
                                var target = trigger.getParent(2).twxiaolian;
                                event.target = target;
                                player.chooseCard('是否将一张牌当做【马】置于' + get.translation(target) + '的武将牌旁？', 'he').ai = function (card) {
                                    if (get.attitude(_status.event.player, _status.event.getParent('twxiaolian_damage').target) > 2) return 7 - get.value(card);
                                    return 0;
                                };
                                ('step 1');
                                if (result.bool) {
                                    player.lose(result.cards, ui.special, 'toStorage');
                                    target.addSkill('twxiaolian_distance');
                                    target.storage.twxiaolian_distance.addArray(result.cards);
                                    target.markSkill('twxiaolian_distance');
                                }
                            },
                        },
                    },
                };

                lib.skill.twsidai = {
                    audio: 2,
                    enable: 'phaseUse',
                    usable: 1,
                    limited: true,
                    filter(event, player) {
                        var cards = player.getCards('h', { type: 'basic' });
                        if (!cards.length) return false;
                        for (var i of cards) {
                            if (!game.checkMod(i, player, 'unchanged', 'cardEnabled2', player)) return false;
                        }
                        return event.filterCard({ name: 'sha', storage: { twsidai: true } }, player, event);
                    },
                    viewAs: {
                        name: 'sha',
                        storage: {
                            twsidai: true,
                        },
                    },
                    filterCard: {
                        type: 'basic',
                    },
                    selectCard: -1,
                    check: () => 1,
                    onuse(result, player) {
                        player.awakenSkill('twsidai');
                        player.addTempSkill('twsidai_effect');
                    },
                    ai: {
                        order: 2.9,
                        result: {
                            target(player, target) {
                                if (get.attitude(player, target) > 0) return 'zeroplayertarget';
                                var cards = ui.selected.cards.slice(0);
                                var names = [];
                                for (var i of cards) names.add(i.name);
                                if (names.length < player.hp) return 0;
                                if (player.hasUnknown() && (player.identity != 'fan' || !target.isZhu)) return 0;
                                if (get.attitude(player, target) >= 0) return -20;
                                return lib.card.sha.ai.result.target.apply(this, arguments);
                            },
                        },
                        yingbian(card, player, targets, viewer) {
                            if (get.attitude(viewer, player) <= 0) return 0;
                            var base = 0,
                                hit = false;
                            if (get.cardtag(card, 'yingbian_hit')) {
                                hit = true;
                                if (
                                    targets.filter(function (target) {
                                        return target.hasShan() && get.attitude(viewer, target) < 0 && get.damageEffect(target, player, viewer, get.nature(card)) > 0;
                                    })
                                )
                                    base += 5;
                            }
                            if (get.cardtag(card, 'yingbian_all')) {
                                if (
                                    game.hasPlayer(function (current) {
                                        return !targets.includes(current) && lib.filter.targetEnabled2(card, player, current) && get.effect(current, card, player, player) > 0;
                                    })
                                )
                                    base += 5;
                            }
                            if (get.cardtag(card, 'yingbian_damage')) {
                                if (
                                    targets.filter(function (target) {
                                        return (
                                            get.attitude(player, target) < 0 &&
                                            (hit ||
                                                !target.mayHaveShan() ||
                                                player.hasSkillTag(
                                                    'directHit_ai',
                                                    true,
                                                    {
                                                        target: target,
                                                        card: card,
                                                    },
                                                    true,
                                                )) &&
                                            !target.hasSkillTag('filterDamage', null, {
                                                player: player,
                                                card: card,
                                                jiu: true,
                                            })
                                        );
                                    })
                                )
                                    base += 5;
                            }
                            return base;
                        },
                        canLink(player, target, card) {
                            if (!target.isLinked() && !player.hasSkill('wutiesuolian_skill')) return false;
                            if (
                                target.mayHaveShan() &&
                                !player.hasSkillTag(
                                    'directHit_ai',
                                    true,
                                    {
                                        target: target,
                                        card: card,
                                    },
                                    true,
                                )
                            )
                                return false;
                            if (player.hasSkill('jueqing') || player.hasSkill('gangzhi') || target.hasSkill('gangzhi')) return false;
                            return true;
                        },
                        basic: {
                            useful: [5, 3, 1],
                            value: [5, 3, 1],
                        },
                        tag: {
                            respond: 1,
                            respondShan: 1,
                            damage(card) {
                                if (card.nature == 'poison') return;
                                return 1;
                            },
                            natureDamage(card) {
                                if (card.nature) return 1;
                            },
                            fireDamage(card, nature) {
                                if (card.nature == 'fire') return 1;
                            },
                            thunderDamage(card, nature) {
                                if (card.nature == 'thunder') return 1;
                            },
                            poisonDamage(card, nature) {
                                if (card.nature == 'poison') return 1;
                            },
                        },
                    },
                    mod: {
                        cardUsable(card) {
                            if (card.storage && card.storage.twsidai) return Infinity;
                        },
                        targetInRange(card) {
                            if (card.storage && card.storage.twsidai) return true;
                        },
                    },
                    subSkill: {
                        effect: {
                            charlotte: true,
                            trigger: {
                                source: 'damageBegin1',
                            },
                            filter(event, player) {
                                if (!event.card || !event.card.storage || !event.card.storage.twsidai || event.parent.type != 'card') return false;
                                for (var i of event.cards) {
                                    if (i.name == 'jiu') return true;
                                }
                                return false;
                            },
                            forced: true,
                            popup: false,
                            content() {
                                trigger.num *= 2;
                                game.log(trigger.card, '的伤害值', '#y×2');
                            },
                            group: ['twsidai_tao', 'twsidai_shan'],
                        },
                        tao: {
                            trigger: {
                                source: 'damageSource',
                            },
                            filter(event, player) {
                                if (!event.card || !event.card.storage || !event.card.storage.twsidai || !event.player.isIn()) return false;
                                for (var i of event.cards) {
                                    if (i.name == 'tao') return true;
                                }
                                return false;
                            },
                            forced: true,
                            popup: false,
                            content() {
                                trigger.player.loseMaxHp();
                            },
                        },
                        shan: {
                            trigger: {
                                player: 'useCardToPlayered',
                            },
                            filter(event, player) {
                                if (!event.card || !event.card.storage || !event.card.storage.twsidai || !event.target.isIn()) return false;
                                for (var i of event.cards) {
                                    if (i.name == 'shan') return true;
                                }
                                return false;
                            },
                            forced: true,
                            popup: false,
                            content() {
                                'step 0';
                                trigger.target.chooseToDiscard('h', { type: 'basic' }, '弃置一张基本牌,否则不能响应' + get.translation(trigger.card)).set('ai', function (card) {
                                    var player = _status.event.player;
                                    if (
                                        player.hasCard(function (cardx) {
                                            return cardx != card && cardx.name == 'shan';
                                        }, 'hs')
                                    )
                                        return 12 - get.value(card);
                                    return 0;
                                });
                                ('step 1');
                                if (!result.bool) trigger.directHit.add(trigger.target);
                            },
                        },
                    },
                    mark: true,
                    intro: {
                        content: 'limited',
                    },
                    init(player, skill) {
                        player.storage[skill] = false;
                    },
                };

                lib.skill.xinfu_jingxie1 = {
                    position: 'he',
                    audio: 'xinfu_jingxie',
                    enable: ['phaseUse', 'chooseToUse'],
                    filter(event, player) {
                        if (event.type == 'dying') {
                            if (player != event.dying) return false;
                            return (
                                player.countCards('he', function (card) {
                                    return get.subtype(card) == 'equip2';
                                }) > 0
                            );
                        } else {
                            var he = player.getCards('he');
                            for (var i = 0; i < he.length; i++) {
                                if (['bagua', 'baiyin', 'lanyinjia', 'renwang', 'tengjia', 'zhuge'].includes(he[i].name)) return true;
                            }
                        }
                        return false;
                    },
                    filterCard(card) {
                        return _status.event.parent.name == 'phaseUse' ? ['bagua', 'baiyin', 'lanyinjia', 'renwang', 'tengjia', 'zhuge'].includes(card.name) : get.subtype(card) == 'equip2';
                    },
                    discard: false,
                    get lose() {
                        return _status.event.parent.type == 'dying';
                    },
                    get loseTo() {
                        if (_status.event.parent.type == 'dying') return 'discardPile';
                    },
                    get prepare() {
                        if (_status.event.parent.type == 'dying')
                            return function (cards, player) {
                                player.$throw(cards, 1000);
                                game.log(player, '将', cards, '置入了弃牌堆');
                            };
                    },
                    delay: false,
                    check() {
                        return 1;
                    },
                    content() {
                        'step 0';
                        if (event.getParent(2).type == 'dying') player.draw();
                        else player.showCards(cards);
                        ('step 1');
                        if (event.getParent(2).type == 'dying') {
                            var num = 1 - player.hp;
                            if (num > 0) player.recover(num);
                            return;
                        }
                        var card = cards[0];
                        var bool = get.position(card) == 'e';
                        if (bool) player.removeEquipTrigger(card);
                        game.addVideo('skill', player, ['xinfu_jingxie', [bool, get.cardInfo(card)]]);
                        game.broadcastAll(function (card) {
                            card.init([card.suit, card.number, 'rewrite_' + card.name]);
                        }, card);
                        if (bool) {
                            var info = get.info(card);
                            if (info.skills) {
                                for (var i = 0; i < info.skills.length; i++) {
                                    player.addSkillTrigger(info.skills[i]);
                                }
                            }
                        }
                    },
                    ai: {
                        get order() {
                            if (_status.event.type == 'dying') return 0.5;
                        },
                        basic: {
                            order: 10,
                        },
                        result: {
                            player() {
                                return _status.event.type == 'dying' ? 10 : 1;
                            },
                        },
                        skillTagFilter(player, arg, target) {
                            if (player != target) return false;
                            return (
                                player.countCards('he', function (card) {
                                    if (_status.connectMode && get.position(card) == 'h') return true;
                                    return get.subtype(card) == 'equip2';
                                }) > 0
                            );
                        },
                        save: true,
                    },
                };

                lib.skill.zhongzuo = {
                    audio: 2,
                    trigger: {
                        global: 'phaseJieshuBegin',
                    },
                    forced: true,
                    filter(event, player) {
                        return player.getHistory('damage').length || player.getHistory('sourceDamage').length;
                    },
                    content() {
                        'step 0';
                        player.chooseTarget(get.prompt('zhongzuo'), '令一名角色摸两张牌.若其已受伤,则你摸一张牌.').set('ai', function (target) {
                            if (target.hasSkillTag('nogain') && target != _status.currentPhase) return target.isDamaged() ? 0 : 1;
                            if (target == player) return 2;
                            if (get.attitude(_status.event.player, target) > 0) {
                                if (target.isDamaged() && target == player) {
                                    return 3.5;
                                }
                                if (target.isDamaged()) {
                                    return 3;
                                }
                                return 2;
                            }
                            if (get.attitude(_status.event.player, target) <= 0) return 0;
                            return 0;
                        });
                        ('step 1');
                        if (result.bool) {
                            var target = result.targets[0];
                            target.draw(2);
                            if (target.isDamaged()) player.draw();
                        }
                    },
                };

                lib.skill.jinjian = {
                    audio: 2,
                    trigger: {
                        source: 'damageBegin1',
                    },
                    logTarget: 'player',
                    filter(event, player) {
                        return !event.jinjian_source2 && !player.hasSkill('jinjian_source2');
                    },
                    prompt2: '令即将对其造成的伤害+1',
                    check(event, player) {
                        return (
                            get.attitude(player, event.player) < 0 &&
                            !event.player.hasSkillTag('filterDamage', null, {
                                player: player,
                                card: event.card,
                            })
                        );
                    },
                    content() {
                        trigger.jinjian_source = true;
                        trigger.num++;
                        player.addTempSkill('jinjian_source2');
                    },
                    group: 'jinjian_player',
                    subSkill: {
                        player: {
                            audio: 'jinjian',
                            trigger: {
                                player: 'damageBegin3',
                            },
                            filter(event, player) {
                                return !event.jinjian_player2 && !player.hasSkill('jinjian_player2');
                            },
                            prompt2: '令即将受到的伤害-1',
                            content() {
                                trigger.jinjian_player = true;
                                trigger.num--;
                                player.addTempSkill('jinjian_player2');
                            },
                        },
                        source2: {
                            trigger: {
                                source: 'damageBegin1',
                            },
                            forced: true,
                            charlotte: true,
                            filter(event, player) {
                                return !event.jinjian_source;
                            },
                            content() {
                                trigger.num--;
                                trigger.jinjian_source2 = true;
                                player.removeSkill('jinjian_source2');
                            },
                            marktext: ' -1 ',
                            intro: {
                                content: '下次造成的伤害-1',
                            },
                        },
                        player2: {
                            trigger: {
                                player: 'damageBegin3',
                            },
                            forced: true,
                            charlotte: true,
                            filter(event, player) {
                                return !event.jinjian_player;
                            },
                            content() {
                                trigger.num++;
                                trigger.jinjian_player2 = true;
                                player.removeSkill('jinjian_player2');
                            },
                            marktext: ' +1 ',
                            intro: {
                                content: '下次受到的伤害+1',
                            },
                        },
                    },
                    ai: {
                        maixie_defend: true,
                        threaten: 0.9,
                        effect: {
                            target(card, player, target) {
                                if (player.hasSkillTag('jueqing')) return;
                                if (target.hujia) return;
                                if (player._jinjian_tmp) return;
                                if (_status.event.getParent('useCard', true) || _status.event.getParent('_wuxie', true)) return;
                                if (get.tag(card, 'damage')) {
                                    if (target.hasSkill('jinjian_player2')) {
                                        return [1, -2];
                                    } else {
                                        if (get.attitude(player, target) < 0 && !player.hasSkillTag('damageBonus')) {
                                            var sha = player.getCardUsable({ name: 'sha' });
                                            player._jinjian_tmp = true;
                                            var num = player.countCards('h', function (card) {
                                                if (card.name == 'sha') {
                                                    if (sha == 0) {
                                                        return false;
                                                    } else {
                                                        sha--;
                                                    }
                                                }
                                                return get.tag(card, 'damage') && player.canUse(card, target) && get.effect(target, card, player, player) > 0;
                                            });
                                            delete player._jinjian_tmp;
                                            if (player.hasSkillTag('damage')) {
                                                num++;
                                            }
                                            if (num < 2) {
                                                return [0, 0.8];
                                            }
                                        }
                                    }
                                }
                            },
                        },
                    },
                };

                lib.skill.xinleiji = {
                    group: 'xinleiji_misa',
                    audio: 2,
                    audioname: ['boss_qinglong'],
                    trigger: {
                        player: ['useCard', 'respond'],
                    },
                    filter(event, player) {
                        return event.card.name == 'shan' || (event.name == 'useCard' && event.card.name == 'shandian');
                    },
                    judgeCheck(card, bool) {
                        var suit = card.suit;
                        if (suit == 'spade') {
                            if (bool && card.number > 1 && card.number < 10) return 5;
                            return 4;
                        }
                        if (suit == 'club') return 2;
                        return 0;
                    },
                    content() {
                        player.judge(lib.skill.xinleiji.judgeCheck).judge2 = function (result) {
                            return result.bool ? true : false;
                        };
                    },
                    ai: {
                        useShan: true,
                        effect: {
                            target(card, player, target, current) {
                                if (
                                    get.tag(card, 'respondShan') &&
                                    !player.hasSkillTag(
                                        'directHit_ai',
                                        true,
                                        {
                                            target: target,
                                            card: card,
                                        },
                                        true,
                                    )
                                ) {
                                    var hastarget = game.hasPlayer(function (current) {
                                        return get.attitude(target, current) < 0;
                                    });
                                    var be = target.countCards('e', { color: 'black' });
                                    if (target.countCards('h', 'shan') && be) {
                                        if (!target.hasSkill('xinguidao')) return 0;
                                        return [0, hastarget ? target.countCards('he') / 2 : 0];
                                    }
                                    if (target.countCards('h', 'shan') && target.countCards('h') > 2) {
                                        if (!target.hasSkill('xinguidao')) return 0;
                                        return [0, hastarget ? target.countCards('h') / 4 : 0];
                                    }
                                    if (target.countCards('h') > 3 || (be && target.countCards('h') >= 2)) {
                                        return [0, 0];
                                    }
                                    if (target.countCards('h') == 0) {
                                        return [1.5, 0];
                                    }
                                    if (target.countCards('h') == 1 && !be) {
                                        return [1.2, 0];
                                    }
                                    if (!target.hasSkill('xinguidao')) return [1, 0.05];
                                    return [1, Math.min(0.5, (target.countCards('h') + be) / 4)];
                                }
                                if (card.name == 'lebu' || card.name == 'bingliang') {
                                    if (get.attitude(target, player) < 0 && (target.countCards('e', { color: 'black' }) > 0 || target.countCards('h') > 2)) {
                                        return [0, 0.5];
                                    }
                                }
                            },
                        },
                    },
                };

                lib.skill.zhanjue = {
                    audio: 2,
                    enable: 'phaseUse',
                    filterCard: true,
                    selectCard: -1,
                    position: 'h',
                    filter(event, player) {
                        if (player.getStat().skill.zhanjue_draw && player.getStat().skill.zhanjue_draw >= 2) return false;
                        var hs = player.getCards('h');
                        if (!hs.length) return false;
                        for (var i = 0; i < hs.length; i++) {
                            var mod2 = game.checkMod(hs[i], player, 'unchanged', 'cardEnabled2', player);
                            if (mod2 === false) return false;
                        }
                        return true;
                    },
                    viewAs: {
                        name: 'juedou',
                    },
                    group: ['zhanjue4'],
                    ai: {
                        damage: true,
                        order(item, player) {
                            return get.order({ name: 'tao' }) - 1;
                        },
                        effect: {
                            player(card, player, target) {
                                if (_status.event.skill == 'zhanjue') {
                                    if (
                                        player.hasSkillTag(
                                            'directHit_ai',
                                            true,
                                            {
                                                target: target,
                                                card: card,
                                            },
                                            true,
                                        )
                                    )
                                        return;
                                    if (player.countCards('h') >= 3 || target.countCards('h') >= 3) return 'zeroplayertarget';
                                    if (player.countCards('h', 'tao')) return 'zeroplayertarget';
                                    if (target.countCards('h', 'sha') > 1) return 'zeroplayertarget';
                                }
                            },
                        },
                        wuxie(target, card, player, viewer) {
                            if (player == game.me && get.attitude(viewer, player) > 0) {
                                return 0;
                            }
                        },
                        basic: {
                            order: 5,
                            useful: 1,
                            value: 5.5,
                        },
                        result: {
                            target: -1.5,
                            player(player, target, card) {
                                if (
                                    player.hasSkillTag(
                                        'directHit_ai',
                                        true,
                                        {
                                            target: target,
                                            card: card,
                                        },
                                        true,
                                    )
                                ) {
                                    return 0;
                                }
                                if (get.damageEffect(target, player, target) > 0 && get.attitude(player, target) > 0 && get.attitude(target, player) > 0) {
                                    return 0;
                                }
                                var hs1 = target.countCards('hs', 'sha');
                                var hs2 = player.countCards('hs', 'sha');
                                if (hs1 > hs2 + 1) {
                                    return -2;
                                }
                                if (player.hp == 1 && hs2 == 0 && hs1 >= 1) {
                                    return -2;
                                }
                                var hsx = target.countCards('hs');
                                if (hsx.length == 0) {
                                    return 0;
                                }
                                if (hsx > 3 && hs2 == 0) {
                                    return -2;
                                }
                                return -0.5;
                            },
                        },
                        tag: {
                            respond: 2,
                            respondSha: 2,
                            damage: 1,
                        },
                    },
                };
                lib.skill.rezhanjue = {
                    audio: 2,
                    enable: 'phaseUse',
                    filterCard(card) {
                        return !card.hasGaintag('reqinwang');
                    },
                    selectCard: -1,
                    position: 'h',
                    filter(event, player) {
                        var stat = player.getStat().skill;
                        if (stat.rezhanjue_draw && stat.rezhanjue_draw >= 3) return false;
                        var hs = player.getCards('h', function (card) {
                            return !card.hasGaintag('reqinwang');
                        });
                        if (!hs.length) return false;
                        for (var i = 0; i < hs.length; i++) {
                            var mod2 = game.checkMod(hs[i], player, 'unchanged', 'cardEnabled2', player);
                            if (mod2 === false) return false;
                        }
                        return event.filterCard({ name: 'juedou' }, player);
                    },
                    viewAs: {
                        name: 'juedou',
                    },
                    onuse(links, player) {
                        player.addTempSkill('rezhanjue_effect', 'phaseUseEnd');
                    },
                    ai: {
                        order(item, player) {
                            return get.order({ name: 'tao' }) - 1;
                        },
                        tag: {
                            respond: 2,
                            respondSha: 2,
                            damage: 1,
                        },
                        result: {
                            target: -1.5,
                            player(player, target) {
                                if (
                                    player.hasSkillTag(
                                        'directHit_ai',
                                        true,
                                        {
                                            target: target,
                                            card: { name: 'juedou' },
                                        },
                                        true,
                                    )
                                ) {
                                    return 0;
                                }
                                if (get.damageEffect(target, player, target) > 0 && get.attitude(player, target) > 0 && get.attitude(target, player) > 0) {
                                    return 0;
                                }
                                var hs1 = target.getCards('h', 'sha');
                                var hs2 = player.getCards('h', function (card) {
                                    return card.hasGaintag('reqinwang') && card.name == 'sha';
                                });
                                if (hs1.length > hs2.length + 1) {
                                    return -2;
                                }
                                var hsx = target.getCards('h');
                                if (hsx.length > 2 && hs2.length == 0 && hsx[0].number < 6) {
                                    return -2;
                                }
                                if (hsx.length > 3 && hs2.length == 0) {
                                    return -2;
                                }
                                if (hs1.length > hs2.length && (!hs2.length || hs1[0].number > hs2[0].number)) {
                                    return -2;
                                }
                                return -0.5;
                            },
                        },
                        wuxie(target, card, player, viewer) {
                            if (player == game.me && get.attitude(viewer, player) > 0) {
                                return 0;
                            }
                        },
                        basic: {
                            order: 5,
                            useful: 1,
                            value: 5.5,
                        },
                    },
                };

                lib.skill.zhiheng = {
                    audio: 2,
                    audioname: ['gz_jun_sunquan'],
                    enable: 'phaseUse',
                    usable: 1,
                    position: 'he',
                    filterCard: true,
                    selectCard: [1, Infinity],
                    prompt: '弃置任意张牌并摸等量的牌',
                    check(card) {
                        return 6 - get.value(card);
                    },
                    content() {
                        player.draw(cards.length);
                    },
                    ai: {
                        order(item, player) {
                            return get.order({ name: 'tao' });
                        },
                        result: {
                            player: 1,
                        },
                        threaten: 1.5,
                    },
                };
                lib.skill.rezhiheng = {
                    audio: 2,
                    audioname: ['shen_caopi'],
                    enable: 'phaseUse',
                    usable: 1,
                    position: 'he',
                    filterCard(card, player, event) {
                        event = event || _status.event;
                        if (typeof event != 'string') event = event.parent.name;
                        var mod = game.checkMod(card, player, event, 'unchanged', 'cardDiscardable', player);
                        if (mod != 'unchanged') return mod;
                        return true;
                    },
                    discard: false,
                    lose: false,
                    delay: false,
                    selectCard: [1, Infinity],
                    check(card) {
                        var player = _status.event.player;
                        if (
                            get.position(card) == 'h' &&
                            !player.countCards('h', 'du') &&
                            (player.hp > 2 ||
                                !player.countCards('h', function (card) {
                                    return get.value(card) >= 8;
                                }))
                        ) {
                            return 1;
                        }
                        return 6 - get.value(card);
                    },
                    content() {
                        'step 0';
                        player.discard(cards);
                        event.num = 1;
                        var hs = player.getCards('h');
                        if (!hs.length) event.num = 0;
                        for (var i = 0; i < hs.length; i++) {
                            if (!cards.includes(hs[i])) {
                                event.num = 0;
                                break;
                            }
                        }
                        ('step 1');
                        player.draw(event.num + cards.length);
                    },
                    subSkill: {
                        draw: {
                            trigger: {
                                player: 'loseEnd',
                            },
                            silent: true,
                            filter(event, player) {
                                if (event.getParent(2).skill != 'rezhiheng' && event.getParent(2).skill != 'jilue_zhiheng') return false;
                                if (player.countCards('h')) return false;
                                if (event.cards && event.cards.length) {
                                    for (var i = 0; i < event.cards.length; i++) {
                                        if (event.cards[i].original == 'h') return true;
                                    }
                                }
                                return false;
                            },
                            content() {
                                player.addTempSkill('rezhiheng_delay', trigger.getParent(2).skill + 'After');
                            },
                            forced: true,
                            popup: false,
                        },
                        delay: {},
                    },
                    ai: {
                        order(item, player) {
                            return get.order({ name: 'tao' }) - 1;
                        },
                        result: {
                            player: 1,
                        },
                        threaten: 1.55,
                    },
                };

                lib.skill.zhuning = {
                    audio: 2,
                    enable: 'phaseUse',
                    usable: 2,
                    filter(event, player) {
                        if (!player.countCards('he')) return false;
                        return !player.getStat('skill').zhuning || player.hasSkill('zhuning_double');
                    },
                    filterCard: true,
                    position: 'he',
                    filterTarget(card, player, target) {
                        return player != target;
                    },
                    selectCard: [1, Infinity],
                    delay: false,
                    lose: false,
                    discard: false,
                    check(card) {
                        if (ui.selected.cards.length && ui.selected.cards[0].name == 'du') return 0;
                        if (!ui.selected.cards.length && card.name == 'du') return 20;
                        var player = get.owner(card);
                        if (ui.selected.cards.length >= Math.max(1, player.countCards('h') - player.hp)) return 0;
                        return 10 - get.value(card);
                    },
                    content() {
                        'step 0';
                        player.give(cards, target).gaintag.add('fengxiang_tag');
                        target.addSkill('fengxiang_card');
                        ('step 1');
                        var list = [];
                        for (var name of lib.inpile) {
                            var type = get.type(name);
                            if (type != 'basic' && type != 'trick') continue;
                            var card = { name: name };
                            if (get.tag(card, 'damage') > 0 && player.hasUseTarget(card)) {
                                list.push([type, '', name]);
                            }
                            if (name == 'sha') {
                                for (var i of lib.inpile_nature) {
                                    card.nature = i;
                                    if (player.hasUseTarget(card)) list.push([type, '', name, i]);
                                }
                            }
                        }
                        if (list.length) {
                            player.chooseButton(['是否视为使用一张伤害牌？', [list, 'vcard']]).set('ai', function (button) {
                                return _status.event.player.getUseValue({ name: button.link[2] });
                            });
                        } else event.finish();
                        ('step 2');
                        if (result.bool) {
                            player.chooseUseTarget({ name: result.links[0][2], nature: result.links[0][3] }, true, false);
                        } else event.finish();
                        ('step 3');
                        if (
                            !player.hasHistory('sourceDamage', function (evt) {
                                if (!evt.card) return false;
                                var evtx = evt.getParent('useCard');
                                return evtx.card == evt.card && evtx.getParent(2) == event;
                            })
                        )
                            player.addTempSkill('zhuning_double');
                    },
                    subSkill: {
                        double: {},
                    },
                    ai: {
                        fireAttack: true,
                        order: 4,
                        result: {
                            target(player, target) {
                                if (target.hasSkillTag('nogain')) return 0;
                                if (ui.selected.cards.length && ui.selected.cards[0].name == 'du') {
                                    if (target.hasSkillTag('nodu')) return 0;
                                    return -10;
                                }
                                if (target.hasJudge('lebu')) return 0;
                                var nh = target.countCards('h');
                                var np = player.countCards('h');
                                if (player.hp == player.maxHp || player.storage.rerende < 0 || player.countCards('h') <= 1) {
                                    if (nh >= np - 1 && np <= player.hp && !target.hasSkill('haoshi')) return 0;
                                }
                                return Math.max(1, 5 - nh);
                            },
                        },
                    },
                };
                lib.skill.fengxiang_card = {
                    trigger: {
                        player: 'loseEnd',
                    },
                    forced: true,
                    popup: false,
                    silent: true,
                    filter(event, player, card) {
                        return (
                            player.getCards('h', function (card) {
                                return card.hasGaintag('fengxiang_tag');
                            }).length == 0
                        );
                    },
                    content() {
                        player.removeSkill('fengxiang_card');
                    },
                    mod: {
                        aiOrder(player, card, num) {
                            if (
                                game.countPlayer(function (current) {
                                    return get.attitude(player, current) > 0 && current.hasSkill('fengxiang');
                                })
                            ) {
                                if (get.itemtype(card) == 'card' && card.hasGaintag('fengxiang_tag')) {
                                    return num + 10;
                                }
                            }
                        },
                    },
                };

                lib.skill.yuanyu = {
                    audio: 2,
                    enable: 'phaseUse',
                    usable: 1,
                    content() {
                        'step 0';
                        player.draw();
                        ('step 1');
                        if (player.countCards('h') > 0) {
                            var suits = lib.suit.slice(0),
                                cards = player.getExpansions('yuanyu');
                            for (var i of cards) suits.remove(i.suit);
                            var str = '选择一张手牌,作为<怨>置于武将牌上;同时选择一名其他角色,令该角色获得〖怨语〗的后续效果.';
                            if (suits.length) {
                                str += '目前<怨>中未包含的花色:';
                                for (var i of suits) str += get.translation(i);
                            }
                            player.chooseCardTarget({
                                filterCard: true,
                                filterTarget: lib.filter.notMe,
                                position: 'h',
                                prompt: '怨语:选择置于武将牌上的牌和目标',
                                prompt2: str,
                                suits: suits,
                                forced: true,
                                ai1(card) {
                                    var val = get.value(card),
                                        evt = _status.event;
                                    if (evt.suits.includes(card.suit)) return 8 - get.value(card);
                                    return 5 - get.value(card);
                                },
                                ai2(target) {
                                    var player = _status.event.player;
                                    if (player.storage.yuanyu_damage && player.storage.yuanyu_damage.includes(target)) return 0;
                                    return -get.attitude(player, target);
                                },
                            });
                        } else event.finish();
                        ('step 2');
                        var target = result.targets[0];
                        player.addSkill('yuanyu_damage');
                        player.markAuto('yuanyu_damage', result.targets);
                        player.line(target, 'green');
                        if (!target.storage.yuanyu_mark) {
                            target.storage.yuanyu_mark = player;
                            target.markSkillCharacter('yuanyu_mark', player, '怨语', '已获得〖怨语〗效果');
                            target.addSkill('yuanyu_mark');
                        }
                        player.addToExpansion(result.cards, player, 'give').gaintag.add('yuanyu');
                    },
                    marktext: '怨',
                    intro: {
                        content: 'expansion',
                        markcount: 'expansion',
                    },
                    onremove(player, skill) {
                        var cards = player.getExpansions(skill);
                        if (cards.length) player.loseToDiscardpile(cards);
                        player.removeSkill('yuanyu_damage');
                    },
                    ai: {
                        order: 7,
                        result: {
                            player: 1,
                        },
                    },
                    subSkill: {
                        mark: {
                            mark: '怨语',
                            intro: {
                                content: '已获得〖怨语〗效果',
                            },
                        },
                        damage: {
                            trigger: {
                                global: 'damageSource',
                            },
                            forced: true,
                            charlotte: true,
                            onremove(player, skill) {
                                if (player.storage[skill]) {
                                    for (var i of player.storage[skill]) {
                                        if (i.storage.yuanyu_mark == player) i.unmarkSkill('yuanyu_mark');
                                    }
                                }
                                delete player.storage[skill];
                            },
                            filter(event, player) {
                                var source = event.source;
                                return source && player.getStorage('yuanyu_damage').includes(source) && source.countCards('h') > 0;
                            },
                            content() {
                                'step 0';
                                event.count = Math.min(trigger.num, 9);
                                event.target = trigger.source;
                                ('step 1');
                                event.count--;
                                var suits = lib.suit.slice(0),
                                    cards = player.getExpansions('yuanyu');
                                for (var i of cards) suits.remove(i.suit);
                                var next = target.chooseCard('h', true, '将一张手牌置于' + get.translation(player) + '的武将牌上');
                                next.set('suits', suits);
                                next.set('ai', function (card) {
                                    var val = get.value(card),
                                        evt = _status.event;
                                    if (evt.suits.includes(card.suit)) return 5 - get.value(card);
                                    return 8 - get.value(card);
                                });
                                if (suits.length) {
                                    var str = '目前未包含的花色:';
                                    for (var i of suits) str += get.translation(i);
                                    next.set('prompt2', str);
                                }
                                ('step 2');
                                player.addToExpansion(result.cards, target, 'give').gaintag.add('yuanyu');
                                ('step 3');
                                if (event.count > 0 && target.countCards('h') > 0 && player.hasSkill('yuanyu_damage')) event.goto(1);
                            },
                        },
                    },
                };

                lib.skill.rejieyue = {
                    audio: 2,
                    trigger: {
                        player: 'phaseJieshuBegin',
                    },
                    forced: true,
                    filter(event, player) {
                        return player.countCards('he') > 0;
                    },
                    content() {
                        'step 0';
                        player.chooseCardTarget({
                            prompt: get.prompt2('rejieyue'),
                            filterCard: true,
                            position: 'he',
                            filterTarget: lib.filter.notMe,
                            ai1(card) {
                                var player = _status.event.player;
                                if (card.name == 'du') return 20;
                                if (get.position(card) == 'e' && get.value(card) <= 0) return 14;
                                if (
                                    get.position(card) == 'h' &&
                                    game.hasPlayer(function (current) {
                                        return current != player && get.attitude(player, current) > 0 && current.getUseValue(card) > player.getUseValue(card) && current.getUseValue(card) > player.getUseValue(card);
                                    })
                                )
                                    return 12;
                                if (
                                    game.hasPlayer(function (current) {
                                        return current != player && get.attitude(player, current) > 0;
                                    })
                                ) {
                                    if (card.name == 'wuxie') return 11;
                                    if (card.name == 'shan' && player.countCards('h', 'shan') > 1) return 9;
                                }
                                return 6 / Math.max(1, get.value(card));
                            },
                            ai2(target) {
                                var player = _status.event.player;
                                var card = ui.selected.cards[0];
                                var att = get.attitude(player, target);
                                if (card.name == 'du') return -6 * att;
                                if (att > 0) {
                                    if (get.position(card) == 'h' && target.getUseValue(card) > player.getUseValue(card)) return 4 * att;
                                    if (get.value(card, target) > get.value(card, player)) return 2 * att;
                                    return 1.2 * att;
                                }
                                return (-att * Math.min(4, target.countCards('he'))) / 4;
                            },
                        });
                        ('step 1');
                        if (result.bool) {
                            var target = result.targets[0];
                            event.target = target;
                            player.give(result.cards, target);
                        } else event.finish();
                        ('step 2');
                        var num = 0;
                        if (target.countCards('h')) num++;
                        if (target.countCards('e')) num++;
                        if (num > 0) {
                            var next = target.chooseCard('he', num, '选择保留每个区域的各一张牌,弃置其余的牌.或点取消,令' + get.translation(player) + '摸三张牌', function (card) {
                                for (var i = 0; i < ui.selected.cards.length; i++) {
                                    if (get.position(ui.selected.cards[i]) == get.position(card)) return false;
                                }
                                return true;
                            });
                            next.set('complexCard', true);
                            next.set('goon', get.attitude(target, player) >= 0);
                            next.set('maxNum', num);
                            next.set('ai', function (card) {
                                if (_status.event.player.countCards('he') >= 4) return -1;
                                if (_status.event.goon) return -1;
                                var num = _status.event.maxNum;
                                if (ui.selected.cards.length >= num - 1) {
                                    var val = get.value(
                                        player.getCards('he', function (cardx) {
                                            return cardx != card && !ui.selected.cards.includes(cardx);
                                        }),
                                    );
                                    if (val >= 14) return 0;
                                }
                                return get.value(card);
                            });
                        } else event._result = { bool: false };
                        ('step 3');
                        if (!result.bool) player.draw(3);
                        else {
                            var cards = target.getCards('he');
                            cards.removeArray(result.cards);
                            if (cards.length) target.discard(cards);
                        }
                    },
                    ai: {
                        threaten: 1.3,
                        expose: 0.2,
                    },
                };
                lib.skill.xinfu_lingren = {
                    usable: 1,
                    audio: 2,
                    trigger: {
                        player: 'useCardToPlayered',
                    },
                    forced: true,
                    filter(event, player) {
                        if (event.parent.triggeredTargets3.length > 1) return false;
                        if (!player.isPhaseUsing()) return false;
                        if (!['basic', 'trick'].includes(get.type(event.card))) return false;
                        if (get.tag(event.card, 'damage')) return true;
                        return false;
                    },
                    content() {
                        'step 0';
                        player
                            .chooseTarget(get.prompt('xinfu_lingren'), '选择一名目标角色并猜测其手牌构成', function (card, player, target) {
                                return _status.event.targets.includes(target);
                            })
                            .set('ai', function (target) {
                                return 2 - get.attitude(_status.event.player, target);
                            })
                            .set('targets', trigger.targets);
                        ('step 1');
                        if (result.bool) {
                            var target = result.targets[0];
                            event.target = target;
                            event.choice = {
                                basic: false,
                                trick: false,
                                equip: false,
                            };
                            player.chooseBool('是否押基本牌？').ai = function (event, player) {
                                var rand = 0.95;
                                if (!target.countCards('h', { type: ['basic'] })) rand = 0.05;
                                if (!target.countCards('h')) rand = 0;
                                if (target.countCards('h') > 0) return Math.random() < rand ? true : false;
                            };
                        } else {
                            player.getStat('triggerSkill').xinfu_lingren--;
                            event.finish();
                        }
                        ('step 2');
                        if (result.bool) {
                            event.choice.basic = true;
                        }
                        player.chooseBool('是否押锦囊牌？').ai = function (event, player) {
                            var rand = 0.9;
                            if (!target.countCards('h', { type: ['trick', 'delay'] })) rand = 0.1;
                            if (!target.countCards('h')) rand = 0;
                            if (target.countCards('h') > 0) return Math.random() < rand ? true : false;
                        };
                        ('step 3');
                        if (result.bool) {
                            event.choice.trick = true;
                        }
                        player.chooseBool('是否押装备牌？').ai = function (event, player) {
                            var rand = 0.75;
                            if (!target.countCards('h', { type: ['equip'] })) rand = 0.25;
                            if (!target.countCards('h')) rand = 0;
                            if (target.countCards('h') > 0) return Math.random() < rand ? true : false;
                        };
                        ('step 4');
                        if (result.bool) {
                            event.choice.equip = true;
                        }
                        var reality = {
                            basic: false,
                            trick: false,
                            equip: false,
                        };
                        var he = target.getCards('h');
                        for (var i = 0; i < he.length; i++) {
                            reality[get.type(he[i], 'trick')] = true;
                        }
                        event.num = 0;
                        var tl = ['basic', 'trick', 'equip'];
                        for (var i = 0; i < tl.length; i++) {
                            if (event.choice[tl[i]] == reality[tl[i]]) event.num++;
                        }
                        ('step 5');
                        player.popup('猜对' + get.cnNumber(event.num) + '项');
                        game.log(player, '猜对了' + get.cnNumber(event.num) + '项');
                        if (event.num > 0) {
                            trigger.parent.baseDamage++;
                        }
                        if (event.num > 1) player.draw(2);
                        if (event.num > 2) {
                            player.addTempSkill('lingren_jianxiong', { player: 'phaseBegin' });
                            player.addTempSkill('lingren_xingshang', { player: 'phaseBegin' });
                        }
                    },
                    ai: {
                        threaten: 2.4,
                    },
                };

                lib.skill.taoluan = {
                    enable: 'chooseToUse',
                    popup: false,
                    filter(event, player) {
                        return !player.hasSkill('taoluan3') && player.countCards('hes') > 0;
                    },
                    hiddenCard(player, name) {
                        return !player.getStorage('taoluan').includes(name) && player.countCards('hes') > 0 && !player.hasSkill('taoluan3') && lib.inpile.includes(name);
                    },
                    init(player) {
                        if (!player.storage.taoluan) player.storage.taoluan = [];
                    },
                    chooseButton: {
                        dialog(event, player) {
                            var list = [];
                            for (var i = 0; i < lib.inpile.length; i++) {
                                var name = lib.inpile[i];
                                if (player.storage.taoluan.includes(name)) continue;
                                if (name == 'sha') {
                                    list.push(['基本', '', 'sha']);
                                    list.push(['基本', '', 'sha', 'fire']);
                                    list.push(['基本', '', 'sha', 'thunder']);
                                    list.push(['基本', '', 'sha', 'ice']);
                                } else if (get.type2(name) == 'trick') list.push(['锦囊', '', name]);
                                else if (get.type(name) == 'basic') list.push(['基本', '', name]);
                            }
                            if (list.length == 0) {
                                return ui.create.dialog('滔乱已无牌可用');
                            }
                            return ui.create.dialog('滔乱', [list, 'vcard']);
                        },
                        filter(button, player) {
                            return _status.event.parent.filterCard(
                                {
                                    name: button.link[2],
                                },
                                player,
                                _status.event.parent,
                            );
                        },
                        check(button) {
                            if (_status.event.parent.type != 'phase') return 1;
                            var player = _status.event.player;
                            if (['wugu', 'zhulu_card', 'yiyi', 'lulitongxin', 'lianjunshengyan', 'diaohulishan'].includes(button.link[2])) return 0;
                            return player.getUseValue({
                                name: button.link[2],
                                nature: button.link[3],
                            });
                        },
                        backup(links, player) {
                            return {
                                filterCard: true,
                                selectCard: 1,
                                popname: true,
                                check(card) {
                                    return 6 - get.value(card);
                                },
                                position: 'hes',
                                viewAs: {
                                    name: links[0][2],
                                    nature: links[0][3],
                                },
                                onuse(result, player) {
                                    player.storage.taoluan.add(result.card.name);
                                },
                            };
                        },
                        prompt(links, player) {
                            return '将一张牌当做' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]) + '使用';
                        },
                    },
                    ai: {
                        fireAttack: true,
                        save: true,
                        respondSha: true,
                        respondShan: true,
                        skillTagFilter(player) {
                            if (!player.countCards('hes')) return false;
                        },
                        order: 4,
                        basic: {
                            useful: [6, 4, 3],
                            value: [6, 4, 3],
                        },
                        result: {
                            player(player) {
                                if (_status.event.dying) return get.attitude(player, _status.event.dying);
                                return 1;
                            },
                        },
                    },
                    group: ['taoluan_2'],
                    subSkill: {
                        2: {
                            trigger: {
                                player: ['useCardAfter', 'respondAfter'],
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return event.skill == 'taoluan_backup';
                            },
                            content() {
                                'step 0';
                                player
                                    .chooseTarget(
                                        true,
                                        function (card, player, target) {
                                            return target != player;
                                        },
                                        '滔乱<br><br><div class="text center">令一名其他角色选择一项:1.交给你一张与你以此法使用的牌类别相同的牌;2.你失去1点体力',
                                    )
                                    .set('ai', function (target) {
                                        var player = _status.event.player;
                                        if (get.attitude(player, target) > 0) {
                                            if (get.attitude(target, player) > 0) {
                                                return target.countCards('he');
                                            }
                                            return target.countCards('he') / 2;
                                        }
                                        return 0;
                                    });
                                ('step 1');
                                var target = result.targets[0];
                                event.target = target;
                                player.line(target, 'green');
                                var type = get.type(trigger.card, 'trick');
                                target
                                    .chooseCard('滔乱<br><br><div class="text center">交给' + get.translation(player) + '一张不为' + get.translation(type) + '牌的牌,或令其失去一点体力且滔乱无效直到回合结束', 'he', function (card, player, target) {
                                        return get.type(card, 'trick') != _status.event.cardType;
                                    })
                                    .set('cardType', type)
                                    .set('ai', function (card) {
                                        if (_status.event.att) {
                                            return 11 - get.value(card);
                                        }
                                        return 0;
                                    })
                                    .set('att', get.attitude(target, player) > 0);
                                ('step 2');
                                var target = event.target;
                                if (result.bool) {
                                    target.give(result.cards, player);
                                } else {
                                    player.addTempSkill('taoluan3');
                                    var next = player.loseHp();
                                    event.next.remove(next);
                                    event.getParent('phase').after.push(next);
                                }
                            },
                        },
                    },
                };

                lib.skill.lvli = {
                    audio: 2,
                    init(player, skill) {
                        player.storage[skill] = 0;
                    },
                    enable: 'chooseToUse',
                    filter(event, player) {
                        if (player.storage.lvli > 1) return false;
                        if (player.storage.lvli > 0 && (player != _status.currentPhase || !player.storage.choujue)) return false;
                        return true;
                    },
                    chooseButton: {
                        dialog(event, player) {
                            var list = [];
                            for (var i = 0; i < lib.inpile.length; i++) {
                                var name = lib.inpile[i];
                                if (name == 'sha') {
                                    list.push(['基本', '', 'sha']);
                                    list.push(['基本', '', 'sha', 'fire']);
                                    list.push(['基本', '', 'sha', 'thunder']);
                                } else if (get.type(name) == 'trick') list.push(['锦囊', '', name]);
                                else if (get.type(name) == 'basic') list.push(['基本', '', name]);
                            }
                            return ui.create.dialog(event.lvli6 ? get.prompt('lvli') : '膂力', [list, 'vcard']);
                        },
                        filter(button, player) {
                            var evt = _status.event.parent;
                            if (evt && typeof evt.filterCard == 'function') return evt.filterCard({ name: button.link[2] }, player, evt);
                            return lib.filter.filterCard({ name: button.link[2] }, player, _status.event.parent);
                        },
                        check(button) {
                            var player = _status.event.player;
                            if (player.countCards('h', button.link[2])) return 0;
                            if (_status.event.parent.type != 'phase' && !_status.event.parent.lvli6) return 1;
                            return player.getUseValue({ name: button.link[2] });
                        },
                        backup(links, player) {
                            return {
                                filterCard() {
                                    return false;
                                },
                                audio: 'lvli',
                                selectCard: -1,
                                check(card) {
                                    return 1;
                                },
                                viewAs: { name: links[0][2], nature: links[0][3] },
                            };
                        },
                        prompt(links, player) {
                            return '请选择' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]) + '的目标';
                        },
                    },
                    ai: {
                        fireAttack: true,
                        save: true,
                        respondSha: true,
                        respondShan: true,
                        skillTagFilter(player) {
                            if (!player.countCards('hes')) return false;
                        },
                        order: 10,
                        basic: {
                            useful: [6, 4, 3],
                            value: [6, 4, 3],
                        },
                        result: {
                            player(player) {
                                if (_status.event.type == 'dying') {
                                    return get.attitude(player, _status.event.dying);
                                } else {
                                    return 1;
                                }
                            },
                        },
                    },
                    group: ['lvli2', 'lvli3', 'lvli6'],
                };
                lib.skill.dawu = {
                    trigger: {
                        player: 'phaseJieshuBegin',
                    },
                    forced: true,
                    filter(event, player) {
                        return player.getExpansions('qixing').length;
                    },
                    audio: 2,
                    content() {
                        'step 0';
                        var num = Math.min(game.countPlayer(), player.getExpansions('qixing').length);
                        player
                            .chooseTarget(get.prompt('dawu'), '令至多' + get.cnNumber(num) + '名角色获得<大雾>标记', [1, num])
                            .set('ai', function (target) {
                                if (target.isMin()) return 0;
                                if (target.hasSkill('biantian2')) return 0;
                                var att = get.attitude(player, target);
                                if (att >= 4) {
                                    if (target.hp > 2 && !target.getDamagedHp()) return 0;
                                    if (target.hp >= 3 && target.hasSkillTag('maixie')) return 0;
                                    if (_status.event.allUse) return att;
                                    if (target.hp == 1) return att;
                                    if (target.hp == 2 && target.countCards('he') <= 2) return att * 0.7;
                                    return 0;
                                }
                                return -1;
                            })
                            .set(
                                'allUse',
                                player.getExpansions('qixing').length >=
                                game.countPlayer(function (current) {
                                    return get.attitude(player, current) > 4;
                                }) *
                                2,
                            );
                        ('step 1');
                        if (result.bool) {
                            var length = result.targets.length;
                            for (var i = 0; i < length; i++) {
                                result.targets[i].addSkill('dawu2');
                            }
                            player.chooseCardButton('选择弃置' + get.cnNumber(length) + '张<星>', length, player.getExpansions('qixing'), true);
                            player.addSkill('dawu3');
                        } else {
                            event.finish();
                        }
                        ('step 2');
                        player.loseToDiscardpile(result.links);
                    },
                    ai: {
                        combo: 'qixing',
                    },
                };

                lib.skill.dcmengchi = {
                    audio: 2,
                    trigger: {
                        player: ['linkBefore', 'damageEnd'],
                    },
                    forced: true,
                    filter(event, player) {
                        var num = player.getStat('gain');
                        if (num && num > 0) return false;
                        if (event.name == 'link') return !player.isLinked();
                        return !event.nature;
                    },
                    content() {
                        if (trigger.name == 'link') trigger.cancel();
                        else player.recover();
                    },
                    ai: {
                        filterDamage: true,
                        effect: {
                            target(card, player, target, current) {
                                if (!target.hasFriend()) return;
                                if (target.getStat('gain')) return;
                                if (get.tag(card, 'natureDamage')) return;
                                if (target.hp == 1) return 0.75;
                                if (
                                    (card.name == 'sha' && !player.hasSkill('jiu')) ||
                                    target.hasSkillTag('filterDamage', null, {
                                        player: player,
                                        card: card,
                                    })
                                )
                                    return [1, 0.75 + 0.25 * target.hp];
                            },
                        },
                    },
                    mod: {
                        cardEnabled(card, player) {
                            if (!player.getStat('gain')) return false;
                        },
                        cardSavable(card, player) {
                            if (!player.getStat('gain')) return false;
                        },
                    },
                };

                lib.skill.dcfengyan = {
                    enable: 'phaseUse',
                    usable: 2,
                    chooseButton: {
                        dialog(event, player) {
                            var dialog = ui.create.dialog('讽言:请选择一项', 'hidden');
                            dialog.add([
                                [
                                    ['gain', '令一名体力值不大于你的其他角色交给你一张手牌'],
                                    ['sha', '视为对一名手牌数不大于你的其他角色使用一张【杀】'],
                                ],
                                'textbutton',
                            ]);
                            return dialog;
                        },
                        filter(button, player) {
                            return !player.hasSkill('dcfengyan_' + button.link, null, null, false);
                        },
                        check(button) {
                            var player = _status.event.player;
                            if (
                                button.link == 'gain' &&
                                game.hasPlayer(function (current) {
                                    return lib.skill.dcfengyan_gain.filterTarget(null, player, current) && get.effect(current, 'dcfengyan_gain', player, player) > 0;
                                })
                            )
                                return 4;
                            if (
                                button.link == 'sha' &&
                                game.hasPlayer(function (current) {
                                    return lib.skill.dcfengyan_sha.filterTarget(null, player, current) && get.effect(current, 'dcfengyan_sha', player, player) > 0;
                                })
                            )
                                return 4;
                            return 2;
                        },
                        backup(links) {
                            return get.copy(lib.skill['dcfengyan_' + links[0]]);
                        },
                        prompt(links) {
                            if (links[0] == 'gain') return '令一名体力值不大于你的其他角色交给你一张手牌';
                            return '视为对一名手牌数不大于你的其他角色使用【杀】';
                        },
                    },
                    ai: {
                        order: 10,
                        threaten: 1.7,
                        result: {
                            player: 0.1,
                            target: -1,
                        },
                    },
                    subSkill: {
                        backup: {
                            audio: 'dcfengyan',
                        },
                        gain: {
                            audio: 'dcfengyan',
                            filterTarget(card, player, target) {
                                return target != player && target.hp <= player.hp && target.countCards('h') > 0;
                            },
                            filterCard: () => false,
                            selectCard: -1,
                            charlotte: true,
                            content() {
                                'step 0';
                                player.addTempSkill('dcfengyan_gain', 'phaseUseAfter');
                                target.chooseCard('h', true, '交给' + get.translation(player) + '一张牌');
                                ('step 1');
                                if (result.bool) target.give(result.cards, player);
                            },
                            ai: {
                                tag: {
                                    loseCard: 1,
                                    gain: 1,
                                },
                                result: {
                                    player: 0.1,
                                    target: -1,
                                },
                            },
                        },
                        sha: {
                            audio: 'dcfengyan',
                            filterTarget(card, player, target) {
                                return target != player && target.countCards('h') <= player.countCards('h') && player.canUse('sha', target, false);
                            },
                            filterCard: () => false,
                            selectCard: -1,
                            charlotte: true,
                            content() {
                                player.addTempSkill('dcfengyan_sha', 'phaseUseAfter');
                                player.useCard(
                                    {
                                        name: 'sha',
                                    },
                                    target,
                                    false,
                                );
                            },
                            ai: {
                                result: {
                                    player(player, target) {
                                        return get.effect(
                                            target,
                                            {
                                                name: 'sha',
                                            },
                                            player,
                                            player,
                                        );
                                    },
                                },
                            },
                        },
                    },
                };

                lib.skill.olfengzi = {
                    audio: 2,
                    trigger: {
                        player: 'useCard',
                    },
                    forced: true,
                    usable: 1,
                    filter(event, player) {
                        if (event.olfengzi_buff || !event.targets.length || !player.isPhaseUsing() || player.hasSkill('olfengzi_buff')) return false;
                        var type = get.type(event.card, false);
                        if (type != 'basic' && type != 'trick') return false;
                        return player.hasCard(function (i) {
                            if (_status.connectMode) return true;
                            return get.type2(i, player) == type;
                        }, 'h');
                    },
                    content() {
                        'step 0';
                        if (player != game.me && !player.isUnderControl() && !player.isOnline()) game.delayx();
                        var type = get.type(trigger.card, false);
                        player
                            .chooseToDiscard('h', get.prompt('olfengzi'), '弃置一张' + get.translation(type) + '牌,令' + get.translation(trigger.card) + '结算两次', function (card, player) {
                                return get.type2(card, player) == _status.event.type;
                            })
                            .set('type', type)
                            .set('ai', function (card) {
                                return 10 - get.value(card);
                            });
                        ('step 1');
                        if (result.bool) {
                            player.addTempSkill('olfengzi_buff', 'phaseUseAfter');
                            trigger.olfengzi_buff = player;
                        } else player.getStat('triggerSkill').olfengzi--;
                    },
                    subSkill: {
                        buff: {
                            trigger: {
                                global: 'useCardToTargeted',
                            },
                            forced: true,
                            charlotte: true,
                            popup: false,
                            lastDo: true,
                            filter(event, player) {
                                return event.parent.olfengzi_buff == player && event.targets.length == event.parent.triggeredTargets4.length;
                            },
                            content() {
                                trigger.parent.targets = trigger.parent.targets.concat(trigger.targets);
                                trigger.parent.triggeredTargets4 = trigger.parent.triggeredTargets4.concat(trigger.targets);
                            },
                            onremove(player) {
                                delete player.getStat('triggerSkill').olfengzi;
                            },
                        },
                    },
                };
                lib.skill.xinkuangfu = {
                    enable: 'phaseUse',
                    usable: 1,
                    audio: 2,
                    delay: false,
                    filterTarget(card, player, target) {
                        if (player == target)
                            return (
                                player.countCards('e', function (card) {
                                    return lib.filter.cardDiscardable(card, player);
                                }) > 0
                            );
                        return target.countDiscardableCards(player, 'e') > 0;
                    },
                    filter(event, player) {
                        return game.hasPlayer(function (current) {
                            return current.countCards('e') > 0;
                        });
                    },
                    content() {
                        'step 0';
                        if (player == target) player.chooseToDiscard('e', true);
                        else player.discardPlayerCard(target, 'e', true);
                        ('step 1');
                        player.chooseUseTarget('sha', true, false, 'nodistance');
                        ('step 2');
                        var bool = game.hasPlayer2(function (current) {
                            return current.getHistory('damage', function (evt) {
                                return evt.getParent(4) == event;
                            }).length;
                        });
                        if (player == target && bool) player.draw(2);
                        else if (player != target && !bool) player.chooseToDiscard('h', 2, true);
                    },
                    ai: {
                        order() {
                            return get.order({ name: 'sha' }) + 0.3;
                        },
                        result: {
                            target(player, target) {
                                var att = get.attitude(player, target);
                                var max = 0;
                                var min = 1;
                                target.countCards('e', function (card) {
                                    var val = get.value(card, target);
                                    if (val > max) max = val;
                                    if (val < min) min = val;
                                });
                                if (att > 0 && min <= 0) return target.hasSkillTag('noe') ? 3 : 1;
                                if (att < 0 && max > 0) {
                                    if (target.hasSkillTag('noe')) return max > 6 ? -max / 3 : 0;
                                    return -max;
                                }
                                var cxdr = game.countPlayer(function (current) {
                                    return current.hp == 1 && player.inRange(current) && current != player && (!current.hasSkill('tengjia1') || !current.hasSkill('rw_tengjia1')) && get.attitude(current, player) < 0;
                                });
                                if (player.countCards('e') > 0 && (player.countCards('hs') <= 1 || player.countCards('hs', { name: 'sha' }) <= 0 || cxdr > 0) && target == player) {
                                    return 1;
                                }
                                return 0;
                            },
                        },
                    },
                };
                //神荀彧
                lib.skill.tianzuo = {
                    audio: 2,
                    trigger: {
                        global: 'phaseBefore',
                        player: 'enterGame',
                    },
                    forced: true,
                    filter(event, player) {
                        return (event.name != 'phase' || game.phaseNumber == 0) && !lib.inpile.includes('qizhengxiangsheng');
                    },
                    content() {
                        game.addGlobalSkill('tianzuo_global');
                        for (var i = 2; i < 10; i++) {
                            var card = game.createCard2('qizhengxiangsheng', i % 2 ? 'club' : 'spade', i);
                            ui.cardPile.insertBefore(card, ui.cardPile.childNodes[get.rand(0, ui.cardPile.childNodes.length)]);
                        }
                        game.broadcastAll(function () {
                            lib.inpile.add('qizhengxiangsheng');
                        });
                        game.updateRoundNumber();
                    },
                    ai: {
                        effect: {
                            target(card, player, target) {
                                var numf = game.countPlayer(function (current) {
                                    return target.getFriends().includes(current);
                                });
                                var nume = game.countPlayer(function (current) {
                                    return player.getFriends().includes(current);
                                });
                                if (get.attitude(player, target) < 0) {
                                    if (card.name == 'qizhengxiangsheng') return [0, 1, 0, 0];
                                }
                            },
                        },
                    },
                    group: 'tianzuo_remove',
                    subSkill: {
                        remove: {
                            audio: 'tianzuo',
                            trigger: {
                                target: 'useCardToBefore',
                            },
                            forced: true,
                            _priority: 15,
                            filter(event, player) {
                                return event.card && event.card.name == 'qizhengxiangsheng';
                            },
                            content() {
                                trigger.cancel();
                            },
                            ai: {
                                result: {
                                    target(card, player, target) {
                                        if (card && card.name == 'qizhengxiangsheng') return 'zerotarget';
                                    },
                                }, //QQQ
                            },
                        },
                        global: {
                            trigger: {
                                player: 'useCardToPlayered',
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return event.card.name == 'qizhengxiangsheng';
                            },
                            content() {
                                'step 0';
                                var target = trigger.target;
                                event.target = target;
                                player
                                    .chooseControl('奇兵', '正兵')
                                    .set('prompt', '请选择' + get.translation(target) + '的标记')
                                    .set(
                                        'choice',
                                        (function () {
                                            var e1 = 1.5 * get.sgn(get.damageEffect(target, player, target));
                                            var e2 = 0;
                                            if (target.countGainableCards(player, 'h') > 0 && !target.hasSkillTag('noh')) e2 = -1;
                                            var es = target.getGainableCards(player, 'e');
                                            if (es.length)
                                                e2 = Math.min(
                                                    e2,
                                                    (function () {
                                                        var max = 0;
                                                        for (var i of es) max = Math.max(max, get.value(i, target));
                                                        return -max / 4;
                                                    })(),
                                                );
                                            if (Math.abs(e1 - e2) <= 0.3) return Math.random() < 0.5 ? '奇兵' : '正兵';
                                            if (e1 < e2) return '奇兵';
                                            return '正兵';
                                        })(),
                                    )
                                    .set('ai', function () {
                                        return _status.event.choice;
                                    });
                                ('step 1');
                                var map = trigger.parent.customArgs,
                                    id = target.playerid;
                                if (!map[id]) map[id] = {};
                                map[id].qizheng_name = result.control;
                            },
                        },
                        rewrite: {
                            audio: 'tianzuo',
                            trigger: {
                                global: 'useCardToTargeted',
                            },
                            filter(event, player) {
                                return event.card.name == 'qizhengxiangsheng';
                            },
                            logTarget: 'target',
                            prompt2: '观看其手牌并修改<奇正相生>标记',
                            content() {
                                'step 0';
                                var target = trigger.target;
                                event.target = target;
                                if (player != target && target.countCards('h') > 0) player.viewHandcards(target);
                                player
                                    .chooseControl('奇兵', '正兵')
                                    .set('prompt', '请选择' + get.translation(target) + '的标记')
                                    .set(
                                        'choice',
                                        (function () {
                                            var shas = target.getCards('h', 'sha'),
                                                shans = target.getCards('h', 'shan');
                                            var e1 = 1.5 * get.sgn(get.damageEffect(target, player, target));
                                            var e2 = 0;
                                            if (target.countGainableCards(player, 'h') > 0 && !target.hasSkillTag('noh')) e2 = -1;
                                            var es = target.getGainableCards(player, 'e');
                                            if (es.length)
                                                e2 = Math.min(
                                                    e2,
                                                    (function () {
                                                        var max = 0;
                                                        for (var i of es) max = Math.max(max, get.value(i, target));
                                                        return -max / 4;
                                                    })(),
                                                );
                                            if (get.attitude(player, target) > 0) {
                                                if (shas.length >= Math.max(1, shans.length)) return '奇兵';
                                                if (shans.length > shas.length) return '正兵';
                                                return e1 > e2 ? '奇兵' : '正兵';
                                            }
                                            if (shas.length) e1 = -0.5;
                                            if (shans.length) e2 = -0.7;
                                            if (Math.abs(e1 - e2) <= 0.3) return Math.random() < 0.5 ? '奇兵' : '正兵';
                                            var rand = Math.random();
                                            if (e1 < e2) return rand < 0.1 ? '奇兵' : '正兵';
                                            return rand < 0.1 ? '正兵' : '奇兵';
                                        })(),
                                    )
                                    .set('ai', () => _status.event.choice);
                                ('step 1');
                                var map = trigger.parent.customArgs,
                                    id = target.playerid;
                                if (!map[id]) map[id] = {};
                                map[id].qizheng_name = result.control;
                                map[id].qizheng_aibuff = get.attitude(player, target) > 0;
                            },
                        },
                    },
                };
                lib.skill.lingce.ai = {
                    threaten: 4.2,
                    effect: {
                        target(card, player, target) {
                            var numf = game.countPlayer(function (current) {
                                return target.getFriends().includes(current);
                            });
                            var nume = game.countPlayer(function (current) {
                                return player.getFriends().includes(current);
                            });
                            if (get.attitude(player, target) < 0) {
                                if (numf > 0 || nume > 0) {
                                    if (!target.getStorage('dinghan').includes(card.name)) {
                                        if (get.type(card) == 'delay') {
                                            if (player.countCards('hs', { name: card.name }) > 1) return [1, 0, 1, 0];
                                            return [0.01, 0, 0, 0];
                                        }
                                        if (get.type(card) == 'trick' && card.name != 'wugu') {
                                            if (player.countCards('hs', { name: card.name }) > 1) return [1, 0, 1, 0];
                                            return [0.2, 0, 0, 0];
                                        }
                                    }
                                }
                                if (nume == 0 && numf == 0) {
                                    if (get.type(card) == 'trick' || (get.type(card) == 'delay' && card.name != 'wugu')) {
                                        if (!target.getStorage('dinghan').includes(card.name)) {
                                            if (player.countCards('hs', { name: card.name }) > 1) {
                                                return [1, 0.5, 1, 0];
                                            }
                                            return [0, 1, 0, 0];
                                        }
                                        if (target.getStorage('dinghan').includes(card.name)) {
                                            return [1, 1, 1, 0];
                                        }
                                    }
                                }
                            }
                        },
                    },
                };
                //神马超
                lib.skill.shouli = {
                    audio: 2,
                    mod: {
                        cardUsable(card) {
                            if (card.storage && card.storage.shouli) return Infinity;
                        },
                    },
                    enable: ['chooseToUse', 'chooseToRespond'],
                    hiddenCard(player, name) {
                        if (player != _status.currentPhase && (name == 'sha' || name == 'shan')) return true;
                    },
                    filter(event, player) {
                        if (event.responded || event.shouli || event.type == 'wuxie') return false;
                        if (
                            game.hasPlayer(function (current) {
                                return current.getEquip(4);
                            }) &&
                            event.filterCard(
                                {
                                    name: 'sha',
                                    storage: { shouli: true },
                                },
                                player,
                                event,
                            )
                        )
                            return true;
                        if (
                            game.hasPlayer(function (current) {
                                return current.getEquip(3);
                            }) &&
                            event.filterCard(
                                {
                                    name: 'shan',
                                    storage: { shouli: true },
                                },
                                player,
                                event,
                            )
                        )
                            return true;
                        return false;
                    },
                    delay: false,
                    filterTarget(card, player, target) {
                        var event = _status.event,
                            evt = event;
                        if (event._backup) evt = event._backup;
                        var equip3 = target.getEquip(3);
                        var equip4 = target.getEquip(4);
                        if (
                            equip3 &&
                            evt.filterCard &&
                            evt.filterCard(
                                {
                                    name: 'shan',
                                    storage: { shouli: true },
                                },
                                player,
                                event,
                            )
                        )
                            return true;
                        if (equip4) {
                            var sha = {
                                name: 'sha',
                                storage: { shouli: true },
                            }; //QQQ
                            if (evt.filterCard(sha, player, event)) {
                                if (!evt.filterTarget) return true;
                                return game.hasPlayer(function (current) {
                                    return evt.filterTarget(sha, player, current);
                                });
                            }
                        }
                        return false;
                    },
                    prompt: '将场上的一张坐骑牌当做【杀】或【闪】使用或打出',
                    content() {
                        'step 0';
                        var evt = event.getParent(2);
                        evt.set('shouli', true);
                        var list = [];
                        var equip3 = target.getEquip(3);
                        var equip4 = target.getEquip(4);
                        var backupx = _status.event;
                        _status.event = evt;
                        if (equip3) {
                            var shan = {
                                name: 'shan',
                                storage: { shouli: true },
                            };
                            if (evt.filterCard && evt.filterCard(shan, player, event)) list.push('shan');
                        }
                        if (equip4) {
                            var sha = {
                                name: 'sha',
                                storage: { shouli: true },
                            };
                            if (
                                evt.filterCard &&
                                evt.filterCard(sha, player, evt) &&
                                (!evt.filterTarget ||
                                    game.hasPlayer(function (current) {
                                        return evt.filterTarget(sha, player, current);
                                    }))
                            )
                                list.push('sha');
                        }
                        _status.event = backupx;
                        if (list.length == 1)
                            event._result = {
                                bool: true,
                                links: [list[0] == 'shan' ? equip3 : equip4],
                            };
                        else
                            player.choosePlayerCard(true, target, 'e').set('filterButton', function (button) {
                                var type = get.subtype(button.link);
                                return type == 'equip3' || type == 'equip4';
                            });
                        ('step 1');
                        var evt = event.getParent(2);
                        if (result.bool && result.links && result.links.length) {
                            var name = get.subtype(result.links[0]) == 'equip3' ? 'shan' : 'sha';
                            if (evt.name == 'chooseToUse') {
                                game.broadcastAll(
                                    function (result, name) {
                                        lib.skill.shouli_backup.viewAs = {
                                            name: name,
                                            cards: [result],
                                            storage: { shouli: true },
                                        };
                                        lib.skill.shouli_backup.prompt = '选择' + get.translation(name) + '(' + get.translation(result) + ')的目标';
                                    },
                                    result.links[0],
                                    name,
                                );
                                if (player != target) target.addTempSkill('fengyin');
                                target.addTempSkill('shouli_thunder');
                                player.addTempSkill('shouli_thunder');
                                evt.set('_backupevent', 'shouli_backup');
                                evt.backup('shouli_backup');
                                evt.set('openskilldialog', '选择' + get.translation(name) + '(' + get.translation(result.links[0]) + ')的目标');
                                evt.set('norestore', true);
                                evt.set('custom', {
                                    add: {},
                                    replace: { window() { } },
                                });
                            } else {
                                evt.result.card = {
                                    name: name,
                                    cards: [result],
                                    storage: { shouli: true },
                                };
                                evt.result.cards = [result.links[0]];
                                target.$give(result.links[0], player, false);
                                if (player != target) target.addTempSkill('fengyin');
                                target.addTempSkill('shouli_thunder');
                                player.addTempSkill('shouli_thunder');
                                evt.redo();
                                return;
                            }
                        }
                        evt.goto(0);
                    },
                    ai: {
                        respondSha: true,
                        respondShan: true,
                        skillTagFilter(player, tag) {
                            var subtype = tag == 'respondSha' ? 'equip4' : 'equip3';
                            return game.hasPlayer(function (current) {
                                return current.getEquip(subtype);
                            });
                        },
                        order(item, player) {
                            for (var i = 0; i < game.players.length; i++) {
                                var current = game.players[i];
                                if (_status.currentPhase != player && get.attitude(player, _status.currentPhase) < 0) {
                                    if (get.translation(_status.event == '杀')) {
                                        if (
                                            (_status.currentPhase.getEquip('guanshi') && _status.currentPhase.countCards('hes') > 4) ||
                                            (_status.currentPhase.getEquip('qinglong') &&
                                                _status.currentPhase.countCards('h', { name: 'sha' }) >
                                                player.countCards('h', { name: 'shan' }) +
                                                current.countCards('e', function (card) {
                                                    return get.subtype(card) == 'equip3';
                                                })) ||
                                            (_status.currentPhase.getEquip('zhuge') &&
                                                _status.currentPhase.countCards('h', { name: 'sha' }) >
                                                player.countCards('h', { name: 'shan' }) +
                                                current.countCards('e', function (card) {
                                                    return get.subtype(card) == 'equip3';
                                                }))
                                        )
                                            return 0;
                                    }
                                    if (get.translation(_status.event == '决斗')) {
                                        if (
                                            _status.currentPhase.countCards('h', { name: 'sha' }) >
                                            player.countCards('h', { name: 'sha' }) +
                                            current.countCards('e', function (card) {
                                                return get.subtype(card) == 'equip4';
                                            })
                                        )
                                            return 0;
                                        return 1;
                                    }
                                }
                                if (
                                    get.translation(_status.event != '决斗') &&
                                    _status.currentPhase == player &&
                                    !player.hasCard('wuzhong', 'hes') &&
                                    !player.hasCard(function (card) {
                                        return get.type(card) == 'trick' && player.hasUseTarget(card) && !get.tag(card, 'damage') && card.name != 'wugu' && card.name != 'wuxie';
                                    })
                                )
                                    return 45;
                                return 1;
                            }
                        },
                        result: {
                            player(player, target) {
                                var att = get.attitude(player, target);
                                var eff = Math.max(0, get.effect(target, { name: 'sha' }, player, player));
                                if (_status.event.type != 'phase') return 11 - att;
                                if (!player.hasValueTarget({ name: 'sha' })) return 0;
                                if (_status.event.type == 'phase') {
                                    if (
                                        player.hasValueTarget({ name: 'sha' }) &&
                                        game.hasPlayer(function (current) {
                                            return (
                                                get.attitude(player, current) < 0 &&
                                                current.hp <= 2 &&
                                                current.hasSkill('shouli_thunder') &&
                                                !player.hasCard(function (card) {
                                                    return get.tag(card, 'damage') && player.hasUseTarget(card);
                                                })
                                            );
                                        })
                                    )
                                        return 15 - att + 3 * eff;
                                    return 9 - att + 3 * eff;
                                }
                            },
                        },
                    },
                    group: 'shouli_init',
                    subSkill: {
                        thunder: {
                            charlotte: true,
                            trigger: {
                                player: 'damageBegin1',
                            },
                            forced: true,
                            mark: true,
                            content() {
                                trigger.num++;
                                trigger.nature = 'thunder';
                            },
                            marktext: '⚡',
                            intro: {
                                content: '受到的伤害+1且改为雷属性',
                            },
                        },
                        init: {
                            trigger: {
                                global: 'phaseBefore',
                                player: 'enterGame',
                            },
                            forced: true,
                            filter(event, player) {
                                return event.name != 'phase' || game.phaseNumber == 0;
                            },
                            logTarget: () => game.filterPlayer(),
                            content() {
                                'step 0';
                                var targets = game.filterPlayer().sortBySeat(player.next);
                                event.targets = targets;
                                event.num = 0;
                                ('step 1');
                                var target = event.targets[num];
                                if (target.isIn()) {
                                    var card = get.cardPile2(function (card) {
                                        if (get.cardtag(card, 'gifts')) return false;
                                        var type = get.subtype(card);
                                        if (type != 'equip3' && type != 'equip4' && type != 'equip6') return false;
                                        return target.canUse(card, target);
                                    });
                                    if (card) target.chooseUseTarget(card, 'nopopup', 'noanimate', true);
                                }
                                event.num++;
                                if (event.num < targets.length) event.redo();
                            },
                            audio: 'shouli',
                        },
                    },
                };
                lib.skill.hengwu = {
                    audio: 2,
                    trigger: {
                        player: ['useCard', 'respond'],
                    },
                    forced: true,
                    filter(event, player) {
                        var suit = event.card.suit;
                        if (
                            !lib.suit.includes(suit) ||
                            player.hasCard(function (card) {
                                return card.suit == suit;
                            }, 'h')
                        )
                            return false;
                        return game.hasPlayer(function (current) {
                            return current.hasCard(function (card) {
                                return card.suit == suit;
                            }, 'e');
                        });
                    },
                    content() {
                        var suit = trigger.card.suit;
                        player.draw(
                            game.countPlayer(function (current) {
                                return current.countCards('e', function (card) {
                                    return card.suit == suit;
                                });
                            }),
                        );
                    },
                    mod: {
                        aiUseful(player, card, num) {
                            var suit = card.suit;
                            var es = game.countPlayer(function (current) {
                                return current.countCards('e', function (cardx) {
                                    return cardx != card && cardx.suit == suit;
                                });
                            });
                            var hs = player.getCards('h');
                            if (player.hp > 2) {
                                //杀
                                for (var i = 0; i < hs.length; i++) {
                                    if (
                                        !game.hasPlayer(function (current) {
                                            return current.hasCard(function (cardx) {
                                                return cardx != card && cardx.suit == hs[i].suit;
                                            }, 'e');
                                        })
                                    )
                                        continue;
                                    if (hs[i].name != 'sha') continue;
                                    var shu = game.countPlayer(function (current) {
                                        return current.countCards('e', function (cardx) {
                                            return cardx != card && cardx.suit == hs[i].suit;
                                        });
                                    });
                                    var max = 0;
                                    var list = [];
                                    for (var j = 1; j < shu.length; j++) {
                                        if (shu[j] > max) {
                                            max = shu[j];
                                        }
                                    }
                                    if (
                                        game.countPlayer(function (current) {
                                            return current.countCards('e', function (cardx) {
                                                return cardx != card && cardx.suit == hs[i].suit;
                                            });
                                        }) == max
                                    )
                                        list.add(i);
                                    var list = list.sort(function (a, b) {
                                        return b.number - a.number;
                                    });
                                    var ka = list[0];
                                    if (card == ka) return num + 5 * es;
                                    if (player.hasCard(ka) && card.name == 'sha' && ka.suit == suit && card != ka) return num - 20;
                                }
                                //闪
                                for (var i = 0; i < hs.length; i++) {
                                    if (
                                        !game.hasPlayer(function (current) {
                                            return current.hasCard(function (cardx) {
                                                return cardx != card && cardx.suit == hs[i].suit;
                                            }, 'e');
                                        })
                                    )
                                        continue;
                                    if (hs[i].name != 'shan') continue;
                                    var shu = game.countPlayer(function (current) {
                                        return current.countCards('e', function (cardx) {
                                            return cardx != card && cardx.suit == hs[i].suit;
                                        });
                                    });
                                    var max = 0;
                                    var list = [];
                                    for (var j = 1; j < shu.length; j++) {
                                        if (shu[j] > max) {
                                            max = shu[j];
                                        }
                                    }
                                    if (
                                        game.countPlayer(function (current) {
                                            return current.countCards('e', function (cardx) {
                                                return cardx != card && cardx.suit == hs[i].suit;
                                            });
                                        }) == max
                                    )
                                        list.add(i);
                                    var list = list.sort(function (a, b) {
                                        return b.number - a.number;
                                    });
                                    var ka = list[0];
                                    if (card == ka) return num + 3 * es;
                                    if (player.hasCard(ka) && card.name == 'shan' && ka.suit == suit && card != ka) return num - 20;
                                }
                                //桃
                                for (var i = 0; i < hs.length; i++) {
                                    if (
                                        !game.hasPlayer(function (current) {
                                            return current.hasCard(function (cardx) {
                                                return cardx != card && cardx.suit == hs[i].suit;
                                            }, 'e');
                                        })
                                    )
                                        continue;
                                    if (hs[i].name != 'tao') continue;
                                    var shu = game.countPlayer(function (current) {
                                        return current.countCards('e', function (cardx) {
                                            return cardx != card && cardx.suit == hs[i].suit;
                                        });
                                    });
                                    var max = 0;
                                    var list = [];
                                    for (var j = 1; j < shu.length; j++) {
                                        if (shu[j] > max) {
                                            max = shu[j];
                                        }
                                    }
                                    if (
                                        game.countPlayer(function (current) {
                                            return current.countCards('e', function (cardx) {
                                                return cardx != card && cardx.suit == hs[i].suit;
                                            });
                                        }) == max
                                    )
                                        list.add(i);
                                    var list = list.sort(function (a, b) {
                                        return b.number - a.number;
                                    });
                                    var ka = list[0];
                                    if (card == ka) return num + 4 * es;
                                    if (
                                        player.hasCard(ka) &&
                                        card.name == 'tao' &&
                                        ka.suit == suit &&
                                        card != ka &&
                                        !game.hasPlayer(function (current) {
                                            return current != player && get.attitude(player, current) > 0 && current.hp < 3;
                                        })
                                    )
                                        return num - 20;
                                    if (
                                        player.hasCard(ka) &&
                                        card.name == 'tao' &&
                                        ka.suit == suit &&
                                        card != ka &&
                                        game.hasPlayer(function (current) {
                                            return current != player && get.attitude(player, current) > 0 && current.hp < 3;
                                        })
                                    )
                                        return num + 10;
                                }
                                //酒
                                for (var i = 0; i < hs.length; i++) {
                                    if (
                                        !game.hasPlayer(function (current) {
                                            return current.hasCard(function (cardx) {
                                                return cardx != card && cardx.suit == hs[i].suit;
                                            }, 'e');
                                        })
                                    )
                                        continue;
                                    if (hs[i].name != 'jiu') continue;
                                    var shu = game.countPlayer(function (current) {
                                        return current.countCards('e', function (cardx) {
                                            return cardx != card && cardx.suit == hs[i].suit;
                                        });
                                    });
                                    var max = 0;
                                    var list = [];
                                    for (var j = 1; j < shu.length; j++) {
                                        if (shu[j] > max) {
                                            max = shu[j];
                                        }
                                    }
                                    if (
                                        game.countPlayer(function (current) {
                                            return current.countCards('e', function (cardx) {
                                                return cardx != card && cardx.suit == hs[i].suit;
                                            });
                                        }) == max
                                    )
                                        list.add(i);
                                    var list = list.sort(function (a, b) {
                                        return b.number - a.number;
                                    });
                                    var ka = list[0];
                                    if (card == ka) return num + 4 * es;
                                    if (player.hasCard(ka) && card.name == 'jiu' && ka.suit == suit && card != ka) return num - 20;
                                }
                                //无懈
                                for (var i = 0; i < hs.length; i++) {
                                    if (
                                        !game.hasPlayer(function (current) {
                                            return current.hasCard(function (cardx) {
                                                return cardx != card && cardx.suit == hs[i].suit;
                                            }, 'e');
                                        })
                                    )
                                        continue;
                                    if (hs[i].name != 'wuxie') continue;
                                    var shu = game.countPlayer(function (current) {
                                        return current.countCards('e', function (cardx) {
                                            return cardx != card && cardx.suit == hs[i].suit;
                                        });
                                    });
                                    var max = 0;
                                    var list = [];
                                    for (var j = 1; j < shu.length; j++) {
                                        if (shu[j] > max) {
                                            max = shu[j];
                                        }
                                    }
                                    if (
                                        game.countPlayer(function (current) {
                                            return current.countCards('e', function (cardx) {
                                                return cardx != card && cardx.suit == hs[i].suit;
                                            });
                                        }) == max
                                    )
                                        list.add(i);
                                    var list = list.sort(function (a, b) {
                                        return b.number - a.number;
                                    });
                                    var ka = list[0];
                                    if (card == ka) return num + 4 * es;
                                    if (player.hasCard(ka) && card.name == 'wuxie' && ka.suit == suit && card != ka) return num - 20;
                                }
                            }
                        },
                        aiOrder(player, card, num) {
                            var suit = card.suit;
                            var hs = player.countCards('h', function (cardx) {
                                return cardx.suit == suit;
                            });
                            var es = game.countPlayer(function (current) {
                                return current.countCards('e', function (cardx) {
                                    return cardx != card && cardx.suit == suit;
                                });
                            });
                            var canusek = player.getCards('h', function (cardx) {
                                return get.type(cardx) == 'trick' || get.type(cardx) == 'delay';
                            });
                            if (get.type(card) == 'equip') {
                                var stp = get.subtype(card);
                                if (!player.getEquip(stp)) return num + 50;
                            }
                            if (
                                game.hasPlayer(function (current) {
                                    return current.hasCard(function (cardx) {
                                        return cardx != card && cardx.suit == suit;
                                    }, 'e');
                                })
                            ) {
                                if (hs == 1) {
                                    if (card.name == 'sha') {
                                        if (player.getEquip('zhuge') || player.getEquip('qinglong')) return num + 2 * es;
                                        return num + 0.1;
                                    }
                                    if (card.name != 'sha' || card.name != 'jiu') return num + 7 * es;
                                }
                                if (card.name == 'jiu') {
                                    if (hs == 1) return num + 3 * es;
                                    if (player.hasCard('sha', 'hes')) return get.order({ name: 'sha' }) + 0.4;
                                }
                                if (!['tao', 'shan', 'wuxie', 'jiu', 'sha'].includes(card.name)) {
                                    if (
                                        player.hasCard(function (cardx) {
                                            return;
                                            cardx != card && cardx.suit == suit && cardx.name != 'tao' && cardx.name != 'shan' && cardx.name != 'wuxie';
                                        }, 'h')
                                    ) {
                                        if (
                                            player.countCards('h', function (cardx) {
                                                return cardx != card && cardx.suit == suit && cardx.name == 'jiu';
                                            }) == 0 ||
                                            (player.countCards('h', function (cardx) {
                                                return cardx != card && cardx.suit == suit && cardx.name == 'jiu';
                                            }) == 1 &&
                                                player.countUsed('jiu', true) == 0) ||
                                            player.countCards('h', function (cardx) {
                                                return cardx != card && cardx.suit == suit && cardx.name == 'sha';
                                            }) == 0 ||
                                            (player.countCards('h', function (cardx) {
                                                return cardx != card && cardx.suit == suit && cardx.name == 'sha';
                                            }) == 1 &&
                                                player.countUsed('sha', true) == 0)
                                        ) {
                                            if (
                                                get.effect(target, card, player, player) >= 0 &&
                                                !player.hasCard(function (cardx) {
                                                    return cardx != card && cardx.suit == suit && get.effect(target, cardx, player, player) < 0;
                                                }, 'h')
                                            ) {
                                                if (get.type(card) == 'equip') return num + (15 * es) / hs;
                                                return num + (7 * es) / hs;
                                            }
                                        }
                                    }
                                }
                            }
                        },
                    },
                    ai: {
                        effect: {
                            player(card, player, target) {
                                var suit = card.suit;
                                var hs = player.countCards('h', function (cardx) {
                                    return cardx.suit == suit;
                                });
                                var sd = game.countPlayer(function (current) {
                                    return current.hasSkillTag('rejudge') && get.attitude(current, player) < 0;
                                });
                                var canusek = player.getCards('h', function (cardx) {
                                    return cardx.suit == suit;
                                });
                                if (target) {
                                    var att = get.attitude(player, target);
                                    if (
                                        game.hasPlayer(function (current) {
                                            return get.attitude(player, current) <= 0 && current.hasSkill('shouli_thunder');
                                        })
                                    ) {
                                        if (get.tag(card, 'damage') && att <= 0) {
                                            if (player.inRange(target)) {
                                                if (target.hasSkill('shouli_thunder')) {
                                                    if (!target.hasSkillTag('nodamage' && 'filterDamage' && 'nothunder')) return [2, 2, 4, -4];
                                                    if (target.hasSkillTag('nodamage' || 'nothunder')) return 'zeroplayertarget';
                                                }
                                                if (!target.hasSkill('shouli_thunder')) {
                                                    if (
                                                        !game.hasPlayer(function (current) {
                                                            return current.hasSkill('shouli_thunder') && !current.hasSkillTag('nodamage' || 'filterDamage' || 'nothunder');
                                                        })
                                                    )
                                                        return 'zeroplayertarget';
                                                    return [1, 0, 1, 0];
                                                }
                                            }
                                        }
                                    }
                                } //QQQ
                                if (
                                    game.hasPlayer(function (current) {
                                        return current.hasCard(function (cardx) {
                                            return cardx != card && cardx.suit == suit;
                                        }, 'e');
                                    })
                                ) {
                                    if (get.type(card) == 'equip') {
                                        var stp = get.subtype(card);
                                        if (
                                            player.getEquip(stp) &&
                                            !player.hasCard(function (card) {
                                                return card == canusek && !player.hasUseTarget(card);
                                            })
                                        ) {
                                            if (get.subtype(card) == 'equip1') {
                                                if (player.getEquip('zhuge') && (player.hasValueTarget({ name: 'sha' }) || player.hasCard('wuzhong', 'hes'))) return 'zeroplayertarget';
                                                if (
                                                    card.name == 'zhuge' &&
                                                    !game.hasPlayer(function (current) {
                                                        return get.distance(player, current) <= 1 && player.canUse('sha', current) && get.effect(current, { name: 'sha' }, player, player) > 0;
                                                    })
                                                )
                                                    return 'zeroplayertarget';
                                            }
                                            return [1, 5];
                                        }
                                        return [0, 0];
                                    }
                                    if (card.name == 'jiu' && hs == 1) return [1, 2];
                                    if (card.name == 'tiesuo' && hs == 1) return [2, 0, 2, 0];
                                    if (card.name == 'shandian') {
                                        if (sd == 0 && hs == 1) return [1, 1];
                                        return [0, -2, 0, 0];
                                    }
                                }
                            },
                            target(card, player, target) {
                                if (
                                    card.name == 'sha' &&
                                    !player.hasSkillTag(
                                        'directHit_ai',
                                        true,
                                        {
                                            target: target,
                                            card: card,
                                        },
                                        true,
                                    )
                                ) {
                                    if (
                                        game.hasPlayer(function (current) {
                                            return current.hasCard(function (cardx) {
                                                return get.subtype(cardx) == 'equip3';
                                            }, 'e');
                                        })
                                    )
                                        return [0, -0.5];
                                }
                            },
                        },
                    },
                };
                lib.skill.shouli_backup = {
                    sourceSkill: 'shouli',
                    precontent() {
                        var cards = event.result.card.cards;
                        event.result.cards = cards;
                        var owner = get.owner(cards[0]);
                        event.target = owner;
                        owner.$give(cards[0], player, false);
                        player.popup(event.result.card.name, 'metal');
                        event.parent.addCount = false;
                    },
                    filterCard() {
                        return false;
                    },
                    prompt: '请选择【杀】的目标',
                    selectCard: -1,
                };
                //曹髦
                //潜龙
                lib.skill.qianlong = {
                    audio: 2,
                    trigger: {
                        player: 'damageEnd',
                    },
                    forced: true,
                    content() {
                        'step 0';
                        var cards = get.cards(3);
                        event.cards = cards;
                        game.cardsGotoOrdering(cards);
                        //展示牌
                        game.log(player, '展示了', event.cards);
                        event.videoId = lib.status.videoId++;
                        game.broadcastAll(
                            function (player, id, cards) {
                                if (player == game.me || player.isUnderControl()) return;
                                var str = get.translation(player) + '发动了【潜龙】';
                                var dialog = ui.create.dialog(str, cards);
                                dialog.videoId = id;
                            },
                            player,
                            event.videoId,
                            event.cards,
                        );
                        game.addVideo('showCards', player, [get.translation(player) + '发动了【潜龙】', get.cardsInfo(event.cards)]);
                        if (player != game.me && !player.isUnderControl() && !player.isOnline()) game.delay(2);
                        //选牌
                        var next = player.chooseToMove('潜龙:获得至多' + get.cnNumber(Math.min(3, player.getDamagedHp())) + '张牌并将其余牌置于牌堆底');
                        next.set('list', [['置于牌堆底', cards], ['自己获得']]);
                        next.set('filterMove', function (from, to, moved) {
                            if (moved[0].includes(from.link)) {
                                if (typeof to == 'number') {
                                    if (to == 1) {
                                        if (moved[1].length >= _status.event.player.getDamagedHp()) return false;
                                    }
                                    return true;
                                }
                            }
                            return true;
                        });
                        next.set('processAI', function (list) {
                            var cards = list[0][1].slice(0),
                                player = _status.event.player;
                            cards.sort(function (a, b) {
                                return get.value(b, player) - get.value(a, player);
                            });
                            if (player.hasCard('sha', 'hes') && player.storage.juetao == false) {
                                var gain,
                                    bottom = [];
                                var pai = cards.filter(function (card) {
                                    return card.name != 'sha';
                                });
                                if (pai.length <= player.getDamagedHp()) {
                                    var gain = pai;
                                } else {
                                    pai.sort(function (a, b) {
                                        return get.value(b, player) - get.value(a, player);
                                    });
                                    var gain = pai.splice(0, player.getDamagedHp());
                                    var bottom = bottom.push(pai);
                                }
                                return [bottom, gain];
                            } else {
                                return [cards, cards.splice(0, _status.event.player.getDamagedHp())];
                            }
                        });
                        ('step 1');
                        game.broadcastAll('closeDialog', event.videoId);
                        game.addVideo('cardDialog', null, event.videoId);
                        var moved = result.moved;
                        if (moved[0].length) {
                            for (var i of moved[0]) {
                                i.fix();
                                ui.cardPile.appendChild(i);
                            }
                        }
                        if (moved[1].length) player.gain(moved[1], 'gain2');
                    },
                    ai: {
                        maixie: true,
                        maixie_hp: true,
                        effect: {
                            target(card, player, target) {
                                if (get.tag(card, 'damage')) {
                                    if (player.hasSkillTag('jueqing', false, target)) return;
                                    if (!target.hasFriend()) return;
                                    var num = 1;
                                    if (!player.needsToDiscard() && target.isDamaged()) {
                                        num = 0.7;
                                    } else {
                                        num = 0.5;
                                    }
                                    if (target.hp >= 4) return [1, num * 2];
                                    if (target.hp == 3) return [1, num * 1.5];
                                    if (target.hp == 2) return [1, num * 0.5];
                                }
                            },
                        },
                    },
                };
                //决讨
                lib.skill.juetao = {
                    audio: 2,
                    trigger: {
                        player: 'phaseUseBegin',
                    },
                    forced: true,
                    limited: true,
                    filter(event, player) {
                        return player.hp == 1;
                    },
                    content() {
                        'step 0';
                        player.chooseTarget(get.prompt2('juetao'), lib.filter.notMe).set('ai', function (target) {
                            var att = get.attitude(_status.event.player, target);
                            if (att < 0) {
                                if (!target.hasSkillTag('nodamage')) {
                                    if (target.getEquip('tengjia')) return -att;
                                    if (target.getEquip('bagua')) return 6 - att;
                                    if (target.getEquip('renwang')) return 4 - att;
                                    if (!target.getEquip(2) || target.getEquip('baiyin' || 'rewrite_baiyin')) return 10 - att;
                                    return 3 - att;
                                } else {
                                    return 0.5;
                                }
                            } else {
                                return 0;
                            }
                        });
                        ('step 1');
                        if (result.bool) {
                            var target = result.targets[0];
                            event.target = target;
                            player.awakenSkill('juetao');
                        } else event.finish();
                        ('step 2');
                        var card = get.bottomCards()[0];
                        game.cardsGotoOrdering(card);
                        player.showCards(card);
                        player
                            .chooseUseTarget(card, true, false, 'nodistance')
                            .set('filterTarget', function (card, player, target) {
                                var evt = _status.event;
                                if (_status.event.name == 'chooseTarget') evt = evt.parent;
                                if (target != player && target != evt.juetao_target) return false;
                                return lib.filter.targetEnabledx(card, player, target);
                            })
                            .set('juetao_target', target);
                        ('step 3');
                        if (result.bool && target.isIn()) event.goto(2);
                    },
                    mark: true,
                    intro: {
                        content: 'limited',
                    },
                    init(player, skill) {
                        player.storage[skill] = false;
                    },
                };

                lib.card.ly_piliche = {
                    fullskin: true,
                    derivation: 'liuye',
                    type: 'equip',
                    subtype: 'equip1',
                    distance: {
                        attackFrom: -8,
                    },
                    skills: ['ly_piliche'],
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                    ai: {
                        equipValue: 9,
                        basic: {
                            order: 9,
                            useful: 2,
                            equipValue: 9,
                            value: 9,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                };

                lib.card.pilitoushiche = {
                    fullskin: true,
                    derivation: 'dc_liuye',
                    cardimage: 'ly_piliche',
                    type: 'equip',
                    subtype: 'equip5',
                    skills: ['pilitoushiche'],
                    ai: {
                        equipValue: 9,
                        basic: {
                            equipValue: 9,
                            order: 9,
                            useful: 2,
                            value: 9,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                };

                lib.card.dagongche = {
                    fullskin: true,
                    derivation: 'zhangfen',
                    type: 'equip',
                    subtype: 'equip5',
                    skills: ['dagongche_skill'],
                    cardPrompt(card) {
                        if (!card.storage) return '出牌阶段开始时,你可以视为使用一张【杀】,且当此【杀】因执行效果而对目标角色造成伤害后,你弃置其一张牌.若此【大攻车】未被强化,则其他角色无法弃置你装备区内的【大攻车】.当此牌离开你的装备区后,销毁之.';
                        var str = '出牌阶段开始时,你可以视为使用一张';
                        if (card.storage.大攻车选项一) str += '无距离限制且无视防具的';
                        str += '【杀】';
                        if (card.storage.大攻车选项二) str += '(此【杀】的目标上限+' + card.storage.大攻车选项二 + ')';
                        str += ',且当此【杀】因执行效果而对目标角色造成伤害后,你弃置其';
                        var num = 1;
                        if (card.storage.大攻车选项三) num += card.storage.大攻车选项三;
                        str += get.cnNumber(num);
                        str += '张牌.当此牌离开你的装备区后,销毁之.';
                        return str;
                    },
                    ai: {
                        equipValue: 9,
                        basic: {
                            equipValue: 9,
                            order: 9,
                            useful: 2,
                            value: 9,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                };

                lib.card.guding = {
                    fullskin: true,
                    type: 'equip',
                    subtype: 'equip1',
                    distance: {
                        attackFrom: -1,
                    },
                    ai: {
                        equipValue(card, player) {
                            if (
                                !game.hasPlayer(function (current) {
                                    if (!player.hasCard({ subtype: 'equip4' }, 'he')) return current != player && get.distance(player, current) <= 2 && get.attitude(player, current) < 0;
                                    return current != player && get.distance(player, current) <= 3 && get.attitude(player, current) < 0;
                                })
                            )
                                return 1;
                            if (!player.hasCard({ subtype: 'equip4' }, 'he')) {
                                for (var i = 0; i < game.players.length; i++) {
                                    if (!(game.players[i] != player && game.players[i].countCards('h') <= 0 && get.distance(player, game.players[i]) <= 2 && get.attitude(player, game.players[i]) < 0)) continue;
                                    var target1 = game.players[i];
                                    if (
                                        game.hasPlayer(function (current) {
                                            return current == target1;
                                        })
                                    ) {
                                        if (
                                            game.hasPlayer(function (current) {
                                                return (
                                                    current == target1 &&
                                                    player.hasCard(function (card) {
                                                        return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                    }) &&
                                                    player.canUse('sha', current) &&
                                                    player.getCardUsable('sha') >= 1
                                                );
                                            })
                                        ) {
                                            if (
                                                player.getEquip('zhuge') &&
                                                player.countCards('h', function (card) {
                                                    return card.name == 'sha' && player.hasValueTarget(card);
                                                }) >= 3
                                            )
                                                return 4.5;
                                            return 11;
                                        }
                                        return 4.5;
                                    }
                                }
                            } else {
                                for (var j = 0; j < game.players.length; j++) {
                                    if (!(game.players[j] != player && game.players[j].countCards('h') <= 0 && get.distance(player, game.players[j]) <= 3 && get.attitude(player, game.players[j]) < 0)) continue;
                                    var target2 = game.players[j];
                                    if (
                                        game.hasPlayer(function (current) {
                                            return current == target2;
                                        })
                                    ) {
                                        if (
                                            game.hasPlayer(function (current) {
                                                return (
                                                    current == target2 &&
                                                    player.hasCard(function (card) {
                                                        return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                    }) &&
                                                    player.canUse('sha', current) &&
                                                    player.getCardUsable('sha') >= 1
                                                );
                                            })
                                        ) {
                                            if (
                                                player.getEquip('zhuge') &&
                                                player.countCards('h', function (card) {
                                                    return card.name == 'sha' && player.hasValueTarget(card);
                                                }) >= 3
                                            )
                                                return 4.5;
                                            return 11;
                                        }
                                        return 4.5;
                                    }
                                }
                            }
                            if (
                                (player.hasSkill('repojun') || player.hasSkill('pojun')) &&
                                game.hasPlayer(function (current) {
                                    if (!player.hasCard({ subtype: 'equip4' }, 'he')) return current != player && get.distance(player, current) <= 2 && get.attitude(player, current) < 0;
                                    return current != player && get.distance(player, current) <= 3 && get.attitude(player, current) < 0;
                                })
                            ) {
                                if (
                                    player.hasCard({ name: 'zhuge' }) &&
                                    player.getCardUsable('sha') == 0 &&
                                    player.countCards('h', function (card) {
                                        return card.name == 'sha' && player.hasValueTarget(card);
                                    }) >= 2
                                )
                                    return 4.5;
                                return 12;
                            }
                            return 2.5;
                        },
                        basic: {
                            equipValue: 2,
                            order: 2,
                            useful: 2,
                            value: 2,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                    skills: ['guding_skill'],
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                };

                lib.card.zhuge = {
                    fullskin: true,
                    type: 'equip',
                    subtype: 'equip1',
                    ai: {
                        order() {
                            return get.order({ name: 'sha' }) - 0.1;
                        },
                        equipValue(card, player) {
                            if (player._zhuge_temp) return 1;
                            player._zhuge_temp = true;
                            var result = (function () {
                                if (
                                    !game.hasPlayer(function (current) {
                                        if (!player.hasCard({ subtype: 'equip4' }, 'he')) return current != player && get.distance(player, current) <= 1 && get.attitude(player, current) < 0;
                                        return current != player && get.distance(player, current) <= 2 && get.attitude(player, current) < 0;
                                    })
                                )
                                    return 1;
                                if (player.hasSha() && _status.currentPhase == player) {
                                    if (
                                        !player.hasCard({ subtype: 'equip4' }, 'he') &&
                                        player.getCardUsable('sha') == 0 &&
                                        game.hasPlayer(function (current) {
                                            return (
                                                current != player &&
                                                get.distance(player, current) <= 1 &&
                                                get.attitude(player, current) < 0 &&
                                                player.hasCard(function (card) {
                                                    return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                }) &&
                                                player.canUse('sha', current)
                                            );
                                        })
                                    )
                                        return 10;
                                    if (
                                        player.hasCard({ subtype: 'equip4' }, 'he') &&
                                        player.getCardUsable('sha') == 0 &&
                                        game.hasPlayer(function (current) {
                                            return (
                                                current != player &&
                                                get.distance(player, current) <= 2 &&
                                                get.attitude(player, current) < 0 &&
                                                player.hasCard(function (card) {
                                                    return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                }) &&
                                                player.canUse('sha', current)
                                            );
                                        })
                                    )
                                        return 10;
                                }
                                if (!(player.hasSha() || player.hasValueTarget('sha')) && _status.currentPhase == player && player.hasCard({ subtype: 'equip1' })) return 0;
                                var num = player.countCards('h', 'sha');
                                if (num > 1) return Math.min(4 + num, 10);
                                return 1 + num;
                            })();
                            delete player._zhuge_temp;
                            return result;
                        },
                        basic: {
                            equipValue: 5,
                            order: 5,
                            useful: 2,
                            value: 5,
                        },
                        tag: {
                            valueswap: 1,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                    skills: ['zhuge_skill'],
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                };

                lib.card.guanshi = {
                    fullskin: true,
                    type: 'equip',
                    subtype: 'equip1',
                    distance: {
                        attackFrom: -2,
                    },
                    ai: {
                        equipValue(card, player) {
                            var he = player.countCards('he', function (card) {
                                return card.name != 'guanshi';
                            });
                            var num = Math.max(he - 1, 2);
                            if (
                                !game.hasPlayer(function (current) {
                                    if (!player.hasCard({ subtype: 'equip4' }, 'he')) return current != player && get.distance(player, current) <= 3 && get.attitude(player, current) < 0;
                                    return current != player && get.distance(player, current) <= 4 && get.attitude(player, current) < 0;
                                })
                            )
                                return 2;
                            if (!player.hasCard({ subtype: 'equip4' }, 'he')) {
                                if (
                                    game.hasPlayer(function (current) {
                                        return current != player && get.distance(player, current) <= 3 && get.attitude(player, current) < 0;
                                    })
                                ) {
                                    if (
                                        game.hasPlayer(function (current) {
                                            return (
                                                current != player &&
                                                get.distance(player, current) <= 3 &&
                                                get.attitude(player, current) < 0 &&
                                                player.hasCard(function (card) {
                                                    return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                }) &&
                                                player.canUse('sha', current) &&
                                                current.hasShan() &&
                                                he > 2 &&
                                                player.getCardUsable('sha') >= 1
                                            );
                                        })
                                    ) {
                                        if (
                                            player.getEquip('zhuge') &&
                                            player.countCards('h', function (card) {
                                                return card.name == 'sha' && player.hasValueTarget(card);
                                            }) >= 2
                                        )
                                            return Math.min(num, 5);
                                        return Math.min(num + 2, 12);
                                    }
                                    if (
                                        _status.currentPhase != player &&
                                        game.hasPlayer(function (current) {
                                            return current != player && get.distance(player, current) <= 3 && get.attitude(player, current) < 0 && current.hp == 1;
                                        })
                                    )
                                        return 10;
                                    return Math.min(num, 5);
                                }
                            } else {
                                if (
                                    game.hasPlayer(function (current) {
                                        return current != player && get.distance(player, current) <= 4 && get.attitude(player, current) < 0;
                                    })
                                ) {
                                    if (
                                        game.hasPlayer(function (current) {
                                            return (
                                                current != player &&
                                                get.distance(player, current) <= 4 &&
                                                get.attitude(player, current) < 0 &&
                                                player.hasCard(function (card) {
                                                    return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                }) &&
                                                player.canUse('sha', current) &&
                                                current.hasShan() &&
                                                he > 2 &&
                                                player.getCardUsable('sha') >= 1
                                            );
                                        })
                                    ) {
                                        if (
                                            player.getEquip('zhuge') &&
                                            player.countCards('h', function (card) {
                                                return card.name == 'sha' && player.hasValueTarget(card);
                                            }) >= 2
                                        )
                                            return Math.min(num, 5);
                                        return Math.min(num + 2, 12);
                                    }
                                    if (
                                        _status.currentPhase != player &&
                                        game.hasPlayer(function (current) {
                                            return current != player && get.distance(player, current) <= 4 && get.attitude(player, current) < 0 && current.hp == 1;
                                        })
                                    )
                                        return 10;
                                    return Math.min(num, 5);
                                }
                            }
                            return 4.8;
                        },
                        basic: {
                            equipValue: 4.5,
                            order: 4.5,
                            useful: 2,
                            value: 4.5,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                    skills: ['guanshi_skill'],
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                };

                lib.card.zhangba = {
                    fullskin: true,
                    type: 'equip',
                    subtype: 'equip1',
                    distance: {
                        attackFrom: -2,
                    },
                    ai: {
                        equipValue(card, player) {
                            var num = Math.min(2.5 + player.countCards('h') / 3, 6);
                            if (
                                !game.hasPlayer(function (current) {
                                    if (!player.hasCard({ subtype: 'equip4' }, 'he')) return current != player && get.distance(player, current) <= 3 && get.attitude(player, current) < 0;
                                    return current != player && get.distance(player, current) <= 4 && get.attitude(player, current) < 0;
                                })
                            )
                                return 1.5;
                            if (!player.hasCard({ subtype: 'equip4' }, 'he')) {
                                if (
                                    game.hasPlayer(function (current) {
                                        return current != player && get.distance(player, current) <= 3 && get.attitude(player, current) < 0;
                                    })
                                ) {
                                    if (
                                        game.hasPlayer(function (current) {
                                            return (
                                                current != player &&
                                                get.distance(player, current) <= 3 &&
                                                get.attitude(player, current) < 0 &&
                                                (current.getEquip('renwang') || current.getEquip('rewrite_renwang')) &&
                                                player.canUse('sha', current) &&
                                                !player.hasCard(function (card) {
                                                    return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                })
                                            );
                                        })
                                    )
                                        return Math.max(num, 5);
                                    return 3.1;
                                }
                            } else {
                                if (
                                    game.hasPlayer(function (current) {
                                        return current != player && get.distance(player, current) <= 4 && get.attitude(player, current) < 0;
                                    })
                                ) {
                                    if (
                                        game.hasPlayer(function (current) {
                                            return (
                                                current != player &&
                                                get.distance(player, current) <= 4 &&
                                                get.attitude(player, current) < 0 &&
                                                (current.getEquip('renwang') || current.getEquip('rewrite_renwang')) &&
                                                player.canUse('sha', current) &&
                                                !player.hasCard(function (card) {
                                                    return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                })
                                            );
                                        })
                                    )
                                        return Math.max(num, 5);
                                    return 3.1;
                                }
                            }
                            if (player.hasSkill('xinfu_tushe')) return 12;
                            return 3.1;
                        },
                        basic: {
                            equipValue: 3.5,
                            order: 3.5,
                            useful: 2,
                            value: 3.5,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                    skills: ['zhangba_skill'],
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                };

                lib.card.cixiong = {
                    fullskin: true,
                    type: 'equip',
                    subtype: 'equip1',
                    distance: {
                        attackFrom: -1,
                    },
                    ai: {
                        equipValue(card, player) {
                            if (
                                !game.hasPlayer(function (current) {
                                    if (!player.hasCard({ subtype: 'equip4' }, 'he')) return current != player && get.distance(player, current) <= 2 && get.attitude(player, current) < 0;
                                    return current != player && get.distance(player, current) <= 3 && get.attitude(player, current) < 0;
                                })
                            )
                                return 1;
                            if (!player.hasCard({ subtype: 'equip4' }, 'he')) {
                                if (
                                    game.hasPlayer(function (current) {
                                        return current != player && get.distance(player, current) <= 2 && get.attitude(player, current) < 0;
                                    })
                                ) {
                                    if (
                                        game.hasPlayer(function (current) {
                                            return (
                                                current != player &&
                                                get.distance(player, current) <= 2 &&
                                                get.attitude(player, current) < 0 &&
                                                player.differentSexFrom(current) &&
                                                player.canUse('sha', current) &&
                                                player.hasCard(function (card) {
                                                    return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                })
                                            );
                                        })
                                    )
                                        return 5;
                                    return 2.5;
                                }
                            } else {
                                if (
                                    game.hasPlayer(function (current) {
                                        return current != player && get.distance(player, current) <= 3 && get.attitude(player, current) < 0;
                                    })
                                ) {
                                    if (
                                        game.hasPlayer(function (current) {
                                            return (
                                                current != player &&
                                                get.distance(player, current) <= 3 &&
                                                get.attitude(player, current) < 0 &&
                                                player.differentSexFrom(current) &&
                                                player.canUse('sha', current) &&
                                                player.hasCard(function (card) {
                                                    return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                })
                                            );
                                        })
                                    )
                                        return 5;
                                    return 2.5;
                                }
                            }
                            return 2;
                        },
                        basic: {
                            equipValue: 2,
                            order: 2,
                            useful: 2,
                            value: 2,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                    skills: ['cixiong_skill'],
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                };

                lib.card.qinggang = {
                    fullskin: true,
                    type: 'equip',
                    subtype: 'equip1',
                    distance: {
                        attackFrom: -1,
                    },
                    ai: {
                        equipValue(card, player) {
                            if (
                                !game.hasPlayer(function (current) {
                                    if (!player.hasCard({ subtype: 'equip4' }, 'he')) return current != player && get.distance(player, current) <= 2 && get.attitude(player, current) < 0;
                                    return current != player && get.distance(player, current) <= 3 && get.attitude(player, current) < 0;
                                })
                            )
                                return 1.9;
                            if (!player.hasCard({ subtype: 'equip4' }, 'he')) {
                                if (
                                    game.hasPlayer(function (current) {
                                        return current != player && get.distance(player, current) <= 2 && get.attitude(player, current) < 0;
                                    })
                                ) {
                                    if (
                                        game.hasPlayer(function (current) {
                                            return (
                                                current != player &&
                                                get.distance(player, current) <= 2 &&
                                                get.attitude(player, current) < 0 &&
                                                current.getEquip(2) &&
                                                player.canUse('sha', current) &&
                                                !player.hasCard(function (card) {
                                                    return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                })
                                            );
                                        })
                                    )
                                        return 8;
                                    return 4.5;
                                }
                            } else {
                                if (
                                    game.hasPlayer(function (current) {
                                        return current != player && get.distance(player, current) <= 3 && get.attitude(player, current) < 0;
                                    })
                                ) {
                                    if (
                                        game.hasPlayer(function (current) {
                                            return (
                                                current != player &&
                                                get.distance(player, current) <= 3 &&
                                                get.attitude(player, current) < 0 &&
                                                current.getEquip(2) &&
                                                player.canUse('sha', current) &&
                                                !player.hasCard(function (card) {
                                                    return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                })
                                            );
                                        })
                                    )
                                        return 8;
                                    return 4.5;
                                }
                            }
                            return 4.5;
                        },
                        basic: {
                            equipValue: 2,
                            order: 2,
                            useful: 2,
                            value: 2,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                    skills: ['qinggang_skill'],
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                };

                lib.card.zhuque = {
                    fullskin: true,
                    type: 'equip',
                    subtype: 'equip1',
                    distance: {
                        attackFrom: -3,
                    },
                    ai: {
                        equipValue(card, player) {
                            if (
                                !game.hasPlayer(function (current) {
                                    if (!player.hasCard({ subtype: 'equip4' }, 'he')) return current != player && get.distance(player, current) <= 4 && get.attitude(player, current) < 0;
                                    return current != player && get.distance(player, current) <= 5 && get.attitude(player, current) < 0;
                                })
                            )
                                return 1.5;
                            if (!player.hasCard({ subtype: 'equip4' }, 'he')) {
                                if (
                                    game.hasPlayer(function (current) {
                                        return current != player && get.distance(player, current) <= 4 && get.attitude(player, current) < 0;
                                    })
                                ) {
                                    if (
                                        game.hasPlayer(function (current) {
                                            return (
                                                current != player &&
                                                get.distance(player, current) <= 4 &&
                                                get.attitude(player, current) < 0 &&
                                                current.getEquip('tengjia') &&
                                                !current.hasSkillTag('nofire') &&
                                                player.canUse('sha', current) &&
                                                !player.hasCard(function (card) {
                                                    return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                })
                                            );
                                        })
                                    )
                                        return 8.1;
                                    return 2.8;
                                }
                            } else {
                                if (
                                    game.hasPlayer(function (current) {
                                        return current != player && get.distance(player, current) <= 5 && get.attitude(player, current) < 0;
                                    })
                                ) {
                                    if (
                                        game.hasPlayer(function (current) {
                                            return (
                                                current != player &&
                                                get.distance(player, current) <= 5 &&
                                                get.attitude(player, current) < 0 &&
                                                current.getEquip('tengjia') &&
                                                !current.hasSkillTag('nofire') &&
                                                player.canUse('sha', current) &&
                                                !player.hasCard(function (card) {
                                                    return card.name == 'sha' && get.effect(current, card, player, player) > 0;
                                                })
                                            );
                                        })
                                    )
                                        return 8.1;
                                    return 2.8;
                                }
                            }
                            return 2.8;
                        },
                        basic: {
                            equipValue: 2,
                            order: 2,
                            useful: 2,
                            value: 2,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                    skills: ['zhuque_skill'],
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                };

                lib.card.tengjia = {
                    fullskin: true,
                    type: 'equip',
                    subtype: 'equip2',
                    ai: {
                        value: 1,
                        equipValue: 1,
                        basic: {
                            equipValue: 3,
                            order: 3,
                            useful: 2,
                            value: 3,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                    skills: ['tengjia1', 'tengjia2', 'tengjia3'],
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                };

                lib.card.renwang = {
                    fullskin: true,
                    type: 'equip',
                    subtype: 'equip2',
                    skills: ['renwang_skill'],
                    ai: {
                        equipValue(card, player) {
                            if (
                                game.hasPlayer(function (current) {
                                    return get.attitude(player, current) < 0 && current.inRange(player) && (current.getEquip('zhangba') || current.hasUsableCard('sha'));
                                })
                            )
                                return 7;
                            return 7.5;
                        },
                        basic: {
                            equipValue: 7.5,
                            order: 7.5,
                            useful: 2,
                            value: 7.5,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                };

                lib.card.bagua = {
                    fullskin: true,
                    type: 'equip',
                    subtype: 'equip2',
                    ai: {
                        equipValue(card, player) {
                            if (
                                game.hasPlayer(function (current) {
                                    return (
                                        get.attitude(player, current) < 0 &&
                                        current.inRange(player) &&
                                        (current.getEquip('guanshi') ||
                                            current.hasSkillTag(
                                                'directHit_ai',
                                                true,
                                                {
                                                    target: player,
                                                    card: { name: 'sha' },
                                                },
                                                true,
                                            ))
                                    );
                                })
                            )
                                return 7;
                            return 7.5;
                        },
                        basic: {
                            equipValue: 7.5,
                            order: 7.5,
                            useful: 2,
                            value: 7.5,
                        },
                        result: {
                            target(player, target, card) {
                                return get.equipResult(player, target, card.name);
                            },
                        },
                    },
                    skills: ['bagua_skill'],
                    enable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player;
                    },
                    modTarget: true,
                    allowMultiple: false,
                    content() {
                        if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                    },
                    toself: true,
                };

                lib.skill.cixiong_skill = {
                    equipSkill: true,
                    trigger: {
                        player: 'useCardToPlayered',
                    },
                    audio: true,
                    logTarget: 'target',
                    check(event, player) {
                        if (get.attitude(player, event.target) > 0) return true;
                        var target = event.target;
                        return target.countCards('h') == 0 || !target.hasSkillTag('noh');
                    },
                    filter(event, player) {
                        if (event.card.name != 'sha') return false;
                        return player.differentSexFrom(event.target);
                    },
                    content() {
                        'step 0';
                        trigger.target.chooseToDiscard('弃置一张手牌,或令' + get.translation(player) + '摸一张牌').set('ai', function (card) {
                            var trigger = _status.event.getTrigger();
                            if ((trigger.target.countCards('h', 'shan') == 1 || trigger.target.countCards('h', 'tao') == 1 || trigger.target.countCards('h', 'jiu') == 1) && trigger.target.countCards('h') == 1) {
                                return false;
                            }
                            return -get.attitude(trigger.target, trigger.player) - get.value(card);
                        });
                        ('step 1');
                        if (result.bool == false) player.draw();
                    },
                };

                lib.skill.hanbing_skill = {
                    equipSkill: true,
                    trigger: {
                        source: 'damageBegin2',
                    },
                    audio: true,
                    filter(event, player) {
                        return event.card && event.card.name == 'sha' && event.notLink() && event.player.getCards('he').length;
                    },
                    check(event, player) {
                        var target = event.player;
                        if (event.getParent(2).jiu == true) return false;
                        var eff = get.damageEffect(target, player, player, event.nature);
                        if (get.attitude(player, target) > 0) {
                            if (eff >= 0) return false;
                            return true;
                        }
                        if (eff <= 0) return true;
                        if (target.hp >= 2 && target.countCards('he') >= 2 && player.hasSkill('yunshangshi') && event.getParent(2).jiu != true) return true;
                        if (target.hp == 1) return false;
                        if (event.num > 1 || player.hasSkill('tianxianjiu') || player.hasSkill('luoyi2') || player.hasSkill('reluoyi2')) return false;
                        if (target.countCards('he') < 2) return false;
                        var num = 0;
                        var cards = target.getCards('he');
                        for (var i = 0; i < cards.length; i++) {
                            if (get.value(cards[i]) >= 6) num++;
                        }
                        if (num >= 3 && event.getParent(2).jiu != true) return true;
                        if (num >= 2 && target.hasSkillTag('maixie') && event.getParent(2).jiu != true) return true;
                        if (num >= 2 && player.hasSkill('yunshangshi') && event.getParent(2).jiu != true) return true;
                        return false;
                    },
                    logTarget: 'player',
                    content() {
                        'step 0';
                        trigger.cancel();
                        ('step 1');
                        if (trigger.player.countDiscardableCards(player, 'he')) {
                            player.line(trigger.player);
                            player.discardPlayerCard('he', trigger.player, true);
                        }
                        ('step 2');
                        if (trigger.player.countDiscardableCards(player, 'he')) {
                            player.line(trigger.player);
                            player.discardPlayerCard('he', trigger.player, true);
                        }
                    },
                };

                lib.skill.zhangba_skill = {
                    equipSkill: true,
                    enable: ['chooseToUse', 'chooseToRespond'],
                    filterCard: true,
                    selectCard: 2,
                    position: 'hs',
                    viewAs: {
                        name: 'sha',
                    },
                    complexCard: true,
                    filter(event, player) {
                        return player.countCards('hs') >= 2;
                    },
                    audio: true,
                    prompt: '将两张手牌当杀使用或打出',
                    check(card) {
                        let player = _status.event.player;
                        if (player.needsToDiscard() >= 2) return 15 - get.value(card);
                        if (
                            player.hasCard(function (card) {
                                return card.name == 'sha' || card.name == 'tao' || card.name == 'jiu';
                            })
                        )
                            return 0;
                        if (_status.currentPhase == player) {
                            if (
                                player.hasCard(function (card) {
                                    return card.name == 'wuxie' || card.name == 'wuzhong' || card.name == 'guohe' || card.name == 'shunshou' || card.name == 'bingliang' || card.name == 'lebu';
                                })
                            )
                                return 0;
                        }
                        if (
                            player.hasSkill('xinfu_tushe') &&
                            player.hasCard(function (card) {
                                return get.type2(card) != 'trick' && (card.name == 'tao' || card.name == 'jiu' || card.name == 'shan');
                            })
                        ) {
                            return 15 - get.value(card);
                        }
                        if (player.hp <= 2 && player.countCards('hs', 'sha') <= 0) return 10 - get.value(card);
                        return 5 - get.value(card);
                    },
                    ai: {
                        respondSha: true,
                        skillTagFilter(player) {
                            return player.countCards('hs') >= 2;
                        },
                        yingbian(card, player, targets, viewer) {
                            if (get.attitude(viewer, player) <= 0) return 0;
                            var base = 0,
                                hit = false;
                            if (get.cardtag(card, 'yingbian_hit')) {
                                hit = true;
                                if (
                                    targets.filter(function (target) {
                                        return target.hasShan() && get.attitude(viewer, target) < 0 && get.damageEffect(target, player, viewer, get.nature(card)) > 0;
                                    })
                                )
                                    base += 5;
                            }
                            if (get.cardtag(card, 'yingbian_all')) {
                                if (
                                    game.hasPlayer(function (current) {
                                        return !targets.includes(current) && lib.filter.targetEnabled2(card, player, current) && get.effect(current, card, player, player) > 0;
                                    })
                                )
                                    base += 5;
                            }
                            if (get.cardtag(card, 'yingbian_damage')) {
                                if (
                                    targets.filter(function (target) {
                                        return (
                                            get.attitude(player, target) < 0 &&
                                            (hit ||
                                                !target.mayHaveShan() ||
                                                player.hasSkillTag(
                                                    'directHit_ai',
                                                    true,
                                                    {
                                                        target: target,
                                                        card: card,
                                                    },
                                                    true,
                                                )) &&
                                            !target.hasSkillTag('filterDamage', null, {
                                                player: player,
                                                card: card,
                                                jiu: true,
                                            })
                                        );
                                    })
                                )
                                    base += 5;
                            }
                            return base;
                        },
                        canLink(player, target, card) {
                            if (!target.isLinked() && !player.hasSkill('wutiesuolian_skill')) return false;
                            if (
                                target.mayHaveShan() &&
                                !player.hasSkillTag(
                                    'directHit_ai',
                                    true,
                                    {
                                        target: target,
                                        card: card,
                                    },
                                    true,
                                )
                            )
                                return false;
                            if (player.hasSkill('jueqing') || player.hasSkill('gangzhi') || target.hasSkill('gangzhi')) return false;
                            return true;
                        },
                        basic: {
                            useful: [5, 3, 1],
                            value: [5, 3, 1],
                        },
                        order(item, player) {
                            if (player.hasSkillTag('presha', true, null, true)) return 10;
                            if (lib.linked.includes(get.nature(item))) {
                                if (
                                    game.hasPlayer(function (current) {
                                        return current != player && current.isLinked() && player.canUse(item, current, null, true) && get.effect(current, item, player, player) > 0 && lib.card.sha.ai.canLink(player, current, item);
                                    }) &&
                                    game.countPlayer(function (current) {
                                        return current.isLinked() && get.damageEffect(current, player, player, get.nature(item)) > 0;
                                    }) > 1
                                )
                                    return 3.1;
                                return 3;
                            }
                            return 3.05;
                        },
                        result: {
                            target(player, target, card, isLink) {
                                var eff = (function () {
                                    if (!isLink && player.hasSkill('jiu')) {
                                        if (
                                            !target.hasSkillTag('filterDamage', null, {
                                                player: player,
                                                card: card,
                                                jiu: true,
                                            })
                                        ) {
                                            if (get.attitude(player, target) > 0) {
                                                return -7;
                                            } else {
                                                return -4;
                                            }
                                        }
                                        return -0.5;
                                    }
                                    return -1.5;
                                })();
                                if (
                                    !isLink &&
                                    target.mayHaveShan() &&
                                    !player.hasSkillTag(
                                        'directHit_ai',
                                        true,
                                        {
                                            target: target,
                                            card: card,
                                        },
                                        true,
                                    )
                                )
                                    return eff / 1.2;
                                return eff;
                            },
                        },
                        tag: {
                            respond: 1,
                            respondShan: 1,
                            damage(card) {
                                if (card.nature == 'poison') return;
                                return 1;
                            },
                            natureDamage(card) {
                                if (card.nature) return 1;
                            },
                            fireDamage(card, nature) {
                                if (card.nature == 'fire') return 1;
                            },
                            thunderDamage(card, nature) {
                                if (card.nature == 'thunder') return 1;
                            },
                            poisonDamage(card, nature) {
                                if (card.nature == 'poison') return 1;
                            },
                        },
                    },
                };

                lib.skill.pyzhuren_heart = {
                    audio: true,
                    trigger: {
                        source: 'damageSource',
                    },
                    usable: 1,
                    equipSkill: true,
                    filter(event, player) {
                        return event.parent.name == 'sha';
                    },
                    check(event, player) {
                        return true;
                    },
                    content() {
                        'step 0';
                        player.judge(function (card) {
                            var player = _status.event.getParent('pyzhuren_heart').player;
                            if (player.isHealthy() && get.color(card) == 'red') return 0;
                            return 2;
                        });
                        ('step 1');
                        if (result.color == 'red') player.recover();
                        else player.draw(2);
                    },
                };

                lib.card.tao = {
                    fullskin: true,
                    type: 'basic',
                    cardcolor: 'red',
                    toself: true,
                    enable(card, player) {
                        return player.hp < player.maxHp;
                    },
                    savable: true,
                    selectTarget: -1,
                    filterTarget(card, player, target) {
                        return target == player && target.hp < target.maxHp;
                    },
                    modTarget(card, player, target) {
                        return target.hp < target.maxHp;
                    },
                    content() {
                        target.recover(event.baseDamage || 1);
                    },
                    ai: {
                        basic: {
                            order(card, player) {
                                if (player.hasSkillTag('pretao')) return 5;
                                return 2;
                            },
                            useful: [6.5, 4, 3, 2],
                            value: [6.5, 4, 3, 2],
                        },
                        result: {
                            target: 2,
                            target_use(player, target) {
                                var numt = player.countCards('hs', 'tao');
                                if (player.canSave(target)) {
                                    if (player.hasSkill('longhun') || player.hasSkill('relonghun')) {
                                        var numt =
                                            player.countCards('hs', 'tao') +
                                            player.countCards('he', function (card) {
                                                return card.suit == 'heart' && card.name != 'tao';
                                            });
                                    } else {
                                        if (
                                            player.countCards('hes', function (card) {
                                                return card.name != 'tao' && get.tag(card, 'recover') >= 1 && get.tag(card, 'save') >= 1;
                                            }) > 0
                                        ) {
                                            var numt =
                                                player.countCards('hs', 'tao') +
                                                player.countCards('hes', function (card) {
                                                    return card.name != 'tao' && get.tag(card, 'recover') >= 1 && get.tag(card, 'save') >= 1;
                                                });
                                        }
                                    }
                                }
                                var tri = _status.event.getTrigger();
                                var numz = game.countPlayer(function (current) {
                                    return current.identity == 'zhong' || current.identity == 'mingzhong';
                                });
                                var numf = game.countPlayer(function (current) {
                                    return current.identity == 'fan';
                                });
                                var cxdy = game.countPlayer(function (current) {
                                    return current.hp <= 1 && get.attitude(player, current) > 0;
                                });
                                if (cxdy >= 1 && player.hp > 1 && numt <= 1 && player == target && tri && tri.name != 'dying') {
                                    return 0;
                                }
                                if (_status.mode == 'normal') {
                                    if (player.identity == 'zhong' && player.hp >= 1 && game.zhu.hp <= 1 && numt <= 1 && player == target && tri && tri.name != 'dying') {
                                        return 0;
                                    }
                                }
                                if (tri && tri.name == 'dying') {
                                    if (target.hasSkill('spshanxi_bj') && target.countCards('he') < 2) {
                                        return 0;
                                    }
                                }
                                if (_status.mode == 'normal' && tri && tri.name == 'dying') {
                                    if (player.identity == 'zhu' && get.attitude(player, target) > 0) {
                                        if (target.identity == 'zhong' || target.identity == 'mingzhong') {
                                            if (numt + target.hp > 0 && player.hp >= 2) {
                                                return 1;
                                            } else {
                                                return 0;
                                            }
                                        }
                                        if (target.identity == 'nei' && numf >= 3 && player.hp >= 2 && target.hasSkill('shanheyonggu_tz')) {
                                            if (numt + target.hp > 0 && player.hp >= 2) {
                                                return 1;
                                            } else {
                                                return 0;
                                            }
                                        }
                                        if (target.identity == 'fan') {
                                            return 0;
                                        }
                                    }
                                    if (player.identity == 'zhong' && get.attitude(player, target) > 0) {
                                        if (target != player && (target.identity == 'zhong' || target.identity == 'mingzhong')) {
                                            if (numt + target.hp > 0) {
                                                return 1;
                                            } else {
                                                return 0;
                                            }
                                        }
                                        if (target.identity == 'zhu') {
                                            return 1;
                                        }
                                        if (target.identity == 'zhong' && target == player) {
                                            return 1;
                                        }
                                        if (target.identity == 'nei' && numf >= 3 && numz <= 1 && target.hasSkill('shanheyonggu_tz')) {
                                            if (numt + target.hp > 0 && player.hp >= 2) {
                                                return 1;
                                            } else {
                                                return 0;
                                            }
                                        }
                                        if (target.identity == 'fan') {
                                            return 0;
                                        }
                                    }
                                    if (player.identity == 'nei' && (player.hasSkill('shanheyonggu_tz') || player.hasSkill('shanheyonggu_tf'))) {
                                        if (target.identity == 'zhu') {
                                            return 1;
                                        }
                                        if (player.hasSkill('shanheyonggu_tz') && (target.identity == 'zhong' || target.identity == 'mingzhong')) {
                                            if (numt + target.hp > 0 && player.hp >= 2 && numf >= 3 && numz <= 1) {
                                                return 1;
                                            } else {
                                                return 0;
                                            }
                                        }
                                        if (player.hasSkill('shanheyonggu_tf') && target.identity == 'fan' && tri.source && tri.source != player) {
                                            if (numt + target.hp > 0 && player.hp >= 2 && numf <= 1 && numz >= 2) {
                                                return 1;
                                            } else {
                                                return 0;
                                            }
                                        }
                                    }
                                    if (player.identity == 'fan') {
                                        if (target.identity == 'fan' && tri.source && tri.source.identity == 'fan' && get.attitude(player, target) > 0) {
                                            if (target.countCards('h') >= 3 && numt + target.hp > 0) {
                                                return 1;
                                            } else {
                                                if (target.countCards('h') < 3 || numt + target.hp <= 0) {
                                                    return 0;
                                                }
                                            }
                                        }
                                        if (target.identity == 'fan' && tri.source && tri.source.identity != 'fan' && get.attitude(player, target) > 0) {
                                            if (target == player) {
                                                return 1;
                                            } else {
                                                if (target != player) {
                                                    if (numt + target.hp > 0) {
                                                        return 1;
                                                    } else {
                                                        if (numt + target.hp <= 0) {
                                                            return 0;
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                        if (target.identity == 'fan' && !tri.source && get.attitude(player, target) > 0) {
                                            if (target == player) {
                                                return 1;
                                            } else {
                                                if (target != player) {
                                                    if (numt + target.hp > 0) {
                                                        return 1;
                                                    } else {
                                                        if (numt + target.hp <= 0) {
                                                            return 0;
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                        if (target.identity == 'nei' && numf <= 1 && numz >= 2 && target.hasSkill('shanheyonggu_tf')) {
                                            if (numt + target.hp > 0 && player.hp >= 2) {
                                                return 1;
                                            }
                                        }
                                        if (target.identity == 'zhu' || target.identity == 'zhong') {
                                            return 0;
                                        }
                                    }
                                } else {
                                    if (tri && tri.name == 'dying' && get.attitude(player, target) > 0) {
                                        if (target == player) {
                                            return 1;
                                        }
                                        if (target != player) {
                                            if (numt + target.hp > 0) {
                                                return 1;
                                            } else {
                                                if (numt + target.hp <= 0) {
                                                    return 0;
                                                }
                                            }
                                        }
                                    }
                                }
                                // if(player==target&&player.hp<=0) return 2;
                                if (player.hasSkillTag('nokeep', true, null, true)) return 2;
                                var nd = player.needsToDiscard();
                                var keep = false;
                                if (nd <= 0) {
                                    keep = true;
                                } else if (nd == 1 && target.hp >= 2 && target.countCards('h', 'tao') <= 1) {
                                    keep = true;
                                }
                                var mode = get.mode();
                                if (target.hp >= 2 && keep && target.hasFriend()) {
                                    if (target.hp > 2 || nd == 0) return 0;
                                    if (target.hp == 2) {
                                        if (
                                            game.hasPlayer(function (current) {
                                                if (target != current && get.attitude(target, current) >= 3) {
                                                    if (current.hp <= 1) return true;
                                                    if ((mode == 'identity' || mode == 'versus' || mode == 'chess') && current.identity == 'zhu' && current.hp <= 2) return true;
                                                }
                                            })
                                        ) {
                                            return 0;
                                        }
                                    }
                                }
                                var att = get.attitude(player, target);
                                if (att < 3 && att >= 0 && player != target) return 0;
                                if (mode == 'identity' && player.identity == 'fan' && target.identity == 'fan') {
                                    if (tri && tri.name == 'dying' && tri.source && tri.source.identity == 'fan' && tri.source != target) {
                                        var num = game.countPlayer(function (current) {
                                            if (current.identity == 'fan') {
                                                return current.countCards('h', 'tao');
                                            }
                                        });
                                        if (num > 1 && player == target) return 2;
                                        return 0;
                                    }
                                }
                                if (mode == 'identity' && player.identity == 'zhu' && target.identity == 'nei') {
                                    if (tri && tri.name == 'dying' && tri.source && tri.source.identity == 'zhong') {
                                        return 0;
                                    }
                                }
                                if (mode == 'stone' && target.isMin() && player != target && tri && tri.name == 'dying' && player.side == target.side && tri.source != target.getEnemy()) {
                                    return 0;
                                }
                                return 2;
                            },
                        },
                        tag: {
                            recover: 1,
                            save: 1,
                        },
                    },
                };

                lib.card.juedou = {
                    audio: true,
                    fullskin: true,
                    type: 'trick',
                    enable: true,
                    yingbian_prompt: '你令此牌不可被响应',
                    yingbian_tags: ['hit'],
                    yingbian(event) {
                        event.directHit.addArray(game.players);
                    },
                    filterTarget(card, player, target) {
                        return target != player;
                    },
                    content() {
                        'step 0';
                        if (event.turn == undefined) event.turn = target;
                        if (typeof event.baseDamage != 'number') event.baseDamage = 1;
                        if (typeof event.extraDamage != 'number') {
                            event.extraDamage = 0;
                        }
                        if (!event.shaReq) event.shaReq = {};
                        if (typeof event.shaReq[player.playerid] != 'number') event.shaReq[player.playerid] = 1;
                        if (typeof event.shaReq[target.playerid] != 'number') event.shaReq[target.playerid] = 1;
                        event.playerCards = [];
                        event.targetCards = [];
                        ('step 1');
                        event.trigger('juedou');
                        event.shaRequired = event.shaReq[event.turn.playerid];
                        ('step 2');
                        if (event.directHit) {
                            event._result = { bool: false };
                        } else {
                            var next = event.turn.chooseToRespond({ name: 'sha' });
                            if (event.shaRequired > 1) {
                                next.set('prompt2', '共需打出' + event.shaRequired + '张杀');
                            }
                            next.set('ai', function (card) {
                                var event = _status.event;
                                var player = event.splayer;
                                var target = event.starget;
                                if (player.hasSkillTag('notricksource')) return 0;
                                if (target.hasSkillTag('notrick')) return 0;
                                if (event.shaRequired > 1 && player.countCards('h', 'sha') < event.shaRequired) return 0;
                                if (event.player == target) {
                                    if (player.hasSkill('naman')) return -1;
                                    if (get.attitude(target, player) < 0 || event.player.hp <= 1) {
                                        return get.order(card);
                                    }
                                    return -1;
                                } else {
                                    if (target.hasSkill('naman')) return -1;
                                    if (get.attitude(player, target) < 0 || event.player.hp <= 1) {
                                        return get.order(card);
                                    }
                                    return -1;
                                }
                            });
                            next.set('splayer', player);
                            next.set('starget', target);
                            next.set('shaRequired', event.shaRequired);
                            next.autochoose = lib.filter.autoRespondSha;
                            if (event.turn == target) {
                                next.source = player;
                            } else {
                                next.source = target;
                            }
                        }
                        ('step 3');
                        if (event.target.isDead() || event.player.isDead()) {
                            event.finish();
                        } else {
                            if (result.bool) {
                                event.shaRequired--;
                                if (event.turn == target) {
                                    if (result.cards) event.targetCards.addArray(result.cards);
                                    if (event.shaRequired > 0) event.goto(2);
                                    else {
                                        event.turn = player;
                                        event.goto(1);
                                    }
                                } else {
                                    if (result.cards) event.playerCards.addArray(result.cards);
                                    if (event.shaRequired > 0) event.goto(2);
                                    else {
                                        event.turn = target;
                                        event.goto(1);
                                    }
                                }
                            } else {
                                if (event.turn == target) {
                                    target.damage(event.baseDamage + event.extraDamage);
                                } else {
                                    player.damage(target, event.baseDamage + event.extraDamage);
                                }
                            }
                        }
                    },
                    ai: {
                        wuxie(target, card, player, viewer) {
                            if (player == game.me && get.attitude(viewer, player) > 0) {
                                return 0;
                            }
                        },
                        basic: {
                            order: 5,
                            useful: 1,
                            value: 5.5,
                        },
                        result: {
                            target: -1.5,
                            player(player, target, card) {
                                if (
                                    player.hasSkillTag(
                                        'directHit_ai',
                                        true,
                                        {
                                            target: target,
                                            card: card,
                                        },
                                        true,
                                    )
                                ) {
                                    return 0;
                                }
                                if (get.damageEffect(target, player, target) > 0 && get.attitude(player, target) > 0 && get.attitude(target, player) > 0) {
                                    return 0;
                                }
                                var hs1 = target.countCards('hs', 'sha');
                                var hs2 = player.countCards('hs', 'sha');
                                if (hs1 > hs2 + 1) {
                                    return -2;
                                }
                                if (player.hp == 1 && hs2 == 0 && hs1 >= 1) {
                                    return -2;
                                }
                                var hsx = target.countCards('hs');
                                if (hsx.length == 0) {
                                    return 0;
                                }
                                if (hsx > 3 && hs2 == 0) {
                                    return -2;
                                }
                                return -0.5;
                            },
                        },
                        tag: {
                            respond: 2,
                            respondSha: 2,
                            damage: 1,
                        },
                    },
                    selectTarget: 1,
                };
                //QQQ覆盖顺手过拆导致逐近弃远出错

                lib.card.nanman = {
                    audio: true,
                    fullskin: true,
                    type: 'trick',
                    enable: true,
                    selectTarget: -1,
                    yingbian_prompt: '当你使用此牌选择目标后,你可为此牌减少一个目标',
                    yingbian_tags: ['remove'],
                    yingbian(event) {
                        event.yingbian_removeTarget = true;
                    },
                    filterTarget(card, player, target) {
                        return target != player;
                    },
                    reverseOrder: true,
                    content() {
                        'step 0';
                        if (typeof event.baseDamage != 'number') event.baseDamage = 1;
                        if (event.directHit) event._result = { bool: false };
                        else {
                            var next = target.chooseToRespond({ name: 'sha' });
                            next.set('ai', function (card) {
                                var evt = _status.event.parent;
                                if (get.damageEffect(evt.target, evt.player, evt.target) >= 0) return 0;
                                if (evt.player.hasSkillTag('notricksource')) return 0;
                                if (evt.target.hasSkillTag('notrick')) return 0;
                                return get.order(card);
                            });
                            next.autochoose = lib.filter.autoRespondSha;
                        }
                        ('step 1');
                        if (result.bool == false) {
                            target.damage(event.baseDamage, event.customSource || player);
                        }
                    },
                    ai: {
                        wuxie(target, card, player, viewer) {
                            var cxdy = game.countPlayer(function (current) {
                                return current.hp == 1 && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && current.countCards('hs', 'sha') == 0 && get.attitude(current, player) > 0;
                            });
                            if (cxdy > 0) {
                                if (get.attitude(viewer, target) > 0 && target.hp > 1) {
                                    return 0;
                                }
                            } else {
                                if (get.attitude(viewer, target) > 0 && target != player && target.hp >= 3) {
                                    return 0;
                                }
                            }
                            if (get.attitude(viewer, target) > 0 && target.countCards('h') > 3 && target.countCards('hs', 'sha') > 0) {
                                return 0;
                            }
                            if (get.attitude(player, target) <= 0) {
                                return 0;
                            }
                        },
                        basic: {
                            order(item, player) {
                                if (player.countCards('hs', 'juedou') > 0 && player.countCards('hs', 'sha') == 0) {
                                    return get.order({ name: 'juedou' }) + 0.5;
                                }
                                return get.order({ name: 'sha' }) - 0.5;
                            },
                            useful: [5, 1],
                            value: 5,
                        },
                        result: {
                            player(player, target) {
                                var dy = game.countPlayer(function (current) {
                                    return get.attitude(current, player) > 0 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1'));
                                });
                                var dr = game.countPlayer(function (current) {
                                    return get.attitude(current, player) <= 0 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1'));
                                });
                                var mxdy = game.countPlayer(function (current) {
                                    return get.attitude(current, player) > 0 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && current.hasSkillTag('maixie') && current.hp >= 3;
                                });
                                var mxdr = game.countPlayer(function (current) {
                                    return get.attitude(current, player) <= 0 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && current.hasSkillTag('maixie') && current.hp >= 3;
                                });
                                var cxdy = game.countPlayer(function (current) {
                                    return current.hp == 1 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && get.attitude(current, player) > 0;
                                });
                                var cxdr = game.countPlayer(function (current) {
                                    return current.hp == 1 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && get.attitude(current, player) <= 0;
                                });
                                var cxzc = game.countPlayer(function (current) {
                                    return current.hp == 1 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && current.identity == 'zhong';
                                });
                                var cxfz = game.countPlayer(function (current) {
                                    return current.hp == 1 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && current.identity == 'fan';
                                });
                                var numz = game.countPlayer(function (current) {
                                    return current.identity == 'zhong' || current.identity == 'mingzhong';
                                });
                                var numf = game.countPlayer(function (current) {
                                    return current.identity == 'fan';
                                });
                                if (get.mode() == 'identity') {
                                    if (player.identity == 'zhu' && cxzc == 0) {
                                        if (target.identity == 'fan' && !target.hasSkill('tengjia1') && !target.hasSkill('rw_tengjia1')) {
                                            return 1;
                                        }
                                    }
                                    if (player.identity == 'zhu' && cxzc > 0) {
                                        return -99;
                                    }
                                    if (player.identity == 'zhong' && (game.zhu.isMinHp() || game.zhu.hp <= 2 || cxzc > 0)) {
                                        return -99;
                                    }
                                    if (player.identity == 'zhong' && cxzc <= 0 && game.zhu.hp > 2 && cxfz > 0) {
                                        if (target.identity == 'fan' && !target.hasSkill('tengjia1') && !target.hasSkill('rw_tengjia1')) {
                                            return 1;
                                        }
                                    }
                                    if (player.identity == 'fan' && game.zhu.hp <= 2) {
                                        if (target.identity == 'zhu' && !target.hasSkill('tengjia1') && !target.hasSkill('rw_tengjia1')) {
                                            return 1;
                                        }
                                    }
                                    if (player.identity == 'fan' && game.zhu.hp >= 3 && cxfz > cxzc && cxfz > 0) {
                                        return -99;
                                    }
                                    if (player.identity == 'nei') {
                                        if ((game.zhu.hp > 2 || (numf == 0 && numz == 0)) && !target.hasSkill('tengjia1') && !target.hasSkill('rw_tengjia1')) {
                                            return 1;
                                        }
                                        if (game.zhu.hp <= 2 && (numf >= 0 || numz >= 0)) {
                                            return -99;
                                        }
                                    }
                                }
                                if (get.mode() != 'identity') {
                                    if (cxdy >= cxdr && cxdy > 0) {
                                        return -99;
                                    }
                                    if (cxdy < cxdr && cxdr > 0) {
                                        if (get.attitude(player, target) < 0 && !target.hasSkill('tengjia1') && !target.hasSkill('rw_tengjia1')) {
                                            return 1;
                                        }
                                    }
                                    if (dy == 0 && dr > 0) {
                                        if (get.attitude(player, target) < 0 && !target.hasSkill('tengjia1') && !target.hasSkill('rw_tengjia1')) {
                                            return 1;
                                        }
                                    }
                                    if (dy >= dr && dy > 0 && cxdy <= 0) {
                                        return -99;
                                    }
                                    if (mxdr > mxdy && mxdr > 0) {
                                        return -99;
                                    }
                                    if (get.mode() == 'doudizhu' && game.zhu == player && dr > 0) {
                                        if (get.attitude(player, target) < 0 && !target.hasSkill('tengjia1') && !target.hasSkill('rw_tengjia1')) {
                                            return 1;
                                        }
                                    }
                                    if (get.mode() == 'doudizhu' && game.zhu != player && dy > 0 && game.zhu.hp > 2) {
                                        return -99;
                                    }
                                    if (get.mode() == 'doudizhu' && game.zhu != player && (game.zhu.hasSkill('tengjia1') || game.zhu.hasSkill('rw_tengjia1'))) {
                                        return 'zeroplayertarget';
                                    }
                                    if (game.players.length == 2) {
                                        if (get.attitude(player, target) < 0 && !target.hasSkill('tengjia1') && !target.hasSkill('rw_tengjia1')) {
                                            return 1;
                                        }
                                    }
                                }
                                return 0;
                            },
                            target_use(player, target) {
                                if (player.hasUnknown(2) && get.mode() != 'guozhan') return 0;
                                var nh = target.countCards('h');
                                if (get.mode() == 'identity') {
                                    if (target.isZhu && nh <= 2 && target.hp <= 1) {
                                        if (player.identity == 'nei') {
                                            return -1;
                                        }
                                        return -100;
                                    }
                                }
                                if (nh == 0) return -2;
                                if (nh == 1) return -1.7;
                                return -1.5;
                            },
                            target(player, target) {
                                var nh = target.countCards('h');
                                if (get.mode() == 'identity') {
                                    if (target.isZhu && nh <= 2 && target.hp <= 1) {
                                        if (player.identity == 'nei') {
                                            return -1;
                                        }
                                        return -100;
                                    }
                                }
                                if (nh == 0) return -2;
                                if (nh == 1) return -1.7;
                                return -1.5;
                            },
                        },
                        tag: {
                            respond: 1,
                            respondSha: 1,
                            damage: 1,
                            multitarget: 1,
                            multineg: 1,
                        },
                    },
                };

                lib.card.wanjian = {
                    audio: true,
                    fullskin: true,
                    type: 'trick',
                    enable: true,
                    selectTarget: -1,
                    reverseOrder: true,
                    yingbian_prompt: '当你使用此牌选择目标后,你可为此牌减少一个目标',
                    yingbian_tags: ['remove'],
                    yingbian(event) {
                        event.yingbian_removeTarget = true;
                    },
                    filterTarget(card, player, target) {
                        return target != player;
                    },
                    content() {
                        'step 0';
                        if (typeof event.baseDamage != 'number') event.baseDamage = 1;
                        if (event.directHit) event._result = { bool: false };
                        else {
                            var next = target.chooseToRespond({ name: 'shan' });
                            next.set('ai', function (card) {
                                var evt = _status.event.parent;
                                if (get.damageEffect(evt.target, evt.player, evt.target) >= 0) return 0;
                                if (evt.player.hasSkillTag('notricksource')) return 0;
                                if (evt.target.hasSkillTag('notrick')) return 0;
                                if (evt.target.hasSkillTag('noShan')) {
                                    return -1;
                                }
                                return get.order(card);
                            });
                            next.autochoose = lib.filter.autoRespondShan;
                        }
                        ('step 1');
                        if (result.bool == false) {
                            target.damage(event.baseDamage);
                        }
                    },
                    ai: {
                        wuxie(target, card, player, viewer) {
                            var cxdy = game.countPlayer(function (current) {
                                return current.hp == 1 && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && get.attitude(current, player) > 0;
                            });
                            if (cxdy > 0) {
                                if (get.attitude(viewer, target) > 0 && target.hp > 1) {
                                    return 0;
                                }
                            } else {
                                if (get.attitude(viewer, target) > 0 && target != player && target.hp >= 3) {
                                    return 0;
                                }
                            }
                            if (get.attitude(viewer, target) > 0 && target.countCards('h') > 3 && target.countCards('hs', 'shan') > 0) {
                                return 0;
                            }
                            if (get.attitude(viewer, target) > 0 && target.hasSkillTag('useShan') && target.countCards('hs', 'shan') > 0) {
                                return 0;
                            }
                            if (get.attitude(player, target) <= 0) {
                                return 0;
                            }
                        },
                        basic: {
                            order(item, player) {
                                return get.order({ name: 'sha' }) + 0.5;
                            },
                            useful: 1,
                            value: 5,
                        },
                        result: {
                            player(player, target) {
                                var dy = game.countPlayer(function (current) {
                                    return get.attitude(current, player) > 0 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && current.hp <= 2;
                                });
                                var dr = game.countPlayer(function (current) {
                                    return get.attitude(current, player) <= 0 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && current.hp <= 2;
                                });
                                var mxdy = game.countPlayer(function (current) {
                                    return get.attitude(current, player) > 0 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && current.hasSkillTag('maixie') && current.hp >= 3;
                                });
                                var mxdr = game.countPlayer(function (current) {
                                    return get.attitude(current, player) <= 0 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && current.hasSkillTag('maixie') && current.hp >= 3;
                                });
                                var cxdy = game.countPlayer(function (current) {
                                    return current.hp == 1 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && get.attitude(current, player) > 0;
                                });
                                var cxdr = game.countPlayer(function (current) {
                                    return current.hp == 1 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && get.attitude(current, player) <= 0;
                                });
                                var cxzc = game.countPlayer(function (current) {
                                    return current.hp == 1 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && current.identity == 'zhong';
                                });
                                var cxfz = game.countPlayer(function (current) {
                                    return current.hp == 1 && current != player && !(current.hasSkill('tengjia1') || current.hasSkill('rw_tengjia1')) && current.identity == 'fan';
                                });
                                var numz = game.countPlayer(function (current) {
                                    return current.identity == 'zhong' || current.identity == 'mingzhong';
                                });
                                var numf = game.countPlayer(function (current) {
                                    return current.identity == 'fan';
                                });
                                if (get.mode() == 'identity') {
                                    if (player.identity == 'zhu' && cxzc == 0) {
                                        if (target.identity == 'fan' && !target.hasSkill('tengjia1')) {
                                            return 1;
                                        }
                                    }
                                    if (player.identity == 'zhu' && cxzc > 0) {
                                        return -99;
                                    }
                                    if (player.identity == 'zhong' && (game.zhu.isMinHp() || game.zhu.hp <= 2 || cxzc > 0)) {
                                        return -99;
                                    }
                                    if (player.identity == 'zhong' && cxzc <= 0 && game.zhu.hp > 2 && cxfz > 0) {
                                        if (target.identity == 'fan' && !target.hasSkill('tengjia1')) {
                                            return 1;
                                        }
                                    }
                                    if (player.identity == 'fan' && game.zhu.hp <= 2) {
                                        if (target.identity == 'zhu' && !target.hasSkill('tengjia1')) {
                                            return 1;
                                        }
                                    }
                                    if (player.identity == 'fan' && game.zhu.hp >= 3 && cxfz > cxzc && cxfz > 0) {
                                        return -99;
                                    }
                                    if (player.identity == 'nei') {
                                        if ((game.zhu.hp > 2 || (numf == 0 && numz == 0)) && !target.hasSkill('tengjia1')) {
                                            return 1;
                                        }
                                        if (game.zhu.hp <= 2 && (numf >= 0 || numz >= 0)) {
                                            return -99;
                                        }
                                    }
                                }
                                if (get.mode() != 'identity') {
                                    if (cxdy >= cxdr && cxdy > 0) {
                                        return -99;
                                    }
                                    if (cxdy < cxdr && cxdr > 0) {
                                        if (get.attitude(player, target) < 0 && !target.hasSkill('tengjia1')) {
                                            return 1;
                                        }
                                    }
                                    if (dy == 0 && dr > 0) {
                                        if (get.attitude(player, target) < 0 && !target.hasSkill('tengjia1')) {
                                            return 1;
                                        }
                                    }
                                    if (dy >= dr && dy > 0) {
                                        return -99;
                                    }
                                    if (mxdr > mxdy && mxdr > 0) {
                                        return -99;
                                    }
                                    if (get.mode() == 'doudizhu' && game.zhu == player && dr > 0) {
                                        if (get.attitude(player, target) < 0 && !target.hasSkill('tengjia1')) {
                                            return 1;
                                        }
                                    }
                                    if (get.mode() == 'doudizhu' && game.zhu != player && dy > 0 && game.zhu.hp > 2) {
                                        return -99;
                                    }
                                    if (get.mode() == 'doudizhu' && game.zhu != player && game.zhu.hasSkill('tengjia1')) {
                                        return 'zeroplayertarget';
                                    }
                                    if (game.players.length == 2 && !target.hasSkill('tengjia1')) {
                                        if (get.attitude(player, target) < 0 && !target.hasSkill('tengjia1')) {
                                            return 1;
                                        }
                                    }
                                }
                                return 0;
                            },
                            target_use(player, target) {
                                if (player.hasUnknown(2) && get.mode() != 'guozhan') return 0;
                                var nh = target.countCards('h');
                                if (target.isZhu && nh <= 2 && target.hp <= 1) {
                                    if (player.identity == 'nei') {
                                        return -1;
                                    }
                                    return -100;
                                }
                                if (nh == 0) return -2;
                                if (nh == 1) return -1.7;
                                return -1.5;
                            },
                            target(player, target) {
                                var nh = target.countCards('h');
                                if (get.mode() == 'identity') {
                                    if (target.isZhu && nh <= 2 && target.hp <= 1) {
                                        if (player.identity == 'nei') {
                                            return -1;
                                        }
                                        return -100;
                                    }
                                }
                                if (nh == 0) return -2;
                                if (nh == 1) return -1.7;
                                return -1.5;
                            },
                        },
                        tag: {
                            respond: 1,
                            respondShan: 1,
                            damage: 1,
                            multitarget: 1,
                            multineg: 1,
                        },
                    },
                };

                lib.card.taoyuan = {
                    audio: true,
                    fullskin: true,
                    type: 'trick',
                    enable: true,
                    selectTarget: -1,
                    cardcolor: 'red',
                    reverseOrder: true,
                    yingbian_prompt: '当你使用此牌选择目标后,你可为此牌减少一个目标',
                    yingbian_tags: ['remove'],
                    yingbian(event) {
                        event.yingbian_removeTarget = true;
                    },
                    filterTarget(card, player, target) {
                        //return target.hp<target.maxHp;
                        return true;
                    },
                    ignoreTarget(card, player, target) {
                        return target.isHealthy();
                    },
                    content() {
                        target.recover();
                    },
                    ai: {
                        basic: {
                            order(item, player) {
                                var ssdy = game.countPlayer(function (current) {
                                    return current.getDamagedHp() && get.attitude(current, player) > 0 && get.recoverEffect(current, player, player) > 0;
                                });
                                var ssdr = game.countPlayer(function (current) {
                                    return current.hp == 1 && get.attitude(current, player) <= 0;
                                });
                                var cxdy = game.countPlayer(function (current) {
                                    return current.hp == 1 && get.attitude(current, player) > 0 && get.recoverEffect(current, player, player) > 0;
                                });
                                var cxdr = game.countPlayer(function (current) {
                                    return current.getDamagedHp() && get.attitude(current, player) <= 0;
                                });
                                if (ssdy > ssdr && ssdr == 0) {
                                    return get.order({ name: 'wanjian' }) + 1;
                                }
                                if (ssdy > ssdr && cxdy > 0) {
                                    return get.order({ name: 'wanjian' }) + 1;
                                }
                            },
                            useful: [3, 1],
                            value: 0,
                        },
                        result: {
                            target(player, target) {
                                return target.hp < target.maxHp ? 2 : 0;
                            },
                        },
                        tag: {
                            recover: 0.5,
                            multitarget: 1,
                        },
                    },
                };

                lib.card.qizhengxiangsheng = {
                    enable: true,
                    type: 'trick',
                    fullskin: true,
                    derivation: 'shen_xunyu',
                    filterTarget(card, player, target) {
                        return player != target;
                    },
                    content() {
                        'step 0';
                        if (!event.qizheng_name) {
                            if (player.isAlive())
                                player
                                    .chooseControl('奇兵', '正兵')
                                    .set('prompt', '请选择' + get.translation(target) + '的标记')
                                    .set(
                                        'choice',
                                        (function () {
                                            var e1 = 1.5 * get.sgn(get.damageEffect(target, player, target));
                                            var e2 = 0;
                                            if (target.countGainableCards(player, 'h') > 0 && !target.hasSkillTag('noh')) e2 = -1;
                                            var es = target.getGainableCards(player, 'e');
                                            if (es.length)
                                                e2 = Math.min(
                                                    e2,
                                                    (function () {
                                                        var max = 0;
                                                        for (var i of es) max = Math.max(max, get.value(i, target));
                                                        return -max / 4;
                                                    })(),
                                                );
                                            if (Math.abs(e1 - e2) <= 0.3) return Math.random() < 0.5 ? '奇兵' : '正兵';
                                            if (e1 < e2) return '奇兵';
                                            return '正兵';
                                        })(),
                                    )
                                    .set('ai', function () {
                                        return _status.event.choice;
                                    });
                            else event.finish();
                        }
                        ('step 1');
                        if (!event.qizheng_name && result && result.control) event.qizheng_name = result.control;
                        if (event.directHit) event._result = { bool: false };
                        else
                            target
                                .chooseToRespond('请打出一张杀或闪响应奇正相生', function (card, player) {
                                    var name = card.name;
                                    return name == 'sha' || name == 'shan';
                                })
                                .set('ai', function (card) {
                                    if (_status.event.choice == 'all') {
                                        var rand = get.rand('qizhengxiangsheng');
                                        if (rand > 0.5) return 0;
                                        return 1 + Math.random();
                                    }
                                    if (card.name == _status.event.choice) return get.order(card);
                                    return 0;
                                })
                                .set(
                                    'choice',
                                    (function () {
                                        if (target.hasSkillTag('useShan')) return 'shan';
                                        if (typeof event.qizheng_aibuff == 'boolean') {
                                            var shas = target.getCards('h', 'sha'),
                                                shans = target.getCards('h', 'shan');
                                            if (event.qizheng_aibuff) {
                                                if (shas.length >= Math.max(1, shans.length)) return 'shan';
                                                if (shans.length > shas.length) return 'sha';
                                                return false;
                                            }
                                            if (!shas.length || !shans.length) return false;
                                        }
                                        var e1 = 1.5 * get.sgn(get.damageEffect(target, player, target));
                                        var e2 = 0;
                                        if (target.countGainableCards(player, 'h') > 0 && !target.hasSkillTag('noh')) e2 = -1;
                                        var es = target.getGainableCards(player, 'e');
                                        if (es.length)
                                            e2 = Math.min(
                                                e2,
                                                (function () {
                                                    var max = 0;
                                                    for (var i of es) max = Math.max(max, get.value(i, target));
                                                    return -max / 4;
                                                })(),
                                            );
                                        if (e1 - e2 >= 0.3) return 'shan';
                                        if (e2 - e1 >= 0.3) return 'sha';
                                        return 'all';
                                    })(),
                                );
                        ('step 2');
                        var name = result.bool ? result.card.name : null,
                            require = event.qizheng_name;
                        if (require == '奇兵' && name != 'sha') target.damage();
                        else if (require == '正兵' && name != 'shan' && target.countGainableCards(player, 'he') > 0) player.gainPlayerCard(target, true, 'he');
                    },
                    ai: {
                        order: 5,
                        tag: {
                            damage: 0.5,
                            gain: 0.5,
                            loseCard: 1,
                            respondShan: 1,
                            respondSha: 1,
                        },
                        result: {
                            player(player, target, card) {
                                if (target.hasSkill('tianzuo_remove')) {
                                    return 'zeroplayertarget';
                                }
                            },
                            target(player, target) {
                                if (target.hasSkill('tianzuo_remove')) {
                                    return 'zeroplayertarget';
                                }
                                var e1 = 1.5 * get.sgn(get.damageEffect(target, player, target));
                                var e2 = 0;
                                if (target.countGainableCards(player, 'h') > 0 && !target.hasSkillTag('noh')) e2 = -1;
                                var es = target.getGainableCards(player, 'e');
                                if (es.length)
                                    e2 = Math.min(
                                        e2,
                                        (function () {
                                            var max = 0;
                                            for (var i of es) max = Math.max(max, get.value(i, target));
                                            return -max / 4;
                                        })(),
                                    );
                                if (
                                    game.hasPlayer(function (current) {
                                        return current.hasSkill('tianzuo') && get.attitude(current, player) <= 0;
                                    })
                                )
                                    return Math.max(e1, e2);
                                return Math.min(e1, e2);
                            },
                        },
                    },
                    selectTarget: 1,
                };

                lib.get.damageEffect = function (target, player, viewer, nature) {
                    if (!player) {
                        player = target;
                    }
                    if (!viewer) {
                        viewer = target;
                    }
                    var name = 'damage';
                    if (nature == 'fire') {
                        name = 'firedamage';
                    } else if (nature == 'thunder') {
                        name = 'thunderdamage';
                    } else if (nature == 'ice') {
                        name = 'icedamage';
                    }
                    var eff = get.effect(target, { name: name }, player, viewer);
                    return eff;
                };
            }

            if (lib.config.extension_云将_xianyunyehe == 'on') {
                lib.skill.chongxu = {
                    enable: 'phaseUse',
                    usable: 1,
                    async content(event, trigger, player) {
                        //QQQ
                        game.log(player, '的演奏评级为#y神话,获得积分点数#y5分');
                        var list = [];
                        if (player.countMark('miaojian') < 2 && player.hasSkill('miaojian')) list.push('修改【妙剑】');
                        if (player.countMark('shhlianhua') < 2 && player.hasSkill('shhlianhua')) list.push('修改【莲华】');
                        if (list.length) {
                            list.push('全部摸牌');
                            const result = await player.chooseControl(list).set('prompt', '冲虚:修改技能并摸1张牌;或摸2张牌').forResult();
                            if (result.control != '全部摸牌') {
                                var skill = result.control == '修改【妙剑】' ? 'miaojian' : 'shhlianhua';
                                player.addMark(skill, 1, false);
                                game.log(player, '修改了技能#g【' + get.translation(skill) + '】');
                                player.draw(1);
                            } else {
                                player.draw(2);
                            }
                        } else {
                            player.draw(2);
                        }
                    },
                    ai: {
                        order: 10,
                        result: {
                            player: 1,
                        },
                    },
                };
                lib.skill.xinfu_pingcai = {
                    audio: true,
                    enable: 'phaseUse',
                    usable: 1,
                    async content(event, trigger, player) {
                        //QQQ
                        const link = [];
                        const list = ['wolong', 'fengchu', 'xuanjian', 'shuijing'];
                        for (var i of list) {
                            link.push([i, get.translation(i)]);
                        }
                        const result = await player
                            .chooseButton(['请选择要擦拭的宝物', [link, 'tdnodes']], true)
                            .set('ai', function (button) {
                                if (button.link.name == 'xuanjian') {
                                    if (game.hasPlayer((current) => current.isDamaged() && current.hp < 3 && get.attitude(player, current) > 1)) {
                                        return 1 + Math.random();
                                    }
                                    return 1;
                                }
                                if (button.link.name == 'wolong') {
                                    if (game.hasPlayer((current) => get.damageEffect(current, player, player, 'fire') > 0)) {
                                        return 1.2 + Math.random();
                                    }
                                    return 0.5;
                                }
                                return 0.6;
                            })
                            .forResult();
                        if (result.links?.length) {
                            switch (result.links[0]) {
                                case 'wolong':
                                    {
                                        var ingame = game.hasPlayer((current) => ['sp_zhugeliang', 're_sp_zhugeliang', 'ol_sp_zhugeliang', 'prp_zhugeliang'].includes(current.name)) ? true : false;
                                        var range = ingame ? [1, 2] : [1, 1];
                                        const { result: result1 } = await player.chooseTarget(`请选择${ingame ? '至多两名' : '一名'}角色,对其造成1点火焰伤害`, range).set('ai', (target) => get.damageEffect(target, player, player, 'fire'));
                                        if (result1.targets && result1.targets[0]) {
                                            for (var i of result1.targets) {
                                                i.damage('fire');
                                            }
                                        }
                                    }
                                    break;
                                case 'fengchu':
                                    {
                                        var ingame = game.hasPlayer((current) => ['re_pangtong', 'pangtong', 'ol_pangtong'].includes(current.name)) ? true : false;
                                        var prompt = `请选择${ingame ? '至多四名' : '至多三名'}要横置的角色`;
                                        var range = ingame ? [1, 4] : [1, 3];
                                        const { result: result1 } = await player.chooseTarget(prompt, range).set('ai', (target) => get.effect(target, { name: 'tiesuo' }, player, player));
                                        if (result1.targets && result1.targets[0]) {
                                            for (var i of result1.targets) {
                                                i.link();
                                            }
                                        }
                                    }
                                    break;
                                case 'xuanjian':
                                    {
                                        var ingame = game.hasPlayer((current) => ['re_xushu', 'xin_xushu', 'xushu', 'dc_xushu'].includes(current.name)) ? true : false;
                                        var prompt = `请选择一名角色,令其回复1点体力并摸一张牌${ingame ? ',你摸一张牌.' : '.'}`;
                                        const { result: result1 } = await player.chooseTarget(prompt).set('ai', (target) => get.attitude(player, target) * (target.isDamaged() ? 2 : 1));
                                        if (result1.targets && result1.targets[0]) {
                                            result1.targets[0].draw();
                                            result1.targets[0].recover();
                                            if (ingame) player.draw();
                                        }
                                    }
                                    break;
                                case 'shuijing':
                                    {
                                        var ingame = game.hasPlayer((current) => current.name == 'simahui') ? true : false;
                                        var prompt = `将一名角色装备区中的${ingame ? '一张牌' : '防具牌'}移动到另一名角色的装备区中`;
                                        const { result: result1 } = await player
                                            .chooseTarget(2, function (card, player, target) {
                                                if (ui.selected.targets.length) {
                                                    return true;
                                                } else {
                                                    if (!ingame) {
                                                        return target.getEquips(2).length;
                                                    }
                                                    return target.countCards('e');
                                                }
                                            })
                                            .set('ai', function (target) {
                                                var att = get.attitude(player, target);
                                                if (ui.selected.targets.length == 0) {
                                                    if (att < 0) {
                                                        if (
                                                            game.hasPlayer(function (current) {
                                                                if (get.attitude(player, current) > 0) {
                                                                    var es = target.getCards('e');
                                                                    for (var i = 0; i < es.length; i++) {
                                                                        if (current.canEquip(es[i])) return true;
                                                                    }
                                                                    return false;
                                                                }
                                                            })
                                                        )
                                                            return -att;
                                                    }
                                                    return 0;
                                                }
                                                if (att > 0) {
                                                    var es = ui.selected.targets[0].getCards('e');
                                                    var i;
                                                    for (i = 0; i < es.length; i++) {
                                                        if (target.canEquip(es[i])) break;
                                                    }
                                                    if (i == es.length) return 0;
                                                }
                                                return -att * get.attitude(player, ui.selected.targets[0]);
                                            })
                                            .set('multitarget', true)
                                            .set('targetprompt', ['被移走', '移动目标'])
                                            .set('prompt', prompt);
                                        if (result1.targets && result1.targets[0]) {
                                            if (result1.targets.length == 2) {
                                                if (!ingame) {
                                                    var cards = result1.targets[0].getEquips(2);
                                                    if (cards.length == 1) {
                                                        result1.targets[1].equip(cards[0]);
                                                    } else {
                                                        const { result: result2 } = await player.choosePlayerCard('e', true, (button) => get.equipValue(button.link), result1.targets[0]).set('filterButton', (button) => get.subtypes(button.link, false).includes('equip2'));
                                                        if (result2.links && result2.links[0]) {
                                                            result1.targets[1].equip(result2.links[0]);
                                                        }
                                                    }
                                                } else {
                                                    const { result: result2 } = await player.choosePlayerCard('e', true, (button) => get.equipValue(button.link), result1.targets[0]);
                                                    if (result2.links && result2.links[0]) {
                                                        result1.targets[1].equip(result2.links[0]);
                                                    }
                                                }
                                            }
                                        }
                                    }
                                    break;
                            }
                        }
                    },
                    ai: {
                        order: 7,
                        fireAttack: true,
                        threaten: 1.7,
                        result: {
                            player: 1,
                        },
                    },
                };
                lib.skill.yufeng = {
                    audio: 2,
                    enable: 'phaseUse',
                    usable: 1,
                    async content(event, trigger, player) {
                        //QQQ
                        game.log(player, '御风飞行成功,获得了#g3分');
                        const result = await player
                            .chooseTarget('请选择【御风】的目标', [1, 3], (card, player, target) => target != player && !target.hasSkill('yufeng2'))
                            .set('ai', function (target) {
                                var att = -get.attitude(player, target),
                                    attx = att * 2;
                                if (att <= 0 || target.hasSkill('xinfu_pdgyingshi')) return 0;
                                if (target.hasJudge('lebu')) attx -= att;
                                if (target.hasJudge('bingliang')) attx -= att;
                                return attx / Math.max(2.25, Math.sqrt(target.countCards('h') + 1));
                            })
                            .forResult();
                        if (result.targets?.length) {
                            game.log(result.targets, '获得了#y<御风>效果');
                            for (var i of result.targets) {
                                i.addSkill('yufeng2');
                            }
                            if (3 > result.targets.length) {
                                player.draw(3 - result.targets.length);
                            }
                        } else {
                            player.draw(3);
                        }
                    },
                    ai: {
                        order: 10,
                        result: {
                            player: 1,
                        },
                        threaten: 3.2,
                    },
                };
                lib.skill.zhengjing = {
                    audio: 2,
                    enable: 'phaseUse',
                    usable: 1,
                    filter(event, player) {
                        return !player.hasSkill('zhengjing3');
                    },
                    async content(event, trigger, player) {
                        //QQQ
                        var cards = Array.from(ui.cardPile.childNodes).randomGets(5);
                        if (cards.length) {
                            player.showCards(cards, get.translation(player) + '整理出了以下经典');
                            const result = await player
                                .chooseTarget('将整理出的经典置于一名角色的武将牌上')
                                .set('ai', (target) => {
                                    if (target.hasSkill('zhengjing2')) return get.attitude(player, target);
                                    return get.attitude(player, target) * (0.3 * target.countCards('j') - 1);
                                })
                                .forResult(); //对目标正价值就乘get.att,负价值就乘-get.att
                            if (result.targets?.length) {
                                const { result: result1 } = await player.chooseButton(['将整理出的经典置于一名角色的武将牌上', cards]).set('ai', (button) => 6 - get.value(button.link));
                                if (result1.links && result1.links[0]) {
                                    cards.remove(result1.links[0]);
                                    result.targets[0].addSkill('zhengjing2');
                                    result.targets[0].addToExpansion(result1.links, 'gain2').gaintag.add('zhengjing2');
                                    player.gain(cards, 'gain2');
                                } else {
                                    player.gain(cards, 'gain2');
                                }
                            } else {
                                player.gain(cards, 'gain2');
                            }
                        }
                    },
                    ai: {
                        order: 10,
                        result: {
                            player: 1,
                        },
                        threaten: 3.2,
                    },
                };
            }
            lib.skill._baihualiaoluan = {
                trigger: {
                    global: 'gameStart',
                },
                forced: true,
                popup: false,
                charlotte: true,
                mode: ['identity'],
                filter(event, player) {
                    if (player.name1 == 'yunyouzi' || player.name1 == 'yunlvlingqi') return false;
                    return lib.config.extension_云将_baihualiaoluan && player.isUnderControl(true) && player == game.me;
                },
                content() {
                    'step 0';
                    player.chooseBool('是否召唤一只帅气的狗子？');
                    ('step 1');
                    if (result.bool) {
                        var name = player.name1;
                        player.reinit(name, 'yunyouzi', false);
                        player.changeGroup('shen');
                        if (_status.characterlist) {
                            _status.characterlist.add(name);
                            _status.characterlist.remove('yunyouzi');
                        }
                    }
                },
            };

            lib.skill._yuxi_xiaohui = {
                trigger: {
                    global: ['loseEnd', 'cardsDiscardEnd'],
                },
                silent: true,
                forced: true,
                popup: false,
                charlotte: true,
                filter(event, player) {
                    var cs = event.cards;
                    for (var i = 0; i < cs.length; i++) {
                        if (cs[i].name.indexOf('yunyuxi') == 0 && get.position(cs[i], true) == 'd') return true;
                    }
                    return false;
                },
                content() {
                    var list = [];
                    var cs = trigger.cards;
                    for (var i = 0; i < cs.length; i++) {
                        if (cs[i].name.indexOf('yunyuxi') == 0 && get.position(cs[i], true) == 'd') {
                            list.push(cs[i]);
                        }
                    }
                    game.cardsGotoSpecial(list);
                },
            };

            if (lib.config.extension_云将_banjuncharacter == '2') {
                if (lib.config.mode == 'identity') {
                    lib.game.chooseCharacter = function () {
                        if (_status.mode == 'purple') {
                            game.chooseCharacterPurple();
                            return;
                        }
                        var next = game.createEvent('chooseCharacter', false);
                        next.showConfig = true;
                        next.addPlayer = function (player) {
                            var list = lib.config.mode_config.identity.identity[game.players.length - 3].slice(0);
                            var list2 = lib.config.mode_config.identity.identity[game.players.length - 2].slice(0);
                            for (var i = 0; i < list.length; i++) list2.remove(list[i]);
                            player.identity = list2[0];
                            player.setIdentity('cai');
                        };
                        next.removePlayer = function () {
                            return game.players.randomGet(game.me, game.zhu);
                        };
                        next.ai = function (player, list, list2, back) {
                            if (_status.brawl && _status.brawl.chooseCharacterAi) {
                                if (_status.brawl.chooseCharacterAi(player, list, list2, back) !== false) {
                                    return;
                                }
                            }
                            if (_status.event.zhongmode) {
                                var listc = list.slice(0, 2);
                                for (var i = 0; i < listc.length; i++) {
                                    var listx = lib.characterReplace[listc[i]];
                                    if (listx && listx.length) listc[i] = listx.randomGet();
                                }
                                if (get.config('double_character')) {
                                    player.init(listc[0], listc[1]);
                                } else {
                                    player.init(listc[0]);
                                }
                                if (player.identity == 'mingzhong') {
                                    player.hp++;
                                    player.maxHp++;
                                    player.update();
                                }
                            } else if (player.identity == 'zhu') {
                                list2.randomSort();
                                var choice, choice2;
                                if (!_status.event.zhongmode && Math.random() - 0.8 < 0 && list2.length) {
                                    choice = list2[0];
                                    choice2 = list[0];
                                    if (choice2 == choice) {
                                        choice2 = list[1];
                                    }
                                } else {
                                    choice = list[0];
                                    choice2 = list[1];
                                }
                                if (lib.characterReplace[choice] && lib.characterReplace[choice].length) choice = lib.characterReplace[choice].randomGet();
                                if (lib.characterReplace[choice2] && lib.characterReplace[choice2].length) choice2 = lib.characterReplace[choice2].randomGet();
                                if (get.config('double_character')) {
                                    player.init(choice, choice2);
                                } else {
                                    player.init(choice);
                                }
                                if (game.players.length > 4) {
                                    player.hp++;
                                    player.maxHp++;
                                    player.update();
                                }
                            } else if (player.identity == 'zhong') {
                                var skillS = lib.character[game.zhu.name][3];
                                for (var j = 0; j < skillS.length; j++) {
                                    var str = '';
                                    str += skillS[j];
                                    if (lib.skill[str].zhuSkill || Math.random() < 0.5) {
                                        var listc = list.slice(0);
                                        for (var i = 0; i < listc.length; i++) {
                                            var listx = lib.characterReplace[listc[i]];
                                            if (listx && listx.length) listc[i] = listx.randomGet();
                                        }
                                        var choice = 0;
                                        for (var i = 0; i < listc.length; i++) {
                                            if (lib.character[listc[i]][1] == game.zhu.group) {
                                                choice = i;
                                                break;
                                            }
                                        }
                                        if (get.config('double_character')) {
                                            player.init(listc[choice], listc[choice == 0 ? choice + 1 : choice - 1]);
                                        } else {
                                            player.init(listc[choice]);
                                        }
                                    } else {
                                        var listc = list.slice(0, 2);
                                        for (var i = 0; i < listc.length; i++) {
                                            var listx = lib.characterReplace[listc[i]];
                                            if (listx && listx.length) listc[i] = listx.randomGet();
                                        }
                                        if (get.config('double_character')) {
                                            player.init(listc[0], listc[1]);
                                        } else {
                                            player.init(listc[0]);
                                        }
                                    }
                                }
                            } else if (player.identity == 'nei') {
                                var listc = list.slice(0, 2);
                                for (var i = 0; i < listc.length; i++) {
                                    var listx = lib.characterReplace[listc[i]];
                                    if (listx && listx.length) listc[i] = listx.randomGet();
                                }
                                var listC = listc.sort(function (a, b) {
                                    var rank = function (name) {
                                        if (game.getRarity(name) == 'junk') return 1;
                                        if (game.getRarity(name) == 'common') return 2;
                                        if (game.getRarity(name) == 'rare') return 3;
                                        if (game.getRarity(name) == 'epic') return 4;
                                        if (game.getRarity(name) == 'legend') return 5;
                                    };
                                    return rank(b) - rank(a);
                                });
                                if (get.config('double_character')) {
                                    player.init(listC[0], listC[1]);
                                } else {
                                    player.init(listC[0]);
                                }
                            } else {
                                var listc = list.slice(0, 2);
                                for (var i = 0; i < listc.length; i++) {
                                    var listx = lib.characterReplace[listc[i]];
                                    if (listx && listx.length) listc[i] = listx.randomGet();
                                }
                                if (get.config('double_character')) {
                                    player.init(listc[0], listc[1]);
                                } else {
                                    player.init(listc[0]);
                                }
                            }
                            if (back) {
                                list.remove(get.sourceCharacter(player.name1));
                                list.remove(get.sourceCharacter(player.name2));
                                for (var i = 0; i < list.length; i++) {
                                    back.push(list[i]);
                                }
                            }
                            if (typeof lib.config.test_game == 'string' && player == game.me.next) {
                                player.init(lib.config.test_game);
                            }
                            if (get.is.double(player.name1)) {
                                player._groupChosen = true;
                                player.group = get.is.double(player.name1, true).randomGet();
                                player.node.name.dataset.nature = get.groupnature(player.group);
                            } else if (get.config('choose_group') && player.group == 'shen' && !player.isUnseen(0)) {
                                var list = lib.group.slice(0);
                                list.remove('shen');
                                if (list.length)
                                    player.group = (function () {
                                        if (_status.mode != 'zhong' && game.zhu && game.zhu.group) {
                                            if (['re_zhangjiao', 'liubei', 're_liubei', 'caocao', 're_caocao', 'sunquan', 're_sunquan', 'zhangjiao', 'sp_zhangjiao', 'caopi', 're_caopi', 'liuchen', 'caorui', 'sunliang', 'sunxiu', 'sunce', 're_sunben', 'ol_liushan', 're_liushan', 'key_akane', 'dongzhuo', 're_dongzhuo', 'ol_dongzhuo', 'jin_simashi', 'caomao'].includes(game.zhu.name)) return game.zhu.group;
                                            if (game.zhu.name == 'yl_yuanshu') {
                                                if (player.identity == 'zhong') list.remove('qun');
                                                else return 'qun';
                                            }
                                            if (['sunhao', 'xin_yuanshao', 're_yuanshao', 're_sunce', 'ol_yuanshao', 'yuanshu', 'jin_simazhao', 'liubian'].includes(game.zhu.name)) {
                                                if (player.identity != 'zhong') list.remove(game.zhu.group);
                                                else return game.zhu.group;
                                            }
                                        }
                                        return list.randomGet();
                                    })();
                            }
                            player.node.name.dataset.nature = get.groupnature(player.group);
                        };
                        next.setContent(function () {
                            'step 0';
                            ui.arena.classList.add('choose-character');
                            var i;
                            var list;
                            var list2 = [];
                            var list3 = [];
                            var list4 = [];
                            var identityList;
                            var chosen = lib.config.continue_name || [];
                            game.saveConfig('continue_name');
                            event.chosen = chosen;
                            if (_status.mode == 'zhong') {
                                event.zhongmode = true;
                                identityList = ['zhu', 'zhong', 'mingzhong', 'nei', 'fan', 'fan', 'fan', 'fan'];
                            } else {
                                identityList = lib.config.mode_config.identity.identity[game.players.length - 2].slice(0);
                                if (get.config('double_nei')) {
                                    switch (get.playerNumber()) {
                                        case 8:
                                            identityList.remove('fan');
                                            identityList.push('nei');
                                            break;
                                        case 7:
                                            identityList.remove('zhong');
                                            identityList.push('nei');
                                            break;
                                        case 6:
                                            identityList.remove('fan');
                                            identityList.push('nei');
                                            break;
                                        case 5:
                                            identityList.remove('fan');
                                            identityList.push('nei');
                                            break;
                                        case 4:
                                            identityList.remove('zhong');
                                            identityList.push('nei');
                                            break;
                                        case 3:
                                            identityList.remove('fan');
                                            identityList.push('nei');
                                            break;
                                    }
                                }
                            }
                            var addSetting = function (dialog) {
                                dialog.add('选择身份').classList.add('add-setting');
                                var table = document.createElement('div');
                                table.classList.add('add-setting');
                                table.style.margin = '0';
                                table.style.width = '100%';
                                table.style.position = 'relative';
                                var listi;
                                if (event.zhongmode) {
                                    listi = ['random', 'zhu', 'mingzhong', 'zhong', 'nei', 'fan'];
                                } else {
                                    listi = ['random', 'zhu', 'zhong', 'nei', 'fan'];
                                }
                                for (var i = 0; i < listi.length; i++) {
                                    var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
                                    td.link = listi[i];
                                    if (td.link === game.me.identity) {
                                        td.classList.add('bluebg');
                                    }
                                    table.appendChild(td);
                                    td.innerHTML = '<span>' + get.translation(listi[i] + '2') + '</span>';
                                    td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', function () {
                                        if (_status.dragged) return;
                                        if (_status.justdragged) return;
                                        _status.tempNoButton = true;
                                        setTimeout(function () {
                                            _status.tempNoButton = false;
                                        }, 500);
                                        var link = this.link;
                                        if (game.zhu) {
                                            if (link != 'random') {
                                                _status.event.parent.fixedseat = get.distance(game.me, game.zhu, 'absolute');
                                            }
                                            if (game.zhu.name) game.zhu.uninit();
                                            delete game.zhu.isZhu;
                                            delete game.zhu.identityShown;
                                        }
                                        var current = this.parentNode.querySelector('.bluebg');
                                        if (current) {
                                            current.classList.remove('bluebg');
                                        }
                                        current = seats.querySelector('.bluebg');
                                        if (current) {
                                            current.classList.remove('bluebg');
                                        }
                                        if (link == 'random') {
                                            if (event.zhongmode) {
                                                link = ['zhu', 'zhong', 'nei', 'fan', 'mingzhong'].randomGet();
                                            } else {
                                                link = ['zhu', 'zhong', 'nei', 'fan'].randomGet();
                                            }
                                            for (var i = 0; i < this.parentNode.childElementCount; i++) {
                                                if (this.parentNode.childNodes[i].link == link) {
                                                    this.parentNode.childNodes[i].classList.add('bluebg');
                                                }
                                            }
                                        } else {
                                            this.classList.add('bluebg');
                                        }
                                        num = get.config('choice_' + link);
                                        if (event.zhongmode) {
                                            num = 6;
                                            if (link == 'zhu' || link == 'nei' || link == 'mingzhong') {
                                                num = 8;
                                            }
                                        }
                                        _status.event.parent.swapnodialog = function (dialog, list) {
                                            var buttons = ui.create.div('.buttons');
                                            var node = dialog.buttons[0].parentNode;
                                            dialog.buttons = ui.create.buttons(list, 'characterx', buttons);
                                            dialog.content.insertBefore(buttons, node);
                                            buttons.addTempClass('start');
                                            node.remove();
                                            game.uncheck();
                                            game.check();
                                            for (var i = 0; i < seats.childElementCount; i++) {
                                                if (get.distance(game.zhu, game.me, 'absolute') === seats.childNodes[i].link) {
                                                    seats.childNodes[i].classList.add('bluebg');
                                                }
                                            }
                                        };
                                        _status.event = _status.event.parent;
                                        _status.event.step = 0;
                                        _status.event.identity = link;
                                        if (link != (event.zhongmode ? 'mingzhong' : 'zhu')) {
                                            seats.previousSibling.style.display = '';
                                            seats.style.display = '';
                                        } else {
                                            seats.previousSibling.style.display = 'none';
                                            seats.style.display = 'none';
                                        }
                                        game.resume();
                                    });
                                }
                                dialog.content.appendChild(table);
                                dialog.add('选择座位').classList.add('add-setting');
                                var seats = document.createElement('div');
                                seats.classList.add('add-setting');
                                seats.style.margin = '0';
                                seats.style.width = '100%';
                                seats.style.position = 'relative';
                                for (var i = 2; i <= game.players.length; i++) {
                                    var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
                                    td.innerHTML = get.cnNumber(i, true);
                                    td.link = i - 1;
                                    seats.appendChild(td);
                                    if (get.distance(game.zhu, game.me, 'absolute') === i - 1) {
                                        td.classList.add('bluebg');
                                    }
                                    td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', function () {
                                        if (_status.dragged) return;
                                        if (_status.justdragged) return;
                                        if (get.distance(game.zhu, game.me, 'absolute') == this.link) return;
                                        var current = this.parentNode.querySelector('.bluebg');
                                        if (current) {
                                            current.classList.remove('bluebg');
                                        }
                                        this.classList.add('bluebg');
                                        for (var i = 0; i < game.players.length; i++) {
                                            if (get.distance(game.players[i], game.me, 'absolute') == this.link) {
                                                game.swapSeat(game.zhu, game.players[i], false);
                                                return;
                                            }
                                        }
                                    });
                                }
                                dialog.content.appendChild(seats);
                                if (game.me == game.zhu) {
                                    seats.previousSibling.style.display = 'none';
                                    seats.style.display = 'none';
                                }
                                dialog.add(ui.create.div('.placeholder.add-setting'));
                                dialog.add(ui.create.div('.placeholder.add-setting'));
                                if (get.is.phoneLayout()) dialog.add(ui.create.div('.placeholder.add-setting'));
                            };
                            var removeSetting = function () {
                                var dialog = _status.event.dialog;
                                if (dialog) {
                                    dialog.style.height = '';
                                    delete dialog._scrollset;
                                    var list = Array.from(dialog.querySelectorAll('.add-setting'));
                                    while (list.length) {
                                        list.shift().remove();
                                    }
                                    ui.update();
                                }
                            };
                            event.addSetting = addSetting;
                            event.removeSetting = removeSetting;
                            event.list = [];
                            identityList.randomSort();
                            if (event.identity) {
                                identityList.remove(event.identity);
                                identityList.unshift(event.identity);
                                if (event.fixedseat) {
                                    var zhuIdentity = _status.mode == 'zhong' ? 'mingzhong' : 'zhu';
                                    if (zhuIdentity != event.identity) {
                                        identityList.remove(zhuIdentity);
                                        identityList.splice(event.fixedseat, 0, zhuIdentity);
                                    }
                                    delete event.fixedseat;
                                }
                                delete event.identity;
                            } else if (_status.mode != 'zhong' && (!_status.brawl || !_status.brawl.identityShown)) {
                                var ban_identity = [];
                                ban_identity.push(get.config('ban_identity') || 'off');
                                if (ban_identity[0] != 'off') {
                                    ban_identity.push(get.config('ban_identity2') || 'off');
                                    if (ban_identity[1] != 'off') {
                                        ban_identity.push(get.config('ban_identity3') || 'off');
                                    }
                                }
                                ban_identity.remove('off');
                                if (ban_identity.length) {
                                    var identityList2 = identityList.slice(0);
                                    for (var i = 0; i < ban_identity.length; i++) {
                                        while (identityList2.remove(ban_identity[i]));
                                    }
                                    ban_identity = identityList2.randomGet();
                                    identityList.remove(ban_identity);
                                    identityList.splice(game.players.indexOf(game.me), 0, ban_identity);
                                }
                            }
                            game.players.forEach((npc, i, arr) => {
                                const identity = identityList[i];
                                if (_status.brawl && _status.brawl.identityShown) {
                                    if (npc.identity == 'zhu') game.zhu = npc;
                                    npc.identityShown = true;
                                } else {
                                    npc.node.identity.classList.add('guessing');
                                    npc.identity = identity;
                                    npc.setIdentity('cai');
                                    if (event.zhongmode) {
                                        if (identity == 'mingzhong') {
                                            game.zhu = npc;
                                        } else if (identity == 'zhu') {
                                            game.zhu2 = npc;
                                        }
                                    } else {
                                        if (identity == 'zhu') {
                                            game.zhu = npc;
                                        }
                                    }
                                    npc.identityShown = false;
                                }
                            }); //QQQ
                            if (get.config('special_identity') && !event.zhongmode && game.players.length == 8) {
                                for (var i = 0; i < game.players.length; i++) {
                                    delete game.players[i].special_identity;
                                }
                                event.special_identity = [];
                                var zhongs = game.filterPlayer(function (current) {
                                    return current.identity == 'zhong';
                                });
                                var fans = game.filterPlayer(function (current) {
                                    return current.identity == 'fan';
                                });
                                if (fans.length >= 1) {
                                    fans.randomRemove().special_identity = 'identity_zeishou';
                                    event.special_identity.push('identity_zeishou');
                                }
                                if (zhongs.length > 1) {
                                    zhongs.randomRemove().special_identity = 'identity_dajiang';
                                    zhongs.randomRemove().special_identity = 'identity_junshi';
                                    event.special_identity.push('identity_dajiang');
                                    event.special_identity.push('identity_junshi');
                                } else if (zhongs.length == 1) {
                                    if (Math.random() < 0.5) {
                                        zhongs.randomRemove().special_identity = 'identity_dajiang';
                                        event.special_identity.push('identity_dajiang');
                                    } else {
                                        zhongs.randomRemove().special_identity = 'identity_junshi';
                                        event.special_identity.push('identity_junshi');
                                    }
                                }
                            }
                            if (!game.zhu) game.zhu = game.me;
                            else {
                                game.zhu.setIdentity();
                                game.zhu.identityShown = true;
                                game.zhu.isZhu = game.zhu.identity == 'zhu';
                                game.zhu.node.identity.classList.remove('guessing');
                                game.me.setIdentity();
                                game.me.node.identity.classList.remove('guessing');
                            }
                            //选将框分配
                            for (var i in lib.characterReplace) {
                                var ix = lib.characterReplace[i];
                                for (var j = 0; j < ix.length; j++) {
                                    if (chosen.includes(ix[j]) || lib.filter.characterDisabled(ix[j])) ix.splice(j--, 1);
                                }
                                if (ix.length) {
                                    event.list.push(i);
                                    list4.addArray(ix);
                                    var bool = false;
                                    for (var j of ix) {
                                        if (lib.character[j][4] && lib.character[j][4].includes('zhu')) {
                                            bool = true;
                                            break;
                                        }
                                    }
                                    (bool ? list2 : list3).push(i);
                                }
                            }
                            for (var i in lib.character) {
                                if (list4.includes(i)) continue;
                                if (chosen.includes(i)) continue;
                                if (lib.filter.characterDisabled(i)) continue;
                                event.list.push(i);
                                list4.push(i);
                                if (lib.character[i][4] && lib.character[i][4].includes('zhu')) {
                                    list2.push(i);
                                } else {
                                    list3.push(i);
                                }
                            }
                            list2.sort(lib.sort.character);
                            event.list.randomSort();
                            _status.characterlist = list4.slice(0).randomSort();
                            list3.randomSort();
                            if (_status.brawl && _status.brawl.chooseCharacterFilter) {
                                _status.brawl.chooseCharacterFilter(event.list, list2, list3);
                            }
                            var num = get.config('choice_' + game.me.identity);
                            if (event.zhongmode) {
                                num = 6;
                                if (game.me.identity == 'zhu' || game.me.identity == 'nei' || game.me.identity == 'mingzhong') {
                                    num = 8;
                                }
                            }
                            if (game.zhu != game.me) {
                                var evtlist = event.list;
                                var evtlist2 = list2;
                                var banZhu = lib.config.extension_云将_zhu;
                                if (lib.config.extension_云将_zhu == undefined) banZhu = [];
                                for (var j = 0; j < evtlist.length; j++) {
                                    if (banZhu.includes(evtlist[j])) evtlist.remove(evtlist[j]);
                                }
                                for (var j = 0; j < evtlist2.length; j++) {
                                    if (banZhu.includes(evtlist2[j])) evtlist2.remove(evtlist2[j]);
                                }
                                event.ai(game.zhu, evtlist, evtlist2);
                                event.list.remove(get.sourceCharacter(game.zhu.name1));
                                event.list.remove(get.sourceCharacter(game.zhu.name2));
                                if (_status.brawl && _status.brawl.chooseCharacter) {
                                    list = _status.brawl.chooseCharacter(event.list, num);
                                    if (list === false || list === 'nozhu') {
                                        list = event.list.slice(0, num);
                                    }
                                } else {
                                    list = event.list.slice(0, num);
                                }
                            } else {
                                if (_status.brawl && _status.brawl.chooseCharacter) {
                                    list = _status.brawl.chooseCharacter(list2, list3, num);
                                    if (list === false) {
                                        if (event.zhongmode) {
                                            list = list3.slice(0, 6);
                                        } else {
                                            list = list2.concat(list3.slice(0, num));
                                        }
                                    } else if (list === 'nozhu') {
                                        list = event.list.slice(0, num);
                                    }
                                } else {
                                    if (event.zhongmode) {
                                        list = list3.slice(0, 8);
                                    } else {
                                        list = list2.concat(list3.slice(0, num));
                                    }
                                }
                            }
                            delete event.swapnochoose;
                            var dialog;
                            if (event.swapnodialog) {
                                dialog = ui.dialog;
                                event.swapnodialog(dialog, list);
                                delete event.swapnodialog;
                            } else {
                                var str = '选择角色';
                                if (_status.brawl && _status.brawl.chooseCharacterStr) {
                                    str = _status.brawl.chooseCharacterStr;
                                }
                                dialog = ui.create.dialog(str, 'hidden', [list, 'characterx']);
                                if (!_status.brawl || !_status.brawl.noAddSetting) {
                                    if (get.config('change_identity')) {
                                        addSetting(dialog);
                                    }
                                }
                            }
                            if (game.me.special_identity) {
                                dialog.setCaption('选择角色(' + get.translation(game.me.special_identity) + ')');
                                game.me.node.identity.firstChild.innerHTML = get.translation(game.me.special_identity + '_bg');
                            } else {
                                dialog.setCaption('选择角色');
                                game.me.setIdentity();
                            }
                            if (!event.chosen.length) {
                                game.me.chooseButton(dialog, true).set('onfree', true).selectButton = function () {
                                    if (_status.brawl && _status.brawl.doubleCharacter) return 2;
                                    return get.config('double_character') ? 2 : 1;
                                };
                            } else {
                                lib.init.onfree();
                            }
                            ui.create.cheat = function () {
                                _status.createControl = ui.cheat2;
                                ui.cheat = ui.create.control('更换', function () {
                                    if (ui.cheat2 && ui.cheat2.dialog == _status.event.dialog) {
                                        return;
                                    }
                                    if (game.changeCoin) {
                                        game.changeCoin(-3);
                                    }
                                    if (game.zhu != game.me) {
                                        event.list.randomSort();
                                        if (_status.brawl && _status.brawl.chooseCharacter) {
                                            list = _status.brawl.chooseCharacter(event.list, num);
                                            if (list === false || list === 'nozhu') {
                                                list = event.list.slice(0, num);
                                            }
                                        } else {
                                            list = event.list.slice(0, num);
                                        }
                                    } else {
                                        list2.sort(lib.sort.character);
                                        list3.randomSort();
                                        if (_status.brawl && _status.brawl.chooseCharacter) {
                                            list = _status.brawl.chooseCharacter(list2, list3, num);
                                            if (list === false) {
                                                if (event.zhongmode) {
                                                    list = list3.slice(0, 6);
                                                } else {
                                                    list = list2.concat(list3.slice(0, num));
                                                }
                                            } else if (list === 'nozhu') {
                                                event.list.randomSort();
                                                list = event.list.slice(0, num);
                                            }
                                        } else {
                                            if (event.zhongmode) {
                                                list = list3.slice(0, 6);
                                            } else {
                                                list = list2.concat(list3.slice(0, num));
                                            }
                                        }
                                    }
                                    var buttons = ui.create.div('.buttons');
                                    var node = _status.event.dialog.buttons[0].parentNode;
                                    _status.event.dialog.buttons = ui.create.buttons(list, 'characterx', buttons);
                                    _status.event.dialog.content.insertBefore(buttons, node);
                                    buttons.addTempClass('start');
                                    node.remove();
                                    game.uncheck();
                                    game.check();
                                });
                                delete _status.createControl;
                            };
                            if (lib.onfree) {
                                lib.onfree.push(function () {
                                    event.dialogxx = ui.create.characterDialog('heightset');
                                    if (ui.cheat2) {
                                        ui.cheat2.addTempClass('controlpressdownx', 500);
                                        ui.cheat2.classList.remove('disabled');
                                    }
                                });
                            } else {
                                event.dialogxx = ui.create.characterDialog('heightset');
                            }
                            ui.create.cheat2 = function () {
                                ui.cheat2 = ui.create.control('自由选将', function () {
                                    if (this.dialog == _status.event.dialog) {
                                        if (game.changeCoin) {
                                            game.changeCoin(50);
                                        }
                                        this.dialog.close();
                                        _status.event.dialog = this.backup;
                                        this.backup.open();
                                        delete this.backup;
                                        game.uncheck();
                                        game.check();
                                        if (ui.cheat) {
                                            ui.cheat.addTempClass('controlpressdownx', 500);
                                            ui.cheat.classList.remove('disabled');
                                        }
                                    } else {
                                        if (game.changeCoin) {
                                            game.changeCoin(-10);
                                        }
                                        this.backup = _status.event.dialog;
                                        _status.event.dialog.close();
                                        _status.event.dialog = _status.event.parent.dialogxx;
                                        this.dialog = _status.event.dialog;
                                        this.dialog.open();
                                        game.uncheck();
                                        game.check();
                                        if (ui.cheat) {
                                            ui.cheat.classList.add('disabled');
                                        }
                                    }
                                });
                                if (lib.onfree) {
                                    ui.cheat2.classList.add('disabled');
                                }
                            };
                            if (!_status.brawl || !_status.brawl.chooseCharacterFixed) {
                                if (!ui.cheat && get.config('change_choice')) ui.create.cheat();
                                if (!ui.cheat2 && get.config('free_choose')) ui.create.cheat2();
                            }
                            ('step 1');
                            if (ui.cheat) {
                                ui.cheat.close();
                                delete ui.cheat;
                            }
                            if (ui.cheat2) {
                                ui.cheat2.close();
                                delete ui.cheat2;
                            }
                            if (event.chosen.length) {
                                event.choosed = event.chosen;
                            } else if (event.modchosen) {
                                if (event.modchosen[0] == 'random') event.modchosen[0] = result.buttons[0].link;
                                else event.modchosen[1] = result.buttons[0].link;
                                event.choosed = event.modchosen;
                            } else if (result.buttons.length == 2) {
                                event.choosed = [result.buttons[0].link, result.buttons[1].link];
                                game.addRecentCharacter(result.buttons[0].link, result.buttons[1].link);
                            } else {
                                event.choosed = [result.buttons[0].link];
                                game.addRecentCharacter(result.buttons[0].link);
                            }
                            var name = event.choosed[0];
                            if (get.is.double(name)) {
                                game.me._groupChosen = true;
                                game.me.chooseControl(get.is.double(name, true)).set('prompt', '请选择你的势力');
                            } else if (lib.character[name][1] == 'shen' && !lib.character[name][4].includes('hiddenSkill') && get.config('choose_group')) {
                                var list = lib.group.slice(0);
                                list.remove('shen');
                                game.me.chooseControl(list).set('prompt', '请选择神武将的势力');
                            }
                            ('step 2');
                            event.group = result.control || false;
                            if (event.choosed.length == 2) {
                                game.me.init(event.choosed[0], event.choosed[1]);
                            } else {
                                game.me.init(event.choosed[0]);
                            }
                            event.list.remove(get.sourceCharacter(game.me.name1));
                            event.list.remove(get.sourceCharacter(game.me.name2));
                            if (game.me == game.zhu && game.players.length > 4) {
                                game.me.hp++;
                                game.me.maxHp++;
                                game.me.update();
                            }
                            for (var i = 0; i < game.players.length; i++) {
                                if (game.players[i] != game.zhu && game.players[i] != game.me) {
                                    event.list.randomSort();
                                    var evtlist = event.list;
                                    var banZhong = lib.config.extension_云将_zhong;
                                    var banFan = lib.config.extension_云将_fan;
                                    var banNei = lib.config.extension_云将_nei;
                                    if (lib.config.extension_云将_zhong == undefined) banZhong = [];
                                    if (lib.config.extension_云将_fan == undefined) banFan = [];
                                    if (lib.config.extension_云将_nei == undefined) banNei = [];
                                    if (game.players[i].identity == 'zhu') {
                                        for (var j = 0; j < evtlist.length; j++) {
                                            if (banZhu.includes(evtlist[j])) evtlist.remove(evtlist[j]);
                                        }
                                    }
                                    if (game.players[i].identity == 'zhong') {
                                        for (var j = 0; j < evtlist.length; j++) {
                                            if (banZhong.includes(evtlist[j])) evtlist.remove(evtlist[j]);
                                        }
                                    }
                                    if (game.players[i].identity == 'fan') {
                                        for (var j = 0; j < evtlist.length; j++) {
                                            if (banFan.includes(evtlist[j])) evtlist.remove(evtlist[j]);
                                        }
                                    }
                                    if (game.players[i].identity == 'nei') {
                                        for (var j = 0; j < evtlist.length; j++) {
                                            if (banNei.includes(evtlist[j])) evtlist.remove(evtlist[j]);
                                        }
                                    }
                                    event.ai(game.players[i], evtlist.splice(0, get.config('choice_' + game.players[i].identity)), null, evtlist);
                                }
                            }
                            ('step 3');
                            if (event.group) {
                                game.me.group = event.group;
                                game.me.node.name.dataset.nature = get.groupnature(game.me.group);
                                game.me.update();
                            }
                            for (var i = 0; i < game.players.length; i++) {
                                _status.characterlist.remove(game.players[i].name);
                                _status.characterlist.remove(game.players[i].name1);
                                _status.characterlist.remove(game.players[i].name2);
                            }
                            ('step 4');
                            setTimeout(function () {
                                ui.arena.classList.remove('choose-character');
                            }, 500);
                            if (event.special_identity) {
                                for (var i = 0; i < event.special_identity.length; i++) {
                                    game.zhu.addSkill(event.special_identity[i]);
                                }
                            }
                        });
                    };
                }
            }
        },
        precontent() {
            //—————————————————————————————————————————————————————————————————————————————技能相关自创函数
            const jineng = function () {
                lib.element.player.qhasSkill = function (s) {
                    const player = this;
                    return player.GS().includes(s);
                }; //武将是否拥有某技能
                lib.element.player.GS = function () {
                    const player = this;
                    const skills = player.skills.slice();
                    for (const i of Array.from(player.node.equips.childNodes)) {
                        if (Array.isArray(lib.card[i.name].skills)) {
                            skills.addArray(lib.card[i.name].skills);
                        }
                    }
                    for (const i in player.additionalSkills) {
                        if (Array.isArray(player.additionalSkills[i])) {
                            skills.addArray(player.additionalSkills[i]);
                        } else if (typeof player.additionalSkills[i] == 'string') {
                            skills.add(player.additionalSkills[i]);
                        }
                    }
                    skills.addArray(Object.keys(player.tempSkills));
                    skills.addArray(player.hiddenSkills);
                    skills.addArray(player.invisibleSkills);
                    return skills;
                }; //获取武将所有技能函数
                lib.element.player.GAS = function () {
                    const player = this;
                    const skills = player.skills.slice();
                    for (const i in player.additionalSkills) {
                        if (Array.isArray(player.additionalSkills[i])) {
                            skills.addArray(player.additionalSkills[i]);
                        } else if (typeof player.additionalSkills[i] == 'string') {
                            skills.add(player.additionalSkills[i]);
                        }
                    }
                    return skills;
                }; //获取武将的武将牌上技能函数
                lib.element.player.GES = function () {
                    const player = this;
                    const skills = [];
                    for (const i of Array.from(player.node.equips.childNodes)) {
                        if (Array.isArray(lib.card[i.name].skills)) {
                            skills.addArray(lib.card[i.name].skills);
                        }
                    }
                    return skills;
                }; //获取武将装备技能函数
                lib.element.player.GTS = function () {
                    const player = this;
                    return Object.keys(player.tempSkills);
                }; //获取武将临时技能函数
                lib.element.player.RS = function (skillx) {
                    const player = this;
                    if (Array.isArray(skillx)) {
                        for (const i of skillx) {
                            player.RS(i);
                        }
                    } else {
                        player.skills.remove(skillx);
                        player.hiddenSkills.remove(skillx);
                        player.invisibleSkills.remove(skillx);
                        delete player.tempSkills[skillx];
                        for (var i in player.additionalSkills) {
                            player.additionalSkills[i].remove(skillx);
                        }
                        player.checkConflict(skillx);
                        player.RST(skillx);
                        if (lib.skill.global.includes(skillx)) {
                            lib.skill.global.remove(skillx);
                            delete lib.skill.globalmap[skillx];
                            for (var i in lib.hook.globalskill) {
                                lib.hook.globalskill[i].remove(skillx);
                            }
                        }
                    }
                    return player;
                }; //移除技能函数
                lib.element.player.RST = function (skills) {
                    const player = this;
                    if (typeof skills == 'string') {
                        skills = [skills];
                    }
                    game.expandSkills(skills);
                    for (const skillx of skills) {
                        player.initedSkills.remove(skillx);
                        for (var i in lib.hook) {
                            if (Array.isArray(lib.hook[i]) && lib.hook[i].includes(skillx)) {
                                try {
                                    delete lib.hook[i];
                                } catch (e) {
                                    console.log(i + 'lib.hook不能delete');
                                }
                            }
                        }
                        for (var i in lib.hook.globalskill) {
                            if (lib.hook.globalskill[i].includes(skillx)) {
                                lib.hook.globalskill[i].remove(skillx);
                                if (lib.hook.globalskill[i].length == 0) {
                                    delete lib.hook.globalskill[i];
                                }
                            }
                        }
                    }
                    return player;
                }; //移除技能时机函数
                lib.element.player.CS = function () {
                    const player = this;
                    const skill = player.GS();
                    game.expandSkills(skill);
                    player.skills = [];
                    player.tempSkills = {};
                    player.initedSkills = [];
                    player.invisibleSkills = [];
                    player.hiddenSkills = [];
                    player.additionalSkills = {};
                    for (const key in lib.hook) {
                        if (key.startsWith(player.playerid)) {
                            try {
                                delete lib.hook[key];
                            } catch (e) {
                                console.log(key + 'lib.hook不能delete');
                            }
                        }
                    }
                    for (const hook in lib.hook.globalskill) {
                        for (const i of skill) {
                            if (lib.hook.globalskill[hook].includes(i)) {
                                lib.hook.globalskill[hook].remove(i);
                            }
                        }
                    }
                    return player;
                }; //清空所有技能函数
                lib.element.player.DS = function () {
                    const player = this;
                    const skill = player.GS();
                    game.expandSkills(skill);
                    player._hookTrigger = ['QQQ_fengjin'];
                    player.storage.skill_blocker = ['QQQ_fengjin'];
                    for (const i of skill) {
                        player.disabledSkills[i] = 'QQQ';
                        player.storage[`temp_ban_${i}`] = true;
                    }
                    return player;
                }; //失效所有技能函数
                lib.skill.QQQ_fengjin = {
                    hookTrigger: {
                        block: (event, player, triggername, skill) => true,
                    },
                    skillBlocker(skill, player) {
                        const info = lib.skill[skill];
                        return info && !info.kangxing;
                    },
                };
            }; //技能相关自创函数
            jineng();
            game.import('character', function (lib, game, ui, get, ai, _status) {
                const QQQ = {
                    name: '云将',
                    connect: true,
                    dynamicTranslate: {},
                    characterIntro: {},
                    skill: {
                        yungkd: {
                            forced: true,
                            popup: false,
                            silent: true,
                            charlotte: true,
                            audio: 'ext:云将/audio:1',
                            enable: 'phaseUse',
                            position: 'hes',
                            usable: 1,
                            filter(event, player) {
                                return player.countCards('hes') > 0 && player.name1 == 'yunyouzi';
                            },
                            filterCard: true,
                            filterTarget(card, player, target) {
                                return target != player;
                            },
                            check(card) {
                                return 7 - get.value(card);
                            },
                            selectCard: 1,
                            prompt: '一回合限一次:弃置一张牌对一名其他角色造成等同弃置牌点数的神圣伤害并移除其武将牌上全部技能,若其因此死亡,重置此技能使用次数',
                            content() {
                                'step 0';
                                var num = cards[0].number;
                                target.disableSkill('yungkd', lib.character[target.name][3]);
                                player.addTempSkill('unequip', 'gkd');
                                target.damage(num)._triggered = null;
                                ('step 1');
                                event.trigger('gkd');
                                if (!target.isAlive()) {
                                    delete player.getStat('skill').yungkd;
                                }
                            },
                        },
                        fengyunshibian_jx: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'phaseZhunbeiBefore',
                            },
                            forced: true,
                            charlotte: true,
                            filter(event, player) {
                                return game.roundNumber == 3;
                            },
                            content() {
                                'step 0';
                                player.chooseControl('魏', '群', true).set('ai', function () {
                                    if (player.identity == 'zhong') {
                                        return '魏';
                                    }
                                    if (player.identity == 'fan') {
                                        return '群';
                                    }
                                    if (player.identity == 'zhu') {
                                        return '群';
                                    }
                                    if (player.identity == 'nei') {
                                        return ['魏', '群'].randomGet();
                                    }
                                });
                                ('step 1');
                                if (result.control == '魏') {
                                    if (player.group != 'wei') player.changeGroup('wei');
                                    player.useSkill('yunyongdi');
                                    ui.backgroundMusic.src = 'extension/云将/audio/fengyunshibian_wei.mp3';
                                }
                                if (result.control == '群') {
                                    if (player.group != 'qun') player.changeGroup('qun');
                                    player.useSkill('yunluanwu');
                                    ui.backgroundMusic.src = 'extension/云将/audio/fengyunshibian_qun.mp3';
                                }
                                ('step 2');
                                player.removeSkill('fengyunshibian_jx');
                            },
                        },
                        fengyunshibian_mc: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'phaseZhunbeiBefore',
                            },
                            forced: true,
                            charlotte: true,
                            filter(event, player) {
                                return game.roundNumber == 3;
                            },
                            content() {
                                'step 0';
                                player.chooseControl('蜀', '群', true).set('ai', function () {
                                    return ['蜀', '群'].randomGet();
                                });
                                ('step 1');
                                if (result.control == '蜀') {
                                    if (player.group != 'shu') player.changeGroup('shu');
                                    player.addSkill('fengyunshibian_mc_shu');
                                    ui.backgroundMusic.src = 'extension/云将/audio/fengyunshibian_shu.mp3';
                                }
                                if (result.control == '群') {
                                    if (player.group != 'qun') player.changeGroup('qun');
                                    player.gainMaxHp();
                                    player.addSkill('fengyunshibian_mc_qun');
                                    ui.backgroundMusic.src = 'extension/云将/audio/fengyunshibian_qun.mp3';
                                }
                                ('step 2');
                                if (player.name1 == 'db_yunmachao') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/天威神将a.jpg');
                                }
                                player.removeSkill('fengyunshibian_mc');
                            },
                        },
                        fengyunshibian_mc_qun: {
                            trigger: {
                                global: 'damageBegin',
                            },
                            popup: false,
                            forced: true,
                            fixed: true,
                            charlotte: true,
                            content() {
                                trigger.num++;
                            },
                        },
                        fengyunshibian_mc_shu: {
                            trigger: {
                                global: 'dieAfter',
                            },
                            forced: true,
                            content() {
                                if (player.maxHp < 9) {
                                    player.gainMaxHp();
                                }
                                if (player.hp > 1) {
                                    player.loseHp();
                                }
                                if (_status.currentPhase != player) {
                                    player.chooseToUse({ name: 'sha' }, '势变•蜀:是否使用一张杀？');
                                }
                            },
                            ai: {
                                threaten: 1.5,
                            },
                        },
                        fengyunshibian_dxq: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'phaseZhunbeiBefore',
                            },
                            forced: true,
                            charlotte: true,
                            filter(event, player) {
                                return game.roundNumber == 3;
                            },
                            content() {
                                'step 0';
                                player.chooseControl('吴', '群', true).set('ai', function () {
                                    return ['吴', '群'].randomGet();
                                });
                                ('step 1');
                                player.node.avatar.setBackgroundImage('extension/云将/image/db_yundaxiaoqiaoa.jpg');
                                if (result.control == '吴') {
                                    if (player.group != 'wu') player.changeGroup('wu');
                                    player.addSkill('fengyunshibian_dxq_wu');
                                    if (player.name1 == 'db_yundaxiaoqiao') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/花开并蒂a.jpg');
                                    }
                                    ui.backgroundMusic.src = 'extension/云将/audio/fengyunshibian_wu.mp3';
                                }
                                if (result.control == '群') {
                                    if (player.group != 'qun') player.changeGroup('qun');
                                    player.addSkill('yuntianxiang_damage');
                                    player.addSkill('fengyunshibian_dxq_qun');
                                    if (player.name1 == 'db_yundaxiaoqiao') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/花开并蒂b.jpg');
                                    }
                                    ui.backgroundMusic.src = 'extension/云将/audio/fengyunshibian_qun.mp3';
                                }
                                ('step 2');
                                player.removeSkill('fengyunshibian_dxq');
                            },
                        },
                        fengyunshibian_dxq_wu: {
                            charlotte: true,
                        },
                        fengyunshibian_dxq_qun: {
                            charlotte: true,
                        },
                        yunshenyi: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                player: 'loseAfter',
                            },
                            forced: true,
                            fixed: true,
                            filter(event, player) {
                                return player.countCards('h') < player.maxHp;
                            },
                            content() {
                                player.draw(player.maxHp - player.countCards('h'));
                            },
                        },
                        yunshenzi: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                global: 'damageEnd',
                            },
                            usable: 1,
                            fixed: true,
                            charlotte: true,
                            superCharlotte: true, //QQQ
                            async content(event, trigger, player) {
                                var skills = trigger.player.GAS().filter((Q) => !player.hasSkill(Q));
                                if (!skills.length) player.draw();
                                else {
                                    const result = await player
                                        .chooseControl('摸一张牌', '获得一个技能')
                                        .set('ai', (event, player) => {
                                            if (player.hp < 2 || player.countCards('h') < 2) return '选项一';
                                            return '选项二';
                                        })
                                        .forResult();
                                    if (result.control == '获得一个技能') {
                                        const result1 = await player
                                            .chooseControl(skills)
                                            .set(
                                                'choiceList',
                                                skills.map((i) => '<div class="skill">【' + get.translation(lib.translate[i + '_ab'] || get.translation(i).slice(0, 2)) + '】</div><div>' + get.skillInfoTranslation(i, player) + '</div>'),
                                            )
                                            .set('displayIndex', false)
                                            .set('prompt', '选择你要获得的技能')
                                            .set('ai', () => skills.randomGet())
                                            .forResult();
                                        player.addAdditionalSkill('yunshenzi', result1.control);
                                        game.log(player, '获得技能', '【' + get.translation(result1.control) + '】');
                                    } else player.draw();
                                }
                            },
                        },
                        yunshenquan: {
                            audio: 'ext:云将/audio:3',
                            enable: 'phaseUse',
                            delay: 0,
                            fixed: true,
                            forced: true,
                            charlotte: true,
                            usable: 1,
                            filter(event, player) {
                                for (var i = 0; i < game.players.length; i++) {
                                    if (game.players[i] != player && game.players[i].countCards('hej')) {
                                        return true;
                                    }
                                }
                                return false;
                            },
                            filterTarget(card, player, target) {
                                return player != target && target.countCards('hej') > 0;
                            },
                            content() {
                                'step 0';
                                event.dialog = ui.create.dialog('hidden');
                                event.dialog.add('使用' + get.translation(target) + '区域内一张牌或不使用令【神权】失效');
                                event.position = 'hej';
                                var position = event.position;
                                for (var i = 0; i < position.length; i++) {
                                    if (position[i] == 'h' && target.countCards('h')) {
                                        event.dialog.add('手牌区');
                                        event.dialog.add(target.getCards('h'));
                                    } else if (position[i] == 'e' && target.countCards('e')) {
                                        event.dialog.add('装备区');
                                        event.dialog.add(target.getCards('e'));
                                    } else if (position[i] == 'j' && target.countCards('j')) {
                                        event.dialog.add('判定区');
                                        event.dialog.add(target.getCards('j'));
                                    }
                                }
                                var dialog = event.dialog;
                                var trigger = event.parent.parent;
                                player.chooseButton(dialog, function () {
                                    return 1;
                                });
                                ('step 1');
                                if (result.bool) {
                                    player.chooseUseTarget(result.links, false, true);
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                if (result.bool) {
                                    delete player.getStat('skill').yunshenquan;
                                }
                            },
                            ai: {
                                order: 8,
                                result: {
                                    player(player) {
                                        return 1;
                                    },
                                },
                                threaten: 10,
                            },
                        },
                        yunjilue: {
                            audio: 'ext:云将/audio:4',
                            enable: ['chooseToUse', 'chooseToRespond'],
                            fixed: true,
                            forced: true,
                            charlotte: true,
                            popup: false,
                            hiddenCard(player, name) {
                                return !player.storage.yunjilue.includes(name) && player.countCards('hes') > 0 && lib.inpile.includes(name);
                            },
                            init(player) {
                                player.storage.yunjilue = [];
                            },
                            chooseButton: {
                                dialog(event, player) {
                                    var list = [];
                                    for (var i = 0; i < lib.inpile.length; i++) {
                                        var name = lib.inpile[i];
                                        if (player.storage.yunjilue.includes(name)) continue;
                                        if (name == 'sha') {
                                            list.push(['基本', '', 'sha']);
                                            list.push(['基本', '', 'sha', 'fire']);
                                            list.push(['基本', '', 'sha', 'thunder']);
                                            list.push(['基本', '', 'sha', 'ice']);
                                        } else if (get.type2(name) == 'trick') list.push(['锦囊', '', name]);
                                        else if (get.type(name) == 'basic') list.push(['基本', '', name]);
                                    }
                                    if (list.length == 0) {
                                        return ui.create.dialog('极略已无牌可用');
                                    }
                                    return ui.create.dialog('极略', [list, 'vcard']);
                                },
                                filter(button, player) {
                                    return _status.event.parent.filterCard(
                                        {
                                            name: button.link[2],
                                        },
                                        player,
                                        _status.event.parent,
                                    );
                                },
                                check(button) {
                                    if (_status.event.parent.type != 'phase') return 1;
                                    var player = _status.event.player;
                                    if (['wugu', 'zhulu_card', 'yiyi', 'lulitongxin', 'lianjunshengyan', 'diaohulishan'].includes(button.link[2])) return 0;
                                    return player.getUseValue({
                                        name: button.link[2],
                                        nature: button.link[3],
                                    });
                                },
                                backup(links, player) {
                                    return {
                                        filterCard: true,
                                        selectCard: 1,
                                        popname: true,
                                        check(card) {
                                            return 6 - get.value(card);
                                        },
                                        position: 'hes',
                                        viewAs: {
                                            name: links[0][2],
                                            nature: links[0][3],
                                        },
                                        onuse(result, player) {
                                            player.storage.yunjilue.add(result.card.name);
                                        },
                                    };
                                },
                                prompt(links, player) {
                                    return '将一张牌当做' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]) + '使用';
                                },
                            },
                            ai: {
                                fireAttack: true,
                                save: true,
                                respondSha: true,
                                respondShan: true,
                                skillTagFilter(player) {
                                    if (!player.countCards('hes')) return false;
                                },
                                order: 10,
                                basic: {
                                    useful: [6, 4, 3],
                                    value: [6, 4, 3],
                                },
                                result: {
                                    player(player) {
                                        if (_status.event.type == 'dying') {
                                            return get.attitude(player, _status.event.dying);
                                        } else {
                                            return 1;
                                        }
                                    },
                                },
                            },
                            group: ['yunjilue_jieshu'],
                            subSkill: {
                                jieshu: {
                                    trigger: {
                                        global: 'phaseJieshuAfter',
                                    },
                                    silent: true,
                                    fixed: true,
                                    forced: true,
                                    popup: false,
                                    charlotte: true,
                                    content() {
                                        player.storage.yunjilue = [];
                                    },
                                },
                            },
                        },
                        yun_luanwu: {
                            audio: 'ext:云将/audio:4',
                            trigger: {
                                player: 'useCardToPlayered',
                            },
                            forced: true,
                            popup: false,
                            usable: 1,
                            init(player) {
                                player.storage.yun_luanwu_jn = 0;
                                player.storage.yun_luanwu_yi_cs = 0;
                                player.storage.yun_luanwu_er_cs = 0;
                            },
                            mark: true,
                            marktext: '乱舞',
                            intro: {
                                name: '乱舞',
                                content(storage, player) {
                                    var num = player.storage.yun_luanwu_jn;
                                    var numb = player.storage.yun_luanwu_yi_cs;
                                    var nume = player.storage.yun_luanwu_er_cs;
                                    var str = '未发动';
                                    if (player.hasSkill('yun_luanwu_yi')) {
                                        str = '<li>本回合限' + (num - numb) + '次,使用一张【杀】首次造成伤害后回复一点体力并摸' + num + '张牌';
                                    }
                                    if (player.hasSkill('yun_luanwu_er')) {
                                        str += '<li>本回合【乱舞】还可额外使用' + (num - Math.min(3, nume)) + '次【杀】';
                                    }
                                    if (player.hasSkill('yun_luanwu_san')) {
                                        str += '<li>本回合使用【杀】指定的目标需要使用额外' + num + '张【闪】响应';
                                    }
                                    return str;
                                },
                            },
                            filter(event, player) {
                                return event.card && event.card.name == 'sha';
                            },
                            content() {
                                'step 0';
                                if (trigger.target.name == 'lvbu' || trigger.target.name == 're_lvbu' || trigger.target.name == 'shen_lvbu' || trigger.target.name == 'yunlvbu') {
                                    player.say(['向端丽的吾父发起叛逆!', 'Clarent Blood Arthur!', '我が丽しき父への叛逆!'].randomGet());
                                }
                                ('step 1');
                                event.num = Math.min(3, player.getDamagedHp() + 1);
                                player.storage.yun_luanwu_jn = Math.min(3, player.getDamagedHp() + 1);
                                ('step 2');
                                if (event.num > 0) {
                                    var controls = ['选项一', '选项二', '选项三'];
                                    if (player.hasSkill('yun_luanwu_yi')) controls.remove('选项一');
                                    if (player.hasSkill('yun_luanwu_er')) controls.remove('选项二');
                                    if (player.hasSkill('yun_luanwu_san')) controls.remove('选项三');
                                    player
                                        .chooseControl(controls, true)
                                        .set('prompt', '乱舞<br>选择获得' + get.cnNumber(event.num) + '项效果直到回合结束<br><br><div class="text">选项一:本回合限' + get.cnNumber(player.storage.yun_luanwu_jn) + '次,使用【杀】造成伤害后回复一点体力并摸' + get.cnNumber(player.storage.yun_luanwu_jn) + '张牌.</div><br><div class="text">选项二:本回合可额外使用' + get.cnNumber(player.storage.yun_luanwu_jn) + '次【杀】.</div><br><div class="text">选项三:本回合使用【杀】指定的目标需要额外使用' + get.cnNumber(player.storage.yun_luanwu_jn) + '张【闪】响应.</div></br>')
                                        .set('ai', function (event, player) {
                                            if (!player.hasSkill('yun_luanwu_yi')) {
                                                return '选项一';
                                            }
                                            if (player.hasSkill('yun_luanwu_yi') && player.hasSkill('yun_luanwu_san')) {
                                                return '选项二';
                                            }
                                            if (player.hasSkill('yun_luanwu_yi') && !player.hasSkill('yun_luanwu_er') && player.countCards('hs', { name: 'sha' }) >= 1) {
                                                return '选项二';
                                            } else {
                                                return '选项三';
                                            }
                                        });
                                }
                                ('step 3');
                                if (result.control == '选项一') {
                                    player.addTempSkill('yun_luanwu_yi');
                                    event.num--;
                                    if (event.num > 0) {
                                        event.goto(2);
                                    } else {
                                        event.finish();
                                    }
                                }
                                if (result.control == '选项二') {
                                    player.addTempSkill('yun_luanwu_er');
                                    event.num--;
                                    if (event.num > 0) {
                                        event.goto(2);
                                    } else {
                                        event.finish();
                                    }
                                }
                                if (result.control == '选项三') {
                                    player.addTempSkill('yun_luanwu_san');
                                    event.num--;
                                    if (event.num > 0) {
                                        event.goto(2);
                                    } else {
                                        event.finish();
                                    }
                                }
                            },
                            mod: {
                                attackFrom(from, to, num) {
                                    return num - Math.min(3, from.getDamagedHp() + 1);
                                },
                            },
                            ai: {
                                threaten: 0.5,
                            },
                            group: ['yun_luanwu_fenghua_liaoluan', 'yun_luanwu_jieshu', 'yun_luanwu_sha'],
                            subSkill: {
                                jieshu: {
                                    trigger: {
                                        global: 'phaseJieshuAfter',
                                    },
                                    silent: true,
                                    fixed: true,
                                    forced: true,
                                    popup: false,
                                    charlotte: true,
                                    content() {
                                        player.storage.yun_luanwu_jn = 0;
                                        player.storage.yun_luanwu_yi_cs = 0;
                                        player.storage.yun_luanwu_er_cs = 0; //QQQ
                                    },
                                },
                                sha: {
                                    trigger: {
                                        player: 'useCard',
                                    },
                                    silent: true,
                                    fixed: true,
                                    forced: true,
                                    popup: false,
                                    charlotte: true,
                                    filter(event, player) {
                                        return event.card && event.card.name == 'sha';
                                    },
                                    content() {
                                        if (player.storage.yun_luanwu_er_cs < player.storage.yun_luanwu_jn) {
                                            player.storage.yun_luanwu_er_cs++;
                                        }
                                    },
                                },
                            },
                        },
                        yun_luanwu_fenghua_liaoluan: {
                            audio: 'ext:云将/audio:4',
                            trigger: {
                                player: 'phaseUseBefore',
                            },
                            popup: false,
                            check(event, player) {
                                if (player.hp <= 2 && player.countCards('hs', 'sha') >= 1) return false;
                                if (player.hp <= 1) return false;
                                return (
                                    !player.getDamagedHp() ||
                                    (player.hp > 1 &&
                                        player.countCards('hs', 'sha') <= 0 &&
                                        game.hasPlayer(function (current) {
                                            return current != player && player.inRange(current) && get.attitude(player, current) < 0;
                                        }))
                                );
                            },
                            prompt(event, player) {
                                var str = '';
                                str += '是否失去一点体力从牌堆获得一张【杀】？';
                                return str;
                            },
                            content() {
                                player.loseHp();
                                var card = get.cardPile2(function (card) {
                                    return card.name == 'sha';
                                });
                                if (card) player.gain(card, 'gain2');
                                else game.log('但是牌堆里已经没有杀了!');
                            },
                        },
                        yun_luanwu_yi: {
                            trigger: {
                                source: 'damageEnd',
                            },
                            forced: true,
                            popup: false,
                            silent: true,
                            charlotte: true,
                            filter(event, player) {
                                return event.card && event.card.name == 'sha' && player.storage.yun_luanwu_yi_cs < player.storage.yun_luanwu_jn;
                            },
                            content() {
                                var num = player.storage.yun_luanwu_jn;
                                player.storage.yun_luanwu_yi_cs++;
                                player.recover();
                                player.draw(num);
                                //player.chooseDrawRecover(num,1,true,'回复一点体力或摸'+num+'张牌');
                            },
                        },
                        yun_luanwu_er: {
                            forced: true,
                            popup: false,
                            silent: true,
                            charlotte: true,
                            mod: {
                                cardUsable(card, player, num) {
                                    if (card.name == 'sha') return (num += player.storage.yun_luanwu_jn);
                                },
                            },
                        },
                        yun_luanwu_san: {
                            trigger: {
                                player: 'useCardToPlayered',
                            },
                            forced: true,
                            popup: false,
                            silent: true,
                            charlotte: true,
                            filter(event, player) {
                                return event.card.name == 'sha' && !event.parent.directHit.includes(event.target);
                            },
                            logTarget: 'target',
                            content() {
                                var num = player.storage.yun_luanwu_jn + 1;
                                var id = trigger.target.playerid;
                                var map = trigger.parent.customArgs;
                                if (!map[id]) map[id] = {};
                                if (typeof map[id].shanRequired == 'number') {
                                    map[id].shanRequired += num;
                                } else {
                                    map[id].shanRequired = num;
                                }
                            },
                            ai: {
                                directHit_ai: true,
                                skillTagFilter(player, tag, arg) {
                                    var num = player.storage.yun_luanwu_jn + 1;
                                    if ((arg && arg.card.name != 'sha') || arg.target.countCards('h', 'shan') > num) return false;
                                },
                            },
                        },
                        yunyice: {
                            audio: 'ext:云将/audio:5',
                            trigger: {
                                player: 'damageEnd',
                            },
                            forced: true,
                            filter(event, player) {
                                return event.num > 0;
                            },
                            content() {
                                'step 0';
                                player.draw(Math.min(3, player.getDamagedHp()));
                                ('step 1');
                                player.chooseCardTarget({
                                    filterCard: true,
                                    selectCard: [1, Math.min(3, player.getDamagedHp())],
                                    filterTarget(card, player, target) {
                                        return player != target;
                                    },
                                    ai1(card) {
                                        if (ui.selected.cards.length) return -1;
                                        if (card.name == 'du') return 20;
                                        return _status.event.player.countCards('h') - _status.event.player.hp;
                                    },
                                    ai2(target) {
                                        var att = get.attitude(_status.event.player, target);
                                        if (ui.selected.cards.length && ui.selected.cards[0].name == 'du') {
                                            if (target.hasSkillTag('nodu')) return 0;
                                            return 1 - att;
                                        }
                                        return att - 4;
                                    },
                                    prompt: '是否将最多' + get.cnNumber(Math.min(3, player.getDamagedHp())) + '张手牌交给一名其他角色？',
                                });
                                ('step 2');
                                if (result.bool) {
                                    player.say(['主公勿忧,奉孝在此!', '虽然我吃喝嫖赌,但我是个好谋士'].randomGet());
                                    player.line(result.targets, 'green');
                                    result.targets[0].gain(result.cards, player, 'giveAuto');
                                }
                                ('step 3');
                                player.judge();
                                ('step 4');
                                event.color = result.color;
                                if (event.color == 'black') {
                                    player
                                        .chooseTarget('弃置一名角色区域内的一张牌', function (card, player, target) {
                                            return target.countCards('hej');
                                        })
                                        .set('ai', function (target) {
                                            var player = _status.event.player;
                                            var att = get.attitude(player, target);
                                            if (att < 0) {
                                                att = -Math.sqrt(-att);
                                            } else {
                                                att = Math.sqrt(att);
                                            }
                                            return att * lib.card.guohe.ai.result.target(player, target);
                                        });
                                } else {
                                    if (event.color == 'red') {
                                        player.recover();
                                        event.finish();
                                    }
                                }
                                ('step 5');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    player.line(target, 'green');
                                    player.discardPlayerCard(target, 'hej', true);
                                }
                            },
                            ai: {
                                maixie: true,
                                maixie_hp: true,
                                effect: {
                                    target(card, player, target) {
                                        if (get.tag(card, 'damage')) {
                                            if (player.hasSkillTag('jueqing', false, target)) return [1, -2];
                                            if (!target.hasFriend()) return;
                                            var num = 1;
                                            if (get.attitude(player, target) > 0) {
                                                if (player.needsToDiscard()) {
                                                    num = 0.7;
                                                } else {
                                                    num = 0.5;
                                                }
                                            }
                                            if (target.hp >= 4) return [1, num * 2];
                                            if (target.hp == 3) return [1, num * 1.5];
                                            if (target.hp == 2) return [1, num * 0.5];
                                        }
                                    },
                                },
                            },
                        },
                        yunguimou: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            usable: 1,
                            filterTarget(card, player, target) {
                                return target != player;
                            },
                            content() {
                                'step 0';
                                player.chooseToPSS(target);
                                ('step 1');
                                if (result.bool == true) {
                                    event.trigger('guimou');
                                    event.finish();
                                }
                                if (result.bool == false) {
                                    player.damage('nosource');
                                    event.finish();
                                }
                            },
                            ai: {
                                expose: 0.5,
                                order: 7,
                                result: {
                                    player(player, target) {
                                        if (player.hp > 1) return 1;
                                    },
                                },
                            },
                            group: 'yunguimou_trick',
                        },
                        yunguimou_trick: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'guimou',
                            },
                            forced: true,
                            content() {
                                'step 0';
                                var list = [];
                                for (var i = 0; i < lib.inpile.length; i++) {
                                    var name = lib.inpile[i];
                                    var type = get.type(name);
                                    if (type == 'trick') {
                                        if (
                                            lib.filter.cardEnabled(
                                                {
                                                    name: name,
                                                },
                                                player,
                                            )
                                        ) {
                                            list.push([get.translation(type), '', name]);
                                        }
                                    }
                                }
                                var dialog = ui.create.dialog('鬼谋', [list, 'vcard']);
                                var taoyuan = 0,
                                    nanman = 0;
                                var players = game.filterPlayer();
                                for (var i = 0; i < players.length; i++) {
                                    var eff1 = get.effect(
                                        players[i],
                                        {
                                            name: 'taoyuan',
                                        },
                                        player,
                                        player,
                                    );
                                    var eff2 = get.effect(
                                        players[i],
                                        {
                                            name: 'nanman',
                                        },
                                        player,
                                        player,
                                    );
                                    if (eff1 > 0) {
                                        taoyuan++;
                                    } else if (eff1 < 0) {
                                        taoyuan--;
                                    }
                                    if (eff2 > 0) {
                                        nanman++;
                                    } else if (eff2 < 0) {
                                        nanman--;
                                    }
                                }
                                player.chooseButton(dialog).ai = function (button) {
                                    var name = button.link[2];
                                    if (Math.max(taoyuan, nanman) > 1) {
                                        if (taoyuan > nanman) return name == 'taoyuan' ? 1 : 0;
                                        return name == 'nanman' ? 1 : 0;
                                    }
                                    if (player.countCards('h') < player.hp && player.hp >= 2) {
                                        return name == 'wuzhong' ? 1.5 : 0;
                                    }
                                    if (player.hp >= 3) {
                                        return name == 'juedou' ? 1.5 : 0;
                                    }
                                    return name == 'wuzhong' ? 1 : 0;
                                    if (
                                        game.hasPlayer(function (current) {
                                            return current != player && get.distance(player, current) <= 1;
                                        })
                                    )
                                        return name == 'shunshou' ? 1.5 : 0;
                                    if (
                                        game.hasPlayer(function (current) {
                                            return current != player && (current.hasJudge('lebu') || current.hasJudge('bingliang')) && get.distance(player, current) <= 1 && get.attitude(player, current) > 0;
                                        })
                                    )
                                        return name == 'shunshou' ? 1.5 : 0;
                                    if (
                                        game.hasPlayer(function (current) {
                                            return current != player && (current.hasJudge('lebu') || current.hasJudge('bingliang')) && get.distance(player, current) > 1 && get.attitude(player, current) > 0;
                                        })
                                    )
                                        return name == 'guohe' ? 1.5 : 0;
                                };
                                ('step 1');
                                if (result.bool) {
                                    player.chooseUseTarget(true, result.links[0][2]);
                                }
                            },
                        },
                        yuntiandu: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            limited: true,
                            mark: true,
                            filterTarget: true,
                            init(player, skill) {
                                player.storage[skill] = false;
                            },
                            prompt() {
                                return '选择一名角色:若其拥有未发动过的觉醒技,则你解除其中一个觉醒技的发动限制条件并立即觉醒;否则其摸三张牌.';
                            },
                            content() {
                                'step 0';
                                player.awakenSkill(event.name);
                                var list = [];
                                var skills = target.getOriginalSkills();
                                for (var i = 0; i < skills.length; i++) {
                                    if (lib.skill[skills[i]].juexingji && !target.awakenedSkills.includes(skills[i])) {
                                        list.push(skills[i]);
                                    }
                                }
                                if (list.length == 1) {
                                    target.storage.yuntiandu = list[0];
                                    target.useSkill(target.storage.yuntiandu);
                                    event.finish();
                                } else if (list.length > 1) {
                                    player.chooseControl(list).set('prompt', '选择一个觉醒技');
                                } else {
                                    target.draw(3);
                                    event.finish();
                                }
                                ('step 1');
                                target.storage.yuntiandu = result.control;
                                target.useSkill(target.storage.yuntiandu);
                            },
                            intro: {
                                content: 'limited',
                            },
                            ai: {
                                order: 3,
                                result: {
                                    target: 1,
                                },
                            },
                        },
                        yunqizhi: {
                            audio: 'ext:云将/audio:5',
                            trigger: {
                                player: 'useCardToPlayered',
                            },
                            forced: true,
                            marktext: '奇制',
                            intro: {
                                name: '奇制',
                                content: '你已发动#次奇制',
                            },
                            mark: true,
                            derivation: 'yunchizhong',
                            filter(event, player) {
                                if (!event.targets) return false;
                                if (!event.isFirstTarget) return false;
                                if (_status.currentPhase != player) return false;
                                var type = get.type(event.card, 'trick');
                                if (type != 'basic' && type != 'trick') return false;
                                if (event.noai) return false;
                                return game.hasPlayer(function (target) {
                                    return target.countCards('hej') > 0;
                                });
                            },
                            prompt: '是否发动【奇制】弃置一张牌',
                            content() {
                                'step 0';
                                player
                                    .chooseTarget(get.prompt('yunqizhi'), '弃置一名角色的一张牌,其摸一张牌', function (card, player, target) {
                                        return target.countCards('he') > 0;
                                    })
                                    .set('ai', function (target) {
                                        var player = _status.event.player;
                                        if (target == player) return 2;
                                        if (get.attitude(player, target) <= 0) {
                                            return 1;
                                        }
                                        return 0.5;
                                    })
                                    .set('targets', trigger.targets);
                                ('step 1');
                                if (result.bool) {
                                    player.getHistory('custom').push({
                                        yunqizhi: true,
                                    });
                                    if (!event.isMine() && !_status.connectMode) game.delay();
                                    player.discardPlayerCard(result.targets[0], true, 'he');
                                    player.addMark('yunqizhi', 1);
                                    event.target = result.targets[0];
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                if (!player.storage.yunqizhi_re) event.target.draw();
                            },
                            group: ['yunqizhi_draw'],
                            subSkill: {
                                draw: {
                                    audio: 'ext:云将/audio:5',
                                    trigger: {
                                        player: 'phaseDiscardBefore',
                                    },
                                    forced: true,
                                    content() {
                                        var num = player.getHistory('custom', function (evt) {
                                            return evt.yunqizhi == true;
                                        }).length;
                                        player.draw(num);
                                        if (player.hp <= num) {
                                            player.say(['吴懿不在此地!', '吴懿在哪？'].randomGet());
                                            player.addTempSkill('yunchizhong', { player: 'phaseZhunbeiBegin' });
                                        }
                                        player.storage.yunqizhi = 0; //QQQ
                                    },
                                },
                            },
                        },
                        yunchizhong: {
                            mark: true,
                            marktext: '持重',
                            intro: {
                                name: '持重',
                                content: true,
                            },
                            mod: {
                                targetEnabled(card, player, target) {
                                    if (get.type(card) == 'delay') {
                                        return false;
                                    }
                                },
                                maxHandcardBase(player, num) {
                                    return num * 2;
                                },
                            },
                        },
                        yuntanlang: {
                            audio: 'ext:云将/audio:2',
                            forced: true,
                            charlotte: true,
                            trigger: {
                                global: 'phaseZhunbeiBegin',
                            },
                            filter(event, player) {
                                return event.player != player && player.countCards('h') < player.maxHp;
                            },
                            content() {
                                player.draw();
                            },
                            group: ['yuntanlang_maxHp'],
                            subSkill: {
                                maxHp: {
                                    audio: 'ext:云将/audio:1',
                                    trigger: {
                                        global: 'gameStart',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return player.identity == 'zhu';
                                    },
                                    content() {
                                        player.loseMaxHp();
                                    },
                                },
                            },
                        },
                        yunwushuang: {
                            audio: 'ext:云将/audio:2',
                            derivation: 'yunjiwu',
                            trigger: {
                                player: 'phaseBegin',
                            },
                            forced: true,
                            content() {
                                game.countPlayer(function (current) {
                                    if (current != player && !current.hasSkill('fengyin')) {
                                        player.line(current, 'green');
                                        current.addTempSkill('fengyin');
                                        player.addTempSkill('yunjiwu');
                                    }
                                });
                            },
                        },
                        yunjiwu: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'useCardToPlayered',
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return player.countCards('h') > 0 && (event.card.name == 'sha' || event.card.name == 'juedou');
                            },
                            logTarget: 'target',
                            usable: 1,
                            content() {
                                'step 0';
                                var next = player.chooseToDiscard('h', get.prompt('yunjiwu', trigger.target), '是否弃置一张手牌发动【极武】令' + get.translation(trigger.card) + '造成的伤害加一');
                                next.ai = function (card) {
                                    if (get.attitude(player, trigger.target) < 0 && (trigger.target.getEquip('baiyin') || trigger.target.getEquip('rewrite_baiyin'))) {
                                        return 0;
                                    }
                                    if (get.attitude(player, trigger.target) < 0) {
                                        return 7 - get.value(card);
                                    }
                                    return 0;
                                };
                                if (trigger.card.name == 'sha') {
                                    player.say(['别心怀侥幸了,你们不可能赢!', '萤烛之火,也敢与日月争辉？'].randomGet());
                                }
                                if (trigger.card.name == 'juedou') {
                                    player.say(['出来单挑啊!', '你有没有砍过人？有没有看过死人？'].randomGet());
                                }
                                ('step 1');
                                if (result.bool) {
                                    player.addTempSkill('yunjiwu_damage', { player: 'useCardAfter' });
                                } else {
                                    player.getStat('triggerSkill').yunjiwu--;
                                }
                            },
                        },
                        yunjiwu_damage: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                source: 'damageBegin',
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return event.card && (event.card.name == 'sha' || event.card.name == 'juedou') && event.notLink();
                            },
                            content() {
                                trigger.num++;
                                if (trigger.num >= trigger.player.hp) {
                                    player.addTempSkill('yunjiwu_die', 'useCardAfter');
                                }
                            },
                        },
                        yunjiwu_die: {
                            trigger: {
                                global: 'dying',
                            },
                            forced: true,
                            silent: true,
                            firstDo: true,
                            popup: false,
                            content() {
                                'step 0';
                                trigger.player.die(trigger.reason);
                                ('step 1');
                                if (!trigger.player.isAlive()) {
                                    trigger.cancel(true);
                                }
                                player.removeSkill('yunjiwu_die');
                            },
                        },
                        yunshelue: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseBegin',
                            },
                            forced: true,
                            prompt: '是否发动【涉略】获得场上一个技能？',
                            createDialog(player, target, onlylist) {
                                var names = [];
                                var list = [];
                                if (target.name && !target.isUnseen(0)) names.add(target.name);
                                if (target.name1 && !target.isUnseen(0)) names.add(target.name1);
                                if (target.name2 && !target.isUnseen(1)) names.add(target.name2);
                                var pss = player.getSkills();
                                for (var i = 0; i < names.length; i++) {
                                    var info = lib.character[names[i]];
                                    if (info) {
                                        var skills = info[3];
                                        for (var j = 0; j < skills.length; j++) {
                                            if (lib.translate[skills[j] + '_info'] && lib.skill[skills[j]] && !lib.skill[skills[j]].unique && !pss.includes(skills[j])) {
                                                list.push(skills[j]);
                                            }
                                        }
                                    }
                                }
                                if (onlylist) return list;
                                var dialog = ui.create.dialog('forcebutton');
                                dialog.add('选择获得一项技能');
                                _status.event.list = list;
                                var clickItem = function () {
                                    _status.event._result = this.link;
                                    if (dialog) dialog.close();
                                    _status.imchoosing = false;
                                    game.resume();
                                };
                                for (var i = 0; i < list.length; i++) {
                                    if (lib.translate[list[i] + '_info']) {
                                        var translation = get.translation(list[i]);
                                        if (translation[0] == '新' && translation.length == 3) {
                                            translation = translation.slice(1, 3);
                                        } else {
                                            translation = translation.slice(0, 2);
                                        }
                                        var item = dialog.add('<div class="popup pointerdiv" style="width:80%;display:inline-block"><div class="skill">【' + translation + '】</div><div>' + lib.translate[list[i] + '_info'] + '</div></div>');
                                        item.firstChild.addEventListener('click', clickItem);
                                        item.firstChild.link = list[i];
                                    }
                                }
                                dialog.add(ui.create.div('.placeholder'));
                                return dialog;
                            },
                            content() {
                                'step 0';
                                if (player.name1 == 'yunlvmeng') {
                                    player.node.avatar.setBackground('yunlvmeng', 'character');
                                }
                                player
                                    .chooseTarget(get.prompt2('yunshelue'), function (card, player, target) {
                                        var names = [];
                                        if (target.name && !target.isUnseen(0)) names.add(target.name);
                                        if (target.name1 && !target.isUnseen(0)) names.add(target.name1);
                                        if (target.name2 && !target.isUnseen(1)) names.add(target.name2);
                                        var pss = player.getSkills();
                                        for (var i = 0; i < names.length; i++) {
                                            var info = lib.character[names[i]];
                                            if (info) {
                                                var skills = info[3];
                                                for (var j = 0; j < skills.length; j++) {
                                                    if (lib.translate[skills[j] + '_info'] && lib.skill[skills[j]] && !lib.skill[skills[j]].zhuSkill && !lib.skill[skills[j]].unique && !lib.skill[skills[j]].juexingji && !lib.skill[skills[j]].charlotte && !lib.skill[skills[j]].dutySkill && !lib.skill[skills[j]].hiddenSkill && !pss.includes(skills[j])) {
                                                        return true;
                                                    }
                                                }
                                            }
                                            return false;
                                        }
                                    })
                                    .set('ai', function (target) {
                                        return Math.random();
                                    });
                                ('step 1');
                                if (result.bool) {
                                    player.say(['学习？学个屁!', '士别三日,当刮目相待'].randomGet());
                                    event.target = result.targets[0];
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                event.skillai = function (list) {
                                    return get.max(list, get.skillRank, 'item');
                                };
                                if (event.isMine()) {
                                    event.dialog = lib.skill.yunshelue.createDialog(player, target);
                                    event.switchToAuto = function () {
                                        event._result = event.skillai(event.list);
                                        event.dialog.close();
                                        _status.imchoosing = false;
                                        game.resume();
                                    };
                                    _status.imchoosing = true;
                                    game.pause();
                                } else if (event.isOnline()) {
                                    event.player.send(function () {
                                        var event = _status.event;
                                        event.skillai = function (list) {
                                            return get.max(list, get.skillRank, 'item');
                                        };
                                        event.dialog = lib.skill.yunshelue.createDialog(player, target);
                                        event.switchToAuto = function () {
                                            event._result = event.skillai(event.list);
                                            event.dialog.close();
                                            _status.imchoosing = false;
                                            game.resume();
                                        };
                                        _status.imchoosing = true;
                                        game.pause();
                                    });
                                    event.player.wait();
                                    game.pause();
                                } else {
                                    event._result = event.skillai(lib.skill.yunshelue.createDialog(player, target, true));
                                }
                                ('step 3');
                                event.trigger('shelue');
                                player.addTempSkill(result, { player: 'shelue' });
                                player.popup(result);
                                game.log(player, '获得了', '【' + get.translation(result) + '】');
                            },
                        },
                        yunkeji: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'damageBefore',
                            },
                            _priority: -10,
                            filter(event, player) {
                                return player.countCards('he', {
                                    type: 'equip',
                                });
                            },
                            prompt: '是否发动【克己】抵消此次伤害？',
                            forced: true,
                            usable: 1,
                            content() {
                                'step 0';
                                var next = player.chooseToDiscard('he', '是否弃置一张装备牌抵消伤害？', function (card, player) {
                                    return get.type(card) == 'equip';
                                });
                                next.ai = function (card) {
                                    if (player.hp == 1 || trigger.num > 1) {
                                        return 10 - get.value(card);
                                    }
                                    if (player.hp == 2) {
                                        return 9 - get.value(card);
                                    }
                                    if (player.hp == 3) {
                                        return 8 - get.value(card);
                                    }
                                    return 7 - get.value(card);
                                };
                                ('step 1');
                                if (result.bool) {
                                    player.draw();
                                    trigger.cancel();
                                } else {
                                    delete player.getStat('skill').yunkeji;
                                }
                            },
                            ai: {
                                order: 8,
                                threaten: 3,
                            },
                            group: 'yunkeji_equip',
                            subSkill: {
                                equip: {
                                    audio: 'ext:云将/audio:4',
                                    trigger: {
                                        player: 'useCard',
                                    },
                                    usable: 1,
                                    filter(event, player) {
                                        return get.type(event.card, 'trick') == 'trick';
                                    },
                                    forced: true,
                                    content() {
                                        var card = get.cardPile2(function (card) {
                                            return get.type(card) == 'equip';
                                        });
                                        if (!card) {
                                            player.chat('无牌可得了吗');
                                            game.log('牌堆里面已经没有装备牌了!');
                                            event.finish();
                                            return;
                                        }
                                        player.gain(card, 'gain2');
                                    },
                                },
                            },
                        },
                        yunduorui: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                source: 'damageSource',
                            },
                            derivation: 'yunsangdan',
                            filter(event, player) {
                                if (event.player.hasSkill('yunfengjin')) return false;
                                return event.player.isAlive() && event.card && player != event.player;
                            },
                            check(event, player) {
                                if (get.attitude(player, event.player) > 0) return false;
                            },
                            logTarget: 'player',
                            prompt(event, player) {
                                var str = '是否对' + get.translation(event.player) + '发动【夺锐】？';
                                return str;
                            },
                            content() {
                                if (!trigger.player.hasSkill('yunsangdan')) {
                                    trigger.player.addSkill('yunsangdan');
                                } else {
                                    trigger.player.addTempSkill('yunfengjin', { player: 'phaseAfter' });
                                }
                                player.say(['随我搏杀!汝等妻子曹公养之!', '今日,定让虎儿丧胆!'].randomGet());
                            },
                            audioname2: {
                                key_shiki: 'shiki_omusubi',
                            },
                        },
                        yunzhiti: {
                            audio: 'ext:云将/audio:3',
                            mark: true,
                            marktext: '止啼',
                            intro: {
                                content(storage, player) {
                                    var num = game.countPlayer(function (current) {
                                        return current.isDamaged();
                                    });
                                    var str = '未发动';
                                    if (num >= 1) {
                                        str = '<li>摸牌阶段摸牌数加一,使用杀次数加一,手牌上限加一';
                                    }
                                    if (num >= 2) {
                                        str += '<li>已受伤的其他角色手牌上限减半(向下取整,若其已〖丧胆〗则向上取整)';
                                    }
                                    if (num >= 3) {
                                        str += '<li>废除因【夺锐】技能失效的【丧胆】角色装备栏';
                                    }
                                    return str;
                                },
                            },
                            trigger: {
                                player: 'phaseDrawBegin',
                            },
                            forced: true,
                            filter(event, player) {
                                var num = game.countPlayer(function (current) {
                                    return current.isDamaged();
                                });
                                return num >= 1;
                            },
                            content() {
                                trigger.num += Math.min(
                                    game.countPlayer(function (current) {
                                        return current.isDamaged();
                                    }),
                                    1,
                                );
                            },
                            mod: {
                                maxHandcard(player, num) {
                                    return (
                                        num +
                                        Math.min(
                                            game.countPlayer(function (current) {
                                                return current.isDamaged();
                                            }),
                                            1,
                                        )
                                    );
                                },
                                cardUsable(card, player, num) {
                                    if (card.name == 'sha')
                                        return (
                                            num +
                                            Math.min(
                                                game.countPlayer(function (current) {
                                                    return current.isDamaged();
                                                }),
                                                1,
                                            )
                                        );
                                },
                            },
                            ai: {
                                effect: {
                                    target(card, player, target, current) {
                                        if (get.tag(card, 'recover') && player.hp >= player.maxHp - 1) return 'zeroplayertarget';
                                    },
                                },
                            },
                            global: 'yunzhiti_maxHandcard',
                        },
                        yunzhiti_maxHandcard: {
                            mod: {
                                maxHandcardBase(player, num) {
                                    var num1 = game.countPlayer(function (current) {
                                        return current.isDamaged();
                                    });
                                    if (
                                        num1 >= 2 &&
                                        game.countPlayer(function (current) {
                                            return current != player && current.hasSkill('yunzhiti');
                                        }) &&
                                        player.isDamaged()
                                    ) {
                                        if (player.hasSkill('yunsangdan')) {
                                            return num - Math.ceil(num / 2);
                                        }
                                        if (!player.hasSkill('yunsangdan')) {
                                            return num - Math.floor(num / 2);
                                        }
                                    }
                                },
                            },
                        },
                        yunsangdan: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                global: ['damageEnd', 'loseHpEnd', 'gainMaxHpEnd'],
                            },
                            forced: true,
                            popup: false,
                            charlotte: true,
                            mark: true,
                            marktext: '丧胆',
                            intro: {
                                name: '丧胆',
                                content: '李典:想让爷跟你配合？爷就算死也不会听你指挥!<br>乐进:没错!劳资不服你!不爽来打一架？<br>张辽:丞相来信,说让你俩只管大胆的干,他想帮你俩养老婆孩子很久了.<br>李典、乐进:辽哥你下命令吧,你说咋干就咋干',
                            },
                            filter(event, player) {
                                var num = game.countPlayer(function (current) {
                                    return current.isDamaged();
                                });
                                return num >= 3 && player.isDamaged() && player.hasSkill('yunfengjin');
                            },
                            content() {
                                'step 0';
                                player.disableEquip('equip1');
                                player.disableEquip('equip2');
                                player.disableEquip('equip3');
                                player.disableEquip('equip4');
                                player.disableEquip('equip5');
                                ('step 1');
                                player.update();
                            },
                            group: ['yunsangdan_buff'],
                        },
                        yunsangdan_buff: {
                            trigger: {
                                player: ['loseMaxHpAfter', 'recoverAfter'],
                            },
                            forced: true,
                            popup: false,
                            charlotte: true,
                            filter(event, player) {
                                return !player.isDamaged();
                            },
                            content() {
                                'step 0';
                                player.enableEquip(1);
                                player.enableEquip(2);
                                player.enableEquip(3);
                                player.enableEquip(4);
                                player.enableEquip(5);
                                ('step 1');
                                player.update();
                                game.countPlayer(function (current) {
                                    if (current.hasSkill('yunzhiti')) {
                                    }
                                });
                                player.removeSkill('yunsangdan');
                            },
                        },
                        yunjieyin: {
                            audio: 'ext:云将/audio:2',
                            limited: true,
                            trigger: {
                                player: 'phaseZhunbeiBegin',
                            },
                            filter(event, player) {
                                return !player.storage.yunjieyin;
                            },
                            init(player) {
                                player.storage.yunjieyin = false;
                            },
                            mark: true,
                            intro: {
                                content: 'limited',
                            },
                            forced: true,
                            content() {
                                'step 0';
                                player
                                    .chooseTarget(get.prompt('yunjieyin'), '选择一名其他角色与其结为〖情缘〗', function (card, player, target) {
                                        return player != target;
                                    })
                                    .set('ai', function (target) {
                                        if (get.mode() == 'identity') {
                                            if (player.identity == 'nei') {
                                                if (Math.random() > 1 / 5) {
                                                    return get.attitude(player, target);
                                                }
                                            } else {
                                                return get.attitude(player, target);
                                            }
                                        }
                                        if (get.mode() != 'identity') {
                                            return get.attitude(player, target);
                                        }
                                    });
                                ('step 1');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    player.awakenSkill('yunjieyin');
                                    player.storage.yunjieyin = true;
                                    game.log(target, '成为了', '【结姻】', '的目标');
                                    player.say(['从现在开始,你就是我的人了!', '我们立刻开始这段感情吧!'].randomGet());
                                    target.addSkill('yunqingyuan');
                                    player.addSkill('yunqingyuan_shunshangxiang');
                                    if (player.name1 == 'yunsunshangxiang') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/yunsunshangxianga.jpg');
                                    }
                                }
                            },
                            ai: {
                                expose: 1,
                            },
                        },
                        yunqingyuan: {
                            audio: 'ext:云将/audio:3',
                            mark: true,
                            marktext: '情缘',
                            intro: {
                                name: '情缘',
                                content: '愿得一心人 白首不相离',
                            },
                            popup: false,
                            forced: true,
                            fixed: true,
                            charlotte: true,
                            ai: {
                                threaten: 1.5,
                            },
                        },
                        yunqingyuan_shunshangxiang: {
                            trigger: {
                                global: 'phaseJieshuAfter',
                            },
                            silent: true,
                            fixed: true,
                            popup: false,
                            forced: true,
                            charlotte: true,
                            mark: true,
                            intro: {
                                name: '情缘',
                                content: '愿得一心人 白首不相离',
                            },
                            init(player) {
                                player.storage.yunqingyuan_equip1 = 0;
                                player.storage.yunqingyuan_equip2 = 0;
                                player.storage.yunqingyuan_equip3 = 0;
                                player.storage.yunqingyuan_equip4 = 0;
                                player.storage.yunqingyuan_equip5 = 0;
                            },
                            content() {
                                player.storage.yunqingyuan_equip1 = 0;
                                player.storage.yunqingyuan_equip2 = 0;
                                player.storage.yunqingyuan_equip3 = 0;
                                player.storage.yunqingyuan_equip4 = 0;
                                player.storage.yunqingyuan_equip5 = 0;
                            },
                            ai: {
                                threaten: 1.5,
                            },
                        },
                        yunxiaoji: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseDrawBegin',
                            },
                            forced: true,
                            content() {
                                'step 0';
                                trigger.num--;
                                var list = get.inpile('equip');
                                list = list.randomGets(5);
                                for (var i = 0; i < list.length; i++) {
                                    list[i] = ['装备', '', list[i]];
                                }
                                var dialog = ui.create.dialog('选择一张装备牌加入你的手牌', [list, 'vcard'], 'hidden');
                                player.chooseButton(dialog, true).set('ai', function (button) {
                                    var card = {
                                        name: button.link[2],
                                    };
                                    var value = get.value(card);
                                    return value;
                                });
                                ('step 1');
                                if (result.bool) {
                                    player.gain(game.createCard(result.buttons[0].link[2]), 'draw');
                                    if (player.name1 == 'yunsunshangxiang') {
                                        player.node.avatar.setBackground('yunsunshangxiang', 'character');
                                    }
                                    if (player.name1 == 'yunsunshangxiang') {
                                        player.node.avatar.setBackground('yunsunshangxiang', 'character');
                                    }
                                }
                            },
                            group: ['yunxiaoji_a', 'yunxiaoji_b', 'yunxiaoji_c', 'yunxiaoji_d', 'yunxiaoji_e'],
                            ai: {
                                reverseEquip: true,
                                effect: {
                                    target(card, player, target, current) {
                                        if (player == target && get.type(card) == 'equip') return [1, 3];
                                    },
                                },
                            },
                            subSkill: {
                                a: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        player: 'equipEnd',
                                    },
                                    forced: true,
                                    popup: false,
                                    filter(event, player) {
                                        return get.subtype(event.card) == 'equip1';
                                    },
                                    content() {
                                        'step 0';
                                        player.draw(2);
                                        if (player.name1 == 'yunsunshangxiang') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/金玉良缘a.jpg');
                                        }
                                        if (player.name1 == 'yunsunshangxiang') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/浮生若梦a.jpg');
                                        }
                                        ('step 1');
                                        if (player.storage.yunqingyuan_equip1 == 0) {
                                            game.countPlayer(function (current) {
                                                if (current != player && current.hasSkill('yunqingyuan')) {
                                                    player.storage.yunqingyuan_equip1 = 1;
                                                    player.line(current, 'green');
                                                    current.draw(2);
                                                }
                                            });
                                        } else {
                                        }
                                    },
                                },
                                b: {
                                    audio: 'ext:云将/audio:1',
                                    trigger: {
                                        player: 'equipEnd',
                                    },
                                    forced: true,
                                    popup: false,
                                    filter(event, player) {
                                        return get.subtype(event.card) == 'equip2';
                                    },
                                    content() {
                                        'step 0';
                                        player.recover();
                                        if (player.name1 == 'yunsunshangxiang') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/金玉良缘b.jpg');
                                        }
                                        if (player.name1 == 'yunsunshangxiang') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/浮生若梦b.jpg');
                                        }
                                        ('step 1');
                                        if (player.storage.yunqingyuan_equip2 == 0) {
                                            game.countPlayer(function (current) {
                                                if (current != player && current.hasSkill('yunqingyuan')) {
                                                    player.storage.yunqingyuan_equip2 = 1;
                                                    player.line(current, 'green');
                                                    current.recover();
                                                }
                                            });
                                        } else {
                                        }
                                    },
                                },
                                c: {
                                    audio: 'ext:云将/audio:1',
                                    trigger: {
                                        player: 'equipEnd',
                                    },
                                    forced: true,
                                    popup: false,
                                    nodelay: true,
                                    filter(event, player) {
                                        return get.subtype(event.card) == 'equip3';
                                    },
                                    content() {
                                        'step 0';
                                        var card = get.cardPile(function (card) {
                                            return get.type2(card) == 'trick';
                                        });
                                        if (card) player.gain(card, 'gain2');
                                        if (player.name1 == 'yunsunshangxiang') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/金玉良缘c.jpg');
                                        }
                                        if (player.name1 == 'yunsunshangxiang') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/浮生若梦c.jpg');
                                        }
                                        ('step 1');
                                        if (player.storage.yunqingyuan_equip3 == 0) {
                                            game.countPlayer(function (current) {
                                                if (current != player && current.hasSkill('yunqingyuan')) {
                                                    player.storage.yunqingyuan_equip3 = 1;
                                                    player.line(current, 'green');
                                                    var card2 = get.cardPile(function (card) {
                                                        return get.type2(card) == 'trick';
                                                    });
                                                    if (card2) current.gain(card2, 'gain2');
                                                }
                                            });
                                        } else {
                                        }
                                    },
                                },
                                d: {
                                    audio: 'ext:云将/audio:1',
                                    trigger: {
                                        player: 'equipEnd',
                                    },
                                    forced: true,
                                    popup: false,
                                    nodelay: true,
                                    filter(event, player) {
                                        return get.subtype(event.card) == 'equip4';
                                    },
                                    content() {
                                        'step 0';
                                        var card = get.cardPile(function (card) {
                                            return get.type2(card) == 'basic';
                                        });
                                        if (card) player.gain(card, 'gain2');
                                        if (player.name1 == 'yunsunshangxiang') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/金玉良缘d.jpg');
                                        }
                                        if (player.name1 == 'yunsunshangxiang') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/浮生若梦d.jpg');
                                        }
                                        ('step 1');
                                        if (player.storage.yunqingyuan_equip4 == 0) {
                                            game.countPlayer(function (current) {
                                                if (current != player && current.hasSkill('yunqingyuan')) {
                                                    player.storage.yunqingyuan_equip4 = 1;
                                                    player.line(current, 'green');
                                                    var card2 = get.cardPile(function (card) {
                                                        return get.type2(card) == 'basic';
                                                    });
                                                    if (card2) current.gain(card2, 'gain2');
                                                }
                                            });
                                        } else {
                                        }
                                    },
                                },
                                e: {
                                    audio: 'ext:云将/audio:1',
                                    trigger: {
                                        player: 'equipEnd',
                                    },
                                    forced: true,
                                    popup: false,
                                    nodelay: true,
                                    filter(event, player) {
                                        return get.subtype(event.card) == 'equip5';
                                    },
                                    content() {
                                        'step 0';
                                        var card = get.cardPile(function (card) {
                                            return get.type2(card) == 'equip';
                                        });
                                        if (card) player.gain(card, 'gain2');
                                        if (player.name1 == 'yunsunshangxiang') {
                                            player.node.avatar.setBackground('yunsunshangxiang', 'character');
                                        }
                                        if (player.name1 == 'yunsunshangxiang') {
                                            player.node.avatar.setBackground('yunsunshangxiang', 'character');
                                        }
                                        ('step 1');
                                        if (player.storage.yunqingyuan_equip5 == 0) {
                                            game.countPlayer(function (current) {
                                                if (current != player && current.hasSkill('yunqingyuan')) {
                                                    player.storage.yunqingyuan_equip5 = 1;
                                                    player.line(current, 'green');
                                                    var card2 = get.cardPile(function (card) {
                                                        return get.type2(card) == 'equip';
                                                    });
                                                    if (card2) current.gain(card2, 'gain2');
                                                }
                                            });
                                        } else {
                                        }
                                    },
                                },
                            },
                        },
                        yunshangce: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'shaBegin',
                            },
                            forced: true,
                            mark: true,
                            marktext: '上策',
                            intro: {
                                name: '上策',
                                content: '你本回合使用首张【杀】无视距离,不可闪避',
                            },
                            content() {
                                trigger.directHit = true;
                                player.removeSkill('yunshangce');
                            },
                            mod: {
                                targetInRange(card) {
                                    if (card.name == 'sha') return true;
                                },
                            },
                        },
                        yunzhongce: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            usable: 1,
                            filterTarget(card, player, target) {
                                return target.countCards('hej');
                            },
                            mark: true,
                            marktext: '中策',
                            intro: {
                                name: '中策',
                                content: '出牌阶段限一次:可弃置一名角色区域内一张牌',
                            },
                            prompt: '选择【中策】弃置牌的目标',
                            content() {
                                player.discardPlayerCard(target, 'hej', true);
                                player.removeSkill('yunzhongce');
                            },
                            ai: {
                                basic: {
                                    order: 9,
                                    useful: 1,
                                    value: 5,
                                },
                                result: {
                                    target(player, target) {
                                        var att = get.attitude(player, target);
                                        var nh = target.countCards('h');
                                        if (att > 0) {
                                            var js = target.getCards('j');
                                            if (js.length) {
                                                var jj = js[0].viewAs
                                                    ? {
                                                        name: js[0].viewAs,
                                                    }
                                                    : js[0];
                                                if (jj.name == 'guohe' || js.length > 1 || get.effect(target, jj, target, player) < 0) {
                                                    return 3;
                                                }
                                            }
                                            if (target.getEquip('baiyin') && target.isDamaged() && get.recoverEffect(target, player, player) > 0) {
                                                if (target.hp == 1 && !target.hujia) return 1.6;
                                                if (target.hp == 2) return 0.01;
                                                return 0;
                                            }
                                        }
                                        var es = target.getCards('e');
                                        var noe = es.length == 0 || target.hasSkillTag('noe');
                                        var noe2 =
                                            es.filter(function (esx) {
                                                return get.value(esx, target) > 0;
                                            }).length == 0;
                                        var noh = nh == 0 || target.hasSkillTag('noh');
                                        if (noh && (noe || noe2)) return 0;
                                        if (att <= 0 && !target.countCards('he')) return 1.5;
                                        return -1.5;
                                    },
                                },
                                tag: {
                                    loseCard: 1,
                                    discard: 1,
                                },
                            },
                        },
                        yunxiace: {
                            audio: 'ext:云将/audio:2',
                            mark: true,
                            marktext: '下策',
                            intro: {
                                name: '下策',
                                content: '回合结束阶段摸等同攻击范围数的牌(至多五张)',
                            },
                            forced: true,
                            trigger: {
                                player: 'phaseJieshuBegin',
                            },
                            content() {
                                player.draw(Math.min(5, player.getAttackRange()));
                            },
                        },
                        yunxiance: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'phaseZhunbeiBegin',
                            },
                            filter(event, player) {
                                if (player != _status.currentPhase) {
                                    return player.countCards('he') > 0;
                                }
                                if (_status.currentPhase == player) {
                                    return player.hp > 0;
                                }
                            },
                            check(event, player) {
                                if (player.name != 'yunpangtong' && player.hp == 1 && _status.currentPhase == player) return false;
                                return get.attitude(player, event.player) > 0;
                            },
                            prompt(event, player) {
                                var str = '';
                                str += '是否对' + get.translation(event.player) + '发动【献策】？';
                                return str;
                            },
                            content() {
                                'step 0';
                                if (trigger.player == player) {
                                    player.loseHp();
                                    player.addTempSkill('yunshangce', 'phaseEnd');
                                    player.addTempSkill('yunzhongce', 'phaseEnd');
                                    player.addTempSkill('yunxiace', 'phaseEnd');
                                    event.finish();
                                } else {
                                    player.chooseToDiscard('he', '请弃置一张牌发动【献策】', true).set('ai', function (card) {
                                        if (get.attitude(player, trigger.target) > 0) return true;
                                        return 6 - get.value(card);
                                    });
                                }
                                ('step 1');
                                if (result.bool && trigger.player != player) {
                                    trigger.player.chooseControl('上策', '中策', '下策', true).set('ai', function () {
                                        if (trigger.player.hasSkill('yunpianhong')) return ['上策', '中策'].randomGet();
                                        if (trigger.player.getCards('j', 'lebu') > 0 || trigger.player.getCards('j', 'bingliang') > 0) {
                                            return '下策';
                                        }
                                        return ['上策', '中策', '下策'].randomGet();
                                    });
                                }
                                ('step 2');
                                if (result.control == '上策') {
                                    trigger.player.addTempSkill('yunshangce', 'phaseEnd');
                                }
                                if (result.control == '中策') {
                                    trigger.player.addTempSkill('yunzhongce', 'phaseEnd');
                                }
                                if (result.control == '下策') {
                                    trigger.player.addTempSkill('yunxiace', 'phaseEnd');
                                }
                            },
                            ai: {
                                expose: 0.5,
                                threaten: 0.5,
                            },
                        },
                        yunfengming: {
                            juexingji: true,
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'dieBefore',
                            },
                            forced: true,
                            derivation: 'yunniepan',
                            content() {
                                'step 0';
                                trigger.cancel();
                                player.hp = 1;
                                player.maxHp = 1;
                                player.update();
                                player.addSkill('yunniepan');
                                player.removeSkill('yunxiance');
                                player.awakenSkill('yunfengming');
                                player.addTempSkill('yunfengminga', { player: 'phaseZhunbeiEnd' });
                                ('step 1');
                                player.say(['落凤坡？!', '大业未成,我岂能就此而亡!'].randomGet());
                                if (player.name1 == 'yunpangtong') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yunpangtonga.jpg');
                                }
                                if (player.name1 == 'yunpangtong') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/凤凰于飞a.jpg');
                                }
                            },
                        },
                        yunfengminga: {
                            trigger: {
                                player: 'damageBefore',
                            },
                            mark: true,
                            marktext: '凤鸣',
                            forced: true,
                            charlotte: true,
                            init(player) {
                                game.log(player, '进入了', '【涅磐】状态');
                            },
                            content() {
                                trigger.cancel();
                            },
                            ai: {
                                nofire: true,
                                nothunder: true,
                                nodamage: true,
                                effect: {
                                    target(card, player, target, current) {
                                        if (get.tag(card, 'damage')) return [0, 0];
                                    },
                                },
                            },
                            intro: {
                                name: '凤鸣',
                                content: '防止一切伤害,不可被其他角色选中',
                            },
                            mod: {
                                targetEnabled(card, player, target) {
                                    if (player != target) return false;
                                },
                            },
                        },
                        yunzongyu: {
                            audio: 'ext:云将/audio:1',
                            zhuSkill: true,
                            limited: true,
                            selectTarget: 1,
                            filterTarget(card, player, target) {
                                return target != player;
                            },
                            derivation: 'yunweiwu',
                            mark: false,
                            init(player) {
                                player.storage.yunzongyu = false;
                            },
                            filter(event, player) {
                                return player.hasZhuSkill('yunzongyu') && player.storage.yunzongyu == false;
                            },
                            enable: 'phaseUse',
                            prompt: '选择一名角色令其增加一点体力上限并修改势力为『魏』',
                            content() {
                                'step 0';
                                player.storage.yunzongyu = true;
                                player.awakenSkill('yunzongyu');
                                ('step 1');
                                game.log(target, '成为了', '【总御】', '的目标');
                                player.say(['扫清六合,席卷八荒!', '若是无孤,天下不知几人称王,几人称帝!'].randomGet());
                                target.draw(2);
                                target.gainMaxHp();
                                if (target.group != 'wei') target.changeGroup('wei');
                                player.addSkill('yunweiwu');
                                if (player.name1 == 'yuncaocao') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yuncaocaoa.jpg');
                                }
                                if (player.name1 == 'yuncaocao') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/珠联璧合cc_a.jpg');
                                }
                                if (player.name1 == 'yuncaocao') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/挟令天下a.jpg');
                                }
                            },
                            ai: {
                                order: 7,
                                result: {
                                    target(player, target) {
                                        if ((target.identity == 'zhong' || target.identity == 'mingzhong') && target.group != 'wei') {
                                            return 10;
                                        }
                                        if (target.identity == 'zhong' || target.identity == 'mingzhong') {
                                            return 5;
                                        }
                                        if (get.attitude(player, target) > 0) {
                                            return 3;
                                        }
                                    },
                                },
                            },
                            intro: {
                                content: 'limited',
                            },
                        },
                        yunweiwu: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseEnd',
                            },
                            check(event, player) {
                                return event.player.identity != 'nei' && get.attitude(player, event.player) > 0;
                            },
                            filter(event, player) {
                                for (var i = 0; i < game.players.length; i++) if (game.players[i] != player && game.players[i].group == 'wei' && game.players[i].hp < player.hp) return true;
                                return false;
                            },
                            forced: true,
                            prompt: '是否选择一名〖魏〗势力角色令其回复一点体力？',
                            content() {
                                'step 0';
                                player
                                    .chooseTarget('是否发动【魏武】？', function (card, player, target) {
                                        return player != target && target.hp < target.maxHp && target.hp < target.getDamagedHp() && target.group == 'wei';
                                    })
                                    .set('ai', function (target) {
                                        return get.attitude(_status.event.player, target) * Math.sqrt(Math.max(1, 4 - target.countCards('h')));
                                    });
                                ('step 1');
                                if (result.bool) {
                                    event.targets = result.targets;
                                    event.targets.sort(lib.sort.seat);
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                if (event.targets.length) {
                                    var target = event.targets.shift();
                                    target.recover();
                                }
                            },
                        },
                        yunxieling: {
                            audio: 'ext:云将/audio:2',
                            limited: true,
                            enable: 'phaseUse',
                            mark: true,
                            intro: {
                                content: 'limited',
                            },
                            init(player, skill) {
                                player.storage[skill] = false;
                            },
                            filter(event, player) {
                                return Math.ceil(game.players.length > 2);
                            },
                            filterTarget(card, player, target) {
                                return target.hp >= player.hp && player.canCompare(target);
                            },
                            prompt: '请选择【挟令】的目标',
                            content() {
                                'step 0';
                                player.awakenSkill('yunxieling');
                                player.storage.yunxieling = true;
                                event.targets = game.filterPlayer();
                                event.targets.remove(player);
                                event.targets.remove(target);
                                ('step 1');
                                if (event.targets.length) {
                                    event.current = event.targets.shift();
                                    if (target.isAlive()) {
                                        event.current.chooseToUse({ name: 'sha' }, target, -1).set('prompt2', '请选择:1. 对' + get.translation(target) + '使用一张杀;2.视为被 ' + get.translation(player) + '使用一张杀');
                                    }
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                if (result.bool == false) {
                                    player.useCard({ name: 'sha' }, event.current, false);
                                }
                                event.goto(1);
                            },
                            ai: {
                                order: 5,
                                result: {
                                    target(player, target) {
                                        var players = game.filterPlayer();
                                        if (player.hp > 1) {
                                            if (game.phaseNumber < game.players.length) return 0;
                                            if (player.hasUnknown()) return 0;
                                        }
                                        var effect = 0;
                                        for (var i = 0; i < players.length; i++) {
                                            if (players[i] != target && players[i] != player && players[i].countCards('he')) effect += get.effect(target, { name: 'sha' }, players[i], target);
                                        }
                                        return effect;
                                    },
                                },
                            },
                        },
                        yunxionglue: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'phaseJieshuAfter',
                            },
                            forced: true,
                            popup: false,
                            mark: true,
                            zhuanhuanji: true,
                            marktext: '☯',
                            init(player) {
                                player.storage.yunxionglue_xiongcai = 0;
                            },
                            intro: {
                                content(storage, player, skill) {
                                    if (player.storage.yunxionglue == true) return '你使用带有〖伤害〗标签的牌指定目标或被带有〖伤害〗标签的牌指定为目标时,可弃置一张手牌获得目标或此牌使用者一张牌.';
                                    return '出牌阶段,你可弃置一张手牌从牌堆随机获得一张带有〖伤害〗标签的牌.';
                                },
                            },
                            content() {
                                player.storage.yunxionglue_xiongcai = 0; //QQQ
                            },
                            group: ['yunxionglue_xiongcai', 'yunxionglue_guixin_a', 'yunxionglue_guixin_b'],
                            subSkill: {
                                xiongcai: {
                                    audio: 'yunxionglue',
                                    enable: 'phaseUse',
                                    filter(event, player, name) {
                                        return player.countCards('h') > 0 && !player.storage.yunxionglue;
                                    },
                                    check(card) {
                                        let player = _status.event.player;
                                        if (
                                            player.hasCard(function (card) {
                                                if (player.countCards('hs', 'sha') > 0) return card.name == 'zhuge';
                                            })
                                        )
                                            return 0;
                                        return 6 - get.value(card);
                                    },
                                    filterCard: true,
                                    selectCard: 1,
                                    position: 'h',
                                    prompt: '是否弃置一张牌发动【雄略】？',
                                    content() {
                                        'step 0';
                                        var card = get.cardPile2(function (card) {
                                            return get.tag(card, 'damage');
                                        });
                                        if (!card) {
                                            player.chat('无牌可得了吗');
                                            game.log('牌堆里面已经没有带〖伤害〗标签的牌了!');
                                            event.finish();
                                            return;
                                        }
                                        player.gain(card, 'gain2');
                                        ('step 1');
                                        player.changeZhuanhuanji('yunxionglue');
                                        if (player.storage.yunxionglue_xiongcai <= player.getDamagedHp()) {
                                            player.storage.yunxionglue_xiongcai++;
                                            player.draw();
                                        }
                                    },
                                    ai: {
                                        order: 10,
                                        threaten: 3,
                                        result: {
                                            player: 7,
                                        },
                                    },
                                },
                                guixin_a: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        player: 'useCardToPlayered',
                                    },
                                    forced: true,
                                    prompt: '是否弃置一张牌发动【雄略】？',
                                    check(event, player) {
                                        return get.attitude(player, event.target) <= 0;
                                    },
                                    filter(event, player) {
                                        return event.target.countCards('he') > 0 && event.target != player && player.countCards('h') > 0 && get.tag(event.card, 'damage') && player.storage.yunxionglue;
                                    },
                                    content() {
                                        'step 0';
                                        player.chooseToDiscard('h', '是否弃置一张手牌发动【雄略】获得' + get.translation(trigger.target) + '一张牌?').set('ai', function (card) {
                                            if (get.attitude(player, trigger.target) <= 0) {
                                                return true;
                                            } else {
                                                return false;
                                            }
                                            if (
                                                player.hasCard(function (card) {
                                                    if (player.countCards('hs', 'sha') > 0) return card.name == 'zhuge';
                                                })
                                            )
                                                return 0;
                                            return 6 - get.value(card);
                                        });
                                        ('step 1');
                                        if (result.bool) {
                                            player.gainPlayerCard(trigger.target, 'he', true);
                                        } else {
                                            event.finish();
                                        }
                                        ('step 2');
                                        player.changeZhuanhuanji('yunxionglue');
                                        if (player.storage.yunxionglue_xiongcai <= player.getDamagedHp()) {
                                            player.storage.yunxionglue_xiongcai++;
                                            player.draw();
                                        }
                                    },
                                },
                                guixin_b: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        target: 'useCardToTargeted',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return event.player.countCards('he') > 0 && player.countCards('h') > 0 && get.tag(event.card, 'damage') && player.storage.yunxionglue;
                                    },
                                    check(event, player) {
                                        return get.attitude(player, event.player) <= 0;
                                    },
                                    content() {
                                        'step 0';
                                        player.chooseToDiscard('h', '是否弃置一张手牌发动【雄略】获得' + get.translation(trigger.player) + '一张牌?').set('ai', function (card) {
                                            if (get.attitude(trigger.player, player) <= 0) {
                                                return true;
                                            } else {
                                                return false;
                                            }
                                            if (
                                                player.hasCard(function (card) {
                                                    if (player.countCards('h', 'shan') <= 1) return card.name == 'shan';
                                                })
                                            )
                                                return 0;
                                            return 6 - get.value(card);
                                        });
                                        ('step 1');
                                        if (result.bool) {
                                            player.gainPlayerCard(trigger.player, 'he', true);
                                        } else {
                                            event.finish();
                                        }
                                        ('step 2');
                                        player.changeZhuanhuanji('yunxionglue');
                                        if (player.storage.yunxionglue_xiongcai <= player.getDamagedHp()) {
                                            player.storage.yunxionglue_xiongcai++;
                                            player.draw();
                                        }
                                    },
                                },
                            },
                        },
                        yunyijue: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                player: 'useCardToPlayered',
                            },
                            filter(event, player, target) {
                                if (!get.tag(event.card, 'damage')) return false;
                                return event.targets.length == 1 && event.target.countCards('h') > 0 && !event.target.hasSkill('yunyijuea') && event.target != player;
                            },
                            prompt(event, player, target) {
                                var str = '';
                                str += '是否对' + get.translation(event.target) + '发动【义绝】？';
                                return str;
                            },
                            check(event, player) {
                                if (get.attitude(player, event.target) <= 0 && event.target.hujia < 1 && event.target.hp != 1) return true;
                                if (get.attitude(player, event.target) > 0 && event.target.hp == 1 && (event.target.hujia > 0 || player.countCards('hs', { name: 'tao' }) >= 1)) return true;
                                return false;
                            },
                            logTarget: 'target',
                            content() {
                                'step 0';
                                player.gainPlayerCard(trigger.target, 'h', true);
                                ('step 1');
                                trigger.target.hp = 1;
                                trigger.target.update();
                                ('step 2');
                                trigger.target.addSkill('yunyijuea');
                            },
                            audioname2: {
                                key_shiki: 'yunshenzi',
                            },
                            ai: {
                                jueqing: true,
                            },
                        },
                        yunyijuea: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                global: 'phaseJieshuAfter',
                            },
                            forced: true,
                            charlotte: true,
                            content() {
                                player.hp = player.maxHp;
                                player.removeSkill('yunyijuea');
                            },
                        },
                        yunyijueb: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                player: 'useCardToPlayered',
                            },
                            filter(event, player, target) {
                                return !event.target.hasSkill('yunyijuea') && event.card && event.card.name == 'sha' && event.notLink();
                            },
                            forced: true,
                            logTarget: 'target',
                            content() {
                                'step 0';
                                trigger.target.hp = 1;
                                trigger.target.update();
                                ('step 1');
                                trigger.target.addSkill('yunyijuea');
                            },
                            audioname2: {
                                key_shiki: 'yunshenzi',
                            },
                            ai: {
                                jueqing: true,
                            },
                        },
                        yunguayin: {
                            juexingji: true,
                            audio: 'ext:云将/audio:2',
                            derivation: ['yunyijueb', 'yunwusheng'],
                            trigger: {
                                global: 'dieEnd',
                            },
                            forced: true,
                            filter(event, player) {
                                return player.getAllHistory('sourceDamage', function (target) {
                                    return target.player == event.player;
                                }).length;
                            },
                            content() {
                                'step 0';
                                player.awakenSkill('yunguayin');
                                player.removeSkill('yunyijue');
                                ('step 1');
                                player.addSkill('yunyijueb');
                                player.addSkill('yunyangwei');
                                player.addSkill('yunwusheng');
                                ('step 2');
                                player.storage.yunwusheng++;
                                player.markSkill('yunwusheng');
                                event.trigger('yunwusheng');
                                if (player.name1 == 'yunguanyu') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yunguanyua.jpg');
                                }
                                if (player.name1 == 'yunguanyu') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/义冠千古a.jpg');
                                }
                            },
                            ai: {
                                order: 6,
                            },
                        },
                        yunwusheng: {
                            audio: 'ext:云将/audio:2',
                            mark: true,
                            marktext: '傲',
                            init(player) {
                                player.storage.yunwusheng = 0;
                                player.markSkill('yunwusheng');
                            },
                            intro: {
                                content: '手牌上限加#<br>攻击距离加#<br>摸牌阶段额外摸#张牌<br>获得【傲】时将护甲补充至#',
                            },
                            trigger: {
                                source: 'dieAfter',
                            },
                            filter(event, player) {
                                if (player.storage.yunwusheng >= player.maxHp) return false;
                                return true;
                            },
                            forced: true,
                            content() {
                                player.storage.yunwusheng++;
                                player.loseMaxHp();
                                player.markSkill('yunwusheng');
                                event.trigger('yunwusheng');
                            },
                            mod: {
                                attackFrom(from, to, num) {
                                    return num - from.storage.yunwusheng;
                                },
                                maxHandcardBase(player, num) {
                                    return num + player.storage.yunwusheng;
                                },
                                cardname(card, player, name) {
                                    if (get.tag({ name: name }, 'damage')) return 'sha';
                                },
                            },
                            group: ['yunwusheng_draw', 'yunwusheng_damage', 'yunwusheng_hujia'],
                            subSkill: {
                                draw: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        player: 'phaseDrawBegin',
                                    },
                                    forced: true,
                                    content() {
                                        var mps = player.storage.yunwusheng;
                                        trigger.num += mps;
                                    },
                                    ai: {
                                        threaten: 1.3,
                                    },
                                },
                                damage: {
                                    trigger: {
                                        source: 'damageBegin',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return event.card && event.card.name == 'sha' && get.color(event.card) == 'red' && event.notLink();
                                    },
                                    content() {
                                        player.say(['观尔等如插标卖首尔!', '酒温否？'].randomGet());
                                        trigger.num++;
                                    },
                                    ai: {
                                        effect: {
                                            player(card, player, target) {
                                                if (card.name == 'sha' && get.color(card) == 'red') return [1, 1];
                                            },
                                        },
                                    },
                                },
                                hujia: {
                                    trigger: {
                                        player: 'yunwusheng',
                                    },
                                    forced: true,
                                    popup: false,
                                    filter(event, player) {
                                        return player.hujia < player.storage.yunwusheng;
                                    },
                                    content() {
                                        player.changeHujia(player.storage.yunwusheng - player.hujia);
                                    },
                                },
                            },
                        },
                        yunqieting: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'useCard',
                            },
                            usable: 1,
                            forced: true,
                            filter(event, player) {
                                return event.player != player && player.inRange(event.player) && event.player.countCards('he') > 0;
                            },
                            prompt(event, player) {
                                var str = '选择获得' + get.translation(event.player) + '一张牌';
                                return str;
                            },
                            content() {
                                player.gainPlayerCard(trigger.player, 'hej', true);
                                if (player.name1 == 'yuncaishi') {
                                    player.node.avatar.setBackground('yuncaishi', 'character');
                                }
                            },
                        },
                        yunxianzhou: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'damageAfter',
                            },
                            forced: true,
                            filter(event, player) {
                                return player.countCards('h') > 0;
                            },
                            filterTarget(card, player, target) {
                                return player != target;
                            },
                            content() {
                                'step 0';
                                player.chooseTarget('将所有手牌交给一名角色', true, function (card, player, target) {
                                    return target != player;
                                }).ai = function (target) {
                                    return get.attitude(player, target);
                                };
                                player.say(['刘氏无德,当献曹公', '还望曹公庇护!'].randomGet());
                                ('step 1');
                                if (result.bool) {
                                    player.$giveAuto(player.getCards('h').length, result.targets[0]);
                                    var cards = player.getCards('h');
                                    player.lose(cards, ui.special);
                                    result.targets[0].gain(cards);
                                }
                                if (player.name1 == 'yuncaishi') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yuncaishia.jpg');
                                }
                                if (player.name1 == 'yuncaishi') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/春梦迷迭a.jpg');
                                }
                                if (player.name1 == 'yuncaishi') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/兰芯惠黛a.jpg');
                                }
                                if (player.name1 == 'yuncaishi') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/请君小酌a.jpg');
                                }
                            },
                        },
                        yunqingleng: {
                            audio: 'ext:云将/audio:4',
                            trigger: {
                                global: 'damageAfter',
                            },
                            usable: 1,
                            intro: {
                                content: '已对$发动过〖清冷〗',
                            },
                            init(player) {
                                if (!player.storage.yunqingleng) player.storage.yunqingleng = [];
                            },
                            filter(event, player) {
                                return event.source && event.source != player && event.source.isAlive();
                            },
                            check(event, player) {
                                return get.attitude(player, event.source) <= 0;
                            },
                            prompt(event, player) {
                                var str = '';
                                str += '是否对' + get.translation(event.source) + '发动【清冷】？';
                                return str;
                            },
                            content() {
                                player.say(['只有死人才会永远保密!', '只怪你看到了不该看的!听到了不该听的!'].randomGet());
                                if (!player.storage.yunqingleng || !player.storage.yunqingleng.includes(trigger.source)) {
                                    trigger.source.damage(player);
                                    if (!player.storage.yunqingleng) player.storage.yunqingleng = [];
                                    player.storage.yunqingleng.push(trigger.source);
                                    player.storage.yunqingleng.sortBySeat();
                                    player.markSkill('yunqingleng');
                                    if (player.name1 == 'yunzhangchunhua') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/yunzhangchunhuaa.jpg');
                                    }
                                    if (player.name1 == 'yunzhangchunhua') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/宜室宜家a.jpg');
                                    }
                                    if (player.name1 == 'yunzhangchunhua') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/宣穆潜鳞a.jpg');
                                    }
                                    if (player.name1 == 'yunzhangchunhua') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/新春意浓a.jpg');
                                    }
                                    if (player.name1 == 'yunzhangchunhua') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/以情传书a.jpg');
                                    }
                                } else {
                                    player.useCard({ name: 'sha', skill: 'yunqingleng' }, trigger.source);
                                    player.node.avatar.setBackground('yunzhangchunhua', 'character');
                                }
                            },
                            ai: {
                                expose: 0.3,
                            },
                        },
                        yunshangshi: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                source: 'damageBegin',
                            },
                            forced: true,
                            content() {
                                trigger.nature = 'ice';
                            },
                            ai: {
                                jueqing: true,
                            },
                            group: ['yunshangshi_lose'],
                            subSkill: {
                                lose: {
                                    trigger: {
                                        global: 'loseAfter',
                                    },
                                    forced: true,
                                    popup: false,
                                    filter(event, player) {
                                        return event.type == 'discard' && event.getParent(3).name == 'icesha_skill' && event.cards.filterInD('d').length;
                                    },
                                    content() {
                                        var cards = trigger.cards.filterInD('d');
                                        player.gain(cards, 'gain2');
                                    },
                                    ai: {
                                        combo: 'icesha_skill',
                                    },
                                },
                            },
                        },
                        yunzhulian: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            usable: 3,
                            selectTarget: 1,
                            init(player) {
                                player.storage.zhulian = 0;
                            },
                            filter(event, player) {
                                return player.storage.zhulian < 3;
                            },
                            filterTarget(card, player, target) {
                                return !target.hasSkill('yunzhulian_mark');
                            },
                            prompt: '选择一名角色获得【株】标记',
                            content() {
                                player.storage.zhulian++;
                                target.addSkill('yunzhulian_mark');
                            },
                            ai: {
                                order: 10,
                                expose: 0.3,
                                threaten: 2,
                                result: {
                                    target: -1,
                                },
                            },
                            audioname2: {
                                yanghuiyu: 'yunshenzi',
                            },
                        },
                        yunzhulian_mark: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'damageEnd',
                            },
                            mark: true,
                            marktext: '株连',
                            intro: {
                                name: '株连',
                                content: '给本小姐跪下!',
                            },
                            forced: true,
                            forceDie: true,
                            charlotte: true,
                            filter(event, player) {
                                return event.source;
                            },
                            mod: {
                                globalTo(from, to, current) {
                                    return current == 1;
                                },
                            },
                            content() {
                                trigger.source.chooseDrawRecover(2, 1, true, get.prompt('yunzhulian_mark'));
                                game.countPlayer(function (current) {
                                    if (current != player && current.hasSkill('yunzhulian')) {
                                        current.storage.zhulian--;
                                    }
                                });
                                player.removeSkill('yunzhulian_mark');
                            },
                            ai: {
                                threaten: 3,
                            },
                            group: ['yunzhulian_mark_die'],
                            subSkill: {
                                die: {
                                    trigger: {
                                        player: 'dieBegin',
                                    },
                                    forced: true,
                                    popup: false,
                                    forceDie: true,
                                    filter(event, player) {
                                        return player.hasSkill('yunzhulian_mark');
                                    },
                                    content() {
                                        game.countPlayer(function (current) {
                                            if (current != player && current.hasSkill('yunzhulian')) {
                                                current.storage.zhulian--;
                                            }
                                        });
                                        player.removeSkill('yunzhulian_mark');
                                    },
                                },
                            },
                        },
                        yunjiaoheng: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'dieAfter',
                            },
                            limited: true,
                            charlotte: true,
                            filter(event, player) {
                                return event.player != player && player.storage.yunjiaoheng == false;
                            },
                            mark: true,
                            prompt: '是否发动【娇横】召唤『董卓』？',
                            mode: ['identity'],
                            content() {
                                'step 0';
                                player.maxHp = 1;
                                player.update();
                                if (player.name1 == 'yundongbai') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yundongbaia.jpg');
                                }
                                if (player.name1 == 'yundongbai') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/娇俏伶俐a.jpg');
                                }
                                if (player.name1 == 'yundongbai') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/甜心宝贝a.jpg');
                                }
                                if (player.name1 == 'yundongbai') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/豆蔻梢头a.jpg');
                                }
                                if (player.name1 == 'yundongbai') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/烈焰炙心a.jpg');
                                }
                                ('step 1');
                                player.awakenSkill('yunjiaoheng');
                                ('step 2');
                                if (player.identity == 'nei') {
                                    if (trigger.player.identity == 'zhong') {
                                        player.identity = 'zhong';
                                        game.zhong = player;
                                        player.showIdentity();
                                        player.update();
                                    }
                                    if (trigger.player.identity == 'fan') {
                                        player.identity = 'fan';
                                        game.fan = player;
                                        player.showIdentity();
                                        player.update();
                                    }
                                } else {
                                    event.goto(3);
                                }
                                ('step 3');
                                player.say(['你知道我爷爷是谁吗？!', '爷爷!有人欺负我,呜哇哇哇～'].randomGet());
                                trigger.player.disableSkill('yunjiaoheng', lib.character[trigger.player.name][3]);
                                trigger.player.revive();
                                trigger.player.disableSkill('yunjiaoheng', lib.character[trigger.player.name][3]);
                                ('step 4');
                                var name = trigger.player.name;
                                _status.characterlist.add(name);
                                _status.characterlist.remove('yundongzuo');
                                trigger.player.reinit(name, 'yundongzuo');
                                trigger.player.maxHp = 9;
                                trigger.player.hp = 9;
                                trigger.player.changeGroup('qun');
                                if (player.identity == 'zhu' || player.identity == 'zhong') {
                                    trigger.player.identity = 'zhong';
                                    game.zhong = trigger.player;
                                    trigger.player.showIdentity();
                                    trigger.player.update();
                                }
                                if (player.identity == 'fan') {
                                    trigger.player.identity = 'fan';
                                    game.fan = trigger.player;
                                    trigger.player.showIdentity();
                                    trigger.player.update();
                                }
                            },
                            intro: {
                                content: 'limited',
                            },
                            ai: {
                                expose: 1,
                            },
                        },
                        hunshixiaomowang: {},
                        yunbaonve: {
                            audio: 'ext:云将/audio:2',
                            fixed: true,
                            charlotte: true,
                            trigger: {
                                global: 'phaseBegin',
                            },
                            check(event, player) {
                                return get.attitude(player, event.player) < 0;
                            },
                            filter(event, player) {
                                return event.player != player && event.player.name != 'yundongbai';
                            },
                            content() {
                                player.say(['满朝公卿,屠之如猪狗', '我董仲颖真是大汉之国柱呀,哈哈哈!'].randomGet());
                                player.useCard(
                                    {
                                        name: 'sha',
                                    },
                                    trigger.player,
                                );
                            },
                        },
                        yunchongni: {
                            trigger: {
                                global: 'damageBegin1',
                            },
                            audio: 'ext:云将/audio:2',
                            forced: true,
                            popup: false,
                            fixed: true,
                            charlotte: true,
                            filter(event, player) {
                                return event.player != player;
                            },
                            content() {
                                trigger.player = player;
                            },
                            group: ['yunchongni_damage'],
                            subSkill: {
                                damage: {
                                    trigger: {
                                        source: 'damageBefore',
                                    },
                                    forced: true,
                                    fixed: true,
                                    content() {
                                        trigger.cancel();
                                    },
                                },
                            },
                        },
                        yunyeyan: {
                            audio: 'ext:云将/audio:4',
                            enable: 'phaseUse',
                            limited: true,
                            mark: true,
                            intro: {
                                content: 'limited',
                            },
                            prompt(event, player) {
                                var str = '';
                                str += '是否弃置装备区' + get.cnNumber(_status.event.player.countCards('e')) + '张牌对一名其他角色造成' + get.cnNumber(_status.event.player.countCards('e')) + '点火属性伤害？';
                                return str;
                            },
                            init(player) {
                                player.storage.yunyeyan = false;
                            },
                            filter(event, player) {
                                return player.countCards('e');
                            },
                            position: 'e',
                            filterCard(card) {
                                return get.subtype(card);
                            },
                            selectCard: -1,
                            filterTarget(card, player, target) {
                                return player != target;
                            },
                            content() {
                                'step 0';
                                if (player.name1 == 'yunzhouyu') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yunzhouyua.jpg');
                                }
                                if (player.name1 == 'yunzhouyu') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/珠联璧合zy_a.jpg');
                                }
                                player.storage.yunyeyan = true;
                                player.awakenSkill('yunyeyan');
                                ('step 1');
                                player.say(['灰飞烟灭吧!', '此火,天下三分!'].randomGet());
                                target.damage('fire', cards.length);
                            },
                            group: ['yunyingzi'],
                            ai: {
                                order: 3,
                                result: {
                                    target(player, target) {
                                        var es = player.countCards('e', { type: 'equip' });
                                        var th = target.hp;
                                        if (es >= th) return -1;
                                    },
                                },
                            },
                        },
                        yunguqu: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                global: 'useCard',
                            },
                            forced: true,
                            usable: 1,
                            check(event, player) {
                                return get.attitude(player, event.player) < 0;
                            },
                            filter(event, player) {
                                if (get.type(event.card) != 'basic' && get.type(event.card) != 'trick' && get.type(event.card) != 'delay') return false;
                                if (event.card.suit && player.countCards('h', { suit: event.card.suit })) {
                                    return event.player && event.player != player;
                                }
                                return false;
                            },
                            content() {
                                'step 0';
                                player
                                    .chooseToDiscard(get.prompt('yunguqu'), 'h', '是否弃置一张花色为' + get.translation(trigger.card.suit + '2') + '的手牌取消' + get.translation(trigger.player) + '使用的' + get.translation(trigger.card) + '所有目标？', {
                                        suit: trigger.card.suit,
                                    })
                                    .set('ai', function (card) {
                                        return get.attitude(player, trigger.player) < 0;
                                    });
                                ('step 1');
                                if (result.bool) {
                                    game.log(player, '取消了', trigger.card.name, '的所有目标');
                                    player.discard(result.cards);
                                    trigger.targets.length = 0;
                                    trigger.all_excluded = true;
                                } else {
                                    player.getStat('triggerSkill').yunguqu--;
                                    event.finish();
                                }
                                ('step 2');
                                if (get.color(trigger.card) == 'red') {
                                    player.draw();
                                } else {
                                    player
                                        .chooseTarget('请选择弃置牌的目标', function (card, player, target) {
                                            return target != player && target.countCards('he') > 0;
                                        })
                                        .set('ai', function (target) {
                                            return -get.attitude(player, target);
                                        });
                                }
                                ('step 3');
                                if (result.bool) {
                                    player.discardPlayerCard(1, result.targets[0], 'he');
                                } else {
                                    event.finish();
                                }
                            },
                            ai: {
                                expose: 0.1,
                            },
                        },
                        yunyingzi: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseZhunbeiBegin',
                            },
                            forced: true,
                            content() {
                                'step 0';
                                var controls = ['选项一', '选项二'];
                                player
                                    .chooseControl(controls, true)
                                    .set('prompt', '英姿<br><br><div class="text">选项一:获得一张装备牌.</div><br><div class="text">选项二:获得一张【铁索连环】.</div></br>')
                                    .set('ai', function () {
                                        return ['装备', '铁索'].randomGet();
                                    });
                                ('step 1');
                                if (result.control == '选项一') {
                                    var list = get.inpile('equip');
                                    list = list.randomGets(3);
                                    for (var i = 0; i < list.length; i++) {
                                        list[i] = ['装备', '', list[i]];
                                    }
                                    var dialog = ui.create.dialog('选择一张装备牌加入你的手牌', [list, 'vcard'], 'hidden');
                                    player.chooseButton(dialog, true).set('ai', function (button) {
                                        var card = {
                                            name: button.link[2],
                                        };
                                        var value = get.value(card);
                                        return value;
                                    });
                                }
                                if (result.control == '选项二') {
                                    var list = ['tiesuo'];
                                    player.gain(game.createCard(list.randomGet()));
                                }
                                ('step 2');
                                if (result.bool) {
                                    player.gain(game.createCard(result.buttons[0].link[2]), 'draw');
                                }
                            },
                            ai: {
                                threaten: 1.5,
                            },
                        },
                        yunjieyi: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            filterCard(card) {
                                return card.suit == 'heart';
                            },
                            selectCard: 1,
                            position: 'he',
                            usable: 1,
                            viewAs: {
                                name: 'taoyuan',
                            },
                            filter(event, player) {
                                return player.countCards('he', { suit: 'heart' }) > 0;
                            },
                            prompt: '将一张♥️️牌当作【桃园结义】使用',
                            check(card) {
                                return 8 - get.useful(card);
                            },
                            ai: {
                                basic: {
                                    order() {
                                        return 12;
                                    },
                                    useful: [3, 1],
                                    value: 0,
                                },
                                result: {
                                    player(player, target) {
                                        if (player.identity == 'zhu' && player.hp <= 2) return 2;
                                    },
                                    target(player, target) {
                                        return target.hp < target.maxHp ? 2 : 0;
                                    },
                                },
                                tag: {
                                    recover: 1,
                                    multitarget: 1,
                                },
                            },
                            group: ['yunjieyi_recover'],
                            subSkill: {
                                recover: {
                                    trigger: {
                                        player: 'recoverBegin',
                                    },
                                    usable: 1,
                                    forced: true,
                                    popup: false,
                                    content() {
                                        trigger.num++;
                                    },
                                },
                            },
                        },
                        yunzhaolie: {
                            audio: 'ext:云将/audio:3',
                            enable: 'phaseUse',
                            filterCard: true,
                            position: 'he',
                            selectCard: [1, Infinity],
                            discard: false,
                            lose: false,
                            delay: false,
                            prepare: 'give',
                            prompt: '将任意张牌交给一名其他角色',
                            check(card) {
                                if (ui.selected.cards.length > 1) return 0;
                                if (ui.selected.cards.length && ui.selected.cards[0].name == 'du') return 0;
                                if (!ui.selected.cards.length && card.name == 'du') return 20;
                                return 10 - get.value(card);
                            },
                            filter(event, player) {
                                return player.countCards('he') > 0;
                            },
                            filterTarget(card, player, target) {
                                return player != target && !target.hasSkill('yunzhaolie_off');
                            },
                            content() {
                                'step 0';
                                target.gain(cards, player, 'giveAuto');
                                target.addTempSkill('yunzhaolie_off', 'phaseAfter');
                                ('step 1');
                                player.chooseControl('武圣', '咆哮', '龙胆', '铁骑', '烈弓').set('ai', function () {
                                    if (target.hasSkill('zhuge')) return '武圣';
                                    if (target.countCards('hs', 'sha') >= 2) return '咆哮';
                                    if (target.countCards('hs', 'jiu') >= 1) return '龙胆';
                                    if (!target.getDamagedHp() && target.countCards('hs', 'sha') == 1) return '铁骑';
                                    if (target.getDamagedHp() && target.countCards('hs', 'sha') == 1) return '烈弓';
                                    return ['武圣', '咆哮', '龙胆', '铁骑', '烈弓'].randomGet();
                                });
                                ('step 2');
                                if (result.control == '武圣') {
                                    player.say(['我二弟天下无敌!', '匡扶汉室就仰望将军了!'].randomGet());
                                    target.addTempSkill('yunzhaoliea', { player: 'phaseAfter' });
                                }
                                if (result.control == '咆哮') {
                                    player.say(['翼德,少喝点酒!', '匡扶汉室就仰望将军了!'].randomGet());
                                    target.addTempSkill('yunzhaolieb', { player: 'phaseAfter' });
                                }
                                if (result.control == '龙胆') {
                                    player.say(['将军真乃一身是胆也!', '匡扶汉室就仰望将军了!'].randomGet());
                                    target.addTempSkill('yunzhaoliec', { player: 'phaseAfter' });
                                }
                                if (result.control == '铁骑') {
                                    player.say(['孟起勇猛不下于吕布啊!', '匡扶汉室就仰望将军了!'].randomGet());
                                    target.addTempSkill('yunzhaolied', { player: 'phaseAfter' });
                                }
                                if (result.control == '烈弓') {
                                    player.say(['老将军尚能饭否？!', '匡扶汉室就仰望将军了!'].randomGet());
                                    target.addTempSkill('yunzhaoliee', { player: 'phaseAfter' });
                                }
                            },
                            ai: {
                                order(item, player) {
                                    if (player.countCards('h') >= 4) {
                                        return get.order({ name: 'sha' }) - 0.5;
                                    } else {
                                        if (!player.hasSkill('jiu')) return get.order({ name: 'sha' }) + 0.5;
                                    }
                                },
                                result: {
                                    target(player, target) {
                                        if (target.hasSkillTag('nogain')) return 0;
                                        if (ui.selected.cards.length && ui.selected.cards[0].name == 'du') {
                                            if (target.hasSkillTag('nodu')) return 0;
                                            return -10;
                                        }
                                        if (target.hasJudge('lebu')) return 0;
                                        return 2;
                                    },
                                },
                            },
                        },
                        yunzhaoliea: {
                            group: 'new_rewusheng',
                            mark: true,
                            marktext: '武圣',
                            intro: {
                                name: '昭烈·武圣',
                                content: '你可将一张红色牌当做【杀】使用或打出,使用的♦️️杀没有距离限制',
                            },
                        },
                        yunzhaolieb: {
                            group: 'olpaoxiao',
                            mark: true,
                            marktext: '咆哮',
                            intro: {
                                name: '昭烈·咆哮',
                                content: '你使用【杀】无次数限制;当你使用的【杀】被【闪】抵消时,获得一枚〖咆〗.你使用【杀】造成伤害时,弃置所有<咆>并令伤害值+X.(X为〖咆〗数)',
                            },
                        },
                        yunzhaoliec: {
                            group: 'ollongdan',
                            mark: true,
                            marktext: '龙胆',
                            intro: {
                                name: '昭烈·龙胆',
                                content: '你可将一张【杀】当做【闪】、【闪】当做【杀】、【酒】当做【桃】、【桃】当做【酒】使用或打出',
                            },
                        },
                        yunzhaolied: {
                            group: 'retieji',
                            mark: true,
                            marktext: '铁骑',
                            intro: {
                                name: '昭烈·铁骑',
                                content: '你使用【杀】指定一名角色为目标后,可进行一次判定并令该角色的非锁定技失效直到回合结束,除非该角色弃置一张与判定结果花色相同的牌,否则不能使用【闪】抵消此【杀】',
                            },
                        },
                        yunzhaoliee: {
                            group: 'xinliegong',
                            mark: true,
                            marktext: '烈弓',
                            intro: {
                                name: '昭烈·烈弓',
                                content: '你使用【杀】可选择你距离不大于此【杀】点数的角色为目标;当你使用【杀】指定一个目标后,你可根据下列条件执行相应的效果:1.其手牌数小于等于你的手牌数,此【杀】不可被【闪】响应,2.其体力值大于等于你的体力值,此【杀】伤害+1',
                            },
                        },
                        yunzhaolie_off: {
                            charlotte: true,
                        },
                        yunsangu: {
                            audio: 'ext:云将/audio:2',
                            zhuSkill: true,
                            limited: true,
                            selectTarget: 1,
                            filterTarget(card, player, target) {
                                return target != player;
                            },
                            derivation: 'yunrenwang',
                            intro: {
                                content: 'limited',
                            },
                            prompt: '选择一名角色令其增加体力上限并修改势力为『蜀』',
                            mark: false,
                            init(player) {
                                if (player.hasZhuSkill('yunsangu')) {
                                    player.markSkill('yunsangu');
                                    player.storage.yunsangu = false;
                                }
                            },
                            filter(event, player) {
                                return player.hasZhuSkill('yunsangu');
                                return player.storage.yunsangu == false;
                            },
                            enable: 'phaseUse',
                            content() {
                                'step 0';
                                player.storage.yunsangu = true;
                                player.awakenSkill('yunsangu');
                                game.log(target, '成为了', '【三顾】', '的目标');
                                target.gainMaxHp();
                                ('step 1');
                                target.recover();
                                if (target.group != 'shu') target.changeGroup('shu');
                                player.addSkill('yunrenwang');
                                if (player.name1 == 'yunliubei') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yunliubeia.jpg');
                                }
                            },
                            ai: {
                                order: 7,
                                result: {
                                    target(player, target) {
                                        if ((target.identity == 'zhong' || target.identity == 'mingzhong') && target.group != 'shu') {
                                            return 10;
                                        }
                                        if (target.identity == 'zhong' || target.identity == 'mingzhong') {
                                            return 5;
                                        }
                                        if (get.attitude(player, target) > 0) {
                                            return 3;
                                        }
                                    },
                                },
                            },
                        },
                        yunrenwang: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'recoverEnd',
                            },
                            filter(event, player) {
                                return event.player != player && event.card && event.card.name == 'taoyuan';
                            },
                            forced: true,
                            _priority: 10,
                            content() {
                                'step 0';
                                player.draw();
                                ('step 1');
                                if (trigger.player.group == 'shu' && trigger.player.getDamagedHp()) {
                                    player.chooseBool('是否令' + get.translation(trigger.player) + '额外回复一点体力？').set('ai', function (event, player) {
                                        if (get.attitude(player, trigger.player) > 0) return true;
                                        return false;
                                    });
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                if (result.bool) {
                                    trigger.player.recover();
                                }
                            },
                        },
                        yunwanjun: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            filterCard: true,
                            selectCard: 2,
                            position: 'he',
                            selectTarget: 1,
                            prompt: '弃置两张牌视为使用一张雷属性【杀】',
                            filter(event, player) {
                                return player.countCards('he') > 1;
                            },
                            filterTarget(card, player, target) {
                                return player != target && !target.hasSkill('yunwanjun_off');
                            },
                            check(card) {
                                return 7 - get.value(card);
                            },
                            content() {
                                target.addTempSkill('yunwanjun_off');
                                player.useCard({ name: 'sha', nature: 'thunder' }, target, false);
                                target.addTempSkill('yunfengjin', 'shaAfter');
                                player.addTempSkill('unequip', 'shaAfter');
                                player.say(['燕人张飞在此!', '三姓家奴,吃你爷爷一矛!'].randomGet());
                            },
                            line: 'thunder',
                            ai: {
                                ignoreSkill: true,
                                jueqing: true,
                                unequip: true,
                                order(item, player) {
                                    return get.order({ name: 'sha' });
                                },
                                expose: 0.8,
                                result: {
                                    target(player, target) {
                                        return -1;
                                    },
                                },
                            },
                        },
                        yunwanjun_off: {
                            charlotte: true,
                        },
                        yunxiongxu: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'useCard',
                            },
                            forced: true,
                            usable: 1,
                            filter(event, player) {
                                return event.card.name == 'jiu' && player.getDamagedHp() && _status.currentPhase == player;
                            },
                            content() {
                                player.draw(Math.min(3, player.getDamagedHp()));
                            },
                            mod: {
                                cardname(card, player, name) {
                                    if (get.tag({ name: name }, 'recover') && _status.currentPhase == player) {
                                        return 'jiu';
                                    }
                                },
                                cardUsable(card, player, num) {
                                    if (card.name == 'jiu') return (num += Math.min(3, player.getDamagedHp()));
                                },
                                ignoredHandcard(card, player) {
                                    if (card.name == 'jiu') {
                                        return true;
                                    }
                                },
                                cardDiscardable(card, player, name) {
                                    if (name == 'phaseDiscard' && card.name == 'jiu') return false;
                                },
                            },
                            ai: {
                                effect: {
                                    target(card, player, target) {
                                        if (get.tag(card, 'recover') && player.hp >= player.maxHp - 1) return 'zeroplayertarget';
                                    },
                                    player(card, player, target) {
                                        var pdh = player.getDamagedHp();
                                        var phe = player.countCards('he');
                                        if (card.name == 'jiu') {
                                            if (pdh + phe > 2 && player.getCardUsable('jiu') >= 1) return [1, pdh + 1];
                                            if (phe >= 2 && pdh > 0) return [1, pdh + 1];
                                        }
                                    },
                                },
                            },
                            group: ['yunxiongxu_zhunbei', 'yunxiongxu_jieshu'],
                            subSkill: {
                                zhunbei: {
                                    audio: 'yunxiongxu',
                                    trigger: {
                                        player: 'phaseZhunbeiBegin',
                                    },
                                    forced: true,
                                    content() {
                                        'step 0';
                                        var card = get.cardPile2(function (card) {
                                            return card.name == 'jiu';
                                        });
                                        if (!card) {
                                            player.chat('无牌可得了吗');
                                            game.log('牌堆里面已经没有酒了!');
                                            event.finish();
                                            return;
                                        }
                                        if (card) player.gain(card, 'gain2');
                                        ('step 1');
                                        game.updateRoundNumber();
                                    },
                                },
                                jieshu: {
                                    audio: 'yunxiongxu',
                                    trigger: {
                                        player: 'phaseJieshuBegin',
                                    },
                                    forced: true,
                                    content() {
                                        'step 0';
                                        var card = get.discardPile(function (card) {
                                            return card.name == 'jiu';
                                        });
                                        if (!card) {
                                            player.chat('无牌可得了吗');
                                            game.log('弃牌堆里面已经没有酒了!');
                                            event.finish();
                                            return;
                                        }
                                        if (card) player.gain(card, 'gain2');
                                        ('step 1');
                                        game.updateRoundNumber();
                                    },
                                },
                            },
                        },
                        yunbiyue: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'damageEnd',
                            },
                            forced: true,
                            popup: false,
                            silent: true,
                            content() {
                                player.say(['月儿～我们要死了吗？', '月儿～这乱世,真的没有我们的容身之地吗？'].randomGet());
                                if (player.name1 == 'yundiaochan') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yundiaochana.jpg');
                                }
                                if (player.name1 == 'yundiaochan') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/美人心计a.jpg');
                                }
                                if (player.name1 == 'yundiaochan') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/戏水照花a.jpg');
                                }
                                if (player.name1 == 'yundiaochan') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/风华绝代dc_a.jpg');
                                }
                                if (player.name1 == 'yundiaochan') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/一颦倾城a.jpg');
                                }
                                if (player.name1 == 'yundiaochan') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/如梦似幻a.jpg');
                                }
                                if (player.name1 == 'yundiaochan') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/我见犹怜a.jpg');
                                }
                            },
                            mod: {
                                maxHandcard(player, num) {
                                    return num + player.hp;
                                },
                                targetEnabled(card, player, target, now) {
                                    if (target.countCards('h') >= target.hp) {
                                        if (card.name == 'sha' || card.name == 'juedou') return false;
                                    }
                                },
                            },
                            ai: {
                                effect: {
                                    target(card, player, target) {
                                        if (target.countCards('h') >= target.hp) {
                                            if (card.name == 'sha' && card.name == 'juedou') return 0;
                                        }
                                    },
                                },
                            },
                        },
                        yunmeihuo: {
                            audio: 'ext:云将/audio:9',
                            enable: 'phaseUse',
                            filterCard: 1,
                            multitarget: true,
                            selectTarget: 2,
                            usable: 1,
                            check(card) {
                                return 10 - get.value(card);
                            },
                            filter(event, player) {
                                return player.countCards('h') > 0;
                            },
                            filterTarget(card, player, target) {
                                if (target == player) return false;
                                if (target.countCards('h') == 0) return false;
                                if (ui.selected.targets.length) {
                                    return ui.selected.targets[0] != target && !ui.selected.targets[0].hasSkillTag('noCompareSource') && !target.hasSkillTag('noCompareTarget');
                                }
                                return true;
                            },
                            position: 'h',
                            targetprompt: ['发起拼点', '被拼点'],
                            prompt: '弃置一张手牌并选择两名进行拼点的目标',
                            content() {
                                'step 0';
                                targets[0].line(targets[1]);
                                targets[0].chooseToCompare(targets[1]);
                                player.say(['将军～一定要赢哟～', '打打杀杀～好害怕哦～'].randomGet());
                                if (player.name1 == 'yundiaochan') {
                                    player.node.avatar.setBackground('yundiaochan', 'character');
                                }
                                ('step 1');
                                if (result.tie) {
                                    var list = [];
                                    if (get.position(result.player) == 'd') list.push(result.player);
                                    if (get.position(result.target) == 'd') list.push(result.target);
                                    player.gain(list, 'gain2');
                                }
                                if (result.bool == true) {
                                    targets[0].useCard({ name: 'juedou' }, targets[1], false);
                                    targets[1].useCard({ name: 'sha' }, targets[0], false);
                                }
                                if (result.bool == false) {
                                    targets[1].useCard({ name: 'juedou' }, targets[0], false);
                                    targets[0].useCard({ name: 'sha' }, targets[1], false);
                                }
                            },
                            ai: {
                                order: 9,
                                expose: 0.3,
                                result: {
                                    target(player, target) {
                                        if (get.attitude(player, target) < 0) return -target.countCards('he');
                                    },
                                },
                            },
                        },
                        yunzhiba: {
                            global: 'yunzhibaa',
                            audio: 'ext:云将/audio:2',
                        },
                        yunzhibaa: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            position: 'hes',
                            usable: 1,
                            selectCard: 1,
                            discard: false,
                            lose: false,
                            delay: false,
                            line: true,
                            clearTime: true,
                            prepare(cards, player, targets) { },
                            filter(event, player) {
                                if (player.group != 'wu') return false;
                                if (player.countCards('hes', { type: 'equip' }) == 0) return 0;
                                return game.hasPlayer(function (target) {
                                    return target != player && target.hasZhuSkill('yunzhiba', player);
                                });
                            },
                            filterCard(card) {
                                return get.type(card) == 'equip';
                            },
                            filterTarget(card, player, target) {
                                return target != player && target.hasZhuSkill('yunzhiba', player);
                            },
                            prompt: '将一张装备牌交给一名拥有【制霸】的角色,你摸两张牌',
                            content() {
                                'step 0';
                                target.gain(cards, player, 'giveAuto');
                                ('step 1');
                                player.draw(2);
                            },
                            ai: {
                                expose: 0.3,
                                order: 10,
                                result: {
                                    target: 5,
                                },
                            },
                        },
                        yunhenglie: {
                            audio: 'ext:云将/audio:2',
                            zhuSkill: true,
                            limited: true,
                            selectTarget: 1,
                            filterTarget(card, player, target) {
                                return target != player;
                            },
                            derivation: 'yunzhiba',
                            intro: {
                                content: 'limited',
                            },
                            mark: false,
                            init(player) {
                                if (player.hasZhuSkill('yunhenglie')) {
                                    player.markSkill('yunhenglie');
                                    player.storage.yunhenglie = false;
                                }
                            },
                            filter(event, player) {
                                return player.hasZhuSkill('yunhenglie');
                                return player.storage.yunhenglie == false;
                            },
                            enable: 'phaseUse',
                            content() {
                                'step 0';
                                event.num = 2;
                                player.storage.yunhenglie = true;
                                player.awakenSkill('yunhenglie');
                                game.log(target, '成为了目标');
                                target.gainMaxHp();
                                target.disableJudge();
                                if (target.group != 'wu') target.changeGroup('wu');
                                player.addSkill('yunzhiba');
                                if (player.name1 == 'yunsunce') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yunsuncea.jpg');
                                }
                            },
                            ai: {
                                order: 7,
                                result: {
                                    target(player, target) {
                                        if ((target.identity == 'zhong' || target.identity == 'mingzhong') && target.group != 'wu') {
                                            return 10;
                                        }
                                        if (target.identity == 'zhong' || target.identity == 'mingzhong') {
                                            return 5;
                                        }
                                        if (get.attitude(player, target) > 0) {
                                            return 3;
                                        }
                                    },
                                },
                            },
                        },
                        yunjiang: {
                            audio: 'ext:云将/audio:3',
                            forced: true,
                            trigger: {
                                source: 'damageEnd',
                            },
                            filter(event, player) {
                                var evt = event.parent;
                                return (evt && evt.name == 'juedou') > 0;
                            },
                            content() {
                                'step 0';
                                player.chooseDrawRecover(2, true);
                                player.say(['属于我们的时代!开始啦!', '激昂起来了!'].randomGet());
                            },
                        },
                        yunyingyang: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'damageEnd',
                            },
                            usable: 1,
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return _status.currentPhase != player && event.source;
                            },
                            content() {
                                'step 0';
                                player.chooseBool('是否从牌堆随机将一张装备牌置入装备栏并视对' + get.translation(trigger.source) + '使用一张决斗？').set('ai', function (event, player) {
                                    var hs1 = trigger.source.getCards('h', 'sha');
                                    var hs2 = player.getCards('h', 'sha');
                                    if (get.attitude(player, trigger.source) < 0 && hs1.length <= hs2.length) {
                                        return true;
                                    }
                                    if (get.attitude(player, trigger.source) < 0 && hs1 == 0) {
                                        return true;
                                    }
                                    if (get.attitude(player, trigger.source) < 0 && hs1 > hs2) {
                                        return false;
                                    }
                                    if (get.attitude(player, trigger.source) > 0) {
                                        return false;
                                    }
                                });
                                ('step 1');
                                if (result.bool) {
                                    for (var i = 1; i < 7; i++) {
                                        if (player.isEmpty(i)) {
                                            var sub = 'equip' + i,
                                                card = get.cardPile2(function (card) {
                                                    return get.subtype(card, false) == sub && !get.cardtag(card, 'gifts');
                                                });
                                            if (!card) {
                                                player.chat('无牌可得了吗');
                                                game.log('牌堆里面已经没有装备牌了!');
                                                event.finish();
                                                return;
                                            }
                                            if (card) {
                                                player.$gain2(card);
                                                player.equip(card);
                                                break;
                                            }
                                        }
                                    }
                                    player.useCard({ name: 'juedou', skill: 'yunyingyang' }, trigger.source);
                                }
                            },
                            ai: {
                                expose: 0.3,
                            },
                            group: ['yunyingyang_use'],
                            subSkill: {
                                use: {
                                    audio: 'ext:云将/audio:2',
                                    enable: 'phaseUse',
                                    filterCard(card) {
                                        return get.type(card) == 'equip';
                                    },
                                    position: 'he',
                                    prompt: '请选择【鹰扬】的目标',
                                    filter(event, player) {
                                        var num = player.getStat().skill.yunyingyang_use;
                                        if (num > 0) {
                                            num = _status.event.player.getStat().skill.yunyingyang_use;
                                        } else {
                                            num = 1;
                                        }
                                        return player.countCards('hes', { type: 'equip' }) >= num;
                                    },
                                    selectCard(card) {
                                        var num = _status.event.player.getStat().skill.yunyingyang_use;
                                        if (num > 0) return num + 1;
                                        return 1;
                                    },
                                    filterTarget(card, player, target) {
                                        return lib.filter.targetEnabled({ name: 'juedou' }, player, target);
                                    },
                                    content() {
                                        player.useCard({ name: 'juedou' }, target, false);
                                    },
                                    ai: {
                                        wuxie(target, card, player, viewer) {
                                            if (player == game.me && get.attitude(viewer, player) > 0) {
                                                return 0;
                                            }
                                        },
                                        basic: {
                                            order: 5,
                                            useful: 1,
                                            value: 5.5,
                                        },
                                        result: {
                                            target(player, target) {
                                                return get.effect(target, { name: 'juedou' }, player, target);
                                            },
                                            player(player, target, card) {
                                                if (
                                                    player.hasSkillTag(
                                                        'directHit_ai',
                                                        true,
                                                        {
                                                            target: target,
                                                            card: card,
                                                        },
                                                        true,
                                                    )
                                                ) {
                                                    return 0;
                                                }
                                                if (get.damageEffect(target, player, target) > 0 && get.attitude(player, target) > 0 && get.attitude(target, player) > 0) {
                                                    return 0;
                                                }
                                                var hs1 = target.getCards('h', 'sha');
                                                var hs2 = player.getCards('h', 'sha');
                                                if (hs1.length > hs2.length + 1) {
                                                    return -2;
                                                }
                                                var hsx = target.getCards('h');
                                                if (hsx.length > 2 && hs2.length == 0 && hsx[0].number < 6) {
                                                    return -2;
                                                }
                                                if (hsx.length > 3 && hs2.length == 0) {
                                                    return -2;
                                                }
                                                if (hs1.length > hs2.length && (!hs2.length || hs1[0].number > hs2[0].number)) {
                                                    return -2;
                                                }
                                                return -0.5;
                                            },
                                        },
                                        tag: {
                                            respond: 2,
                                            respondSha: 2,
                                            damage: 1,
                                        },
                                    },
                                },
                            },
                        },
                        yunbugua: {
                            audio: 'yunbugua_off',
                            trigger: {
                                player: 'damageEnd',
                            },
                            forced: true,
                            usable: 1,
                            content() {
                                player.chooseToGuanxing();
                                player.draw();
                                if (player.hasSkill('yunfuzhu') && player.storage.yunfuzhu < 3) {
                                    player.storage.yunfuzhu++;
                                    player.markSkill('yunfuzhu');
                                    player.node.avatar.setBackground('yunxushi', 'character');
                                }
                            },
                            global: 'yunbugua_off',
                            subSkill: {
                                off: {
                                    audio: 'ext:云将/audio:2',
                                    enable: 'phaseUse',
                                    usable: 1,
                                    popup: false,
                                    filterTarget(card, player, target) {
                                        return player != target && target.hasSkill('yunbugua');
                                    },
                                    filter(event, player) {
                                        if (player.hasSkill('yunbugua')) return false;
                                        return game.hasPlayer(function (current) {
                                            return current.hasSkill('yunbugua');
                                        });
                                    },
                                    prompt: '令一名拥有〖卜卦〗的角色进行一次【卜算】,你与其各摸一张牌',
                                    content() {
                                        target.chooseToGuanxing();
                                        player.draw();
                                        target.draw();
                                        if (target.hasSkill('yunfuzhu')) {
                                            target.storage.yunfuzhu++;
                                            target.markSkill('yunfuzhu');
                                            if (target.name == 'yunxushi') {
                                                target.node.avatar.setBackground('yunxushi', 'character');
                                            }
                                        }
                                    },
                                    ai: {
                                        order: 10,
                                        result: {
                                            player(player, target) {
                                                var target = game.findPlayer(function (current) {
                                                    return current.hasSkill('yunbugua');
                                                });
                                                if (target) {
                                                    return get.attitude(player, target);
                                                }
                                            },
                                        },
                                    },
                                },
                            },
                        },
                        yunfuzhu: {
                            audio: 'ext:云将/audio:3',
                            enable: 'phaseUse',
                            filterTarget(card, player, target) {
                                return target != player && player.inRange(target) && player.storage.yunfuzhu >= target.hp;
                            },
                            mark: true,
                            marktext: '伏诛',
                            intro: {
                                name: '伏诛',
                                content: '当前已拥有#个〖伏诛〗标记',
                            },
                            selectTarget: 1,
                            prompt: '选择伏诛目标',
                            init(player) {
                                player.storage.yunfuzhu = 0;
                                player.markSkill('yunfuzhu');
                            },
                            filter(event, player) {
                                return player.storage.yunfuzhu >= 3;
                            },
                            content() {
                                'step 0';
                                if (player.name1 == 'yunxushi') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yunxushia.jpg');
                                }
                                if (player.name1 == 'yunxushi') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/落花有意a.jpg');
                                }
                                if (player.name1 == 'yunxushi') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/诗情碧宵a.jpg');
                                }
                                ('step 1');
                                player.removeMark('yunfuzhu');
                                player.useCard({ name: 'sha' }, target, false);
                                ('step 2');
                                if (target.isDead()) {
                                    player.storage.yunfuzhu = 0; //QQQ
                                    event.finish();
                                }
                                ('step 3');
                                if (player.storage.yunfuzhu > 0) {
                                    event.goto(1);
                                }
                                ('step 4');
                                player.turnOver();
                                player.loseHp(player.hp - 1);
                            },
                            ai: {
                                order: 3,
                                result: {
                                    target(player, target) {
                                        if (get.attitude(player, target) < 0) return get.effect(target, { name: 'sha' }, player, target);
                                        if (player.storage.yunfuzhu >= target.hp && get.attitude(player, target) < 0) {
                                            var att = player.storage.yunfuzhu - target.hp - 1;
                                            return 3 * -att;
                                        }
                                    },
                                },
                            },
                        },
                        yundanlie: {
                            mark: true,
                            marktext: '胆裂',
                            intro: {
                                content(storage, player) {
                                    var num = player.hp;
                                    return '<div>子龙!叶轩!贼寇已然胆寒,随我杀!<br><br>你本回合使用牌数至多为' + num + '<br>你计算与其他角色的距离加' + num + '<br>其他角色计算与你的距离减' + num + '</span></div>';
                                },
                            },
                            mod: {
                                globalFrom(from, to, current) {
                                    return current + Math.max(0, from.hp);
                                },
                                globalTo(from, to, current) {
                                    return current - Math.max(0, from.hp);
                                },
                                cardEnabled(card, player) {
                                    if (_status.currentPhase != player) return;
                                    if (player.countUsed() >= player.hp) return false;
                                },
                            },
                            trigger: {
                                player: 'recoverEnd',
                            },
                            forced: true,
                            content() {
                                player.removeSkill('yundanlie');
                            },
                            ai: {
                                threaten: 3,
                            },
                        },
                        yunzhujing: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseDiscardEnd',
                            },
                            forced: true,
                            filter(event, player) {
                                return event.cards && event.cards.length && player.hujia == 0;
                            },
                            content() {
                                player.changeHujia(Math.min(5, trigger.cards.length));
                            },
                            mod: {
                                maxHandcard(player, num) {
                                    return (num = 2);
                                },
                            },
                            group: ['yunzhujing_draw'],
                            subSkill: {
                                draw: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        player: 'phaseDrawBefore',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return player.hujia > 0;
                                    },
                                    content() {
                                        event.num = Math.min(trigger.num, 9);
                                        trigger.num = Math.min(5, player.hujia + event.num);
                                    },
                                },
                            },
                        },
                        yunyicong: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                source: 'damageBegin',
                            },
                            usable: 1,
                            frequent: 'check',
                            check(event, player) {
                                return get.attitude(player, event.player) < 0;
                            },
                            derivation: 'yundanlie',
                            filter(event, player) {
                                return event.player.isAlive() && event.card && event.card.name == 'sha';
                            },
                            content() {
                                trigger.player.addSkill('yundanlie');
                            },
                            ai: {
                                threaten: 1,
                            },
                            group: ['yunyicong_use'],
                            subSkill: {
                                use: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        target: 'useCardToBegin',
                                    },
                                    forced: true,
                                    usable: 1,
                                    filter(event, player) {
                                        return event.card.name == 'sha';
                                    },
                                    content() {
                                        player.useCard({ name: 'sha' }, trigger.player, false);
                                    },
                                },
                            },
                        },
                        yunwansha: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                global: 'dying',
                            },
                            logTarget: 'player',
                            usable: 1,
                            forced: true,
                            check(event, player) {
                                return get.attitude(player, event.player) < 0;
                            },
                            filter(event, player) {
                                return _status.currentPhase == player && event.player != player && player.countCards('he') > 0;
                            },
                            content() {
                                'step 0';
                                player.chooseToDiscard('he', '是否弃置一张牌对' + get.translation(trigger.player) + '发动【完杀】？').set('ai', function (card) {
                                    if (get.attitude(player, trigger.player) <= 0) {
                                        return true;
                                    } else {
                                        return false;
                                    }
                                    return 6 - get.value(card);
                                });
                                ('step 1');
                                if (result.bool) {
                                    player.judge(function (card) {
                                        return get.color(card) == 'black' ? 1 : 0;
                                    });
                                } else {
                                    player.getStat('triggerSkill').yunwansha--;
                                    event.finish();
                                }
                                ('step 2');
                                if (result.color == 'black') {
                                    player.say(['死!', '英雄,也会孤掌难鸣呐!'].randomGet());
                                    trigger.player.die(trigger.reason);
                                }
                                if (result.color == 'red') {
                                    player.say(['哼!', '濒死求活的小可怜哟～'].randomGet());
                                    trigger.player.addTempSkill('yunfengjin', { player: 'phaseAfter' });
                                }
                                ('step 3');
                                if (!trigger.player.isAlive()) {
                                    trigger.cancel(true);
                                }
                            },
                        },
                        yunjimou: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseZhunbeiBegin',
                            },
                            forced: true,
                            content() {
                                player.addTempSkill('yunkongju');
                                player.node.avatar.setBackground('db_yunjiaxu', 'character');
                            },
                            group: ['yunjimou_jieshu'],
                            subSkill: {
                                jieshu: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        player: 'phaseJieshuBegin',
                                    },
                                    forced: true,
                                    content() {
                                        player.addTempSkill('yunweiwo', { player: 'phaseZhunbeiBegin' });
                                        player.node.avatar.setBackgroundImage('extension/云将/image/db_yunjiaxua.jpg');
                                        player.node.avatar.setBackgroundImage('extension/云将/image/文和乱武a.jpg');
                                    },
                                },
                            },
                        },
                        yunyongdi: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseZhunbeiBegin',
                            },
                            content() {
                                'step 0';
                                player
                                    .chooseTarget(true, '选择一名其他角色作为【拥嫡】目标', function (card, player, target) {
                                        return target != player;
                                    })
                                    .set('ai', function (target) {
                                        return get.attitude(player, target);
                                    });
                                ('step 1');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    target.gainMaxHp(true);
                                    target.recover();
                                    var mode = get.mode();
                                    if (mode == 'identity' || (mode == 'versus' && _status.mode == 'four')) {
                                        if (target.name && lib.character[target.name]) {
                                            var skills = lib.character[target.name][3];
                                            target.storage.zhuSkill_yunyongdi = [];
                                            for (var i = 0; i < skills.length; i++) {
                                                var info = lib.skill[skills[i]];
                                                if (info.zhuSkill) {
                                                    target.storage.zhuSkill_yunyongdi.push(skills[i]);
                                                    if (info.init) {
                                                        info.init(target);
                                                    }
                                                    if (info.init2) {
                                                        info.init2(target);
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            },
                            ai: {
                                expose: 0.2,
                            },
                        },
                        yunluanwu: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            filterTarget(card, player, target) {
                                return target != player;
                            },
                            selectTarget: -1,
                            content() {
                                'step 0';
                                event.current = player.next;
                                event.currented = [];
                                ('step 1');
                                event.currented.push(event.current);
                                event.current.addTempClass('target');
                                event.current
                                    .chooseToUse(
                                        '乱武:使用一张杀或失去一点体力',
                                        function (card) {
                                            if (card.name != 'sha') return false;
                                            return lib.filter.filterCard.apply(this, arguments);
                                        },
                                        function (card, player, target) {
                                            if (player == target) return false;
                                            if (target.hasSkill('yunjimou')) return false;
                                            var dist = get.distance(player, target);
                                            if (dist > 1) {
                                                if (
                                                    game.hasPlayer(function (current) {
                                                        return current != player && get.distance(player, current) < dist;
                                                    })
                                                ) {
                                                    return false;
                                                }
                                            }
                                            return lib.filter.filterTarget.apply(this, arguments);
                                        },
                                    )
                                    .set('ai2', function () {
                                        return get.effect_use.apply(this, arguments) + 0.01;
                                    });
                                ('step 2');
                                if (result.bool == false) event.current.loseHp();
                                event.current = event.current.next;
                                if (event.current != player && !event.currented.includes(event.current)) {
                                    event.goto(1);
                                }
                            },
                            ai: {
                                order: 1,
                                result: {
                                    player(player) {
                                        if (lib.config.mode == 'identity' && game.zhu.isZhu && player.identity == 'fan') {
                                            if (game.zhu.hp == 1 && game.zhu.countCards('h') <= 2) return 1;
                                        }
                                        var num = 0;
                                        var players = game.filterPlayer();
                                        for (var i = 0; i < players.length; i++) {
                                            var att = get.attitude(player, players[i]);
                                            if (att > 0) att = 1;
                                            if (att < 0) att = -1;
                                            if (players[i] != player && players[i].hp <= 3) {
                                                if (players[i].countCards('h') == 0) num += att / players[i].hp;
                                                else if (players[i].countCards('h') == 1) num += att / 2 / players[i].hp;
                                                else if (players[i].countCards('h') == 2) num += att / 4 / players[i].hp;
                                            }
                                            if (players[i].hp == 1) num += att * 1.5;
                                        }
                                        if (player.hp == 1) {
                                            return -num;
                                        }
                                        if (player.hp == 2) {
                                            return -game.players.length / 4 - num;
                                        }
                                        return -game.players.length / 3 - num;
                                    },
                                },
                            },
                        },
                        yunkongju: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                player: 'useCardAfter',
                            },
                            mark: true,
                            marktext: '控局',
                            intro: {
                                name: '控局',
                                content: '你使用红色的基本牌或普通锦囊牌可额外结算一次',
                            },
                            prompt(event, player) {
                                var str = '';
                                str += '是否令' + get.translation(event.card) + '额外结算一次？';
                                return str;
                            },
                            check(event, player) {
                                if (get.tag({ name: event.card.name }, 'norepeat')) return false;
                                return true;
                            },
                            filter(event, player) {
                                if (event.parent.name == 'yunkongju') return false;
                                if (!event.targets || !event.card) return false;
                                if (event.card && event.card.name == 'wuxie') return false;
                                if (event.card && event.card.name == 'wanjian') return false;
                                if (event.card && event.card.name == 'nanman') return false;
                                if (event.card && event.card.name == 'shan') return false;
                                var type = get.type(event.card);
                                if (type != 'basic' && type != 'trick') return false;
                                var color = get.color(event.card);
                                if (color != 'red') return false;
                                var card = game.createCard(event.card.name, event.card.suit, event.card.number, event.card.nature);
                                var targets = event._targets || event.targets;
                                for (var i = 0; i < targets.length; i++) {
                                    if (!targets[i].isIn()) return false;
                                    if (
                                        !player.canUse(
                                            {
                                                name: event.card.name,
                                            },
                                            targets[i],
                                            false,
                                            false,
                                        )
                                    ) {
                                        return false;
                                    }
                                }
                                return true;
                            },
                            content() {
                                var card = game.createCard(trigger.card.name, trigger.card.suit, trigger.card.number, trigger.card.nature);
                                player.useCard(card, (trigger._targets || trigger.targets).slice(0), false).set('ai', () => -1);
                            },
                            ai: {
                                effect: {
                                    player(card, player, target) {
                                        if (get.color(card) == 'red' && card.name == 'sha' && get.attitude(player, target) < 0) return 1.5;
                                    },
                                },
                            },
                            group: ['yunkongju_aoe'],
                            subSkill: {
                                aoe: {
                                    audio: 'yunkongju',
                                    trigger: {
                                        player: 'useCardAfter',
                                    },
                                    prompt(event, player) {
                                        var str = '';
                                        str += '是否令' + get.translation(event.card) + '额外结算一次？';
                                        return str;
                                    },
                                    check(event, player) {
                                        if (get.tag({ name: event.card.name }, 'norepeat')) return false;
                                        return true;
                                    },
                                    filter(event, player) {
                                        if (event.parent.name == 'yunkongju_aoe') return false;
                                        return (event.card && event.card.name == 'wanjian') || (event.card.name == 'nanman' && get.color(event.card) == 'red');
                                    },
                                    content() {
                                        var card = game.createCard(trigger.card.name, trigger.card.suit, trigger.card.number, trigger.card.nature);
                                        player.useCard(card, (trigger._targets || trigger.targets).slice(0)).set('ai', () => -1);
                                    },
                                    ai: {
                                        effect: {
                                            player(card, player, target) {
                                                if (get.color(card) == 'red' && card.name == 'sha' && get.attitude(player, target) < 0) return 1.5;
                                            },
                                        },
                                    },
                                },
                            },
                        },
                        yunweiwo: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'useCard',
                            },
                            mark: true,
                            marktext: '帷幄',
                            intro: {
                                name: '帷幄',
                                content: '其他角色使用的黑色牌不能指定你为目标',
                            },
                            forced: true,
                            firstDo: true,
                            filter(event, player, card) {
                                if (get.color(event.card) != 'black') return false;
                                return (event.card.name == 'nanman' && player != event.player) || (event.card.name == 'wanjian' && player != event.player) || (event.card.name == 'taoyuan' && player.hp < player.maxHp) || event.card.name == 'wugu';
                            },
                            content() { },
                            mod: {
                                targetEnabled(card, player, target) {
                                    if (get.color(card) == 'black' && player != target) return false;
                                },
                            },
                        },
                        yunjiance: {
                            audio: 'ext:云将/audio:4',
                            enable: 'phaseUse',
                            usable: 1,
                            check(card) {
                                return 10 - get.value(card);
                            },
                            filter(event, player) {
                                return player.countCards('h') > 0;
                            },
                            filterTarget(card, player, target) {
                                if (target == player) return false;
                                if (ui.selected.targets.length) {
                                    return ui.selected.targets[0] != target && !ui.selected.targets[0].hasSkillTag('noCompareSource') && target.countCards('h') && !target.hasSkillTag('noCompareTarget');
                                }
                                return true;
                            },
                            filterCard: true,
                            targetprompt: ['发起拼点', '被拼点'],
                            prompt: '选择【间策】的两名目标',
                            selectTarget: 2,
                            multitarget: true,
                            content() {
                                'step 0';
                                targets[0].gain(cards, player, 'give');
                                ('step 1');
                                targets[0].chooseToCompare(targets[1]);
                                ('step 2');
                                if (result.bool) {
                                    targets[0].chooseToDiscard('he', 2, true);
                                    targets[1].damage();
                                } else if (result.tie) {
                                    targets[0].damage();
                                    targets[1].damage();
                                } else {
                                    targets[1].chooseToDiscard('he', 2, true);
                                    targets[0].damage();
                                }
                            },
                            intro: {
                                content: 'limited',
                            },
                            ai: {
                                expose: 0.3,
                                order: 7,
                                result: {
                                    target(player, target) {
                                        if (player.hasUnknown()) return 0;
                                        if (ui.selected.targets.length) return -2;
                                        return -1;
                                    },
                                },
                            },
                        },
                        yunlongdan: {
                            audio: 'ext:云将/audio:4',
                            enable: ['chooseToUse', 'chooseToRespond'],
                            hiddenCard(player, name) {
                                return get.type(name) == 'basic' && player.countCards('h') > 0;
                            },
                            filter(event, player) {
                                return player.countCards('h') > 0 && ((event.filterCard && event.filterCard({ name: 'sha' }, player, event)) || event.filterCard({ name: 'shan' }, player, event));
                            },
                            chooseButton: {
                                dialog(event, player) {
                                    var list = [];
                                    if (event.filterCard && event.filterCard({ name: 'sha' }, player, event)) {
                                        list.push(['基本', '', 'sha']);
                                    }
                                    if (event.filterCard && event.filterCard({ name: 'shan' }, player, event)) {
                                        list.push(['基本', '', 'shan']);
                                    }
                                    return ui.create.dialog('龙胆', [list, 'vcard'], 'hidden');
                                },
                                check(button) {
                                    var player = _status.event.player;
                                    var card = { name: button.link[2], nature: button.link[3] };
                                    if (
                                        game.hasPlayer(function (current) {
                                            return get.effect(current, card, player, player) > 0;
                                        })
                                    ) {
                                        if (card.name == 'sha') {
                                            return 10;
                                        } else if (card.name == 'shan') {
                                            return 10;
                                        }
                                    }
                                    return 0;
                                },
                                backup(links, player) {
                                    return {
                                        filterCard: true,
                                        selectCard: true,
                                        popname: true,
                                        check(card) {
                                            return 9 - get.value(card);
                                        },
                                        position: 'h',
                                        viewAs: {
                                            name: links[0][2],
                                            nature: links[0][3],
                                        },
                                        precontent() { },
                                    };
                                },
                                prompt(links, player) {
                                    return '将任意一张牌当做【' + get.translation(links[0][3] || '') + get.translation(links[0][2]) + '】使用或打出';
                                },
                            },
                            mod: {
                                cardUsable(card, player, num) {
                                    if (card.name == 'sha') return (num += Math.min(3, player.getDamagedHp()));
                                },
                                aiValue(player, card, num) {
                                    if (card.name != 'sha' && card.name != 'shan') return;
                                    var geti = function () {
                                        var cards = player.getCards('hs', function (card) {
                                            return card.name == 'sha' || card.name == 'shan';
                                        });
                                        if (cards.includes(card)) {
                                            return cards.indexOf(card);
                                        }
                                        return cards.length;
                                    };
                                    return Math.max(num, [7, 5, 5, 3][Math.min(geti(), 3)]);
                                },
                                aiUseful() {
                                    return lib.skill.yunlongdan.mod.aiValue.apply(this, arguments);
                                },
                            },
                            ai: {
                                expose: 0.3,
                                order() {
                                    return 4;
                                },
                                respondSha: true,
                                respondShan: true,
                                skillTagFilter(player, tag, arg) {
                                    return player.countCards('h') > 0;
                                },
                                result: {
                                    player(player) {
                                        return 1;
                                    },
                                },
                            },
                        },
                        yunlonglie: {
                            audio: 'ext:云将/audio:4',
                            trigger: {
                                player: ['useCardEnd', 'respondEnd'],
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                if (event.skill != 'yunlongdan_backup') return false;
                                return player.storage.yunlonglie_spade == 0 || player.storage.yunlonglie_heart == 0 || player.storage.yunlonglie_club == 0 || player.storage.yunlonglie_diamond == 0;
                            },
                            content() {
                                'step 0';
                                var map = {};
                                for (var i of trigger.cards) {
                                    map[i.suit] = true;
                                }
                                if (map.spade && player.storage.yunlonglie_spade == 0) {
                                    player.storage.yunlonglie_spade = 1;
                                    event.goto(2);
                                }
                                if (map.heart && player.storage.yunlonglie_heart == 0) {
                                    player.storage.yunlonglie_heart = 1;
                                    player.recover();
                                    event.finish();
                                }
                                if (map.club && player.storage.yunlonglie_club == 0) {
                                    player.storage.yunlonglie_club = 1;
                                    event.goto(5);
                                }
                                if (map.diamond && player.storage.yunlonglie_diamond == 0) {
                                    player.storage.yunlonglie_diamond = 1;
                                    player.draw(2);
                                    event.finish();
                                }
                                ('step 1');
                                event.finish();
                                ('step 2');
                                player.chooseTarget('是否发动【龙烈】对一名其他角色造成一点伤害', function (card, player, target) {
                                    return player != target;
                                }).ai = function (target) {
                                    return get.damageEffect(target, player, player);
                                };
                                ('step 3');
                                if (result.bool) {
                                    result.targets[0].damage(player);
                                }
                                ('step 4');
                                event.finish();
                                ('step 5');
                                player
                                    .chooseTarget('是否发动【龙烈】弃置一名其他角色一张牌', function (card, player, target) {
                                        return target != player && target.countCards('he') > 0;
                                    })
                                    .set('ai', function (target) {
                                        player = get.player();
                                        return -get.attitude(player, target);
                                    });
                                ('step 6');
                                if (result.bool) {
                                    player.discardPlayerCard(result.targets[0], 'he', true);
                                }
                            },
                            ai: {
                                expose: 0.3,
                            },
                            group: 'yunlonglie_jieshu',
                            subSkill: {
                                jieshu: {
                                    trigger: {
                                        global: 'phaseJieshuAfter',
                                    },
                                    silent: true,
                                    fixed: true,
                                    forced: true,
                                    popup: false,
                                    charlotte: true,
                                    init(player) {
                                        player.storage.yunlonglie_spade = 0;
                                        player.storage.yunlonglie_heart = 0;
                                        player.storage.yunlonglie_club = 0;
                                        player.storage.yunlonglie_diamond = 0;
                                    },
                                    content() {
                                        player.storage.yunlonglie_spade = 0;
                                        player.storage.yunlonglie_heart = 0;
                                        player.storage.yunlonglie_club = 0;
                                        player.storage.yunlonglie_diamond = 0;
                                    },
                                },
                            },
                        },
                        yunlinglong: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'gainAfter',
                            },
                            forced: true,
                            filter(event, player) {
                                return event.getParent(2).name != 'yunlinglong';
                            },
                            content() {
                                'step 0';
                                player.chooseTarget('请选择【玲珑】摸牌的目标', '令一名角色摸一张牌').set('ai', function (target) {
                                    return get.attitude(_status.event.player, target) * Math.sqrt(Math.max(1, 4 - target.countCards('h')));
                                });
                                ('step 1');
                                if (result.bool) {
                                    player.say(['七巧玲珑心!', '夫君,切勿操劳'].randomGet());
                                    var target = result.targets[0];
                                    player.line(target, 'green');
                                    target.draw();
                                    if (player.storage.yunlinglong != 1) {
                                        player.storage.yunlinglong_recover++;
                                        player.storage.yunlinglong = 1;
                                    }
                                }
                            },
                            mod: {
                                maxHandcard(player, num) {
                                    return num + player.storage.yunlinglong_recover;
                                },
                                ai: {
                                    expose: 0.3,
                                },
                            },
                            group: ['yunlinglong_damage', 'yunlinglong_discard', 'yunlinglong_recover', 'yunlinglong_qiqiao'],
                            subSkill: {
                                discard: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        player: 'loseAfter',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return event.getParent(3).name != 'yunlinglong_discard' && event.cards2.length && _status.currentPhase != player;
                                    },
                                    content() {
                                        'step 0';
                                        player
                                            .chooseTarget('请选择【玲珑】弃置牌的目标', '弃置一名角色区域内一张牌', function (card, player, target) {
                                                return target.countDiscardableCards(player, 'hej') > 0;
                                            })
                                            .set('ai', function (target) {
                                                var player = _status.event.player;
                                                var att = get.attitude(player, target);
                                                if (att < 0) {
                                                    att = -Math.sqrt(-att);
                                                } else {
                                                    att = Math.sqrt(att);
                                                }
                                                return att * lib.card.guohe.ai.result.target(player, target);
                                            });
                                        ('step 1');
                                        if (result.bool) {
                                            player.say(['七巧玲珑心!', '夫君,切勿操劳'].randomGet());
                                            player.discardPlayerCard(1, result.targets[0], 'hej');
                                            if (player.storage.yunlinglong_damage != 1) {
                                                player.storage.yunlinglong_recover++;
                                                player.storage.yunlinglong_damage = 1;
                                            }
                                        }
                                    },
                                    ai: {
                                        expose: 0.3,
                                    },
                                },
                                recover: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        player: 'recoverEnd',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return event.getParent(1).name != 'yunlinglong_recover' && _status.currentPhase == player;
                                    },
                                    content() {
                                        'step 0';
                                        player
                                            .chooseTarget('请选择【玲珑】回复体力的目标', '令一名角色回复一点体力', function (card, player, target) {
                                                return target.hp < target.maxHp;
                                            })
                                            .set('ai', function (target) {
                                                return get.attitude(_status.event.player, target) * target.getDamagedHp();
                                            });
                                        ('step 1');
                                        if (result.bool) {
                                            player.say(['七巧玲珑心!', '夫君,切勿操劳'].randomGet());
                                            var target = result.targets[0];
                                            player.line(target, 'green');
                                            target.recover();
                                            if (player.storage.yunlinglong_discard != 1) {
                                                player.storage.yunlinglong_recover++;
                                                player.storage.yunlinglong_discard = 1;
                                            }
                                            if (player.name1 == 'yunhuangyueying') {
                                                player.node.avatar.setBackground('yunhuangyueying', 'character');
                                            }
                                        }
                                    },
                                    ai: {
                                        expose: 0.3,
                                    },
                                },
                                damage: {
                                    popup: false,
                                    forced: true,
                                    trigger: {
                                        player: 'damageEnd',
                                    },
                                    content() {
                                        if (player.name1 == 'yunhuangyueying') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/yunhuangyueyinga.jpg');
                                        }
                                        if (player.name1 == 'yunhuangyueying') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/风华绝代_hyy_a.jpg');
                                        }
                                        if (player.name1 == 'yunhuangyueying') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/红梅傲雪a.jpg');
                                        }
                                        if (player.name1 == 'yunhuangyueying') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/繁华落尽a.jpg');
                                        }
                                        if (player.name1 == 'yunhuangyueying') {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/书韵生香a.jpg');
                                        }
                                    },
                                },
                            },
                        },
                        yunlinglong_qiqiao: {
                            trigger: {
                                player: 'phaseJieshuAfter',
                            },
                            silent: true,
                            fixed: true,
                            forced: true,
                            popup: false,
                            charlotte: true,
                            init(player) {
                                player.storage.yunlinglong_recover = 0;
                            },
                            content() {
                                player.storage.yunlinglong_recover = 0; //QQQ
                                player.storage.yunlinglong = 0;
                                player.storage.yunlinglong_damage = 0;
                                player.storage.yunlinglong_discard = 0;
                            },
                        },
                        yunyuhu: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            usable: 1,
                            filter(event, player) {
                                if (player.countCards('h') == 0) return false;
                                return game.hasPlayer(function (current) {
                                    return current.countCards('h');
                                });
                            },
                            filterTarget(card, player, target) {
                                return player.canCompare(target);
                            },
                            content() {
                                'step 0';
                                player.chooseToCompare(target);
                                event.target_hu = target;
                                ('step 1');
                                if (result.bool) {
                                    player.recover(event.target_hu);
                                    if (
                                        game.countPlayer(function (current) {
                                            return current != event.target_hu;
                                        })
                                    ) {
                                        player
                                            .chooseTarget('请选择【驭虎】目标', '是否指定一名角色受到一点伤害', function (card, player, target) {
                                                return target != event.target_hu && target != player;
                                            })
                                            .set('ai', function (target) {
                                                var source = _status.event.source;
                                                return get.damageEffect(target, _status.event.source, player);
                                            })
                                            .set('source', target);
                                    } else {
                                        event.finish();
                                    }
                                } else {
                                    event.goto(3);
                                }
                                ('step 2');
                                if (result.bool && result.targets && result.targets.length) {
                                    target.line(result.targets[0], 'green');
                                    result.targets[0].damage(event.target_hu);
                                    event.finish();
                                } else {
                                    event.finish();
                                }
                                ('step 3');
                                player.damage(event.target_hu);
                                if (
                                    game.countPlayer(function (current) {
                                        return current != player && current.getDamagedHp();
                                    })
                                ) {
                                    player
                                        .chooseTarget('请选择【驭虎】目标', '是否指定一名角色回复一点体力', function (card, player, target) {
                                            return target != player && target != event.target_hu && target.getDamagedHp();
                                        })
                                        .set('ai', function (target) {
                                            if (get.attitude(player, target) > 0) return target.getDamagedHp() + 1;
                                            return 0;
                                        });
                                } else {
                                    event.finish();
                                }
                                ('step 4');
                                if (result.bool) {
                                    player.line(result.targets[0], 'green');
                                    result.targets[0].recover(player);
                                } else {
                                    event.finish();
                                }
                            },
                            ai: {
                                order: 1,
                                result: {
                                    target(player, target) {
                                        var att = get.attitude(player, target);
                                        var oc = target.countCards('h') == 1;
                                        if (att > 0 && oc) return 0;
                                        var players = game.filterPlayer();
                                        for (var i = 0; i < players.length; i++) {
                                            if (players[i] != target && players[i] != player && target.inRange(players[i])) {
                                                if (get.damageEffect(players[i], target, player) > 0) {
                                                    return att > 0 ? att / 2 : att - (oc ? 5 : 0);
                                                }
                                            }
                                        }
                                        return 0;
                                    },
                                    player(player, target) {
                                        if (target.hasSkillTag('jueqing', false, target)) return -10;
                                        var mn = 1;
                                        var hs = player.getCards('h');
                                        for (var i = 0; i < hs.length; i++) {
                                            mn = Math.max(mn, hs[i].number);
                                        }
                                        if (player.hp >= 2) return 5;
                                        if (mn <= 11 && player.hp < 2) return -20;
                                        var max = player.maxHp - hs.length;
                                        var players = game.filterPlayer();
                                        for (var i = 0; i < players.length; i++) {
                                            if (get.attitude(player, players[i]) > 2) {
                                                max = Math.max(Math.min(5, players[i].hp) - players[i].countCards('h'), max);
                                            }
                                        }
                                        switch (max) {
                                            case 0:
                                                return mn == 13 ? 0 : -20;
                                            case 1:
                                                return mn >= 12 ? 0 : -15;
                                            case 2:
                                                return 0;
                                            case 3:
                                                return 1;
                                            default:
                                                return max;
                                        }
                                    },
                                },
                                expose: 0.2,
                            },
                        },
                        yunwangzuo: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'damageEnd',
                            },
                            popup: false,
                            forced: true,
                            content() {
                                'step 0';
                                event.count = Math.min(trigger.num, 9);
                                ('step 1');
                                event.count--;
                                player
                                    .chooseTarget(get.prompt2('yunwangzuo'), function (card, player, target) {
                                        return target.maxHp > 0;
                                    })
                                    .set('ai', function (target) {
                                        if (get.attitude(_status.event.player, target) > 0) {
                                            var att = 7;
                                        }
                                        if (get.attitude(_status.event.player, target) < 0) {
                                            var att = -7;
                                        }
                                        if (get.attitude(_status.event.player, target) == 0) {
                                            var att = 0;
                                        }
                                        var draw = Math.min(5, target.maxHp) - target.countCards('h');
                                        if (draw >= 0) {
                                            if (target.hasSkillTag('nogain')) att /= 6;
                                            if (att > 0 && target == player) {
                                                return Math.sqrt(draw + 0.5) * att;
                                            }
                                            if (att > 0 && target != player) {
                                                return Math.sqrt(draw + 1) * att;
                                            }
                                            return att / 3;
                                        }
                                        if (draw < -1) {
                                            if (target.hasSkillTag('nogain')) att *= 6;
                                            if (att < 0) {
                                                return -Math.sqrt(1 - draw) * att;
                                            }
                                        }
                                        return 0;
                                    });
                                ('step 2');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    event.target = target;
                                    target.draw(Math.min(5, target.maxHp));
                                    if (target != player) {
                                        player.draw(Math.min(5, target.maxHp));
                                    }
                                } else {
                                    event.finish();
                                }
                                ('step 3');
                                var num = target.countCards('h') - Math.min(5, target.maxHp);
                                var nump = player.countCards('h') - Math.min(5, target.maxHp);
                                if (num > 0) {
                                    target.chooseToDiscard('h', true, num);
                                }
                                if (target != player && nump > 0) {
                                    player.chooseToDiscard('h', true, nump);
                                }
                                ('step 4');
                                if (event.count > 0) {
                                    event.goto(1);
                                }
                            },
                            ai: {
                                expose: 0.2,
                                maixie: true,
                                maixie_hp: true,
                                effect: {
                                    target(card, player, target, current) {
                                        if (get.tag(card, 'damage') && target.hp > 1 && target.countCards('h') <= 5) {
                                            if (player.hasSkillTag('jueqing', false, target)) return [1, -2];
                                            var max = 0;
                                            var players = game.filterPlayer();
                                            for (var i = 0; i < players.length; i++) {
                                                if (get.attitude(target, players[i]) > 0) {
                                                    max = Math.max(Math.min(5, players[i].hp) - players[i].countCards('h'), max);
                                                }
                                            }
                                            switch (max) {
                                                case 0:
                                                    return 2;
                                                case 1:
                                                    return 1.5;
                                                case 2:
                                                    return [1, 2];
                                                default:
                                                    return [0, max];
                                            }
                                        }
                                        if ((card.name == 'tao' || card.name == 'caoyao') && target.hp > 1 && target.countCards('h') <= target.hp) return [0, 0];
                                    },
                                },
                            },
                        },
                        yunqiaobian: {
                            enable: 'phaseUse',
                            audio: 'yunqiaobian_backup',
                            mark: true,
                            marktext: '巧变',
                            intro: {
                                content(storage, player) {
                                    var qbcs = player.storage.shanlue;
                                    return '<div>还可使用' + qbcs + '次【巧变】</span></div>';
                                },
                            },
                            init(player) {
                                player.storage.shanlue = 1;
                                player.markSkill('shanlue');
                            },
                            filter(event, player) {
                                if (player.hasSkill('yunqiaobian_off') || player.storage.shanlue < 1) return false;
                                var hs = player.getCards('h');
                                if (!hs.length) return false;
                                for (var i = 0; i < hs.length; i++) {
                                    var mod2 = game.checkMod(hs[i], player, 'unchanged', 'cardEnabled2', player);
                                    if (mod2 === false) return false;
                                }
                                return true;
                            },
                            chooseButton: {
                                dialog(player) {
                                    var list = [];
                                    for (var i = 0; i < lib.inpile.length; i++) {
                                        if (get.type(lib.inpile[i]) == 'basic') list.push(['基本', '', lib.inpile[i]]);
                                        if (get.type(lib.inpile[i]) == 'trick') list.push(['锦囊', '', lib.inpile[i]]);
                                    }
                                    return ui.create.dialog(get.translation('yunqiaobian'), [list, 'vcard']);
                                },
                                filter(button, player) {
                                    return lib.filter.filterCard(
                                        {
                                            name: button.link[2],
                                        },
                                        player,
                                        _status.event.parent,
                                    );
                                },
                                check(button) {
                                    var player = _status.event.player;
                                    if (player.countCards('h', button.link[2]) > 0) return 0;
                                    if (button.link[2] == 'wugu') return 0;
                                    var effect = player.getUseValue(button.link[2]);
                                    if (effect > 0) return effect;
                                    return 0;
                                },
                                backup(links, player) {
                                    return {
                                        filterCard: true,
                                        selectCard: 1,
                                        position: 'hes',
                                        audio: 'ext:云将/audio:2',
                                        popname: true,
                                        viewAs: {
                                            name: links[0][2],
                                        },
                                        onuse(result, player) {
                                            player.storage.shanlue--;
                                            if (player.storage.shanlue <= 0) {
                                                player.addTempSkill('yunqiaobian_off');
                                            }
                                        },
                                    };
                                },
                                prompt(links, player) {
                                    return '将一张牌当做' + get.translation(links[0][2]) + '使用';
                                },
                            },
                            mod: {
                                aiOrder(player, card, num) {
                                    if (get.itemtype(card) == 'card' && card.hasGaintag('yunqiaobian')) return num + 1;
                                },
                            },
                            ai: {
                                threaten: 1.5,
                                order: 6,
                                result: {
                                    player(player) {
                                        var allshown = true,
                                            players = game.filterPlayer();
                                        for (var i = 0; i < players.length; i++) {
                                            if (players[i].ai.shown == 0) {
                                                allshown = false;
                                            }
                                            if (players[i] != player && players[i].countCards('h') && get.attitude(player, players[i]) > 0) {
                                                return 1;
                                            }
                                        }
                                        if (allshown) return 1;
                                        return 0;
                                    },
                                },
                            },
                            group: ['yunqiaobian_jieshu'],
                            subSkill: {
                                jieshu: {
                                    trigger: {
                                        player: 'phaseJieshuBegin',
                                    },
                                    forced: true,
                                    popup: false,
                                    filter(event, player) {
                                        return player.storage.shanlue <= 0;
                                    },
                                    content() {
                                        player.storage.shanlue++;
                                        player.markSkill('shanlue');
                                    },
                                },
                                backup: {
                                    audio: 'ext:云将/audio:2',
                                },
                            },
                        },
                        yunqiaobian_off: {
                            charlotte: true,
                        },
                        yunshanlue: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'damageEnd',
                            },
                            forced: true,
                            content() {
                                'step 0';
                                event.num = Math.min(trigger.num, 9);
                                player.say(['膝盖中了一箭!', '就这？!'].randomGet());
                                ('step 1');
                                player.judge();
                                ('step 2');
                                if (result.color == 'black') {
                                    player.storage.shanlue++;
                                    player.markSkill('shanlue');
                                    if (player.storage.shanlue > 0) {
                                        player.removeSkill('yunqiaobian_off');
                                    }
                                } else {
                                    player.draw(2);
                                }
                                ('step 3');
                                event.num--;
                                if (event.num > 0) {
                                    event.goto(1);
                                }
                            },
                        },
                        yunniepan: {
                            mark: true,
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseZhunbeiBegin',
                            },
                            forced: true,
                            content() {
                                if (player.maxHp < 9) {
                                    player.gainMaxHp();
                                }
                                player.recover();
                            },
                            ai: {
                                threaten: 9,
                            },
                            group: ['yunniepan_draw', 'yunniepan_maxHp'],
                            subSkill: {
                                draw: {
                                    trigger: {
                                        player: 'phaseDrawBegin',
                                    },
                                    popup: false,
                                    forced: true,
                                    content() {
                                        trigger.num = 0;
                                        var num = Math.min(9, player.maxHp);
                                        var cards = [];
                                        var names = [];
                                        while (cards.length < num) {
                                            var card = get.cardPile2(function (card) {
                                                return !cards.includes(card) && !names.includes(card.name);
                                            });
                                            if (card) {
                                                cards.push(card);
                                                names.push(card.name);
                                            } else break;
                                        }
                                        if (cards.length) player.gain(cards, 'gain2');
                                    },
                                    ai: {
                                        threaten: 1.5,
                                    },
                                },
                                maxHp: {
                                    trigger: {
                                        player: 'gainMaxHpBefore',
                                    },
                                    popup: false,
                                    forced: true,
                                    filter(event, player) {
                                        return player.maxHp >= 9;
                                    },
                                    content() {
                                        trigger.cancel();
                                    },
                                    ai: {
                                        threaten: 1.5,
                                    },
                                },
                            },
                        },
                        yunqixing: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseDrawAfter',
                            },
                            forced: true,
                            content() {
                                'step 0';
                                event.num = Math.min(7, game.countPlayer());
                                ('step 1');
                                if (event.num > 0) {
                                    event.num--;
                                    var card = get.cards()[0];
                                    event.card = card;
                                    player.showCards(card);
                                }
                                ('step 2');
                                if (get.type2(card) == 'trick') {
                                    player.gain(card, 'gain2');
                                    if (player.hasUseTarget(card)) {
                                        player.chooseUseTarget(card);
                                    }
                                } else {
                                    game.cardsDiscard(card);
                                }
                                ('step 3');
                                if (event.num > 0) {
                                    event.goto(1);
                                }
                            },
                            ai: {
                                order: 7,
                                result: {
                                    player: 1,
                                },
                                threaten: 1.5,
                            },
                        },
                        yunranghan: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'damageEnd',
                            },
                            forced: true,
                            filter(event, player) {
                                return true;
                            },
                            content() {
                                player.addTempSkill('yunranghan_damage');
                            },
                            group: ['yunranghan_qixing'],
                            subSkill: {
                                qixing: {
                                    trigger: {
                                        global: 'phaseUseEnd',
                                    },
                                    forced: true,
                                    popup: false,
                                    filter(event, player) {
                                        return event.player != player && (player.hasSkill('yunranghan_damage') || player.hasSkill('yunranghan_cangji'));
                                    },
                                    content() {
                                        player.useSkill('yunqixing');
                                    },
                                },
                                damage: {
                                    trigger: {
                                        player: 'damageBefore',
                                    },
                                    mark: true,
                                    marktext: '禳汉',
                                    forced: true,
                                    charlotte: true,
                                    init(player) {
                                        game.log(player, '获得了', '【免疫】');
                                    },
                                    content() {
                                        trigger.cancel();
                                    },
                                    ai: {
                                        nofire: true,
                                        nothunder: true,
                                        nodamage: true,
                                        effect: {
                                            target(card, player, target, current) {
                                                if (get.tag(card, 'damage')) return 'zeroplayertarget';
                                            },
                                        },
                                    },
                                    intro: {
                                        name: '禳汉',
                                        content: '防止一切伤害',
                                    },
                                },
                            },
                        },
                        yunranghan_cangji: {
                            charlotte: true,
                        },
                        yuncangji: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            filterTarget(card, player, target) {
                                if (target == player) return false;
                                var stat = player.getStat('skill').yuncangji_targets;
                                return !stat || !stat.includes(target);
                            },
                            filter(event, player) {
                                return player.countCards('h') > 0 && game.hasPlayer((current) => lib.skill.yuncangji.filterTarget(null, player, current));
                            },
                            lose: false,
                            delay: false,
                            filterCard: true,
                            content() {
                                target.gain(cards, player, 'giveAuto').gaintag.add('yuncangji');
                                target.addTempSkill('yunjinnang', { player: 'cangji' });
                                var stat = player.getStat('skill');
                                if (!stat.yuncangji_targets) stat.yuncangji_targets = [];
                                stat.yuncangji_targets.push(target);
                            },
                            check(card) {
                                return 6 - get.value(card);
                            },
                            ai: {
                                order: 3,
                                result: {
                                    target(player, target) {
                                        if (get.attitude(player, target) > 0 && target.hasSkillTag('nogain')) return 0;
                                        return 1;
                                    },
                                },
                            },
                        },
                        yunjinnang: {
                            mark: true,
                            marktext: '锦囊',
                            intro: {
                                name: '藏计',
                                content: '快打开丞相给的【锦囊】',
                            },
                            enable: 'phaseUse',
                            audio: 'yuncangji',
                            filter(event, player) {
                                return player.getCards('h', function (card) {
                                    return card.hasGaintag('yuncangji');
                                }).length;
                                var hs = player.getCards('h');
                                if (!hs.length) return false;
                                for (var i = 0; i < hs.length; i++) {
                                    var mod2 = game.checkMod(hs[i], player, 'unchanged', 'cardEnabled2', player);
                                    if (mod2 === false) return false;
                                }
                                return true;
                            },
                            chooseButton: {
                                dialog(player) {
                                    var list = [];
                                    for (var i = 0; i < lib.inpile.length; i++) {
                                        if (get.type(lib.inpile[i]) == 'trick') list.push(['锦囊', '', lib.inpile[i]]);
                                    }
                                    return ui.create.dialog(get.translation('yunjinnang'), [list, 'vcard']);
                                },
                                filter(button, player) {
                                    return lib.filter.filterCard(
                                        {
                                            name: button.link[2],
                                        },
                                        player,
                                        _status.event.parent,
                                    );
                                },
                                check(button) {
                                    var player = _status.event.player;
                                    var recover = 0,
                                        lose = 1,
                                        players = game.filterPlayer();
                                    for (var i = 0; i < players.length; i++) {
                                        if (players[i].hp == 1 && get.damageEffect(players[i], player, player) > 0 && !players[i].hasSha()) {
                                            return button.link[2] == 'juedou' ? 2 : -1;
                                        }
                                        if (!players[i].isOut()) {
                                            if (players[i].hp < players[i].maxHp) {
                                                if (get.attitude(player, players[i]) > 0) {
                                                    if (players[i].hp < 2) {
                                                        lose--;
                                                        recover += 0.5;
                                                    }
                                                    lose--;
                                                    recover++;
                                                } else if (get.attitude(player, players[i]) < 0) {
                                                    if (players[i].hp < 2) {
                                                        lose++;
                                                        recover -= 0.5;
                                                    }
                                                    lose++;
                                                    recover--;
                                                }
                                            } else {
                                                if (get.attitude(player, players[i]) > 0) {
                                                    lose--;
                                                } else if (get.attitude(player, players[i]) < 0) {
                                                    lose++;
                                                }
                                            }
                                        }
                                    }
                                    if (lose > recover && lose > 0) return button.link[2] == 'nanman' ? 1 : -1;
                                    if (lose < recover && recover > 0) return button.link[2] == 'taoyuan' ? 1 : -1;
                                    return button.link[2] == 'wuzhong' ? 1 : -1;
                                },
                                backup(links, player) {
                                    return {
                                        filterCard(card, player) {
                                            return card.hasGaintag('yuncangji');
                                        },
                                        selectCard: 1,
                                        position: 'h',
                                        audio: 'yuncangji',
                                        popname: true,
                                        precontent() {
                                            game.countPlayer(function (current) {
                                                if (current != player && current.hasSkill('yunranghan')) {
                                                    current.addTempSkill('yunranghan_cangji');
                                                }
                                            });
                                        },
                                        viewAs: {
                                            name: links[0][2],
                                        },
                                    };
                                },
                                prompt(links, player) {
                                    return '打开〖锦囊〗使用一张' + get.translation(links[0][2]) + '';
                                },
                            },
                            ai: {
                                order: 7,
                                result: {
                                    player(player) {
                                        var allshown = true,
                                            players = game.filterPlayer();
                                        for (var i = 0; i < players.length; i++) {
                                            if (players[i].ai.shown == 0) {
                                                allshown = false;
                                            }
                                            if (players[i] != player && players[i].countCards('h') && get.attitude(player, players[i]) > 0) {
                                                return 1;
                                            }
                                        }
                                        if (allshown) return 1;
                                        return 0.5;
                                    },
                                },
                            },
                            group: ['yunjinnang_lose'],
                            subSkill: {
                                lose: {
                                    trigger: {
                                        player: 'loseEnd',
                                    },
                                    forced: true,
                                    popup: false,
                                    silent: true,
                                    filter(event, player, card) {
                                        return (
                                            player.getCards('h', function (card) {
                                                return card.hasGaintag('yuncangji');
                                            }).length == 0
                                        );
                                    },
                                    content() {
                                        event.trigger('cangji');
                                    },
                                },
                            },
                        },
                        yunyingshi: {
                            audio: 'ext:云将/audio:2',
                            derivation: ['yunlanggu'],
                            juexingji: true,
                            trigger: {
                                player: 'phaseZhunbeiBegin',
                            },
                            forced: true,
                            filter(event, player) {
                                return player.getExpansions('yunrenjie').length >= game.players.length;
                            },
                            content() {
                                'step 0';
                                player.maxHp = 5;
                                player.update();
                                player.awakenSkill(event.name);
                                player.addSkill('yunlanggu');
                                player.addTempSkill('yunyingshi_draw');
                                ('step 1');
                                player.say(['现在是时候了!', '忍无可忍,无需再忍!'].randomGet());
                                player.gain(player.getExpansions('yunrenjie'), 'gain2', 'fromStorage');
                                ('step 2');
                                player.removeSkill('yunrenjie');
                                if (player.name1 == 'yunsimayi') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/掌棋待机a.jpg');
                                }
                                if (player.name1 == 'yunsimayi') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/贪狼噬日a.jpg');
                                }
                            },
                        },
                        yunyingshi_draw: {
                            trigger: {
                                player: 'phaseDrawBefore',
                            },
                            forced: true,
                            popup: false,
                            content() {
                                trigger.cancel();
                            },
                        },
                        yunrenjie: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: ['damageEnd'],
                            },
                            forced: true,
                            filter(event, player) {
                                return player.getExpansions('yunrenjie').length < game.players.length;
                            },
                            content() {
                                'step 0';
                                player.draw();
                                player.say(['还不到时候!还不到时候!', '筹权谋变,静候天时'].randomGet());
                                ('step 1');
                                if (player.countCards('he') < 1) {
                                    event.finish();
                                } else
                                    player
                                        .chooseCard('he', true, '选择一张牌置于' + get.translation(player) + '的武将牌上作为「忍」')
                                        .set('ai', function (card) {
                                            var player = _status.event.player;
                                            if (player.hasUseTarget(card) && !player.hasValueTarget(card)) return 0;
                                            if (['wuxie'].includes(card.name)) return 2 + Math.random();
                                            if (['sha'].includes(card.name)) return 2 + Math.random();
                                            return Math.random();
                                        })
                                        .set('complexCard', true);
                                ('step 2');
                                game.log(player, '选择了', result.cards);
                                player.addToExpansion(result.cards, player, 'give').gaintag.add('yunrenjie');
                            },
                            intro: {
                                content: 'expansion',
                                markcount: 'expansion',
                            },
                            onremove(player, skill) {
                                var cards = player.getExpansions(skill);
                                if (cards.length) player.loseToDiscardpile(cards);
                            },
                            ai: {
                                threaten: 3,
                                combo: 'yunyingshi',
                            },
                            group: ['yunrenjie_dying'],
                            subSkill: {
                                dying: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        player: ['dying'],
                                    },
                                    forced: true,
                                    content() {
                                        player.gain(player.getExpansions('yunrenjie'), 'gain2', 'fromStorage');
                                    },
                                },
                            },
                        },
                        yunlanggu: {
                            audio: 'ext:云将/audio:2',
                            mod: {
                                targetInRange(card, player) {
                                    if (player.hp >= 1) return true;
                                },
                                cardUsable(card, player) {
                                    if (player.hp >= 2) return Infinity;
                                },
                            },
                            trigger: {
                                player: 'useCard2',
                            },
                            forced: true,
                            filter(event, player) {
                                if (player.hp < 3) return false;
                                var card = event.card;
                                var info = get.info(card);
                                if (info.allowMultiple == false) return false;
                                if (event.targets && !info.multitarget) {
                                    if (
                                        game.hasPlayer(function (current) {
                                            return !event.targets.includes(current) && lib.filter.targetEnabled2(card, player, current);
                                        })
                                    ) {
                                        return true;
                                    }
                                }
                                return false;
                            },
                            content() {
                                'step 0';
                                var prompt2 = '是否为' + get.translation(trigger.card) + '增加一个目标';
                                player
                                    .chooseTarget(get.prompt('yunlanggu'), function (card, player, target) {
                                        var player = _status.event.player;
                                        return !_status.event.targets.includes(target) && lib.filter.targetEnabled2(_status.event.card, player, target);
                                    })
                                    .set('prompt2', prompt2)
                                    .set('ai', function (target) {
                                        var trigger = _status.event.getTrigger();
                                        var player = _status.event.player;
                                        if (trigger.card.name == 'juedou') return false;
                                        return get.effect(target, trigger.card, player, player);
                                    })
                                    .set('card', trigger.card)
                                    .set('targets', trigger.targets);
                                ('step 1');
                                if (result.bool) {
                                    if (!event.isMine()) game.delayx();
                                    event.targets = result.targets;
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                if (event.targets) {
                                    trigger.targets.addArray(event.targets);
                                }
                            },
                            group: ['yunlanggu_draw', 'yunlanggu_jieshu'],
                            subSkill: {
                                draw: {
                                    audio: 'ext:云将/audio:1',
                                    trigger: {
                                        player: 'phaseDrawBegin',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return !event.numFixed && player.maxHp - player.hp >= 2;
                                    },
                                    content() {
                                        trigger.num++;
                                    },
                                    ai: {
                                        threaten: 1.5,
                                    },
                                    mod: {
                                        maxHandcardBase(player, num) {
                                            if (player.maxHp - player.hp >= 1) return player.maxHp;
                                        },
                                    },
                                },
                                jieshu: {
                                    audio: 'ext:云将/audio:1',
                                    trigger: {
                                        player: 'phaseJieshuBegin',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return !event.numFixed && player.maxHp - player.hp >= 3;
                                    },
                                    content() {
                                        'step 0';
                                        player.chooseToDiscard('是否弃置牌？', [1, Infinity], 'h').set('ai', function (card) {
                                            return 6 - get.value(card);
                                        });
                                        ('step 1');
                                        player.draw(player.maxHp - player.countCards('h'));
                                    },
                                    ai: {
                                        effect: {
                                            target(card, player, target) {
                                                if (get.tag(card, 'damage')) {
                                                    if (!target.hasFriend()) return;
                                                    if (target.hp > 3) return [0, 0.5];
                                                }
                                                if (get.tag(card, 'recover') && player.hp >= player.maxHp - 2) return 'zeroplayertarget';
                                            },
                                        },
                                    },
                                },
                            },
                        },
                        yunzhiheng: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'useCard2',
                            },
                            forced: true,
                            usable: 3,
                            filter(event, player) {
                                var type = get.type(event.card);
                                return type == 'trick';
                            },
                            content() {
                                'step 0';
                                var goon = false;
                                var info = get.info(trigger.card);
                                if (trigger.targets && !info.multitarget) {
                                    var players = game.filterPlayer();
                                    for (var i = 0; i < players.length; i++) {
                                        if (lib.filter.targetEnabled2(trigger.card, player, players[i]) && !trigger.targets.includes(players[i])) {
                                            goon = true;
                                            break;
                                        }
                                    }
                                }
                                if (goon) {
                                    player
                                        .chooseTarget([1, Infinity], '制衡:是否额外指定任意名' + get.translation(trigger.card) + '的目标？', function (card, player, target) {
                                            var trigger = _status.event;
                                            if (trigger.targets.includes(target)) return false;
                                            return lib.filter.targetEnabled2(trigger.card, _status.event.player, target);
                                        })
                                        .set('ai', function (target) {
                                            var trigger = _status.event.getTrigger();
                                            var player = _status.event.player;
                                            if (trigger.card.name == 'juedou') return false;
                                            return get.effect(target, trigger.card, player, player);
                                        })
                                        .set('card', trigger.card)
                                        .set('targets', trigger.targets);
                                } else {
                                    if (!info.multitarget && trigger.targets && trigger.targets.length > 1) {
                                        event.goto(3);
                                    }
                                }
                                ('step 1');
                                if (result.bool) {
                                    if (!event.isMine()) event.targets = result.targets;
                                } else {
                                    player.getStat('triggerSkill').yunzhiheng--;
                                    event.finish();
                                }
                                ('step 2');
                                if (event.targets) {
                                    trigger.targets.addArray(event.targets);
                                }
                                event.finish();
                                ('step 3');
                                player
                                    .chooseTarget([1, Infinity], '制衡:是否减少任意名名' + get.translation(trigger.card) + '的目标？', function (card, player, target) {
                                        return _status.event.targets.includes(target);
                                    })
                                    .set('ai', function (target) {
                                        var player = _status.event.player;
                                        var trigger = _status.event.getTrigger();
                                        if (trigger.card.name == 'wugu' || trigger.card.name == 'taoyuan') {
                                            return get.attitude(player, target) < 0;
                                        }
                                        if (trigger.card.name == 'nanman' || trigger.card.name == 'wanjian') {
                                            return get.attitude(player, target) > 0;
                                        } else {
                                            return -get.effect(target, trigger.card, trigger.player, _status.event.player);
                                        }
                                    })
                                    .set('targets', trigger.targets);
                                ('step 4');
                                if (result.bool) {
                                    event.targets = result.targets;
                                    if (event.isMine()) {
                                        event.finish();
                                    }
                                    for (var i = 0; i < result.targets.length; i++) {
                                        trigger.targets.remove(result.targets[i]);
                                    }
                                } else {
                                    player.getStat('triggerSkill').yunzhiheng--;
                                    event.finish();
                                }
                                ('step 5');
                            },
                            ai: {
                                effect: {
                                    player(card, player, target) {
                                        if (get.itemtype(target) == 'player') {
                                            if (card.name == 'nanman' || card.name == 'wanjian' || card.name == 'wugu') {
                                                return [2.5, 2.5];
                                            }
                                            var ssdy = game.countPlayer(function (current) {
                                                return current.getDamagedHp() && get.attitude(current, player) > 0;
                                            });
                                            if (ssdy > 0) {
                                                if (card.name == 'taoyuan') {
                                                    return [2.5, 2.5];
                                                }
                                            }
                                        }
                                    },
                                },
                            },
                        },
                        yunhuju: {
                            trigger: {
                                global: 'gameStart',
                            },
                            forced: true,
                            audio: 'ext:云将/audio:2',
                            filter(event, player) {
                                return player.identity !== 'zhu';
                            },
                            content() {
                                player.gainMaxHp();
                            },
                            group: ['yunhuju_zhunbei'],
                            subSkill: {
                                zhunbei: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        player: 'phaseZhunbeiBegin',
                                    },
                                    forced: true,
                                    content() {
                                        var card = get.cardPile2(function (card) {
                                            return get.type(card) == 'trick';
                                        });
                                        if (!card) {
                                            player.chat('无牌可得了吗');
                                            game.log('牌堆里面已经没有普通锦囊牌了!');
                                            event.finish();
                                            return;
                                        }
                                        player.gain(card, 'gain2');
                                    },
                                },
                            },
                        },
                        yunjianye: {
                            audio: 'ext:云将/audio:2',
                            zhuSkill: true,
                            limited: true,
                            selectTarget: 1,
                            filterTarget(card, player, target) {
                                return target != player;
                            },
                            derivation: 'yunhuangwu',
                            intro: {
                                content: 'limited',
                            },
                            mark: false,
                            init(player) {
                                if (player.hasZhuSkill('yunjianye')) {
                                    player.markSkill('yunjianye');
                                    player.storage.yunjianye = false;
                                }
                            },
                            filter(event, player) {
                                return player.hasZhuSkill('yunjianye');
                                return player.storage.yunjianye == false;
                            },
                            enable: 'phaseUse',
                            content() {
                                'step 0';
                                player.storage.yunjianye = true;
                                player.awakenSkill('yunjianye');
                                game.log(target, '成为了目标');
                                target.gainMaxHp();
                                ('step 1');
                                var card = get.cardPile(function (card) {
                                    return card.name == 'tao';
                                });
                                target.gain(card, 'gain2');
                                ('step 2');
                                if (target.group != 'wu') target.changeGroup('wu');
                                player.addSkill('yunhuangwu');
                                player.say('十万对八百,此战优势在我!');
                                if (player.name1 == 'yunsunquan') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yunsunquana.jpg');
                                }
                                if (player.name1 == 'yunsunquan') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/虎啸龙吟a.jpg');
                                }
                            },
                            ai: {
                                order: 7,
                                result: {
                                    target(player, target) {
                                        if ((target.identity == 'zhong' || target.identity == 'mingzhong') && target.group != 'wu') {
                                            return 10;
                                        }
                                        if (target.identity == 'zhong' || target.identity == 'mingzhong') {
                                            return 5;
                                        }
                                        if (get.attitude(player, target) > 0) {
                                            return 3;
                                        }
                                    },
                                },
                            },
                        },
                        yunhuangwu: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'recoverEnd',
                            },
                            forced: true,
                            usable: 1,
                            filter(event, player) {
                                return player != event.player && event.player.group == 'wu' && event.parent.name != 'yunhuangwu';
                            },
                            content() {
                                'step 0';
                                trigger.player.chooseBool('是否对' + get.translation(player) + '发动【黄武】？', '你令其回复一点体力或摸两张牌').set('ai', function () {
                                    var evt = _status.event;
                                    return get.attitude(evt.player, evt.parent.player) > 0;
                                });
                                ('step 1');
                                if (result.bool) {
                                    trigger.player.line(player, 'green');
                                    player.chooseDrawRecover(2, true, get.prompt('yunhuangwu'));
                                }
                            },
                        },
                        yunlingren: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                player: 'useCardToPlayered',
                            },
                            forced: true,
                            usable: 1,
                            init(player) {
                                player.storage.lingren_characters = [];
                            },
                            group: ['weiyingfengming'],
                            filter(event, player) {
                                if (event.parent.triggeredTargets3.length > 1) return false;
                                if (!['basic', 'trick'].includes(get.type(event.card))) return false;
                                if (!get.tag(event.card, 'damage')) return false;
                                if (event.targets.length == 1 && event.target == player) return false;
                                return true;
                            },
                            content() {
                                'step 0';
                                event.count = 0;
                                player
                                    .chooseTarget(get.prompt('yunlingren'), '是否选择一名目标角色猜测其手牌构成？', function (card, player, target) {
                                        return _status.event.targets.includes(target);
                                    })
                                    .set('ai', function (target) {
                                        return get.attitude(_status.event.player, target) <= 0;
                                    })
                                    .set('targets', trigger.targets);
                                ('step 1');
                                if (result.targets || (event.targets && event.count < event.targets.length)) {
                                    if (!event.targets) event.targets = result.targets;
                                    var target = event.targets[event.count];
                                    event.target = target;
                                    event.choice = {
                                        basic: false,
                                        trick: false,
                                        equip: false,
                                    };
                                    var list = [];
                                    list.push(['', '', 'basic']);
                                    list.push(['', '', 'trick']);
                                    list.push(['', '', 'equip']);
                                    var choice = [];
                                    var rand1 = 0.95;
                                    if (!target.countCards('h', { type: ['basic'] })) rand1 = 0.05;
                                    if (!target.countCards('h')) rand1 = 0;
                                    if (target.hasSkill('yunkunlong')) {
                                        if (target.countCards('h', { type: ['basic'] })) choice.add('basic');
                                    } else {
                                        if (target.countCards('h') > 0 && Math.random() < rand1) choice.add('basic');
                                    }
                                    var rand2 = 0.9;
                                    if (!target.countCards('h', { type: ['trick', 'delay'] })) rand2 = 0.1;
                                    if (!target.countCards('h')) rand2 = 0;
                                    if (target.hasSkill('yunkunlong')) {
                                        if (target.countCards('h', { type: ['trick', 'delay'] })) choice.add('trick');
                                    } else {
                                        if (target.countCards('h') > 0 && Math.random() < rand2) choice.add('trick');
                                    }
                                    var rand3 = 0.75;
                                    if (!target.countCards('h', { type: ['equip'] })) rand3 = 0.25;
                                    if (!target.countCards('h')) rand3 = 0;
                                    if (target.hasSkill('yunkunlong')) {
                                        if (target.countCards('h', { type: ['equip'] })) choice.add('equip');
                                    } else {
                                        if (target.countCards('h') > 0 && Math.random() < rand3) choice.add('equip');
                                    }
                                    var dialog = ui.create.dialog('〖凌人〗', '猜测' + get.translation(target) + '的手牌构成', [list, 'vcard']);
                                    for (var i = 0; i < 3; i++) {
                                        var dd = document.querySelector('[data-card-name="' + list[i][2] + '"]');
                                        dd.style['background-size'] = '100% 100%';
                                        var imgObj = new Image(105, 150);
                                        imgObj.src = 'extension/云将/mark/' + list[i][2] + '.png';
                                        dd.appendChild(imgObj);
                                    }
                                    if (!event.isMine() && choice.length == 0) event.goto(3);
                                    player
                                        .chooseButton([0, 3], dialog)
                                        .set('ai', function (button) {
                                            var select = _status.event.button;
                                            if (select.length == 0) return 0;
                                            return select.includes(button.link[2]);
                                        })
                                        .set('button', choice);
                                } else {
                                    player.getStat('triggerSkill').yunlingren--;
                                    event.finish();
                                }
                                ('step 2');
                                if (result.bool) {
                                    for (var i = 0; i < result.links.length; i++) {
                                        event.choice[result.links[i][2]] = true;
                                    }
                                } else {
                                    player.getStat('triggerSkill').yunlingren--;
                                    event.finish();
                                }
                                ('step 3');
                                var reality = {
                                    basic: false,
                                    trick: false,
                                    equip: false,
                                };
                                var he = event.targets[event.count].getCards('h');
                                for (var i = 0; i < he.length; i++) {
                                    reality[get.type(he[i], 'trick')] = true;
                                }
                                event.num = 0;
                                var tl = ['basic', 'trick', 'equip'];
                                for (var i = 0; i < tl.length; i++) {
                                    if (event.choice[tl[i]] == reality[tl[i]]) event.num++;
                                }
                                ('step 4');
                                player.popup('猜对' + get.cnNumber(event.num) + '项');
                                game.log(player, '猜对了' + get.cnNumber(event.num) + '项');
                                if (event.num > 0) {
                                    player.draw(event.num);
                                }
                                if (event.num > 1) {
                                    if (!target.hasSkill('yunkunlong')) {
                                        target.addSkill('yunkunlong');
                                    } else {
                                        target.addTempSkill('yunlingren_damage');
                                        target.storage.yunlingren = {
                                            card: trigger.card,
                                        };
                                    }
                                    player.say(['将军一副好骨,不如留于此山!', '精兵如炬,困龙难飞'].randomGet());
                                }
                                if (event.num > 2) {
                                    event.trigger('yunlingren');
                                    if (event.num == 3 && player.name1 == 'yuncaoying') {
                                        if (Math.random() > 1 / 2) {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/yuncaoyinga.jpg');
                                        } else {
                                            player.node.avatar.setBackground('yuncaoying', 'character');
                                        }
                                    }
                                    if (event.num == 3 && player.name1 == 'yuncaoying') {
                                        if (Math.random() > 1 / 2) {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/缨飞凤鸣a.jpg');
                                        } else {
                                            player.node.avatar.setBackground('yuncaoying', 'character');
                                        }
                                    }
                                    if (event.num == 3 && player.name1 == 'yuncaoying') {
                                        if (Math.random() > 1 / 2) {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/决阵岐山a.jpg');
                                        } else {
                                            player.node.avatar.setBackground('yuncaoying', 'character');
                                        }
                                    }
                                    if (event.num == 3 && player.name1 == 'yuncaoying') {
                                        if (Math.random() > 1 / 2) {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/剑舞八方a.jpg');
                                        } else {
                                            player.node.avatar.setBackground('yuncaoying', 'character');
                                        }
                                    }
                                    if (event.num == 3 && player.name1 == 'yuncaoying') {
                                        if (Math.random() > 1 / 2) {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/湛蓝玫瑰a.jpg');
                                        } else {
                                            player.node.avatar.setBackground('yuncaoying', 'character');
                                        }
                                    }
                                    if (event.num == 3 && player.name1 == 'yuncaoying') {
                                        if (Math.random() > 1 / 2) {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/惊鸿殊色a.jpg');
                                        } else {
                                            player.node.avatar.setBackground('yuncaoying', 'character');
                                        }
                                    }
                                    if (event.num == 3 && player.name1 == 'yuncaoying') {
                                        if (Math.random() > 1 / 2) {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/无限魅力a.jpg');
                                        } else {
                                            player.node.avatar.setBackground('yuncaoying', 'character');
                                        }
                                    }
                                    if (event.num == 3 && player.name1 == 'yuncaoying') {
                                        if (Math.random() > 1 / 2) {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/夜色撩人a.jpg');
                                        } else {
                                            player.node.avatar.setBackground('yuncaoying', 'character');
                                        }
                                    }
                                    if (event.num == 3 && player.name1 == 'yuncaoying') {
                                        if (Math.random() > 1 / 2) {
                                            player.node.avatar.setBackgroundImage('extension/云将/image/倾城绝世a.jpg');
                                        } else {
                                            player.node.avatar.setBackground('yuncaoying', 'character');
                                        }
                                    }
                                    lib.skill[event.name].clearMark(player);
                                    var list = [];
                                    var list2 = [];
                                    var players = game.players.concat(game.dead);
                                    for (var i = 0; i < players.length; i++) {
                                        list2.add(players[i].name);
                                        list2.add(players[i].name1);
                                        list2.add(players[i].name2);
                                    }
                                    for (var i in lib.character) {
                                        if (lib.character[i][1] != 'wei') continue;
                                        if (lib.character[i][4].includes('boss')) continue;
                                        if (lib.character[i][4].includes('minskin')) continue;
                                        if (player.storage.lingren_characters.includes(i)) continue;
                                        if (list2.includes(i)) continue;
                                        var banlist = ['caocao', 'guojia', 'simayi', 'xiahoudun', 'xuzhu', 'zhangliao', 'zhenji', 're_zhenji', 'caoying', 'yuncaoying', 'xf_tangzi', 'heyan', 'xunyou', 'cuiyan', 'caoanmin', 'xinpi', 'old_caoren', 'jsp_caoren', 'old_jiakui', 're_xiahouyuan', 'xiahouyuan', 'licaiwei', 'dianwei', 'xunyu', 're_caopi', 'xuhuang', 'dengai', 're_dengai', 'ol_dengai', 'caozhi', 'yujin', 'zhangchunhua', 're_zhangchunhua', 'caozhang', 'xin_caozhang', 're_caozhang', 'zhonghui', 'guohuai', 're_guohuai', 'ol_guohuai', 'yujin_yujin', 'xin_yujin', 'xin_zhonghui', 'old_wangyi', 'xin_guohuai', 're_zhonghui', 'liuye', 'tw_caoang', 'kuailiangkuaiyue', 'sunziliufang', 'wangji', 'wenyang', 'xin_xiahoudun', 're_xiahoudun', 'manchong', 'caozhen', 'xin_caozhen', 'old_caozhen', 'chenqun', 'old_chenqun', 're_chenqun', 'hanhaoshihuan', 'caoxiu', 'old_caoxiu', 'guohuanghou', 'ol_bianfuren', 'jsp_guanyu', 'duji', 'zhangchangpu', 'ol_zhangchangpu', 're_niujin', 'sp_xinxianying', 'dufuren', 'wangshuang', 'simazhao', 'sp_simazhao', 'caizhenji', 'guanqiujian', 'wenpin', 'old_majun', 'old_caochun', 'caohong', 'yuanhuan', 're_guanqiujian', 'zhanghe', 'old_guanqiujian', 'zhanggong', 'sp_pangde', 're_zhanggong', 'sp_jiangwei', 'sp_bianfuren', 'sp_huaxin', 'ns_xinxianying', 're_xinxianying', 'diy_liufu', 'old_zhonghui', 'yunsimayi', 'yj_caoang', 'zangba', 'luzhi', 'ol_zhuling', 'yuejin', 'simalang', 'lvqian', 'litong', 'jikang', 're_jikang', 'ns_guanlu', 'ol_xinxianying', 're_lidian', 'tw_caozhao', 'tw_caohong', 're_xuhuang', 'chenlin', 'tw_guohuai', 'sp_jiaxu', 'simafu', 'caoang', 'huaxin', 'ol_huaxin', 'yangxiu', 'caimaozhangyun', 're_duji', 're_xunyu', 'zhaoyǎn', 'bianxi', 'zhaoang', 'ruanhui', 'th_guanqiujian', 'th_caiyang', 'dc_caiyang', 'xiahouxuan', 're_dianwei', 'dingshangwan', 'yinfuren', 'dc_lvkuanglvxiang', 'sp_caosong', 'liwan', 'ns_caoshuang', 'tw_guanqiujian'];
                                        if (banlist.includes(i)) continue;
                                        list.push(i);
                                    }
                                    var name = list.randomGet();
                                    player.storage.lingren_characters.push(name);
                                    var skills = lib.character[name][3];
                                    for (var i = 0; i < skills.length; i++) {
                                        if (!lib.skill[skills[i]].charlotte && !lib.skill[skills[i]].zhuSkill && !lib.skill[skills[i]].juexingji && !lib.skill[skills[i]].dutySkill && !lib.skill[skills[i]].hiddenSkill) {
                                            player.restoreSkill(skills[i]);
                                            player.addTempSkill(skills[i], { player: 'yunlingren' });
                                        }
                                    }
                                    game.log(player, '获得了', [name], '的技能');
                                    event.dialog = ui.create.dialog('<div class="text center">' + get.translation(player) + '发动了【凌人】', [[name], 'character']);
                                }
                                ('step 5');
                                if (event.dialog) {
                                    event.dialog.close();
                                }
                            },
                            clearMark(player) {
                                'step 0';
                                var skills = player.getSkills(null, false);
                                for (var skill of skills) {
                                    var info = get.info(skill);
                                    if (info.mark || info.marktext || info.intro) {
                                        if (!game.expandSkills(player.getOriginalSkills()).includes(skill)) {
                                            player.removeSkill(skill);
                                        }
                                    }
                                }
                                ('step 1');
                                while (player.node.marks.childNodes.length > 1) {
                                    player.node.marks.lastChild.remove();
                                }
                                ('step 2');
                                for (var mark in player.marks) {
                                    var info = get.info(mark);
                                    if (info.equipSkill || info.limited || info.zhuanhuanji || mark == 'renku') continue;
                                    var datatype = Object.prototype.toString.call(player.storage[mark]);
                                    if (datatype.includes('Object')) player.storage[mark] = {};
                                    if (datatype.includes('Array')) player.storage[mark] = [];
                                    if (datatype.includes('Number')) player.storage[mark] = 0;
                                    if (player.countCards('xs')) player.loseToDiscardpile(player.getCards('xs'));
                                    player.unmarkSkill(mark);
                                }
                            },
                            ai: {
                                threaten: 1.5,
                            },
                        },
                        yunlingren_damage: {
                            trigger: {
                                player: 'damageBegin3',
                            },
                            silent: true,
                            popup: false,
                            forced: true,
                            charlotte: true,
                            onremove(player) {
                                delete player.storage.yunlingren;
                            },
                            filter(event, player) {
                                var info = player.storage.yunlingren;
                                return event.card && event.card == info.card;
                            },
                            content() {
                                trigger.num++;
                            },
                        },
                        yunkunlong: {
                            mark: true,
                            marktext: '困龙',
                            forced: true,
                            charlotte: true,
                            intro: {
                                mark(dialog, content, player) {
                                    return game.hasPlayer(function (current) {
                                        if (current.hasSkill('yunlingren')) {
                                            if (current == game.me || current.isUnderControl()) {
                                                dialog.addText('' + get.translation(player) + '的手牌');
                                                var kl = player.getCards('h');
                                                if (kl.length) {
                                                    dialog.addSmall(kl);
                                                } else {
                                                    dialog.addText('无手牌');
                                                }
                                            }
                                        }
                                    });
                                },
                                content(content, player) {
                                    var kl = player.getCards('h');
                                    if (kl.length) {
                                        return get.translation(kl);
                                    } else {
                                        return '无手牌';
                                    }
                                },
                            },
                        },
                        weiyingfengming: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                global: 'dieAfter',
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return player.isTurnedOver() || player.getDamagedHp() || player.isLinked() || player.countCards('j') > 0 || event.player.hasSkill('yunkunlong');
                            },
                            content() {
                                if (player.getDamagedHp()) {
                                    player.recover();
                                }
                                if (player.isLinked()) {
                                    player.link(false);
                                }
                                if (player.isTurnedOver()) {
                                    player.turnOver(false);
                                }
                                if (player.countCards('j') > 0) {
                                    player.discard(player.getCards('j'));
                                }
                                if (trigger.player.hasSkill('yunkunlong')) {
                                    if (player.countDisabled() > 0) {
                                        var list = ['equip1', 'equip2', 'equip3', 'equip4', 'equip5'];
                                        for (var i = 0; i < list.length; i++) {
                                            if (!player.isDisabled(list[i])) list.splice(i--, 1);
                                            else player.enableEquip(list[i]);
                                        }
                                    }
                                    if (
                                        game.countPlayer(function (current) {
                                            return current != player && !current.hasSkill('yunkunlong');
                                        })
                                    ) {
                                        var list = game.filterPlayer(function (target) {
                                            return player != target && !target.hasSkill('yunkunlong');
                                        });
                                        var target = list.randomGet();
                                        player.line(target);
                                        target.addSkill('yunkunlong');
                                    }
                                }
                            },
                            mod: {
                                maxHandcard(player, num) {
                                    if (num < player.maxHp) return (num = player.maxHp);
                                },
                            },
                        },
                        yunyugui: {
                            audio: 'ext:云将/audio:4',
                            trigger: {
                                player: ['phaseZhunbeiBefore', 'phaseJieshuEnd'],
                            },
                            forced: true,
                            popup: false,
                            content() {
                                'step 0';
                                player
                                    .chooseTarget('是否发动【驭鬼】选择一名角色令其获得一个你选择的技能', function (card, player, target) {
                                        return !target.hasSkill('yunyugui_off');
                                    })
                                    .set('ai', function (target) {
                                        var player = _status.event.player;
                                        var att = get.attitude(player, target);
                                        if (target == player) {
                                            return 10 + att;
                                        }
                                        return att;
                                    });
                                ('step 1');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    event.target = target;
                                    player.line(target, 'green');
                                    var list = [];
                                    if (!_status.characterlist) {
                                        if (_status.connectMode) var list = get.character();
                                        else {
                                            var list = [];
                                            for (var i in lib.character) {
                                                if (lib.filter.characterDisabled2(i) || lib.filter.characterDisabled(i)) continue;
                                                list.push(i);
                                            }
                                        }
                                        game.countPlayer2(function (current) {
                                            list.remove(current.name);
                                            list.remove(current.name1);
                                            list.remove(current.name2);
                                            if (current.storage.rehuashen && current.storage.rehuashen.character) list.removeArray(current.storage.rehuashen.character);
                                        });
                                        _status.characterlist = list;
                                    }
                                    _status.characterlist.randomSort();
                                    var chara = [];
                                    var skills = [];
                                    for (var i of _status.characterlist) {
                                        var character = lib.character[i];
                                        var banlist = ['zhaoxiang', 'tw_zhaoxiang', 'shen_luxun', 'zhangji', 're_sunyi', 'yanghuiyu', 're_sunxiu', 'yunsunshangxiang', 'old_lingju', 'yundongbai', 're_baosanniang', 'sp_pangtong', 'shen_jiangwei', 'yanpu', 'yunjiangwei', 'yunzhaoxiang', 'yuncaoying', 'yunzuoci', 'yunlvmeng', 'yunxushi', 'yunsimayi'];
                                        if (banlist.includes(i)) continue;
                                        list.push(i);
                                        if (character && character[3]) {
                                            for (var j of character[3]) {
                                                var info = get.info(j);
                                                if (info && info.limited && !info.zhuSkill) {
                                                    skills.add(j);
                                                    chara.add(i);
                                                    continue;
                                                }
                                            }
                                        }
                                        if (skills.length >= 3) break;
                                    }
                                    if (!skills.length) {
                                        event.finish();
                                        return;
                                    }
                                    event.chara = chara;
                                    event.skills = skills;
                                    player.chooseControl(skills).set('dialog', ['选择令' + get.translation(target) + '获得一个技能', [chara, 'character']]);
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                event.trigger('yugui');
                                player.say(['天法地,地法道,道法自然!', '天法地,地法……蒂法是耶路撒冷!'].randomGet());
                                target.addTempSkill(result.control, { target: 'yugui' });
                                target.addTempSkill('yunyugui_off', 'roundStart');
                                target.restoreSkill(result.control);
                                target.storage.zhuSkill_yunyugui = [result.control];
                                target.setAvatarQueue(target.name, [event.chara[event.skills.indexOf(result.control)]]);
                            },
                            ai: {
                                expose: 0.5,
                                threaten: 0.5,
                            },
                        },
                        yunyugui_off: {
                            charlotte: true,
                        },
                        yunfengjin: {
                            init(player, skill) {
                                player.addSkillBlocker(skill);
                            },
                            onremove(player, skill) {
                                player.removeSkillBlocker(skill);
                            },
                            charlotte: true,
                            skillBlocker(skill, player) {
                                return !lib.skill[skill].charlotte;
                            },
                            mark: true,
                            marktext: '封禁',
                            intro: {
                                content(storage, player, skill) {
                                    var list = player.getSkills(null, false, false).filter(function (i) {
                                        return lib.skill.baiban.skillBlocker(i, player);
                                    });
                                    if (list.length) return '武将技能已失效';
                                    return '无失效技能';
                                },
                            },
                        },
                        yunchengzhi: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'roundStart',
                            },
                            content() {
                                player.storage.chengzhi++;
                                var skills = lib.skill.yunchengzhi_cz.derivation.randomGets(3);
                                player.addAdditionalSkill('yunchengzhi', skills);
                                game.log(player, '获得了以下技能:', '#g' + get.translation(skills));
                            },
                        },
                        yunchengzhi_cz: {
                            derivation: ['reguanxing', 'kongcheng', 'bazhen', 'rehuoji', 'rekanpo', 'cangzhuo', 'nsxingyun', 'nshanlang', 'yuncangji', 'yunranghan', 'yunqixing'],
                        },
                        yunqilin: {
                            derivation: ['yunjueji', 'yunjuezhen'],
                            audio: 'ext:云将/audio:2',
                            juexingji: true,
                            trigger: {
                                player: 'phaseZhunbeiBegin',
                            },
                            forced: true,
                            mark: true,
                            marktext: '麒麟',
                            usable: 1,
                            init(player, skill) {
                                player.storage[skill] = false;
                                player.storage.chengzhi = 0;
                                player.markSkill('chengzhi');
                            },
                            intro: {
                                content(storage, player) {
                                    var cz = player.storage.chengzhi;
                                    var ss = player.storage.yunqilin_damage;
                                    return '<div>【承志】已获得' + cz + '次技能<br>已累计造成和受到' + ss + '点伤害</span></div>';
                                },
                            },
                            filter(event, player) {
                                return player.storage.chengzhi >= 3;
                            },
                            content() {
                                'step 0';
                                player.awakenSkill(event.name);
                                player.storage.chengzhi = 0; //QQQ
                                ('step 1');
                                if (player.storage.yunqilin_damage >= 3) {
                                    player.say(['我,定不负丞相所望!'].randomGet());
                                    player.addSkill('yunjueji');
                                    if (player.name1 == 'yunjiangwei') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/yunjiangweia.jpg');
                                    }
                                    if (player.name1 == 'yunjiangwei') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/麒麟庇佑a.jpg');
                                    }
                                }
                                if (player.storage.yunqilin_damage >= 5) {
                                    player.say(['一切交给我,丞相且安心去吧～'].randomGet());
                                    player.addSkill('yunjuezhen');
                                    if (player.name1 == 'yunjiangwei') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/yunjiangweib.jpg');
                                    }
                                    if (player.name1 == 'yunjiangwei') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/麒麟庇佑b.jpg');
                                    }
                                }
                                ('step 2');
                                if (player.hasSkill('yunjueji') || player.hasSkill('yunjuezhen')) {
                                    player.loseMaxHp();
                                    player.removeAdditionalSkill('yunchengzhi');
                                    player.removeSkill('yunchengzhi');
                                    player.removeSkill('bazhen');
                                    player.storage.yunqilin_damage = 0; //QQQ
                                }
                                if (!player.hasSkill('yunjueji') && !player.hasSkill('yunjuezhen')) {
                                    player.say(['丞相,请告诉我,该怎么做……'].randomGet());
                                    player.draw(3);
                                    player.restoreSkill('yunqilin');
                                    if (player.name1 == 'yunjiangwei') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/yunjiangweic.jpg');
                                    }
                                    if (player.name1 == 'yunjiangwei') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/麒麟庇佑c.jpg');
                                    }
                                }
                            },
                            group: ['yunqilin_damage'],
                            subSkill: {
                                damage: {
                                    init(player) {
                                        player.storage.yunqilin_damage = 0;
                                    },
                                    mark: false,
                                    trigger: {
                                        source: 'damageSource',
                                        player: 'damageAfter',
                                    },
                                    forced: true,
                                    popup: false,
                                    content() {
                                        player.storage.yunqilin_damage += trigger.num;
                                    },
                                },
                            },
                        },
                        yunjuezhen: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: ['chooseToRespondBegin', 'chooseToUseBegin'],
                            },
                            filter(event, player) {
                                if (event.responded) return false;
                                if (!event.filterCard || !event.filterCard({ name: 'shan' }, player, event)) return false;
                                return !player.hasSkill('bagua_skill');
                            },
                            check(event, player) {
                                if (event && (event.ai || event.ai1)) {
                                    var ai = event.ai || event.ai1;
                                    var tmp = _status.event;
                                    _status.event = event;
                                    var result = ai(
                                        {
                                            name: 'shan',
                                        },
                                        _status.event.player,
                                        event,
                                    );
                                    _status.event = tmp;
                                    return result > 0;
                                }
                                return true;
                            },
                            content() {
                                'step 0';
                                trigger.yunjuezhen = true;
                                player.judge('bagua', function (card) {
                                    return get.color(card) == 'red' ? 1.5 : -0.5;
                                }).judge2 = function (result) {
                                    return result.bool;
                                };
                                ('step 1');
                                if (result.judge > 0) {
                                    trigger.untrigger();
                                    trigger.set('responded', true);
                                    trigger.result = {
                                        bool: true,
                                        card: { name: 'shan' },
                                    };
                                }
                            },
                            ai: {
                                respondShan: true,
                            },
                        },
                        yunjueji: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            usable: 1,
                            position: 'he',
                            filterCard(card) {
                                if (ui.selected.cards.length == 0) return true;
                                for (var i = 0; i < ui.selected.cards.length; i++) {
                                    if (get.type(ui.selected.cards[i], 'trick') == get.type(card, 'trick')) return false;
                                }
                                return true;
                            },
                            complexCard: true,
                            selectCard: [1, 3],
                            check(card) {
                                return 10 - get.value(card);
                                var player = _status.event.player;
                                var cards = ui.selected.cards || [];
                                num = cards.length;
                                var val = 0,
                                    eff = 0;
                                for (var i = 0; i < game.players.length; i++) {
                                    if (!game.players[i].isIn() || game.players[i] == player || game.players[i].hasSkillTag('nodamage')) continue;
                                    eff = get.damageEffect(game.players[i], player, player);
                                    if (game.players[i].hp == 1) eff *= 1.5;
                                    val += eff;
                                }
                                if (num == 0) {
                                    if (val <= 0 && !player.isDamaged()) return 0;
                                    return 4 - get.useful(card);
                                } else if (num == 1) {
                                    if (player.isDamaged() && get.recoverEffect(player, player, player) > 0) return 6 - get.useful(card);
                                    if (val > 0) return 4 - get.useful(card);
                                } else if (num == 2) {
                                    if (val > 0) return 3 / Math.min(get.useful(card), 1.5);
                                } else if (num == 3) {
                                    return 2 / Math.min(get.useful(card), 1);
                                }
                                return -1;
                            },
                            content() {
                                'step 0';
                                if (cards.length >= 1 && player.hasSkill('yunjuezhen')) {
                                    var num = Math.max(1, player.maxHp - player.countCards('h'));
                                    player.chooseDrawRecover(num, true);
                                } else {
                                    if (cards.length >= 1) {
                                        player.chooseDrawRecover(true);
                                    }
                                }
                                if (cards.length >= 2) {
                                    game.countPlayer(function (current) {
                                        if (current != player) {
                                            player.line(current, 'green');
                                            current.chooseToDiscard('he', true);
                                        }
                                    });
                                }
                                if (cards.length >= 3) {
                                    game.countPlayer(function (current) {
                                        if (current != player) {
                                            player.line(current, 'green');
                                            current.damage(player);
                                        }
                                    });
                                }
                            },
                            ai: {
                                order: 6,
                                result: {
                                    player: 1,
                                },
                            },
                        },
                        yunchuandao: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'phaseDiscardBefore',
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return event.player != player && event.target != player && event.player.isIn() && event.player.countCards('he') && event.player.isDamaged();
                            },
                            content() {
                                'step 0';
                                trigger.player.chooseCardTarget({
                                    filterCard: true,
                                    selectCard: 1,
                                    usable: 1,
                                    position: 'he',
                                    filterTarget(card, player, target) {
                                        return target != trigger.player && target.hasSkill('yunchuandao');
                                    },
                                    ai1(card) {
                                        return 9 - get.value(card);
                                    },
                                    ai2(target) {
                                        return get.attitude(_status.event.player, target) > 0;
                                    },
                                    prompt: '是否将一张牌交给一名拥有【传道】的角色？',
                                });
                                ('step 1');
                                if (result.bool && result.targets[0] == player) {
                                    player.gain(result.cards, trigger.player, 'giveAuto');
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                player.chooseBool('是否令' + get.translation(trigger.player) + '回复一点体力？').set('ai', function () {
                                    if (get.attitude(trigger.player, player) <= 0) return false;
                                    return get.attitude(trigger.player, player) > 0;
                                });
                                ('step 3');
                                if (result.bool) {
                                    trigger.player.recover();
                                }
                            },
                            ai: {
                                effect: {
                                    target(card) {
                                        if (card.name == 'tiesuo') return 'zeroplayertarget';
                                    },
                                },
                            },
                            group: ['daxianliangshi', 'yunchuandao_zhunbei'],
                            subSkill: {
                                zhunbei: {
                                    trigger: {
                                        global: 'phaseZhunbeiBegin',
                                    },
                                    forced: true,
                                    popup: false,
                                    filter(event, player) {
                                        return player != event.player;
                                    },
                                    content() {
                                        trigger.player.link(true);
                                    },
                                },
                            },
                        },
                        daxianliangshi: {
                            trigger: {
                                player: ['linkBefore', 'enterGame'],
                                global: ['phaseBefore', 'useCardAfter'],
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                if (event.name == 'link') return player.isLinked();
                                return (event.name != 'phase' || game.phaseNumber == 0) && !player.isLinked();
                            },
                            content() {
                                if (trigger.name != 'link') player.link(true);
                                else trigger.cancel();
                            },
                        },
                        yundunshu: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'damageBegin3',
                            },
                            forced: true,
                            filter(event, player) {
                                return event.nature == 'thunder';
                            },
                            content() {
                                trigger.cancel();
                                var num = trigger.num;
                                player.chooseDrawRecover(num, num, true, get.prompt('yundunshu'));
                            },
                            ai: {
                                nothunder: true,
                                effect: {
                                    target(card, player, target, current) {
                                        if (get.tag(card, 'thunderDamage')) return 'zerotarget';
                                    },
                                },
                            },
                            group: ['yundunshu_fulei'],
                            subSkill: {
                                fulei: {
                                    audio: 'yundunshu',
                                    trigger: {
                                        player: 'phaseZhunbeiBegin',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return !game.hasPlayer(function (current) {
                                            return current.hasJudge('fulei');
                                        });
                                    },
                                    content() {
                                        var list = ['fulei'];
                                        player.addJudge(game.createCard(list.randomGet()));
                                    },
                                },
                            },
                        },
                        yunhuangtian: {
                            audio: 'ext:云将/audio:2',
                            zhuSkill: true,
                            limited: true,
                            selectTarget: 1,
                            filterTarget(card, player, target) {
                                return target != player;
                            },
                            derivation: 'yuntaiping',
                            intro: {
                                content: 'limited',
                            },
                            mark: false,
                            init(player) {
                                if (player.hasZhuSkill('yunhuangtian')) {
                                    player.markSkill('yunhuangtian');
                                    player.storage.yunhuangtian = false;
                                }
                            },
                            filter(event, player) {
                                return player.hasZhuSkill('yunhuangtian');
                                return player.storage.yunhuangtian == false;
                                return !player.hasJudge('fulei');
                            },
                            enable: 'phaseUse',
                            content() {
                                'step 0';
                                player.storage.yunhuangtian = true;
                                player.awakenSkill('yunhuangtian');
                                game.log(target, '成为了目标');
                                ('step 1');
                                target.gainMaxHp();
                                target.changeHujia();
                                if (target.group != 'qun') target.changeGroup('qun');
                                player.addSkill('yuntaiping');
                                if (player.name1 == 'yunzhangjiao') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yunzhangjiaoa.jpg');
                                }
                                if (player.name1 == 'yunzhangjiao') {
                                    if (Math.random() > 1 / 2) {
                                        player.say(['化身为黄吧!', '江江～你也不想黑历史被人知道吧？'].randomGet());
                                        player.node.avatar.setBackgroundImage('extension/云将/image/化身为黄a.jpg');
                                    } else {
                                        player.say(['化身为黄吧!', '江江～你也不想黑历史被人知道吧？'].randomGet());
                                        player.node.avatar.setBackgroundImage('extension/云将/image/化身为黄b.jpg');
                                    }
                                }
                            },
                            ai: {
                                order: 7,
                                result: {
                                    target: 5,
                                },
                            },
                        },
                        yuntaiping: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'damageEnd',
                            },
                            prompt(event, player) {
                                var str = '';
                                str += '是否对' + get.translation(event.player) + '发动【太平】令其摸两张牌？';
                                return str;
                            },
                            check(event, player) {
                                return get.attitude(player, event.player) > 0;
                            },
                            filter(event, player) {
                                return event.nature == 'thunder' && event.player != player && event.player.group == 'qun' && event.player.isAlive();
                            },
                            content() {
                                trigger.player.draw(2);
                            },
                        },
                        yunshichou: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                source: 'damageEnd',
                            },
                            forced: true,
                            popup: false,
                            init(player) {
                                player.storage.shichou = 0;
                            },
                            filter(event, player) {
                                return player.storage.shichou < Math.min(3, player.getDamagedHp()) && event.card && event.card.name == 'sha' && _status.currentPhase == player;
                            },
                            content() {
                                player.storage.shichou++;
                                player.getStat().card.sha = 0;
                            },
                            mod: {
                                globalFrom(from, to, current) {
                                    return current - Math.min(3, from.getDamagedHp());
                                },
                            },
                            ai: {
                                effect: {
                                    target(card, player, target) {
                                        if (get.tag(card, 'recover') && player.hp >= player.maxHp - 1) return 'zeroplayertarget';
                                    },
                                },
                            },
                            group: ['yunshichou_draw', 'yunshichou_jieshu'],
                            subSkill: {
                                draw: {
                                    trigger: {
                                        player: 'phaseDrawBegin',
                                    },
                                    audio: 'ext:云将/audio:2',
                                    forced: true,
                                    filter(event, player) {
                                        return player.getDamagedHp();
                                    },
                                    content() {
                                        trigger.num += Math.min(3, player.getDamagedHp());
                                    },
                                },
                                jieshu: {
                                    trigger: {
                                        player: 'phaseJieshuAfter',
                                    },
                                    silent: true,
                                    fixed: true,
                                    forced: true,
                                    popup: false,
                                    charlotte: true,
                                    content() {
                                        player.storage.shichou = 0; //QQQ
                                    },
                                },
                            },
                        },
                        yunjinshi: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'dying',
                            },
                            round: 1,
                            forced: true,
                            filter(event, player) {
                                return player.maxHp > 1 && !player.hasSkill('yunjinshi_off');
                            },
                            content() {
                                'step 0';
                                if (player.hasSkill('fengyunshibian_mc_qun')) {
                                    event.goto(1);
                                } else {
                                    player.loseMaxHp();
                                }
                                ('step 1');
                                player.recover(1 - player.hp);
                                player.addTempSkill('yunjinshi_off', 'roundStart');
                            },
                            ai: {
                                threaten: 1,
                                expose: 0.2,
                            },
                            group: ['yunjinshi_sha'],
                            subSkill: {
                                sha: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        player: 'useCardToPlayered',
                                    },
                                    filter(event, player) {
                                        return event.target != player && event.card.name == 'sha';
                                    },
                                    forced: true,
                                    preHidden: true,
                                    _priority: 99,
                                    content() {
                                        player.say(['长须红袍的是曹操!', '曹贼!奸贼!恶贼!'].randomGet());
                                        if (trigger.target.countCards('h') >= player.countCards('h') || trigger.target.hp >= player.hp) {
                                            trigger.target.addTempSkill('yunjinshi_fengjin', 'shaAfter');
                                        }
                                    },
                                    ai: {
                                        ignoreSkill: true,
                                        jueqing: true,
                                        expose: 0.2,
                                    },
                                },
                                fengjin: {
                                    charlotte: true,
                                    mod: {
                                        cardEnabled2(card, player) {
                                            return false;
                                        },
                                    },
                                    init(player, skill) {
                                        player.addSkillBlocker(skill);
                                    },
                                    onremove(player, skill) {
                                        player.removeSkillBlocker(skill);
                                    },
                                    charlotte: true,
                                    skillBlocker(skill, player) {
                                        return !lib.skill[skill].charlotte;
                                    },
                                    mark: true,
                                    marktext: '锦狮·禁',
                                    intro: {
                                        content(storage, player, skill) {
                                            var list = player.getSkills(null, false, false).filter(function (i) {
                                                return lib.skill.baiban.skillBlocker(i, player);
                                            });
                                            return '武将技能失效,不能使用或打出牌';
                                        },
                                    },
                                },
                            },
                        },
                        yunjinshi_off: {
                            charlotte: true,
                            mark: true,
                            marktext: '锦狮',
                            intro: {
                                name: '锦狮',
                                content: '已发动过【锦狮】',
                            },
                        },
                        yunmoshi: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: ['useCard', 'respond'],
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return !player.getStorage('yunmoshi').includes(event.card.name);
                            },
                            content() {
                                player.markAuto('yunmoshi', [trigger.card.name]);
                            },
                            mark: true,
                            marktext: '默识',
                            intro: {
                                content: '【默识】已记录:$',
                            },
                            group: 'yunmoshi_Auto',
                            subSkill: {
                                Auto: {
                                    trigger: {
                                        global: ['useCard', 'respond'],
                                    },
                                    forced: true,
                                    popup: false,
                                    filter(event, player) {
                                        return player != event.player && player.getStorage('yunmoshi').includes(event.card.name);
                                    },
                                    content() {
                                        'step 0';
                                        var name = trigger.card.name;
                                        player.unmarkAuto('yunmoshi', [name]);
                                        player
                                            .discardPlayerCard(trigger.player, 'he')
                                            .set('ai', function () {
                                                if (trigger.player.countCards('he') && get.attitude(player, trigger.player) < 0) {
                                                    return true;
                                                } else {
                                                    return false;
                                                }
                                            })
                                            .set('prompt', get.prompt2('yunmoshi_Auto'), '弃置其一张牌或取消令自己摸一张牌');
                                        ('step 1');
                                        if (!result.bool) {
                                            player.draw();
                                        }
                                    },
                                },
                            },
                        },
                        yunbeige: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'damageEnd',
                            },
                            usable: 3,
                            check(event, player) {
                                var att1 = get.attitude(player, event.player);
                                var att2 = get.attitude(player, event.source);
                                return att1 >= 0 && att2 < 0;
                            },
                            filter(event, player) {
                                return event.player.isAlive() && event.source && event.source != player;
                            },
                            preHidden: true,
                            prompt(event, player) {
                                var str = '';
                                str += '是否对' + get.translation(event.source) + '发动【悲歌】？';
                                return str;
                            },
                            content() {
                                'step 0';
                                player.judge();
                                ('step 1');
                                if (result.suit == 'spade') {
                                    ui.backgroundMusic.src = 'extension/云将/audio/beige_hei.mp3';
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackground('yuncaiwenji', 'character');
                                    }
                                    trigger.source.addTempSkill('fengyin');
                                }
                                if (result.suit == 'heart') {
                                    ui.backgroundMusic.src = 'extension/云将/audio/beige_hong.mp3';
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/yuncaiwenjia.jpg');
                                    }
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/北国汉音a.jpg');
                                    }
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/琴音袅袅a.jpg');
                                    }
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/一顾倾城a.jpg');
                                    }
                                    trigger.player.recover();
                                }
                                if (result.suit == 'club') {
                                    ui.backgroundMusic.src = 'extension/云将/audio/beige_mei.mp3';
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/yuncaiwenjib.jpg');
                                    }
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/北国汉音b.jpg');
                                    }
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/琴音袅袅b.jpg');
                                    }
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/一顾倾城b.jpg');
                                    }
                                    trigger.source.loseHp();
                                }
                                if (result.suit == 'diamond') {
                                    trigger.player.draw(2);
                                    ui.backgroundMusic.src = 'extension/云将/audio/beige_fang.mp3';
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/yuncaiwenjic.jpg');
                                    }
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/北国汉音c.jpg');
                                    }
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/琴音袅袅c.jpg');
                                    }
                                    if (player.name1 == 'yuncaiwenji') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/一顾倾城c.jpg');
                                    }
                                }
                                ('step 2');
                                player.say(['悲歌可以当泣,远望可以当归', '欲归家无人,欲渡河无船'].randomGet());
                            },
                            ai: {
                                expose: 0.3,
                            },
                        },
                        //破虏代码来源为玄武江湖乾渊龙
                        yunpolu: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'damageEnd',
                                source: 'damageEnd',
                            },
                            usable: 1,
                            forced: true,
                            content() {
                                'step 0';
                                player
                                    .chooseTarget('选择一名角色对其发动【破虏】？', 1, true, function (current) {
                                        return true;
                                    })
                                    .set('ai', function (target) {
                                        if (player.hp != player.maxHp - player.hp) {
                                            var bigger = player.maxHp - player.hp - player.hp;
                                            if (bigger < 0) {
                                                bigger = -bigger;
                                            }
                                            var attitude = get.attitude(player, target);
                                            if (attitude == 0) return 0;
                                            if (bigger <= 1) {
                                                return attitude;
                                            }
                                            if (bigger >= 3) {
                                                if (attitude < 0 && target.countCards('e')) {
                                                    return 4;
                                                }
                                            }
                                            if (attitude > 0) {
                                                if (target.getUseValue({ name: 'sha' }) > 0) {
                                                    return target.getUseValue({ name: 'sha' }) / 1.5;
                                                }
                                                if (target.hp < target.maxHp) {
                                                    return 3;
                                                }
                                            }
                                            return Math.random() * 3 - 1.5;
                                        }
                                        return get.attitude(player, target);
                                    });
                                ('step 1');
                                if (result.targets && result.targets.length) {
                                    event.tar = result.targets[0];
                                    var str = '是否令';
                                    str += get.translation(event.tar);
                                    str += '摸';
                                    str += player.maxHp - player.hp;
                                    str += '张牌并弃置';
                                    str += player.hp;
                                    str += '张牌？否则';
                                    str += get.translation(event.tar);
                                    str += '摸';
                                    str += player.hp;
                                    str += '张牌并弃置';
                                    str += player.maxHp - player.hp;
                                    str += '张牌.';
                                    var tar1 = event.tar;
                                    var x = player.maxHp - player.hp;
                                    var y = player.hp;
                                    player.chooseBool(str).set('ai', function () {
                                        if (x >= y) {
                                            return get.attitude(player, tar1) > 0;
                                        } else {
                                            return get.attitude(player, tar1) <= 0;
                                        }
                                    });
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                if (result.bool) {
                                    event.dwx = player.maxHp - player.hp;
                                    event.dwy = player.hp;
                                } else {
                                    event.dwx = player.hp;
                                    event.dwy = player.maxHp - player.hp;
                                }
                                ('step 3');
                                if (event.dwx > 0) {
                                    event.tar.draw(event.dwx);
                                }
                                ('step 4');
                                if (event.dwy > 0) {
                                    event.tar.chooseToDiscard('he', event.dwy, true);
                                }
                            },
                            ai: {
                                threaten: 1.5,
                                effect: {
                                    target(card, player, target) {
                                        if (get.tag(card, 'recover')) {
                                            if (player.maxHp - player.hp == 1 && player.maxHp > 3) {
                                                return 'zeroplayertarget';
                                            }
                                        }
                                        var num = get.tag(card, 'damage');
                                        if (num > 0) {
                                            if (player.hasSkillTag('jueqing', false, target, true)) return [1, -2];
                                            if (!target.hasFriend()) return;
                                            if (target.hp == target.maxHp) return;
                                            if (target.hp == 1) {
                                                return;
                                            }
                                        }
                                    },
                                },
                            },
                        },
                        yunwulie: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'dieBegin',
                            },
                            forced: true,
                            popup: false,
                            forceDie: true,
                            content() {
                                'step 0';
                                player
                                    .chooseTarget('是否选择一名其他角色获得【破虏】', function (card, player, target) {
                                        return target != player;
                                    })
                                    .set('ai', function (target) {
                                        return get.attitude(player, target);
                                    });
                                ('step 1');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    game.log(target, '继承了', '【武烈】');
                                    player.awakenSkill('yunwulie');
                                    target.gainMaxHp();
                                    target.addSkill('yunpolu');
                                    if (player.identity == 'zhu' && (target.identity == 'zhong' || target.identity == 'mingzhong')) {
                                        game.broadcastAll(
                                            function (player, target) {
                                                player.identity = target.identity;
                                                if (target.identity == 'zhong') game.zhong = target;
                                                delete target.isZhu;
                                                target.identity = 'zhu';
                                                game.zhu = player;
                                                target.showIdentity();
                                                player.showIdentity();
                                            },
                                            target,
                                            player,
                                        );
                                        event.trigger('zhuUpdate');
                                    }
                                }
                            },
                            mod: {
                                maxHandcard(player, num) {
                                    return num + player.countCards('e');
                                },
                            },
                        },
                        yuntaolue: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                player: ['discardEnd', 'respondEnd', 'useCardEnd'],
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                if (player.countCards('h') >= 1) return false;
                                if (event.cards && event.cards.length) {
                                    for (var i = 0; i < event.cards.length; i++) {
                                        if (event.cards[i].original == 'e') return false;
                                    }
                                }
                                return player.countCards('h') == 0;
                            },
                            content() {
                                'step 0';
                                player.draw();
                                ('step 1');
                                if (Array.isArray(result) && result.length && _status.currentPhase == player) {
                                    var gained = result[0];
                                    if (lib.filter.cardEnabled(gained, target)) {
                                        var next = player.chooseToUse();
                                        next.filterCard = function (card) {
                                            return card == gained;
                                        };
                                        next.prompt = '是否使用' + get.translation(gained) + '？';
                                    } else {
                                        event.finish();
                                    }
                                }
                            },
                            ai: {
                                reverseEquip: true,
                                effect: {
                                    player(card, player, target) {
                                        if (card.name == 'huogong' || card.name == 'tao' || card.name == 'jiu') {
                                            return [1, 0.5];
                                        }
                                    },
                                },
                                noh: true,
                                nodiscard: true,
                            },
                            group: ['yuntaolue_lose'],
                            subSkill: {
                                lose: {
                                    trigger: {
                                        player: ['loseAfter'],
                                        global: ['addJudgeAfter', 'gainAfter', 'loseAsyncAfter', 'addToExpansionAfter'],
                                    },
                                    forced: true,
                                    popup: false,
                                    filter(event, player) {
                                        if (player.countCards('h') >= 1) return false;
                                        if (event.cards && event.cards.length) {
                                            //QQQ
                                            for (var i = 0; i < event.cards.length; i++) {
                                                if (event.cards[i].original == 'e') return false;
                                            }
                                        }
                                        return player.countCards('h') == 0;
                                    },
                                    content() {
                                        if (trigger.name == 'lose' && !['discard', 'useCard', 'respond'].includes(trigger.parent.name)) {
                                            player.draw();
                                        }
                                    },
                                },
                            },
                        },
                        yunqianya: {
                            audio: 'ext:云将/audio:3',
                            trigger: {
                                player: 'phaseDrawBefore',
                            },
                            prompt: '是否发动【谦雅】弃置一张牌跳过摸牌阶段？',
                            content() {
                                'step 0';
                                trigger.cancel();
                                player.chooseToDiscard('h', true).set('ai', function (card) {
                                    return 10 - get.value(card);
                                });
                                ('step 1');
                                if (result.bool) {
                                    if (player.hujia == 0) {
                                        player.changeHujia();
                                    }
                                    player
                                        .chooseTarget('是否令一名其他角色进行一个额外的摸牌阶段', function (card, player, target) {
                                            return player != target;
                                        })
                                        .set('ai', function (target) {
                                            return get.attitude(player, target);
                                        });
                                }
                                ('step 2');
                                if (result.bool) {
                                    player.say(['谦谦君子,温润如玉!'].randomGet());
                                    var target = result.targets[0];
                                    var next = target.phaseDraw();
                                    event.next.remove(next);
                                    trigger.parent.next.push(next);
                                }
                            },
                            ai: {
                                expose: 0.5,
                                noh: true,
                                nogain: 1,
                                effect: {
                                    target(card) {
                                        if (card.name == 'guohe') return [1, 0.5];
                                    },
                                },
                            },
                            group: ['yunqianya_use', 'yunqianya_Discard'],
                            subSkill: {
                                use: {
                                    audio: 'yunqianya',
                                    trigger: {
                                        target: 'useCardToTargeted',
                                    },
                                    popup: false,
                                    filter(event, player) {
                                        if (!event.targets || event.targets.length != 1) return false;
                                        return get.type2(event.card) == 'trick' && event.player != player;
                                    },
                                    prompt: '是否发动【谦雅】弃置一张牌？',
                                    content() {
                                        'step 0';
                                        player.chooseToDiscard('h', true).set('ai', function (card) {
                                            return 10 - get.value(card);
                                        });
                                        ('step 1');
                                        if (result.bool) {
                                            if (player.hujia == 0) {
                                                player.changeHujia();
                                            }
                                            player
                                                .chooseTarget('是否令一名其他角色进行一个额外的出牌阶段', function (card, player, target) {
                                                    return player != target;
                                                })
                                                .set('ai', function (target) {
                                                    var player = _status.event.player;
                                                    var num = target.countCards('h');
                                                    var att = get.attitude(player, target);
                                                    return num * att;
                                                    if (target == trigger.target) {
                                                        return 1;
                                                    }
                                                });
                                        } else {
                                            event.finish();
                                        }
                                        ('step 2');
                                        if (result.bool) {
                                            player.say(['谦虚温谨,不以才地矜物'].randomGet());
                                            var target = result.targets[0];
                                            var next = target.phaseUse();
                                            event.next.remove(next);
                                            trigger.parent.next.push(next);
                                        }
                                    },
                                    ai: {
                                        expose: 0.3,
                                    },
                                },
                                Discard: {
                                    audio: 'yunqianya',
                                    trigger: {
                                        player: 'phaseDiscardEnd',
                                    },
                                    popup: false,
                                    prompt: '是否发动【谦雅】弃置一张牌？',
                                    content() {
                                        'step 0';
                                        player.chooseToDiscard('h', true).set('ai', function (card) {
                                            return 10 - get.value(card);
                                        });
                                        ('step 1');
                                        if (result.bool) {
                                            if (player.hujia == 0) {
                                                player.changeHujia();
                                            }
                                            player
                                                .chooseTarget('是否令一名其他角色进行一个额外的弃牌阶段', function (card, player, target) {
                                                    return player != target;
                                                })
                                                .set('ai', function (target) {
                                                    var player = _status.event.player;
                                                    var num = target.countCards('h');
                                                    var att = -get.attitude(player, target);
                                                    return num * att;
                                                });
                                        } else {
                                            event.finish();
                                        }
                                        ('step 2');
                                        if (result.bool) {
                                            player.say(['虚则欹,中则正,满则震'].randomGet());
                                            var target = result.targets[0];
                                            var next = target.phaseDiscard();
                                            event.next.remove(next);
                                            trigger.parent.next.push(next);
                                        }
                                    },
                                    ai: {
                                        expose: 0.3,
                                    },
                                },
                            },
                        },
                        yunguose: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseDiscardEnd',
                            },
                            forced: true,
                            filter(event, player) {
                                return event.cards && event.cards.length && !player.hasSkill('fengyunshibian_dxq_wu');
                            },
                            content() {
                                'step 0';
                                var num = Math.min(3, trigger.cards.length);
                                player
                                    .chooseTarget('弃置一名角色至多' + num + '张牌', get.prompt('yunguose'), function (card, player, target) {
                                        return target != player && target.countCards('he') > 0;
                                    })
                                    .set('ai', function (target) {
                                        player = get.player();
                                        return -get.attitude(player, target);
                                    });
                                ('step 1');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    var num = Math.min(3, trigger.cards.length);
                                    player.discardPlayerCard(target, 'he', [1, num], true);
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                if (player.hasSkill('fengyunshibian_dxq_qun')) {
                                    var num = Math.min(3, trigger.cards.length);
                                    player
                                        .chooseTarget('弃置一名角色至多' + num + '张牌', get.prompt('yunguose'), function (card, player, target) {
                                            return target != player && target.countCards('he') > 0;
                                        })
                                        .set('ai', function (target) {
                                            player = get.player();
                                            return -get.attitude(player, target);
                                        });
                                } else {
                                    event.finish();
                                }
                                ('step 3');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    var num = Math.min(3, trigger.cards.length);
                                    player.discardPlayerCard(target, 'he', [1, num], true);
                                }
                            },
                            ai: {
                                expose: 0.3,
                            },
                            group: ['yunguose_Use', 'yunguose_Discard', 'yunguose_shibian_wu'],
                            subSkill: {
                                Use: {
                                    trigger: {
                                        player: 'useCard',
                                    },
                                    forced: true,
                                    popup: false,
                                    init(player) {
                                        player.storage.yunguose_Use = 0;
                                        player.markSkill('yunguose_Use');
                                    },
                                    filter(event, player) {
                                        if (!player.isPhaseUsing()) return false;
                                        return player.storage.yunguose_Use < 3;
                                    },
                                    content() {
                                        player.storage.yunguose_Use++;
                                        player.markSkill('yunguose_Use');
                                    },
                                },
                                Discard: {
                                    trigger: {
                                        player: 'phaseDiscardBefore',
                                    },
                                    forced: true,
                                    popup: false,
                                    filter(event, player) {
                                        return player.storage.yunguose_Use > 0;
                                    },
                                    content() {
                                        'step 0';
                                        var num = player.storage.yunguose_Use;
                                        player.draw(num);
                                        ('step 1');
                                        player.storage.yunguose_Use = 0; //QQQ
                                    },
                                },
                                shibian_wu: {
                                    trigger: {
                                        player: 'phaseDiscardEnd',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return event.cards && event.cards.length && player.hasSkill('fengyunshibian_dxq_wu');
                                    },
                                    content() {
                                        'step 0';
                                        var num = Math.min(3, trigger.cards.length);
                                        player
                                            .chooseTarget('弃置一名角色至多' + num + '张牌', get.prompt('yunguose'), function (card, player, target) {
                                                return target != player && target.countCards('he') > 0;
                                            })
                                            .set('ai', function (target) {
                                                player = get.player();
                                                return -get.attitude(player, target);
                                            });
                                        ('step 1');
                                        if (result.bool) {
                                            var target = result.targets[0];
                                            target.disableEquip('equip1');
                                            target.disableEquip('equip2');
                                            target.disableEquip('equip3');
                                            target.disableEquip('equip4');
                                            target.disableEquip('equip5');
                                            var num = Math.min(3, trigger.cards.length);
                                            player.discardPlayerCard(target, 'he', [1, num], true);
                                            target.addTempSkill('yunguose_equip', { player: 'phaseAfter' });
                                        }
                                    },
                                },
                                equip: {
                                    mark: true,
                                    marktext: '国色',
                                    intro: {
                                        name: '国色',
                                        content: '你累了,请休息吧',
                                    },
                                    trigger: {
                                        player: 'phaseJieshuBegin',
                                    },
                                    silent: true,
                                    forced: true,
                                    popup: false,
                                    charlotte: true,
                                    content() {
                                        player.enableEquip(1);
                                        player.enableEquip(2);
                                        player.enableEquip(3);
                                        player.enableEquip(4);
                                        player.enableEquip(5);
                                    },
                                },
                            },
                        },
                        yuntianxiang: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseZhunbeiBegin',
                            },
                            forced: true,
                            filter(event, player) {
                                return game.hasPlayer(function (current) {
                                    return current != player && !current.hasSkill('yuntianxiang_damage');
                                });
                            },
                            content() {
                                'step 0';
                                if (!player.hasSkill('fengyunshibian_dxq_wu')) {
                                    event.trigger('tianxiang');
                                }
                                player
                                    .chooseTarget('令一名角色获得〖天香〗标记', function (card, player, target) {
                                        return player != target && !target.hasSkill('yuntianxiang_damage');
                                    })
                                    .set('ai', function (target) {
                                        if (get.attitude(player, target) > 0) return 0.5;
                                        if (get.attitude(player, target) <= 0) return target.countCards('h');
                                    });
                                ('step 1');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    player.line(target, 'green');
                                    game.log(player, '选择了', target);
                                    if (player.hasSkill('fengyunshibian_dxq_wu')) {
                                        target.addSkill('yuntianxiang_damage');
                                    } else {
                                        target.addTempSkill('yuntianxiang_damage', 'tianxiang');
                                    }
                                }
                            },
                        },
                        yuntianxiang_damage: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                source: 'damageEnd',
                                player: 'damageEnd',
                            },
                            usable: 1,
                            forced: true,
                            popup: false,
                            charlotte: true,
                            mark: true,
                            marktext: '天香',
                            intro: {
                                name: '天香',
                                content: '江江要加油!要努力哟!',
                            },
                            content() {
                                game.countPlayer(function (current) {
                                    if (current.hasSkill('yuntianxiang')) {
                                        player.line(current, 'green');
                                        current.draw();
                                    }
                                });
                            },
                        },
                        yunduodi: {
                            trigger: {
                                source: 'damageSource',
                            },
                            forced: true,
                            audio: 'ext:云将/audio:2',
                            content() {
                                'step 0';
                                player.storage.yunduodi++;
                                game.swapSeat(player, player.previous);
                                ('step 1');
                                if (game.players.length > 2) {
                                    player.chooseToDiscard('he', get.prompt('yunduodi', player.next), '请弃置一张牌或不弃置令' + get.translation(player.next) + '翻面').set('ai', function (card) {
                                        if (player.next.hasSkillTag('noturn')) return false;
                                        if (get.attitude(player.next, player) > 0 && !player.next.classList.contains('turnedover')) return 7 - get.value(card);
                                        if (get.attitude(player.next, player) < 0 && player.next.classList.contains('turnedover')) return 7 - get.value(card);
                                        return false;
                                    });
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                if (!result.bool) {
                                    player.next.turnOver();
                                }
                            },
                            ai: {
                                expose: 0.3,
                            },
                            group: ['yunduodi_Use'],
                            subSkill: {
                                Use: {
                                    audio: 'ext:云将/audio:2',
                                    enable: 'phaseUse',
                                    usable: 1,
                                    filterTarget(card, player, target) {
                                        if (player.seatNum % 2 == 1) {
                                            return player != target;
                                        } else {
                                            return player != target && target.countCards('he') > 0;
                                        }
                                    },
                                    prompt(event, player) {
                                        var str = '';
                                        if (event.player.seatNum % 2 == 1) {
                                            str += '选择一名其他角色令其摸' + get.cnNumber(Math.min(3, event.player.seatNum)) + '张牌？';
                                        }
                                        if (event.player.seatNum % 2 != 1) {
                                            str += '选择一名其他角色令其弃置' + get.cnNumber(Math.min(3, event.player.seatNum)) + '张牌？';
                                        }
                                        return str;
                                    },
                                    content() {
                                        player.storage.yunduodi++;
                                        var num = Math.min(3, player.seatNum);
                                        if (player.seatNum % 2 == 1) {
                                            target.draw(num);
                                        } else {
                                            target.chooseToDiscard('he', num, true);
                                        }
                                    },
                                    ai: {
                                        order(item, player) {
                                            var num = player.seatNum;
                                            return num;
                                        },
                                        expose: 1,
                                        result: {
                                            target(player, target) {
                                                var num = player.seatNum;
                                                if (get.attitude(target, player) > 0 && player.seatNum % 2 == 1) return num;
                                                if (get.attitude(target, player) <= 0 && target.countCards('he') > 0 && player.seatNum % 2 != 1) return -num;
                                            },
                                        },
                                    },
                                },
                            },
                        },
                        yunxingshang: {
                            trigger: {
                                global: 'die',
                            },
                            audio: 'ext:云将/audio:2',
                            preHidden: true,
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return event.player != player;
                            },
                            content() {
                                if (player.isDamaged()) {
                                    player.recover();
                                } else {
                                    if (trigger.player.countCards('he') > 0) {
                                        event.togain = trigger.player.getCards('he');
                                        player.gain(event.togain, trigger.player, 'giveAuto');
                                    }
                                }
                            },
                            group: ['weiwangshizi'],
                        },
                        weiwangshizi: {
                            audio: 'yunxingshang',
                            trigger: {
                                global: 'dieBegin',
                            },
                            forced: true,
                            filter(event, player) {
                                return event.player.identity == 'zhu' && (player.identity == 'zhong' || player.identity == 'mingzhong');
                            },
                            logTarget: 'player',
                            content() {
                                'step 0';
                                player.useSkill('yundengwei');
                                ('step 1');
                                game.broadcastAll(
                                    function (player, target) {
                                        target.identity = player.identity;
                                        if (player.identity == 'zhong' || player.identity == 'mingzhong') game.zhong = target;
                                        delete target.isZhu;
                                        player.identity = 'zhu';
                                        game.zhu = player;
                                        player.showIdentity();
                                        target.showIdentity();
                                    },
                                    player,
                                    trigger.player,
                                );
                                event.trigger('zhuUpdate');
                            },
                        },
                        yundengwei: {
                            audio: 'ext:云将/audio:2',
                            derivation: ['yunchengtian', 'yunsongwei'],
                            juexingji: true,
                            trigger: {
                                player: 'phaseJieshuBegin',
                            },
                            forced: true,
                            mark: true,
                            marktext: '登位',
                            init(player) {
                                player.storage.yunduodi = 0;
                                player.storage.dengwei = 0;
                            },
                            intro: {
                                content(storage, player) {
                                    var dd = player.storage.yunduodi;
                                    return '<div>已发动' + dd + '次【夺嫡】</span></div>';
                                },
                            },
                            filter(event, player) {
                                return player.storage.yunduodi >= 5;
                            },
                            content() {
                                'step 0';
                                player.gainMaxHp();
                                player.awakenSkill(event.name);
                                player.removeSkill('yunduodi');
                                player.removeSkill('yunxingshang');
                                player.addSkill('yunchengtian');
                                if (player.identity == 'zhu') {
                                    player.addSkill('yunsongwei');
                                }
                                ('step 1');
                                player.recover();
                                player.storage.yunduodi = 0; //QQQ
                                player.say(['魏文帝!魏文帝!魏文帝!', '孤承天命,四海归心!'].randomGet());
                                if (player.name1 == 'yuncaopi') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/yuncaopia.jpg');
                                }
                                if (player.name1 == 'yuncaopi') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/悠然自乐a.jpg');
                                }
                            },
                        },
                        jiuponzhongzheng: {},
                        yunchengtian: {
                            trigger: {
                                player: 'damageAfter',
                            },
                            audio: 'ext:云将/audio:2',
                            forced: true,
                            content() {
                                'step 0';
                                game.swapSeat(player, player.next);
                                ('step 1');
                                if (game.players.length > 2) {
                                    player.chooseToDiscard('he', get.prompt('yunchengtian', player.previous), '请弃置一张牌或不弃置令' + get.translation(player.previous) + '翻面').set('ai', function (card) {
                                        if (player.previous.hasSkillTag('noturn')) return false;
                                        if (get.attitude(player.previous, player) > 0 && !player.previous.classList.contains('turnedover')) return 7 - get.value(card);
                                        if (get.attitude(player.previous, player) < 0 && player.previous.classList.contains('turnedover')) return 7 - get.value(card);
                                        return false;
                                    });
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                if (!result.bool) {
                                    player.previous.turnOver();
                                }
                            },
                            ai: {
                                maixie: true,
                                skillTagFilter(player, tag, arg) {
                                    if (tag == 'maixie' && player.hp <= 2) return false;
                                },
                                effect: {
                                    target(card, player, target) {
                                        if (get.tag(card, 'damage') && game.players.length > 2) {
                                            if (player.hasSkillTag('jueqing', false, target)) return [1, -2];
                                            if (target.hp >= 3 && get.attitude(player, player.next) <= 0 && !player.next.classList.contains('turnedover')) {
                                                return [0.5, 0.5];
                                            }
                                        }
                                    },
                                },
                            },
                            group: ['yunchengtian_Use'],
                            subSkill: {
                                Use: {
                                    audio: 'ext:云将/audio:2',
                                    enable: 'phaseUse',
                                    usable: 1,
                                    filterTarget(card, player, target) {
                                        if (player.seatNum % 2 == 1) {
                                            return true;
                                        } else {
                                            return target.countCards('he') > 0;
                                        }
                                    },
                                    prompt(event, player) {
                                        var str = '';
                                        if (event.player.seatNum % 2 == 1) {
                                            str += '选择一名角色令其摸' + get.cnNumber(Math.min(3, event.player.seatNum)) + '张牌？';
                                        }
                                        if (event.player.seatNum % 2 != 1) {
                                            str += '选择一名角色令其弃置' + get.cnNumber(Math.min(3, event.player.seatNum)) + '张牌？';
                                        }
                                        return str;
                                    },
                                    content() {
                                        var num = Math.min(3, player.seatNum);
                                        if (player.seatNum % 2 == 1) {
                                            target.draw(num);
                                        } else {
                                            target.chooseToDiscard('he', num, true);
                                        }
                                    },
                                    ai: {
                                        order(item, player) {
                                            var num = player.seatNum;
                                            return num;
                                        },
                                        expose: 1,
                                        result: {
                                            target(player, target) {
                                                var num = player.seatNum;
                                                if (get.attitude(target, player) > 0 && player.seatNum % 2 == 1) return num;
                                                if (get.attitude(target, player) <= 0 && target.countCards('he') > 0 && player.seatNum % 2 != 1) return -num;
                                            },
                                        },
                                    },
                                },
                            },
                        },
                        yunsongwei: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'damageEnd',
                            },
                            forced: true,
                            usable: 1,
                            filter(event, player) {
                                return event.player.isAlive() && player != event.player && event.player.group == 'wei' && event.parent.name != 'yunsongwei' && player.isDamaged();
                            },
                            content() {
                                'step 0';
                                trigger.player.chooseBool('是否对' + get.translation(player) + '发动【颂威】？', '你进行一次判定:若结果为黑色' + get.translation(player) + '回复一点体力').set('ai', function () {
                                    var evt = _status.event;
                                    return get.attitude(evt.player, evt.parent.player) > 0;
                                });
                                ('step 1');
                                if (result.bool) {
                                    trigger.player.judge(function (card) {
                                        return get.color(card) == 'black' ? 1 : -1;
                                    });
                                }
                                ('step 2');
                                if (result.color == 'black') {
                                    trigger.player.line(player, 'green');
                                    player.recover();
                                }
                            },
                        },
                        yunfanghun: {
                            audio: 'ext:云将/audio:5',
                            trigger: {
                                player: 'phaseZhunbeiBegin',
                            },
                            popup: false,
                            check(event, player) {
                                if (player.hp <= 2 && game.roundNumber > 1) return false;
                                return true;
                            },
                            async content(event, trigger, player) {
                                player.removeAdditionalSkill('yunfanghun');
                                if (player.hp > 3) {
                                    if (player.name1 == 'yunzhaoxiang') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/蝶影芳踪a.jpg');
                                    }
                                    if (player.name1 == 'yunzhaoxiang') {
                                        player.node.avatar.setBackgroundImage('extension/云将/image/梅酒芬香a.jpg');
                                    }
                                } else {
                                    player.node.avatar.setBackground('yunzhaoxiang', 'character');
                                }
                                var list = [];
                                for (var i in lib.character) {
                                    if (lib.character[i][1] == 'shu') list.push(i);
                                }
                                list = list.randomGets(player.hp);
                                var skills = [];
                                for (var i of list) {
                                    skills.addArray(lib.character[i][3]);
                                }
                                var skill1 = skills.slice();
                                if (skills.length >= 1) {
                                    const result = await player
                                        .chooseButton(['【芳魂】', [list, 'character'], [skills.map((i) => [i, get.translation(i)]), 'tdnodes']], true, [1, player.hp])
                                        .set('skill1', skill1)
                                        .set('filterButton', (button) => _status.event.skill1.includes(button.link))
                                        .set('ai', () => skills.randomGets(_status.event.player.hp))
                                        .forResult();
                                    if (result.links && result.links.length) {
                                        for (var i of result.links) {
                                            game.log(player, '获得了技能', '#g【' + get.translation(i) + '】');
                                        }
                                        player.addAdditionalSkill('yunfanghun', result.links, true);
                                    }
                                }
                                if (player.countCards('he') <= 0) {
                                    player.loseHp();
                                    player.draw();
                                } else {
                                    const result1 = await player.chooseControl('弃置一张牌并回复一点体力', '失去一点体力并摸一张牌').forResult();
                                    if (result1.control == '弃置一张牌并回复一点体力') {
                                        await player.chooseToDiscard(true, 'he');
                                    } else {
                                        player.loseHp();
                                        player.draw();
                                    }
                                }
                            }, //QQQ
                        },
                        yunyazi: {
                            audio: 'yunmouce',
                            trigger: {
                                player: ['useCardEnd', 'respondEnd'],
                            },
                            forced: true,
                            popup: false,
                            mark: true,
                            marktext: '睚眦',
                            intro: {
                                content: '【睚眦】已记录:$',
                            },
                            filter(event, player) {
                                return get.tag(event.card, 'damage');
                            },
                            content() {
                                if (!player.getStorage('yunyazi').includes(trigger.card.name)) {
                                    player.markAuto('yunyazi', [trigger.card.name]);
                                } else {
                                    player.unmarkAuto('yunyazi', [trigger.card.name]);
                                    event.trigger('mouce');
                                }
                            },
                            group: ['yunyazi_damage', 'yunmouce'],
                            subSkill: {
                                damage: {
                                    audio: 'ext:云将/audio:3',
                                    trigger: {
                                        player: 'damageEnd',
                                    },
                                    forced: true,
                                    popup: false,
                                    logTarget: 'source',
                                    content() {
                                        event.trigger('mouce');
                                        if (trigger.card && trigger.source && trigger.source != player) {
                                            if (!player.getStorage('yunyazi').includes(trigger.card.name)) {
                                                player.markAuto('yunyazi', [trigger.card.name]);
                                            } else {
                                                player.unmarkAuto('yunyazi', [trigger.card.name]);
                                                trigger.source.damage(player);
                                            }
                                        }
                                    },
                                    ai: {
                                        maixie_defend: true,
                                        effect: {
                                            target(card, player, target) {
                                                if (player.hasSkillTag('jueqing', false, target)) return [1, -1];
                                                if (get.tag(card, 'damage') && get.attitude(player, target) < 0) return 1;
                                            },
                                        },
                                    },
                                },
                            },
                        },
                        yunmouce: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'mouce',
                            },
                            forced: true,
                            async content(event, trigger, player) {
                                var card = get.cards(Math.min(5, 2 * player.maxHp - player.hp));
                                game.cardsGotoOrdering(card);
                                const result = await player
                                    .chooseButton(['谋策:获取种类不相同的牌各一张', card], [1, 3])
                                    .set('filterButton', function (button) {
                                        for (var i = 0; i < ui.selected.buttons.length; i++) {
                                            if (get.type2(ui.selected.buttons[i].link) == get.type2(button.link)) return false;
                                        }
                                        return true;
                                    })
                                    .forResult();
                                if (result.links && result.links.length) {
                                    player.gain(result.links, 'log', 'gain2');
                                    if (player.countCards('he')) {
                                        const result1 = await player
                                            .chooseCardTarget({
                                                forced: true,
                                                filterCard: true,
                                                selectCard: 1,
                                                position: 'he',
                                                filterTarget: (card, player, target) => player != target,
                                                ai1: (card) => 4 - get.value(card),
                                                ai2: (target) => get.attitude(_status.event.player, target),
                                                prompt: '请选择要送人的牌',
                                            })
                                            .forResult();
                                        if (result1.bool) {
                                            player.say(['孝直最是知恩图报～', '此獠还需将军出马!'].randomGet());
                                            player.line(result1.targets, 'green');
                                            result1.targets[0].gain(result1.cards, player, 'giveAuto'); //QQQ
                                        }
                                    }
                                }
                            },
                        },
                        yunchangqu: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'useCardToPlayered',
                            },
                            filter(event, player) {
                                if (!get.tag(event.card, 'damage')) return false;
                                return event.target != player && event.target.countDiscardableCards(player, 'he') > 0 && event.targets.length == 1;
                            },
                            check(event, player) {
                                if (get.attitude(player, event.target) <= 0) return true;
                            },
                            logTarget: 'target',
                            preHidden: true,
                            prompt(event, player, target) {
                                var str = '';
                                if (event.target.hasSkill('yunpoqian_mark')) {
                                    str += '是否获得' + get.translation(event.target) + '一张牌？';
                                } else {
                                    str += '是否弃置' + get.translation(event.target) + '一张牌？';
                                }
                                return str;
                            },
                            content() {
                                if (trigger.target.hasSkill('yunpoqian_mark')) {
                                    player.gainPlayerCard(trigger.target, 'he', true);
                                } else {
                                    player.discardPlayerCard(trigger.target, 'he', true);
                                }
                            },
                            mod: {
                                targetInRange(card, player, target) {
                                    if (target.hasSkill('yunpoqian_mark')) {
                                        return true;
                                    }
                                },
                            },
                            ai: {
                                expose: 0.5,
                            },
                        },
                        yunpoqian: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'damageEnd',
                            },
                            forced: true,
                            filter(event, player) {
                                if (event.source && event.source.hasSkill('yunpoqian_mark')) return false;
                                return event.source && event.source != player;
                            },
                            content() {
                                trigger.source.addSkill('yunpoqian_mark');
                            },
                            group: ['yunpoqian_die', 'yunpoqian_draw'],
                            subSkill: {
                                die: {
                                    audio: 'ext:云将/audio:1',
                                    trigger: {
                                        source: 'dieAfter',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return event.player.hasSkill('yunpoqian_mark');
                                    },
                                    content() {
                                        player.say(['今日,取关羽首级者赏黄金千两!', '连破十堑,易如反掌!'].randomGet());
                                        player.phase('nodelay');
                                    },
                                },
                                draw: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        global: 'phaseDrawBegin2',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return event.player != player && event.player.hasSkill('yunpoqian_mark');
                                    },
                                    content() {
                                        'step 0';
                                        var nums = Math.ceil(trigger.num / 2);
                                        player.chooseBool('确认:令' + get.translation(trigger.player) + '少摸' + nums + '张牌;取消:令' + get.translation(trigger.player) + '额外摸' + nums + '张牌').set('ai', function (event, player) {
                                            if (get.attitude(player, trigger.player) <= 0) return true;
                                            if (get.attitude(player, trigger.player) > 0) return false;
                                        });
                                        ('step 1');
                                        if (result.bool) {
                                            if (trigger.num <= 1) {
                                                trigger.cancel();
                                                player.line(trigger.player, 'green');
                                                event.finish();
                                            }
                                            if (trigger.num > 1) {
                                                player.line(trigger.player, 'green');
                                                var nump = Math.ceil(trigger.num / 2);
                                                trigger.num -= Math.ceil(trigger.num / 2);
                                            }
                                            trigger.player.removeSkill('yunpoqian_mark');
                                        } else {
                                            if (trigger.num <= 1) {
                                                trigger.num++;
                                                player.line(trigger.player, 'green');
                                                event.finish();
                                            }
                                            if (trigger.num > 1) {
                                                player.line(trigger.player, 'green');
                                                var nump = Math.ceil(trigger.num / 2);
                                                trigger.num += Math.ceil(trigger.num / 2);
                                            }
                                            trigger.player.removeSkill('yunpoqian_mark');
                                        }
                                    },
                                    ai: {
                                        expose: 0.3,
                                    },
                                },
                            },
                        },
                        yunpoqian_mark: {
                            forced: true,
                            forceDie: true,
                            charlotte: true,
                            mark: true,
                            marktext: '堑',
                            intro: {
                                name: '破堑',
                                content: '我搥跛汝钨錥邪？',
                            },
                        },
                        yunliegong: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'shaBegin',
                            },
                            forced: true,
                            content() {
                                if (!player.inRange(trigger.target)) {
                                    trigger.directHit = true;
                                }
                                if (player.inRange(trigger.target)) {
                                    player.getStat().card.sha--;
                                }
                            },
                            mod: {
                                cardUsableTarget(card, player, target) {
                                    if (card.name == 'sha' && player.inRange(target)) return true;
                                },
                                targetInRange(card) {
                                    if (card.name == 'sha') return true;
                                },
                            },
                        },
                        yunposhi: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                source: 'damageBegin',
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return !player.hasSkill('yunposhi_off');
                            },
                            mark: true,
                            marktext: '破势',
                            intro: {
                                name: '破势',
                                content(storage, player) {
                                    var ps = player.storage.yunposhi;
                                    return '<div>可额外造成' + ps + '点伤害并摸' + ps + '张牌</span></div>';
                                },
                            },
                            init(player, skill) {
                                player.storage.yunposhi = 0;
                            },
                            content() {
                                'step 0';
                                var num = player.storage.yunposhi;
                                if (player.storage.yunposhi > 0) {
                                    player.chooseBool('是否摸' + num + '张牌并令' + get.translation(trigger.player) + '受到的此次伤害加' + num + '？').set('ai', function (event, player) {
                                        return get.attitude(player, trigger.player) < 0 && player.storage.yunposhi >= trigger.player.hp;
                                    });
                                } else {
                                    if (player.storage.yunposhi <= 0) {
                                        player.storage.yunposhi++;
                                        event.finish();
                                    }
                                }
                                ('step 1');
                                if (result.bool) {
                                    player.say(['中!', '看箭!'].randomGet());
                                    trigger.num += player.storage.yunposhi;
                                    player.draw(player.storage.yunposhi);
                                    player.storage.yunposhi = 0; //QQQ
                                    player.addTempSkill('yunposhi_off');
                                } else {
                                    player.storage.yunposhi++;
                                }
                            },
                        },
                        yunposhi_off: {
                            charlotte: true,
                        },
                        yunxuezhao: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'useCardToPlayered',
                            },
                            usable: 3, //QQQ
                            popup: false,
                            filter(event, player) {
                                return player.hp > 1 && event.targets && event.targets.length == 1 && ['basic', 'trick'].includes(get.type(event.card, false)) && game.hasPlayer((Q) => !event.targets.includes(Q) && player != Q);
                            },
                            check(event, player) {
                                return get.effect(event.target, event.card, player, player);
                            },
                            prompt: '是否失去一点体力发动【血诏】？',
                            async content(event, trigger, player) {
                                player.loseHp();
                                var num = Math.min(3, player.maxHp - player.hp);
                                const result = await player
                                    .chooseTarget([1, num], '血诏:令至多' + num + '名角色视为对' + get.translation(trigger.target) + '使用一张' + get.translation(trigger.card), (card, player, Q) => trigger.targets.includes(Q) && player != Q)
                                    .set('ai', function (target) {
                                        if (get.attitude(_status.event.player, target) > 0) {
                                            return 3;
                                        }
                                        if (get.attitude(_status.event.player, target) <= 0) {
                                            return 1;
                                        }
                                    })
                                    .forResult();
                                if (result.targets && result.targets.length) {
                                    player.say(['爱卿,全靠你了～', '众卿还不诛杀国贼？'].randomGet());
                                    for (var i of result.targets) {
                                        var card = game.createCard(trigger.card.name, trigger.card.suit, trigger.card.number, trigger.card.nature);
                                        i.useCard(trigger.target, card, false, false);
                                    }
                                }
                            },
                        },
                        yuntianming: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: ['phaseZhunbeiBegin', 'phaseJieshuBegin'],
                            },
                            filter(event, player) {
                                return player.getDamagedHp() > player.hp || player.getDamagedHp() > player.countCards('h');
                            },
                            forced: true,
                            content() {
                                if (player.getDamagedHp() > player.countCards('h')) {
                                    player.draw();
                                }
                                if (player.getDamagedHp() > player.hp) {
                                    player.recover();
                                }
                            },
                        },
                        yunhanzuo: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'dieAfter',
                            },
                            forced: true,
                            zhuSkill: true,
                            filter(event, player) {
                                if (event.player == player) return false;
                                if (!player.hasZhuSkill('yunhanzuo')) return false;
                                return true;
                            },
                            content() {
                                if (trigger.player.identity == 'zhong') {
                                    player.loseMaxHp();
                                }
                                if (trigger.player.identity == 'nei') {
                                    player.link(false);
                                    player.turnOver(false);
                                    player.disableJudge();
                                }
                                if (trigger.player.identity == 'fan') {
                                    player.chooseDrawRecover(2, true, get.prompt('yunhanzuo'));
                                }
                            },
                        },
                        yunhuwei: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'damageBefore',
                            },
                            usable: 1,
                            prompt2(event, player) {
                                return '是否令<span style>' + get.translation(event.player) + '</span>获得一点护甲,你受到一点伤害？';
                            },
                            filter(event, player) {
                                return event.source && event.player != player && event.source != player && event.player.isDamaged();
                            },
                            check(event, player) {
                                var att = get.attitude(player, event.player);
                                var att2 = player.hp;
                                if (att2 > 1) {
                                    return att > 0;
                                } else {
                                    return false;
                                }
                            },
                            content() {
                                trigger.player.changeHujia();
                                player.damage('nosource');
                            },
                            ai: {
                                expose: 0.5,
                                threaten: 0.5,
                            },
                        },
                        yunelai: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'damage',
                            },
                            forced: true,
                            popup: false,
                            mark: true,
                            marktext: '恶来',
                            intro: {
                                name: '恶来',
                                content: '你已累计获得#层〖恶〗',
                            },
                            init(player) {
                                player.storage.yunelai = 0;
                                player.markSkill('yunelai');
                            },
                            content() {
                                'step 0';
                                event.num = Math.min(trigger.num, 9);
                                ('step 1');
                                player.storage.yunelai++;
                                player.markSkill('yunelai');
                                ('step 2');
                                event.num--;
                                if (event.num > 0) {
                                    event.goto(1);
                                }
                            },
                            group: ['yunelai_recover'],
                            subSkill: {
                                recover: {
                                    audio: 'yunelai',
                                    trigger: {
                                        player: 'phaseZhunbeiBegin',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return player.storage.yunelai > 0;
                                    },
                                    content() {
                                        'step 0';
                                        var num = player.storage.yunelai;
                                        player.draw(Math.floor(num / 2));
                                        player.recover(Math.floor(num / 2));
                                        player.chooseTarget('对一名角色造成一点伤害', get.prompt('yunelai_recover'), function (card, player, target) {
                                            return player != target;
                                        }).ai = function (target) {
                                            return get.damageEffect(target, player, player);
                                        };
                                        ('step 1');
                                        player.storage.yunelai = 0; //QQQ
                                        if (result.bool) {
                                            result.targets[0].damage(player);
                                        }
                                    },
                                },
                            },
                        },
                        yundingding: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseZhunbeiBegin',
                            },
                            limited: true,
                            filter(event, player) {
                                return !player.storage.yundingding;
                            },
                            init(player) {
                                player.storage.yundingding = false;
                            },
                            mark: true,
                            intro: {
                                content: 'limited',
                            },
                            forced: true,
                            content() {
                                'step 0';
                                player
                                    .chooseTarget([1, 2], get.prompt('yundingding'), '是否发动【定鼎】？', function (card, player, target) {
                                        return true;
                                    })
                                    .set('ai', function (target) {
                                        if (get.mode() == 'identity') {
                                            if (player.identity == 'nei') {
                                                if (Math.random() > 1 / 5) {
                                                    return get.attitude(player, target);
                                                }
                                            } else {
                                                return get.attitude(player, target);
                                            }
                                        }
                                        if (get.mode() != 'identity') {
                                            return get.attitude(player, target);
                                        }
                                    });
                                ('step 1');
                                if (result.bool) {
                                    player.awakenSkill('yundingding');
                                    player.storage.yundingding = true;
                                    player.say(['为将军计,惟有鼎足江东,以观天下之衅!', '荆楚之地若据而有之,此帝王之资也!'].randomGet());
                                    for (var i = 0; i < result.targets.length; i++) {
                                        result.targets[i].addSkill('yunlianjun');
                                        game.log(result.targets[i], '成为了', '【定鼎】', '的目标');
                                    }
                                    var num = 6 - _status.renku.length;
                                    var cards = get.cards(num);
                                    game.cardsGotoSpecial(cards, 'toRenku');
                                }
                            },
                            ai: {
                                expose: 1,
                            },
                        },
                        yunlianjun: {
                            audio: 'yundingding',
                            trigger: {
                                target: 'useCardToTargeted',
                            },
                            forced: true,
                            filter(event, player) {
                                return _status.renku.length;
                            },
                            content() {
                                'step 0';
                                player.chooseButton(['选择要获得的牌', _status.renku], true);
                                ('step 1');
                                if (result.bool) {
                                    var cards = result.links;
                                    _status.renku.removeArray(cards);
                                    game.updateRenku();
                                    player.gain(result.links, 'gain2', 'fromRenku');
                                }
                                ('step 2');
                                if (_status.renku.length <= 0) {
                                    event.trigger('dingding');
                                }
                            },
                            init(player) {
                                player.storage.renku = true;
                            },
                        },
                        yunhaoshi: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            usable: 1,
                            filterTarget(card, player, target) {
                                return player != target;
                            },
                            content() {
                                'step 0';
                                var cards = target.getCards('h');
                                if (cards.length) player.gain(cards, target, 'giveAuto');
                                ('step 1');
                                var num = Math.floor(player.countCards('h') / 2);
                                player.chooseCard('hes', num, true, '交给' + get.translation(target) + get.cnNumber(num) + '张牌');
                                ('step 2');
                                if (result.bool && result.cards && result.cards.length) target.gain(result.cards, player, 'giveAuto');
                            },
                            ai: {
                                order: 1,
                                expose: 0.3,
                                result: {
                                    target(player, target) {
                                        var delta = player.countCards('h') - target.countCards('h');
                                        return delta;
                                    },
                                },
                            },
                            group: ['yunhaoshi_dingding'],
                            subSkill: {
                                dingding: {
                                    audio: 'yunhaoshi',
                                    trigger: {
                                        global: 'dingding',
                                    },
                                    forced: true,
                                    content() {
                                        var num = 6 - _status.renku.length;
                                        var cards = get.cards(num);
                                        game.cardsGotoSpecial(cards, 'toRenku');
                                    },
                                },
                            },
                        },
                        yunxiandeng: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseBefore',
                            },
                            forced: true,
                            popup: false,
                            content() {
                                'step 0';
                                if (player.countMark('yunxiaolie') < 3) {
                                    player.addMark('yunxiaolie');
                                }
                                if (player.countCards('h') > 0 && player.countMark('yunxiaolie') < 3) {
                                    player
                                        .chooseTarget('是否发动【先登】？', '选择一名其他角色与其进行拼点', function (card, player, target) {
                                            return player.canCompare(target);
                                        })
                                        .set('ai', function (target) {
                                            return -get.attitude(_status.event.player, target) / target.countCards('h');
                                        });
                                }
                                ('step 1');
                                if (result.bool) {
                                    player.chooseToCompare(result.targets[0]);
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                if (result.bool) {
                                    player.phase('nodelay');
                                }
                            },
                            ai: {
                                expose: 0.5,
                            },
                        },
                        yunxiaolie: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'roundStart',
                            },
                            mark: true,
                            marktext: '骁烈',
                            intro: {
                                name2: '骁烈',
                                content: '已拥有了#个〖骁烈〗标记',
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return player.countMark('yunxiaolie') > 0;
                            },
                            content() {
                                'step 0';
                                'step 1';
                                var num = player.countMark('yunxiaolie');
                                player.chooseBool('〖骁烈〗还可移动场上至多' + num + '张牌').set('check', player.canMoveCard(true));
                                ('step 2');
                                if (result.bool) {
                                    player.moveCard();
                                    player.removeMark('yunxiaolie', 1);
                                } else {
                                    player.storage.yunxiaolie = 0; //QQQ
                                    event.finish();
                                }
                                ('step 3');
                                if (player.countMark('yunxiaolie') > 0) {
                                    event.goto(1);
                                }
                            },
                            mod: {
                                cardnumber(card) {
                                    if (card.suit == 'club') return 13;
                                },
                                aiUseful(player, card, num) {
                                    if (card.suit == 'club') return num + 3;
                                },
                            },
                        },
                        yunzhenjun: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            usable: 1,
                            filterTarget(card, player, target) {
                                return player.countCards('h') != target.countCards('h');
                            },
                            content() {
                                var num1 = player.countCards('h');
                                var num2 = target.countCards('h');
                                var num3 = player.maxHp + target.maxHp;
                                if (num1 < num2) {
                                    target.chooseToDiscard(num2 - num1, true, 'h');
                                } else {
                                    target.draw(Math.min(num3, num1 - num2));
                                }
                            },
                            ai: {
                                threaten: 1.8,
                                order(name, player) {
                                    var max = true,
                                        num = 0;
                                    var players = game.filterPlayer();
                                    for (var i = 0; i < players.length; i++) {
                                        if (players[i] == player) continue;
                                        var att = get.attitude(player, players[i]);
                                        var dh = player.countCards('h') - players[i].countCards('h');
                                        if (att * dh > num) {
                                            if (att > 0) {
                                                max = true;
                                            } else if (att < 0) {
                                                max = false;
                                            }
                                            num = att * dh;
                                        }
                                    }
                                    if (max) return 10;
                                    return 0.5;
                                },
                                result: {
                                    player(player, target) {
                                        if (get.attitude(player, target) > 0 && target.hasSkillTag('nogain')) return 0;
                                        return (player.countCards('h') - target.countCards('h')) * get.attitude(player, target);
                                    },
                                },
                                expose: 0.2,
                            },
                        },
                        yunyizhong: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'useCardAfter',
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return event.card.name == 'wuxie';
                            },
                            content() {
                                'step 0';
                                if (trigger.player != player) {
                                    if (get.itemtype(trigger.cards) == 'cards' && get.position(trigger.cards[0], true) == 'o') {
                                        player.gain(trigger.cards, 'gain2');
                                    } else {
                                        event.finish();
                                    }
                                } else {
                                    if (!player.isMaxHp(true)) {
                                        player
                                            .chooseTarget('请选择【毅重】失去体力的目标', '令一名体力值不小于你的其他角色失去一点体力', function (card, player, target) {
                                                return target.hp >= player.hp && target != player;
                                            })
                                            .set('ai', function (target) {
                                                return -get.attitude(_status.event.player, target);
                                            });
                                    } else {
                                        event.finish();
                                    }
                                }
                                ('step 1');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    player.line(target, 'green');
                                    target.loseHp();
                                }
                            },
                        },
                        yunmanshi: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                target: 'useCardToBefore',
                            },
                            forced: true,
                            _priority: 15,
                            filter(event, player) {
                                return event.card.name == 'nanman';
                            },
                            content() {
                                trigger.cancel();
                            },
                            ai: {
                                effect: {
                                    target(card, player, target) {
                                        if (card.name == 'nanman') return 'zeroplayertarget';
                                    },
                                },
                            },
                            group: ['yunmanshi_Use', 'yunmanshi_damage'],
                            subSkill: {
                                Use: {
                                    audio: 'yunmanshi',
                                    enable: 'phaseUse',
                                    viewAs: {
                                        name: 'nanman',
                                    },
                                    usable: 1,
                                    filterCard: true,
                                    prompt: '将任意张手牌当做【南蛮入侵】并指定等量的角色使用',
                                    selectCard() {
                                        if (ui.selected.targets.length) return [ui.selected.targets.length, Math.min(ui.selected.targets.length + 1, game.players.length - 1)];
                                        return [1, game.countPlayer() - 1];
                                    },
                                    check(card) {
                                        var player = _status.event.player;
                                        if (
                                            game.countPlayer(function (current) {
                                                return (
                                                    current != player &&
                                                    player.canUse('nanman', current) &&
                                                    get.effect(
                                                        current,
                                                        {
                                                            name: 'nanman',
                                                        },
                                                        player,
                                                        player,
                                                    ) > 0
                                                );
                                            }) <= ui.selected.cards.length
                                        )
                                            return 0;
                                        return 7 - get.value(card);
                                    },
                                    selectTarget() {
                                        return ui.selected.cards.length;
                                    },
                                    ai: {
                                        basic: {
                                            order: 9,
                                            useful: [5, 1],
                                            value: 5,
                                        },
                                        result: {
                                            target_use(player, target) {
                                                var nh = target.countCards('h');
                                                if (nh == 0) return -2;
                                                if (nh == 1) return -1.7;
                                                return -1.5;
                                            },
                                            target(player, target) {
                                                var nh = target.countCards('h');
                                                if (nh == 0) return -2;
                                                if (nh == 1) return -1.7;
                                                return -1.5;
                                            },
                                        },
                                        tag: {
                                            respond: 1,
                                            respondSha: 1,
                                            damage: 1,
                                            multitarget: 1,
                                            multineg: 1,
                                        },
                                    },
                                },
                                damage: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        source: 'damageBegin1',
                                    },
                                    check(event, player) {
                                        if (get.attitude(player, event.player) < 0 && player.storage.yunzhanyuan == false && player.hp > 1) return true;
                                        if (get.attitude(player, event.player) < 0 && player.storage.yunzhanyuan == true && player.hp > 2) return true;
                                        return false;
                                    },
                                    filter(event, player) {
                                        return event.player.hp >= player.hp && event.card && event.card.name == 'nanman';
                                    },
                                    preHidden: true,
                                    prompt(event, player) {
                                        var str = '';
                                        str += '是否失去一点体力令' + get.translation(event.player) + '受到的伤害加一？';
                                        return str;
                                    },
                                    content() {
                                        player.loseHp();
                                        trigger.num++;
                                    },
                                },
                            },
                        },
                        yunzhanyuan: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'dying',
                            },
                            mark: true,
                            marktext: '战缘',
                            intro: {
                                content: 'limited',
                            },
                            limited: true,
                            init(player) {
                                player.storage.yunzhanyuan = false;
                            },
                            filter(event, player) {
                                return player.storage.yunzhanyuan == false;
                            },
                            content() {
                                'step 0';
                                player.awakenSkill('yunzhanyuan');
                                player.storage.yunzhanyuan = true;
                                if (player.name1 == 'yunhuaman') {
                                    player.node.avatar.setBackgroundImage('extension/云将/image/南方艳色a.jpg');
                                }
                                ('step 1');
                                player.draw(5);
                                player.hp = player.maxHp;
                                ('step 2');
                                if (get.mode() != 'boss') {
                                    player
                                        .chooseTarget('请选择【战缘】的目标角色', true, function (card, player, target) {
                                            return player != target;
                                        })
                                        .set('ai', function (target) {
                                            if (_status.characterlist.includes('lyz_guansuo') || _status.characterlist.includes('JX_baosanniang')) {
                                                if (player.identity == 'nei') {
                                                    if (Math.random() > 2 / 3) {
                                                        return false;
                                                    }
                                                }
                                                return get.attitude(player, target);
                                            } else {
                                                return -get.attitude(player, target);
                                            }
                                        });
                                }
                                ('step 3');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    if ((target.sex == 'male' && _status.characterlist.includes('lyz_guansuo')) || (target.sex == 'female' && _status.characterlist.includes('JX_baosanniang'))) {
                                        var name = target.name;
                                        _status.characterlist.add(name);
                                        if (target.sex == 'male') {
                                            player.say(['生死结缘,与君厮守', '这是哪家的小哥哥,真好看,待我绑回寨子里当压寨相公!'].randomGet());
                                            _status.characterlist.remove('lyz_guansuo');
                                            target.reinit(name, 'lyz_guansuo', false);
                                            player.addTempSkill('yunzhanyuan_liangren', { player: 'zhanyuan' });
                                            if (target.group != 'shu') target.changeGroup('shu');
                                            if (target.identity !== 'zhu') {
                                                target.maxHp = 4;
                                            } else {
                                                target.maxHp = 5;
                                            }
                                            target.addTempSkill('yunzhanyuan_liangren', { player: 'zhanyuan' });
                                        }
                                        if (target.sex == 'female') {
                                            player.say(['那我是叫你姐姐呢？还是叫你妹妹？', '我是来加入这个家,不是来拆散你们～'].randomGet());
                                            _status.characterlist.remove('JX_baosanniang');
                                            target.reinit(name, 'JX_baosanniang', false);
                                            player.addTempSkill('yunzhanyuan_liangren', { player: 'zhanyuan' });
                                            if (target.group != 'shu') target.changeGroup('shu');
                                            if (target.identity !== 'zhu') {
                                                target.maxHp = 3;
                                            } else {
                                                target.maxHp = 4;
                                            }
                                            target.addTempSkill('yunzhanyuan_liangren', { player: 'zhanyuan' });
                                        }
                                    } else {
                                        target.addTempSkill('yunzhanyuan_noliangren', { player: 'zhanyuan' });
                                    }
                                    player.turnOver(true);
                                    target.turnOver(true);
                                    player.addSkill('yunzhanyuan_over');
                                    target.addSkill('yunzhanyuan_over');
                                }
                            },
                        },
                        longfengjin: {},
                        yunzhanyuan_noliangren: {
                            trigger: {
                                player: 'dieBegin',
                            },
                            forced: true,
                            popup: false,
                            forceDie: true,
                            mark: true,
                            marktext: '良人',
                            intro: {
                                name: '良人',
                                content: '<太好了,你这么年轻,应该趾高气昂,为人所不能为之事.你跟花蔓相处不过半载,感情不会太深,很容易放下的.我希望你能写封信给花蔓,说明你的胸襟不止儿女私情这么窄.><br><我不会写的!><br><你不写？你以为愤怒就能改变跟花蔓的命运？你以为很不满蜀人就会忍让南边的蛮人？要怨就怨你们生错了地方,生在这个人命如草芥的乱世,人人都这么的虚伪迂腐和势利,要怨就怨你们太多想法年少无知到以为不喜欢就能改变周围的人,以为靠你们两个就能改变这个时代!>',
                            },
                            filter(event, player) {
                                return player.isTurnedOver();
                            },
                            content() {
                                event.trigger('zhanyuan');
                            },
                            ai: {
                                threaten: 10,
                            },
                        },
                        yunzhanyuan_liangren: {
                            mark: true,
                            marktext: '良人',
                            intro: {
                                name: '良人',
                                content: '所有的故事都需要有一个皆大欢喜的结局,不是吗？',
                            },
                            mod: {
                                targetEnabled(card, player, target) {
                                    if (target.isTurnedOver()) return false;
                                },
                            },
                        },
                        yunzhanyuan_over: {
                            trigger: {
                                player: 'turnOverEnd',
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return !player.isTurnedOver();
                            },
                            content() {
                                event.trigger('zhanyuan');
                                player.removeSkill('yunzhanyuan_over');
                            },
                        },
                        yunshiguan: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'drawBegin',
                            },
                            forced: true,
                            usable: 3,
                            filter(event, player) {
                                return event.getParent(2).name != 'yunshiguan_discard';
                            },
                            content() {
                                trigger.num = player.hp;
                            },
                            group: ['yunshiguan_discard'],
                            subSkill: {
                                discard: {
                                    trigger: {
                                        source: 'damageEnd',
                                        global: 'lisi',
                                    },
                                    forced: true,
                                    popup: false,
                                    usable: 3,
                                    async content(event, trigger, player) {
                                        if (player.isMaxHandcard(true) && player.countCards('h') > 1) {
                                            const result = await player.chooseToDiscard(Math.ceil(player.countCards('h') / 2), 'h', true).forResult();
                                            if (result.cards && result.cards.length >= player.hp) {
                                                //QQQ
                                                event.trigger('shiguan');
                                            }
                                        } else {
                                            await player.chooseDrawRecover(player.hp, true);
                                        }
                                    },
                                },
                            },
                        },
                        yunshiguan_lisi: {
                            audio: 'yunlisi',
                            trigger: {
                                player: 'drawBegin',
                            },
                            mark: true,
                            marktext: '立嗣',
                            intro: {
                                name: '立嗣',
                                content: '你拥有【势冠】直到回合结束',
                            },
                            forced: true,
                            usable: 3,
                            filter(event, player) {
                                return event.getParent(2).name != 'yunshiguan_lisi_discard';
                            },
                            content() {
                                trigger.num = player.hp;
                            },
                            group: ['yunshiguan_lisi_discard'],
                            subSkill: {
                                discard: {
                                    trigger: {
                                        source: 'damageEnd',
                                    },
                                    forced: true,
                                    popup: false,
                                    usable: 3,
                                    filter(event, player) {
                                        return !player.hasSkill('yunshiguan');
                                    },
                                    content() {
                                        var num = Math.ceil(player.countCards('h') / 2);
                                        if (player.isMaxHandcard(true)) {
                                            if (player.countCards('h') > 1) {
                                                player.chooseToDiscard(num, 'h', true);
                                            }
                                        } else {
                                            player.chooseDrawRecover(player.hp, true);
                                        }
                                        if (player.group == 'qun') {
                                            event.trigger('lisi');
                                        }
                                    },
                                },
                            },
                        },
                        yunxiongjie: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'shiguan',
                            },
                            usable: 1,
                            forced: true,
                            popup: false,
                            content() {
                                'step 0';
                                var num = player.hp;
                                player
                                    .chooseTarget([1, num], '是否发动【雄桀】选择至多' + num + '名角色？<br>视为对这些角色使用一张【万箭齐发】', function (card, player, target) {
                                        return player != target;
                                    })
                                    .set('ai', function (target) {
                                        return -get.attitude(player, target);
                                    });
                                ('step 1');
                                if (result.bool) {
                                    var targets = result.targets;
                                    player.useCard({ name: 'wanjian' }, targets, false);
                                } else {
                                    player.getStat('triggerSkill').yunxiongjie--;
                                }
                            },
                        },
                        yunlisi: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseJieshuBegin',
                            },
                            zhuSkill: true,
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                return player.hasZhuSkill('yunlisi');
                            },
                            content() {
                                'step 0';
                                player
                                    .chooseTarget(get.prompt('yunlisi'), '是否令一名其他角色获得【势冠】直到其回合结束？', function (card, player, target) {
                                        return player != target;
                                    })
                                    .set('ai', function (target) {
                                        if (get.attitude(player, target) < 0 && target.hp == 1) return -get.attitude(player, target);
                                        if (get.attitude(player, target) > 0) return target.hp;
                                        if (target.group == 'qun' && get.attitude(player, target) > 0) return target.hp + 1;
                                        return 0;
                                    });
                                ('step 1');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    game.log(target, '成为了', '【立嗣】', '的目标');
                                    target.addTempSkill('yunshiguan_lisi', { player: 'phaseAfter' });
                                }
                            },
                        },
                        yuntianyi: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            usable: 1,
                            prompt: '请选择至多三名角色作为【天义】拼点的目标',
                            selectTarget() {
                                return [1, 3];
                            },
                            filterTarget(card, player, target) {
                                return player.canCompare(target);
                            },
                            filter(event, player) {
                                return player.countCards('h') > 0;
                            },
                            multitarget: true,
                            multiline: true,
                            content() {
                                player.chooseToCompare(targets).callback = lib.skill.yuntianyi.callback;
                            },
                            callback() {
                                if (event.num1 > event.num2) {
                                    player.draw();
                                    if (!player.hasSkill('yuntianyi_sha')) {
                                        player.addTempSkill('yuntianyi_sha');
                                    }
                                    target.addTempSkill('yuntianyi_buyi');
                                }
                                if (event.num1 < event.num2) {
                                    if (!player.hasSkill('yuntianyi_nosha')) {
                                        player.addTempSkill('yuntianyi_nosha');
                                    }
                                    target.addTempSkill('yuntianyi_zhengyi');
                                }
                            },
                            ai: {
                                order(name, player) {
                                    var cards = player.getCards('h');
                                    if (player.countCards('h', 'sha') == 0) {
                                        return 1;
                                    }
                                    for (var i = 0; i < cards.length; i++) {
                                        if (cards[i].name != 'sha' && cards[i].number > 11 && get.value(cards[i]) < 7) {
                                            return 9;
                                        }
                                    }
                                    return get.order({ name: 'sha' }) - 1;
                                },
                                expose: 0.5,
                                result: {
                                    target(player, target) {
                                        var hs = player.getCards('h');
                                        for (var i = 0; i < hs.length; i++) {
                                            if (get.value(hs[i]) <= 6) {
                                                switch (hs[i].number) {
                                                    case 13:
                                                        return -3.5;
                                                    case 12:
                                                        return -3;
                                                    case 11:
                                                        return -2.5;
                                                    case 10:
                                                        return -2;
                                                    case 9:
                                                        return -1.5;
                                                    case 8:
                                                        return -1;
                                                    case 7:
                                                        return -0.5;
                                                    case 6:
                                                        return -0.5;
                                                    case 5:
                                                        return -0.5;
                                                    case 4:
                                                        return -0.5;
                                                    case 3:
                                                        return -0.5;
                                                    case 2:
                                                        return -0.5;
                                                    case 1:
                                                        return -0.5;
                                                }
                                            }
                                        }
                                    },
                                },
                                effect: {
                                    player(card, player, target) {
                                        if (card.name == 'sha') {
                                            if (get.attitude(target, player) < 0 && !target.hasSkill('yuntianyi_buyi')) {
                                                return [1, 0.05];
                                                if (!card.nature && (target.hasSkill('tengjia3') || target.hasSkill('rw_tengjia4'))) {
                                                    return 'zeroplayertarget';
                                                }
                                                if (get.color(card) == 'black' && target.hasSkill('renwang_skill')) {
                                                    return 'zeroplayertarget';
                                                }
                                            }
                                        }
                                    },
                                },
                            },
                        },
                        yuntianyi_sha: {
                            trigger: {
                                player: 'useCard2',
                            },
                            forced: true,
                            charlotte: true,
                            filter(event, player) {
                                var num = game.countPlayer(function (current) {
                                    return current.hasSkill('yuntianyi_buyi');
                                });
                                return event.card && event.card.name == 'sha' && num >= 1;
                            },
                            content() {
                                'step 0';
                                var num = game.countPlayer(function (current) {
                                    return current.hasSkill('yuntianyi_buyi');
                                });
                                player
                                    .chooseTarget('是否发动【天义】指定额外角色成为此【杀】的目标？', [1, num], function (card, player, target) {
                                        var evt = _status.event.getTrigger();
                                        return target != player && target.hasSkill('yuntianyi_buyi') && !evt.targets.includes(target);
                                    })
                                    .set('sourcex', trigger.targets)
                                    .set('ai', function (target) {
                                        var player = _status.event.player;
                                        return get.effect(target, _status.event.card, player, player);
                                    })
                                    .set('card', trigger.card);
                                ('step 1');
                                if (result.bool && result.targets && result.targets.length) {
                                    var targets = result.targets;
                                    player.line(targets, trigger.card.nature);
                                    trigger.targets.addArray(targets);
                                    trigger.yuntianyi_sha = true;
                                }
                            },
                        },
                        yuntianyi_buyi: {
                            mark: true,
                            marktext: '不义',
                            intro: {
                                name: '天义',
                                content: '不仁不义之徒,受死!',
                            },
                            charlotte: true,
                        },
                        yuntianyi_nosha: {
                            mod: {
                                playerEnabled(card, player, target) {
                                    if (card.name == 'sha' && target.hasSkill('yuntianyi_zhengyi')) return false;
                                },
                            },
                        },
                        yuntianyi_zhengyi: {
                            mark: true,
                            marktext: '正义',
                            intro: {
                                name: '天义',
                                content: '阁下乃正直仁爱之士,子义佩服!',
                            },
                            charlotte: true,
                        },
                        yunaozhan: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'chooseToCompareBegin',
                            },
                            filter(event, player) {
                                if (player == event.player) return true;
                                if (event.targets) return event.targets.includes(player);
                                return player == event.target;
                            },
                            logTarget(event, player) {
                                if (player != event.player) return event.player;
                                return event.targets || event.target;
                            },
                            forced: true,
                            content() {
                                var targets = player == trigger.player ? (trigger.targets ? trigger.targets.slice(0) : [trigger.target]) : [trigger.player];
                                if (!trigger.fixedResult) trigger.fixedResult = {};
                                while (targets.length) {
                                    var target = targets.shift();
                                    var hs = target.getCards('h');
                                    if (hs.length) trigger.fixedResult[target.playerid] = hs.randomGet();
                                }
                            },
                            mod: {
                                cardUsable(card, player, num) {
                                    var nums = game.countPlayer(function (current) {
                                        return current.hasSkill('yuntianyi_buyi');
                                    });
                                    if (card.name == 'sha') {
                                        return (num += nums);
                                    }
                                },
                            },
                        },
                        yunjiliang: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseDrawBegin2',
                            },
                            forced: true,
                            content() {
                                'step 0';
                                trigger.num += 3;
                                ('step 1');
                                player.addTempSkill('yunjiliang_Draw');
                            },
                            intro: {
                                content: 'expansion',
                                markcount: 'expansion',
                            },
                            onremove(player, skill) {
                                var cards = player.getExpansions(skill);
                                if (cards.length) player.loseToDiscardpile(cards);
                            },
                            group: ['yunjiliang_jieshu'],
                            subSkill: {
                                jieshu: {
                                    trigger: {
                                        player: 'phaseJieshuBegin',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return player.getExpansions('yunjiliang').length;
                                    },
                                    content() {
                                        player.loseToDiscardpile(player.getExpansions('yunjiliang'));
                                    },
                                },
                            },
                        },
                        yunjiliang_Draw: {
                            trigger: {
                                player: 'phaseDrawEnd',
                            },
                            forced: true,
                            content() {
                                'step 0';
                                player
                                    .chooseCard('h', true, 3, '积粮:请选择至多三张类型各不相同的手牌作为〖粮〗', function (card, player) {
                                        if (!ui.selected.cards.length) return true;
                                        var type = get.type2(card);
                                        for (var i of ui.selected.cards) {
                                            if (get.type2(i, player) == type) return false;
                                        }
                                        return true;
                                    })
                                    .set('complexCard', true);
                                ('step 1');
                                if (result.bool) {
                                    var cards = result.cards;
                                    player.addToExpansion('giveAuto', cards, player).gaintag.add('yunjiliang');
                                }
                            },
                        },
                        yunpoxian: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'useCard',
                            },
                            forced: true,
                            filter(event, player) {
                                if (player.getExpansions('yunjiliang').length == 0) return false;
                                return player.getExpansions('yunjiliang').filter(function (magic) {
                                    return get.type2(magic) == get.type2(event.card);
                                }).length;
                            },
                            init(player) {
                                player.storage.yunpoxian = 0;
                                player.storage.yunpoxian_sx = 0;
                            },
                            mark: false,
                            content() {
                                'step 0';
                                player
                                    .chooseCardButton(true, get.prompt('yunjiliang', trigger.player), player.getExpansions('yunjiliang'), function (button) {
                                        return true;
                                    })
                                    .set('ai', function (button) {
                                        return 1 + Math.random();
                                    });
                                ('step 1');
                                if (result.bool) {
                                    player.storage.yunpoxian++;
                                    event.card = result.links[0];
                                    player.gain(event.card, 'give', player);
                                }
                                ('step 2');
                                if (player.storage.yunpoxian == 1) {
                                    player.draw();
                                }
                                if (player.storage.yunpoxian == 2) {
                                    player.getStat().card.sha = 0;
                                    player.getStat().card.jiu = 0;
                                }
                                if (player.storage.yunpoxian == 3) {
                                    player.storage.yunpoxian_sx++;
                                }
                            },
                            mod: {
                                maxHandcard(player, num) {
                                    return num + player.storage.yunpoxian_sx;
                                },
                                aiOrder(player, card, num) {
                                    if (player.getExpansions('yunjiliang').length) {
                                        if (typeof card == 'object' && player.isPhaseUsing()) {
                                            var evt = player.getLastUsed();
                                            if (evt && evt.card && get.type2(evt.card) && get.type2(evt.card) != get.type2(card)) {
                                                return num + 10;
                                            }
                                        }
                                    }
                                },
                            },
                            group: ['yunpoxian_jieshu'],
                            subSkill: {
                                jieshu: {
                                    trigger: {
                                        player: 'phaseJieshuBegin',
                                    },
                                    forced: true,
                                    silent: true,
                                    forced: true,
                                    popup: false,
                                    content() {
                                        player.storage.yunpoxian = 0;
                                    },
                                },
                            },
                        },

                        yunpianhong: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'gainBefore',
                                global: ['gameDrawAfter'],
                            },
                            forced: true,
                            popup: false,
                            filter(event, player) {
                                if (event.name == 'gain') return event.cards && event.cards.length; //QQQ
                                return player.countCards('h');
                            },
                            content() {
                                if (trigger.name == 'gain') {
                                    trigger.cancel();
                                    var owner = get.owner(trigger.cards[0]);
                                    if (owner && owner.getCards('hejsx').includes(trigger.cards[0])) owner.lose(trigger.cards, ui.discardPile);
                                    else game.cardsDiscard(trigger.cards);
                                    game.log(trigger.cards, '进入了弃牌堆');
                                } else {
                                    var cards = player.getCards('h');
                                    if (cards.length) {
                                        player.discard(cards);
                                    }
                                }
                            },
                            mod: {
                                targetEnabled(card, player, target) {
                                    if (get.type(card) == 'delay') {
                                        return false;
                                    }
                                },
                            },
                            ai: {
                                nokeep: true,
                            },
                            group: ['yunpianhong_changeHp', 'yunpianhong_Over'],
                            subSkill: {
                                changeHp: {
                                    audio: 'yunpianhong',
                                    trigger: {
                                        player: ['damageEnd', 'recoverEnd'],
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return event.num > 0;
                                    },
                                    content() {
                                        event.card = get.cards()[0];
                                        event.card.discard();
                                        game.log(player, '将', event.card, '置入弃牌堆');
                                    },
                                },
                                Over: {
                                    audio: 'yunpianhong',
                                    trigger: {
                                        player: 'turnOverBefore',
                                    },
                                    _priority: 20,
                                    forced: true,
                                    filter(event, player) {
                                        return !player.isTurnedOver();
                                    },
                                    content() {
                                        trigger.cancel();
                                        game.log(player, '取消了翻面');
                                    },
                                    ai: {
                                        noturn: true,
                                    },
                                },
                            },
                        },
                        yunwangzun: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseZhunbeiBegin',
                            },
                            forced: true,
                            filter(event, player) {
                                return (
                                    !game.hasPlayer(function (current) {
                                        return current.countCards('hesj', 'yunyuxi');
                                    }) && !player.isDisabled(5)
                                );
                            },
                            content() {
                                player.loseHp();
                                lib.inpile.push('yunyuxi');
                                player.equip(game.createCard('yunyuxi'));
                            },
                            mod: {
                                aiOrder(player, card, num) {
                                    var history = player.getHistory('useCard', function (evt) {
                                        return evt.isPhaseUsing();
                                    });
                                    var types = [];
                                    for (var i = 0; i < history.length; i++) {
                                        var type = get.type2(history[i].card);
                                        if (type) types.add(type);
                                    }
                                    if (player.getEquip('yunyuxi') && types.length < 3) {
                                        if (typeof card == 'object' && player.isPhaseUsing()) {
                                            var evt = player.getLastUsed();
                                            if (evt && evt.card && get.type2(evt.card) && get.type2(evt.card) != get.type2(card)) {
                                                return num + 25;
                                            }
                                        }
                                    }
                                },
                            },
                        },
                        yunyuxi_skill: {
                            equipSkill: true,
                            trigger: {
                                player: 'phaseDrawBegin',
                            },
                            forced: true,
                            filter(event, player) {
                                return player.getEquip('yunyuxi');
                            },
                            content() {
                                trigger.num = Math.min(9, player.maxHp);
                                if (player.hasSkill('yunwangzun')) {
                                    var a = [1, 2].randomGet();
                                    if (a == 1) {
                                        game.playAudio('../extension/云将/audio/yunwangzun1.mp3');
                                    }
                                    if (a == 2) {
                                        game.playAudio('../extension/云将/audio/yunwangzun2.mp3');
                                    }
                                }
                            },
                            group: ['yunyuxi_skill_Use'],
                            subSkill: {
                                Use: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        player: 'phaseUseEnd',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return player.getEquip('yunyuxi');
                                    },
                                    content() {
                                        'step 0';
                                        var history = player.getHistory('useCard', function (evt) {
                                            return evt.isPhaseUsing();
                                        });
                                        var types = [];
                                        for (var i = 0; i < history.length; i++) {
                                            var type = get.type2(history[i].card);
                                            if (type) types.add(type);
                                        }
                                        if (types.length >= 3) {
                                            if (player.maxHp < 9) {
                                                player.gainMaxHp(true);
                                            }
                                            event.finish();
                                        } else {
                                            player.chat('朕要喝蜜水!朕要喝蜜水!');
                                            event.num = 3 - types.length;
                                        }
                                        ('step 1');
                                        if (event.num > 0 && player.maxHp > 1) {
                                            player.loseMaxHp(true);
                                        }
                                        event.num--;
                                        ('step 2');
                                        if (event.num > 0) {
                                            event.goto(1);
                                        }
                                    },
                                },
                            },
                        },
                        yunjingwu: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseUseBefore',
                            },
                            forced: true,
                            filter(event, player) {
                                return player.countCards('he', { type: 'equip' }) > 0 || player.countDisabled() < 5;
                            },
                            content() {
                                'step 0';
                                var cards = player.getCards('he', { type: 'equip' });
                                player.loseToDiscardpile(cards);
                                player.draw(cards.length);
                                ('step 1');
                                if (player.countDisabled() < 5) {
                                    var list = ['equip1', 'equip2', 'equip3', 'equip4', 'equip5'];
                                    for (var i = 0; i < list.length; i++) {
                                        if (player.isDisabled(list[i])) list.splice(i--, 1);
                                    }
                                    player.chooseControl(list).set('prompt', '请选择废除一个装备栏').ai = function () {
                                        if (
                                            list.includes('equip1') &&
                                            player.isEmpty('equip1') &&
                                            player.countCards('h', function (card) {
                                                return card.name == 'sha' && player.getUseValue(card) > 0;
                                            })
                                        )
                                            return 'equip1';
                                        if (list.includes('equip2') && player.isEmpty('equip2')) return 'equip2';
                                        if (list.includes('equip3') && player.isEmpty('equip3')) return 'equip3';
                                        if (list.includes('equip4') && player.isEmpty('equip4')) return 'equip4';
                                        if (list.includes('equip5') && player.isEmpty('equip5')) return 'equip5';
                                        return list.randomGet();
                                    };
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                player.disableEquip(result.control);
                                ('step 3');
                                player.draw(player.countDisabled());
                                player.removeSkill('yundizhu_off');
                                player.addTempSkill('yunjingwu_buff', { player: 'phaseZhunbeiBegin' });
                            },
                            ai: {
                                combo: 'yundizhu',
                            },
                        },
                        yunjingwu_buff: {
                            silent: true,
                            charlotte: true,
                        },
                        yundizhu: {
                            audio: 'ext:云将/audio:2',
                            enable: ['chooseToUse', 'chooseToRespond'],
                            popup: false,
                            hiddenCard(player, name) {
                                if (player.hasSkill('yundizhu_off')) return false;
                                return (
                                    get.type(name) == 'basic' &&
                                    game.hasPlayer(function (current) {
                                        return current.countCards('hej') > 0;
                                    }) >
                                    game.hasPlayer(function (current) {
                                        return current.countCards('hej') <= 0;
                                    })
                                );
                            },
                            filter(event, player) {
                                if (player.hasSkill('yundizhu_off')) return false;
                                if (event.type == 'wuxie') return false;
                                if (
                                    game.hasPlayer(function (current) {
                                        return current.countCards('hej') > 0;
                                    }) >
                                    game.hasPlayer(function (current) {
                                        return current.countCards('hej') <= 0;
                                    })
                                ) {
                                    return true;
                                }
                                for (var i of lib.inpile) {
                                    if (get.type(i) != 'basic') continue;
                                    var card = { name: i };
                                    if (event.filterCard(card, player, event)) return true;
                                    if (i == 'sha') {
                                        for (var j of lib.inpile_nature) {
                                            card.nature = j;
                                            if (event.filterCard(card, player, event)) return true;
                                        }
                                    }
                                }
                                return false;
                            },
                            chooseButton: {
                                dialog(event, player) {
                                    var list = [];
                                    if (event.filterCard && event.filterCard({ name: 'sha' }, player, event)) {
                                        list.push(['基本', '', 'sha']);
                                        for (var j of lib.inpile_nature) list.push(['基本', '', 'sha', j]);
                                    }
                                    if (event.filterCard && event.filterCard({ name: 'shan' }, player, event)) {
                                        list.push(['基本', '', 'shan']);
                                    }
                                    if (event.filterCard && event.filterCard({ name: 'tao' }, player, event)) {
                                        list.push(['基本', '', 'tao']);
                                    }
                                    if (event.filterCard && event.filterCard({ name: 'jiu' }, player, event)) {
                                        list.push(['基本', '', 'jiu']);
                                    }
                                    return ui.create.dialog('砥柱', [list, 'vcard'], 'hidden');
                                },
                                check(button) {
                                    var player = _status.event.player;
                                    var card = { name: button.link[2], nature: button.link[3] };
                                    if (
                                        player.hasSkill('yunjingwu_buff') &&
                                        player.countCards('hs', { name: 'sha' }) >= 1 &&
                                        game.hasPlayer(function (current) {
                                            return current != player && player.inRange(current) && get.attitude(player, current) < 0;
                                        }) &&
                                        player.isPhaseUsing()
                                    ) {
                                        if (card.name == 'jiu') return 10;
                                    } else {
                                        if (card.name == 'jiu') return 0;
                                    }
                                    if (
                                        game.hasPlayer(function (current) {
                                            return get.effect(current, card, player, player) > 0;
                                        })
                                    ) {
                                        if (card.name == 'sha') {
                                            var eff = player.getUseValue(card);
                                            if (eff > 0) return 2.9 + eff / 10;
                                            return 0;
                                        } else if (card.name == 'tao' || card.name == 'shan') {
                                            return 4;
                                        }
                                    }
                                    return 0;
                                },
                                backup(links, player) {
                                    return {
                                        filterCard() {
                                            return false;
                                        },
                                        viewAs: {
                                            name: links[0][2],
                                            nature: links[0][3],
                                        },
                                        selectCard: -1,
                                        precontent() {
                                            'step 0';
                                            if (player.hasSkill('yunjingwu_buff')) {
                                                player.addTempSkill('yundizhu_off');
                                            } else {
                                                player.addTempSkill('yundizhu_off', 'roundStart');
                                            }
                                            ('step 1');
                                            player.useSkill('yundizhu_discard');
                                        },
                                    };
                                },
                                prompt(links, player) {
                                    return '选择【' + get.translation(links[0][3] || '') + get.translation(links[0][2]) + '】的目标';
                                },
                            },
                            ai: {
                                order() {
                                    var player = _status.event.player;
                                    var event = _status.event;
                                    if (
                                        game.hasPlayer(function (current) {
                                            return current.countCards('hes') > 0;
                                        })
                                    ) {
                                        if (event.type == 'dying') {
                                            if (event.filterCard && event.filterCard({ name: 'tao' }, player, event)) {
                                                return 0.5;
                                            }
                                        } else {
                                            if ((event.filterCard && event.filterCard({ name: 'tao' }, player, event)) || event.filterCard({ name: 'shan' }, player, event)) {
                                                return 4;
                                            }
                                            if (player.hasSkill('yunjingwu_buff')) {
                                                if (event.filterCard && event.filterCard({ name: 'sha' }, player, event)) {
                                                    return 3.5;
                                                }
                                            }
                                            if (event.filterCard && event.filterCard({ name: 'sha' }, player, event)) {
                                                return 2.9;
                                            }
                                        }
                                    }
                                    return 0;
                                },
                                save: true,
                                respondSha: true,
                                respondShan: true,
                                skillTagFilter(player, tag, arg) {
                                    return game.hasPlayer(function (current) {
                                        return current.countCards('hej') > 0;
                                    });
                                },
                                result: {
                                    player(player) {
                                        if (_status.event.type == 'dying') {
                                            return get.attitude(player, _status.event.dying);
                                        } else {
                                            return 1;
                                        }
                                    },
                                },
                                effect: {
                                    target(card, player, target) {
                                        if (card.name == 'sha' && player.getCardUsable('sha') <= 1 && target.hasSkill('yunjingwu_buff') && !target.hasSkill('yundizhu_off')) return 'zeroplayertarget';
                                    },
                                },
                            },
                            subSkill: {
                                discard: {
                                    audio: 'yundizhu',
                                    content() {
                                        'step 0';
                                        player
                                            .chooseTarget('请选择弃置牌的目标', '弃置一名角色区域内一张牌', true, function (card, player, target) {
                                                return target.countDiscardableCards(player, 'hej') > 0;
                                            })
                                            .set('ai', function (target) {
                                                var player = _status.event.player;
                                                var att = get.attitude(player, target);
                                                if (att < 0) {
                                                    att = -Math.sqrt(-att);
                                                } else {
                                                    att = Math.sqrt(att);
                                                }
                                                return att * lib.card.guohe.ai.result.target(player, target);
                                            });
                                        ('step 1');
                                        if (result.bool) {
                                            player.say(['锦秀江东,由我守护!', '下战伐兵,上战伐谋'].randomGet());
                                            player.discardPlayerCard(1, result.targets[0], 'hej');
                                        }
                                    },
                                    ai: {
                                        expose: 0.3,
                                    },
                                },
                            },
                        },
                        yundizhu_off: {
                            silent: true,
                            charlotte: true,
                            mark: true,
                            marktext: '砥柱',
                            intro: {
                                name: '砥柱',
                                content: '已发动过【砥柱】',
                            },
                        },
                        yunduce: {
                            audio: 'ext:云将/audio:2',
                            enable: 'phaseUse',
                            filter(event, player) {
                                return player.countCards('he', { color: 'black' });
                            },
                            position: 'he',
                            filterCard(card) {
                                return get.color(card) == 'black';
                            },
                            filterTarget(card, player, target) {
                                return target != player && target.countCards('h') > 0 && !target.hasSkill('yunduce_off');
                            },
                            discard: false,
                            delay: false,
                            loseTo: 'cardPile',
                            insert: true,
                            visible: true,
                            check(card) {
                                return 10 - get.value(card);
                            },
                            content() {
                                'step 0';
                                player.showCards(cards);
                                game.log(player, '将', cards, '置于牌堆顶');
                                ('step 1');
                                var card = target
                                    .getCards('h', function (card) {
                                        return card.name != 'du';
                                    })
                                    .randomGet();
                                if (card) {
                                    card.init([card.suit, card.number, 'du']);
                                }
                                game.log(target, '将一张手牌转化为', { name: 'du' });
                                target.addTempSkill('yunduce_off', 'phaseAfter');
                            },
                            ai: {
                                order: 7,
                                result: {
                                    target(player, target) {
                                        return -1;
                                    },
                                },
                            },
                        },
                        yunduce_off: {
                            charlotte: true,
                        },
                        yunzhensha: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'loseHpAfter',
                            },
                            forced: true,
                            popup: false,
                            usable: 1,
                            content() {
                                'step 0';
                                player.chooseTarget('令一名角色受到一点火属性伤害', function (card, player, target) {
                                    return player != target;
                                }).ai = function (target) {
                                    return get.damageEffect(target, null, player, player, 'fire');
                                };
                                ('step 1');
                                if (result.bool) {
                                    result.targets[0].damage(player, 'fire');
                                } else {
                                    player.getStat('triggerSkill').yunzhensha--;
                                    event.finish();
                                }
                            },
                            ai: {
                                expose: 0.3,
                            },
                            group: ['yunzhensha_discard'],
                            subSkill: {
                                discard: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        global: 'useCardAfter',
                                    },
                                    forced: true,
                                    popup: false,
                                    usable: 1,
                                    filter(event, player) {
                                        return (
                                            get.color(event.card) == 'black' &&
                                            game.countPlayer(function (current) {
                                                return current.countCards('h', 'du') > 0;
                                            })
                                        );
                                    },
                                    content() {
                                        'step 0';
                                        player
                                            .chooseTarget('弃置一名手牌中有【毒】的角色一张手牌', function (card, player, target) {
                                                return target.countCards('h', 'du') > 0;
                                            })
                                            .set('ai', function (target) {
                                                return get.attitude(player, target) < 0;
                                            });
                                        ('step 1');
                                        if (result.bool) {
                                            player.discardPlayerCard(1, result.targets[0], 'h');
                                        } else {
                                            player.getStat('triggerSkill').yunzhensha_discard--;
                                            event.finish();
                                        }
                                    },
                                    ai: {
                                        expose: 0.3,
                                    },
                                },
                            },
                        },
                        yunxiemu: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'useCardAfter',
                            },
                            forced: true,
                            popup: false,
                            usable: 1,
                            filter(event, player) {
                                var type = get.type(event.card);
                                if (type != 'equip') return false;
                                return event.card.name != 'muniu' && !get.cardtag(event.card, 'gifts');
                            },
                            content() {
                                'step 0';
                                player
                                    .chooseTarget('是否令一名角色获得' + get.translation(trigger.card) + '的装备效果', function (card, player, target) {
                                        return target != trigger.player;
                                    })
                                    .set('ai', function (target) {
                                        if (trigger.card.name != 'tengjia' && !target.hasSkill('yunxiemu_equip') && get.attitude(player, target) > 0) return 2;
                                        if (trigger.card.name == 'tengjia' && target.getEquip('baiyin') && get.attitude(player, target) > 0) return 1.5;
                                        if (trigger.card.name == 'baiyin' && target.getEquip('tengjia') && get.attitude(player, target) > 0) return 1.5;
                                        if (trigger.card.name != 'tengjia' && get.attitude(player, target) > 0) return 1;
                                        return 0;
                                    });
                                ('step 1');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    event.target = target;
                                    player.line(target);
                                    target.removeSkill('yunxiemu_equip');
                                } else {
                                    player.getStat('triggerSkill').yunxiemu--;
                                    event.finish();
                                }
                                ('step 2');
                                target.addSkill('yunxiemu_equip');
                                ('step 3');
                                var name = trigger.card.name;
                                target.storage.yunxiemu_equip2 = name;
                                target.markAuto('yunxiemu_equip', trigger.card);
                                var info = lib.card[name].skills;
                                if (info && info.length) target.addAdditionalSkill('yunxiemu_equip', info);
                                game.log(target, '获得了', '#y' + get.translation(name));
                            },
                            ai: {
                                expose: 0.3,
                            },
                            group: ['yanhanzuoyi'],
                        },
                        yanhanzuoyi: {
                            audio: 'yunxiemu',
                            trigger: {
                                player: 'phaseDiscardEnd',
                            },
                            popup: false,
                            forced: true,
                            filter(event, player) {
                                return (
                                    game.countPlayer(function (current) {
                                        return current != player && current.hasSkill('yunxiemu_equip');
                                    }) && player.hasSkill('yunxiemu')
                                );
                            },
                            content() {
                                'step 0';
                                if (trigger.cards && trigger.cards.length) {
                                    player
                                        .chooseTarget('是否令一名角色失去【协穆】装备效果', '' + (trigger.cards.length ? '获得' + get.translation(trigger.cards) + '？' : '') + '', function (card, player, target) {
                                            return target != player && target.hasSkill('yunxiemu_equip');
                                        })
                                        .set('ai', function (target) {
                                            var num = trigger.cards.length;
                                            if (num <= 1) return get.attitude(player, target) <= 0;
                                            if (num > 1) return get.attitude(player, target);
                                        });
                                } else {
                                    player
                                        .chooseTarget('是否令一名角色失去【协穆】装备效果', function (card, player, target) {
                                            return target != player && target.hasSkill('yunxiemu_equip');
                                        })
                                        .set('ai', function (target) {
                                            return get.attitude(player, target) <= 0;
                                        });
                                }
                                ('step 1');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    event.target = target;
                                    player.line(target);
                                    target.removeSkill('yunxiemu_equip');
                                    if (trigger.cards && trigger.cards.length) {
                                        target.gain(trigger.cards, player, 'gain2');
                                    }
                                }
                            },
                        },
                        yunxiemu_equip: {
                            charlotte: true,
                            mod: {
                                globalFrom(from, to, distance) {
                                    var info = lib.card[from.storage.yunxiemu_equip2];
                                    if (info && info.distance && info.distance.globalFrom) return distance + info.distance.globalFrom;
                                },
                                globalTo(from, to, distance) {
                                    var info = lib.card[to.storage.yunxiemu_equip2];
                                    if (info && info.distance && info.distance.globalTo) return distance + info.distance.globalTo;
                                },
                                attackRange(from, distance) {
                                    var info = lib.card[from.storage.yunxiemu_equip2];
                                    if (info && info.distance && info.distance.attackFrom) return distance - info.distance.attackFrom;
                                },
                                attackTo(from, to, distance) {
                                    var info = lib.card[to.storage.yunxiemu_equip2];
                                    if (info && info.distance && info.distance.attackTo) return distance + info.distance.attackTo;
                                },
                            },
                            mark: true,
                            marktext: '协穆',
                            intro: {
                                markcount: '协穆',
                                mark(dialog, storage, player) {
                                    dialog.addText('当前装备:' + get.translation(player.storage.yunxiemu_equip2));
                                    var str2 = lib.translate[player.storage.yunxiemu_equip2 + '_info'];
                                    if (str2) {
                                        if (str2.length >= 12) dialog.addText(str2, false);
                                        else dialog.addText(str2);
                                    }
                                },
                                onunmark(storage, player) {
                                    player.removeAdditionalSkill('yunxiemu_equip');
                                    delete player.storage.yunxiemu_equip2;
                                    player.addEquipTrigger();
                                },
                            },
                        },
                        yunzishu: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: ['phaseZhunbeiBefore', 'phaseDrawBefore', 'phaseUseBefore', 'phaseDiscardBefore', 'phaseJieshuBefore'],
                            },
                            popup: false,
                            forced: true,
                            silent: true,
                            content() {
                                player.draw();
                            },
                            group: ['yunzishu_draw'],
                            subSkill: {
                                draw: {
                                    trigger: {
                                        player: 'phaseDrawBegin',
                                    },
                                    popup: false,
                                    forced: true,
                                    filter(event, player) {
                                        return !event.numFixed;
                                    },
                                    content() {
                                        trigger.cancel();
                                        player.moveCard();
                                    },
                                },
                            },
                        },
                        yunfulang: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                source: 'damageSource',
                            },
                            mark: true,
                            marktext: '缚浪',
                            init(player) {
                                player.storage.yunfulang_yi = 0;
                                player.storage.yunfulang_er = 0;
                                player.storage.yunfulang_san = 0;
                                player.storage.yunfulang_si = 0;
                                player.storage.yunfulang_wu = 0;
                            },
                            intro: {
                                content(storage, player, skill) {
                                    var jlyi = player.storage.yunfulang_yi;
                                    var jler = player.storage.yunfulang_er;
                                    var jlsan = player.storage.yunfulang_san;
                                    var jlsi = player.storage.yunfulang_si;
                                    var jlwu = player.storage.yunfulang_wu;
                                    var str = '宁轻侠杀人,藏舍亡命,闻于郡中.其出入,步则陈车骑,水则连轻舟,侍从被文绣,所如光道路,住止常以缯锦维舟,去或割弃,以示奢也.';
                                    if (jlyi >= 1) {
                                        str += '<li>【缚浪】已获得体力上限:' + jlyi + '';
                                    }
                                    if (jler >= 1) {
                                        str += '<li>【缚浪】已获得摸牌数:' + jler + '';
                                    }
                                    if (jlsan >= 1) {
                                        str += '<li>【缚浪】已获得手牌上限:' + jlsan + '';
                                    }
                                    if (jlsi >= 1) {
                                        str += '<li>【缚浪】已获得攻击范围:' + jlsi + '';
                                    }
                                    if (jlwu >= 1) {
                                        str += '<li>【缚浪】已获得技能:' + jlwu + '';
                                    }
                                    return str;
                                },
                            },
                            check(event, player) {
                                return get.attitude(player, event.player) <= 0;
                            },
                            filter(event, player) {
                                if (event.player.isDead() || event.player == player) return false;
                                return player.storage.fulang_yi != 1 || player.storage.fulang_er != 1 || player.storage.fulang_san != 1 || player.storage.fulang_si != 1 || player.storage.fulang_wu != 1;
                            },
                            prompt(event, player) {
                                var str = '';
                                str += '是否对' + get.translation(event.player) + '发动【缚浪】？';
                                return str;
                            },
                            content() {
                                'step 0';
                                var list = [],
                                    choiceList = ['令' + get.translation(trigger.player) + '失去一点体力上限,你增加一点体力上限.', '令' + get.translation(trigger.player) + '摸牌阶段摸牌数减一,你摸牌阶段摸牌数加一.', '令' + get.translation(trigger.player) + '手牌上限减一,你手牌上限加一.', '令' + get.translation(trigger.player) + '攻击范围减一,你攻击范围加一.', '令' + get.translation(trigger.player) + '武将一个技能失效,你获得此技能.'];
                                if (player.storage.fulang_yi != 1) {
                                    list.push('选项一');
                                } else choiceList[0] = '<span style="opacity:0.5">' + choiceList[0] + '</span>';
                                if (player.storage.fulang_er != 1) {
                                    list.push('选项二');
                                } else choiceList[1] = '<span style="opacity:0.5">' + choiceList[1] + '</span>';
                                if (player.storage.fulang_san != 1) {
                                    list.push('选项三');
                                } else choiceList[2] = '<span style="opacity:0.5">' + choiceList[2] + '</span>';
                                if (player.storage.fulang_si != 1) {
                                    list.push('选项四');
                                } else choiceList[3] = '<span style="opacity:0.5">' + choiceList[3] + '</span>';
                                if (player.storage.fulang_wu != 1) {
                                    list.push('选项五');
                                } else choiceList[4] = '<span style="opacity:0.5">' + choiceList[4] + '</span>';
                                player
                                    .chooseControl(list, 'cancel2')
                                    .set('choiceList', choiceList)
                                    .set('ai', function (event, player) {
                                        if (player.storage.fulang_yi != 1) {
                                            return '选项一';
                                        }
                                        if (player.storage.fulang_er != 1) {
                                            return '选项二';
                                        }
                                        if (player.storage.fulang_san != 1) {
                                            return '选项三';
                                        }
                                        if (player.storage.fulang_si != 1) {
                                            return '选项四';
                                        }
                                        if (player.storage.fulang_wu != 1) {
                                            return '选项五';
                                        }
                                    })
                                    .set('listx', list)
                                    .set('prompt', get.prompt('yunfulang'));
                                ('step 1');
                                if (result.control == '选项一') {
                                    player.storage.fulang_yi = 1;
                                    player.gainMaxHp();
                                    trigger.player.loseMaxHp();
                                    trigger.player.addSkill('yunfulang_yi');
                                    player.storage.yunfulang_yi++;
                                    event.finish();
                                }
                                if (result.control == '选项二') {
                                    player.storage.fulang_er = 1;
                                    trigger.player.addSkill('yunfulang_er');
                                    player.storage.yunfulang_er++;
                                    event.finish();
                                }
                                if (result.control == '选项三') {
                                    player.storage.fulang_san = 1;
                                    trigger.player.addSkill('yunfulang_san');
                                    player.storage.yunfulang_san++;
                                    event.finish();
                                }
                                if (result.control == '选项四') {
                                    player.storage.fulang_si = 1;
                                    trigger.player.addSkill('yunfulang_si');
                                    player.storage.yunfulang_si++;
                                    event.finish();
                                }
                                if (result.control == '选项五') {
                                    event.goto(3);
                                }
                                ('step 2');
                                event.finish();
                                ('step 3');
                                var list = [];
                                var listm = [];
                                var listv = [];
                                if (trigger.player.name1 != undefined) listm = lib.character[trigger.player.name1][3];
                                else listm = lib.character[trigger.player.name][3];
                                if (trigger.player.name2 != undefined) listv = lib.character[trigger.player.name2][3];
                                listm = listm.concat(listv);
                                var func = function (skill) {
                                    var info = get.info(skill);
                                    if (!info || info.charlotte || info.zhuSkill || info.juexingji || info.dutySkill || info.hiddenSkill) return false;
                                    return true;
                                };
                                for (var i = 0; i < listm.length; i++) {
                                    if (func(listm[i])) list.add(listm[i]);
                                }
                                event.skills = list;
                                ('step 4');
                                if (event.skills.length) {
                                    player
                                        .chooseControl(event.skills)
                                        .set('prompt', '请选择要获得的技能')
                                        .set('ai', function () {
                                            return event.skills.randomGet();
                                        });
                                } else event.finish();
                                ('step 5');
                                player.addSkill(result.control);
                                player.popup(result.control, 'thunder');
                                player.storage.yun_fulang_wu = [result.control];
                                player.storage.yun_fulang_wu_player = trigger.player;
                                trigger.player.storage.yun_fulang_wu = [result.control];
                                player.storage.fulang_wu = 1;
                                trigger.player.addSkill('yunfulang_wu');
                                player.storage.yunfulang_wu++;
                                game.log(player, '获得了技能', '#g【' + get.translation(result.control) + '】');
                            },
                            mod: {
                                maxHandcardBase(player, num) {
                                    if (player.storage.yunfulang_san) return (num += player.storage.yunfulang_san);
                                },
                                attackFrom(from, to, num) {
                                    if (from.storage.yunfulang_si > 0) return (num -= from.storage.yunfulang_si);
                                },
                            },
                            group: ['yunfulang_draw'],
                            subSkill: {
                                draw: {
                                    audio: 'yunfulang',
                                    trigger: {
                                        player: 'phaseDrawBegin2',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return !event.numFixed && player.storage.yunfulang_er > 0;
                                    },
                                    content() {
                                        trigger.num += player.storage.yunfulang_er;
                                    },
                                },
                            },
                        },
                        yunjinling: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'dieAfter',
                            },
                            forceDie: true,
                            forced: true,
                            popup: false,
                            content() {
                                if (trigger.player == player) {
                                    game.countPlayer(function (current) {
                                        if (current.hasSkill('yunfulang_yi')) {
                                            player.loseMaxHp();
                                            player.storage.yunfulang_yi = 0;
                                            current.gainMaxHp();
                                            current.removeSkill('yunfulang_yi');
                                        }
                                        if (current.hasSkill('yunfulang_er')) {
                                            player.storage.yunfulang_er = 0;
                                            current.removeSkill('yunfulang_er');
                                        }
                                        if (current.hasSkill('yunfulang_san')) {
                                            player.storage.yunfulang_san = 0;
                                            current.removeSkill('yunfulang_san');
                                        }
                                        if (current.hasSkill('yunfulang_si')) {
                                            player.storage.yunfulang_si = 0;
                                            current.removeSkill('yunfulang_si');
                                        }
                                        if (current.hasSkill('yunfulang_wu')) {
                                            player.removeSkill(player.storage.yun_fulang_wu);
                                            delete player.storage.yun_fulang_wu_player;
                                            player.storage.yun_fulang_wu = [];
                                            player.storage.yunfulang_wu = 0;
                                            current.removeSkill('yunfulang_wu');
                                            game.log(current, '回复了失去的技能');
                                        }
                                    });
                                }
                                if (trigger.player != player && (trigger.player.hasSkill('yunfulang_yi') || trigger.player.hasSkill('yunfulang_er') || trigger.player.hasSkill('yunfulang_san') || trigger.player.hasSkill('yunfulang_si') || trigger.player.hasSkill('yunfulang_wu'))) {
                                    if (trigger.player.hasSkill('yunfulang_yi')) {
                                        player.storage.fulang_yi = 0;
                                    }
                                    if (trigger.player.hasSkill('yunfulang_er')) {
                                        player.storage.fulang_er = 0;
                                    }
                                    if (trigger.player.hasSkill('yunfulang_san')) {
                                        player.storage.fulang_san = 0;
                                    }
                                    if (trigger.player.hasSkill('yunfulang_si')) {
                                        player.storage.fulang_si = 0;
                                    }
                                    if (trigger.player.hasSkill('yunfulang_wu')) {
                                        player.storage.fulang_wu = 0;
                                    }
                                }
                            },
                            mod: {
                                cardUsable(card, player, num) {
                                    if (player.storage.fulang_yi != 1 || player.storage.fulang_er != 1 || player.storage.fulang_san != 1 || player.storage.fulang_si != 1 || player.storage.fulang_wu != 1) {
                                        if (card.name == 'sha') {
                                            return num + 1;
                                        }
                                    }
                                },
                            },
                        },
                        yunfulang_yi: {
                            mark: true,
                            marktext: '缚浪',
                            intro: {
                                name: '缚浪',
                                content: '失去了一点体力上限',
                            },
                            charlotte: true,
                        },
                        yunfulang_er: {
                            mark: true,
                            marktext: '缚浪',
                            intro: {
                                name: '缚浪',
                                content: '摸牌阶段摸牌数减一',
                            },
                            forced: true,
                            charlotte: true,
                            trigger: {
                                player: 'phaseDrawBegin2',
                            },
                            filter(event, player) {
                                return !event.numFixed;
                            },
                            content() {
                                trigger.num--;
                            },
                        },
                        yunfulang_san: {
                            mark: true,
                            marktext: '缚浪',
                            intro: {
                                name: '缚浪',
                                content: '手牌上限减一',
                            },
                            charlotte: true,
                            mod: {
                                maxHandcardBase(player, num) {
                                    return (num -= 1);
                                },
                            },
                        },
                        yunfulang_si: {
                            mark: true,
                            marktext: '缚浪',
                            intro: {
                                name: '缚浪',
                                content: '攻击范围减一',
                            },
                            charlotte: true,
                            mod: {
                                attackFrom(from, to, num) {
                                    return (num += 1);
                                },
                            },
                        },
                        yunfulang_wu: {
                            mark: true,
                            marktext: '缚浪',
                            charlotte: true,
                            init(player, skill) {
                                player.disableSkill(skill, player.storage.yun_fulang_wu);
                            },
                            onremove(player, skill) {
                                player.enableSkill(skill);
                            },
                            intro: {
                                content(storage, player, skill) {
                                    let list = Object.keys(player.disabledSkills);
                                    if (list.length) {
                                        var str = '失效技能:';
                                        for (var i = 0; i < list.length; i++) {
                                            if (lib.translate[list[i] + '_info']) str += get.translation(list[i]) + '、';
                                        }
                                        return str.slice(0, str.length - 1);
                                    }
                                },
                            },
                        },
                        yunanle: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'useCardToPlayered',
                                target: 'useCardToTargeted',
                            },
                            forced: true,
                            filter(event, player) {
                                if (event.player == event.target || get.color(event.card) == 'none') return false;
                                return true;
                            },
                            content() {
                                'step 0';
                                player.judge(function (card) {
                                    if (get.effect(trigger.target, trigger.card, trigger.player, trigger.player) > 0) {
                                        if (get.color(trigger.card) == 'black') {
                                            if (get.color(card) == 'black') {
                                                return 1;
                                            }
                                            return -1;
                                        }
                                        if (get.color(trigger.card) == 'red') {
                                            if (get.color(card) == 'red') return 1;
                                            return -1;
                                        }
                                    }
                                    if (get.effect(trigger.target, trigger.card, trigger.player, trigger.player) < 0) {
                                        if (get.color(trigger.card) == 'black') {
                                            if (get.color(card) == 'black') {
                                                return -1;
                                            }
                                            return 1;
                                        }
                                        if (get.color(trigger.card) == 'red') {
                                            if (get.color(card) == 'red') return -1;
                                            return 1;
                                        }
                                    }
                                });
                                ('step 1');
                                if ((result.color == 'black' && get.color(trigger.card) == 'red') || (result.color == 'red' && get.color(trigger.card) == 'black')) {
                                    trigger.parent.excluded.add(trigger.target);
                                }
                            },
                            ai: {
                                effect: {
                                    target(card, player, target) {
                                        if (player != target) return [1, 0.5, 1, -0.5];
                                    },
                                },
                                threaten: 0,
                            },
                        },
                        yuntuozheng: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                player: 'phaseUseBefore',
                            },
                            forced: true,
                            content() {
                                'step 0';
                                player
                                    .chooseTarget('是否立即结束回合并令一名其他角色执行一个额外回合', function (card, player, target) {
                                        return player != target && !target.hasSkill('yun_tuozheng_off');
                                    })
                                    .set('ai', function (target) {
                                        var player = _status.event.player;
                                        var num = target.countCards('h');
                                        var att = get.attitude(player, target);
                                        if (get.attitude(player, target) > 0 && (target.hasJudge('lebu') || target.classList.contains('turnedover'))) {
                                            return -1;
                                        }
                                        if (get.attitude(player, target) > 0 && !target.hasJudge('lebu') && !target.classList.contains('turnedover')) {
                                            return 10 * att + num;
                                        }
                                        return 0;
                                    });
                                ('step 1');
                                if (result.bool) {
                                    player.say(['全凭相父做主', '祭由寡人,政由相父', '出去玩咯～'].randomGet());
                                    var target = result.targets[0];
                                    target.addTempSkill('yun_tuozheng_off', 'roundStart');
                                    target.addMark('yuntuozheng_mark');
                                    target.phase('nodelay');
                                } else {
                                    event.finish();
                                }
                                ('step 2');
                                trigger.cancel();
                                var evt = _status.event.getParent('phaseUse');
                                if (evt && evt.name == 'phaseUse') {
                                    evt.skipped = true;
                                }
                                var evt = _status.event.getParent('phase');
                                if (evt && evt.name == 'phase') {
                                    evt.finish();
                                }
                            },
                            group: ['yuntuozheng_die', 'yuntuozheng_phase'],
                            subSkill: {
                                die: {
                                    audio: 'ext:云将/audio:2',
                                    trigger: {
                                        global: 'dieEnd',
                                    },
                                    forced: true,
                                    filter(event, player) {
                                        return event.player.countMark('yuntuozheng_mark') > 0;
                                    },
                                    content() {
                                        'step 0';
                                        player.storage.yuntuozheng_mark += trigger.player.countMark('yuntuozheng_mark');
                                        ('step 1');
                                        event.trigger('yuntuozheng_phase');
                                    },
                                },
                                phase: {
                                    trigger: {
                                        global: ['phaseEnd', 'yuntuozheng_phase'],
                                    },
                                    forced: true,
                                    init(player) {
                                        player.storage.yuntuozheng_mark = 0;
                                    },
                                    filter(event, player) {
                                        return player.storage.yuntuozheng_mark > 0;
                                    },
                                    content() {
                                        player.storage.yuntuozheng_mark--;
                                        player.phase('nodelay');
                                    },
                                },
                            },
                        },
                        yuntuozheng_mark: {
                            charlotte: true,
                            mark: true,
                            marktext: '托政',
                            intro: {
                                name2: '托政',
                                content: '已被【托政】#次',
                            },
                        },
                        yun_tuozheng_off: {
                            charlotte: true,
                        },
                        yunhuairen: {
                            audio: 'ext:云将/audio:2',
                            trigger: {
                                global: 'phaseBefore',
                                player: 'enterGame',
                            },
                            forced: true,
                            zhuSkill: true,
                            limited: true,
                            mark: false,
                            filter(event, player) {
                                return player.hasZhuSkill('yunhuairen') && (event.name != 'phase' || game.phaseNumber == 0);
                            },
                            content() {
                                'step 0';
                                player
                                    .chooseTarget('怀仁:选择一名其他角色为【怀仁】目标', '若其『蜀』势力角色则令其增加一点体力上限', function (card, player, target) {
                                        return player != target;
                                    })
                                    .set('ai', function (target) {
                                        return get.attitude(_status.event.player, target) > 0;
                                    });
                                ('step 1');
                                if (result.bool) {
                                    var target = result.targets[0];
                                    if (target.group == 'shu') target.gainMaxHp();
                                    target.addSkill('yunhuairen_Buff');
                                }
                                ('step 2');
                            },
                        },
                        yunhuairen_Buff: {
                            charlotte: true,
                        },
                    },
                    //武将列表
                    character: {
                        yunyouzi: ['male', 'shen', 5, ['yungkd'], ['zhu', 'des:我只想当一只安静的狗子,享受纯粹的快乐!<br><br><br><br><br><br><br>———作者养的狗']],
                        yunhanyun: ['male', 'shen', 3, ['yunshenzi'], ['zhu', 'des:吾之所好,杀人诛心!<br><br><br><br><br><br><br>———作者DIY人物,因为非正史或演义角色,所以设定为仅玩家可使用']],
                        yunjihanyun: ['male', 'shen', 3, ['yunjilue'], ['zhu', 'des:来,让我看一出好戏吧.<br><br><br><br><br><br><br>———作者DIY人物,因为非正史或演义角色,所以设定为仅玩家可使用']],
                        yunshenhanyun: ['male', 'shen', 3, ['yunshenquan'], ['zhu', 'des:天命难违？哈哈哈哈!<br><br><br><br><br><br><br>———作者DIY人物,因为非正史或演义角色,所以设定为仅玩家可使用']],
                        yunlvlingqi: ['female', 'qun', 3, ['yun_luanwu'], ['des:下次不要再往小树林里逃跑了哟～(￣▽￣～)~会从IF线跳到无惨线的哟╮(￣▽￣)╭<br><br><br><br><br><br><br>———百花缭乱&一世风华:专属武将']],
                        yunguojia: ['male', 'wei', 3, ['yunguimou', 'yunyice'], ['des:<如若奉孝在,何使孤至此……痛哉!奉孝……惜哉!奉孝……>———宇宙战神的狂热粉丝']],
                        yuncaocao: ['male', 'wei', 3, ['yunxionglue', 'yunxieling', 'yunzongyu'], ['zhu', 'des:<丞相何故发笑？><br><吾不笑别人,单笑周瑜无谋,诸葛亮少智.若是吾用兵之时,预先在这里伏下一军,如之奈何？><br><丞相何故……><br><吾笑诸葛亮、周瑜毕竟智谋不足.若是我用时,就这个去处,也埋伏一彪军马,以逸待劳;我等纵然脱得性命,也不免重伤矣.彼见不到此,我是以笑之.><br><丞相……你……><br><人皆言周瑜、诸葛亮足智多谋,以吾观之,到底是无能之辈.若使此处伏一旅之师,吾等皆束手受缚矣.><br><丞相你特么快闭嘴吧!求你了!>']],
                        yunlvbu: ['male', 'qun', 5, ['yuntanlang', 'yunwushuang'], ['zhu', 'des:<那是……是刘虎哒!!!刘虎出阵了!!> <快逃!!快!这种怪物我们怎么可能……> <鬼神!鬼神降临在虎牢关!!!神鬼惊怖!!!>']],
                        yunwangji: ['male', 'wei', 3, ['yunqizhi'], ['des:别特么再问吴懿跟我有什么关系了,我们不熟,没有任何绯闻!']],
                        yunzhugeliang: ['male', 'shu', 3, ['yuncangji', 'yunqixing', 'yunranghan'], ['des:臣必鞠躬尽瘁,死而后已,以报先帝三顾之恩!']],
                        yunlvmeng: ['male', 'wu', 4, ['yunshelue', 'yunkeji'], ['des:背后捅刀子这回事,还得是读书人下手才狠呀……']],
                        yunzhangliao: ['male', 'wei', '3/4', ['yunduorui', 'yunzhiti'], ['des:有一个男人武力高强,早年跟随一个不得人心的主公,始终不得出人头地,离开这个主公以后他纵横天下,无可匹敌.他有一个觉醒技,但觉醒条件十分困难,觉醒后不仅能让自己多过牌,还能帮队友补牌.他一生最大的功绩与东吴息息相关,他对孙权有着天然的压制力.他就是……<br><孙家天下孙……><br>(捂嘴～)<br><没错!就是我SP群张辽哒!>']],
                        yuncaishi: ['female', 'qun', 3, ['yunqieting', 'yunxianzhou'], ['des:青蛇竹儿口,黄蜂尾后针,两者皆不毒,最毒妇人心']],
                        yunsunshangxiang: ['female', 'wu', 4, ['yunjieyin', 'yunxiaoji'], ['des:你～愿不愿意和我在一起？']],
                        yunpangtong: ['male', 'shu', 3, ['yunxiance', 'yunfengming'], ['des:……欲火重生 !<br>浴……<br>欲!!<br>哦~']],
                        yunzhangchunhua: ['female', 'wei', 3, ['yunqingleng', 'yunshangshi'], ['des:司马仲达,如果有来世,我不要再遇见你!']],
                        yundongbai: ['female', 'qun', 3, ['yunzhulian', 'yunjiaoheng'], ['des:我要让爷爷定你们死罪!!!']],
                        yundongzuo: ['male', 'qun', 9, ['yunbaonve', 'yunchongni'], ['des:竟敢欺负我的宝贝孙女?薛刀,给我把这个杂碎剁了!']],
                        yunzhouyu: ['male', 'wu', 3, ['yunguqu', 'yunyeyan'], ['des:遥想公瑾当年,小乔初嫁了,雄姿英发.羽扇纶巾,谈笑间,樯橹灰飞烟灭']],
                        yunliubei: ['male', 'shu', 4, ['yunjieyi', 'yunzhaolie', 'yunsangu'], ['zhu', 'des:<为图大事,我漂流半生,苦苦寻找志同道合之人,直到今日淘尽狂沙始见真金,天可怜见,将二位英雄赐予刘备,备欲同你二人结拜为生死弟兄,不知二位意下如何？>']],
                        yunguanyu: ['male', 'shu', 4, ['yunyijue', 'yunguayin'], ['des:<关某虽一介武夫,亦颇知忠义二字,正所谓择木之禽,必栖良木,择主之臣,得遇明主.从今往后,关某之命即是刘兄之命,关某之躯即是刘兄之躯,但凭趋使,绝无二心!>']],
                        yunzhangfei: ['male', 'shu', 4, ['yunwanjun', 'yunxiongxu'], ['des:<俺也一样!>']],
                        yundiaochan: ['female', 'qun', 3, ['yunmeihuo', 'yunbiyue'], ['des:歌月徘徊孤楼前,舞影零游群雄间.如花朱颜非吾愿,香消玉殒惹谁怜？']],
                        yunsunce: ['male', 'wu', 4, ['yunyingyang', 'yunjiang', 'yunhenglie'], ['zhu', 'des:父亲在上,公瑾在旁,一拜天地,二拜高堂']],
                        yunxushi: ['female', 'wu', 3, ['yunbugua', 'yunfuzhu'], ['des:我连做梦都在等这一天呢!!!']],
                        yungongsunzhan: ['male', 'qun', 4, ['yunyicong', 'yunzhujing'], ['des:伯珪疏犷,武才趫猛.虞好无终,绍势难并']],
                        db_yunjiaxu: ['male', 'qun', 3, ['yunjiance', 'yunwansha', 'yunjimou', 'fengyunshibian_jx'], ['doublegroup:qun:wei', 'des:吾之所求,不过是安身立命而已,天下人的死活,与我何干？']],
                        yunzhaoyun: ['male', 'shu', 3, ['yunlongdan', 'yunlonglie'], ['des:捂奶长衫造纸农!']],
                        yunhuangyueying: ['female', 'shu', 3, ['yunlinglong'], ['des:孔明大人……']],
                        yunxunyu: ['male', 'wei', 3, ['yunyuhu', 'yunwangzuo'], ['des:令君留香,君子如彧']],
                        yunzhanghe: ['male', 'wei', 4, ['yunqiaobian', 'yunshanlue'], ['des:我曾经翻山越岭,直到膝盖中了一箭……']],
                        yunsimayi: ['male', 'wei', 3, ['yunrenjie', 'yunyingshi'], ['des:出来混一定不要讲信用,说不杀他全家就不杀他全家？那我还怎么混啊？!']],
                        yunsunquan: ['male', 'wu', 3, ['yunzhiheng', 'yunhuju', 'yunjianye'], ['zhu', 'des:十万对八百,此战优势在我!<br>你八百兵能打赢我？你今天要是能赢我,我当场把这十万大军给吃了!<br>十万大军骑脸我都不知道要怎么输!']],
                        yuncaoying: ['female', 'wei', 3, ['yunlingren'], ['des:老将军虎威犹在,可惜命不久矣!<br>将军一副好骨,不如留于此山？<br>将军这些把戏,可难不倒我哦.<br>我已在此,等候将军多时了!<br>你这点小心思,我还会猜不透？<br>你输了,这些就要归我.<br><br>———曹婴的世界只有将军(๑>؂<๑)!!!']],
                        yunzuoci: ['male', 'qun', 3, ['yunyugui'], ['des:谁说化身不算女装？']],
                        yunjiangwei: ['male', 'shu', 4, ['yunchengzhi', 'yunqilin'], ['des:丞相遗志,伯约竭力而为,奈何大汉国运衰微至此!丞相……伯约……力尽矣……']],
                        yunzhangjiao: ['male', 'qun', 3, ['yunchuandao', 'yundunshu', 'yunhuangtian'], ['zhu', 'des:贫道张角,请大汉赴死!']],
                        db_yunmachao: ['male', 'qun', '3/4', ['yunshichou', 'yunjinshi', 'fengyunshibian_mc'], ['doublegroup:qun:shu', 'des:今超弃父,以将军为父,将军亦当弃子,以超为子']],
                        yuncaiwenji: ['female', 'wei', 3, ['yunmoshi', 'yunbeige'], ['des:我生之初尚无为,我生之后汉祚衰.天不仁兮降乱离,地不仁兮使我逢此时.干戈日寻兮道路危,民卒流亡兮共哀悲.烟尘蔽野兮胡虏盛,志意乖兮节义亏.对殊俗兮非我宜,遭忍辱兮当告谁？笳一会兮琴一拍,心愤怨兮无人知.<br><br>戎羯逼我兮为室家,将我行兮向天涯.云山万重兮归路遐,疾风千里兮扬尘沙.人多暴猛兮如虺蛇,控弦被甲兮为骄奢.两拍张弦兮弦欲绝,志摧心折兮自悲嗟.<br><br>越汉国兮入胡城,亡家失身兮不如无生.毡裘为裳兮骨肉震惊,羯羶为味兮枉遏我情.鼙鼓喧兮从夜达明,胡风浩浩兮暗塞营.伤今感晋兮三拍成,衔悲畜恨兮何时平.<br><br>无日无夜兮不思我乡土,禀气合生兮莫过我最苦.天灾国乱兮人无主,唯我薄命兮没戎虏.殊俗心异兮身难处,嗜欲不同兮谁可与语!寻思涉历兮多艰阻,四拍成兮益凄楚.<br><br>雁南征兮欲寄边声,雁北归兮为得汉音.雁飞高兮邈难寻,空断肠兮思愔愔.攒眉向月兮抚雅琴,五拍泠泠兮意弥深.<br><br>冰霜凛凛兮身苦寒,饥对肉酪兮不能餐.夜间陇水兮声呜咽,朝见长城兮路杳漫.追思往日兮行李难,六拍悲来兮欲罢弹.<br><br>日暮风悲兮边声四起,不知愁心兮说向谁是!原野萧条兮烽戍万里,俗贱老弱兮少壮为美.逐有水草兮安家葺垒,牛羊满野兮聚如蜂蚁.草尽水竭兮羊马皆徙,七拍流恨兮恶居于此.<br><br>为天有眼兮何不见我独漂流？为神有灵兮何事处我天南海北头？我不负天兮天何配我殊匹？我不负神兮神何殛我越荒州？制兹八拍兮拟排忧,何知曲成兮心转愁.<br><br>天无涯兮地无边,我心愁兮亦复然.人生倏忽兮如白驹之过隙,然不得欢乐兮当我之盛年.怨兮欲问天,天苍苍兮上无缘.举头仰望兮空云烟,九拍怀情兮谁与传？<br><br>城头烽火不曾灭,疆场征战何时歇？杀气朝朝冲塞门,胡风夜夜吹边月.故乡隔兮音生绝,哭无声兮气将咽.一生辛苦兮缘别离,十拍悲深兮泪成血.<br><br>我非食生而恶死,不能捐身兮心有以.生仍冀得兮归桑梓,死当埋骨兮长已矣.日居月诸兮在戎垒,胡人宠我兮有二子.鞠之育之兮不羞耻,憋之念之兮生长边鄙.十有一拍兮因兹起,哀响缠绵兮彻心髓.<br><br>东风应律兮暖气多,知是汉家天子兮布阳和.羌胡蹈舞兮共讴歌,两国交欢兮罢兵戈.忽遇汉使兮称近诏,遗千金兮赎妾身.喜得生还兮逢圣君,嗟别稚子兮会无因.十有二拍兮哀乐均,去住两情兮难具陈.<br><br>不谓残生兮却得旋归,抚抱胡儿兮注下沾衣.汉使迎我兮四牡騑騑,胡儿号兮谁得知？与我生死兮逢此时,愁为子兮日无光辉,焉得羽翼兮将汝归.一步一远兮足难移,魂消影绝兮恩爱遗.十有三拍兮弦急调悲,肝肠搅刺兮人莫我知.<br><br>身归国兮儿莫之随,心悬悬兮长如饥.四时万物兮有盛衰,唯我愁苦兮不暂移.山高地阔兮见汝无期,更深夜阑兮梦汝来斯.梦中执手兮一喜一悲,觉后痛吾心兮无休歇时.十有四拍兮涕泪交垂,河水东流兮心是思.<br><br>十五拍兮节调促,气填胸兮谁识曲？处穹庐兮偶殊俗.愿得归来兮天从欲,再还汉国兮欢心足.心有怀兮愁转深,日月无私兮曾不照临.子母分离兮意难怪,同天隔越兮如商参,生死不相知兮何处寻!<br><br>十六拍兮思茫茫,我与儿兮各一方.日东月西兮徒相望,不得相随兮空断肠.对萱草兮忧不忘,弹鸣琴兮情何伤!今别子兮归故乡,旧怨平兮新怨长!泣血仰头兮诉苍苍,胡为生兮独罹此殃!<br><br>十七拍兮心鼻酸,关山阻修兮行路难.去时怀土兮心无绪,来时别儿兮思漫漫.塞上黄蒿兮枝枯叶干,沙场白骨兮刀痕箭瘢.风霜凛凛兮春夏寒,人马饥豗兮筋力单.岂知重得兮入长安,叹息欲绝兮泪阑干.<br> <br>胡笳本自出胡中,缘琴翻出音律同.十八拍兮曲虽终,响有余兮思无穷.是知丝竹微妙兮均造化之功,哀乐各随人心兮有变则通.胡与汉兮异域殊风,天与地隔兮子西母东.苦我怨气兮浩于长空,六合虽广兮受之应不容!']],
                        yunsunjian: ['male', 'wu', 4, ['yunpolu', 'yunwulie'], ['zhu', 'des:此去泉台招旧部,旌旗十万斩阎罗!']],
                        yunluxun: ['male', 'wu', 3, ['yuntaolue', 'yunqianya'], ['des:吴中有连清人者,名曰陆逊,字伯言.逊为人谦雅亦有闭月羞花之容,倾国倾城之色,吴人见之,皆叹惋.逊自比八神之神,八神闻之,皆不服,之其处于其相辨.仲谋先至,逊笑而问曰:<汝有何能哉？>权曰:<吾善换牌也,杀换闪,闪换桃,桃换无懈,制衡之道,敌未能测也!>逊对曰:<敌若顺之汝牌,卸之汝甲,盖使汝善制衡,无牌者,以何换之？>权闻之,无言以对,羞然退也.少焉,七神皆至,逊端坐于室,神情自若,仰天笑曰:<吾通汝之弊处甚矣!>七神皆大惊,心中暗曰:<此儒子为何如此了得？吾观日后其必超神矣!>逊侃侃道来:<彧,善补牌也,三张鸡肋也,四张不足也,五张多乎哉？吾之连营,贯彻于全牌之中,变化于瞬息之间,三十余张亦不足挂齿.辽、术二人,汝皆惧兵也,敌若兵吾,吾即连营,摧枯拉朽之势,敌闻之皆丧胆.备、肃二人,汝固善仁望汝之牌、换敌我之牌,但汝之防御不堪一击,以致为敌突破之处,哀哉!诸葛孔明,观星何用？汝未曾闻,天命难违？吾料蜀汉必亡矣!貂蝉,汝为妇人,其貌却不如吾,吾不做多言!>七神皆哑然,跪拜逊为当世神将']],
                        db_yundaxiaoqiao: ['female', 'wu', 3, ['yunguose', 'yuntianxiang', 'fengyunshibian_dxq'], ['doublegroup:qun:wu', 'des:揽二乔于东南兮,乐朝夕之与共.——丞相,我悟了!']],
                        yuncaopi: ['male', 'wei', 3, ['yunduodi', 'yunxingshang', 'yundengwei'], ['zhu', 'des:甄姬拔菜啊!']],
                        yunzhaoxiang: ['female', 'shu', 4, ['yunfanghun'], ['des:我感受到了……仙贝们的力量!']],
                        yunfazheng: ['male', 'shu', 3, ['yunyazi'], ['des:孝直最是知恩图报!嗯～有仇？开玩笑!我都没有什么仇人的啦.(法正拎着昨天吵架对手的人头如此说到)']],
                        yunxuhuang: ['male', 'wei', 4, ['yunchangqu', 'yunpoqian'], ['des:<贼围堑鹿角十重,将军致战全胜,遂陷贼围,多斩首虏.吾用兵三十馀年,及所闻古之善用兵者,未有长驱径入敌围者也.且樊、襄阳之在围,过于莒、即墨,将军之功,逾孙武、穰苴.徐将军可谓有周亚夫之风矣!><br><老板夸人不算奖金的吧？>']],
                        yunhuangzhong: ['male', 'shu', 4, ['yunliegong', 'yunposhi'], ['des:都说关羽猛得一批,我一点都不慌,只要一个滑铲……!!!']],
                        yunliuxie: ['male', 'qun', 4, ['yunxuezhao', 'yuntianming', 'yunhanzuo'], ['zhu', 'des:来人啊!命曹皇后今晚穿丞相制服等着朕!']],
                        yundianwei: ['male', 'wei', 4, ['yunhuwei', 'yunelai'], ['des:曹公速走!!!']],
                        yunlusu: ['male', 'wu', 3, ['yundingding', 'yunhaoshi'], ['des:<子敬乃是实诚人也～><啊～对对对～>']],
                        yunlejin: ['male', 'wei', 4, ['yunxiandeng', 'yunxiaolie'], ['des:<喂～那边那个矮子,五尺差半寸的那个,就是你,矮要承认挨打站稳……诶诶诶……诶……别打脸……>']],
                        yunyujin: ['male', 'wei', 4, ['yunzhenjun', 'yunyizhong'], ['des:战无不胜的左将军即将与红脸贼对阵!<br> 文则将军与关羽相持不下!<br>关云长水淹七军,于禁大败!<br>贪生怕死的无义之徒投降了威震华夏的武圣! ']],
                        yunhuaman: ['female', 'shu', 4, ['yunmanshi', 'yunzhanyuan'], ['des:<这是个孩子？><br><是孩子.><br><几岁？> <br><十六……><br><这他妈十六岁？!!!><br><没人信,我都不信,八岁就颇具规模,十二岁就跟她妈一样大,十六岁就已然这样了!>']],
                        yunyuanshao: ['male', 'qun', 4, ['yunshiguan', 'yunxiongjie', 'yunlisi'], ['zhu', 'des:呦……这不曹阿瞒嘛？几天不见怎么这么扌……我艹!!!我乌巢呢？!!']],
                        yuntaishici: ['male', 'wu', 4, ['yuntianyi', 'yunaozhan'], ['des:周瑜:<为什么会变成这样呢……第一次,称霸了江东,第一次交到了一生的挚友.这两件愉快的事情交织在了一起.而这两份喜悦,又会给我带来许许多多的喜悦.我本应该获得了这种如梦一般的幸福时光才对.可是,为什么,会变成现在这样呢……明明是我先来的,认识伯符也好,一起战斗也好……><br><br>太史慈:<伯符,速速与我一战!>']],
                        yundengai: ['male', 'wei', 4, ['yunjiliang', 'yunpoxian'], ['des:<请问一下邓艾将军,你是带着什么样的壮志才能带头冲下摩天岭？从而带领魏国大军获得胜利的呢？><br><妈……妈……><br><哦？你是要在这成功的时刻先感谢母亲吗？你想对家人说些什么吗？<br><……的……谁……谁……谁推……推的我？>>']],
                        yunzhenfu: ['female', 'wei', 3, ['yunpianhong'], ['des:曹植:<父亲,嫂嫂昨晚对我说你比袁本初要厉害><br>曹操:<哈？她居然跟袁绍也有一……？><br>曹丕:<？？？!!!>']],
                        yunyuanshu: ['male', 'qun', '4/6', ['yunwangzun'], ['zhu', 'des:朕口渴了,朕要喝快乐水!!!']],
                        yunlukang: ['male', 'wu', 3, ['yunjingwu', 'yundizhu'], ['des:大逆风队友摆烂,老板发疯,对面还有小代,怎么玩啊？']],
                        yunliru: ['male', 'qun', 3, ['yunduce', 'yunzhensha'], ['des:时代的骄傲侬晓得伐？']],
                        yunmaliang: ['male', 'shu', 3, ['yunxiemu', 'yunzishu'], ['des:神笔？我不造啊？']],
                        yunganning: ['male', 'wu', 4, ['yunfulang', 'yunjinling'], ['des:孙权:<一百都这样了,十万……十万不得起飞咯哇？>']],
                        yunliushan: ['male', 'shu', 3, ['yunanle', 'yuntuozheng', 'yunhuairen'], ['zhu', 'des:<当年赵四叔跟在小爷后面从长坂坡头砍到长坂坡尾,眼睛都不眨一下!><br><那你眼睛干不干?>']],
                    },

                    characterSort: {
                        云将: {
                            weiwuyangbian: ['yuncaocao', 'yuncaopi', 'yuncaoying', 'yunxunyu', 'yunguojia', 'yunzhangliao', 'yunzhanghe', 'yunwangji', 'yuncaiwenji', 'yunsimayi', 'yunzhangchunhua', 'yunxuhuang', 'yundianwei', 'yunlejin', 'yunyujin', 'yundengai', 'yunzhenfu'],
                            zhaoliejihan: ['yunliubei', 'yunguanyu', 'yunzhangfei', 'yunzhaoyun', 'yunzhaoxiang', 'yunzhugeliang', 'yunpangtong', 'yunhuangyueying', 'yunjiangwei', 'yunfazheng', 'yunhuangzhong', 'yunhuaman', 'yunmaliang', 'yunliushan'],
                            hujujiangdong: ['yuntaishici', 'yunsunjian', 'yunsunce', 'yunsunquan', 'yunsunshangxiang', 'yunzhouyu', 'yunlvmeng', 'yunluxun', 'yunxushi', 'yunlusu', 'yunlukang', 'yunganning'],
                            qunxiongluanwu: ['yunyuanshao', 'yunliuxie', 'yunlvbu', 'yundiaochan', 'yunzhangjiao', 'yunzuoci', 'yuncaishi', 'yundongbai', 'yungongsunzhan', 'yunlvlingqi', 'yunyuanshu', 'yunliru'],
                            fengyunshibian: ['db_yunjiaxu', 'db_yunmachao', 'db_yundaxiaoqiao'],
                            qianlongzaiyuan: ['yunhanyun', 'yunjihanyun', 'yunshenhanyun'],
                        },
                    },

                    characterTitle: {
                        db_yunjiaxu: '乱武',
                        db_yunmachao: '神威天将',
                        db_yundaxiaoqiao: '并蒂花',
                        yunguojia: '鬼才',
                        yuncaocao: '乱世枭雄',
                        yunlvbu: '虓虎',
                        yunwangji: '学行坚白',
                        yunzhugeliang: '卧龙',
                        yunlvmeng: '折节筹略',
                        yunzhangliao: '古之召虎',
                        yuncaishi: '蛇蝎夫人',
                        yunsunshangxiang: '弓腰姬',
                        yunpangtong: '凤雏',
                        yunguanyu: '义绝古今',
                        yunzhangchunhua: '清冷多姿',
                        yundongbai: '长乐未央',
                        yundongzuo: '乱汉之始',
                        yunzhouyu: '大都督',
                        yunliubei: '昭烈帝',
                        yunzhangfei: '万人敌',
                        yundiaochan: '闭月羞花',
                        yunsunce: '小霸王',
                        yunxushi: '节义烈女',
                        yungongsunzhan: '白马将军',
                        yunzhaoyun: '一身皆胆',
                        yunhuangyueying: '慧心巧手',
                        yunxunyu: '王佐之才',
                        yunzhanghe: '善战巧变',
                        yunsimayi: '鹰视狼顾',
                        yunsunquan: '东吴大帝',
                        yuncaoying: '魏缨凤鸣',
                        yunzuoci: '幻之仙人',
                        yunjiangwei: '麒麟儿',
                        yunzhangjiao: '天公将军',
                        yuncaiwenji: '汉末悲歌',
                        yunsunjian: '江东猛虎',
                        yunluxun: '雄略儒将',
                        yuncaopi: '魏文帝',
                        yunzhaoxiang: '梅踪芳迹',
                        yunfazheng: '睚眦必报',
                        yunxuhuang: '长驱直入',
                        yunhuangzhong: '百步穿杨',
                        yunliuxie: '汉献帝',
                        yundianwei: '古之恶来',
                        yunlusu: '定鼎东吴',
                        yunlejin: '百战不殆',
                        yunyujin: '毅重御严',
                        yunhuaman: '南中俏色',
                        yunyuanshao: '雄视海内',
                        yuntaishici: '信义笃烈',
                        yundengai: '筹画有方',
                        yunlvlingqi: '虓姬',
                        yunzhenfu: '洛神',
                        yunyuanshu: '名门悍鬼',
                        yunlukang: '奕世载美',
                        yunliru: '毒士',
                        yunmaliang: '才干贞良',
                        yunganning: '锦帆贼',
                        yunliushan: '无为之君',
                    },
                    translate: {
                        weiwuyangbian: '魏武扬鞭',
                        zhaoliejihan: '昭烈季汉',
                        hujujiangdong: '虎踞江东',
                        qunxiongluanwu: '群雄乱武',
                        fengyunshibian: '风云势变',
                        qianlongzaiyuan: '潜龙在渊',
                        yunyouzi: '柚子丶',
                        yunhanyun: '韩云',
                        yunjihanyun: '极韩云',
                        yunshenhanyun: '神韩云',
                        db_yunjiaxu: '贾诩',
                        db_yunmachao: '马超',
                        db_yundaxiaoqiao: '大小乔',
                        yunlvlingqi: '吕玲绮',
                        yunguojia: '郭嘉',
                        yuncaocao: '曹操',
                        yunlvbu: '吕布',
                        yunwangji: '王基',
                        yunzhugeliang: '诸葛亮',
                        yunlvmeng: '吕蒙',
                        yunzhangliao: '张辽',
                        yuncaishi: '蔡氏',
                        yunsunshangxiang: '孙尚香',
                        yunpangtong: '庞统',
                        yunguanyu: '关羽',
                        yunzhangchunhua: '张春华',
                        yundongbai: '董白',
                        yundongzuo: '董卓',
                        yunzhouyu: '周瑜',
                        yunliubei: '刘备',
                        yunzhangfei: '张飞',
                        yundiaochan: '貂蝉',
                        yunsunce: '孙策',
                        yunxushi: '徐氏',
                        yungongsunzhan: '公孙瓒',
                        yunzhaoyun: '赵云',
                        yunhuangyueying: '黄月英',
                        yunxunyu: '荀彧',
                        yunzhanghe: '张郃',
                        yunsimayi: '司马懿',
                        yunsunquan: '孙权',
                        yuncaoying: '曹婴',
                        yunzuoci: '左慈',
                        yunjiangwei: '姜维',
                        yunzhangjiao: '张角',
                        yuncaiwenji: '蔡琰',
                        yunsunjian: '孙坚',
                        yunluxun: '陆逊',
                        yuncaopi: '曹丕',
                        yunzhaoxiang: '赵襄',
                        yunfazheng: '法正',
                        yunxuhuang: '徐晃',
                        yunhuangzhong: '黄忠',
                        yunliuxie: '刘协',
                        yundianwei: '典韦',
                        yunlusu: '鲁肃',
                        yunlejin: '乐进',
                        yunyujin: '于禁',
                        yunhuaman: '花蔓',
                        yunyuanshao: '袁绍',
                        yuntaishici: '太史慈',
                        yundengai: '邓艾',
                        yunzhenfu: '甄宓',
                        yunyuanshu: '袁术',
                        yunlukang: '陆抗',
                        yunliru: '李儒',
                        yunmaliang: '马良',
                        yunganning: '甘宁',
                        yunliushan: '刘禅',
                        _sanfentianxia_wei: '魏武扬鞭',
                        _sanfentianxia_shu: '昭烈季汉',
                        _sanfentianxia_wu: '虎踞江东',
                        _sanfentianxia_qun: '群雄乱武',
                        _sanfentianxia_jini: '三分归晋',
                        yunfengjin: '封禁',
                        yunfengjin_info: '武将技能失效',
                        yungkd: '🐶',
                        yungkd_info: '【一刀斩】:Silence wench I do not wish to be horny anymore I just want to be happy',
                        yunshenyi: '神裔',
                        yunshenyi_info: '锁定技:你的手牌数低于体力上限时,补充至体力上限',
                        yunshenzi: '神姿',
                        yunshenzi_info: '一回合限一次:场上有角色受到伤害后,你选择一项:①摸一张牌;②获得其一个技能',
                        yunshenquan: '神权',
                        yunshenquan_info: '出牌阶段:你可选择一名其他角色,选择使用其区域内一张牌或令【神权】直到回合结束前失效',
                        yunjilue: '极略',
                        yunjilue_info: '每回合一种牌名限一次:你可将一张牌当做任意基本牌或锦囊牌使用或打出',
                        fengyunshibian_jx: '风云势变',
                        fengyunshibian_jx_info: '【风云势变】:第三轮游戏开始时,你选择切换为:①『群』势力,发动【乱武】,且【乱武】期间你不会成为目标;②『魏』势力,发动【拥嫡】,且【拥嫡】发动无性别限制',
                        fengyunshibian_mc: '风云势变',
                        fengyunshibian_mc_info: '【风云势变】:第三轮游戏开始时,你选择切换为:①『群』势力,你增加一点体力上限并取消【锦狮】失去体力上限效果,场上角色受到伤害额外加一;②『蜀』势力,场上角色死亡后你增加一点体力上限(最多加至九)并失去一点体力(最多失去至一),若此时为你回合外,则你可立即使用一张【杀】',
                        fengyunshibian_dxq: '风云势变',
                        fengyunshibian_dxq_info: '【风云势变】:第三轮游戏开始时,你选择切换为:①『群』势力,发动【国色】后可额外执行一次效果,你视为拥有〖天香〗标记;②『吴』势力,发动【国色】后令目标废除全部装备栏直到其回合结束,将〖天香〗标记持续时间调整为永续',
                        yunluanwu: '乱武',
                        yunluanwu_info: '限定技:出牌阶段:你可令除你外的所有角色依次对与其距离最近的另一名角色使用一张【杀】,否则失去1点体力',
                        yunyongdi: '拥嫡',
                        yunyongdi_info: '限定技:准备阶段开始时:你可令一名其他角色增加一点体力上限并回复1点体力,若该角色的武将牌上有主公技且其不为主公,其获得此主公技',
                        yun_luanwu: '乱舞',
                        yun_luanwu_info: '锁定技:你的攻击距离加X,每回合首次使【杀】指定目标时,选择获得X项效果直到回合结束:①本回合至多X次,使用【杀】造成伤害后回复一点体力并摸X张牌;②使用【杀】次数加X;③使用【杀】指定的目标需要额外使用X张【闪】响应.(X为你已损失体力加一,至多为三)<br><br>【风华缭乱】:出牌阶段前,你可失去一点体力从牌堆随机获得一张【杀】',
                        yun_luanwu_fenghua_liaoluan: '风华缭乱',
                        yunyice: '遗策',
                        yunyice_info: '锁定技:你受到伤害后摸X张牌,并可将最多X张手牌交给一名其他角色,进行一次判定,若结果为红色:你回复一点体力;黑色:你弃置一名角色区域内一张牌.(X为你已损失的体力值,至多为三)',
                        yunguimou: '鬼谋',
                        yunguimou_info: '出牌阶段限一次:你可与一名其他角色猜拳,若你赢:则可视为使用一张普通锦囊牌;若你输:则受到一点伤害',
                        yuntiandu: '天妒',
                        yuntiandu_info: '限定技:出牌阶段你可选择一名角色,若其有未发动的觉醒技,则令其中一个觉醒技无视条件立即发动,否则令其摸三张牌',
                        yunqizhi: '奇制',
                        yunqizhi_info: '①出牌阶段你使用非装备牌指定目标后,可弃置一名角色一张牌,其摸一张牌.②弃牌阶段你摸X张牌,若X不小于你的体力值,则你获得【持重】直到你下个回合开始.(X为你出牌阶段发动【奇制】的次数)',
                        yunchizhong: '持重',
                        yunchizhong_info: '锁定技:你的手牌上限翻倍,不能成为延时锦囊目标',
                        yuntanlang: '贪狼',
                        yuntanlang_info: '锁定技:①你的身份为[主公]、［主帅］、[地主]时不增加体力上限;②其他角色的准备阶段:若你的手牌数小于体力上限,你摸一张牌',
                        yunwushuang: '无双',
                        yunwushuang_info: '锁定技:准备阶段:你令其他角色的非锁定技失效,获得技能【极武】直到回合结束',
                        yunjiwu: '极武',
                        yunjiwu_info: '一回合限一次:你使用【杀】或【决斗】指定目标时,可弃置一张手牌令此【杀】或【决斗】造成的伤害加一,若目标因【极武】进入濒死状态,则令其死亡',
                        yunqixing: '祈星',
                        yunqixing_info: '摸牌阶段结束后:你依次展示牌堆顶X张牌,获得并可立即使用展示的锦囊牌,将其余牌置入弃牌堆.(X为场上存活人数,至多为七)',
                        yunranghan: '禳汉',
                        yunranghan_info: '锁定技:①你每回合只会受到一次伤害;②一名其他角色出牌阶段结束时,若你此回合受到过伤害或其本回合因【藏计】使用过锦囊牌,则你发动一次【祈星】',
                        yuncangji: '藏计',
                        yuncangji_info: '出牌阶段每名角色限一次:你可将一张手牌标记为〖锦囊〗并交给一名其他角色.<br><br>【锦囊】:出牌阶段:你可将一张〖锦囊〗当做任意普通锦囊牌使用',
                        yunjinnang: '锦囊',
                        yunjinnang_info: '出牌阶段:你可将一张〖锦囊〗当做任意普通锦囊牌使用',
                        yunshelue: '涉略',
                        yunshelue_info: '准备阶段:你可选择获得场上其他角色的一个技能直到你再次因【涉略】获得技能.(主公技、觉醒技、限定技、隐匿技、使命技除外)',
                        yunkeji: '克己',
                        yunkeji_info: '锁定技:每回合各限一次:①你使用锦囊牌时,从牌堆随机获得一张装备牌;②你受到伤害前,可弃置一张装备牌免疫此伤害并摸一张牌',
                        yunduorui: '夺锐',
                        yunduorui_info: '你对其他角色造成伤害后:若其未〖丧胆〗则可令其〖丧胆〗,若其已〖丧胆〗则可令其所有技能失效直到其回合结束',
                        yunsangdan: '丧胆',
                        yunsangdan_info: '若你未受伤,则移除〖丧胆〗状态并回复已废除的准备栏',
                        yunzhiti: '止啼',
                        yunzhiti_info: '锁定技:若场上受伤角色不小于一:你摸牌阶段摸牌数加一,出牌阶段使用【杀】次数加一,手牌上限加一;不小于二:其他已受伤角色手牌上限减半(向下取整,若其已〖丧胆〗则向上取整);不小于三:废除因【夺锐】技能失效的〖丧胆〗角色装备栏',
                        yunjieyin: '结姻',
                        yunjieyin_info: '限定技:准备阶段:你可选择与一名其他角色结为〖情缘〗<br><br>【情缘】:每回合『孙尚香』首次执行【枭姬】②的一项效果时,〖情缘〗目标执行一次相同效果',
                        yunqingyuan: '情缘',
                        yunqingyuan_info: '每回合『孙尚香』首次执行【枭姬】②的一项效果时,你执行一次相同效果',
                        yunqingyuan_shunshangxiang: '情缘',
                        yunqingyuan_shunshangxiang_info: '每回合你首次执行【枭姬】②的一项效果时,〖情缘〗目标执行一次相同效果',
                        yunxiaoji: '枭姬',
                        yunxiaoji_info: '锁定技:①摸牌阶段你少摸一张牌改为获得一张装备牌;②你使用武器牌后摸两张牌、使用防具牌后回复一点体力、使用防御马后随机获得一张锦囊牌、使用进攻马后随机获得一张基本牌、使用宝物牌后随机获得一张装备牌',
                        yunshangce: '上策',
                        yunshangce_info: '本回合使用首张【杀】无距离限制且不响应',
                        yunzhongce: '中策',
                        yunzhongce_info: '出牌阶段限一次:可弃置一名其他角色区域内一张牌',
                        yunxiace: '下策',
                        yunxiace_info: '结束阶段摸等同攻击范围数的牌(至多五张)',
                        yunxiance: '献策',
                        yunxiance_info: '一名角色的准备阶段:①你可弃置一张牌令其选择获得以下一项效果直到其回合结束:〖上策〗、使用的首张【杀】无距离限制且不可响应;〖中策〗、出牌阶段限一次,可弃置一名其他角色区域内一张牌;〖下策〗、结束阶段摸等同攻击范围数的牌(至多五张);②若当前角色为你则改为失去一点体力获得全部效果',
                        yunfengming: '凤鸣',
                        yunfengming_info: '觉醒技:你死亡时将体力和体力上限调整为一,获得【涅磐】失去【献策】,免疫伤害、不可选中直到准备阶段结束',
                        yunniepan: '涅磐',
                        yunniepan_info: '锁定技:①准备阶段:你增加一点体力上限并回复一点体力;②摸牌阶段:你改为从牌堆随机获得等同体力上限数且牌名各不相同的牌;③你的体力上限至多为九',
                        yunxionglue: '雄略',
                        yunxionglue_info: '①锁定技:每回合限X次:发动【雄略】后,你摸一张牌(X为你已损失体力值加一);②转换技:阴、出牌阶段:你可弃置一张手牌从牌堆随机获得一张带有〖伤害〗标签的牌;阳、你使用带有〖伤害〗标签的牌指定目标/被带有〖伤害〗标签的牌指定为目标时,可弃置一张手牌获得目标/此牌使用者一张牌',
                        yunxieling: '挟令',
                        yunxieling_info: '限定技:出牌阶段你可指一名体力值不小于你的角色,令其余角色选择:①其对你指定的角色使用一张【杀】;②你视为对其使用一张杀',
                        yunzongyu: '总御',
                        yunzongyu_info: '主公技:限定技:出牌阶段:你可令一名其他角色增加一点体力上限并摸两张牌,将其势力更改为『魏』,你获得【魏武】',
                        yunweiwu: '魏武',
                        yunweiwu_info: '结束阶段:你可令一名体力值小于已损失体力值的其他『魏』势力角色回复一点体力',
                        yunyijue: '义绝',
                        yunyijue_info: '每回合一名角色限一次:你使用带有〖伤害〗标签的牌指定唯一目标后,可获得其一张手牌并将其体力调整为一,结束阶段其将体力回复至上限',
                        yunyijueb: '义绝',
                        yunyijueb_info: '锁定技:每回合一名角色限一次:你使用【杀】指定目标后将其体力调整为一,结束阶段其将体力回复至上限',
                        yunguayin: '挂印',
                        yunguayin_info: '觉醒技:场上有角色死亡时,若你对其造成过伤害,则你修改【义绝】并获得【武圣】和一枚〖傲〗标记',
                        yunwusheng: '武圣',
                        yunwusheng_info: '锁定技:①你击杀一名角色后,若你拥有的〖傲〗标记数小于体力上限,则失去一点体力上限获得一枚〖傲〗;②你带有〖伤害〗标签的牌均视为【杀】,使用红色【杀】造成的伤害加一;③你的手牌上限加X,攻击距离加X,摸牌阶段额外摸X张牌,获得〖傲〗时将护甲补充至X.(X为你拥有的〖傲〗标记数)',
                        yunqieting: '窃听',
                        yunqieting_info: '锁定技:每回合限一次:你攻击范围内的其他角色使用牌时,你获得其一张牌',
                        yunxianzhou: '献州',
                        yunxianzhou_info: '锁定技:你受到伤害后须将全部手牌交给一名其他角色',
                        yunqingleng: '清冷',
                        yunqingleng_info: '一回合限一次:场上有角色受到不为你造成的伤害后:①若本局游戏你未对伤害来源发动过【清冷】,则可对伤害来源造成一点伤害;②若本局游戏已对伤害来源发动过【清冷】,则可视为对伤害来源使用一张【杀】',
                        yunshangshi: '伤逝',
                        yunshangshi_info: '锁定技:①你造成的伤害均视为由冰属性【杀】造成;②场上有角色因冰属性【杀】弃置牌后,你获得这些牌',
                        yunzhulian: '株连',
                        yunzhulian_info: '出牌阶段至多三次:若场上〖株〗标记小于三,则你可令一名没有〖株〗标记的其他角色获得〖株〗标记.<br><br>【株】:其他角色计算与你的距离为一,你受到伤害时,伤害来源摸两张牌或回复一点体力,你移除〖株〗标记',
                        yunzhulian_mark: '株',
                        yunzhulian_mark_info: '其他角色计算与你的距离为一,你受到伤害时,伤害来源摸两张牌或回复一点体力,你移除〖株〗标记',
                        yunjiaoheng: '娇横',
                        yunjiaoheng_info: '限定技:一名其他角色死亡后,你可将体力上限调整为一,将其替换为『董卓』身份设定为与你同一阵营进行游戏.<br><br>【混世小魔王】:若你的身份为［内奸］,则改为将身份替换与死亡角色相同',
                        hunshixiaomowang: '混世小魔王',
                        yunbaonve: '暴虐',
                        yunbaonve_info: '锁定技:除『董白』以外其他角色的准备阶段时,你可视为对其使用一张【杀】',
                        yunchongni: '宠溺',
                        yunchongni_info: '锁定技:①你不会对『董白』造成伤害;②『董白』受到伤害时你替代其承担伤害',
                        yunyeyan: '业炎',
                        yunyeyan_info: '限定技:出牌阶段:你可弃置装备区所有牌,对一名其他角色造成等量的火属性伤害.<br><br>【英姿】:准备阶段选择一项:①获得一张装备牌;②获得一张【铁索连环】',
                        yunguqu: '顾曲',
                        yunguqu_info: '一回合限一次:一名其他角色使用非装备牌时,你可弃置一张花色相同的牌取消此牌所有目标;若此牌为:红色、你摸一张牌;黑色、你弃置一名其他角色一张牌',
                        yunyingzi: '英姿',
                        yunjieyi: '结义',
                        yunjieyi_info: '①出牌阶段限一次:你可将一张♥️️️牌当做【桃园结义】使用;②你每回合首次回复体力时回复值额外加一',
                        yunzhaolie: '昭烈',
                        yunzhaolie_info: '出牌阶段每名角色限一次:你可将任意张牌交给一名其他角色,令其获得【武圣】【咆哮】【龙胆】【铁骑】【烈弓】之一直到其回合结束',
                        yunzhaoliea: '昭烈',
                        yunzhaoliea_info: '①你可将一张红色牌当做【杀】使用或打出;②你使用的♦️️️【杀】无距离限制',
                        yunzhaolieb: '昭烈',
                        yunzhaolieb_info: '锁定技:①你使用【杀】无次数限制;②当你使用的【杀】被【闪】抵消时,获得一枚〖咆〗;③你使用【杀】造成伤害时,弃置所有〖咆〗令伤害值+X.(X为弃置的〖咆〗数)',
                        yunzhaoliec: '昭烈',
                        yunzhaoliec_info: '你可将一张【杀】当做【闪】、【闪】当做【杀】、【酒】当做【桃】、【桃】当做【酒】使用或打出',
                        yunzhaolied: '昭烈',
                        yunzhaolied_info: '你使用【杀】指定一名角色为目标后,可进行一次判定并令该角色的非锁定技失效直到回合结束,该角色须弃置一张与判定结果花色相同的牌,否则不能使用【闪】抵消此【杀】',
                        yunzhaoliee: '昭烈',
                        yunzhaoliee_info: '①你使用【杀】可选择你距离不大于此【杀】点数的角色为目标;②当你使用【杀】指定目标后:若其手牌数小于等于你的手牌数,则此【杀】不可被【闪】响应;若你的体力值不大于其,则此【杀】伤害加一',
                        yunsangu: '三顾',
                        yunsangu_info: '主公技:限定技:出牌阶段,你可令一名其他角色增加一点体力上限并回复一点体力,将其势力更改为『蜀』,你获得【仁望】',
                        yunrenwang: '仁望',
                        yunrenwang_info: '锁定技:场上其他角色因【桃园结义】回复体力后,你摸一张牌,若其为势力为『蜀』,则你可令其额外回复一点体力',
                        yunwanjun: '万钧',
                        yunwanjun_info: '出牌阶段每名角色限一次:你可弃置两张牌,视为对一名其他角色使用一张无次数限制、无视技能、无视防具的雷属性【杀】',
                        yunxiongxu: '凶酗',
                        yunxiongxu_info: '锁定技,你的回合内:①带有〖回复〗标签的牌均视为【酒】,且【酒】不计入手牌上限;②使用【酒】次数额外增加X,首次使用【酒】时摸X张牌;③准备阶段/结束阶段:你从牌堆/弃牌堆随机获得一张【酒】.(X为你已损失体力,至多为三)',
                        yunbiyue: '闭月',
                        yunbiyue_info: '锁定技:①你的手牌上限加X;②你的手牌数不小于体力值时不能成为【杀】或【决斗】的目标.(X为你的体力值)',
                        yunmeihuo: '魅惑',
                        yunmeihuo_info: '出牌阶段限一次:你可弃置一张手牌并指定两名其他角色拼点,赢家视为对输家使用一张【决斗】,输家视为对赢家使用一张【杀】,平局你获得双方拼点牌',
                        yunzhiba: '制霸',
                        yunzhiba_info: '场上其他『吴』势力角色出牌阶段限一次:其可交给你一张装备牌,其摸两张牌',
                        yunzhibaa: '制霸',
                        yunzhibaa_info: '出牌阶段限一次:你可交给【制霸】角色一张装备牌,摸两张牌',
                        yunhenglie: '恒烈',
                        yunhenglie_info: '主公技:限定技:出牌阶段,你可令一名其他角色增加一点体力上限并废除判定区,将其势力更改为『吴』,你获得【制霸】',
                        yunjiang: '激昂',
                        yunjiang_info: '锁定技:你因【决斗】对其他角色造成伤害后回复一点体力或摸两张牌',
                        yunyingyang: '鹰扬',
                        yunyingyang_info: '①出牌阶段:你可弃置X张装备牌选择一名其他角色,视为对其使用一张【决斗】;②你于回合外首次受到伤害后,可从牌堆将一张装备牌置入装备栏,视为对伤害来源使用一张【决斗】.(X为你出牌阶段发动【鹰扬】的次数加一)',
                        yunbugua: '卜卦',
                        yunbugua_info: '①场上其他角色出牌阶段限一次:其可令你进行一次卜算,与你各摸一张牌;②锁定技:你每回合首次受到伤害后发动一次【卜卦】①;③场上有角色发动【卜卦】①时,你获得一枚〖伏诛〗标记',
                        yunfuzhu: '伏诛',
                        yunfuzhu_info: '出牌阶段:若你拥有的〖伏诛〗标记不小于三,则你可选择攻击范围内一名体力值不大于〖伏诛〗标记的其他角色,依次移除所有〖伏诛〗标记并视为对其使用等量不计入次数的【杀】,若其未死亡,你失去体力至一点翻面',
                        yunyicong: '义从',
                        yunyicong_info: '每回合各限一次:①你被【杀】指定为目标时,视为对此【杀】的使用者使用一张【杀】;②你使用【杀】造成伤害时可令目标获得〖胆裂〗',
                        yundanlie: '胆裂',
                        yundanlie_info: '你计算与其他角色的距离时始终加X,其他角色计算与你的距离始终减X,每回合至多使用X张牌,回复体力后移除〖胆裂〗.(X为你的体力值)',
                        yunzhujing: '筑京',
                        yunzhujing_info: '锁定技:①你的手牌上限始终为二;②摸牌阶段前,若你有护甲,则额外摸等同护甲数的牌(至多为五);③弃牌阶段后,若你没有护甲,则获得等同弃牌数的护甲(至多为五)',
                        yunwansha: '完杀',
                        yunwansha_info: '每回合限一次:其他角色于你回合内进入濒死状态时,你可弃置一张牌进行一次判定,若判定结果为黑色:令其死亡;红色、令其所有技能失效直到其回合结束',
                        yunjimou: '极谋',
                        yunjimou_info: '锁定技:准备阶段/结束阶段,你获得【控局】直到回合结束/你获得【帷幄】直到回合开始.<br><br>【控局】:你使用的红色基本牌或普通锦囊牌可额外结算一次.<br><br>【帷幄】:锁定技:其他角色使用的黑色牌不能指定你为目标',
                        yunjiance: '间策',
                        yunjiance_info: '出牌阶段限一次:你可将一张手牌交给一名其他角色,令其与另一名角色拼点,赢的角色弃置两张牌,没赢的角色受到一点伤害',
                        yunlongdan: '龙胆',
                        yunlongdan_info: '锁定技:①你可将任意一张手牌当做【杀】或【闪】使用或打出;②你使用【杀】的次数加X(X为你已损失体力值,至多为三)',
                        yunlonglie: '龙烈',
                        yunlonglie_info: '锁定技:每回合你因【龙胆】首次使用或打出一种花色的牌后,若为♠️️️:你可对一名其他角色造成一点伤害;♥️️️:你回复一点体力;♣️️️:你可弃置一名其他角色一张牌;♦️️️:你摸两张牌',
                        yunlinglong: '玲珑',
                        yunlinglong_info: '锁定技:①你回合内不因【玲珑】回复体力后,可令一名角色回复一点体力;②你不因【玲珑】获得牌后,可令一名角色摸一张牌;③你回合外不因【玲珑】失去牌后,可弃置一名角色区域内一张牌.<br><br>【奇巧】:你首次执行【玲珑】其中一项效果后手牌上限加一,结束阶段重置【奇巧】',
                        yunlinglong_qiqiao: '奇巧',
                        yunyuhu: '驭虎',
                        yunyuhu_info: '出牌阶段限一次:你可与一名角色拼点,若你赢:你回复一点体力,你可指定其对其以外的一名角色造成一点伤害;若你没赢:其对你造成一点伤害,你可指定你以外的一名角色回复一点体力',
                        yunwangzuo: '王佐',
                        yunwangzuo_info: '锁定技:你每受到一点伤害后可选择一名角色,你与其各摸X张牌,将手牌弃至X张.(X为你选择角色的体力上限,至多为五)',
                        yunqiaobian: '巧变',
                        yunqiaobian_info: '出牌阶段限一次:你可将一张牌当做任意基本牌或普通锦囊牌使用',
                        yunshanlue: '善略',
                        yunshanlue_info: '锁定技:你每受到一点伤害后进行一次判定,若结果为黑色:出牌阶段使用【巧变】次数加一;红色:你摸两张牌.(【巧变】额外使用次数可叠加,使用后扣除)',
                        yunyingshi: '鹰视',
                        yunyingshi_info: '觉醒技:准备阶段:若你武将牌上的〖忍〗不小于场上存活人数,你将所有〖忍〗加入手牌并体力上限调整为五,跳过摸牌阶段,失去【忍戒】获得【狼顾】',
                        yunrenjie: '忍戒',
                        yunrenjie_info: '锁定技:①你濒死时将所有〖忍〗加入手牌;②场上有角色受到伤害后,若你拥有的〖忍〗小于场上存活人数,则摸一张牌将一张牌置于武将牌上称为〖忍〗',
                        yunlanggu: '狼顾',
                        yunlanggu_info: '锁定技:①若你的体力值不小于一点,你使用牌无距离限制;不小于两点:你使用牌无次数限制;不小于三点:你使用牌可额外指定一个目标;②若你已损失的体力值不小于一点:你的手牌上限等于体力上限;不小于两点:你摸牌阶段额外摸一张牌;不小于三点:结束阶段你可弃置任意张手牌,将手牌摸至体力上限',
                        yunzhiheng: '制衡',
                        yunzhiheng_info: '每回合至多三次:你使用普通锦囊牌可增加或减少任意角色为目标',
                        yunhuju: '虎踞',
                        yunhuju_info: '锁定技:①你的身份不为[主公]、［主帅］、[地主]、时增加一点体力上限;②准备阶段:你从牌堆随机获得一张普通锦囊牌',
                        yunjianye: '建业',
                        yunjianye_info: '主公技:限定技:出牌阶段,你可令一名其他角色增加一点体力上限并获得一张【桃】,将其势力更改为『吴』,你获得【黄武】',
                        yunhuangwu: '黄武',
                        yunhuangwu_info: '一回合一次:场上其他『吴』势力角色回复体力后,其可令你回复一点体力或摸两张牌',
                        yunlingren: '凌人',
                        yunlingren_info: '一回合限一次:你使用带有〖伤害〗标签的牌指定其他角色为目标后,可猜测其中的一个目标的手牌中是否有基本牌、锦囊牌、装备牌,若猜中项目数不小于一:你摸猜中项目数的牌;不小于二:若其已为〖困龙〗则此牌对其造成的伤害加一,否则标记其为〖困龙〗;不小于三:你随机获得一个『魏』势力角色的技能直到你再次因【凌人】获得技能.(主公技、觉醒技、隐匿技、使命技除外)<br><br>【魏缨凤鸣】:①你的手牌上限始终不会小于体力上限,且〖困龙〗角色手牌对你可见;②场上有角色死亡后你回复一点体力/重置武将牌/清空判定区全部牌,若死亡角色为〖困龙〗,则额外回复已废除的装备栏,标记一名随机角色为〖困龙〗;③因【凌人】获得技能时清空自身全部标记',
                        weiyingfengming: '魏缨凤鸣',
                        yunkunlong: '困龙',
                        yunkunlong_info: '我们中出了一个奸细!',
                        yunyugui: '驭鬼',
                        yunyugui_info: '准备阶段/结束阶段:你可选择一名本轮未成为过【驭鬼】目标的角色,其获得一个由你选择的限定技(启动〖风云再起〗后改为主公技、觉醒技、限定技、使命技、隐匿技以外的技能),若其已因【驭鬼】获得过技能,则替换并重置之',
                        yunchengzhi: '承志',
                        yunchengzhi_info: '一轮游戏开始时:你可随机获得『诸葛亮』武将牌上的三个技能直到你再次因【承志】获得技能',
                        yunqilin: '麒麟',
                        yunqilin_info: '觉醒技:准备阶段:若你已因【承志】获得过至少三次技能,则执行以下效果:①你本局游戏造成和受到的伤害累计不小于三,获得【绝计】;②你本局游戏造成和受到的伤害累计不小于五,获得【绝阵】;③若以上选项均不满足,你摸三张牌,视为未发动【麒麟】并重置【承志】次数,否则你移除【承志】与【承志】获得的技能并失去一点体力上限',
                        yunjueji: '绝计',
                        yunjueji_info: '出牌阶段限一次:你可弃置最多三张不同类型的牌执行以下效果:不小于一张、你摸一张牌或回复一点体力;不小于两张、令场上其他角色弃置一张牌;不小于三张、对场上其他角色造成一点伤害',
                        yunjuezhen: '绝阵',
                        yunjuezhen_info: '锁定技;你始终视为装备有【八卦阵】,并将【绝计】摸牌数修改为摸至体力上限,至少为一',
                        yunchuandao: '传道',
                        yunchuandao_info: '锁定技:一名其他角色:①准备阶段时你令其横置;②弃牌阶段时,其可交给你一张牌你可令其回复一点体力.<br><br>【大贤良师】:你始终处于横置状态',
                        daxianliangshi: '大贤良师',
                        yundunshu: '遁术',
                        yundunshu_info: '锁定技:①你受到雷属性伤害时改为回复等量的体力或摸等量的牌;②准备阶段若场上没有【浮雷】,你将一张【浮雷】置入判定区',
                        yunhuangtian: '黄天',
                        yunhuangtian_info: '主公技:限定技:出牌阶段,你可选择一名其他角色令其增加一点体力上限并获得一点护甲,将其势力修改为『群』,你获得【太平】',
                        yuntaiping: '太平',
                        yuntaiping_info: '场上其他『群』势力角色受到雷属性伤害后,你可令其摸两张牌',
                        yunshichou: '誓仇',
                        yunshichou_info: '锁定技:你摸牌阶段额外摸X张牌,计算与其他角色的距离减X,每回合至多X次,使用【杀】造成伤害后令本回合使用【杀】次数加一.(X为你已损失的体力值且至多为三)',
                        yunjinshi: '锦狮',
                        yunjinshi_info: '锁定技:①一轮限一次:你濒死时失去一点体力上限,将体力回复至一点;②你使用【杀】指定目标时,若其手牌数或体力值不小于你,则其不能使用或打出牌且技能失效直到此【杀】结算完毕',
                        yunmoshi: '默识',
                        yunmoshi_info: '锁定技:①你记录你使用或打出过的牌名;②场上其他角色使用或打出你记录过的同名牌时,你可选择弃置其一张牌或摸一张牌,移除此牌记录',
                        yunbeige: '悲歌',
                        yunbeige_info: '每回合至多三次:场上有角色受到不为你造成的伤害后,你可进行一次判定,若结果为:♠️️️伤害来源非锁定技失效直到回合结束;♥️️️受到伤害的角色回复一点体力;♣️️️伤害来源失去一点体力;♦️️️受到伤害的角色摸两张牌',
                        yunpolu: '破虏',
                        yunpolu_info: '锁定技:每回合你首次造成或受到伤害后,选择一名角色令其摸X张牌弃置Y张牌或摸Y张牌弃置X张牌.(X为你已损失体力值,Y为你当前体力值)',
                        yunwulie: '武烈',
                        yunwulie_info: '锁定技:①你手牌上限额外增加装备区的牌数;②你死亡后可选择一名其他角色增加一点体力上限并获得【破虏】,若你的身份为［主公］且其为［忠臣］,则令其以［主公］身份继续进行游戏.(若『孙坚』为玩家操控,则『孙坚』死亡后玩家操控因【武烈】继承［主公］身份的目标进行游戏)',
                        yuntaolue: '韬略',
                        yuntaolue_info: '锁定技:①你失去最后一张手牌后摸一张牌;②你回合内因使用、打出、弃置牌发动【韬略】摸牌后可立即使用之',
                        yunqianya: '谦雅',
                        yunqianya_info: '①摸牌阶段前你可弃置一张牌并跳过此阶段,令一名其他角色立即执行一次额外的摸牌阶段;②其他角色使用锦囊牌指定你为唯一目标时,你可弃置一张牌令一名其他角色立即执行一次额外的出牌阶段;③弃牌阶段后你可弃置一张牌令一名其他角色立即执行一次额外的弃牌阶段;④若你发动【谦雅】时没有护甲,则获得一点护甲',
                        yunguose: '国色',
                        yunguose_info: '①弃牌阶段前:你摸X张牌(X为你出牌阶段使用的牌数,至多为三);②弃牌阶段后:你可选择一名其他角色弃置其至多Y张牌(Y为你弃牌阶段弃置的牌数,至多为三)',
                        yuntianxiang: '天香',
                        yuntianxiang_info: '锁定技:①准备阶段:你可令一名其他角色获得〖天香〗标记直到你下个回合开始;②一回合一次:有〖天香〗标记的角色造成或受到伤害时,你摸一张牌',
                        yunduodi: '夺嫡',
                        yunduodi_info: '锁定技:①你造成伤害后与上家交换座次,若存活人数大于二,则你选择弃置一张牌或令其翻面;②出牌阶段限一次:若你当前座次号数为奇数/偶数,你可令一名其他角色摸/弃置X张牌.(X为你当前座次号数,至多为三)',
                        yundengwei: '登位',
                        yundengwei_info: '觉醒技:结束阶段若你发动过至少五次【夺嫡】,则增加一点体力上限并回复一点体力,失去【夺嫡】【行殇】获得【承天】.<br><br>【九品中正】:你身份为［主公］时额外获得【颂威】',
                        jiupinzhongzheng: '九品中正',
                        yunchengtian: '承天',
                        yunchengtian_info: '①锁定技:你受到伤害后与下家交换座次,若存活人数大于二,则你可选择弃置一张牌或令其翻面;②出牌阶段限一次:若你当前座次号数为奇数/偶数,你可令一名角色摸/弃置X张牌.(X为你当前座次号数,至多为三)',
                        yunxingshang: '行殇',
                        yunxingshang_info: '锁定技:场上其他角色死亡时你回复一点体力,若你未受伤则获得其全部牌.<br><br>【魏王世子】:死亡角色身份为［主公］且你身份为［忠臣］时,无视条件立即发动【登位】,与其交换身份牌以［主公］身份继续进行游戏',
                        weiwangshizi: '魏王世子',
                        yunsongwei: '颂威',
                        yunsongwei_info: '一回合限一次:场上其他『魏』势力角色受到伤害后可进行一次判定,若结果为黑色、你回复一点体力',
                        yunfanghun: '芳魂',
                        yunfanghun_info: '准备阶段:你可失去因【芳魂】获得的技能从随机X名『蜀』势力角色中选择至多X个主公技、觉醒技、隐匿技、使命技以外的技能获得,选择一项:①弃置一张牌并回复一点体力;②失去一点体力并摸一张牌.(X为你当前体力值,至多为五,启动〖风云再起〗后X至多为三)',
                        yunyazi: '睚眦',
                        yunyazi_info: '锁定技:①你使用或打出带〖伤害〗标签的牌后,若【睚眦】未记录此牌名,则记录此牌名,否则你发动一次【谋策】,移除此牌记录;②你受到伤害后发动一次【谋策】,若为【睚眦】记录牌名所造成伤害,则你对伤害来源造成一点伤害并移除此牌记录,否则记录此牌名.<br><br>【谋策】:你亮出牌堆顶的X张牌并获得其中不同种类的牌各一张,须将一张牌交给一名其他角色.(X为你体力上限加已损失体力值,至多为五)',
                        yunmouce: '谋策',
                        yunchangqu: '长驱',
                        yunchangqu_info: '①锁定技:你对有〖堑〗的角色使用牌无距离限制;②你使用带〖伤害〗标签的牌指定唯一目标时可弃置其一张牌,若其有〖堑〗,则改为获得其一张牌',
                        yunpoqian: '破堑',
                        yunpoqian_info: '锁定技:①有角色受到不为你造成的伤害后,若伤害来源没有〖堑〗,则令伤害来源获得〖堑〗;②有〖堑〗的角色摸阶段摸牌时:你可移除其〖堑〗令其少摸X张牌或额外摸X张牌(X为其摸牌数的一半,向上取整);③若你击杀了有〖堑〗的角色,则当前回合结束后获得一个额外回合',
                        yunliegong: '烈弓',
                        yunliegong_info: '锁定技:①你使用【杀】无距离限制;②你使用【杀】指定目标时,若其在你攻击范围内/攻击范围外,此【杀】不计入次数/此【杀】不可响应',
                        yunposhi: '破势',
                        yunposhi_info: '一回合限一次:你造成伤害时可令此伤害加X并摸X张牌,你重置X的数值并令【破势】失效直到回合结束.(X为你造成伤害的次数)',
                        yunxuezhao: '血诏',
                        yunxuezhao_info: '每回合至多三次:你使用非转化的基本牌或普通锦囊牌指定唯一目标时,若你体力值大于一,则可失去一点体力选择令至多X名其他角色视为对此牌目标使用一张同名牌.(X为你已损失体力值,至多为三)',
                        yuntianming: '天命',
                        yuntianming_info: '锁定技:准备阶段或结束阶段:若你已损失体力值大于当前体力值/当前手牌数,则回复一点体力/摸一张牌',
                        yunhanzuo: '汉祚',
                        yunhanzuo_info: '主公技:场上角色死亡后,若其身份为:［忠臣］你失去一点体力上限;［内奸］你废除判定区并重置武将牌;［反贼］你回复一点体力或摸两张牌',
                        yunhuwei: '虎卫',
                        yunhuwei_info: '一回合限一次:场上一名其他角色受到不为你造成的伤害前,若其已受伤,则你可令其获得一点护甲,你受到一点伤害',
                        yunelai: '恶来',
                        yunelai_info: '锁定技:①你受到伤害后可获得等量层数的〖恶〗;②准备阶段:你移除全部〖恶〗,回复X点体力并摸X张牌,可对一名其他角色造成一点伤害.(X为你移除〖恶〗的一半,向下取整)',
                        yundingding: '定鼎',
                        yundingding_info: '限定技:准备阶段:你可选择至多两名角色获得【联军】,将牌堆顶X张牌置入【仁库】.(X为【仁库】上限减当前【仁库】牌数)<br><br>【联军】:锁定技:当你成为卡牌目标时从【仁库】中获得一张牌',
                        yunlianjun: '联军',
                        yunlianjun_info: '锁定技:当你成为卡牌目标时从【仁库】中获得一张牌',
                        yunhaoshi: '豪士',
                        yunhaoshi_info: '①出牌阶段限一次:你可获得一名其他角色的全部手牌,交给其你手牌数一半的牌(向下取整);②【联军】角色获得【仁库】中最后一张牌时,你将牌堆顶X张牌置入【仁库】.(X为【仁库】上限减当前【仁库】牌数)',
                        yunxiandeng: '先登',
                        yunxiandeng_info: '回合开始前:若你拥有的〖骁烈〗标记小于三,则获得一枚〖骁烈〗标记并可与一名其他角色进行拼点,若你赢:你获得一个额外回合',
                        yunxiaolie: '骁烈',
                        yunxiaolie_info: '锁定技:①你的♣️️️牌点数均视为K;②一轮游戏开始时,你失去全部〖骁烈〗标记,移动场上等量的牌',
                        yunzhenjun: '镇军',
                        yunzhenjun_info: '出牌阶段限一次:你可选择一名其他角色令其将手牌数摸至或弃至X张.(X为你的手牌数,至多不超过你与其体力上限总和)',
                        yunyizhong: '毅重',
                        yunyizhong_info: '锁定技:场上有角色使用【无懈可击】结算后:若使用者为你,则你可令一名体力值不小于你的其他角色失去一点体力,若使用者不为你,则你获得此牌对应的所有实体牌',
                        yunmanshi: '蛮氏',
                        yunmanshi_info: '①出牌阶段限一次:你可将任意张牌当做【南蛮入侵】对等量的角色使用;②你使用【南蛮入侵】对体力值不小于你的角色造成伤害时,你可失去一点体力令此伤害加一;③【南蛮入侵】对你无效',
                        yunzhanyuan: '战缘',
                        yunzhanyuan_info: '限定技:你濒死时可将体力回复至上限并摸五张牌,你与一名其他角色翻面.<br><br>【龙凤巾】:若未登场的武将中有『阳·关索』或『天牢·鲍三娘』,则令【战缘】选择的男性/女性角色将武将牌替换为『阳·关索』/『天牢·鲍三娘』,并且你与其直到翻回正面前不能成为卡牌目标',
                        longfengjin: '龙凤巾',
                        yunshiguan: '势冠',
                        yunshiguan_info: '锁定技:每回合各限三次:①你不因【势冠】摸牌时摸牌数为X;②你造成伤害后:若你的手牌数为场上最多,你须弃置一半手牌(向上取整),若你手牌数不为场上最多,你回复一点体力或摸X张牌.(X为你的体力值)',
                        yunshiguan_lisi: '势冠',
                        yunshiguan_lisi_info: '锁定技:每回合各限三次:①你不因【势冠】摸牌时摸牌数为X;②你造成伤害后:若你的手牌数为场上最多,你须弃置一半手牌(向上取整),若你手牌数不为场上最多,你回复一点体力或摸X张牌.(X为你的体力值)',
                        yunxiongjie: '雄桀',
                        yunxiongjie_info: '一回合限一次:若你发动【势冠】②弃置了至少X张牌,则可视为对至多X名角色使用一张【万箭齐发】.(X为你的体力值)',
                        yunlisi: '立嗣',
                        yunlisi_info: '主公技:结束阶段:你可令一名其他角色获得【势冠】直到其回合结束,若【立嗣】目标势力为『群』,则其发动【势冠】②时你执行一次【势冠】②',
                        yuntianyi: '天义',
                        yuntianyi_info: '出牌阶段限一次:你可用一张牌与至多三名角色拼点,若你赢:你摸一张牌,并且本回合使用【杀】可额外指定其为目标;若你输:本回合你不能使用【杀】指定其为目标',
                        yunaozhan: '鏖战',
                        yunaozhan_info: '锁定技:①你使用【杀】次数额外加X(X为【天义】可额外指定的目标数);②你拼点时,令对手随机选择一张手牌作为拼点牌',
                        yunjiliang: '积粮',
                        yunjiliang_info: '①摸牌阶段:你可额外摸三张牌,须将至多三张类型不同的牌置于武将牌上称为〖粮〗;②结束阶段:你将全部〖粮〗置入弃牌堆',
                        yunpoxian: '破险',
                        yunpoxian_info: '①锁定技:若你武将牌上有〖粮〗,则你使用与〖粮〗同类型的牌后选择一张〖粮〗获得;②你因【破险】首次获得〖粮〗后:摸一张牌;第二次获得〖粮〗后:重置本回合使用牌的次数;第三次获得〖粮〗后:本局游戏手牌上限加一',
                        yunpianhong: '翩鸿',
                        yunpianhong_info: '锁定技:①你的手牌数始终为零,翻面时取消,不能成为延时锦囊牌的目标;②回复体力或受到伤害后将牌堆顶一张牌置入弃牌堆',
                        yunzhaoying: '照影',
                        yunzhaoying_info: '锁定技:①牌堆顶X张牌始终对你可见;②你可将牌堆顶X张牌如手牌般使用或打出.(X为你的体力上限加已损失体力值,至多为五)',
                        yunwangzun: '妄尊',
                        yunwangzun_info: '锁定技:准备阶段:若场上区域内没有【玉玺】,则你失去一点体力将【玉玺】置入装备区.<br><br>【玉玺】:锁定技:①摸牌阶段你的摸牌数等于体力上限(至多为九);②出牌阶段结束后,若你本回合使用了至少三种类型的牌,则增加一点体力上限(至多增加至九),否则每缺少一种类型便失去一点体力上限(至多失去至一)',
                        yunjingwu: '靖武',
                        yunjingwu_info: '出牌阶段前:你可重铸手牌与装备区内全部装备牌并废除一个装备栏,若你因【靖武】废除了装备栏,则摸X张牌,重置【砥柱】且直到下个回合开始前将【砥柱】修改为每回合限一次.(X为你已废除的装备栏数)',
                        yundizhu: '砥柱',
                        yundizhu_info: '每轮限一次:你需要使用或打出一张基本牌时,可弃置一名角色区域内一张牌,视为使用或打出此牌',
                        yunduce: '毒策',
                        yunduce_info: '出牌阶段每名角色限一次:你可将一张黑色牌置于牌堆顶,将一名其他角色的一张随机手牌转化为【毒】',
                        yunzhensha: '鸩杀',
                        yunzhensha_info: '锁定技:每回合各限一次:①场上有角色使用黑色牌后,你可弃置一名手牌中有【毒】的角色一张手牌;②场上有角色失去体力后,你可对一名其他角色造成一点火属性伤害',
                        yunxiemu: '协穆',
                        yunxiemu_info: '每回合限一次:场上有角色使用装备牌后你可指定另一名角色获得此装备效果,若其已因【协穆】获得过装备效果则替换之.(赠物与木牛流马除外)<br><br>【炎汉佐翊】:弃牌阶段你可选择一名有【协穆】装备效果的其他角色失去【协穆】装备效果,其获得你弃置的牌',
                        yunxiemu_equip: '协穆',
                        yanhanzuoyi: '炎汉佐翊',
                        yunzishu: '自书',
                        yunzishu_info: '锁定技:准备阶段/判定阶段/出牌阶段/弃牌阶段/结束阶段前你摸一张牌,摸牌阶段你改为移动场上一张牌',
                        yunfulang: '缚浪',
                        yunfulang_info: '你对一名其他角色造成伤害后,可选择一项未执行过的选项:①其失去一点体力上限,你增加一点体力上限;②其摸牌阶段摸牌数减一,你摸牌阶段摸牌数加一;③其手牌上限减一,你手牌上限加一;④其攻击范围减一,你攻击范围加一;⑤其武将牌上一个技能失效,你获得此技能',
                        yunfulang_yi: '缚浪',
                        yunfulang_er: '缚浪',
                        yunfulang_san: '缚浪',
                        yunfulang_si: '缚浪',
                        yunfulang_wu: '缚浪',
                        yunjinling: '锦铃',
                        yunjinling_info: '锁定技:①若【缚浪】有未执行的选项,则你使用【杀】次数加一;②你死亡后:受【缚浪】影响的角色回复因【缚浪】失去的体力上限/摸牌数/手牌上限/攻击范围/技能;③其他角色死亡后:若其已因【缚浪】失去了体力上限/摸牌数/手牌上限/攻击范围/技能,则你重置【缚浪】对应的选项',
                        yunanle: '安乐',
                        yunanle_info: '锁定技:你使用牌指定其他角色为目标/成为其他角色使用牌指定为目标时进行一次判定,若判定结果与此牌颜色不同,则此牌对当前目标无效',
                        yuntuozheng: '托政',
                        yuntuozheng_info: ' 锁定技:①出牌阶段开始前:你可选择一名本轮未成为过【托政】目标的其他角色,你立即结束当前回合令其执行一个额外回合;②场上有成为过【托政】①目标的角色死亡后,你连续执行X个额外回合.(X为【托政】①指定其的次数)',
                        yunhuairen: '怀仁',
                        yunhuairen_info: '主公技:①游戏开始时,你可选择一名其他角色,若其势力为『蜀』,则令其体力上限加一;②若『刘禅』为玩家操控,且【怀仁】目标身份为［忠臣］,则其因【托政】执行额外回合时由玩家操控',
                    },
                };
                lib.config.all.characters.add('云将');
                lib.config.characters.add('云将');
                for (var i in QQQ.character) {
                    QQQ.character[i][4].add(`ext:云将/image/${i}.jpg`);
                }
                lib.translate['云将_character_config'] = `云将`;
                return QQQ;
            });
        },
        config: {
            tuozhanjieshao: {
                name: '拓展介绍',
                init: 'jieshao',
                unforced: true,
                item: {
                    jieshao: '点击查看',
                },
                textMenu(node, link) {
                    lib.setScroll(node.parentNode);
                    node.parentNode.style.transform = 'translateY(-100px)';
                    node.parentNode.style.height = '300px';
                    node.parentNode.style.width = '300px';
                    switch (link) {
                        case 'jieshao':
                            node.innerHTML = '欢迎使用本拓展<br>本拓展武将设计思路尽量遵循历史记载和话本戏剧演义,代码机制大部分参考了其他大佬的代码缝合而来,立绘来自各类三国游戏,尽量做到缝合又不失乐趣.强度控制在玩的时候虽然很爽,但还是会翻车.AI操控的时候觉得难打而不是没法打,大概就这么个定位,所以感觉那个武将太强或者太弱都可以反馈一下,我会在不影响武将人设的情况下做调整.<br><br><br>隐藏武将<br><br>柚子——作者养的狗,打开百花缭乱可出场<br>吕玲绮——我本来不想写但她真的太***了,一世风华&百花缭乱打开可选<br>董白——身份模式可选<br><br><br>【山河永固AI增强详细介绍】<br>部分AI优化代码由翩翩浊世许公子编写,很高兴有伙伴加入AI优化这个大工程,使用AI优化功能需知:请不要与其他AI增强或给AI开局替换武将的功能拓展同时使用<br><br>全局AI相关:<br><br>存牌逻辑优化为桃>酒>闪>无懈,AI会弃牌阶段会优先存桃,弃置牌添加关键牌对策<br><br>受到大伤害进入濒死,血量为负,队友手里桃能救活的时候不会再拒绝出桃.(这里还是按照座次结算,比如你-2血,你手里有一桃,AI队友手里两桃,AI还是会认为他手里的两桃救不活你,不会出)<br><br>部分关键牌序优化,如南蛮入侵、万箭齐发、桃园结义、决斗等<br><br>卖血武将优化,有卖血标签技能的武将现在会更乐意卖血,没有绝情标签技能的队友也会主动帮忙卖血,拒绝杀闪砸蛋你干嘛？ 对敌人卖血武将会优先处理非卖血武将的队友<br><br>闪相关技能对策优化,目前张角比较明显,队友酒杀张角这种傻逼行为杜绝掉.<br><br>铁索相关优化,AI在场上横置敌人比横置队友少,不会使用属性杀和火攻,如果是忠臣,目标横置,主公横置,不会使用属性杀和火攻.<br><br>增加AI喝闷酒对策.<br><br>顺拆敌人白银狮子,照月狮子对策,并会优先拆顺队友兵乐.<br><br>无懈对策,判定区有牌的时候会考虑保留无解,无防具和防御马时无懈过河拆桥优先级降低,血量健康无懈不多火攻目标无藤甲无懈火攻优先级降低,借刀无懈优先级降低,南蛮万箭铁索添加无懈使用优化.<br><br>南蛮万箭对策,残血队友大于残血敌人时降低开AOE概率,以及添加卖血敌友筛选.<br><br>高评级武将增加嘲讽度,AI会更愿意针对评级较高的武将.<br><br>AI小人防酒杀.<br><br>五谷选牌对策,满状态依次优先无中生有 无懈可击 顺手牵羊 桃 过河拆桥 乐不思蜀 兵粮寸断 酒,非满状态依次 桃 无中生有 无懈可击 酒 顺手牵羊 过河拆桥 乐不思蜀 兵粮寸断.<br><br>闪电对策,场上没有改判或者闪电浮雷存在收益的队友时降低闪电使用优先级.<br><br>斗地主自己有免伤或者改判不飞扬闪电,判定区同时有兵乐存在手牌数多飞扬乐不思蜀否则飞扬兵粮寸断.<br><br>本体对护甲伤害判定相关优化,比如张角不劈带护甲的敌人.<br><br><br>主公AI相关:<br><br>主公开局默认知晓一名忠臣身份(无名杀没法看将面,只能如此),AI主公会对未知身份的角色进行压制,八人场三分之一概率盲到忠内.<br><br>反贼全灭忠臣存活,内奸此时跳忠,AI主公会识别内奸.<br><br>主公一血,忠臣濒死,主公手里只有一桃,那主公会留着自己吃.<br><br>忠臣残血,主公不会看见反贼残血就开南蛮万箭,顺便把忠剁了.<br><br>忠臣全灭,反贼存活不小于三,内奸濒死主公桃充足,主公会救内奸.<br><br><br>忠臣AI相关:<br><br>忠对忠现在不会见死不救.<br><br>只存活一名忠臣,反贼全活,内奸濒死,忠会出桃捞内奸.<br><br>反贼全灭,内奸未跳身份 AI忠臣会识别内奸.<br><br><br>内奸AI相关:<br><br>内奸全场保持活跃状态,强势增加存在感.<br><br>主忠反将面(也就是评级和嘲讽,某些低评级但技能很离谱的不在考虑范围)差距过大,且主公血量健康,内奸优先按照主忠反将面优劣来选择行动策略,否则根据主忠反人数选择行动策略.<br><br>内奸在主忠方人数小于反贼人数,跳忠.<br><br>内奸在反贼人数不大于主忠方人数且主公血量大于二,跳反.<br><br>只要主公血量不大于二,内奸无视前面的设定,跳忠.<br><br>内奸在有反贼存活的情况下不会对主公使用伤害牌.<br><br>反贼全员存活,忠臣只存活一名,内奸会对濒死忠臣出桃.<br><br>主忠全员存活,反贼只存活一名,内奸会对濒死反贼出桃.<br><br><br>反贼AI相关:<br><br>反贼在手牌数不小于三,因为队友造成伤害进入濒死时,不会再主动求死.<br><br>反贼只活一人,主忠全存活,内奸跳反濒死,反贼会出桃.<br><br>在主公血量不大于二时,反贼会强势开AOE输出主公.<br><br><br>本体武将AI优化相关:<br>整肃AI<br>手杀文鸯 椎锋<br>DIY文鸯 膂力<br>OL邓芝 修好<br>卧龙凤雏 游龙<br>张绣 雄乱<br>左棻 诏颂<br>蒲元 天匠<br>诸葛亮 空城<br>钟繇 活墨<br>孙策&孙坚 英魂<br>刘焉 立牧<br>潘濬 观微<br>TW曹昂 孝廉<br>曹昂 慷忾<br>许贡 业仇<br>诸葛瞻 父荫<br>刘禅 放权<br>曹丕 放逐<br>神司马懿 放逐 完杀<br>华佗 急救<br>司马徽 荐杰 隐士<br>蒋琬 镇庭<br>手杀全宗 邀名<br>韩馥 节应<br>邓艾 屯田<br>朱然 胆守<br>王粲 善檄<br>TW霍峻 伺怠<br>孙策 激昂<br>神荀彧 灵策 奇正相生<br>神马超 横婺 狩郦<br>童渊 朝凤<br>贾逵 忠佐<br>吕虔 献刀<br>牛金 摧锐<br>辛毗 引据<br>葛玄 扎符<br>OL界孙坚 武烈<br>界孙休 诏缚<br>张济 屯军<br>SP贾诩 拥嫡<br>王允 居功<br>范疆张达 决生<br>司马师 拜移<br>骆统 进谏<br>张角 雷击<br>刘谌 战绝<br>孙权 制衡<br>刘永 封乡<br>手杀于禁 节钺<br>曹婴 凌人<br>张让 滔乱<br>神赵云 龙魂<br>神诸葛 大雾<br>丁尚涴 讽言<br>OL王荣 丰姿<br>新潘凤 狂斧<br>曹髦 潜龙 决讨<br><br><br>本体部分装备AI优化:<br>寒冰剑<br>雌雄双股剑<br>古锭刀<br>朱雀羽扇<br>青釭剑<br>丈八蛇矛<br>朱雀羽扇<br>霹雳车<br>霹雳投石车<br>大攻车<br>藤甲<br>仁王盾<br>八卦阵<br><br>本体武将相关优化后续更新慢慢添加……(或许吧)<br><br><br><br>特别鸣谢玄武江湖星城大佬提供的代码支持 </b><br><img style=width:200px src=extension/云将/image/MG_xwjh.jpg><br><br><br>特别鸣谢感时花溅泪乀摧林大佬的帮助 </b><br><img style=width:200px src=extension/云将/shenfen/cuilin.jpg><br><br><br>特别鸣谢时空枢纽爪爪<br><br>特别感谢 翩翩浊世许公子 的高质量反馈以及部分AI优化编写<br>特别鸣谢群友马大钧 小姜同学 小虎 阿枫 戴夫 蝙蝠老师 跟我捉迷藏 仮面ライダー提供的素材支持<br><br><br>特别鸣谢群友Empty city° 菓菓 余丰慧の聚魂棺 Δ红色拓跋Δ等群友提供的设计思路支持与测试<br><br><br>很高兴能在无名杀这个圈子认识大家,希望大家天天开心(๑>؂<๑)';
                            break;
                    }
                },
            },
            baihualiaoluan: {
                name: '<span class="Qmenu">百花缭乱</span>',
                intro: '开启后,可以召唤一只帅气的狗子',
                init: false,
            },
            shanheyonggu: {
                name: '山河永固',
                intro: 'AI优化增强,重启生效.(注意:与其他拓展AI功能同时打开可能会导致AI错乱,相关AI增强优化不建议重复开启,具体AI优化内容请在拓展介绍里查看)',
                init: 'on',
                item: {
                    on: '开',
                    off: '关',
                },
                onclick(item) {
                    game.saveConfig('extension_云将_shanheyonggu', item);
                },
            },
            tianxiasanfen: {
                name: '天下三分',
                intro: '身份模式下每个势力会获得专属势力BUFF.<br>『魏武扬鞭』:每回合首次受到伤害后亮出牌堆顶一张牌,若为黑色则获得之.<br>『昭烈季汉』:每回合首次使用【杀】被闪避后收回此【杀】(【空城】不触发).<br>『虎踞江东』:回合内首次使用【桃】后,从牌堆随机获得一张【火攻】或【铁索连环】.<br>『群雄乱武』:准备阶段:若场上存活人数大于四且当前体力值等于一,摸一张牌或回复一点体力.<br>『三分归晋』:结束阶段:可弃置一张手牌观看牌堆顶一张牌,将其置于牌堆顶或牌堆底并摸一张牌.<br>『神话降临』:准备阶段:可弃置两张牌移除判定区一张牌.',
                init: 'off',
                item: {
                    on: '开',
                    off: '关',
                },
                onclick(item) {
                    game.saveConfig('extension_云将_tianxiasanfen', item);
                },
            },
            xianyunyehe: {
                name: '闲云野鹤',
                intro: '去除部分小游戏,重启生效.(注意:若有其他拓展修改了小游戏可能会报错,关闭此选项即可)',
                init: 'off',
                item: {
                    on: '开',
                    off: '关',
                },
                onclick(item) {
                    game.saveConfig('extension_云将_xianyunyehe', item);
                },
            },
            banjuncharacter: {
                name: '声名鹊起',
                intro: '对身份局ai的选将框进行过滤,可防止出现内奸选单挑王(刘备)这种奇景.如果主公有主公技,忠臣会尽量选择与主势力相同的武将,而且内奸会倾向于选强将(评级高的)',
                init: '1',
                item: {
                    1: '关',
                    2: '开',
                },
                onclick(item) {
                    game.saveConfig('extension_云将_banjuncharacter', item);
                },
            },
            //主公
            banjunzhu: {
                clear: true,
                name: '<li>主公禁将',
                onclick() {
                    var temp = this;
                    var lists = lib.config.extension_云将_zhu;
                    game.prompt('请输入要主公ai禁选的武将id<br>(如标曹操为<caocao>,神曹操为<shen_caocao>),再次输入同id即可退出', function (str) {
                        if (str) {
                            var thisstr = false;
                            if (lib.character[str]) {
                                thisstr = str;
                                var lists = lib.config.extension_云将_zhu;
                                if (lists) lists = lists.slice(0);
                                else lists = [];
                                if (lists && lists.includes(thisstr)) {
                                    lists.remove(thisstr);
                                    temp.innerHTML = '<div style="color: rgb(210,210,000);font-family:xinwei"><font size="4">武将已退出主公ai禁选</font></div>';
                                    temp.ready = true;
                                    setTimeout(() => {
                                        temp.innerHTML = '<li>主公禁将';
                                        delete temp.ready;
                                    }, 1600);
                                } else {
                                    lists.push(thisstr);
                                    temp.innerHTML = '<div style="color: rgb(255,97,3);font-family:xinwei"><font size="4">武将已加入主公ai禁选</font></div>';
                                    temp.ready = true;
                                    setTimeout(() => {
                                        temp.innerHTML = '<li>主公禁将';
                                        delete temp.ready;
                                    }, 1600);
                                }
                                game.saveConfig('extension_云将_zhu', lists);
                                zhuwrite(lists);
                            } else {
                                temp.innerHTML = '<div style="color: rgb(255,0,0);font-family:xinwei"><font size="4">找不到该武将</font></div>';
                                temp.ready = true;
                                setTimeout(() => {
                                    temp.innerHTML = '<li>主公禁将';
                                    delete temp.ready;
                                }, 1600);
                            }
                        }
                    });
                },
            },
            banzhubiao: {
                name: '<li>主公禁选(点击查看)',
                clear: true,
                onclick() {
                    var h = document.body.offsetHeight;
                    var w = document.body.offsetWidth;
                    banzhucharacter = ui.create.div('', '<div style="z-index:999"><iframe width="' + w + 'px" height="' + h + 'px" src="extension/云将/shenfen/banZhu.html"></iframe></div>', ui.window);
                    banzhucharacter_close = ui.create.div('', '<div style="height:10px;width:' + w + 'px;text-align:center;z-index:999"><font size="5em">关闭</font></div>', banzhucharacter, function () {
                        banzhucharacter.delete();
                    });
                },
            },
            //忠臣
            banjunzhong: {
                clear: true,
                name: '<li>忠臣禁将',
                onclick() {
                    var temp = this;
                    var lists = lib.config.extension_云将_zhong;
                    game.prompt('请输入要忠臣ai禁选的武将id<br>(如标曹操为<caocao>,神曹操为<shen_caocao>),再次输入同id即可退出', function (str) {
                        if (str) {
                            var thisstr = false;
                            if (lib.character[str]) {
                                thisstr = str;
                                var lists = lib.config.extension_云将_zhong;
                                if (lists) lists = lists.slice(0);
                                else lists = [];
                                if (lists && lists.includes(thisstr)) {
                                    lists.remove(thisstr);
                                    temp.innerHTML = '<div style="color: rgb(210,210,000);font-family:xinwei"><font size="4">武将已退出忠臣ai禁选</font></div>';
                                    temp.ready = true;
                                    setTimeout(() => {
                                        temp.innerHTML = '<li>忠臣禁将';
                                        delete temp.ready;
                                    }, 1600);
                                } else {
                                    lists.push(thisstr);
                                    temp.innerHTML = '<div style="color: rgb(255,97,3);font-family:xinwei"><font size="4">武将已加入忠臣ai禁选</font></div>';
                                    temp.ready = true;
                                    setTimeout(() => {
                                        temp.innerHTML = '<li>忠臣禁将';
                                        delete temp.ready;
                                    }, 1600);
                                }
                                game.saveConfig('extension_云将_zhong', lists);
                                zhongwrite(lists);
                            } else {
                                temp.innerHTML = '<div style="color: rgb(255,0,0);font-family:xinwei"><font size="4">找不到该武将</font></div>';
                                temp.ready = true;
                                setTimeout(() => {
                                    temp.innerHTML = '<li>忠臣禁将';
                                    delete temp.ready;
                                }, 1600);
                            }
                        }
                    });
                },
            },
            banzhongbiao: {
                name: '<li>忠臣禁选(点击查看)',
                clear: true,
                onclick() {
                    var h = document.body.offsetHeight;
                    var w = document.body.offsetWidth;
                    banzhongcharacter = ui.create.div('', '<div style="z-index:999"><iframe width="' + w + 'px" height="' + h + 'px" src="extension/云将/shenfen/banZhong.html"></iframe></div>', ui.window);
                    banzhongcharacter_close = ui.create.div('', '<div style="height:10px;width:' + w + 'px;text-align:center;z-index:999"><font size="5em">关闭</font></div>', banzhongcharacter, function () {
                        banzhongcharacter.delete();
                    });
                },
            },
            //反贼
            banjunfan: {
                clear: true,
                name: '<li>反贼禁将',
                onclick() {
                    var temp = this;
                    var lists = lib.config.extension_云将_fan;
                    game.prompt('请输入要反贼ai禁选的武将id<br>(如标曹操为<caocao>,神曹操为<shen_caocao>),再次输入同id即可退出', function (str) {
                        if (str) {
                            var thisstr = false;
                            if (lib.character[str]) {
                                thisstr = str;
                                var lists = lib.config.extension_云将_fan;
                                if (lists) lists = lists.slice(0);
                                else lists = [];
                                if (lists && lists.includes(thisstr)) {
                                    lists.remove(thisstr);
                                    temp.innerHTML = '<div style="color: rgb(210,210,000);font-family:xinwei"><font size="4">武将已退出反贼ai禁选</font></div>';
                                    temp.ready = true;
                                    setTimeout(() => {
                                        temp.innerHTML = '<li>反贼禁将';
                                        delete temp.ready;
                                    }, 1600);
                                } else {
                                    lists.push(thisstr);
                                    temp.innerHTML = '<div style="color: rgb(255,97,3);font-family:xinwei"><font size="4">武将已加入反贼ai禁选</font></div>';
                                    temp.ready = true;
                                    setTimeout(() => {
                                        temp.innerHTML = '<li>反贼禁将';
                                        delete temp.ready;
                                    }, 1600);
                                }
                                game.saveConfig('extension_云将_fan', lists);
                                fanwrite(lists);
                            } else {
                                temp.innerHTML = '<div style="color: rgb(255,0,0);font-family:xinwei"><font size="4">找不到该武将</font></div>';
                                temp.ready = true;
                                setTimeout(() => {
                                    temp.innerHTML = '<li>反贼禁将';
                                    delete temp.ready;
                                }, 1600);
                            }
                        }
                    });
                },
            },
            banfanbiao: {
                name: '<li>反贼禁选(点击查看)',
                clear: true,
                onclick() {
                    var h = document.body.offsetHeight;
                    var w = document.body.offsetWidth;
                    banfancharacter = ui.create.div('', '<div style="z-index:999"><iframe width="' + w + 'px" height="' + h + 'px" src="extension/云将/shenfen/banFan.html"></iframe></div>', ui.window);
                    banfancharacter_close = ui.create.div('', '<div style="height:10px;width:' + w + 'px;text-align:center;z-index:999"><font size="5em">关闭</font></div>', banfancharacter, function () {
                        banfancharacter.delete();
                    });
                },
            },
            //内奸
            banjunnei: {
                clear: true,
                name: '<li>内奸禁将',
                onclick() {
                    var temp = this;
                    var lists = lib.config.extension_云将_nei;
                    game.prompt('请输入要内奸ai禁选的武将id<br>(如标曹操为<caocao>,神曹操为<shen_caocao>),再次输入同id即可退出', function (str) {
                        if (str) {
                            var thisstr = false;
                            if (lib.character[str]) {
                                thisstr = str;
                                var lists = lib.config.extension_云将_nei;
                                if (lists) lists = lists.slice(0);
                                else lists = [];
                                if (lists && lists.includes(thisstr)) {
                                    lists.remove(thisstr);
                                    temp.innerHTML = '<div style="color: rgb(210,210,000);font-family:xinwei"><font size="4">武将已退出内奸ai禁选</font></div>';
                                    temp.ready = true;
                                    setTimeout(() => {
                                        temp.innerHTML = '<li>内奸禁将';
                                        delete temp.ready;
                                    }, 1600);
                                } else {
                                    lists.push(thisstr);
                                    temp.innerHTML = '<div style="color: rgb(255,97,3);font-family:xinwei"><font size="4">武将已加入内奸ai禁选</font></div>';
                                    temp.ready = true;
                                    setTimeout(() => {
                                        temp.innerHTML = '<li>内奸禁将';
                                        delete temp.ready;
                                    }, 1600);
                                }
                                game.saveConfig('extension_云将_nei', lists);
                                neiwrite(lists);
                            } else {
                                temp.innerHTML = '<div style="color: rgb(255,0,0);font-family:xinwei"><font size="4">找不到该武将</font></div>';
                                temp.ready = true;
                                setTimeout(() => {
                                    temp.innerHTML = '<li>内奸禁将';
                                    delete temp.ready;
                                }, 1600);
                            }
                        }
                    });
                },
            },
            banneibiao: {
                name: '<li>内奸禁选(点击查看)',
                clear: true,
                onclick() {
                    var h = document.body.offsetHeight;
                    var w = document.body.offsetWidth;
                    banneicharacter = ui.create.div('', '<div style="z-index:999"><iframe width="' + w + 'px" height="' + h + 'px" src="extension/云将/shenfen/banNei.html"></iframe></div>', ui.window);
                    banneicharacter_close = ui.create.div('', '<div style="height:10px;width:' + w + 'px;text-align:center;z-index:999"><font size="5em">关闭</font></div>', banneicharacter, function () {
                        banneicharacter.delete();
                    });
                },
            },
        },
        package: {
            card: {
                card: {
                    yunyuxi: {
                        image: `ext:云将/mark/yunyuxi.png`,
                        type: 'equip',
                        fullskin: true,
                        loseDelay: false,
                        subtype: 'equip5',
                        skills: ['yunyuxi_skill'],
                        ai: {
                            equipValue: 9,
                            basic: {
                                order: 9,
                                useful: 2,
                                equipValue: 9,
                                value: 9,
                            },
                            result: {
                                target(player, target, card) {
                                    return get.equipResult(player, target, card.name);
                                },
                            },
                        },
                        enable: true,
                        selectTarget: -1,
                        filterTarget(card, player, target) {
                            return target == player;
                        },
                        modTarget: true,
                        allowMultiple: false,
                        content() {
                            if (cards.length && get.position(cards[0], true) == 'o') target.equip(cards[0]);
                        },
                        toself: true,
                    },
                },
                translate: {
                    yunyuxi: '玉玺',
                    yunyuxi_info: '锁定技:①摸牌阶段你的摸牌数等于体力上限(至多为九);②出牌阶段结束后,若你本回合使用了至少三种类型的牌,则增加一点体力上限(至多增加至九),否则每缺少一种类型便失去一点体力上限(至多失去至一)',
                },
            },
            intro: "内置部分功能,详细内容请看拓展介绍或长按相关开关(PC端右键)查看.<br>QQ交流群:813569942</b><br><img style=width:150px src=extension/云将/image/MG_yj.jpg><br><span style='color: gold'>潜水的火修复版<br>『无名杀扩展大全群』:771901025<br></span>",
            author: '柚子丶奶茶丶猫   翩翩浊世许公子',
            version: '8.81',
        },
    };
});
