import { lib, game, ui, get, ai, _status } from '../../noname.js';
game.import('extension', function (lib, game, ui, get, ai, _status) {
	return {
		name: '浪琴天阙',
		content(config, pack) {
			//神/thunder,魏/water,蜀/soil,吴/wood,群/metal,主/fire
			//color/颜色,suit/花色,number/点数,type/类型
			//basic/基本,trick/锦囊,equip/装备,delay/延迟锦囊
			//black/黑色,red/红色
			//heart♥️️️,spade♠️️️ diamond♦️️️,club♣️️️
			//player.indexOf//player!=_status.currentPhase,player.isAlive()//存活,player.getStat('triggerSkill').xinfu_lingren--,event._result={control:d[0]}/单选_status.event.//isEnemiesOf//isFriendsOf//chooseButtonBefore
			// --------------------------------------武将评级------------------------------------------//
			lib.rank.rarity.junk.addArray([]);
			lib.rank.rarity.rare.addArray(['lqym_No.66', 'lqym_No.76']);
			lib.rank.rarity.epic.addArray(['lqhc_yinbuniao', 'lqtqt_sunshangxiang', 'lqym_No.18', 'lqcy_lidian', 'lqym_No.15', 'lqym_No.14', 'lqym_No.13', 'lqym_No.31', 'lqym_No.12', 'lqym_No.6', 'lqym_No.4', 'lqym_No.3', 'lqym_No.2', 'lqym_No.0', 'lqym_No.1', 'lqcy_caocao', 'lqcy_guojia', 'lqym_No.78', 'lqhcgai_simayi', 'lqhcgai_xuhuang', 'lqhcgai_liuye', 'lqhc_yitianxing', 'lqhc_xiangyu', 'lqym_qingyanbailong', 'lqym_zhenhongyanheilong', 'lqym_heimodao', 'lqtqx_dengai', 'lqym_No.10', 'lqtqt_yuji']);
			lib.rank.rarity.legend.addArray(['lqym_No.21', 'lqym_No.22', 'lqym_No.23', 'lqhcgaix_zhaoyun', 'lqhc_gujian', 'lqhc_yilite', 'lqym_No.16', 'lqym_No.17', 'lqym_No.11', 'lqym_No.9', 'lqym_No.8', 'lqym_No.7', 'lqym_No.5', 'lqtqx_zhonghui', 'lqtqxshen_guojia', 'lqtqxshen_sunce', 'lqtq_langqinjun', 'lqhc_hanmaojun', 'lqhc_fengxuanjun', 'lqtq_baibanbiao', 'lqtqx_huangzhong', 'lqtq_xiutianbiao', 'lqhc_xinlianyue', 'lqtqt_huangyueying', 'lqtqt_caiwenji', 'lqtqt_zhouchu', 'lqtqt_sunhanhua', 'lqtqt_caojinyu', 'lqtqt_duyu', 'lqtqt_daxiaoqiao', 'lqtqt_baosanniang', 'lqtqt_zhugeguo', 'lqtq_miaoxiaoyu', 'lqtqt_guojia', 'lqtqt_majun', 'lqhc_qin', 'lqtqt_zuoci', 'lqtqj_linglong', 'lqtq_abiao', 'lqym_No.19', 'lqym_No.20', 'lqhc_xiangquyuanfang', 'lqhcgai_simashi', 'lqhcgai_mayuanyi', 'lqhc_chenping', 'lqhc_dongfangyuechu', 'lqhc_tushanhonghong', 'lqtqt_guanyinping', 'lqtqxshen_xunyu', 'lqtqj_shashen', 'lqtq_yufengbiao', 'lqtqj_qixian', 'lqhcgai_zhangfei', 'lqhcgai_zhaoyun', 'lqhcgai_lvmeng', 'lqhcgaishen_jiangwei', 'lqtqj_hongdie', 'lqtqxshen_ganning', 'lqhc_hanxin', 'lqym_qingyanheilong', 'lqym_zhanmolongqishi', 'lqym_modaolongqishi', 'lqtqx_xusheng', 'lqjxshen_zhaoyun', 'lqhc_qijing', 'lqhc_shence', 'lqhcgai_jiangwei', 'lqtq_dubiao', 'lqcy_daqiao']);
			// --------------------------------------主代码------------------------------------------//
			for (const i in lib.character) {
				//我要涩涩//隐匿前奏//评价选将
				const info = lib.character[i],
					sese = Number(lib.config.extension_浪琴天阙_lqtq_woyao_sese),
					pingji = Number(lib.config.extension_浪琴天阙_lqtq_pingji_xuanjiang);
				if ((sese == 1 && info[0] != 'female') || (sese == 2 && info[0] != 'male')) {
					info.isAiForbidden = true;
				}
				if ((pingji == 1 && !lib.rank.rarity.legend.includes(i)) || (pingji == 2 && !lib.rank.rarity.epic.includes(i)) || (pingji == 3 && !lib.rank.rarity.rare.includes(i)) || (pingji == 4 && (lib.rank.rarity.rare.includes(i) || lib.rank.rarity.epic.includes(i) || lib.rank.rarity.legend.includes(i)))) {
					info.isAiForbidden = true;
				}
				if (lib.config.extension_浪琴天阙_lqtq_yinni_qianzou && ['☄', '◎', '❁'].includes(lib.translate[i][0])) {
					lib.translate[i] = lib.translate[i].substring(1, lib.translate[i].length);
				}
				if (get.mode() != 'boss' && lib.config.extension_浪琴天阙_lqtq_tili_tiaozheng && (info[4].includes('boss') || info[4].includes('hiddenboss') || info[4].includes('bossallowed'))) {
					let num = 0,
						num1 = '';
					for (const j of get.translation(info[2])) {
						if (j != '/') {
							num1 += j;
						}
						if (j == '/') {
							break;
						}
					}
					for (const j of info[3]) {
						if (lib.translate[j] && lib.translate[j + '_info']) {
							num++;
						}
					}
					if (num > 2 && (info[2] > 3 || num1 > 3)) {
						info[2] = 3;
					}
					if (num < 3 && (info[2] > 4 || num1 > 4)) {
						info[2] = 4;
					}
				}
			} //QQQ
			lib.init.css('extension/浪琴天阙', 'extension');
			lib.element.player.chooseToLqtqMouyi = function (target) {
				//谋弈
				const next = game.createEvent('chooseToLqtqMouyi');
				next.player = this;
				next.setContent('chooseToLqtqMouyi');
				next.target = target;
				for (let i = 1; i < arguments.length; i++) {
					if (typeof arguments[i] == 'string') {
						next.mouyi = arguments[i];
					}
				}
			};
			lib.element.content.chooseToLqtqMouyi = function () {
				//谋弈
				'step 0';
				var target = event.target;
				var mouyi = event.mouyi;
				game.log(player, '对', target, '发起了', '#y谋弈');
				var list = ['lqtq_pohuaizhilun', 'lqtq_hejuzhilun', 'lqtq_fangyuzhilun', 'lqtq_guangfangzhilun'];
				var dialog = ui.create.dialog(mouyi, '请选择你的攻敌之策', 'hidden');
				var table = document.createElement('div');
				table.classList.add('add-setting');
				table.style.margin = '0';
				table.style.width = '100%';
				table.style.position = 'relative';
				for (let i = 0; i < 2; i++) {
					var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
					td.innerHTML = '<span><img src="extension/浪琴天阙/image/辅助图/' + list[i] + '.png" width="135" height="160"></span>';
					td.link = list[i];
					td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
					Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
					table.appendChild(td);
					dialog.buttons.add(td);
				}
				dialog.content.appendChild(table);
				player.chooseButton(dialog, true).ai = function () {
					return 1 + Math.random();
				};
				event.list = list;
				('step 1');
				var target = event.target;
				var mouyi = event.mouyi;
				var list = event.list;
				if (result.links?.length) {
					event.mes = result.links[0];
					var dialog = ui.create.dialog(mouyi, '请选择你的对敌之策', 'hidden');
					var table = document.createElement('div');
					table.classList.add('add-setting');
					table.style.margin = '0';
					table.style.width = '100%';
					table.style.position = 'relative';
					for (let i = 2; i < 4; i++) {
						var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
						td.innerHTML = '<span><img src="extension/浪琴天阙/image/辅助图/' + list[i] + '.png" width="135" height="160"></span>';
						td.link = list[i];
						td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
						Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
						table.appendChild(td);
						dialog.buttons.add(td);
					}
					dialog.content.appendChild(table);
					target.chooseButton(dialog, true).ai = function () {
						return 1 + Math.random();
					};
				} else {
					event.finish();
				}
				('step 2');
				var target = event.target;
				var mouyi = event.mouyi;
				if (result.links?.length) {
					event.tes = result.links[0];
					const mes = event.mes;
					const tes = event.tes;
					game.log(player, '选择的攻敌之策为', '#g' + mes);
					game.log(target, '选择的对敌之策为', '#g' + tes);
					if (('lqtq_pohuaizhilun' == mes && 'lqtq_fangyuzhilun' != tes) || ('lqtq_hejuzhilun' == mes && 'lqtq_guangfangzhilun' != tes)) {
						game.log(player, '#y谋弈成功');
						event.result = {
							bool: true,
							mouyi: mes,
						};
						event.str = '<span><img src="extension/浪琴天阙/image/辅助图/谋弈成功.png" width="421" height="50"></span>';
					} else {
						game.log(player, '#y谋弈失败');
						event.result = { bool: false };
						event.str = '<span><img src="extension/浪琴天阙/image/辅助图/谋弈失败.png" width="347" height="100"></span>';
					}
				}
				('step 3');
				if (event.str) {
					const str = event.str;
					game.broadcastAll(function (str) {
						const dialog = ui.create.dialog(str);
						dialog.classList.add('center');
						setTimeout(function () {
							dialog.close();
						}, 1000);
					}, str);
				}
			};
			game.chazhao = function (str) {
				//硬币
				for (const i in lib.skill) {
					const text = lib.translate[i + '_info'];
					if (text && text.includes(str)) {
					}
				}
			};
			game.throwCoin = function (bool) {
				//硬币
				if (bool !== true && bool !== false) {
					bool = Math.random() < 0.5 ? true : false;
				}
				_status.event._result = {
					bool: bool,
				};
				game.pause();
				game.broadcastAll(function (bool) {
					const coinContainer = ui.create.div('.fullsize.dice-container', ui.window);
					ui.window.classList.add('dicepaused');
					const coin = ui.create.div('.lqtq_coin', coinContainer);
					ui.create.div('.lqtq_coin1', coin).setBackgroundImage('extension/浪琴天阙/image/辅助图/lqtq_zheng.png');
					for (let i = 1; i < 20; i++) {
						ui.create.div(coin).style.transform = 'translateZ(' + i + 'px)';
					}
					ui.create.div('.lqtq_coin2', coin).setBackgroundImage('extension/浪琴天阙/image/辅助图/lqtq_fan.png');
					coinContainer.appendChild(coin);
					if (bool === true) {
						coin.classList.add('lqtq_coin_true');
					} else {
						coin.classList.add('lqtq_coin_false');
					}
					coin.addEventListener('webkitAnimationEnd', function () {
						setTimeout(function () {
							coinContainer.delete();
							ui.window.classList.remove('dicepaused');
						}, 300);
						setTimeout(game.resume, 800);
					});
				}, bool);
			};
			game.changeDuoMo = function (num) {
				//堕魔
				if (!game.duomo) {
					game.duomo = 0;
				}
				if (num > 0) {
					game.duomo += num;
				}
				if (num < 0) {
					num = Math.min(-num, game.duomo);
					game.duomo -= num;
				}
				game.broadcast(function (num) {
					game.duomo = num;
				}, game.duomo);
				if (!window.dm) {
					window.dm = ui.create.div('.dm', ui.arena);
					if (lib.config.dm_renkuIndex) {
						window.dm.style.setProperty('--l', Math.round(lib.config.dm_renkuIndex.x * document.body.offsetWidth) + 'px');
						window.dm.style.setProperty('--t', Math.round(lib.config.dm_renkuIndex.y * document.body.offsetHeight) + 'px');
					}
				}
				let mp = 0,
					cs = 0,
					sx = 0;
				for (let i = 1; i <= game.duomo; i++) {
					if ([1, 4, 7, 10, 13].includes(i)) {
						mp++;
					}
					if ([2, 5, 8, 11, 14].includes(i)) {
						cs++;
					}
					if ([3, 6, 9, 12, 15].includes(i)) {
						sx++;
					}
				}
				window.dm.innerHTML = '回合摸牌+' + mp + '<br>回合出杀+' + cs + '<br>手牌上限+' + sx;
				let flag = 0,
					ol = 0,
					ot = 0;
				function defaultevent(e) {
					e.preventDefault();
				}
				if (lib.config.touchscreen) {
					window.dm.addEventListener('touchstart', function (e) {
						const evt = e || window.event;
						ol = evt.touches[0].clientX - window.dm.offsetLeft;
						ot = evt.touches[0].clientY - window.dm.offsetTop;
						document.addEventListener('touchmove', defaultevent, false);
					});
					window.dm.addEventListener('touchmove', function (e) {
						const evt = e || window.event;
						const oleft = evt.touches[0].clientX - ol;
						const otop = evt.touches[0].clientY - ot;
						window.dm.style.left = oleft + 'px';
						window.dm.style.top = otop + 'px';
					});
					window.dm.addEventListener('touchend', function () {
						if (!lib.config.dm_renkuIndex) {
							lib.config.dm_renkuIndex = {
								x: this.offsetLeft / document.body.offsetWidth,
								y: this.offsetTop / document.body.offsetHeight,
							};
						} else {
							lib.config.dm_renkuIndex.x = this.offsetLeft / document.body.offsetWidth;
							lib.config.dm_renkuIndex.y = this.offsetTop / document.body.offsetHeight;
						}
						game.saveConfig('dm_renkuIndex', lib.config.dm_renkuIndex);
						document.removeEventListener('touchmove', defaultevent);
					});
				} else {
					window.dm.onmousedown = function (e) {
						const evt = e || window.event;
						if (document.setCapture) {
							this.setCapture();
						}
						if (window.captureEvents) {
							window.captureEvents(Event.MOUSEMOVE | Event.MOUSEUP);
						}
						flag = 1;
						ol = evt.clientX - window.dm.offsetLeft;
						ot = evt.clientY - window.dm.offsetTop;
					};
					document.onmousemove = function (e) {
						const evt = e || window.event;
						if (flag) {
							window.dm.style.left = parseInt(evt.clientX - ol) + 'px';
							window.dm.style.top = parseInt(evt.clientY - ot) + 'px';
						} else {
							return null;
						}
					};
					window.dm.onmouseup = function () {
						if (!lib.config.dm_renkuIndex) {
							lib.config.dm_renkuIndex = {
								x: this.offsetLeft / document.body.offsetWidth,
								y: this.offsetTop / document.body.offsetHeight,
							};
						} else {
							lib.config.dm_renkuIndex.x = this.offsetLeft / document.body.offsetWidth;
							lib.config.dm_renkuIndex.y = this.offsetTop / document.body.offsetHeight;
						}
						game.saveConfig('dm_renkuIndex', lib.config.dm_renkuIndex);
						if (document.releaseCapture) {
							this.releaseCapture();
						}
						if (window.releaseEvents) {
							window.releaseEvents(Event.MOUSEMOVE | Event.MOUSEUP);
						}
						flag = 0;
					};
				}
			};
			game.changeMoShi = function (num) {
				//专属模式过滤
				if (!game.moshi) {
					game.moshi = 0;
				}
				if (num > 0) {
					game.moshi += num;
				}
			};
			game.changeMiHu = function (target) {
				//白板婊
				if (!game.mihu) {
					game.mihu = [];
				}
				if (target) {
					game.mihu = target;
				}
			};
			game.changeZhuGuoZi = function (num) {
				//朱果
				if (!game.zhuguozi) {
					game.zhuguozi = 0;
				}
				if (num > 0) {
					game.zhuguozi += num;
				}
				if (num < 0) {
					num = Math.min(-num, game.zhuguozi);
					game.zhuguozi -= num;
				}
				game.broadcast(function (num) {
					game.zhuguozi = num;
				}, game.zhuguozi);
				if (!window.zgz) {
					window.zgz = ui.create.div('.zgz', ui.arena);
					if (lib.config.zgz_renkuIndex) {
						window.zgz.style.setProperty('--l', Math.round(lib.config.zgz_renkuIndex.x * document.body.offsetWidth) + 'px');
						window.zgz.style.setProperty('--t', Math.round(lib.config.zgz_renkuIndex.y * document.body.offsetHeight) + 'px');
					}
				}
				window.zgz.innerHTML = '🍒' + ([0].includes(game.zhuguozi) ? '<b><font color=\"#FF5500\">' : '') + game.zhuguozi;
				let flag = 0,
					ol = 0,
					ot = 0;
				function defaultevent(e) {
					e.preventDefault();
				}
				if (lib.config.touchscreen) {
					window.zgz.addEventListener('touchstart', function (e) {
						const evt = e || window.event;
						ol = evt.touches[0].clientX - window.zgz.offsetLeft;
						ot = evt.touches[0].clientY - window.zgz.offsetTop;
						document.addEventListener('touchmove', defaultevent, false);
					});
					window.zgz.addEventListener('touchmove', function (e) {
						const evt = e || window.event;
						const oleft = evt.touches[0].clientX - ol;
						const otop = evt.touches[0].clientY - ot;
						window.zgz.style.left = oleft + 'px';
						window.zgz.style.top = otop + 'px';
					});
					window.zgz.addEventListener('touchend', function () {
						if (!lib.config.zgz_renkuIndex) {
							lib.config.zgz_renkuIndex = {
								x: this.offsetLeft / document.body.offsetWidth,
								y: this.offsetTop / document.body.offsetHeight,
							};
						} else {
							lib.config.zgz_renkuIndex.x = this.offsetLeft / document.body.offsetWidth;
							lib.config.zgz_renkuIndex.y = this.offsetTop / document.body.offsetHeight;
						}
						game.saveConfig('zgz_renkuIndex', lib.config.zgz_renkuIndex);
						document.removeEventListener('touchmove', defaultevent);
					});
				} else {
					window.zgz.onmousedown = function (e) {
						const evt = e || window.event;
						if (document.setCapture) {
							this.setCapture();
						}
						if (window.captureEvents) {
							window.captureEvents(Event.MOUSEMOVE | Event.MOUSEUP);
						}
						flag = 1;
						ol = evt.clientX - window.zgz.offsetLeft;
						ot = evt.clientY - window.zgz.offsetTop;
					};
					document.onmousemove = function (e) {
						const evt = e || window.event;
						if (flag) {
							window.zgz.style.left = parseInt(evt.clientX - ol) + 'px';
							window.zgz.style.top = parseInt(evt.clientY - ot) + 'px';
						} else {
							return null;
						}
					};
					window.zgz.onmouseup = function () {
						if (!lib.config.zgz_renkuIndex) {
							lib.config.zgz_renkuIndex = {
								x: this.offsetLeft / document.body.offsetWidth,
								y: this.offsetTop / document.body.offsetHeight,
							};
						} else {
							lib.config.zgz_renkuIndex.x = this.offsetLeft / document.body.offsetWidth;
							lib.config.zgz_renkuIndex.y = this.offsetTop / document.body.offsetHeight;
						}
						game.saveConfig('zgz_renkuIndex', lib.config.zgz_renkuIndex);
						if (document.releaseCapture) {
							this.releaseCapture();
						}
						if (window.releaseEvents) {
							window.releaseEvents(Event.MOUSEMOVE | Event.MOUSEUP);
						}
						flag = 0;
					};
				}
			}; //伤害音效
			if (lib.config.extension_浪琴天阙_lqtq_shanghai_damage) {
				lib.skill._lqtq_gaodamage = {
					//二毛五特效
					trigger: { source: 'damageBegin5' },
					forced: true,
					charlotte: true,
					firstDo: true,
					filter(event, player) {
						return event.source && event.source != event.player && event.num >= 3;
					},
					content() {
						if (trigger.num == 3) {
							trigger.player.$fullscreenpop('癫狂屠戮', 'fire');
							game.playAudio('../extension/浪琴天阙/audio/lqtq_diankuang.mp3');
						} else {
							trigger.player.$fullscreenpop('无双<br>万军取首', 'fire');
							game.playAudio('../extension/浪琴天阙/audio/lqtq_wanjun.mp3');
						}
					},
				};
			}
			// --------------------------------------挑战主代码------------------------------------------//

			const pian = lib.config.extension_浪琴天阙_lqtq_shanhaizhiyi_pian,
				pian1 = lib.config.extension_浪琴天阙_lqtq_langqintianque_pian,
				pia = lib.translate.lqtz_shanhai_info,
				pia1 = lib.translate.lqtz_tianque_info;
			if (pian == 'lqtz_jianghunjuexing') {
				((lib.translate.lqtz_shanhai_info = pia + '<br><br>第一关:<br>挑战魂·许褚、魂·甘宁、魂·关羽、魂·貂蝉中的随机一个.<br><br>第二关:<br>挑战魂·司马懿、魂·周瑜、魂·诸葛亮中的随机一个.<br><br>第三关:<br>挑战魂·曹操、魂·孙权、魂·刘备、魂·神吕布中的随机一个.'), (lib.translate.lqtz_shanhaizhiyi = get.translation('lqtz_jianghunjuexing2')));
			}
			if (pian == 'lqtz_quzhunianshou') {
				((lib.translate.lqtz_shanhai_info = pia + '<br><br>第一关:<br>挑战魑、魅、魍、魉中的随机一个.<br><br>第二关:<br>挑战牛头、马面中的随机一个.<br><br>第三关:<br>挑战年兽.'), (lib.translate.lqtz_shanhaizhiyi = get.translation('lqtz_quzhunianshou2')));
			}
			if (pian == 'lqtz_quguibixie') {
				((lib.translate.lqtz_shanhai_info = pia + '<br><br>第一关:<br>挑战魑、魅、魍、魉中的随机一个.<br><br>第二关:<br>挑战牛头、马面中的随机一个.<br><br>第三关:<br>挑战罗刹、夜叉、黑无常、白无常中的随机一个.'), (lib.translate.lqtz_shanhaizhiyi = get.translation('lqtz_quguibixie2')));
			}
			if (pian == 'lqtz_ruilinjiangshi') {
				((lib.translate.lqtz_shanhai_info = pia + '<br><br>召唤助阵:<br>游戏开始时召唤麒麟为我方助阵,击败敌人的同时也要保护好麒麟,若麒麟阵亡则直接判定挑战方败北.<br><br>第一关:<br>挑战穷奇、混沌中的随机一个.<br><br>第二关:<br>挑战饕餮、梼杌中的随机一个.<br><br>第三关:<br>挑战逐阴.'), (lib.translate.lqtz_shanhaizhiyi = get.translation('lqtz_ruilinjiangshi2')));
			}
			if (pian == 'lqtz_huhushengwei') {
				((lib.translate.lqtz_shanhai_info = pia + '<br><br>召唤助阵:<br>游戏开始时召唤小虎为我方助阵,击败敌人的同时也要保护好小虎,若小虎阵亡则直接判定挑战方败北.<br><br>关卡说明:<br>挑战朱雀、玄武两位神兽,BOSS阵亡后会进入修整会进入修整等待再度复活并进化变得更加强大,同时击杀两位神兽的真身才能获得胜利.'), (lib.translate.lqtz_shanhaizhiyi = get.translation('lqtz_huhushengwei2')));
			}
			if (pian == 'lqtz_qingqiunongqin') {
				((lib.translate.lqtz_shanhai_info = pia + '<br><br>关卡说明:<br>挑战青女,BOSS阵亡后会进入修整等待再度复活并进化变得更加强大,只有击杀BOSS的真身九尾狐才能获得胜利.'), (lib.translate.lqtz_shanhaizhiyi = get.translation('lqtz_qingqiunongqin2')));
			}
			if (pian1 == '0') {
				((lib.translate.lqtz_tianque_info = pia1 + '<br><br>第一关:<br>挑战扩展中「突发奇想」系列以及「闲做改值」系列的随机三名武将.<br><br>第二关:<br>挑战〖啊婊〗以及「亿婊堂堂」系列的随机一名武将.<br><br>第三关:<br>挑战「稀有专属」系列中随机一名武将.'), (lib.translate.lqtz_langqintianque = get.translation('lqtz_langqintianque')));
			}
			if (pian1 == '1') {
				((lib.translate.lqtz_tianque_info = pia1 + '<br><br>关卡说明:<br>挑战为无尽关卡,从第四关开始,BOSS方的体力开始上调,BOSS会获得<阙>标记,打开此标记点击标记中的<不响丸辣>按钮再打倒本次的BOSS可获得游戏胜利.<br><br>第一关:<br>挑战扩展中随机一名武将.<br><br>第二关:<br>挑战扩展中随机两名武将.<br><br>第三关以后:<br>挑战扩展中的随机三名武将.'), (lib.translate.lqtz_langqintianque = get.translation('lqtz_luanzhantianque')));
			}
			if (get.mode() == 'boss' && lib.config.extension_浪琴天阙_lqtq_xuanyuan_tiaozhan != '3') {
				//减员挑战
				lib.game.chooseCharacter = function (func) {
					const next = game.createEvent('chooseCharacter', false);
					next.showConfig = true;
					next.customreplacetarget = func;
					next.ai = function (player, list) {
						if (get.config('double_character')) {
							player.init(list[0], list[1]);
						} else {
							player.init(list[0]);
						}
					};
					next.setContent(function () {
						'step 0';
						let i;
						const list = [];
						event.list = list;
						for (const i in lib.character) {
							if (lib.character[i][4].includes('minskin')) {
								continue;
							}
							if (lib.character[i][4].includes('boss')) {
								continue;
							}
							if (lib.character[i][4].includes('hiddenboss')) {
								continue;
							}
							if (lib.config.forbidboss.includes(i)) {
								continue;
							}
							if (lib.filter.characterDisabled(i)) {
								continue;
							}
							list.push(i);
						}
						list.randomSort();
						const dialog = ui.create.dialog('选择参战角色', 'hidden');
						dialog.classList.add('fixed');
						ui.window.appendChild(dialog);
						dialog.classList.add('bosscharacter');
						dialog.classList.add('modeshortcutpause');
						dialog.classList.add('withbg');
						dialog.add([list.slice(0, 20), 'character']);
						dialog.noopen = true;
						const next = game.me.chooseButton(dialog, true).set('onfree', true);
						next._triggered = null;
						next.custom.replace.target = event.customreplacetarget;
						if (lib.config.extension_浪琴天阙_lqtq_xuanyuan_tiaozhan == '1') {
							next.selectButton = [1, 1];
						}
						if (lib.config.extension_浪琴天阙_lqtq_xuanyuan_tiaozhan == '2') {
							next.selectButton = [2, 2];
						}
						event.changeDialog = function () {
							if (ui.cheat2 && ui.cheat2.dialog == _status.event.dialog) {
								return;
							}
							if (game.changeCoin) {
								game.changeCoin(-3);
							}
							list.randomSort();
							const buttons = ui.create.div('.buttons');
							const node = _status.event.dialog.buttons[0].parentNode;
							_status.event.dialog.buttons = ui.create.buttons(list.slice(0, 20), 'character', buttons);
							_status.event.dialog.content.insertBefore(buttons, node);
							buttons.addTempClass('start');
							node.remove();
							game.uncheck();
							game.check();
						};
						ui.create.cheat = function () {
							_status.createControl = ui.cheat2 || event.asboss;
							ui.cheat = ui.create.control('更换', event.changeDialog);
							delete _status.createControl;
						};
						const createCharacterDialog = function () {
							event.dialogxx = ui.create.characterDialog();
							event.dialogxx.classList.add('bosscharacter');
							event.dialogxx.classList.add('withbg');
							event.dialogxx.classList.add('fixed');
							if (ui.cheat2) {
								ui.cheat2.addTempClass('controlpressdownx', 500);
								ui.cheat2.classList.remove('disabled');
							}
						};
						if (lib.onfree) {
							lib.onfree.push(createCharacterDialog);
						} else {
							createCharacterDialog();
						}
						ui.create.cheat2 = function () {
							_status.createControl = event.asboss;
							ui.cheat2 = ui.create.control('自由选将', function () {
								if (this.dialog == _status.event.dialog) {
									if (game.changeCoin) {
										game.changeCoin(50);
									}
									this.dialog.close();
									_status.event.dialog = this.backup;
									ui.window.appendChild(this.backup);
									delete this.backup;
									game.uncheck();
									game.check();
									if (ui.cheat) {
										ui.cheat.addTempClass('controlpressdownx', 500);
										ui.cheat.classList.remove('disabled');
									}
									if (_status.bosschoice) {
										_status.bosschoice.addTempClass('controlpressdownx', 500);
										_status.bosschoice.classList.remove('disabled');
									}
								} else {
									if (game.changeCoin) {
										game.changeCoin(-10);
									}
									this.backup = _status.event.dialog;
									_status.event.dialog.close();
									_status.event.dialog = _status.event.parent.dialogxx;
									this.dialog = _status.event.dialog;
									ui.window.appendChild(this.dialog);
									game.uncheck();
									game.check();
									if (ui.cheat) {
										ui.cheat.classList.add('disabled');
									}
									if (_status.bosschoice) {
										_status.bosschoice.classList.add('disabled');
									}
								}
							});
							if (lib.onfree) {
								ui.cheat2.classList.add('disabled');
							}
							delete _status.createControl;
						};
						if (!ui.cheat && get.config('change_choice')) {
							ui.create.cheat();
						}
						if (!ui.cheat2 && get.config('free_choose')) {
							ui.create.cheat2();
						}
						event.asboss = ui.create.control('应战', function () {
							event.boss = true;
							event.enemy = [];

							for (const i of ui.selected.buttons) {
								event.enemy.push(i.link);
								event.list.remove(i.link);
							}

							let sks = 3;
							if (lib.config.extension_浪琴天阙_lqtq_xuanyuan_tiaozhan == '1') {
								sks = 1;
							}
							if (lib.config.extension_浪琴天阙_lqtq_xuanyuan_tiaozhan == '2') {
								sks = 2;
							}
							while (event.enemy.length < sks) {
								let name = event.list.randomRemove();
								if (lib.boss[lib.storage.current] && lib.boss[lib.storage.current].randchoice) {
									name = lib.boss[lib.storage.current].randchoice(name, event.enemy);
								}
								event.enemy.push(name);
							}
							game.uncheck();
							if (ui.confirm) {
								ui.confirm.close();
							}
							game.resume();
						});
						('step 1');
						if (ui.cheat) {
							ui.cheat.close();
							delete ui.cheat;
						}
						if (ui.cheat2) {
							ui.cheat2.close();
							delete ui.cheat2;
						}
						event.asboss.close();
						if (_status.bosschoice) {
							_status.bosschoice.close();
							delete _status.bosschoice;
						}
						if (event.boss) {
							event.result = {
								boss: true,
								links: event.enemy,
							};
						} else {
							event.result = {
								boss: false,
								links: result.links,
							};
							_status.coinCoeff = get.coinCoeff(result.links);
						}
					});
					return next;
				};
			}
			if (lib.boss) {
				lib.boss.lqtz_shanhaizhiyi = {
					//山海志异
					loopType: 1,
					chongzheng: 0,
					checkResult(player) {
						if (player == game.boss && !['lqtz_jiuweihu', 'lqtz_nianshou', 'lqtzhun_caocao', 'lqtzhun_sunquan', 'lqtzhun_liubei', 'lqtzhun_lubu', 'lqtz_luocha', 'lqtz_yecha', 'lqtz_baiwuchang', 'lqtz_heiwuchang', 'lqtz_zhuyin'].includes(game.boss.name)) {
							return false;
						}
					},
					init() {
						game.addGlobalSkill('lqtz_shanhai');
						_status.additionalReward = function () {
							return 500;
						};
					},
				};
				lib.boss.lqtz_quguizhuxie = {
					//驱鬼逐邪
					loopType: 1,
					chongzheng: 0,
					gameDraw(player) {
						return player == game.boss ? 10 : 4;
					},
					checkResult(player) {
						return false;
					},
					init() {
						game.addGlobalSkill('lqtz_shibaipanduan');
						game.addGlobalSkill('lqtz_zhuxie');
						_status.additionalReward = function () {
							return 500;
						};
					},
				};
				lib.boss.lqtz_aolihagang = {
					//奥利哈刚
					loopType: 1,
					chongzheng: 0,
					minion: {
						2: 'lqtz_qijiasi',
						8: 'lqtz_jingzishibing',
					},
					checkResult(player) {
						if (player == game.boss && game.boss.name != 'lqtz_zhishen') {
							return false;
						}
					},
					gameDraw(player) {
						return player == game.boss ? 10 : 4;
					},
					init() {
						game.addGlobalSkill('lqtz_aogang');
						_status.additionalReward = function () {
							return 500;
						};
					},
				};
				lib.boss.lqtz_qingqingzijin = {
					//青青子衿
					loopType: 1,
					chongzheng: 0,
					minion: {
						2: ['lqtzqqzj_caocao', 'lqtzqqzj_simayi', 'lqtzqqzj_lubu', 'lqtzqqzj_dongzhuo', 'lqtzqqzj_zhangjiao', 'lqtzqqzj_yuanshu'].randomGet(),
					},
					checkResult(player) {
						return false;
					},
					gameDraw(player) {
						if (player == game.boss || player.getFriends(true).includes(game.boss)) {
							return 10;
						}
						return 4;
					},
					init() {
						game.addGlobalSkill('lqtz_shenglipanduan');
						game.addGlobalSkill('lqtz_shibaipanduan');
						game.addGlobalSkill('lqtz_zijin');
						_status.additionalReward = function () {
							return 500;
						};
					},
				};
				lib.boss.lqtz_tianweishenjia = {
					//天威神甲
					loopType: 1,
					chongzheng: 0,
					minion: {
						2: 'lqtz_ren',
						8: 'lqtz_ren',
					},
					checkResult(player) {
						if (player == game.boss && game.boss.name != 'lqtz_maerxiusishen') {
							return false;
						}
					},
					gameDraw(player) {
						if (player == game.boss || player.getFriends(true).includes(game.boss)) {
							return 10;
						}
						return 4;
					},
					init() {
						game.addGlobalSkill('lqtz_shenjia1');
						_status.additionalReward = function () {
							return 500;
						};
					},
				};
				lib.boss.lqtz_langqintianque = {
					//浪琴天阙
					loopType: 1,
					chongzheng: 0,
					checkResult(player) {
						return false;
					},
					gameDraw(player) {
						if (lib.config.extension_浪琴天阙_lqtq_langqintianque_pian == '0') {
							if (player == game.boss) {
								return 10;
							}
							if (player.getFriends(true).includes(game.boss)) {
								return 6;
							}
						}
						return 4;
					},
					init() {
						game.addGlobalSkill('lqtz_tianque');
						game.addGlobalSkill('lqtz_shibaipanduan');
						_status.additionalReward = function () {
							return 500;
						};
					},
				};
				lib.boss.lqtz_jiximengqin = {
					//急袭猛禽
					loopType: 1,
					chongzheng: 0,
					minion: {
						2: 'lqtz_quzhubojue',
						8: 'lqtz_quzhubojue',
					},
					checkResult(player) {
						if (player == game.boss && game.boss.name != 'lqtz_zuizhongbaoleilieying') {
							return false;
						}
					},
					gameDraw(player) {
						if (player == game.boss || player.getFriends(true).includes(game.boss)) {
							return 8;
						}
						return 5;
					},
					init() {
						game.addGlobalSkill('lqtz_RR');
						_status.additionalReward = function () {
							return 500;
						};
					},
				};
				lib.boss.lqtz_langqinbiao = {
					//浪琴婊
					loopType: 1,
					chongzheng: 0,
					checkResult(player) {
						if (player == game.boss) {
							return false;
						}
					},
					init() {
						setInterval(function () {
							if (game.boss && game.boss.name != 'lqtz_langqinbiao') {
								const zi = get.translation(game.boss.name);
								game.boss.name = 'lqtz_langqinbiao';
								lib.translate[game.boss.name] = zi;
								game.boss.update();
							}
							if (
								!game.hasPlayer(function (current) {
									return current == game.boss;
								})
							) {
								game.changeBossQ('lqtz_langqinbiao');
							}
						}, 100);
						_status.additionalReward = function () {
							return 500;
						};
					},
				};
				//—————————————————————————————————————————————————————————————————————————————boss模式相关函数,目前改用代理来排序
				const boss = function () {
					lib.skill._sort = {
						trigger: {
							player: ['phaseEnd'],
						},
						silent: true,
						forceDie: true,
						forceOut: true,
						filter() {
							game.sort();
						},
						content() { },
					}; //排座位
					let _me;
					Reflect.defineProperty(game, 'me', {
						get() {
							return _me;
						},
						set(v) {
							_me = v;
							if (game.players.includes(v) && game.players[0] != v) {
								game.sort(); //因为李白最先进入players,挑战模式不管选什么挑战李白,都会变成game.me是李白
							} //如果数组target[meIndex]是李白,那么替换掉的一瞬间,接下来调用就会再添加一个李白,导致数组两个李白
						}, //更换game.me之后第一时间排序
					});
					game.sort = function () {
						const players = game.players.filter(Boolean);
						const deads = game.dead.filter(Boolean);
						const allPlayers = deads.concat(players); //先移除players后面玩家会前移,再添加入dead需要同排序取前
						const bool = lib.config.dieremove;
						const playerx = bool ? players : allPlayers;
						ui.arena.setNumber(playerx.length);
						if (bool) {
							deads.forEach((player) => {
								player.classList.add('removing', 'hidden');
								if (!player.deadposition) {
									const num = Number(player.dataset.position);
									player.deadposition = num;
									player.dataset.position = num - 1;
								}
							});
						} //隐藏死亡角色
						playerx.sort((a, b) => Number(a.dataset.position) - Number(b.dataset.position));
						if (playerx.includes(game.me) && playerx[0] != game.me) {
							while (playerx[0] != game.me) {
								const start = playerx.shift();
								playerx.push(start);
							}
						} //将玩家排至数组首位
						playerx.forEach((player, index, array) => {
							player.dataset.position = index;
							const zhu = _status.roundStart || game.zhu || game.boss || array.find((p) => p.seatNum == 1) || array[0];
							const zhuPos = Number(zhu.dataset.position);
							const num = index - zhuPos + 1;
							if (index < zhuPos) {
								player.seatNum = players.length - num;
							} else {
								player.seatNum = num;
							}
						}); //修改dataset.position与seatNum
						players.sort((a, b) => Number(a.dataset.position) - Number(b.dataset.position));
						players.forEach((player, index, array) => {
							if (bool) {
								player.classList.remove('removing', 'hidden');
							}
							if (index == 0) {
								if (ui.handcards1Container && ui.handcards1Container.firstChild != player.node.handcards1) {
									while (ui.handcards1Container.firstChild) {
										ui.handcards1Container.firstChild.remove();
									}
									ui.handcards1Container.appendChild(player.node.handcards1.addTempClass('start').fix());
								}
								if (game.me != player) {
									ui.updatehl();
								}
							}
							player.previous = array[index === 0 ? array.length - 1 : index - 1];
							player.next = array[index === array.length - 1 ? 0 : index + 1];
						}); //展示零号位手牌/修改previous/显示元素
						allPlayers.sort((a, b) => Number(a.dataset.position) - Number(b.dataset.position));
						allPlayers.forEach((player, index, array) => {
							player.previousSeat = array[index === 0 ? array.length - 1 : index - 1];
							player.nextSeat = array[index === array.length - 1 ? 0 : index + 1];
						}); //修改previousSeat
						game.players.sort((a, b) => Number(a.dataset.position) - Number(b.dataset.position));
						return true;
					};
					game.players = new Proxy([], {
						set(target, property, value) {
							const result = Reflect.set(target, property, value);
							if (property === 'length') {
								game.sort();
							}
							return result;
						},
					});
					game.dead = new Proxy([], {
						set(target, property, value) {
							const result = Reflect.set(target, property, value);
							if (property === 'length') {
								game.sort();
							}
							return result;
						},
					});
					game.kongfunc = function () {
						return game.kong;
					};
					game.kong = {
						set() {
							return this;
						},
						get player() {
							return game.me;
						}, //先声明后赋值的,后面调用会是underfined,所以用getter实时获取
						cards: [],
						result: {
							cards: [],
						},
						gaintag: [],
						forResult() { },
					};
					game.changeBossQ = function (name) {
						_status.event.forceDie = true;
						const boss = game.addPlayerQ(name);
						boss.side = true;
						if (game.additionaldead) {
							game.additionaldead.push(game.boss);
						} else {
							game.additionaldead = [game.boss];
						}
						boss.setIdentity('zhu');
						boss.identity = 'zhu';
						const player = game.boss;
						game.boss = boss;
						game.addVideo('bossSwap', player, '_' + boss.name);
						if (game.me == player) {
							game.swapControl(boss);
						}
						return boss;
					};
					game.addPlayerQ = function (name) {
						const player = ui.create.player(ui.arena).addTempClass('start');
						player.getId();
						if (name) {
							player.init(name);
						}
						game.players.push(player);
						player.draw(Math.min(player.maxHp, 20));
						return player;
					};
					lib.element.player.addFellow = function (name) {
						const player = this;
						const npc = game.addPlayerQ(name);
						player.guhuo(npc);
						return npc;
					}; //添加随从
					lib.element.player.guhuo = function (target) {
						const player = this;
						target.side = player.side;
						let identity = player.identity;
						if (player.identity == 'zhu') {
							identity = 'zhong';
						} // 挑战模式多个主身份,会导致boss多个回合
						target.identity = identity;
						target.setIdentity(identity, 'blue');
						target.boss = player;
						target.ai.modAttitudeFrom = function (from, to, att) {
							if (to == from.boss) {
								return 99;
							}
							return att;
						}; //这里from是本人
						target.ai.modAttitudeTo = function (from, to, att) {
							if (to.boss == from) {
								return 99;
							}
							return att;
						}; //这里to是本人
						return player;
					}; //令一名角色服从你
				};
				boss();
			}
			// ------------------------------联机扩展武将栏-----------------------------------//
		},
		precontent(langqintianque) {
			game.import('character', function () {
				const langqintianque = {
					name: 'langqintianque',
					connect: true,
					// --------------------------------------武将技能------------------------------------------//
					skill: {
						_lqtq_xiaohui: {
							trigger: {
								global: 'cardsDiscardEnd',
								player: 'loseEnd',
							},
							filter(event, player) {
								if (Array.isArray(event.cards)) {
									for (const i of event.cards) {
										//QQQ
										if ((i.storage.lqtq_xiaohui || i.storage.lqtq_fuyuan) && get.position(i, true) == 'd') {
											return true;
										}
									}
								}
								return false;
							},
							forced: true,
							popup: false,
							content() {
								const cards = [];
								if (Array.isArray(trigger.cards)) {
									for (const i of trigger.cards) {
										if (i.storage.lqtq_xiaohui && get.position(i) == 'd') {
											cards.push(i);
										}
										if (i.storage.lqtq_fuyuan && get.position(i) == 'd') {
											const car = i.storage.lqtq_fuyuan;
											i.init(game.createCard(car[0], car[1], car[2], car[3]));
										}
									}
								}
								if (cards.length) {
									game.cardsGotoSpecial(cards);
								}
							},
						},
						_lqtq_wuxianbiaodu: {
							trigger: {
								player: 'dieBegin',
							},
							filter(event, player) {
								const a = Number(lib.config.extension_浪琴天阙_lqtq_wuxian_biaodu);
								if (a == 1) {
									return player == game.me && !game.mihu;
								}
								if (a == 2) {
									return !game.mihu;
								}
								return false;
							},
							popup: false,
							forced: true,
							_priority: Infinity,
							content() {
								player.uninit();
								player.init('lqtq_baibanbiao');
							},
						}, //测试

						//浪琴天阙技能
						lqtq_longling: {
							audio: 'ext:浪琴天阙/audio:2',
							init(player) {
								if (!player.storage.lqtq_longling) {
									player.storage.lqtq_longling = [];
								}
							},
							enable: ['chooseToUse', 'chooseToRespond'],
							derivation: 'lqtq_longling_zhu',
							filter(event, player) {
								for (const i of lib.inpile) {
									if (event.filterCard && event.filterCard({ name: i }, player, event)) {
										if (!event.responded && event.name != 'chooseToRespond' && !_status.dying.length && event.type != 'phase') {
											return player.countCards('hes', { suit: 'spade' }) > 0;
										}
										if (event.name == 'chooseToRespond') {
											return player.countCards('hes', { suit: 'club' }) > 0;
										}
										if (!player.storage.lqtq_longling.includes(i) && event.type == 'phase') {
											return player.countCards('hes', { suit: 'diamond' });
										}
										return player.countCards('hes', { suit: 'heart' }) > 0;
									}
								}
							},
							hiddenCard(player, name) {
								if (!_status.event.responded && _status.event.name != 'chooseToRespond' && !_status.dying.length && _status.event.type != 'phase') {
									return player.countCards('hes', { suit: 'spade' }) > 0;
								}
								if (_status.event.name == 'chooseToRespond') {
									return player.countCards('hes', { suit: 'club' }) > 0;
								}
								if (!player.storage.lqtq_longling.includes(name) && _status.event.type == 'phase') {
									return player.countCards('hes', { suit: 'diamond' });
								}
								return player.countCards('hes', { suit: 'heart' }) > 0;
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (player.storage.lqtq_longling.includes(name)) {
											continue;
										}
										if (name == 'sha') {
											if (event.filterCard && event.filterCard({ name: name }, player, event)) {
												list.push(['基本', '', 'sha']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'fire' }, player, event)) {
												list.push(['基本', '', 'sha', 'fire']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'thunder' }, player, event)) {
												list.push(['基本', '', 'sha', 'thunder']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'ice' }, player, event)) {
												list.push(['基本', '', 'sha', 'ice']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'kami' }, player, event)) {
												list.push(['基本', '', 'sha', 'kami']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'stab' }, player, event)) {
												list.push(['基本', '', 'sha', 'stab']);
											}
										} else if (get.type(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										} else if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['基本', '', name]);
										}
									}

									return ui.create.dialog('龙灵', [list, 'vcard']);
								},
								filter(button, player) {
									return _status.event.parent.filterCard({ name: button.link[2] }, player, _status.event.parent);
								},
								check(button) {
									if (_status.event.parent.type != 'phase') {
										return 1;
									}
									const player = _status.event.player;
									return player.getUseValue({
										name: button.link[2],
										nature: button.link[3],
									});
								},
								backup(links, player) {
									return {
										filterCard(card, event) {
											if (_status.event.respondTo) {
												return card.suit == 'spade';
											}
											if (_status.event.name == 'chooseToRespond') {
												return card.suit == 'club';
											}
											if (_status.event.type == 'phase') {
												return card.suit == 'diamond';
											}
											return card.suit == 'heart';
										},
										popname: true,
										selectCard: 1,
										check(card) {
											return 7 - get.value(card);
										},
										position: 'hes',
										viewAs: { name: links[0][2], nature: links[0][3] },
										onuse(result, player) {
											player.addTempSkill('lqtq_longling_count');
											if (get.type(result.card) == 'trick' && _status.event.type == 'phase') {
												player.storage.lqtq_longling.add(result.card.name);
											}
										},
									};
								},
								prompt(links, player) {
									return '将一张牌当做' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]) + '使用';
								},
							},
							ai: {
								fireAttack: true,
								respondSha: true,
								respondShan: true,
								skillTagFilter(player) {
									if (!player.countCards('hes')) {
										return false;
									}
								},
								order: 1,
								result: {
									player(player, target, card) {
										if (_status.event.dying) {
											return get.attitude(player, _status.event.dying);
										}
										return 1;
									},
								},
							},
						},
						lqtq_longling_count: {
							forced: true,
							charlotte: true,
							onremove(player) {
								player.storage.lqtq_longling = [];
							},
						},
						lqtq_shihuifen: {
							filter(event, player) {
								if (event.player.hasSkill('shihuifen')) {
									return false;
								}
								if (event.player == player) {
									return false;
								}
								if (!lib.filter.targetEnabled({ name: 'shihuifen' }, player, event.player)) {
									return false;
								}
								if (player.hasSkillTag('shihuifen')) {
									return true;
								}
								if (player.hasUsableCard('shihuifen')) {
									return true;
								}
								return false;
							},
						},
						lqtq_jinlianzhu: {
							filter(event, player) {
								if (!lib.filter.targetEnabled({ name: 'jinlianzhu' }, player, event.player)) {
									return false;
								}
								if (player.hasUsableCard('jinlianzhu')) {
									return true;
								}
								return false;
							},
						},
						lqtq_shenmiguo: {
							filter(event, player) {
								if (event.parent.name == 'g_shenmiguo') {
									return false;
								}
								if (_status.currentPhase != player) {
									return false;
								}
								if (event.parent.parent.name != 'phaseUse') {
									return false;
								}
								if (!event.targets || !event.card) {
									return false;
								}
								if (event.card.name == 'shenmiguo') {
									return false;
								}
								if (event.card.name == 'yuchankan') {
									return false;
								}
								if (player.hasSkill('shenmiguo2')) {
									return false;
								}
								if (get.info(event.card).complexTarget) {
									return false;
								}
								if (!lib.filter.cardEnabled(event.card, player, event.parent)) {
									return false;
								}
								const type = get.type(event.card);
								if (type != 'basic' && type != 'trick') {
									return false;
								}
								const card = game.createCard(event.card.name, event.card.suit, event.card.number, event.card.nature);
								const targets = event._targets || event.targets;

								for (const i of targets) {
									if (!i.isIn()) {
										return false;
									}
									if (!player.canUse({ name: event.card.name }, i, false, false)) {
										return false;
									}
								}

								if (player.hasUsableCard('shenmiguo')) {
									return true;
								}
								if (player.hasCard('yuchankan', 'hes')) {
									return true;
								}
								return false;
							},
						},
						lqtq_shencaojie: {
							filter(event, player) {
								if (get.type(event.card) != 'trick') {
									return false;
								}
								if (player.hasUsableCard('shencaojie')) {
									return true;
								}
								return false;
							},
						},
						lqtq_juejing: {
							mod: {
								maxHandcard(player, num) {
									return 3 + num;
								},
							},
							trigger: {
								player: ['damageBefore', 'damageEnd', 'loseHpBefore', 'loseHpEnd', 'phaseBegin', 'phaseEnd'],
							},
							audio: 'ext:浪琴天阙/audio:4',
							forced: true,
							content() {
								const suits = lib.suit.slice(0),
									list = [],
									a = [];
								for (const i of player.getCards('h')) {
									suits.remove(i.suit);
								}
								if (!suits.length) {
									player.draw();
								} else {
									get.cardPile(function (card) {
										if (!player.countCards('h', { suit: card.suit }) && !a.includes(card.suit)) {
											list.add(card);
											a.add(card.suit);
										}
									});
									if (trigger.name != 'phase') {
										player.gain(list.randomGet(), 'draw2');
									} else {
										player.gain(list, 'draw2');
									}
								}
							},
							ai: {
								maixie: true,
								maixie_hp: true,
							},
						},
						lqtq_huanshang: {
							trigger: {
								player: ['damageBegin4', 'loseHpBegin'],
							},
							audio: 'ext:浪琴天阙/audio:1',
							forced: true,
							_priority: -Infinity,
							filter(event, player) {
								return event.num > 1;
							},
							content() {
								'step 0';
								if (trigger.parent.name == 'lqtq_huanshang') {
									trigger.num = 1;
									event.finish();
								} else {
									event.num = Math.min(trigger.num, 9);
									trigger.cancel();
								}
								('step 1');
								event.num--;
								if (trigger.name == 'damage') {
									player.damage(trigger.nature, trigger.source || 'nosource');
								} else {
									player.loseHp();
								}
								('step 2');
								if (event.num > 0) {
									event.goto(1);
								}
							},
						},
						lqtq_yabo: {
							derivation: 'lqtq_yabo_zhu',
							audio: 'ext:浪琴天阙/audio:4',
							trigger: {
								global: ['phaseBegin', 'phaseAfter', 'phaseUseAfter', 'phaseUseBegin', 'loseAfter', 'gainAfter', 'equipAfter', 'changeHp', 'judgeAfter', 'linkAfter', 'turnOverAfter'],
							},
							init(player) {
								if (!player.storage.lqtq_yabo1) {
									player.storage.lqtq_yabo1 = [];
								}
								const a = [];
								for (const i in lib.character) {
									if (player != game.me && lib.character[i][3].length != 1) {
										continue;
									}
									a.push(i);
								}
								for (const i of a) {
									for (const j of lib.character[i][3].slice(0).slice(0)) {
										if (j.includes('rewrite') || player.storage.lqtq_yabo1.includes(j)) {
											continue;
										}
										if (!lib.translate[j] || !lib.translate[j + '_info']) {
											continue;
										}
										const info = get.info(j);
										if (!info || info.juexingji || info.hiddenSkill || info.groupSkill || info.zhuSkill || info.charlotte || info.unique) {
											continue;
										}
										if (
											game.hasPlayer(function (current) {
												return current.hasSkill(j) || (current.storage.lqtq_yabo2 && current.storage.lqtq_yabo2.includes(j));
											})
										) {
											continue;
										}
										player.storage.lqtq_yabo1.push(j);
									}
								}
								player.storage.lqtq_yabo2 = player.storage.lqtq_yabo1.randomGets(6);
							},
							forced: true,
							_priority: 26,
							filter(event, player) {
								if (Math.random() > 1 / 3) {
									return false;
								}
								if (
									game.hasPlayer(function (current) {
										return current.hasSkill('lqtq_yabo_add') && current.storage.lqtq_yabo3 == player;
									})
								) {
									return false;
								}
								return true;
							},
							content() {
								//QQQ
								const target = game.players.filter((q) => q != player && !q.hasSkill('lqtq_yabo_add')).randomGet();
								if (target) {
									target.storage.lqtq_yabo3 = player;
									target.addSkill('lqtq_yabo_add');
									setTimeout(function () {
										target.removeSkill('lqtq_yabo_add');
									}, 4000);
									setTimeout(function () {
										if (_status.auto || !player.isUnderControl(true)) {
											if (target.hasSkill('lqtq_yabo_add') && target.storage.lqtq_yabo3 == player) {
												lib.skill.lqtq_yabo_add.clickable(target);
											}
										}
									}, 2000);
								}
							},
							mark: true,
							marktext: '🐀',
							intro: {
								name: '🐀笼子',
								mark(dialog, content, player) {
									if (player.getStorage('lqtq_yabo').length) {
										dialog.addText('抓捕的🐀数:' + player.storage.lqtq_yabo.length);
									}
									if (player.isUnderControl(true)) {
										const b = [];
										for (const i of player.getSkills(true, false)) {
											const info = get.info(i);
											if (info && !info.charlotte && i.indexOf('rewrite') == -1 && lib.translate[i] && lib.translate[i + '_info'] && i != 'lqtq_yabo') {
												b.add(i);
											}
										}
										if (b.length && _status.gameStarted) {
											dialog.addText('可失去一个技能来获得一只🐀.');
											for (const i of b) {
												dialog.add(
													ui.create.div('.menubutton.pointerdiv', get.translation(i), function () {
														if (!this.disabled) {
															this.disabled = true;
															this.classList.add('disabled');
															this.style.opacity = 0.5;
															player.removeSkill(i);
															if (!player.storage.lqtq_yabo) {
																player.storage.lqtq_yabo = [];
															}
															player.storage.lqtq_yabo.push('🐀');
															player.markSkill('lqtq_yabo');
														}
													}),
												);
											}
										}
										if (player.getStorage('lqtq_yabo2').length && _status.gameStarted) {
											dialog.addText('可消耗两只🐀来获得一个技能.');
											for (const nama of player.getStorage('lqtq_yabo2')) {
												dialog.add(
													ui.create.div('.menubutton.pointerdiv', get.translation(nama), function () {
														if (!this.disabled) {
															this.disabled = true;
															this.classList.add('disabled');
															this.style.opacity = 0.5;
															if (player.getStorage('lqtq_yabo').length > 1) {
																player.addSkillLog(nama);
																const x = player.storage.lqtq_yabo,
																	y = x[0],
																	z = x[1];
																player.storage.lqtq_yabo.remove(y);
																player.storage.lqtq_yabo.remove(z);
																player.markSkill('lqtq_yabo');
																const a = [];
																for (const i of player.storage.lqtq_yabo1) {
																	if (
																		!game.hasPlayer(function (current) {
																			return current.hasSkill(i) || current.getStorage('lqtq_yabo2').includes(i);
																		})
																	) {
																		a.push(i);
																	}
																}
																player.storage.lqtq_yabo2 = a.randomGets(6);
															}
														}
													}),
												);
											}
										}
										if (player.getStorage('lqtq_yabo2').length) {
											for (const i of player.getStorage('lqtq_yabo2')) {
												dialog.add('<div><div class="skill">【' + lib.translate[i] + '】</div><div>' + get.skillInfoTranslation(i) + '</div></div>');
											}
										}
									}
								},
							},
							subSkill: {
								add: {
									clickable(player) {
										const players = game.findPlayer(function (current) {
											return player.storage.lqtq_yabo3 == current;
										});
										if (players) {
											if (!players.storage.lqtq_yabo) {
												players.storage.lqtq_yabo = [];
											}
											players.storage.lqtq_yabo.push('🐀');
											players.markSkill('lqtq_yabo');
											if ((_status.auto || !players.isUnderControl(true)) && players.storage.lqtq_yabo2 && players.getStorage('lqtq_yabo').length > 1) {
												const skills = players.storage.lqtq_yabo2.randomGet();
												const x = players.storage.lqtq_yabo,
													y = x[0],
													z = x[1];
												players.storage.lqtq_yabo.remove(y);
												players.storage.lqtq_yabo.remove(z);
												players.markSkill('lqtq_yabo');
												players.addSkillLog(skills);
												const a = [];
												for (const i of players.storage.lqtq_yabo1) {
													if (
														!game.hasPlayer(function (current) {
															return current.hasSkill(i) || current.getStorage('lqtq_yabo2').includes(i);
														})
													) {
														a.push(i);
													}
												}
												players.storage.lqtq_yabo2 = a.randomGets(6);
											}
										}
										delete player.storage.lqtq_yabo3;
										player.removeSkill('lqtq_yabo_add');
									},
									mark: true,
									marktext: '🐀',
									intro: {
										name: '看到这些鸭脖了吗',
										mark(dialog, content, player) {
											if (_status.gameStarted) {
												dialog.add(
													ui.create.div('.menubutton.pointerdiv', '🐀🐀🐀', function () {
														if (!this.disabled) {
															this.disabled = true;
															this.classList.add('disabled');
															this.style.opacity = 0.5;
															lib.skill.lqtq_yabo_add.clickable(player);
														}
													}),
												);
											}
										},
									},
								},
							},
						},
						lqtq_guhuo: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								global: ['phaseBegin', 'phaseAfter', 'phaseUseAfter', 'phaseUseBegin', 'loseAfter', 'gainAfter', 'equipAfter', 'useCard', 'useCardEnd', 'linkAfter'],
							},
							forced: true,
							_priority: 28,
							filter(event, player, name) {
								if (Math.random() > 1 / 4) {
									return false;
								}
								return !player.hasSkill('lqtq_guhuo_turn') && !_status.dying.length;
							},
							content() {
								'step 0';
								player.addTempSkill('lqtq_guhuo_turn', { player: 'lqtq_guhuoAfter' });
								const lis = [],
									a = ['lqym_xieyin', 'lqtz_xumou', 'lqtz_heiya', 'jxtp_gx_chongyingshenfu', 'gx_chongyingshenfu', 'txhj_chongyingshenfu', 'xdz_chongyinshenfux', 'jianxiong', 'rejianxiong', 'yxs_fanji', 'naxiang', 'new_wuhun', 'twwuhun', 'wuhun', 'ningbin', 'yishan', 'shiyong', 'duodao', 'zyexin', 'yuzhang', 'shiina_qingshen', 'nsqingde', 'ns_chuanshu2', 'nstaiping_nh', 'xicai', 'yiyong', 'xinmingshi', 'mjzuici', 'spguixiu', 'reluanzhan', 'fanghun', 'tuifeng', 'ranshang', 'bushi', 'xinfu_zhenyi', 'dcjinjin', 'dczimu', 'xinlvli'];
								for (const i in lib.skill) {
									const info = get.info(i);
									if (!info || info.hiddenSkill || info.groupSkill || info.zhuSkill || info.charlotte || info.unique || lis.includes(i) || a.includes(i) || !lib.translate[i] || !lib.translate[i + '_info']) {
										continue;
									}
									if (lib.skill[i].trigger) {
										if (lib.skill[i].trigger.player) {
											const j = lib.skill[i].trigger.player;
											if (typeof j == 'string') {
												if (j == 'damageEnd') {
													lis.push(i);
												} else {
													for (const l of j) {
														if (l == 'damageEnd') {
															lis.push(i);
														}
													}
												}
											}
										}
									}
								}
								if (lis.length) {
									const dialog = ui.create.dialog('<b>选择一个技能获得', 'hidden');
									const skills = lis.randomGets(4);
									const yi = document.createElement('div');
									yi.classList.add('add-setting');
									yi.style.margin = '0';
									yi.style.width = '100%';
									yi.style.position = 'relative';

									for (const i of skills) {
										const td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
										td.innerHTML = '<font size=4>【' + get.translation(i) + '】<br><P align=left>' + get.skillInfoTranslation(i);
										td.link = i;
										td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
										Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
										yi.appendChild(td);
										dialog.buttons.add(td);
									}

									dialog.content.appendChild(yi);
									const chooseButton = player.chooseButton(dialog, true);
									chooseButton.set('ai', function (button) {
										return Math.random();
									});
								} else {
									player.say('没有符合的技能,请开启武将包或关闭AI禁用武将');
									event.finish();
								}
								('step 1');
								if (result.links?.length) {
									const link = result.links[0];
									player.addTempSkill(link, { player: 'lqtq_guhuoAfter' });
									player.popup(link);
									game.log(player, '获得了技能', '【' + get.translation(link) + '】');
								}
								('step 2');
								if (player.hp == player.maxHp) {
									player.gainMaxHp();
								}
								player.recover();
								player.chooseTarget(true, '令一名角色对你造成一点伤害.', function (card, player, target) {
									return target.isAlive();
								}).ai = function (target) {
									return get.damageEffect(player, target, target);
								};
								('step 3');
								if (result.targets?.length) {
									player.damage(result.targets[0]);
								}
							},
							subSkill: {
								turn: {
									trigger: {
										player: 'turnOverBefore',
									},
									_priority: 20,
									forced: true,
									charlotte: true,
									audio: 'lqtq_guhuo',
									filter(event, player) {
										return !player.isTurnedOver();
									},
									content() {
										trigger.cancel();
										game.log(player, '取消了翻面');
									},
								},
							},
						},
						lqtq_xiqiao: {
							enable: 'phaseUse',
							usable: 1,
							filterTarget: true,
							prompt: '失去一点体力上限,令一名角色摸两张牌.',
							content() {
								'step 0';
								event.a = target;
								('step 1');
								player.loseMaxHp();
								event.a.draw(2);
								('step 2');
								player.chooseTarget('戏乔', '失去一点体力上限,令一名角色摸两张牌.').set('ai', function (target) {
									if ((player.maxHp > 3 && player.isDamaged()) || player.maxHp > 5) {
										return get.attitude(_status.event.player, target);
									}
									return false;
								});
								('step 3');
								if (result.targets?.length) {
									event.a = result.targets[0];
									event.goto(1);
								}
							},
							ai: {
								order: 5,
								result: {
									target(player, target) {
										if ((player.maxHp > 3 && player.isDamaged()) || player.maxHp > 5) {
											return 2;
										}
										return 0;
									},
								},
							},
						},
						lqtq_beixian: {
							trigger: {
								player: 'damageEnd',
							},
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player, name) {
								return player.countCards('he') && event.num > 0;
							},
							forced: true,
							content() {
								'step 0';
								player.chooseCardTarget({
									position: 'he',
									filterTarget: true,
									selectCard: [0, 2],
									filterCard(card, player, event) {
										return lib.filter.cardDiscardable(card, player, event);
									},
									ai1(card) {
										const player = _status.event.player;
										if (
											(!ui.selected.cards.length &&
												player.countCards('he', function (card1) {
													return (card.name != card1.name || get.type2(card) == get.type2(card1) || card.suit == card1.suit) && card.number == card1.number;
												})) ||
											(ui.selected.cards.length && card.number == ui.selected.cards[0].number)
										) {
											return 100 - get.value(card);
										}
										return 7 - get.value(card);
									},
									ai2(target) {
										if (target == player) {
											return 10;
										}
										return Math.random();
									},
									prompt: '是否发动【悲娴】？',
									prompt2: '弃置最多两张牌并选择一名角色.',
								});
								('step 1');
								if (result.targets?.length) {
									((event.target = result.targets[0]), (event.num = result.cards.length + trigger.num + 1 || trigger.num + 1));
									player.discard(result.cards, true);
									if (result.cards.length > 1) {
										if (result.cards[0].number == result.cards[1].number) {
											player.recover(trigger.num);
										}
									}
									const list = ['令' + get.translation(event.target) + '摸' + get.translation(event.num) + '张牌', '你弃置' + get.translation(event.target) + get.translation(event.num) + '张牌'];
									player
										.chooseControl()
										.set('choiceList', list)
										.set('choice', function () {
											return player.isAlive();
										})
										.set('ai', function () {
											if (get.attitude(_status.event.player, event.target) > 0) {
												return 0;
											}
											return 1;
										});
								}
								('step 2');
								if (result.index == 0) {
									event.target.draw(event.num);
								}
								if (result.index == 1) {
									player.discardPlayerCard(event.target, 'he', true, event.num);
								}
							},
						},
						lqtq_huiyi: {
							group: ['lqtq_huiyi_end', 'lqtq_huiyi_buff', 'lqtq_huiyi_use'],
							enable: 'chooseToUse',
							audio: 'ext:浪琴天阙/audio:2',
							usable: 1,
							filter(event, player) {
								if (player.getStat('skill').lqtq_huiyi_backup > 0 || !player.countCards('hes')) {
									return false;
								}
								for (const i of lib.inpile) {
									if (get.type(i) == 'trick' && event.filterCard && event.filterCard({ name: i }, player, event)) {
										return true;
									}
								}
								return false;
							},
							hiddenCard(player, name) {
								if (player.getStat('skill').lqtq_huiyi_backup > 0) {
									return false;
								}
								return get.type(name) == 'trick' && player.countCards('hes');
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (get.type(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										}
									}

									return ui.create.dialog('慧宜', [list, 'vcard']);
								},
								filter(button, player) {
									return _status.event.parent.filterCard({ name: button.link[2] }, player, _status.event.parent);
								},
								check(button) {
									if (_status.event.parent.type != 'phase') {
										return 1;
									}
									const player = _status.event.player;
									return player.getUseValue({
										name: button.link[2],
										nature: button.link[3],
									});
								},
								backup(links, player) {
									return {
										filterCard: true,
										popname: true,
										selectCard() {
											if (lib.card[links[0][2]].enable) {
												return [1, 2];
											}
											return 1;
										},
										check(card) {
											return 7 - get.value(card);
										},
										position: 'hes',
										viewAs: { name: links[0][2], nature: links[0][3] },
										onuse(result, player) { },
									};
								},
								prompt(links, player) {
									return '将一张牌当做' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]) + '使用';
								},
							},
							ai: {
								skillTagFilter(player) {
									if (!player.countCards('hes') || player.getStat('skill').lqtq_huiyi_backup > 0) {
										return false;
									}
									return true;
								},
								order: 1,
								result: {
									player: 1,
								},
							},
							subSkill: {
								end: {
									trigger: {
										global: 'phaseUseAfter',
									},
									forced: true,
									audio: 'lqtq_huiyi',
									_priority: 2,
									filter(event, player) {
										return (
											player.countCards('hes') &&
											event.player.getHistory('useCard', function (evt) {
												return (
													evt.isPhaseUsing() &&
													'trick' == get.type(evt.card) &&
													player.hasUseTarget({
														name: evt.card.name,
														nature: evt.card.nature,
													})
												);
											}).length
										);
									},
									content() {
										'step 0';
										const list = [];
										trigger.player.getHistory('useCard', function (evt) {
											if (!evt.isPhaseUsing() || 'trick' != get.type(evt.card)) {
												return;
											}
											list.add(evt.card.name);
										});
										for (let i = 0; i < list.length; i++) {
											list[i] = [get.type(list[i]), '', list[i]];
										}
										player
											.chooseButton([get.prompt('lqtq_huiyi'), [list, 'vcard']])
											.set('filterButton', function (button) {
												return player.hasUseTarget({ name: button.link[2], nature: button.link[3] });
											})
											.set('ai', function (button) {
												return player.getUseValue({ name: button.link[2], nature: button.link[3] });
											});
										('step 1');
										if (result.links?.length) {
											const card = { name: result.links[0][2], nature: result.links[0][3] };
											lib.skill.lqtq_huiyix.viewAs = card;
											const next = player.chooseToUse();
											if (next.isOnline()) {
												player.send(function (card) {
													lib.skill.lqtq_huiyix.viewAs = card;
												}, card);
											}
											next.set('openskilldialog', '###是否发动【慧宜】？###将一张牌当' + get.translation(card) + '使用');
											next.set('norestore', true);
											next.set('_backupevent', 'lqtq_huiyix');
											next.set('custom', {
												add: {},
												replace: { window() { } },
											});
											next.backup('lqtq_huiyix');
										}
									},
								},
								buff: {
									trigger: {
										player: 'useCard',
									},
									forced: true,
									audio: 'lqtq_huiyi',
									_priority: 2,
									filter(event, player, name) {
										return event.targets.length && event.cards.length > 1 && (event.skill == 'lqtq_huiyix' || event.skill == 'lqtq_huiyi_backup');
									},
									content() {
										trigger.effectCount++;
										game.log(player, '令', trigger.card, '额外了结算一次');
									},
								},
								use: {
									mod: {
										ignoredHandcard(card, player) {
											return card.hasGaintag('lqtq_huiyi');
										},
										cardDiscardable(card, player, name) {
											if (name == 'phaseDiscard' && card.hasGaintag('lqtq_huiyi')) {
												return false;
											}
										},
									},
									trigger: {
										player: 'gainBegin',
									},
									forced: true,
									audio: 'lqtq_huiyi',
									_priority: 2,
									filter(event, player) {
										return player.hasHistory('useCard', function (evt) {
											return [event.parent.name, event.getParent(2).name].includes(evt.card.name) && (evt.getParent(2).name == 'lqtq_huiyix' || evt.skill == 'lqtq_huiyi_backup');
										});
									},
									content() {
										trigger.gaintag.add('lqtq_huiyi');
										game.log(player, '令', trigger.cards, '标记', get.translation('lqtq_huiyi'));
									},
								},
							},
						},
						lqtq_huiyix: {
							filterCard(card, player) {
								return get.itemtype(card) == 'card';
							},
							selectCard: [1, 2],
							position: 'hes',
							popname: true,
						},
						lqtq_meizhi: {
							marktext: '汁',
							intro: {
								content: 'mark',
							},
							audio: 'ext:浪琴天阙/audio:2',
							enable: 'phaseUse',
							usable: 2,
							filterCard: true,
							selectCard() {
								if (!_status.event.player.getStat('skill').lqtq_meizhi) {
									return 0;
								}
								return [1, Infinity];
							},
							discard: false,
							lose: false,
							delay: 0,
							position: 'he',
							selectTarget() {
								if (!_status.event.player.getStat('skill').lqtq_meizhi) {
									return [0, 0];
								}
								return [1, 1];
							},
							filterTarget(card, player, target) {
								return player != target;
							},
							prompt(event) {
								return '将任意张牌交给一名其他角色.';
							},
							check(card) {
								if (ui.selected.cards.length && ui.selected.cards[0].name == 'du') {
									return 0;
								}
								if (!ui.selected.cards.length && card.name == 'du') {
									return 20;
								}
								const player = get.owner(card);
								if (ui.selected.cards.length >= Math.max(2, player.countCards('h') - player.hp)) {
									return 0;
								}
								if (player.hp == player.maxHp || player.countCards('h') <= 1) {
									const players = game.filterPlayer();
									for (const i of players) {
										if (i.hasSkill('haoshi') && !i.isTurnedOver() && !i.hasJudge('lebu') && get.attitude(player, i) >= 3 && get.attitude(i, player) >= 3) {
											return 11 - get.value(card);
										}
									}
									if (player.countCards('h') > player.hp) {
										return 10 - get.value(card);
									}
									return -1;
								}
								return 0;
							},
							content() {
								'step 0';
								if (player.getStat('skill').lqtq_meizhi > 1) {
									if (cards.length > 3) {
										player.addMark('lqtq_meizhi', 1);
									}
									target.gain(cards, player, 'giveAuto');
									event.finish();
									return;
								}
								((event.a = player.countMark('lqtq_meizhi') + 4 || 4), (event.b = ['lqtq_c1', 'lqtq_c2', 'lqtq_c3', 'lqtq_c4', 'lqtq_c5', 'lqtq_c6', 'lqtq_c7', 'lqtq_c8', 'lqtq_c9'].randomGets(4)));
								if (event.a > 9) {
									event.a = 9;
								}
								if (player.countMark('lqtq_meizhi') > 0) {
									player.removeMark('lqtq_meizhi', player.countMark('lqtq_meizhi'));
								}
								event.videoId = lib.status.videoId++;
								if (player.isUnderControl()) {
									game.swapPlayerAuto(player);
								}
								const switchToAuto = function () {
									game.pause();
									game.countChoose();
									setTimeout(function () {
										_status.imchoosing = false;
										event._result = {
											bool: true,
											links: [].concat(['lqtq_c1', 'lqtq_c2', 'lqtq_c3', 'lqtq_c4', 'lqtq_c5', 'lqtq_c6', 'lqtq_c7', 'lqtq_c8', 'lqtq_c9'].randomGets(event.a)),
										};
										if (event.dialog) {
											event.dialog.close();
										}
										if (event.controls) {
											for (const i of event.controls) {
												i.close();
											}
										}
										game.resume();
									}, 1000);
								};
								const createDialog = function (player, id) {
									if (player == game.me) {
										return;
									}
									let str = get.translation(player) + '正在表演...<br>';
									for (let i = 1; i < 10; i++) {
										str += get.translation('lqtq_c' + i);
										if (i % 3 != 0) {
											str += '　　';
										}
										if (i == 3) {
											str += '<br>';
										}
										if (i == 6) {
											str += '<br>';
										}
									}
									ui.create.dialog(str, 'forcebutton').videoId = id;
								};
								const chooseButton = function (player) {
									const event = _status.event;
									player = player || event.player;
									event.status = { lqtq_c1: 0, lqtq_c2: 0, lqtq_c3: 0, lqtq_c4: 0, lqtq_c5: 0, lqtq_c6: 0, lqtq_c7: 0, lqtq_c8: 0, lqtq_c9: 0 };
									event.map = { lqtq_c1: [100, 120], lqtq_c2: [100, 120], lqtq_c3: [100, 120], lqtq_c4: [100, 120], lqtq_c5: [100, 120], lqtq_c6: [100, 120], lqtq_c7: [100, 120], lqtq_c8: [100, 120], lqtq_c9: [100, 120] };
									if (player.countMark('lqtq_meizhi') > 0) {
										player.removeMark('lqtq_meizhi', player.countMark('lqtq_meizhi'));
									}
									event.finishedx = [];
									event.str = '【妹子戏汁图】<br><img src="extension/浪琴天阙/image/辅助图/lqtq_card1.png" width="70" height="70">lqtq_c1% <img src="extension/浪琴天阙/image/辅助图/lqtq_card2.png" width="70" height="70">lqtq_c2% <img src="extension/浪琴天阙/image/辅助图/lqtq_card3.png" width="70" height="70">lqtq_c3%<br><img src="extension/浪琴天阙/image/辅助图/lqtq_card4.png" width="70" height="70">lqtq_c4%<img src="extension/浪琴天阙/image/辅助图/lqtq_card5.png" width="70" height="70">lqtq_c5% <img src="extension/浪琴天阙/image/辅助图/lqtq_card6.png" width="70" height="70">lqtq_c6%<br><img src="extension/浪琴天阙/image/辅助图/lqtq_card7.png" width="70" height="70">lqtq_c7% <img src="extension/浪琴天阙/image/辅助图/lqtq_card8.png" width="70" height="70">lqtq_c8% <img src="extension/浪琴天阙/image/辅助图/lqtq_card9.png" width="70" height="70">lqtq_c9%';
									event.dialog = ui.create.dialog(event.str, 'forcebutton', 'hidden');
									event.dialog.addText('<li>点击下方的妹子按钮,猜测拥有妹汁的妹子是谁.', false);
									event.dialog.open();
									for (const i in event.status) {
										event.dialog.content.childNodes[0].innerHTML = event.dialog.content.childNodes[0].innerHTML.replace(i, event.status[i]);
									}

									for (const i of event.dialog.buttons) {
										i.classList.add('pointerdiv');
									}

									event.switchToAuto = function () {
										event._result = {
											bool: true,
											links: event.finishedx.slice(0),
										};
										event.dialog.close();
										for (const i of event.controls) {
											i.close();
										}
										game.resume();
										_status.imchoosing = false;
									};
									event.controls = [];
									for (let i = 1; i <= 9; i++) {
										event.controls.push(
											ui.create.control('lqtq_c' + i, function (link) {
												const event = _status.event;
												if (event.finishedx.includes(link)) {
													return;
												}
												event.status[link] += get.rand.apply(get, event.map[link]);
												if (event.status[link] >= 100) {
													event.status[link] = 100;
													var str = event.str.slice(0);
													for (const i in event.status) {
														str = str.replace(i, event.status[i]);
													}
													event.dialog.content.childNodes[0].innerHTML = str;
													event.finishedx.push(link);
													if (event.finishedx.length >= event.a) {
														event._result = {
															bool: true,
															links: event.finishedx.slice(0),
														};
														event.dialog.close();
														for (const i of event.controls) {
															i.close();
														}
														game.resume();
														_status.imchoosing = false;
													}
												} else {
													var str = event.str.slice(0);
													for (const i in event.status) {
														str = str.replace(i, event.status[i]);
													}
													event.dialog.content.childNodes[0].innerHTML = str;
												}
											}),
										);
									}

									for (const i of event.dialog.buttons) {
										i.classList.add('selectable');
									}

									game.pause();
									game.countChoose();
								};
								game.broadcastAll(createDialog, player, event.videoId);
								if (event.isMine()) {
									chooseButton();
								} else if (event.isOnline()) {
									event.player.send(chooseButton, event.player);
									event.player.wait();
									game.pause();
								} else {
									switchToAuto();
								}
								('step 1');
								game.broadcastAll('closeDialog', event.videoId);
								let map = event.result || result,
									list = map.links,
									d = [],
									e = 0,
									f = [],
									g = [];
								if (!map || !map.bool || !map.links || !list.length) {
									game.log(player, '寻找失败');
									event.finish();
									return;
								}
								for (const i of list) {
									if (event.b.includes(i)) {
										e++;
									}
								}
								for (let i = 1; i <= 9; i++) {
									if (event.b.includes('lqtq_c' + i)) {
										d.add('lqtq_card' + i);
									}
								}
								let dialog = ui.create.dialog('【妹子戏汁图】', '恭喜你找到了' + get.translation(e) + '份妹汁,拥有妹汁的妹子如下', 'hidden');
								if (e == 0) {
									dialog = ui.create.dialog('【妹子戏汁图】', '很遗憾你没有找到妹汁,拥有妹汁的妹子如下', 'hidden');
								}
								const er = document.createElement('div');
								er.classList.add('add-setting');
								er.style.margin = '0';
								er.style.width = '100%';
								er.style.position = 'relative';
								for (const i of d) {
									const td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
									td.innerHTML = '<img src="extension/浪琴天阙/image/辅助图/' + i + '.png" width="75" height="75"></span>';
									td.link = i;
									td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
									Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
									er.appendChild(td);
									dialog.buttons.add(td);
								}
								dialog.content.appendChild(er);
								if (e == 0) {
									dialog.add('请求一名妹子安慰一下你脆弱的心灵');
								} else {
									dialog.add('请求一名妹子为你发放奖励');
								}
								player.chooseButton(dialog, true);
								if (e > 3) {
									player.addMark('lqtq_meizhi');
								}
								if (e > 0) {
									get.cardPile(function (card) {
										let h = get.type2(card),
											c = 3;
										if (e > 1) {
											((h = card.suit), (c = 4));
										}
										if (e > 2) {
											((h = card.name), (c = 5));
										}
										if ((!g.length && get.type(card) == 'equip') || (g.length && g.length < c && !f.includes(h))) {
											g.push(card);
											f.push(h);
										}
									});
								}
								if (g.length) {
									player.gain(g, 'gain2');
								} else {
									player.draw();
								}
							},
							ai: {
								order(skill, player) {
									if (!player.getStat('skill').lqtq_meizhi) {
										return 20;
									}
									return 0.4;
								},
								result: {
									player(player, target) {
										if (!player.getStat('skill').lqtq_meizhi) {
											return 2;
										}
										return 0;
									},
									target(player, target) {
										const a = player.getStat('skill').lqtq_meizhi || 0;
										if (a > 0) {
											if (target.hasSkillTag('nogain')) {
												return 0;
											}
											if (ui.selected.cards.length && ui.selected.cards[0].name == 'du') {
												if (target.hasSkillTag('nodu')) {
													return 0;
												}
												return -10;
											}
											if (target.hasJudge('lebu')) {
												return 0;
											}
											const nh = target.countCards('h');
											const np = player.countCards('h');
											if (player.hp == player.maxHp || player.storage.rende < 0 || player.countCards('h') <= 1) {
												if (nh >= np - 1 && np <= player.hp && !target.hasSkill('haoshi')) {
													return 0;
												}
											}
											return Math.max(1, 5 - nh);
										}
									},
								},
								effect: {
									target(card, player, target) {
										const a = player.getStat('skill').lqtq_meizhi || 0;
										if (a > 0 && player == target && get.type(card) == 'equip') {
											if (player.countCards('e', { subtype: get.subtype(card) })) {
												const players = game.filterPlayer();
												for (const i of players) {
													if (i != player && get.attitude(player, i) > 0) {
														return 0;
													}
												}
											}
										}
									},
								},
								threaten: 0.8,
							},
						},
						lqtq_jingqiao: {
							audio: 'ext:浪琴天阙/audio:2',
							mod: {
								maxHandcard(player, num) {
									return (
										3 +
										player.countCards('e', function (card) {
											return get.type(card) == 'equip' && card.storage.lqtq_jingqiao;
										})
									);
								},
							},
							enable: 'phaseUse',
							audio: 'ext:浪琴天阙/audio:2',
							prompt: '选择一张装备牌镶合技能.',
							filter(event, player) {
								return (
									player.countCards('he', function (card) {
										return get.type(card) == 'equip' && !card.storage.lqtq_jingqiao;
									}) > 0
								);
							},
							filterCard(card, player) {
								return !card.storage.lqtq_jingqiao && get.type(card) == 'equip';
							},
							position: 'he',
							check(card) {
								return 100 - get.value(card);
							},
							discard: false,
							lose: false,
							delay: 0,
							content() {
								'step 0';
								const a = [],
									b = [],
									c = [];
								for (const i in lib.character) {
									const d = lib.character[i][4];
									if (lib.character[i][3].length == 1 && (lib.rank.rarity.rare.includes(i) || lib.rank.rarity.epic.includes(i) || lib.rank.rarity.legend.includes(i)) && ((game.me == player && lib.config.extension_浪琴天阙_lqtq_shaiji_kuochong) || !lib.config.forbidai.includes(i))) {
										a.push(i);
									}
								}
								a.randomSort();
								for (const i of a) {
									for (const j of lib.character[i][3].slice(0).slice(0)) {
										if (j.includes('rewrite') || b.includes(j) || !lib.translate[j] || !lib.translate[j + '_info']) {
											continue;
										}
										if (
											game.hasPlayer(function (current) {
												return current.hasSkill(j);
											})
										) {
											continue;
										}
										const info = get.info(j);
										if (info && !info.juexingji && !info.hiddenSkill && !info.zhuSkill && !info.charlotte && !info.limited && !info.dutySkill && !info.unique) {
											b.push(j);
											c.push(i);
											continue;
										}
									}
									if (c.length > 3) {
										break;
									}
								}
								if (c.length) {
									player.chooseControl(b).set('dialog', ['选择一个技能镶合在' + get.translation(cards[0]) + '上.', [c, 'character']]);
								} else {
									event.finish();
									player.say('没有符合的武将技能,请开启「武将包」或本扩展的「筛技扩充」功能');
								}
								('step 1');
								const name1 = cards[0].name,
									name = 'lqtq_' + result.control + name1;
								if (!lib.card[name]) {
									lib.card[name] = lib.card[name1];
									if (!lib.card[name].skills) {
										lib.card[name].skills = [];
									}
									lib.card[name].skills.push(result.control);
									lib.translate[name + '_info'] = lib.translate[name1 + '_info'] + lib.translate[result.control + '_info'];
									lib.card[name].cardimage = name1;
									let links = get.translation(cards[0].name),
										str = get.translation(result.control);
									if (links.length > 3) {
										for (let i = 0; i < links.length; i++) {
											if (i == 0 || i == 1 || i == links.length - 1) {
												str += get.translation(links[i]);
											}
										}
									} else {
										str += links;
									}
									lib.translate[name] = str;
								}
								cards[0].init(game.createCard(name, cards[0].suit, cards[0].number));
								cards[0].storage.lqtq_jingqiao = true;
							},
							ai: {
								order: 20,
								result: {
									player: 2,
								},
							},
							group: ['lqtq_jingqiao_dying', 'lqtq_jingqiao_gain'],
							subSkill: {
								dying: {
									prompt: '重铸一张装备牌,将体力回复至1点.',
									audio: 'lqtq_jingqiao',
									enable: 'chooseToUse',
									filterCard(card, player) {
										return get.type(card) == 'equip';
									},
									filter(event, player) {
										return event.type == 'dying' && player == event.dying && player.countCards('he', { type: 'equip' });
									},
									check() {
										return 1;
									},
									position: 'he',
									discard: false,
									loseTo: 'discardPile',
									prepare(cards, player) {
										player.$throw(cards, 1000);
										game.log(player, '将', cards, '置入了弃牌堆');
									},
									content() {
										'step 0';
										player.draw();
										('step 1');
										const num = 1 - player.hp;
										if (num > 0) {
											player.recover(num);
										}
										player.addMark('lqtq_meizhi', 1);
										if (cards[0].storage.lqtq_jingqiao && get.subtype(cards[0]) == 'equip2') {
											player.hp = player.maxHp;
										}
										if (cards[0].storage.lqtq_jingqiao && get.subtype(cards[0]) == 'equip1') {
											player.addTempSkill('lqtq_jingqiao_damage');
										}
									},
									ai: {
										order: 0.5,
										skillTagFilter(player, arg, target) {
											if (player != target) {
												return false;
											}
											return (
												player.countCards('he', function (card) {
													if (_status.connectMode && get.position(card) == 'h') {
														return true;
													}
													return get.type(card) == 'equip';
												}) > 0
											);
										},
										save: true,
										result: {
											player(player) {
												return 10;
											},
										},
									},
								},
								gain: {
									trigger: {
										player: 'loseAfter',
										global: ['equipAfter', 'addJudgeAfter', 'gainAfter', 'loseAsyncAfter'],
									},
									forced: true,
									audio: 'lqtq_jingqiao',
									filter(event, player) {
										const evt = event.getl(player);
										if (evt && evt.player == player && evt.es && evt.es.length) {
											for (const i of evt.es) {
												if (i.storage.lqtq_jingqiao) {
													return true;
												}
											}
										}
										return false;
									},
									content() {
										'step 0';
										let a = 0,
											b = 0;
										for (const i of trigger.getl(player).es) {
											if (get.subtype(i) == 'equip4') {
												a++;
											}
											if (get.subtype(i) == 'equip3') {
												b++;
											}
										}
										if (a > 0) {
											player.draw(a);
										}
										if (b > 0) {
											player.changeHujia(b);
										}
									},
								},
								damage: {
									trigger: {
										player: 'dyingAfter',
									},
									popup: false,
									forced: true,
									content() {
										player.removeSkill('lqtq_jingqiao_damage');
										if (trigger.source) {
											trigger.source.damage();
										}
									},
								},
							},
						},
						lqtq_tianhui: {
							audio: 'ext:浪琴天阙/audio:2',
							init(player) {
								if (!player.storage.lqtq_tianhui2) {
									player.storage.lqtq_tianhui2 = [];
								}
								if (!player.storage.lqtq_tianhui3) {
									player.storage.lqtq_tianhui3 = [];
								}
							},
							enable: ['chooseToUse', 'chooseToRespond'],
							filter(event, player) {
								const list = [],
									lists = get.typeCard('basic');
								for (const i of get.typeCard('trick')) {
									lists.push(i);
								}
								for (const i of get.typeCard('delay')) {
									lists.push(i);
								}
								for (const i of get.typeCard('equip')) {
									lists.push(i);
								}

								for (const j of lists) {
									const i = j;
									if (event.filterCard && event.filterCard({ name: i }, player, event)) {
										if (player.storage.lqtq_tianhui3.includes(i) && player.hasSkill('lqtq_tianhui_count')) {
											return false;
										}
										if (player.countCards('hes')) {
											return true;
										}
									}
								}

								return false;
							},
							hiddenCard(player, name) {
								if (player.storage.lqtq_tianhui3.includes(name) && player.hasSkill('lqtq_tianhui_count')) {
									return false;
								}
								return player.countCards('hes');
							},
							chooseButton: {
								dialog(event, player) {
									const list = [],
										lists = get.typeCard('basic');
									for (const i of get.typeCard('trick')) {
										lists.push(i);
									}
									for (const i of get.typeCard('delay')) {
										lists.push(i);
									}
									for (const i of get.typeCard('equip')) {
										lists.push(i);
									}

									for (const j of lists) {
										const name = j;
										if (['gw_dieyi_judge', 'yanxiao_card', 'nsfuzhou_card', 'feichu_equip1', 'gw_dieyi_equip1', 'feichu_equip2', 'gw_dieyi_equip2', 'feichu_equip3', 'gw_dieyi_equip3', 'feichu_equip4', 'gw_dieyi_equip4', 'feichu_equip5', 'gw_dieyi_equip5'].includes(name)) {
											continue;
										}
										if (player.storage.lqtq_tianhui3.includes(name) && player.hasSkill('lqtq_tianhui_count')) {
											continue;
										}
										if (name == 'sha') {
											if (event.filterCard && event.filterCard({ name: name }, player, event)) {
												list.push(['基本', '', 'sha']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'fire' }, player, event)) {
												list.push(['基本', '', 'sha', 'fire']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'thunder' }, player, event)) {
												list.push(['基本', '', 'sha', 'thunder']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'ice' }, player, event)) {
												list.push(['基本', '', 'sha', 'ice']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'kami' }, player, event)) {
												list.push(['基本', '', 'sha', 'kami']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'stab' }, player, event)) {
												list.push(['基本', '', 'sha', 'stab']);
											}
										} else if (get.type2(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										} else if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['基本', '', name]);
										} else if (get.type(name) == 'equip' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['装备', '', name]);
										}
									}

									const a = ui.create.dialog('天慧');
									a.addSmall([list, 'vcard']);
									return a;
								},
								filter(button, player) {
									return _status.event.parent.filterCard({ name: button.link[2] }, player, _status.event.parent);
								},
								check(button) {
									if (_status.event.parent.type != 'phase') {
										return 1;
									}
									const player = _status.event.player;
									if (player.countCards('h', button.link[2]) > 0) {
										return 0;
									}
									if (player.countCards('hes') < 3) {
										return button.link[2] == 'tianjitu';
									}
									return player.getUseValue({
										name: button.link[2],
										nature: button.link[3],
									});
								},
								backup(links, player) {
									return {
										filterCard: true,
										popname: true,
										selectCard: 1,
										check(card) {
											if (_status.event.player.countCards('hes') > 3 && card.name == 'tianjitu') {
												return 0;
											}
											return 100 - get.value(card);
										},
										position: 'hes',
										viewAs: { name: links[0][2], nature: links[0][3] },
										onuse(result, player) {
											player.storage.lqtq_tianhui3.add(result.card.name);
											if (get.type(result.card) == 'equip') {
												player.storage.lqtq_tianhui2.add(result.card.name);
											}
										},
										onrespond(result, player) {
											player.storage.lqtq_tianhui3.add(result.card.name);
										},
									};
								},
								prompt(links, player) {
									return '将一张牌张牌当做' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]) + '使用';
								},
							},
							ai: {
								fireAttack: true,
								respondSha: true,
								respondShan: true,
								skillTagFilter(player) {
									if (!player.countCards('hes')) {
										return false;
									}
								},
								order(skill, player) {
									if (_status.event.player.countCards('hes') < 3 && _status.event.parent.type == 'phase') {
										return 20;
									}
									return 1;
								},
								result: {
									player(player) {
										if (_status.event.dying) {
											return get.attitude(player, _status.event.dying);
										}
										return 1;
									},
								},
							},
							group: ['lqtq_tianhui_equip', 'g_jinlianzhu', 'g_shenmiguo', 'g_shencaojie', 'g_shihuifen', 'g_chenhuodajie', 'g_youdishenru', 'caochuan_skill'],
							intro: {
								content: 'mark',
							},
							subSkill: {
								equip: {
									trigger: {
										player: ['useCardAfter', 'respondAfter', 'useCardToBegin', 'gainEnd'],
									},
									popup: false,
									forced: true,
									filter(event, player, name) {
										if (event.parent.parent.name == 'phaseDraw') {
											return false;
										}
										if (name == 'useCardToBegin') {
											return event.skill == 'lqtq_tianhui_backup' && get.type(event.card) == 'equip' && player.storage.lqtq_tianhui2.length;
										}
										if (name == 'gainEnd') {
											return player == _status.currentPhase && event.cards && event.cards.length && event.cards.length > 1;
										}
										return event.skill == 'lqtq_tianhui_backup' && player.countCards('he');
									},
									content() {
										'step 0';
										if (event.triggername == 'useCardToBegin') {
											const car = [trigger.cards[0].name, trigger.cards[0].suit, trigger.cards[0].number, trigger.cards[0].nature],
												card = player.storage.lqtq_tianhui2;

											for (const i of card) {
												var name = i;
											}

											const card1 = trigger.cards[0].init(game.createCard(name, trigger.cards[0].suit, trigger.cards[0].number));
											if (!card1.storage.lqtq_fuyuan) {
												card1.storage.lqtq_fuyuan = car;
											}
											player.storage.lqtq_tianhui2 = [];
										} else if (event.triggername == 'gainEnd') {
											player.addTempSkill('lqtq_tianhui_count');
										} else {
											player.chooseToDiscard(true, 'he');
										}
										('step 1');
										if (result.bool) {
											player.addMark('lqtq_tianhui');
										}
									},
								},
								count: {
									forced: true,
									popup: false,
									charlotte: true,
									onremove(player) {
										player.storage.lqtq_tianhui3 = [];
									},
								},
							},
						},
						lqtq_tianzhi: {
							mod: {
								maxHandcard(player, num) {
									return (num = 1 + player.countMark('lqtq_tianhui'));
								},
							},
							trigger: {
								player: ['phaseUseAfter', 'dyingBefore'],
							},
							audio: 'ext:浪琴天阙/audio:3',
							forced: true,
							filter(event, player) {
								return player.hasMark('lqtq_tianhui');
							},
							content() {
								'step 0';
								if (event.isMine()) {
									if (!event.num) {
										event.num = 0;
									}
									if (!event.arr) {
										event.arr = [0, player.countMark('lqtq_tianhui')];
									}
									if (!event.str) {
										event.str = '###是否发动【天智】？###选择一个数值摸等量的牌,数值为0时则视为不发动.';
									}
									event.dialog = ui.create.dialog(event.str);
									event.count = ui.create.control('-', event.num, '+');
									const newcount = event.count.cloneNode(true);
									var list = newcount.childNodes;
									list[0].style.width = '20px';
									list[0].onclick = function () {
										event.num--;
										if (event.num < event.arr[0]) {
											event.num = event.arr[1];
										}
										list[1].innerHTML = event.num;
									};
									list[1].style.width = '60px';
									list[1].onclick = function () {
										newcount.parentNode.removeChild(newcount);
										event.dialog.parentNode.removeChild(event.dialog);
										game.resume();
									};
									list[2].style.width = '20px';
									list[2].onclick = function () {
										event.num++;
										if (event.num > event.arr[1]) {
											event.num = event.arr[0];
										}
										list[1].innerHTML = event.num;
									};
									event.count.parentNode.replaceChild(newcount, event.count);
									ui.updatec();
									game.pause();
								} else if (trigger.name == 'dying') {
									event.num = Math.min(player.countMark('lqtq_tianhui'), 3);
								} else {
									let a = player.getHandcardLimit() - player.countCards('h');
									if (player.getHandcardLimit() > 5 && player.getHandcardLimit() % 2 == 0) {
										a--;
									}
									event.num = Math.floor(a / 2);
								}
								('step 1');
								if (event.num > 0) {
									player.removeMark('lqtq_tianhui', event.num);
									if (trigger.name != 'dying') {
										player.draw(event.num);
										var list = [];
										get.cardPile(function (card) {
											if (card.name == 'wuxie' || (get.type(card) == 'basic' && get.color(card) == 'black')) {
												list.push(card);
											}
										});
										if (event.num > 2) {
											player.gain(list.randomGet(), 'draw2');
										}
									} else {
										const lise = ['guihan', 'xinjuejing', 'duhun', 'gwfusheng', 'shouwang', 'yuelu', 'chongsheng', 'xinfu_jingxie', 'wanlan', 'liechi', 'refuli', 'requanfeng', 'zuici', 'spyuejian', 'rezuici', 'mduohun', 'niepan', 'oldniepan', 'buqu', 'chenqing', 'luanfeng', 'decadexushen', 'xinfu_jiyuan', 'shouyin', 'busi', 'huanhun', 'chengxin', 'tanhua', 'shenmu', 'xinfu_qiai', 'fuli', 'xinfuli', 'buyi'];
										const dialog = ui.create.dialog('<b>选择一个技能获得', 'hidden');
										const skills = lise.randomGets(event.num);
										const yi = document.createElement('div');
										yi.classList.add('add-setting');
										yi.style.margin = '0';
										yi.style.width = '100%';
										yi.style.position = 'relative';

										for (const i of skills) {
											const td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
											td.innerHTML = '<font size=4>【' + get.translation(i) + '】<br><P align=left>' + get.skillInfoTranslation(i);
											td.link = i;
											td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
											Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
											yi.appendChild(td);
											dialog.buttons.add(td);
										}

										dialog.content.appendChild(yi);
										const chooseButton = player.chooseButton(dialog, true);
										chooseButton.set('ai', function (button) {
											return Math.random();
										});
									}
								}
								('step 2');
								if (result.links?.length) {
									const link = result.links[0];
									player.addTempSkill(link, { player: 'dyingAfter' });
									player.popup(link);
								}
							},
						},
						lqtq_sanchen: {
							enable: 'phaseUse',
							audio: 'spsanchen',
							usable: 1,
							filterTarget: true,
							content() {
								'step 0';
								if (!event.count) {
									event.count = [];
								}
								target.draw();
								target.chooseToDiscard(true, 'he').set('ai', function (card) {
									if (event.count.length) {
										for (const i of event.count) {
											if (get.type2(card) != i) {
												return 1000 - get.value(card);
											}
										}
									}
									return 7 - get.value(card);
								});
								('step 1');
								if (result.cards?.length) {
									const type = get.type2(result.cards[0]);
									if (!event.count.includes(type)) {
										event.count.push(type);
										if (event.count.length < 3) {
											event.goto(0);
										}
									}
								}
								('step 2');
								target.draw(event.count.length);
							},
							ai: {
								basic: {
									order: 10,
								},
								result: {
									target: 3,
								},
							},
						},
						lqtq_miewu: {
							audio: 'spmiewu',
							enable: ['chooseToUse', 'chooseToRespond'],
							filter(event, player) {
								if (player.getStat('skill').lqtq_miewu_backup > 0) {
									return false;
								}
								for (const i of lib.inpile) {
									if ((get.type(i) == 'basic' || get.type(i) == 'trick') && event.filterCard && event.filterCard({ name: i }, player, event)) {
										return true;
									}
								}
								return false;
							},
							hiddenCard(player, name) {
								if (player.getStat('skill').lqtq_miewu_backup > 0) {
									return false;
								}
								return get.type(name) == 'basic' || get.type(name) == 'trick';
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (name == 'sha') {
											if (event.filterCard && event.filterCard({ name: name }, player, event)) {
												list.push(['基本', '', 'sha']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'fire' }, player, event)) {
												list.push(['基本', '', 'sha', 'fire']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'thunder' }, player, event)) {
												list.push(['基本', '', 'sha', 'thunder']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'ice' }, player, event)) {
												list.push(['基本', '', 'sha', 'ice']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'kami' }, player, event)) {
												list.push(['基本', '', 'sha', 'kami']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'stab' }, player, event)) {
												list.push(['基本', '', 'sha', 'stab']);
											}
										} else if (get.type(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										} else if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['基本', '', name]);
										}
									}

									return ui.create.dialog('灭吴', [list, 'vcard']);
								},
								filter(button, player) {
									return _status.event.parent.filterCard({ name: button.link[2] }, player, _status.event.parent);
								},
								check(button) {
									if (_status.event.parent.type != 'phase') {
										return 1;
									}
									const player = _status.event.player;
									return player.getUseValue({
										name: button.link[2],
										nature: button.link[3],
									});
								},
								backup(links, player) {
									return {
										filterCard() {
											return false;
										},
										viewAs: { name: links[0][2], nature: links[0][3] },
										selectCard: -1,
										popname: true,
										precontent() {
											'step 0';
											player.chooseTarget('###是否发动【三陈】？###出牌阶段限一次,你可以令一名角色摸一张牌,其弃置一张牌,若其本回合以此法弃置的牌类型均不相同,则其重复此流程(最多3次),否则其摸X张牌.(X为目标弃置的牌的类型数)', true).ai = function (target) {
												return get.attitude(player, target) > 0;
											};
											('step 1');
											if (result.targets?.length) {
												event.target = result.targets[0];
												event.count = [];
											}
											('step 2');
											event.target.draw();
											event.target.chooseToDiscard(true, 'he').set('ai', function (card) {
												if (event.count.length) {
													for (const i of event.count) {
														if (get.type2(card) != i) {
															return 1000 - get.value(card);
														}
													}
												}
												return 7 - get.value(card);
											});
											('step 3');
											if (result.cards?.length) {
												const type = get.type2(result.cards[0]);
												if (!event.count.includes(type)) {
													event.count.push(type);
													if (event.count.length < 3) {
														event.goto(2);
													}
												}
											}
											('step 4');
											event.target.draw(event.count.length);
										},
									};
								},
								prompt(links, player) {
									return '视为使用一张' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]);
								},
							},
							ai: {
								fireAttack: true,
								respondSha: true,
								respondShan: true,
								skillTagFilter(player) {
									if (player.getStat('skill').lqtq_miewu_backup > 0) {
										return false;
									}
									return true;
								},
								order: 9,
								result: {
									player(player) {
										if (_status.event.dying) {
											return get.attitude(player, _status.event.dying);
										}
										return 1;
									},
								},
							},
						},
						//锁定技:①此技能不会被失去或失效.②游戏开始时,你随机记录两张未加入游戏的武将牌和两张牌堆中有的非装备牌
						//.③一名角色死亡后,你记录该角色的所有你未记录的非装备牌和该角色的武将牌.
						//④一名角色使用你未记录的非装备牌后,你记录那张牌.
						//⑤每回合限一次,其他角色发动技能后,你记录一张随机武将牌
						lqtq_yuanjiao: {
							init(player) {
								if (!player.storage.lqtq_yuanjiao1) {
									player.storage.lqtq_yuanjiao1 = [];
								}
								if (!player.storage.lqtq_yuanjiao2) {
									player.storage.lqtq_yuanjiao2 = [];
								}
								if (!player.storage.lqtq_yuanjiao3) {
									player.storage.lqtq_yuanjiao3 = [];
								}
							},
							intro: {
								name: '援交',
								mark(dialog, content, player) {
									if (player.storage.lqtq_yuanjiao3.length) {
										dialog.addText('置顶的武将牌');
										dialog.addSmall([player.storage.lqtq_yuanjiao3, 'character']);
									}
									dialog.addText('记录的武将牌');
									dialog.addSmall([player.storage.lqtq_yuanjiao1, 'character']);
									dialog.addText('记录的牌');
									dialog.addSmall([player.storage.lqtq_yuanjiao2, 'vcard']);
								},
							},
							audio: 'ext:浪琴天阙/audio:4',
							fixed: true,
							charlotte: true,
							forced: true,
							trigger: {
								global: ['phaseBefore', 'die', 'useCardAfter'],
								player: 'enterGame',
							},
							forced: true,
							_priority: 200,
							filter(event, player, name) {
								const a = player.storage.lqtq_yuanjiao1,
									b = player.storage.lqtq_yuanjiao2;
								if (name == 'die' && event.player != player) {
									const c = event.player,
										d = c.name1 || c.name,
										e = c.name2 || c.name;
									for (const i of event.player.getCards('h')) {
										if (!b.includes(i.name)) {
											return true;
										}
									}
									for (const j of [d, e]) {
										if (!a.includes(j)) {
											return true;
										}
									}
								}
								if (name == 'useCardAfter') {
									return b && !b.includes(event.card.name) && get.type(event.card) != 'equip';
								}
								if (name == 'enterGame') {
									return true;
								}
								return game.phaseNumber == 0;
							},
							content() {
								'step 0';
								let list = [],
									list2 = [];
								if (trigger.name == 'die') {
									const a = trigger.player,
										b = a.name1 || a.name,
										c = a.name2 || a.name;
									player.line(a, 'fire');
									if (a.countCards('h')) {
										for (const i of a.getCards('h')) {
											if (!player.storage.lqtq_yuanjiao2.includes(i.name) && get.type(i) != 'equip') {
												list2.add(i.name);
											}
										}
									}
									for (const j of [b, c]) {
										if (!player.storage.lqtq_yuanjiao1.includes(j)) {
											list.add(j);
										}
									}
								} else if (trigger.name == 'useCard') {
									list2.add(trigger.card.name);
								} else {
									for (const i in lib.character) {
										const d = lib.character[i][4];
										if (player.storage.lqtq_yuanjiao1.includes(i)) {
											continue;
										}
										for (const j of game.players.concat(game.dead)) {
											if ([j.name, j.name1, j.name2].includes(i)) {
												continue;
											}
										}
										//if(lib.config.forbidai.includes(i)) continue;
										if ((game.me == player && lib.config.extension_浪琴天阙_lqtq_shaiji_kuochong) || !lib.config.forbidai.includes(i)) {
											list.push(i);
										}
									}
									list = list.randomGets(2);
									get.cardPile(function (card) {
										if (get.type2(card) != 'equip') {
											list2.add(card.name);
										}
									});
									list2 = list2.randomGets(2);
								}
								event.dialog = ui.create.dialog('<div class="text center">' + get.translation(player) + '记录了');
								if (list.length) {
									event.dialog.add([list, 'character']);
									for (const i of list) {
										player.storage.lqtq_yuanjiao1.push(i);
									}
								}
								if (list2.length) {
									event.dialog.add([list2, 'vcard']);
									for (const i of list2) {
										player.storage.lqtq_yuanjiao2.push(i);
									}
								}
								player.markSkill('lqtq_yuanjiao');
								('step 1');
								event.dialog.close();
							},
							group: 'lqtq_yuanjiao_skill',
							subSkill: {
								skill: {
									audio: 'lqtq_yuanjiao',
									trigger: {
										global: 'logSkillBegin',
									},
									usable: 1,
									silent: true,
									_priority: 200,
									filter(event, player) {
										if (event.getParent('lqtq_yuanjiao_skill').name == 'lqtq_yuanjiao_skill') {
											return false;
										}
										if (event.player == player) {
											return false;
										}
										const skills = event.player.getSkills(true, false);
										game.expandSkills(skills);
										return skills.includes(event.skill); //QQQ
									},
									content() {
										'step 0';
										let list = [];
										for (const i in lib.character) {
											const d = lib.character[i][4];
											if (player.storage.lqtq_yuanjiao1.includes(i)) {
												continue;
											}
											for (const j of game.players.concat(game.dead)) {
												if ([j.name, j.name1, j.name2].includes(i)) {
													continue;
												}
											}
											if ((game.me == player && lib.config.extension_浪琴天阙_lqtq_shaiji_kuochong) || !lib.config.forbidai.includes(i)) {
												list.push(i);
											}
										}
										list = list.randomGets(1);
										event.dialog = ui.create.dialog('<div class="text center">' + get.translation(player) + '记录了');
										if (list.length) {
											event.dialog.add([list, 'character']);
											for (const i of list) {
												player.storage.lqtq_yuanjiao1.push(i);
											}
										}
										player.markSkill('lqtq_yuanjiao');
										('step 1');
										event.dialog.close();
									},
								},
							},
						},
						lqtq_weiniang: {
							audio: 'ext:浪琴天阙/audio:2',
							fixed: true,
							charlotte: true,
							init(player) {
								if (!player.storage.lqtq_weiniang1) {
									player.storage.lqtq_weiniang1 = [];
								}
							},
							enable: ['chooseToUse', 'chooseToRespond'],
							filter(event, player) {
								if (!player.storage.lqtq_yuanjiao2) {
									return false;
								}
								for (const i of player.storage.lqtq_yuanjiao2) {
									if (
										event.filterCard &&
										event.filterCard({ name: i }, player, event) &&
										player.countCards('hes', function (card) {
											return !player.storage.lqtq_weiniang1.includes(get.type2(card));
										})
									) {
										return true;
									}
								}
								return false;
							},
							hiddenCard(player, name) {
								return (
									player.storage.lqtq_yuanjiao2 &&
									player.storage.lqtq_yuanjiao2.includes(name) &&
									player.countCards('hes', function (card) {
										return !player.storage.lqtq_weiniang1.includes(get.type2(card));
									})
								);
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];
									const list1 = player.storage.lqtq_yuanjiao2;

									for (const j of list1) {
										const name = j;
										if (name == 'sha') {
											if (event.filterCard && event.filterCard({ name: name }, player, event)) {
												list.push(['基本', '', 'sha']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'fire' }, player, event)) {
												list.push(['基本', '', 'sha', 'fire']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'thunder' }, player, event)) {
												list.push(['基本', '', 'sha', 'thunder']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'ice' }, player, event)) {
												list.push(['基本', '', 'sha', 'ice']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'kami' }, player, event)) {
												list.push(['基本', '', 'sha', 'kami']);
											}
											if (event.filterCard && event.filterCard({ name: name, nature: 'stab' }, player, event)) {
												list.push(['基本', '', 'sha', 'stab']);
											}
										} else if (get.type2(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										} else if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['基本', '', name]);
										}
									}

									return ui.create.dialog('伪娘', [list, 'vcard']);
								},
								filter(button, player) {
									return _status.event.parent.filterCard({ name: button.link[2] }, player, _status.event.parent);
								},
								check(button) {
									if (_status.event.parent.type != 'phase') {
										return 1;
									}
									const player = _status.event.player;
									return player.getUseValue({
										name: button.link[2],
										nature: button.link[3],
									});
								},
								backup(links, player) {
									return {
										popname: true,
										selectCard: 1,
										filterCard(card, player) {
											if (player.storage.lqtq_weiniang1.includes(get.type2(card))) {
												return false;
											}
											return true;
										},
										check(card) {
											return 10 - get.value(card);
										},
										position: 'hes',
										viewAs: { name: links[0][2], nature: links[0][3] },
										precontent() {
											player.addTempSkill('lqtq_weiniang_count');
										},
									};
								},
								prompt(links, player) {
									return '将一张牌当做' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]) + '使用';
								},
							},
							ai: {
								fireAttack: true,
								respondSha: true,
								respondShan: true,
								skillTagFilter(player) {
									if (player.countCards('hes') < 1) {
										return false;
									}
								},
								order: 1,
								result: {
									player(player) {
										if (_status.event.dying) {
											return get.attitude(player, _status.event.dying);
										}
										return 1;
									},
								},
							},
							group: 'lqtq_weiniang_skill',
							subSkill: {
								count: {
									onremove(player) {
										player.storage.lqtq_weiniang1 = [];
									},
									trigger: { player: ['useCard', 'respond', 'useCardAfter', 'respondAfter'] },
									forced: true,
									popup: false,
									charlotte: true,
									filter(event, player) {
										return event.skill == 'lqtq_weiniang_backup';
									},
									content() {
										if (event.triggername == 'useCardAfter' || event.triggername == 'respondAfter') {
											player.draw();
										} else if (trigger.card && trigger.addCount !== false && trigger.cards) {
											player.storage.lqtq_weiniang1.push(get.type(trigger.cards[0], 'trick'));
										}
									},
								},
								skill: {
									audio: 'lqtq_weiniang',
									trigger: {
										global: 'phaseBefore',
										player: ['phaseBegin', 'phaseUseEnd', 'damageBegin', 'enterGame'],
									},
									forced: true,
									filter(event, player, name) {
										if (name == 'phaseBefore' && game.phaseNumber > 0) {
											return false;
										}
										return player.storage.lqtq_yuanjiao1 && player.storage.lqtq_yuanjiao1.length;
									},
									content() {
										'step 0';
										const list = [];

										for (const i of player.storage.lqtq_yuanjiao1) {
											if (player.storage.lqtq_yuanjiao3.includes(i)) {
												continue;
											}
											list.push(i);
										}

										if (list.length) {
											let shiji = '游戏开始,';
											if (event.triggername == 'phaseBegin') {
												shiji = '回合开始,';
											}
											if (event.triggername == 'phaseUseEnd') {
												shiji = '出牌阶段结束,';
											}
											if (event.triggername == 'damageBegin') {
												shiji = '即将受到伤害,';
											}
											player
												.chooseButton()
												.set('ai', function (button) {
													return get.rank(button.link, true) - lib.character[button.link][2];
												})
												.set('createDialog', ['###是否发动【伪娘】？###' + get.translation(shiji) + '你可以选择一张武将牌,视为拥有该武将牌的所有技能', [list, 'character']]);
										}
										('step 1');
										if (result.links?.length) {
											player.storage.lqtq_yuanjiao3.push(result.links[0]);
											for (const i of lib.character[result.links[0]][3]) {
												if (!player.hasSkill(i)) {
													player.addSkill(i);
												}
											}
										} else {
											event.finish();
										}
										if (player.storage.lqtq_yuanjiao3.length > 3) {
											player
												.chooseButton(true)
												.set('ai', function (button) {
													return get.rank(button.link, true) - lib.character[button.link][2];
												})
												.set('createDialog', ['置顶的武将牌超过三张,你需选择一张置顶的武将牌取消置顶', [player.storage.lqtq_yuanjiao3, 'character']]);
										} else {
											event.finish();
										}
										('step 2');
										if (result.links?.length) {
											player.storage.lqtq_yuanjiao3.remove(result.links[0]);
											for (const i of lib.character[result.links[0]][3]) {
												if (player.hasSkill(i)) {
													player.removeSkill(i);
												}
											}
										}
									},
								},
							},
						},
						lqtq_baihe: {
							trigger: {
								player: ['phaseUseBegin', 'phaseDiscardBegin'],
							},
							_priority: 3,
							forced: true,
							audio: 'ext:浪琴天阙/audio:2',
							content() {
								trigger.cancel();
							},
							group: ['lqtq_baihe_1', 'lqtq_baihe_2'],
							subSkill: {
								1: {
									trigger: {
										global: 'gainEnd',
									},
									audio: 'lqtq_baihe',
									forced: true,
									filter(event, player) {
										return player.countCards('h') < 20 && event.player != player;
									},
									content() {
										player.draw();
									},
								},
								2: {
									trigger: {
										global: 'useCardAfter',
									},
									audio: 'lqtq_baihe',
									filter(event, player) {
										if (!event.card || event.player == player) {
											return false;
										}
										if (get.type(event.card) == 'equip') {
											return player.countCards('h', { type: 'equip' });
										}
										if (get.type(event.card) != 'equip') {
											return (
												player.countCards('h', event.card.name) &&
												game.hasPlayer(function (target) {
													return player.canUse(event.card.name, target);
												})
											);
										}
										return false;
									},
									forced: true,
									content() {
										if (get.type(trigger.card) == 'equip') {
											player.chooseToUse(function (card) {
												if (!lib.filter.cardEnabled(card, _status.event.player, _status.event)) {
													return false;
												}
												const type = get.type(card, 'trick');
												return type == 'equip';
											}, '###是否发动【百合】？###你可以使用一张装备牌');
										} else {
											const card = trigger.card;
											player.chooseToUse({ name: card.name }, '###是否发动【百合】？###你可以使用一张' + get.translation(card.name) + '?');
										}
									},
								},
							},
						},
						lqtq_shuangxiu: {
							trigger: {
								global: ['damageEnd', 'loseHpEnd', 'recoverEnd', 'turnOverEnd', 'linkEnd', 'disableEquipEnd', 'disableJudgeEnd', 'gainEnd', 'discardEnd', 'loseMaxHpEnd', 'gainMaxHpEnd'],
							},
							forced: true,
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player) {
								const a = event.name;
								if (
									a == 'recover' &&
									!game.hasPlayer(function (current) {
										return current.isDamaged();
									})
								) {
									return false;
								}
								return !player.storage.lqtq_shuangxiu || !player.storage.lqtq_shuangxiu.includes(event.name);
							},
							content() {
								'step 0';
								var a = '###是否发动【双修】？###' + get.translation(trigger.player),
									b = trigger.name;
								if (b == 'damage') {
									a += '受到了伤害,你可以对一名角色造成一点伤害.';
								}
								if (b == 'loseHp') {
									a += '失去了体力,你可以令一名角色失去一点体力.';
								}
								if (b == 'recover') {
									a += '回复了体力,你可以令一名角色回复一点体力.';
								}
								if (b == 'turnOver') {
									a += '被翻面,你可以令一名角色翻面.';
								}
								if (b == 'link') {
									a += '被横置,你可以令一名角色横置或解除横置.';
								}
								if (b == 'disableEquip') {
									a += '废除了装备区,你可以令一名角色随机废除一个装备区.';
								}
								if (b == 'disableJudge') {
									a += '废除了判定区,你可以令一名角色废除判定区.';
								}
								if (b == 'gain') {
									a += '获得了牌,你可以令一名角色摸一张牌.';
								}
								if (b == 'discard') {
									a += '弃置了牌,你可以弃置一名角色一张牌.';
								}
								if (b == 'loseMaxHp') {
									a += '减少了体力上限,你可以令一名角色减少一点体力上限.';
								}
								if (b == 'gainMaxHp') {
									a += '增加了体力上限,你可以令一名角色增加一点体力上限.';
								}
								player
									.chooseTarget(a, function (card, player, target) {
										if (b == 'recover') {
											return target.isDamaged();
										}
										if (b == 'disableEquip') {
											return target.countDisabled() < 5;
										}
										if (b == 'discard') {
											return target.countCards('he');
										}
										return target.isAlive();
									})
									.set('ai', function (target) {
										const c = get.attitude(_status.event.player, target);
										if (b == 'turnOver') {
											if (c > 0 && target.isTurnedOver()) {
												return 10;
											}
											if (c < 0 && !target.isTurnedOver()) {
												return 9;
											}
											return 0;
										}
										if (b == 'link') {
											if (c > 0 && target.isLinked()) {
												return 10;
											}
											if (c < 0 && !target.isLinked()) {
												return 9;
											}
											return 0;
										}
										if (['recover', 'disableJudge', 'gain', 'gainMaxHp'].includes(b)) {
											return c;
										}
										return -c;
									});
								('step 1');
								if (result.bool) {
									var a = trigger.name,
										b = result.targets[0];
									if (a == 'damage') {
										b.damage();
									}
									if (a == 'loseHp') {
										b.loseHp();
									}
									if (a == 'recover') {
										b.recover();
									}
									if (a == 'turnOver') {
										b.turnOver();
									}
									if (a == 'link') {
										b.link();
									}
									if (a == 'disableEquip') {
										const list = [];
										for (let i = 1; i <= 5; i++) {
											if (!b.isDisabled(i)) {
												list.add(i);
											}
										}
										b.disableEquip(list.randomGet());
									}
									if (a == 'disableJudge') {
										b.disableJudge();
									}
									if (a == 'gain') {
										b.draw();
									}
									if (a == 'discard') {
										player.discardPlayerCard(b, 'he', true);
									}
									if (a == 'loseMaxHp') {
										b.loseMaxHp();
									}
									if (a == 'gainMaxHp') {
										b.gainMaxHp();
									}
									if (!player.storage.lqtq_shuangxiu) {
										player.storage.lqtq_shuangxiu = [];
									}
									player.storage.lqtq_shuangxiu.push(a);
									player.addTempSkill('lqtq_shuangxiu_round', 'roundStart');
									if (player.countCards('h')) {
										player.chooseToDiscard('你可以弃置一张手牌.', 'h').set('ai', function (card) {
											if (_status.event.player.countCards('h') > 17) {
												return -get.value(card);
											}
											return false;
										});
									}
								}
							},
							subSkill: {
								round: {
									mark: true,
									intro: {
										name: '一轮后重置',
										mark(dialog, content, player) {
											const a = player.storage.lqtq_shuangxiu;
											if (a.includes('damage')) {
												dialog.addText('伤害已发动');
											}
											if (a.includes('turnOver')) {
												dialog.addText('翻面已发动');
											}
											if (a.includes('link')) {
												dialog.addText('横置已发动');
											}
											if (a.includes('gain')) {
												dialog.addText('获得牌已发动');
											}
											if (a.includes('discard')) {
												dialog.addText('弃置牌已发动');
											}
											if (a.includes('loseHp')) {
												dialog.addText('失去体力已发动');
											}
											if (a.includes('recover')) {
												dialog.addText('回复体力已发动');
											}
											if (a.includes('disableEquip')) {
												dialog.addText('废除装备栏已发动');
											}
											if (a.includes('disableJudge')) {
												dialog.addText('废除判定区已发动');
											}
											if (a.includes('loseMaxHp')) {
												dialog.addText('减少体力上限已发动');
											}
											if (a.includes('gainMaxHp')) {
												dialog.addText('增加体力上限已发动');
											}
										},
									},
									forced: true,
									charlotte: true,
									onremove(player) {
										delete player.storage.lqtq_shuangxiu;
									},
								},
							},
						},
						lqtq_lingrang: {
							trigger: {
								player: 'equipEnd',
							},
							forced: true,
							audio: 'ext:浪琴天阙/audio:2',
							onremove(player) {
								delete player.storage.lqtq_lingrang;
							},
							filter(event, player) {
								return !player.storage.lqtq_lingrang || !player.storage.lqtq_lingrang.includes(event.card.name);
							},
							content() {
								'step 0';
								if (!player.storage.lqtq_lingrang) {
									player.storage.lqtq_lingrang = [];
								}
								event.num = get.translation(trigger.card.name).length;
								const dialog = ui.create.dialog('<font size=6><b>灵禳</b></font>', 'hidden');
								dialog.add('选择一个性别');
								var sex = ['male', 'female'];
								const yi = document.createElement('div');
								yi.classList.add('add-setting');
								yi.style.margin = '0';
								yi.style.width = '100%';
								yi.style.position = 'relative';

								for (const i of sex) {
									var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
									td.innerHTML = get.translation(i);
									td.link = i;
									td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
									Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
									yi.appendChild(td);
									dialog.buttons.add(td);
								}

								dialog.content.appendChild(yi);
								dialog.add('选择一个势力');
								var group = ['wei', 'wu', 'shu', 'qun'];
								const er = document.createElement('div');
								er.classList.add('add-setting');
								er.style.margin = '0';
								er.style.width = '100%';
								er.style.position = 'relative';

								for (const i of group) {
									var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
									td.innerHTML = get.translation(i);
									td.link = i;
									td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
									Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
									er.appendChild(td);
									dialog.buttons.add(td);
								}

								dialog.content.appendChild(er);
								dialog.add('选择一个体力');
								var hp = [3, 4, '其他'];
								const san = document.createElement('div');
								san.classList.add('add-setting');
								san.style.margin = '0';
								san.style.width = '100%';
								san.style.position = 'relative';

								for (const i of hp) {
									var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
									td.innerHTML = get.translation(i);
									td.link = i;
									td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
									Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
									san.appendChild(td);
									dialog.buttons.add(td);
								}

								dialog.content.appendChild(san);
								const chooseButton = player.chooseButton(dialog, 3, true);
								chooseButton
									.set('ai', function (button) {
										return 1 + Math.random();
									})
									.set('filterButton', function (button) {
										for (const i of ui.selected.buttons) {
											if ((sex.includes(i.link) && sex.includes(button.link)) || (group.includes(i.link) && group.includes(button.link)) || (hp.includes(i.link) && hp.includes(button.link))) {
												return false;
											}
										}

										return true;
									});
								('step 1');
								if (result.links?.length) {
									for (const i of result.links) {
										if (i == 'male' || i == 'female') {
											var sex = i;
										} else if (i == '3' || i == '4' || i == '其他') {
											var hp = i;
										} else {
											var group = i;
										}
									}
									game.log(player, '选择了', '#g【', '#g' + sex, '#g】,【', '#g' + group, '#g】,【', '#g' + hp, '#g】');
									const skills = [];
									let chaii = [];
									for (const i in lib.character) {
										if (hp == '其他' && [3, 4].includes(lib.character[i][2])) {
											continue;
										}
										if (lib.character[i][0] != sex) {
											continue;
										}
										if (lib.character[i][1] != group) {
											continue;
										}
										if (hp != '其他' && lib.character[i][2] != hp) {
											continue;
										}
										const d = lib.character[i][4];
										if (player.storage.lqtq_lingrang.includes(i)) {
											continue;
										}
										if ((game.me == player && lib.config.extension_浪琴天阙_lqtq_shaiji_kuochong) || !lib.config.forbidai.includes(i)) {
											chaii.push(i);
										}
									}
									if (chaii.length) {
										event.count = chaii.length;
										if (chaii.length > event.num) {
											chaii = chaii.randomGets(event.num);
										}
										player
											.chooseButton(true)
											.set('ai', function (button) {
												return get.rank(button.link, true) - lib.character[button.link][2];
											})
											.set('createDialog', ['选择一张武将牌,并获得该武将牌的所有技能', [chaii, 'character']]);
									} else {
										player.say('没有符合的武将牌,请重新选择');
										event.goto(0);
									}
								}
								('step 2');
								const link = result.links[0];
								player.storage.lqtq_lingrang.push(link);
								for (const i of lib.character[link][3]) {
									player.addSkillLog(i);
								}
								if (event.count < event.num) {
									player.draw(event.num - event.count);
								}
								('step 3');
								player.storage.lqtq_lingrang.push(trigger.card.name);
							},
							ai: {
								effect: {
									target(card, player, target, current) {
										if (get.type(card) == 'equip' && !get.cardtag(card, 'gifts')) {
											return [1, 3];
										}
									},
								},
							},
						},
						lqtq_huayue: {
							trigger: {
								player: 'phaseDrawBegin',
							},
							dutySkill: true,
							derivation: 'lqtq_lingnian',
							audio: 'ext:浪琴天阙/audio:2',
							preHidden: true,
							filter(event, player) {
								return !event.numFixed;
							},
							forced: true,
							content() {
								const a = [],
									b = [];
								get.cardPile(function (card) {
									if (!a.includes(get.type2(card))) {
										b.add(card);
										a.add(get.type2(card));
									}
								});
								trigger.cancel();
								if (b.length) {
									player.gain(b, 'draw2');
								}
							},
							group: ['lqtq_huayue_achieve', 'lqtq_huayue_fail'],
							subSkill: {
								achieve: {
									trigger: { player: 'phaseZhunbeiBegin' },
									forced: true,
									filter(event, player) {
										let num = 0;
										for (let i = 1; i <= 6; i++) {
											if (player.getEquip(i)) {
												num += get.translation(player.getEquip(i).name).length;
											}
										}
										game.log(num);
										return num > 8; //player.isHealthy()//健康的
									},
									content() {
										game.log(player, '成功完成使命');
										player.awakenSkill('lqtq_huayue');
										player.addSkillLog('lqtq_lingnian');
									},
								},
								fail: {
									trigger: { player: 'dying' },
									forced: true,
									content() {
										game.log(player, '使命失败');
										player.awakenSkill('lqtq_huayue');
										player.recover();
										player.moveCard();
									},
								},
							},
						},
						lqtq_lingnian: {
							enable: 'phaseUse',
							usable: 1,
							position: 'he',
							viewAsFilter(player) {
								return (
									player.countCards('he', { suit: 'diamond' }) > 0 &&
									game.hasPlayer(function (current) {
										return current != player && current.getEquip(1);
									})
								);
							},
							viewAs: {
								name: 'jiedao',
							},
							filterCard: {
								suit: 'diamond',
							},
							check(card) {
								return 8 - get.value(card);
							},
						},
						_lqtq_baibanbiao1: {
							//开摆
							trigger: {
								player: ['dyingBegin', 'dieBegin'],
							},
							forced: true,
							_priority: Infinity,
							filter(event, player) {
								return !game.mihu && player.name == 'lqtq_baibanbiao';
							},
							content() {
								'step 0';
								game.changeMiHu(player);
								if (
									!game.hasPlayer(function (current) {
										return player.getFriends().includes(current);
									})
								) {
									trigger.cancel();
									let a = 4;
									if (player.identity == 'zhu') {
										a = 5;
									}
									player.maxHp = a;
									player.hp = a;
									player.draw(4, false);
									player.update();
									game.addGlobalSkill('lqtq_baibanbiao4');
									event.finish();
								} else if (player.identity == 'zhu' || player.countCards('hej')) {
									player
										.chooseTarget(true, '<font color=#ff00ff>寄!</font>', '<font color=#ffff00>反正你也要希特了,不如将你的全部家产交给别人.</font>', function (card, player, target) {
											return target != player;
										})
										.set('ai', function (target) {
											return get.attitude(_status.event.player, target);
										});
								}
								('step 1');
								if (result.targets?.length) {
									player.line(result.targets[0], 'green');
									if (player.identity == 'zhu') {
										game.zhu = result.targets[0];
										result.targets[0].identity = 'zhu';
										result.targets[0].showIdentity();
										result.targets[0].maxHp = result.targets[0].maxHp + 1;
										result.targets[0].hp = result.targets[0].hp + 1;
										result.targets[0].update();
										player.maxHp = player.maxHp - 1;
										player.update();
									}
									if (player.countCards('hej')) {
										result.targets[0].gain(player.getCards('hej'), 'give', player);
									}
								}
								player.die();
								game.addGlobalSkill('lqtq_baibanbiao2');
								player.chooseBool('<font color=#5555ff>本人已死,有事烧香<br>小事招魂,大事挖坟<br>如有撞见,纯属诈尸<br>(≧▽≦)</font>');
							},
						},
						lqtq_baibanbiao2: {
							//纵观全局
							trigger: {
								global: 'phaseUseBegin',
							},
							forced: true,
							forceDie: true,
							filter(event, player) {
								return event.player.countCards('h') && player.isDead() && game.mihu && game.mihu == player;
							},
							content() {
								'step 0';
								player.line(trigger.player, 'green');
								player.chooseCardButton('<font color=#ff00ff>诈尸中......<br>纵观全局的上帝</font><br><font color=#eeee00>你可以选择</font><font color=#ff0000>' + get.translation(trigger.player) + '</font><font color=#eeee00>的一张手牌展示,并选择一名目标角色,若</font><font color=#ff0000>' + get.translation(trigger.player) + '</font><font color=#eeee00>可以对目标使用你选择的牌,则其必须对目标使用那张牌,否则目标获得那张牌.</font>', trigger.player.getCards('h'), true).ai = function (card) {
									if (get.attitude(player, trigger.player) > 0) {
										return -get.value(card);
									}
									if (
										game.hasPlayer(function (target) {
											return trigger.player.canUse(card, target) && get.attitude(trigger.player, target) > 0;
										})
									) {
										return 20;
									}
									return get.value(card);
								};
								('step 1');
								if (result.links?.length) {
									event.card = result.links[0];
									trigger.player.showCards(event.card);
									player.chooseTarget(true, '<font color=#eeee00>选择一名目标角色</font>').ai = function (target) {
										const a = get.effect(target, event.card, trigger.player, player),
											b = get.attitude(trigger.player, player),
											c = trigger.player.canUse(event.card, target);
										if (c) {
											return a;
										}
										if (!c) {
											return get.attitude(target, player);
										}
										return -get.attitude(target, player);
									};
								} else {
									event.finish();
								}
								('step 2');
								if (result.targets?.length) {
									player.line(result.targets[0], 'green');
									if (trigger.player.canUse(event.card, result.targets[0]) && event.card.name != 'shengdong') {
										trigger.player.useCard(event.card, result.targets[0]);
									} else {
										game.log(result.targets[0], '获得了', trigger.player, '的', event.card);
										result.targets[0].gain(event.card, true, 'draw2');
									}
								}
							},
						},
						_lqtq_baibanbiao3: {
							//负隅顽抗
							trigger: {
								global: 'dieBegin',
							},
							_priority: 22,
							forceDie: true,
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player) {
								return (
									game.mihu &&
									game.mihu == player &&
									player.isDead() &&
									player.getFriends().includes(event.player) &&
									(event.player == game.zhu ||
										!game.hasPlayer(function (current) {
											return event.player.getFriends().includes(current);
										}))
								);
							},
							forced: true,
							content() {
								'step 0';
								player
									.chooseControl('<font color=#ff44aa>负隅顽抗</font>', '<font color=#ff44aa>放弃抵抗</font>')
									.set('ai', function () {
										return '<font color=#ff44aa>负隅顽抗</font>';
									})
									.set('prompt', '<font color=#ff00ff>白板的倔强</font>')
									.set('prompt2', '<font color=#ffff00>游戏好像要结束了耶,要不要再努力一下下呢?</font>');
								('step 1');
								if (result.control == '<font color=#ff44aa>放弃抵抗</font>') {
									return;
								}
								player.revive(player.maxHp);
								let a = 4;
								if (player.identity == 'zhu') {
									a = 5;
									player.maxHp--;
									player.update();
								} //QQQ
								player.maxHp = a;
								player.hp = a;
								player.draw(4, false);
								player.update();
								game.removeGlobalSkill('lqtq_baibanbiao2');
								game.addGlobalSkill('lqtq_baibanbiao4');
							},
						},
						lqtq_baibanbiao4: {
							trigger: { global: 'useCard1' },
							filter(event, player) {
								if (event.targets.length < 1 || event.card.notarget || !game.mihu || game.mihu != player) {
									return false;
								}
								if (event.player != player) {
									return event.targets.includes(player);
								}
								return !event._lqtq_baibanbiao4;
							},
							forced: true,
							content() {
								'step 0';
								const list = [];

								for (const i of lib.inpile) {
									const info = lib.card[i];
									if (info.notarget || ['suijiyingbian', 'shengdong', 'jiedao'].includes(i)) {
										continue;
									}
									if (info.type != 'equip') {
										const cardinfo = [trigger.card.suit || '', trigger.card.number || '', i];
										for (const j of trigger.targets) {
											if (cardinfo[2] == 'wuzhong' || trigger.player.canUse(cardinfo[2], j, false, false)) {
												list.add(cardinfo);
											}
										}
									}
								}

								if (trigger.player != player) {
									player.chooseButton(['<font color=#eeee00>躺平了那么久,感觉眼前迷迷糊糊的,难道我产生幻觉了？</font><font color=#ff0000>' + get.translation(trigger.player) + '</font><font color=#eeee00>对</font><font color=#ff0000>' + get.translation(trigger.targets) + '</font><font color=#eeee00>使用的是什么来着？</font>', [list, 'vcard']]).ai = function (button) {
										const card = { suit: trigger.card.suit, number: trigger.card.number, name: button.link[2] };
										return get.effect(trigger.targets[0], card, player, player);
									};
								} else {
									const eff1 = get.effect(trigger.targets[0], trigger.card, trigger.player, player);
									const val1 = get.value(trigger.card, player, 'raw');
									player.chooseButton(['<font color=#eeee00>躺平了那么久,感觉眼前迷迷糊糊的,难道我产生幻觉了？我使用的是什么来着？</font>', [list, 'vcard']]).ai = function (button) {
										const card = { suit: trigger.card.suit, number: trigger.card.number, name: button.link[2] };
										const eff2 = get.effect(trigger.targets[0], card, trigger.player, player);
										const val2 = get.value(card, player, 'raw');
										if (eff1 > 0) {
											if (eff2 <= 0) {
												return 0;
											}
											return val2 - val1;
										} else if (eff1 < 0) {
											if (eff2 >= 0) {
												return val2;
											}
											return 0;
										} else if (eff1 == 0) {
											if (eff2 > 0) {
												return val2;
											}
											return 0;
										}
									};
								}
								('step 1');
								if (result.bool) {
									if (trigger.player == player) {
										trigger._lqtq_baibanbiao4 = true;
										var stat = player.stat[player.stat.length - 1].card;
										if (stat[trigger.card.name]) {
											stat[trigger.card.name]--;
										}
									}
									const card = game.createCard({
										suit: trigger.card.suit || lib.suit.randomGet(),
										number: trigger.card.number || Math.ceil(Math.random() * 13),
										name: result.links[0][2],
									});
									event.card = card;
									game.log(player, '将', trigger.card, '变为', card);
									trigger.card = card;
									trigger.cards = [card];
									trigger.card.cards = trigger.cards;
									game.cardsGotoOrdering(card).relatedEvent = trigger;
								} else {
									event.finish();
								}
								('step 2');
								player.$throw(event.card, null, null, true);
								if (player == trigger.player) {
									player.line(trigger.targets[0], 'green');
								} else {
									player.line(trigger.player, 'green');
								}
								('step 3');
								if (trigger.player == player) {
									var stat = player.stat[player.stat.length - 1].card;
									if (!stat[trigger.card.name]) {
										stat[trigger.card.name] = 1;
									} else {
										stat[trigger.card.name]++;
									}
								}
							},
						},
						lqtq_xihai: {
							audio: 'chuhai',
							enable: 'phaseUse',
							usable: 1,
							filterTarget(card, player, target) {
								return target != player;
							},
							content() {
								'step 0';
								if (!target.countCards('h')) {
									target.draw();
								}
								('step 1');
								if (Array.isArray(result) && result.length) {
									target.showCards(result[0]);
									event.num = result[0].number;
									event.type = get.type2(result[0], false);
									event.goto(4);
								}
								('step 2');
								player.discardPlayerCard(target, 'h', true);
								('step 3');
								if (result.cards?.length) {
									event.num = result.cards[0].number;
									event.type = get.type2(result.cards[0], false);
								}
								('step 4');
								if (event.num < 7) {
									target.damage();
								} else if (event.num > 12) {
									target.damage(3);
								} else {
									target.damage(2);
								}
								const cards = [],
									list = [];
								list.push(event.type);

								for (const i of ui.cardPile.childNodes) {
									const type = get.type2(i, false);
									if (!list.includes(type)) {
										list.push(type);
										cards.push(i);
									}
								}

								player.gain(cards, 'gain2');
							},
							ai: {
								order: 9,
								result: {
									target: -1,
								},
							},
						},
						//每回合限一次,一名其他角色发动技能时,你可以弃置一张牌取消之,直到回合结束,该角色每次发动技能,你摸一张牌
						lqtq_zhangquan: {
							trigger: {
								player: ['logSkillBegin'],
							},
							forced: true,
							popup: false,
							usable: 1,
							audio: 'xianghai',
							filter(event, player) {
								return event.player != player && player.countCards('he');
							},
							async content(event, trigger, player) {
								const name = trigger.skill;
								const info = lib.skill[name];
								const zi = '弃置一张牌令' + get.translation(trigger.player) + '技能【' + get.translation(name) + '】无效';
								const { cards } = await player
									.chooseToDiscard(zi, 'he')
									.set('ai', function (card) {
										if (get.attitude(player, trigger.player) > 0) {
											return false;
										}
										return 10 - get.value(card);
									})
									.forResult();
								if (cards?.length) {
									const arr = trigger.parent.next;
									for (let i = arr.length - 1; i >= 0; i--) {
										if (arr[i].name === name) {
											arr.splice(i, 1);
										}
									}
									game.log(player, `终止${get.translation(name)}的发动`);
									if (info.limited || info.juexingji) {
										trigger.player.awakenSkill(name);
									}
									player.storage.lqtq_zhangquan = trigger.player;
									player.addTempSkill('lqtq_zhangquan_1');
								} else {
									player.getStat('triggerSkill').lqtq_zhangquan--;
								}
							},
							subSkill: {
								1: {
									onremove(player) {
										delete player.storage.lqtq_zhangquan;
									},
									trigger: {
										global: 'logSkillBegin', //QQQ
									},
									audio: 'xianghai',
									logTarget: 'player',
									filter(event, player) {
										if (event.getParent('lqtq_zhangquan_1').name == 'lqtq_zhangquan_1') {
											return false;
										}
										const skills = event.player.getSkills(true, false);
										game.expandSkills(skills);
										return player.storage.lqtq_zhangquan && event.player == player.storage.lqtq_zhangquan && skills.includes(event.skill);
									}, //QQQ
									silent: true,
									content() {
										player.draw();
									},
								},
							},
						},
						lqtq_huiling: {
							audio: 'ext:浪琴天阙/audio:2',
							enable: 'phaseUse',
							usable: 2,
							filterCard: true,
							selectCard() {
								const player = _status.event.player;
								if (
									ui.selected.targets.length ||
									player.hasSkill('lqtq_huiling_init') ||
									!player.hasHistory('useCard', function (card) {
										return ['basic', 'trick'].includes(get.type(card.card)) && player.hasUseTarget(card.card);
									})
								) {
									return 0;
								}
								if (player.hasSkill('lqtq_huiling_add')) {
									return [1, 2];
								}
								return [0, 2];
							},
							selectTarget() {
								const player = _status.event.player;
								if (ui.selected.cards.length || player.hasSkill('lqtq_huiling_add') || !player.countCards('h')) {
									return [0, 0];
								}
								return [1, 1];
							},
							filterTarget(card, player, target) {
								return player.canCompare(target);
							},
							discard: false,
							lose: false,
							delay: 0,
							position: 'he',
							prompt(event) {
								const player = _status.event.player;
								if (
									player.hasSkill('lqtq_huiling_init') ||
									!player.hasHistory('useCard', function (card) {
										return ['basic', 'trick'].includes(get.type(card.card)) && player.hasUseTarget(card.card);
									})
								) {
									return '与一名角色拼点.';
								}
								if (
									player.hasSkill('lqtq_huiling_add') ||
									!game.hasPlayer(function (current) {
										return current != player && player.canCompare(current);
									})
								) {
									return '将一张牌置于武将牌上作为<灵>.';
								}
								return '与一名角色拼点或将一张牌置于武将牌上作为<灵>.';
							},
							check(card) {
								return 10 - get.value(card);
							},
							filter(event, player) {
								if (
									!player.hasSkill('lqtq_huiling_init') &&
									player.hasHistory('useCard', function (card) {
										return ['basic', 'trick'].includes(get.type(card.card)) && player.hasUseTarget(card.card);
									}) &&
									player.countCards('he')
								) {
									return true;
								}
								if (
									!player.hasSkill('lqtq_huiling_add') &&
									player.countCards('h') &&
									game.hasPlayer(function (current) {
										return current != player && player.canCompare(current);
									})
								) {
									return true;
								}
								return false;
							},
							content() {
								'step 0';
								if (cards.length) {
									player.addToExpansion(cards, player, 'giveAuto').gaintag.add('lqtq_huiling');
									var list = [];
									player.getHistory('useCard', function (evt) {
										if (!evt.isPhaseUsing() || !['basic', 'trick'].includes(get.type(evt.card))) {
											return;
										}
										if (evt.card.name == 'sha' && evt.card.nature) {
											list.add('sha:' + evt.card.nature);
										} else {
											list.add(evt.card.name);
										}
									});
									for (let i = 0; i < list.length; i++) {
										if (list[i].indexOf('sha:') == 0) {
											list[i] = ['基本', '', 'sha', list[i].slice(4)];
										} else {
											list[i] = [get.type(list[i]), '', list[i]];
										}
									}
									player
										.chooseButton(true, ['纵灵', [list, 'vcard']])
										.set('filterButton', function (button) {
											return player.hasUseTarget({ name: button.link[2], nature: button.link[3] });
										})
										.set('ai', function (button) {
											const player = _status.event.player,
												evt = player.getLastUsed(1);
											if (evt && get.type2(evt.card) == get.type2(button.link)) {
												return 1 + player.getUseValue({ name: button.link[2], nature: button.link[3] });
											}
											return player.getUseValue({ name: button.link[2], nature: button.link[3] });
										});
								} else {
									event.goto(2);
								}
								('step 1');
								if (result.links?.length) {
									player.chooseUseTarget(false, 'nodistance', true, { name: result.links[0][2], nature: result.links[0][3] });
									card = get.cardPile(function (card) {
										//弃牌堆或牌堆
										return get.type(card) == 'equip';
									});
									if (card) {
										player.gain(card, 'draw2');
									}
									player.addTempSkill('lqtq_huiling_init');
									event.finish();
								} else {
									event.finish();
								}
								('step 2');
								player.chooseToCompare(targets[0]);
								('step 3');
								if (get.position(result.target) == 'd') {
									player.addToExpansion(result.target, player, 'gain2').gaintag.add('lqtq_huiling');
								}
								if (result.bool) {
									var cards = [],
										list = [];
									for (const i of ui.cardPile.childNodes) {
										const type = get.type(i, false);
										if ((!list.length || list.includes(type)) && type != get.type(result.target)) {
											list.push(type);
											cards.push(i);
										}
									}
									player.gain(cards.randomGets(2), 'gain2');
								} else {
									player.draw();
								}
								player.addTempSkill('lqtq_huiling_add');
							},
							intro: {
								content: 'expansion',
								markcount: 'expansion',
							},
							onremove(player, skill) {
								const cards = player.getExpansions('lqtq_huiling');
								if (cards.length) {
									player.loseToDiscardpile(cards);
								}
							},
							subSkill: {
								init: {},
								add: {},
							},
							ai: {
								order: 12,
								result: {
									player: 3,
									target(player, target) {
										if (player != target) {
											return -1;
										}
										return 1;
									},
								},
							},
						},
						lqtq_chiwu: {
							mod: {
								aiOrder(player, card, num) {
									if (typeof card == 'object') {
										const evt = player.getLastUsed(),
											evtt = player.getLastUsed(2);
										if (evt && evt.card && get.type2(evt.card) != 'none' && get.type2(card) != 'none' && get.type2(evt.card) == get.type2(card)) {
											return num + 20;
										}
										if (evtt && evtt.card && get.type2(evtt.card) != 'none' && get.type2(card) != 'none' && get.type2(evtt.card) == get.type2(card)) {
											return num + 200;
										}
									}
								},
							},
							trigger: {
								player: 'useCardEnd',
							},
							audio: 'ext:浪琴天阙/audio:2',
							forced: true,
							filter(event, player) {
								return player.countMark('lqtq_chiwu') > 2;
							},
							group: 'lqtq_chiwu_count',
							intro: {
								content: 'mark',
							},
							content() {
								'step 0';
								player
									.chooseTarget('是否发动【炽舞】？', '选择你自己摸三张牌并交换<灵>,或选择一名其他角色弃置1～3张牌并判定.', function (card, player, target) {
										if (target != player) {
											return target.countDiscardableCards(target, 'he') > 0;
										}
										return true;
									})
									.set('ai', function (target) {
										return get.attitude(target, player);
									});
								('step 1');
								if (result.targets?.length) {
									event.target = result.targets[0];
									player.removeMark('lqtq_chiwu', player.countMark('lqtq_chiwu'));
									if (event.target == player) {
										player.draw(3);
									} else {
										event.target.chooseToDiscard([1, 3], true, '请弃置1~3张牌', 'he').set('ai', function (card) {
											return 6 - get.value(card);
										});
									}
								} else {
									event.finish();
								}
								('step 2');
								if (result.cards?.length) {
									if (result.cards.length < 3) {
										event.target.damage();
									}
									if (result.cards.length < 2) {
										player.gain(result.cards, 'gain2');
									}
								}
								('step 3');
								var cards = player.getExpansions('lqtq_huiling');
								if (event.target == player && cards.length && player.countCards('h')) {
									const next = player.chooseToMove('交换<灵>和手牌？');
									next.set('list', [
										[get.translation(player) + '(你)的<灵>', cards],
										['手牌区', player.getCards('h')],
									]);
									next.set('filterMove', function (from, to) {
										return typeof to != 'number';
									});
									next.set('processAI', function (list) {
										const player = _status.event.player,
											cards = list[0][1].concat(list[1][1]).sort(function (a, b) {
												return get.value(a) - get.value(b);
											}),
											cards2 = cards.splice(0, player.getExpansions('lqtq_huiling').length);
										return [cards2, cards];
									});
								} else {
									event.goto(5);
								}
								('step 4');
								if (result.bool) {
									const pushs = result.moved[0],
										gains = result.moved[1];
									pushs.removeArray(player.getExpansions('lqtq_huiling'));
									gains.removeArray(player.getCards('h'));
									if (!pushs.length || pushs.length != gains.length) {
										return;
									}
									player.addToExpansion(pushs).gaintag.add('lqtq_huiling');
									player.gain(gains, 'gain2', 'log');
								}
								('step 5');
								if (event.target != player) {
									player.judge(function (card) {
										return get.color(card) == 'red' ? 1 : 0;
									});
								} else {
									event.finish();
								}
								('step 6');
								event.count = result.number;
								if (result.color == 'red') {
									player.addTempSkill('lqtq_chiwu_hand');
									player.addMark('lqtq_chiwu_hand');
								}
								if (player.getExpansions('lqtq_huiling').length) {
									let num = 0;
									for (const i of player.getExpansions('lqtq_huiling')) {
										num += i.number;
									}
									if (event.count == num) {
										var cards = player.getExpansions('lqtq_huiling');
										event.num1 = cards.length;
										event.num = 0;
										player.loseToDiscardpile(cards);
									} else {
										event.finish();
									}
								} else {
									event.finish();
								}
								('step 7');
								event.num++;
								player.chooseTarget('对一名其他角色造成一点伤害(' + event.num + '/' + event.num1 + ')').set('ai', function (target) {
									return -get.attitude(_status.event.player, target);
								});
								('step 8');
								if (result.targets?.length) {
									result.targets[0].damage('fire');
									if (event.num < event.num1) {
										event.goto(7);
									}
								}
							},
							subSkill: {
								hand: {
									intro: {
										content: '本回合手牌上限+#.',
									},
									marktext: '上',
									onremove(player) {
										player.removeMark('lqtq_chiwu_hand', player.countMark('lqtq_chiwu_hand'));
									},
									mod: {
										maxHandcard(player, num) {
											return num + player.countMark('lqtq_chiwu_hand');
										},
									},
								},
								count: {
									trigger: {
										player: 'useCard',
									},
									forced: true,
									filter(event, player) {
										const evt = lib.skill.dcjianying.getLastUsed(player, event);
										return player.countMark('lqtq_chiwu') < 3 || (evt && evt.card && get.type2(evt.card) != get.type2(event.card));
									},
									content() {
										const evt = lib.skill.dcjianying.getLastUsed(player, trigger);
										if (!evt || !evt.card) {
											player.addMark('lqtq_chiwu');
										} else if (evt && evt.card) {
											if (get.type2(evt.card) != get.type2(trigger.card)) {
												player.removeMark('lqtq_chiwu', player.countMark('lqtq_chiwu'));
											}
											player.addMark('lqtq_chiwu');
										}
									},
								},
							},
						},
						lqtq_zongling: {
							group: 'lqtq_zongling_compare',
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								global: 'judge',
							},
							forced: true,
							filter(event, player) {
								return player.getExpansions('lqtq_huiling').length && event.player.isAlive();
							},
							content() {
								'step 0';
								const list = player.getExpansions('lqtq_huiling');
								player
									.chooseButton([get.translation(trigger.player) + '的' + (trigger.judgestr || '') + '判定为' + get.translation(trigger.player.judging[0]) + ',' + get.prompt('lqtq_zongling'), list, 'hidden'], function (button) {
										const card = button.link,
											trigger = _status.event.getTrigger(),
											player = _status.event.player,
											judging = _status.event.judging,
											result = trigger.judge(card) - trigger.judge(judging),
											attitude = get.attitude(player, trigger.player);
										return result * attitude;
									})
									.set('judging', trigger.player.judging[0])
									.set('filterButton', function (button) {
										const player = _status.event.player;
										const card = button.link;
										const mod2 = game.checkMod(card, player, 'unchanged', 'cardEnabled2', player);
										if (mod2 != 'unchanged') {
											return mod2;
										}
										const mod = game.checkMod(card, player, 'unchanged', 'cardRespondable', player);
										if (mod != 'unchanged') {
											return mod;
										}
										return true;
									});
								('step 1');
								if (result.bool) {
									event.forceDie = true;
									player.respond(result.links, 'lqtq_zongling', 'highlight', 'noOrdering');
									result.cards = result.links;
									event.card = result.cards[0];
								} else {
									event.finish();
								}
								('step 2');
								if (result.bool) {
									if (trigger.player.judging[0].clone) {
										trigger.player.judging[0].clone.classList.remove('thrownhighlight');
										game.broadcast(function (card) {
											if (card.clone) {
												card.clone.classList.remove('thrownhighlight');
											}
										}, trigger.player.judging[0]);
										game.addVideo('deletenode', player, get.cardsInfo([trigger.player.judging[0].clone]));
									}
									game.cardsDiscard(trigger.player.judging[0]);
									trigger.player.judging[0] = result.cards[0];
									trigger.orderingCards.addArray(result.cards);
									game.log(trigger.player, '的判定牌改为', card);
								}
							},
							ai: {
								rejudge: true,
								tag: {
									rejudge: 0.6,
								},
							},
							subSkill: {
								compare: {
									forced: true,
									trigger: {
										global: 'compare',
									},
									filter(event, player) {
										return player.getExpansions('lqtq_huiling').length;
									},
									content() {
										'step 0';
										const a = ui.create.dialog('<font size=5>是否发动【纵灵】？</font>', 'hidden');
										a.add('<font size=3>你可以打出一张<灵>替换一名角色的拼点牌</font>');
										const b = ['替换' + get.translation(trigger.player) + '的' + get.translation(trigger.card1), '替换' + get.translation(trigger.target) + '的' + get.translation(trigger.card2)];
										event.b = b;
										const yi = document.createElement('div');
										yi.classList.add('add-setting');
										yi.style.margin = '0';
										yi.style.width = '100%';
										yi.style.position = 'relative';

										for (const i of b) {
											const td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
											td.innerHTML = get.translation(i);
											td.link = i;
											td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
											Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
											yi.appendChild(td);
											a.buttons.add(td);
										}

										a.content.appendChild(yi);
										a.add(player.getExpansions('lqtq_huiling'));
										const c = player.chooseButton(a, 2);
										c.set('ai', function (button) {
											const aa = get.attitude(_status.event.player, trigger.player),
												bb = get.attitude(_status.event.player, trigger.target);
											if ((aa > 0 || bb < 0) && trigger.num1 > trigger.num2) {
												return false;
											}
											if ((bb > 0 || aa < 0) && trigger.num1 < trigger.num2) {
												return false;
											}
											for (const i of player.getExpansions('lqtq_huiling')) {
												if (aa > 0 && i.number > trigger.num2 && [b[0], i].includes(button.link)) {
													return 20;
												} else if (bb > 0 && i.number > trigger.num1 && [b[1], i].includes(button.link)) {
													return 20;
												}
											}
											for (const i of player.getExpansions('lqtq_huiling')) {
												if (aa < 0 && i.number < trigger.num2 && [b[0], i].includes(button.link)) {
													return 20;
												} else if (bb < 0 && i.number < trigger.num1 && [b[1], i].includes(button.link)) {
													return 20;
												}
											}
											return false;
										}).set('filterButton', function (button) {
											if (ui.selected.buttons.length) {
												return typeof ui.selected.buttons[0].link != typeof button.link;
											}
											return true;
										});
										('step 1');
										if (result.links?.length) {
											for (const i of result.links) {
												if (event.b.includes(i)) {
													var d = i;
												} else {
													var e = i;
												}
											}
											player.respond(e, 'highlight', 'lqtq_zongling', 'noOrdering');
											if (d == '替换' + get.translation(trigger.target) + '的' + get.translation(trigger.card2)) {
												player.line(trigger.target);
												trigger.num2 = e.number;
												trigger.card2 = e;
												game.log(trigger.target, '的拼点牌改为', e);
											} else {
												trigger.num1 = e.number;
												trigger.card1 = e;
												game.log(trigger.player, '的拼点牌改为', e);
											}
										}
									},
								},
							},
						},
						lqtq_anhong: {
							trigger: {
								player: 'damageEnd',
							},
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player) {
								return event.num > 0;
							},
							forced: true, //自动
							content() {
								'step 0';
								event.count = Math.min(trigger.num, 9);
								('step 1');
								event.count--;
								if (!trigger.source || !trigger.source.countCards('he')) {
									player.draw(3);
									event.goto(4);
								} else {
									player.discardPlayerCard('是否弃置' + get.translation(trigger.source) + '一张牌,否则' + get.translation(trigger.source) + '摸一张牌', trigger.source, 'he');
								}
								('step 2');
								if (result.cards?.length) {
									event.color = get.color(result.cards[0]);
								} else {
									trigger.source.draw();
									if (trigger.source.countDiscardableCards(trigger.source, 'he') > 0) {
										trigger.source.chooseToDiscard(true, '请选择一张牌弃置', 'he');
									}
								}
								('step 3');
								if (result.cards?.length) {
									event.color = get.color(result.cards[0]);
								}
								if (event.color) {
									const list = [];
									get.cardPile(function (card) {
										if (card.name != 'du' && get.color(card) != event.color) {
											list.push(card);
										}
									});
									player.gain(list.randomGets(2), 'draw2');
								}
								('step 4');
								if (event.count > 0) {
									player.chooseBool(get.prompt2('lqtq_anhong')).set('frequentSkill', 'lqtq_anhong');
								} else {
									event.finish();
								}
								('step 5');
								if (result.bool) {
									event.goto(1);
								}
							},
							ai: {
								maixie: true,
								maixie_hp: true,
								threaten: 0.8,
								effect: {
									target(card, player, target) {
										if (get.tag(card, 'damage')) {
											if (player.hasSkillTag('jueqing', false, target)) {
												return [1, -2];
											}
											if (!target.hasFriend()) {
												return;
											}
											if (target.hp >= 4) {
												return [0.5, get.tag(card, 'damage') * 2];
											}
											if (!target.hasSkill('lqtq_anhong') && target.hp > 1) {
												return [0.5, get.tag(card, 'damage') * 1.5];
											}
											if (target.hp == 3) {
												return [0.5, get.tag(card, 'damage') * 1.5];
											}
											if (target.hp == 2) {
												return [1, get.tag(card, 'damage') * 0.5];
											}
										}
									},
								},
							},
						},
						lqtq_weishang: {
							trigger: {
								global: 'damage',
							},
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player) {
								return event.player != player;
							},
							firstDo: true,
							_priority: null,
							usable: 1,
							derivation: 'lqtq_weishang_zhu',
							content() {
								'step 0';
								game.log('此次受伤角色变为了', player);
								trigger.player = player;
								('step 1');
								const targets = game.players;

								for (const i of targets) {
									if (i.hp <= 0 && i.isAlive()) {
										i.dying();
									}
								}
							},
						},
						lqtq_yujue: {
							trigger: {
								source: 'damageBefore',
							},
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player) {
								return event.player != player;
							},
							_priority: null,
							forced: true,
							async content(event, trigger, player) {
								trigger.player.hp -= trigger.num;
								trigger.cancel();
							},
						},
						lqtq_tiantuo: {
							audio: 'ext:浪琴天阙/audio:3',
							intro: {
								name: '天拓',
								mark(dialog, content, player) {
									let a = 0,
										b = 0,
										c = 0,
										d = 0,
										e = 0,
										f = 0,
										g = 0;
									for (const i of player.storage.lqtq_tiantuo) {
										if (i == 'spade') {
											a++;
										}
										if (i == 'heart') {
											b++;
										}
										if (i == 'club') {
											c++;
										}
										if (i == 'diamond') {
											d++;
										}
										if (i == 'basic') {
											e++;
										}
										if (i == 'trick') {
											f++;
										}
										if (i == 'equip') {
											g++;
										}
									}
									dialog.addText('总记录数:' + player.storage.lqtq_tiantuo.length);
									if (a > 0) {
										dialog.addText('<font size=4>&nbsp;♠️️️️&nbsp</font>:' + a);
									}
									if (b > 0) {
										dialog.addText('<font size=4>&nbsp;♥️️️️&nbsp;</font>:' + b);
									}
									if (c > 0) {
										dialog.addText('<font size=4>&nbsp;♣️️️️&nbsp;</font>:' + c);
									}
									if (d > 0) {
										dialog.addText('<font size=4>&nbsp;♦️️️️&nbsp;</font>:' + d);
									}
									if (e > 0) {
										dialog.addText('基本:' + e);
									}
									if (f > 0) {
										dialog.addText('锦囊:' + f);
									}
									if (g > 0) {
										dialog.addText('装备:' + g);
									}
								},
							},
							forced: true,
							trigger: {
								global: ['useCardEnd', 'respondEnd'],
								player: ['damageEnd', 'phaseBegin'],
							},
							filter(event, player) {
								if (event.name == 'phase') {
									return !player.storage.lqtq_tiantuo2 || player.storage.lqtq_tiantuo2.length < 16;
								}
								if (event.name == 'damage') {
									return player.storage.lqtq_tiantuo;
								}
								return event.cards && ((event.player == player && player.storage.lqtq_tiantuo) || event.cards.length);
							},
							derivation: ['wusheng', 'qingguo', 'jijiu', 'jiuchi', 'qixi', 'huoji', 'lqtq_lingnian', 'yushou_huofu', 'wanjun', 'tannang', 'fengze', 'lianhuan', 'luanji', 'liangce', 'kanpo', 'zhijie'],
							content() {
								'step 0';
								if (trigger.card && !trigger.card.isCard) {
									if (!player.storage.lqtq_tiantuo) {
										player.storage.lqtq_tiantuo = [];
									}
									var list = [];
									for (const i of trigger.cards) {
										list.push(get.translation(get.type2(i)));
										list.push(get.translation(i.suit));
										player.storage.lqtq_tiantuo.push(get.type2(i));
										player.storage.lqtq_tiantuo.push(i.suit);
										player.markSkill('lqtq_tiantuo');
									}
									game.log(player, '记录了', '#g' + list);
									event.finish();
								} else if (trigger.name == 'phase') {
									if (!player.storage.lqtq_tiantuo2) {
										player.storage.lqtq_tiantuo2 = [];
									}
									const a = [];
									for (const i of lib.skill.lqtq_tiantuo.derivation) {
										if (!player.storage.lqtq_tiantuo2.includes(i)) {
											a.push(i);
										}
									}
									const dialog = ui.create.dialog('<b>选择一个技能获得', 'hidden');
									const yi = document.createElement('div');
									yi.classList.add('add-setting');
									yi.style.margin = '0';
									yi.style.width = '100%';
									yi.style.position = 'relative';

									for (const i of a) {
										const td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
										td.innerHTML = '<font size=4>【' + get.translation(i) + '】<br><P align=left>' + get.skillInfoTranslation(i);
										td.link = i;
										td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
										Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
										yi.appendChild(td);
										dialog.buttons.add(td);
									}

									dialog.content.appendChild(yi);
									const chooseButton = player.chooseButton(dialog, true);
									chooseButton.set('ai', function (button) {
										return Math.random();
									});
								} else {
									var list = [];
									for (const i of player.storage.lqtq_tiantuo) {
										if (lib.suit.includes(i)) {
											list.add(i);
										}
									}
									for (const i of player.storage.lqtq_tiantuo) {
										if (['basic', 'trick', 'equip'].includes(i)) {
											list.add(i);
										}
									}
									if (!list.length) {
										return;
									}
									player
										.chooseControl(list)
										.set('ai', function () {
											return 0;
										})
										.set('prompt', '###【天拓】###移去一个记录的花色或类型,从牌堆中获得一张相同花色或相同类型的牌.');
								}
								('step 1');
								if (result.bool && trigger.name == 'phase') {
									const skill = result.links[0];
									player.addTempSkill(skill, { source: 'damageSource' });
									player.popup(skill);
									player.storage.lqtq_tiantuo2.push(skill);
									game.log(player, '获得了技能', '#g【' + get.translation(skill) + '】');
								} else {
									player.storage.lqtq_tiantuo.remove(result.control);
									if (!player.storage.lqtq_tiantuo.length) {
										player.unmarkSkill('lqtq_tiantuo');
									}
									var list = [];
									get.cardPile(function (card) {
										if (card.suit == result.control || get.type2(card) == result.control) {
											list.push(card);
										}
									});
									if (list.length) {
										player.gain(list.randomGet(), 'gain2');
									}
								}
							},
						},
						lqtq_fuyin: {
							enable: ['chooseToUse', 'chooseToRespond'],
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player) {
								for (const i of lib.inpile) {
									if (event.filterCard && event.filterCard({ name: i }, player, event) && player.countCards('h', i)) {
										return true;
									}
								}
								return false;
							},
							hiddenCard(player, name) {
								return player.countCards('h', name) > 0;
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (!player.countCards('h', name)) {
											continue;
										}
										if (name == 'sha') {
											if (
												player.countCards('h', function (card) {
													return card.name == 'sha' && !card.nature;
												}) &&
												event.filterCard &&
												event.filterCard({ name: name }, player, event)
											) {
												list.push(['基本', '', 'sha']);
											}
											for (const j of lib.inpile_nature) {
												if (player.countCards('h', { name: name, nature: j }) && event.filterCard && event.filterCard({ name: name, nature: j }, player, event)) {
													list.push(['基本', '', 'sha', j]);
												}
											}
										} else if (get.type2(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										} else if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['基本', '', name]);
										} else if (get.type(name) == 'equip' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['装备', '', name]);
										}
									}

									return ui.create.dialog('复音', [list, 'vcard']);
								},
								filter(button, player) {
									return _status.event.parent.filterCard({ name: button.link[2] }, player, _status.event.parent);
								},
								check(button) {
									const player = _status.event.player;
									if (player.storage.lqtq_tiantuo && player.storage.lqtq_tiantuo.length) {
										return 0;
									}
									if (_status.event.parent.type != 'phase') {
										return 1;
									}
									return player.getUseValue({
										name: button.link[2],
										nature: button.link[3],
									});
								},
								backup(links, player) {
									return {
										filterCard(card, player) {
											return card.name == links[0][2] && card.nature == links[0][3];
										},
										popname: true,
										check(card) {
											return true;
										},
										viewAs: { name: links[0][2], nature: links[0][3] },
										precontent() { },
									};
								},
								prompt(links, player) {
									return '将一张' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]) + '当做' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]) + '使用';
								},
							},
							ai: {
								skillTagFilter(player) {
									return player.countCards('h') > 0;
								},
								order: 19,
								result: {
									player(player) {
										if (_status.event.dying) {
											return get.attitude(player, _status.event.dying);
										}
										return 1;
									},
								},
							},
						},
						lqtq_pingnan: {
							audio: 'ext:浪琴天阙/audio:1',
							init(player) {
								player.addAdditionalSkill('lqtq_pingnan', ['xinzhengnan']);
							},
							onremove(player) {
								player.removeAdditionalSkill('lqtq_pingnan');
							},
							derivation: 'xinzhengnan',
							trigger: {
								global: ['useCardAfter', 'loseHpAfter', 'damageCancelled', 'damageZero', 'damageAfter'],
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'useCardAfter') {
									return event.card && event.card.name == 'sha' && _status.currentPhase != event.player;
								}
								if (name == 'damageCancelled') {
									return player.countCards('he');
								}
								if (name == 'loseHpAfter') {
									return true;
								}
								for (const i of event.change_history) {
									if (i < 0 && player.countCards('he')) {
										return true;
									}
								}
								return false;
							},
							content() {
								'step 0';
								var a = trigger.name;
								if (a == 'useCard' || a == 'loseHp') {
									player.chooseBool(get.prompt('lqtq_pingnan'), '令' + get.translation(trigger.player) + (a == 'useCard' ? '本回合出杀上限+1.' : '摸两张牌.')).set('ai', function () {
										return get.attitude(_status.event.player, trigger.player) > 0;
									});
								} else {
									player.chooseToDiscard('he', get.prompt('lqtq_pingnan'), '弃置一张牌,令' + get.translation(trigger.player) + '受到一点无来源的伤害.').set('ai', function (card) {
										return get.attitude(_status.event.player, trigger.player) < 0;
									});
								}
								('step 1');
								if (result.bool) {
									var a = trigger.name;
									if (a == 'useCard') {
										trigger.player.addTempSkill('lqtq_pingnan_more');
										trigger.player.addMark('lqtq_pingnan_more', 1, false);
									} else if (a == 'loseHp') {
										trigger.player.draw(2);
									} else {
										trigger.player.damage('nosource');
									}
								}
							},
							subSkill: {
								more: {
									charlotte: true,
									mod: {
										cardUsable(card, player, num) {
											if (card.name == 'sha') {
												return num + player.countMark('lqtq_pingnan_more');
											}
										},
									},
								},
							},
						},
						lqtq_jingzhi: {
							trigger: {
								player: 'phaseUseBegin',
							},
							forced: true,
							audio: 'ext:浪琴天阙/audio:2',
							derivation: 'lqtq_jingzhi_tuiyan',
							group: ['lqtq_jingzhi2_add', 'lqtq_jingzhi2'],
							intro: {
								content: 'expansion',
								markcount: 'expansion',
							},
							onremove(player, skill) {
								const cards = player.getExpansions(skill);
								if (cards.length) {
									player.loseToDiscardpile(cards);
								}
							},
							content() {
								'step 0';
								player.throwDice();
								('step 1');
								game.log('骰子点数为', num);
								const list = [],
									cards = [];
								get.cardPile(function (card) {
									const number = card.number;
									if (((num == 1 && [1, 7, 11, 13].includes(card.number)) || (num != 1 && card.number % num == 0)) && !list.includes(number)) {
										list.push(number);
										cards.push(card);
									}
								});
								player.addToExpansion(cards, player, 'giveAuto').gaintag.add('lqtq_jingzhi');
							},
						},
						lqtq_jingzhi2: {
							audio: 'lqtq_jingzhi',
							enable: 'phaseUse',
							filter(event, player) {
								return player.getExpansions('lqtq_jingzhi').length && (!player.hasSkill('lqtq_jingzhi2_0') || !player.hasSkill('lqtq_jingzhi2_1'));
							},
							chooseButton: {
								check(button) {
									return 1;
								},
								dialog(event, player) {
									const dialog = ui.create.dialog('精智阵', 'hidden');
									const table = document.createElement('div');
									table.classList.add('add-setting');
									table.style.margin = '0';
									table.style.width = '100%';
									table.style.position = 'relative';
									const list = ['获得一张精智牌', '获得场上一张牌'];
									for (let i = 0; i < list.length; i++) {
										if (player.hasSkill('lqtq_jingzhi2_' + i)) {
											continue;
										}
										if (
											!game.hasPlayer(function (current) {
												return current.countCards('ej') > 0;
											}) &&
											'lqtq_jingzhi2_' + i == 'lqtq_jingzhi2_1'
										) {
											continue;
										}
										const td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
										td.innerHTML = '<span>' + list[i] + '</span>';
										td.link = i;
										td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
										Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
										table.appendChild(td);
										dialog.buttons.add(td);
									}
									dialog.content.appendChild(table);
									dialog.add(player.getExpansions('lqtq_jingzhi'));
									return dialog;
								},
								select: 2,
								filter(button, player) {
									if (ui.selected.buttons.length) {
										return typeof ui.selected.buttons[0].link != typeof button.link;
									}
									return true;
								},
								backup(links) {
									if (typeof links[0] == 'object') {
										links.reverse();
									}
									const next = get.copy(lib.skill['lqtq_jingzhi2_backup' + links[0]]);
									next.card = links[1];
									return next;
								},
								prompt(links, player) {
									if (typeof links[0] == 'object') {
										links.reverse();
									}
									const card = get.translation(links[1]);
									if (links[0] == 0) {
										return '获得' + card;
									}
									return '移去' + card + '并获得场上一张牌';
								},
							},
							ai: {
								order: 11,
								result: { player: 1 },
							},
							subSkill: {
								add: {
									trigger: {
										player: 'phaseEnd',
									},
									audio: 'lqtq_jingzhi',
									forced: true,
									filter(event, player) {
										return (
											player.getExpansions('lqtq_jingzhi').length > 1 &&
											game.hasPlayer(function (current) {
												return current != player && current.countCards('h') > 0;
											})
										);
									},
									content() {
										'step 0';
										player.chooseCardButton(2, player.getExpansions('lqtq_jingzhi'), '###【精智推演】###弃置两张精智牌并查看一名其他角色的一张手牌,若该牌点数的倍数中有数值等于X,你获得该角色区域内的所有牌,等于X+1/-1,你摸两张牌,否则你获得该角色的展示牌.(X为弃置精智牌的点数合计).').ai = function (button) {
											return 8 - get.value(button.link);
										};
										('step 1');
										if (result.links?.length) {
											event.num1 = result.links[0].number + result.links[1].number;
											event.card1 = result.links;
											player.loseToDiscardpile(result.links);
											player
												.chooseTarget(true, '查看一名其他角色的一张手牌', function (card, player, target) {
													return target != player && target.countCards('h') > 0;
												})
												.set('ai', function (target) {
													return -get.attitude(_status.event.player, target);
												});
										} else {
											event.finish();
										}
										('step 2');
										event.count = result.targets[0];
										player.choosePlayerCard(result.targets[0], 'h', true);
										('step 3');
										const card = result.cards[0];
										player.showCards(card);
										const num1 = event.num1 + 1,
											num2 = event.num1 - 1;
										if (event.num1 % card.number == 0) {
											event.count.give(event.count.getCards('hej'), player);
										} else if (num1 % card.number == 0 || num2 % card.number == 0) {
											player.draw(2);
										} else {
											player.gain(card, 'draw2');
										}
									},
								},
								0: {},
								1: {},
								backup0: {
									audio: 'lqtq_jingzhi',
									filterCard: () => false,
									selectCard: -1,
									delay: false,
									content() {
										player.addTempSkill('lqtq_jingzhi2_0', 'phaseUseEnd');
										const card = lib.skill.lqtq_jingzhi2_backup.card;
										player.gain(card, 'draw2');
									},
									ai: {
										result: {
											player: 1,
										},
									},
								},
								backup1: {
									audio: 'lqtq_jingzhi',
									filterCard: () => false,
									selectCard: -1,
									filterTarget(card, player, target) {
										return target.countCards('ej');
									},
									delay: false,
									selectTarget: 1,
									content() {
										player.addTempSkill('lqtq_jingzhi2_1', 'phaseUseEnd');
										const card = lib.skill.lqtq_jingzhi2_backup.card;
										player.loseToDiscardpile(card);
										player.gainPlayerCard('je', target, true);
									},
									ai: {
										result: {
											target(player, target) {
												return lib.card.guohe.ai.result.target.apply(this, arguments);
											},
										},
									},
								},
							},
						},
						lqtq_xuansha: {
							intro: {
								content: 'expansion',
								markcount: 'expansion',
							},
							onremove(player, skill) {
								const cards = player.getExpansions(skill);
								if (cards.length) {
									player.loseToDiscardpile(cards);
								}
							},
							audio: 'ext:浪琴天阙/audio:1',
							trigger: {
								global: ['loseAfter', 'cardsDiscardAfter'],
								player: 'phaseAfter',
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'phaseAfter') {
									return player.getExpansions('lqtq_xuansha').length;
								}
								if (event.name == 'lose' && event.position != ui.discardPile) {
									return false;
								}
								return event.cards?.length;
							},
							async content(event, trigger, player) {
								if (event.triggername == 'phaseAfter') {
									let count = 0;
									while (player.getExpansions('lqtq_xuansha').length) {
										const { links } = await player
											.chooseCardButton(count > 0 ? '' : true, '将一张<玄刹>牌置于牌堆底.', player.getExpansions('lqtq_xuansha'))
											.set('filterButton', function (button) {
												if (count > 0) {
													return button.link.number == count + 1;
												}
												return true;
											})
											.set('ai', function (button) {
												return 20 - button.link.number;
											})
											.forResult();
										if (links?.length) {
											count = links[0].number;
											ui.cardPile.appendChild(links[0], ui.cardPile.firstChild);
											game.log(player, '将', links[0], '置于牌堆底');
											const { targets } = await player
												.chooseTarget(true, '对一名角色造成一点雷电伤害.')
												.set('ai', (t) => -get.attitude(player, t))
												.forResult();
											if (targets?.length) {
												targets[0].damage('thunder');
											}
										} else {
											break;
										}
									}
								} else {
									await player.addToExpansion(trigger.cards, player, 'gain2').gaintag.add('lqtq_xuansha');
									if (player.getExpansions('lqtq_xuansha').length > ui.cardPile.childElementCount) {
										const { links } = await player
											.chooseCardButton(true, 5, '获得5张<玄刹>牌.', player.getExpansions('lqtq_xuansha'))
											.set('ai', (button) => get.value(button.link))
											.forResult();
										if (links?.length) {
											await player.gain(links, 'gain2');
											for (const i of player.getExpansions('lqtq_xuansha')) {
												ui.cardPile.appendChild(i, ui.cardPile.firstChild);
												game.log(player, '将', i, '置于牌堆底');
											}
										}
									}
								}
							},
							group: 'lqtq_xuansha_dying',
							subSkill: {
								dying: {
									enable: ['chooseToUse', 'chooseToRespond'],
									filter(event, player) {
										return event.filterCard && event.filterCard({ name: 'tao' }, player, event) && player.getExpansions('lqtq_xuansha').length;
										return false;
									},
									hiddenCard(player, name) {
										return name == 'tao' && player.getExpansions('lqtq_xuansha').length;
									},
									chooseButton: {
										dialog(event, player) {
											return ui.create.dialog('玄刹', player.getExpansions('lqtq_xuansha'), 'hidden');
										},
										filter(button, player) {
											const g = button.link.suit,
												u = ui.selected.buttons.length;
											if (u == 1) {
												return g != ui.selected.buttons[0].suit;
											}
											if (u == 2) {
												return g != ui.selected.buttons[0].suit && g != ui.selected.buttons[1].suit;
											}
											if (u == 3) {
												return g != ui.selected.buttons[0].suit && g != ui.selected.buttons[1].suit && g != ui.selected.buttons[2].suit;
											}
											return true;
										},
										select: 4,
										check(button) {
											return 100 - get.value(button.link);
										},
										backup(links, player) {
											return {
												viewAs: {
													name: 'tao',
												},
												card: links,
												filterCard() {
													return false;
												},
												selectCard: -1,
												onuse(result, player) {
													const cards = lib.skill.lqtq_xuansha_dying_backup.card;
													while (cards.length) {
														const card = cards.pop();
														card.fix();
														ui.cardPile.appendChild(card, ui.cardPile.firstChild);
														game.log(player, '将', card, '置于牌堆底');
													}
												},
											};
										},
										prompt(links, player) {
											return '视为使用一张【桃】';
										},
									},
									ai: {
										skillTagFilter(player) {
											return player.getExpansions('lqtq_xuansha').length;
										},
										order: 19,
										result: {
											player(player) {
												if (_status.event.dying) {
													return get.attitude(player, _status.event.dying);
												}
												return 1;
											},
										},
									},
								},
							},
						},
						lqtq_buju: {
							audio: 'ext:浪琴天阙/audio:2',
							enable: 'phaseUse',
							content() {
								'step 0';
								const d = [];
								for (const i of ['shuizhuan', 'zhengli', 'yvfeng', 'jiezou', 'pinzi', 'guale', 'touzi', 'yingbi', 'qiuqian', 'mouyi']) {
									let a = 'majun',
										b = '水转百戏图',
										c = 'lqtq_youxi_' + i;
									if (i == 'zhengli') {
										a = 'zhengxuan';
										b = '整理卡牌';
									}
									if (i == 'yvfeng') {
										a = 'nanhualaoxian';
										b = '御风飞行';
									}
									if (i == 'jiezou') {
										a = 'sunhanhua';
										b = '节奏大师';
									}
									if (i == 'pinzi') {
										a = 'lqtq_langqinjun';
										b = '拼字成牌';
									}
									if (i == 'guale') {
										a = 'pangdegong';
										b = '刮刮乐';
									}
									if (i == 'touzi') {
										a = 'old_majun';
										b = '掷骰子';
									}
									if (i == 'yingbi') {
										a = 'lqtq_dubiao';
										b = '抛硬币';
									}
									if (i == 'qiuqian') {
										a = 'zhouqun';
										b = '求签';
									}
									if (i == 'mouyi') {
										a = ['sb_xuhuang', 'sb_machao'].randomGet();
										b = '谋弈';
									}
									lib.card[c] = {
										fullimage: true,
										image: 'character:' + a,
									};
									lib.translate[c] = get.translation(b);
									lib.translate[c + '_info'] = '你进行一次<' + b + '>';
									d.push(['', '', c]);
								}
								player.chooseButton(['做点什么好呢？', [d, 'vcard']], true).set('ai', function () {
									return Math.random();
								});
								('step 1');
								event.aa = result.links[0][2].slice(11);
								event.bb = [];
								if (event.aa == 'shuizhuan') {
									event.videoId = lib.status.videoId++;
									if (player.isUnderControl()) {
										game.swapPlayerAuto(player);
									}
									var switchToAuto = function () {
										game.pause();
										game.countChoose();
										setTimeout(function () {
											_status.imchoosing = false;
											event._result = {
												bool: true,
												links: ['lqtq_c1', 'lqtq_c6'].concat(['lqtq_c2', 'lqtq_c5'].randomGet()),
											};
											if (event.dialog) {
												event.dialog.close();
											}
											if (event.controls) {
												for (const i of event.controls) {
													i.close();
												}
											}
											game.resume();
										}, 1000);
									};
									var createDialog = function (player, id) {
										if (player == game.me) {
											return;
										}
										let str = get.translation(player) + '正在表演...<br>';
										for (let i = 1; i < 10; i++) {
											str += get.translation('lqtq_c' + i);
											if (i % 3 != 0) {
												str += '　　';
											}
											if (i == 3) {
												str += '<br>';
											}
											if (i == 6) {
												str += '<br>';
											}
										}
										ui.create.dialog(str, 'forcebutton').videoId = id;
									};
									var chooseButton = function (player) {
										const event = _status.event;
										player = player || event.player;
										event.status = { lqtq_c1: 0, lqtq_c2: 0, lqtq_c3: 0, lqtq_c4: 0, lqtq_c5: 0, lqtq_c6: 0 };
										event.map = { lqtq_c1: [100, 90], lqtq_c2: [100, 120], lqtq_c3: [100, 120], lqtq_c4: [100, 120], lqtq_c5: [100, 120], lqtq_c6: [100, 90] };
										event.finishedx = [];
										event.str = '【妹汁转百戏图】<br><img src="extension/浪琴天阙/image/辅助图/lqtq_card1.png" width="70" height="70">lqtq_c1% <img src="extension/浪琴天阙/image/辅助图/lqtq_card2.png" width="70" height="70">lqtq_c2% <img src="extension/浪琴天阙/image/辅助图/lqtq_card3.png" width="70" height="70">lqtq_c3%<br><img src="extension/浪琴天阙/image/辅助图/lqtq_card4.png" width="70" height="70">lqtq_c4%<img src="extension/浪琴天阙/image/辅助图/lqtq_card5.png" width="70" height="70">lqtq_c5% <img src="extension/浪琴天阙/image/辅助图/lqtq_card6.png" width="70" height="70">lqtq_c6%';
										event.dialog = ui.create.dialog(event.str, 'forcebutton', 'hidden');
										event.dialog.addText('<li>点击下方的妹子按钮,可增加妹汁填满此图.一次表演最多只能完成3名妹子的进度.', false);
										event.dialog.open();
										for (const i in event.status) {
											event.dialog.content.childNodes[0].innerHTML = event.dialog.content.childNodes[0].innerHTML.replace(i, event.status[i]);
										}

										for (const i of event.dialog.buttons) {
											i.classList.add('pointerdiv');
										}

										event.switchToAuto = function () {
											event._result = {
												bool: true,
												links: event.finishedx.slice(0),
											};
											event.dialog.close();
											for (const i of event.controls) {
												i.close();
											}
											game.resume();
											_status.imchoosing = false;
										};
										event.controls = [];
										for (let i = 1; i <= 6; i++) {
											event.controls.push(
												ui.create.control('lqtq_c' + i, function (link) {
													const event = _status.event;
													if (event.finishedx.includes(link)) {
														return;
													}
													event.status[link] += get.rand.apply(get, event.map[link]);
													if (event.status[link] >= 100) {
														event.status[link] = 100;
														var str = event.str.slice(0);
														for (const i in event.status) {
															str = str.replace(i, event.status[i]);
														}
														event.dialog.content.childNodes[0].innerHTML = str;
														event.finishedx.push(link);
														if (event.finishedx.length >= 3) {
															event._result = {
																bool: true,
																links: event.finishedx.slice(0),
															};
															event.dialog.close();
															for (const i of event.controls) {
																i.close();
															}
															game.resume();
															_status.imchoosing = false;
														}
													} else {
														var str = event.str.slice(0);
														for (const i in event.status) {
															str = str.replace(i, event.status[i]);
														}
														event.dialog.content.childNodes[0].innerHTML = str;
													}
												}),
											);
										}

										for (const i of event.dialog.buttons) {
											i.classList.add('selectable');
										}

										game.pause();
										game.countChoose();
									};
									game.broadcastAll(createDialog, player, event.videoId);
									if (event.isMine()) {
										chooseButton();
									} else if (event.isOnline()) {
										event.player.send(chooseButton, event.player);
										event.player.wait();
										game.pause();
									} else {
										switchToAuto();
									}
								} else if (event.aa == 'zhengli') {
									if (_status.connectMode) {
										event.time = lib.configOL.choose_timeout;
									}
									const names = [];
									while (true) {
										var card = get.cardPile(function (carde) {
											return carde.name != 'du' && !names.includes(carde.name);
										});
										if (card) {
											event.bb.push(card);
											names.push(card.name);
											if (event.bb.length >= 5) {
												break;
											}
										} else {
											break;
										}
									}
									if (!event.bb.length) {
										event.finish();
										return;
									}
									event.videoId = lib.status.videoId++;
									if (player.isUnderControl()) {
										game.swapPlayerAuto(player);
									}
									var switchToAuto = function () {
										game.pause();
										game.countChoose();
										setTimeout(function () {
											_status.imchoosing = false;
											event._result = {
												bool: true,
												links: names.slice(0),
											};
											if (event.dialog) {
												event.dialog.close();
											}
											if (event.control) {
												event.control.close();
											}
											game.resume();
										}, 5000);
									};
									var createDialog = function (player, id) {
										if (_status.connectMode) {
											lib.configOL.choose_timeout = '30';
										}
										if (player == game.me) {
											return;
										}
										const str = get.translation(player) + '正在整理经书...<br>';
										ui.create.dialog(str).videoId = id;
									};
									var chooseButton = function (list) {
										let roundmenu = false;
										if (ui.roundmenu && ui.roundmenu.display != 'none') {
											roundmenu = true;
											ui.roundmenu.style.display = 'none';
										}
										const event = _status.event;
										event.settleed = false;
										event.finishedx = [];
										event.map = {};
										let names = list.slice(0);
										event.zhengjing_nodes = [];
										names.randomSort();
										const names2 = names.slice(0);
										for (let i = 0; i < 2; i++) {
											names2.randomSort();
											names = names.concat(names2);
										}
										event.zhengjing = names;
										for (const i of list) {
											event.map[i] = 0;
										}
										event.dialog = ui.create.dialog('forcebutton', 'hidden');
										event.dialog.textPrompt = event.dialog.add('<div class="text center">及时点击卡牌!</div>');
										let str = '<div class="text center">';
										for (const i of list) {
											str += get.translation(i) + ':' + Math.min(2, event.map[i]) + '/2 ';
										}
										str += '</div>';
										event.dialog.textPrompt2 = event.dialog.add(str);
										event.switchToAuto = function () {
											event._result = {
												bool: true,
												links: event.finishedx.slice(0),
											};
											event.dialog.close();
											game.resume();
											_status.imchoosing = false;
											if (roundmenu) {
												ui.roundmenu.style.display = '';
											}
										};
										event.dialog.classList.add('fixed');
										event.dialog.classList.add('scroll1');
										event.dialog.classList.add('scroll2');
										event.dialog.classList.add('fullwidth');
										event.dialog.classList.add('fullheight');
										event.dialog.classList.add('noupdate');
										event.dialog.open();
										event.settle = function (du) {
											if (event.settleed) {
												return;
											}
											event.settleed = true;
											event.dialog.textPrompt2.innerHTML = '';
											if (lib.config.background_speak) {
												game.playAudio('skill', 'zhengjing_finish');
											}
											event.dialog.textPrompt.innerHTML = '<div class="text center">整理经典结束!共整理出' + get.cnNumber(event.finishedx.length) + '份经典</div>';
											while (event.zhengjing_nodes.length) {
												event.zhengjing_nodes.shift().delete();
											}
											setTimeout(function () {
												event.switchToAuto();
											}, 1000);
										};
										const click = function () {
											const name = this.name;
											if (lib.config.background_speak) {
												game.playAudio('skill', 'zhengjing_click');
											}
											event.map[name]++;
											if (event.map[name] > 1) {
												event.finishedx.add(name);
											}
											if (event.finishedx.length < list.length) {
												let str = '<div class="text center">';
												for (const i of list) {
													str += get.translation(i) + ':' + Math.min(2, event.map[i]) + '/2 ';
												}
												str += '</div>';
												event.dialog.textPrompt2.innerHTML = str;
											} else {
												event.zhengjing.length = 0;
												event.settle();
											}
											event.zhengjing_nodes.remove(this);
											this.style.transition = 'all 0.5s';
											this.style.transform = 'scale(1.2)';
											this.delete();
										};
										const addNode = function () {
											if (event.zhengjing.length) {
												const card = ui.create.card(ui.special, 'noclick', true);
												card.init(['', '', event.zhengjing.shift()]);
												card.addEventListener(lib.config.touchscreen ? 'touchstart' : 'mousedown', click);
												event.zhengjing_nodes.push(card);
												card.style.position = 'absolute';
												const rand1 = Math.round(Math.random() * 100);
												const rand2 = Math.round(Math.random() * 100);
												const rand3 = Math.round(Math.random() * 40) - 20;
												card.style.left = 'calc(' + rand1 + '% - ' + rand1 + 'px)';
												card.style.top = 'calc(' + rand2 + '% - ' + rand2 + 'px)';
												card.style.transform = 'scale(0.8) rotate(' + rand3 + 'deg)';
												card.style.opacity = 0;
												event.dialog.appendChild(card);
												ui.refresh(card);
												card.style.opacity = 1;
												card.style.transform = 'scale(1) rotate(' + rand3 + 'deg)';
											}
											if (event.zhengjing_nodes.length > (event.zhengjing.length ? 2 : 0)) {
												event.zhengjing_nodes.shift().delete();
											}
											if (event.zhengjing.length || event.zhengjing_nodes.length) {
												setTimeout(function () {
													addNode();
												}, 800);
											} else {
												event.settle();
											}
										};
										game.pause();
										game.countChoose();
										addNode();
									};
									game.broadcastAll(createDialog, player, event.videoId);
									if (event.isMine()) {
										chooseButton(names);
									} else if (event.isOnline()) {
										event.player.send(chooseButton, names);
										event.player.wait();
										game.pause();
									} else {
										switchToAuto();
									}
								} else if (event.aa == 'yvfeng') {
									event.count = 5;
									if (_status.auto || !player.isUnderControl(true)) {
										event.count = 5;
									}
									if (_status.connectMode) {
										event.time = lib.configOL.choose_timeout;
									}
									event.videoId = lib.status.videoId++;
									if (player.isUnderControl()) {
										game.swapPlayerAuto(player);
									}
									var switchToAuto = function () {
										game.pause();
										game.countChoose();
										setTimeout(function () {
											_status.imchoosing = false;
											const score = Math.random() < 0.5 ? event.count : get.rand(1, event.count);
											event._result = {
												bool: true,
												score: score,
												win: score >= event.count,
											};
											if (event.dialog) {
												event.dialog.close();
											}
											if (event.control) {
												event.control.close();
											}
											game.resume();
										}, 5000);
									};
									var createDialog = function (player, id) {
										if (_status.connectMode) {
											lib.configOL.choose_timeout = '30';
										}
										if (player == game.me) {
											return;
										}
										const str = get.translation(player) + '正在表演<御风飞行>...<br>';
										ui.create.dialog(str).videoId = id;
									};
									var chooseButton = function () {
										let roundmenu = false;
										if (ui.roundmenu && ui.roundmenu.display != 'none') {
											roundmenu = true;
											ui.roundmenu.style.display = 'none';
										}
										const event = _status.event;
										event.settleed = false;
										event.score = 0;
										event.dialog = ui.create.dialog('forcebutton', 'hidden');
										event.dialog.textPrompt = event.dialog.add('<div class="text center">准备好了吗？准备好了的话就点击屏幕开始吧!</div>');
										event.dialog.textPrompt.style['z-index'] = 10;
										event.switchToAuto = function () {
											event._result = {
												bool: true,
												score: event.score,
												win: event.score >= event.count,
											};
											event.dialog.close();
											game.resume();
											_status.imchoosing = false;
											if (roundmenu) {
												ui.roundmenu.style.display = '';
											}
										};
										event.dialog.classList.add('fixed');
										event.dialog.classList.add('scroll1');
										event.dialog.classList.add('scroll2');
										event.dialog.classList.add('fullwidth');
										event.dialog.classList.add('fullheight');
										event.dialog.classList.add('noupdate');
										event.dialog.style.overflow = 'hidden';
										event.dialog.open();
										const height = event.dialog.offsetHeight;
										const width = event.dialog.offsetWidth;
										let top = 50;
										let speed = 0;
										let start = false;
										const bird = ui.create.div('');
										bird.style['background-image'] = 'linear-gradient(rgba(240, 235, 3, 1), rgba(230, 225, 5, 1))';
										bird.style['border-radius'] = '3px';
										const pipes = [];
										bird.style.position = 'absolute';
										bird.style.height = '40px';
										bird.style.width = '40px';
										bird.style.left = Math.ceil(width / 3) + 'px';
										bird.style.top = (top / 100) * height + 'px';
										bird.updatePosition = function () {
											bird.style.transform = 'translateY(' + ((top / 100) * height - bird.offsetTop) + 'px)';
										};
										event.dialog.appendChild(bird);
										const isDead = function () {
											if (top > 100 || top < 0) {
												return true;
											}
											const btop = top;
											const bleft = 100 / 3;
											const bdown = btop + 5;
											const bright = bleft + 5;
											for (const i of pipes) {
												const left2 = i.left;
												const right2 = left2 + 10;
												const bottom2 = i.height1;
												const top2 = i.height2;
												if (left2 > bright || right2 < bleft) {
													continue;
												}
												if (btop < bottom2) {
													return true;
												}
												if (bdown > top2) {
													return true;
												}
												return false;
											}
											return false;
										};
										const fly = function () {
											if (!start) {
												start = true;
												event.dialog.textPrompt.innerHTML = '<div class="text center">当前分数:' + event.score + '</div>';
												speed = -4;
												event.fly = setInterval(function () {
													top += speed;
													if (top < 0) {
														top = 0;
													}
													bird.updatePosition();
													for (const i of pipes) {
														i.left -= 0.5;
														i.updateLeft();
													}
													speed += 0.5;
													if (speed > 2.5) {
														speed = 2.5;
													}
													if (isDead() == true) {
														event.settle();
													}
												}, 35);
												const addPipe = function () {
													const num = get.rand(5, 55);
													const pipe1 = ui.create.div('');
													pipe1.style['background-image'] = 'linear-gradient(rgba(57, 133, 4, 1), rgba(60, 135, 6, 1))';
													pipe1.style['border-radius'] = '3px';
													pipe1.style.position = 'absolute';
													pipe1.height1 = num;
													pipe1.height2 = num + 50;
													pipe1.left = -110;
													pipe1.num = 1;
													pipe1.style.height = Math.ceil((height * num) / 100) + 'px';
													pipe1.style.width = width / 10 + 'px';
													pipe1.style.left = (pipe1.left * width) / 100 + 'px';
													pipe1.style.top = '0px';
													const pipe2 = ui.create.div('');
													pipe2.style['background-image'] = 'linear-gradient(rgba(57, 133, 4, 1), rgba(60, 135, 6, 1))';
													pipe2.style['border-radius'] = '3px';
													pipe1.pipe2 = pipe2;
													pipe2.style.position = 'absolute';
													pipe2.style.height = Math.ceil(((100 - pipe1.height2) * height) / 100) + 'px';
													pipe2.style.width = width / 10 + 'px';
													pipe2.style.left = (pipe1.left * width) / 100 + 'px';
													pipe2.style.top = Math.ceil((pipe1.height2 * height) / 100) + 'px';
													pipes.add(pipe1);
													event.dialog.appendChild(pipe1);
													event.dialog.appendChild(pipe2);
													pipe1.updateLeft = function () {
														this.style.transform = 'translateX(' + ((this.left / 100) * width - this.offsetLeft) + 'px)';
														this.pipe2.style.transform = 'translateX(' + ((this.left / 100) * width - this.pipe2.offsetLeft) + 'px)';
														if (this.left < 25 && !this.score) {
															this.score = true;
															event.score++;
															event.dialog.textPrompt.innerHTML = '<div class="text center">当前分数:' + event.score + '</div>';
															if (event.score >= event.count) {
																event.settle();
															}
														}
														if (this.left < -15) {
															this.remove();
															this.pipe2.remove();
															pipes.remove(this);
														}
													};
												};
												event.addPipe = setInterval(addPipe, 700);
											} else if (speed > 0) {
												speed = -4;
											}
										};
										document.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', fly);
										event.settle = function () {
											clearInterval(event.fly);
											clearInterval(event.addPipe);
											document.removeEventListener(lib.config.touchscreen ? 'touchend' : 'click', fly);
											setTimeout(function () {
												event.switchToAuto();
											}, 1000);
										};
										game.pause();
										game.countChoose();
									};
									game.broadcastAll(createDialog, player, event.videoId);
									if (event.isMine()) {
										chooseButton();
									} else if (event.isOnline()) {
										event.player.send(chooseButton);
										event.player.wait();
										game.pause();
									} else {
										switchToAuto();
									}
								} else if (event.aa == 'jiezou') {
									player.chooseToPlayBeatmap(lib.skill.lqtq_buju.beatmaps.randomGet());
								}
								('step 2');
								if (event.aa == 'shuizhuan') {
									game.broadcastAll('closeDialog', event.videoId);
									const map = event.result || result,
										list = map.links,
										list2 = [];
									if (!map || !map.bool || !map.links || !list.length) {
										game.log(player, '表演失败');
										event.finish();
										return;
									}
									if (list.includes('lqtq_c1')) {
										list2.push('trick');
										list2.push('trick');
									}
									if (list.includes('lqtq_c2')) {
										if (list.includes('lqtq_c1')) {
											list2.push([Math.random() < 0.7 ? 'jiu' : 'sha']);
										} else {
											list2.push(Math.random() < 0.7 ? 'equip' : [Math.random() < 0.7 ? 'jiu' : 'sha']);
										}
									}
									if (list.includes('lqtq_c3')) {
										list2.push([Math.random() < 0.6 ? 'sha' : 'jiu']);
									}
									if (list.includes('lqtq_c4')) {
										list2.push([Math.random() < 0.6 ? 'shan' : 'tao']);
									}
									if (list.includes('lqtq_c5')) {
										if (list.includes('lqtq_c6')) {
											list2.push([Math.random() < 0.7 ? 'tao' : 'shan']);
										} else {
											list2.push(Math.random() < 0.7 ? 'trick' : [Math.random() < 0.7 ? 'tao' : 'shan']);
										}
									}
									if (list.includes('lqtq_c6')) {
										list2.push('equip');
										list2.push('equip');
									}
									while (list2.length) {
										var filter = list2.shift();
										var card = get.cardPile(function (x) {
											if (event.bb.includes(x)) {
												return false;
											}
											if (typeof filter == 'string' && get.type(x, 'trick') == filter) {
												return true;
											}
											if (typeof filter == 'object' && filter.includes(x.name)) {
												return true;
											}
										});
										if (card) {
											event.bb.push(card);
										} else {
											var card = get.cardPile(function (x) {
												return !event.bb.includes(x);
											});
											if (card) {
												event.bb.push(card);
											}
											if (!event.bb.length) {
												event.finish();
											}
										}
									}
								} else if (event.aa == 'zhengli') {
									game.broadcastAll(
										function (id, time) {
											if (_status.connectMode) {
												lib.configOL.choose_timeout = time;
											}
											const dialog = get.idDialog(id);
											if (dialog) {
												dialog.close();
											}
										},
										event.videoId,
										event.time,
									);
									var result = event.result || result;
									for (let i = 0; i < event.bb.length; i++) {
										if (!result.links.includes(event.bb[i].name)) {
											event.bb.splice(i--, 1);
										}
									}
									if (event.bb.length) {
										player.showCards(event.bb, get.translation(player) + '整理出了以下经典');
										game.cardsGotoOrdering(event.bb);
									} else {
										game.log(player, '并没有整理出经典');
										player.popup('杯具');
										event.finish();
									}
								} else if (event.aa == 'yvfeng') {
									game.broadcastAll(
										function (id, time) {
											if (_status.connectMode) {
												lib.configOL.choose_timeout = time;
											}
											const dialog = get.idDialog(id);
											if (dialog) {
												dialog.close();
											}
										},
										event.videoId,
										event.time,
									);
									var result = event.result || result;
									if (result.score > 0) {
										player.popup(get.cnNumber(result.score) + '分', 'fire');
										game.log(player, '御风飞行', '#g成功');
										game.log(player, '获得了', '#g' + result.score + '分');
										event.bb = result.score;
										player
											.chooseTarget('御风:可以令最多' + result.score + '名角色跳过下回合的摸牌或出牌阶段.', [1, result.score], function (card, player, target) {
												return target != player && !target.hasSkill('lqtq_buju2');
											})
											.set('ai', function (target) {
												const player = _status.event.player;
												let att = -get.attitude(player, target),
													attx = att * 2;
												if (att <= 0 || target.hasSkill('xinfu_pdgyingshi')) {
													return 0;
												}
												if (target.hasJudge('lebu')) {
													attx -= att;
												}
												if (target.hasJudge('bingliang')) {
													attx -= att;
												}
												return attx / Math.max(2.25, Math.sqrt(target.countCards('h') + 1));
											});
									} else {
										event.finish();
									}
								}
								if (event.aa == 'jiezou') {
									const score = Math.floor(Math.min(5, result.accuracy / 17));
									game.log(player, '的演奏评级为', '#y' + result.rank[0], ',获得积分点数', '#y' + score, '分');
									game.changeZhuGuoZi(score);
								}
								('step 3');
								if (['shuizhuan', 'zhengli'].includes(event.aa)) {
									player.chooseCardButton('获得任意张牌,剩余的牌折算为等量的朱果.', event.bb, [0, Infinity], true).ai = function (button) {
										return player.getUseValue(button.link) > 6;
									};
								} else if (event.aa == 'yvfeng') {
									if (result.targets?.length) {
										result.targets.sortBySeat();
										player.line(result.targets, 'green');
										game.log(result.targets, '获得了', '#y<御风>', '效果');
										for (const i of result.targets) {
											i.addSkill('lqtq_buju2');
										}
										if (event.bb > result.targets.length) {
											game.changeZhuGuoZi(event.bb - result.targets.length);
										}
									} else {
										game.changeZhuGuoZi(event.bb);
									}
								}
								('step 4');
								if (['shuizhuan', 'zhengli'].includes(event.aa)) {
									player.gain(result.links, 'gain2');
									if (result.links.length < event.bb.length) {
										game.changeZhuGuoZi(event.bb.length - result.links.length);
									}
								}
							},
							ai: {
								order: 10,
								result: { player: 1 },
								threaten: 3.2,
							},
							beatmaps: [
								{
									name: '鳥の詩',
									//歌曲文件名(默认在audio/effect文件夹下 若要重定向到扩展 请写为'ext:扩展名称'的格式 并将文件名重命名为和上面的歌曲名称相同)
									filename: 'tori_no_uta',
									//每个音符的开始时间点(毫秒,相对未偏移的开始播放时间)
									timeleap: [1047, 3012, 4978, 5469, 5961, 6452, 6698, 7435, 8909, 10875, 12840],
									//开始播放时间的偏移量(毫秒)
									current: 500,
									//判定栏高度(相对整个对话框高度比例)
									judgebar_height: 0.9,
									//Good/Great/Prefect的位置判定范围(百分比,相对于整个对话框.以滑条的底部作为判定基准)
									range1: [20, 110],
									range2: [90, 104],
									range3: [20, 110],
									//滑条每相对于整个对话框下落1%所需的时间(毫秒)
									speed: 10,
								},
								{
									name: '竹取飛翔　～ Lunatic Princess',
									filename: 'taketori_hishou',
									timeleap: [1021, 1490, 1959, 2896, 3834, 4537, 4771, 5709, 6646, 7585, 8039, 8494, 9403, 10291, 11180, 11832, 12049, 12920, 13345, 13771, 14196],
									current: 500,
									judgebar_height: 0.9,
									range1: [20, 110],
									range2: [90, 104],
									range3: [20, 110],
									speed: 10,
									node_color: 'linear-gradient(rgba(250, 170, 190, 1), rgba(240, 160, 180, 1))',
									judgebar_color: 'linear-gradient(rgba(240, 120, 243, 1), rgba(245, 106, 230, 1))',
								},
								{
									name: 'ignotus',
									filename: 'ignotus',
									//Number of tracks
									//轨道数量
									number_of_tracks: 4,
									//Customize the track to generate for every note (0 is the first track)
									//自定义每个音符生成的轨道(0是第一个轨道)
									mapping: [0, 2, 3, 1, 1, 0, 3, 0, 0, 3, 0, 0, 2, 1, 2],
									//Convert from beats (0 is the first beat) to timeleap
									//将节拍(0是第一拍)转换为开始时间点
									timeleap: game.generateBeatmapTimeleap(170, [0, 4, 8, 12, 14, 16, 16.5, 23.5, 24, 31, 32, 40, 45, 46, 47]),
									current: 500,
									judgebar_height: 0.9,
									range1: [20, 110],
									range2: [90, 104],
									range3: [20, 110],
									speed: 10,
									node_color: 'linear-gradient(rgba(240, 250, 240, 1), rgba(230, 240, 230, 1))',
									judgebar_color: 'linear-gradient(rgba(161, 59, 150, 1), rgba(58, 43, 74, 1))',
								},
								{
									name: 'Super Mario 3D World Theme',
									filename: 'sm3dw_overworld',
									//Random (Randomly choose tracks to generate notes each play)
									//随机(每次演奏时音符会随机选择轨道生成)
									mapping: 'random',
									timeleap: [0, 1071, 1518, 2054, 4018, 4286, 5357, 6429, 7500, 8571, 9643, 10714, 11786, 12321, 12589, 12857, 13929, 15000, 16071, 17143, 18214, 18482, 18750, 19018, 19286, 20357],
									current: 500,
									judgebar_height: 0.9,
									range1: [20, 110],
									range2: [90, 104],
									range3: [20, 110],
									speed: 10,
									node_color: 'linear-gradient(rgba(120, 130, 240, 1), rgba(100, 100, 230, 1))',
									judgebar_color: 'linear-gradient(rgba(230, 40, 30, 1), rgba(220, 30, 10, 1))',
								},
							],
						},
						lqtq_buju2: {
							trigger: { player: 'phaseZhunbeiBegin' },
							forced: true,
							charlotte: true,
							content() {
								'step 0';
								player.removeSkill('lqtq_buju2');
								player.judge();
								('step 1');
								if (result.color == 'red') {
									player.skip('phaseDraw');
								} else {
									player.skip('phaseUse');
								}
							},
							mark: true,
							intro: {
								content: '准备阶段时进行判定,结果为红则跳过摸牌阶段,为黑则跳过出牌阶段',
							},
							ai: {
								order: 7,
								result: {
									player: 1,
								},
							},
						},
						lqtq_tianshu: {
							audio: 'ext:浪琴天阙/audio:2',
							enable: 'phaseUse',
							usable: 1,
							filter(event, player) {
								return player.countCards('he') > 0;
							},
							position: 'he',
							filterCard(card, target, player) {
								return card.name != 'taipingyaoshu';
							},
							filterTarget: true,
							check(card) {
								return 7 - get.value(card);
							},
							content() {
								'step 0';
								event.card = get.cardPile(function (card) {
									return card.name == 'taipingyaoshu';
								});
								if (!event.card) {
									if (!lib.inpile.includes('taipingyaoshu')) {
										lib.inpile.push('taipingyaoshu');
									}
									event.card = game.createCard2('taipingyaoshu', 'heart', 3);
								}
								target.gain(event.card, 'gain2');
								('step 1');
								if (target.getCards('h').includes(card) && card.name == 'taipingyaoshu') {
									target.chooseUseTarget(card, 'nopopup', true);
								}
							},
							ai: {
								order: 3,
								result: {
									target(player, target) {
										return target.getUseValue({ name: 'taipingyaoshu' });
									},
								},
							},
							group: 'lqtq_tianshu_lose',
							subSkill: {
								lose: {
									audio: 'lqtq_tianshu',
									trigger: {
										global: 'loseHpAfter',
									},
									filter(event, player) {
										return event.getParent('taipingyaoshu').name == 'taipingyaoshu' && event.player.isDamaged(); //QQQ
									},
									prompt: '是否发动【天书】？',
									prompt2(event) {
										return '令' + get.translation(event.player) + '回复一点体力.';
									},
									content() {
										trigger.player.recover();
									},
								},
							},
						},
						lqtqj_shashen: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								global: 'dying',
								player: 'useCard',
							},
							intro: {
								content: 'mark',
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'useCard') {
									return player.hasMark('lqtqj_shashen');
								}
								return !event.player.storage.lqtqj_shashen2;
							},
							mod: {
								targetInRange(card, player) {
									if (player.countMark('lqtqj_shashen') > 0) {
										return true;
									}
								},
								cardUsable(card, player) {
									if (player.countMark('lqtqj_shashen') > 1) {
										return Infinity;
									}
								},
							},
							content() {
								'step 0';
								if (trigger.name == 'useCard') {
									const num = player.countMark('lqtqj_shashen');
									if (num > 0) {
										game.log(trigger.card, '无距离限制');
									}
									if (num > 1) {
										game.log(trigger.card, '无距数量制');
									}
									if (num > 2) {
										trigger.directHit.addArray(
											game.filterPlayer(function (current) {
												return current != player;
											}),
										);
										game.log(trigger.card, '不能被响应');
									}
									if (num > 3) {
										player.draw();
									}
								} else {
									player.line(trigger.player, 'fire'); //指示线
									player.addMark('lqtqj_shashen');
									trigger.player.storage.lqtqj_shashen2 = true;
								}
							},
						},
						//使命技,①游戏开始时,你选择一名其他角色,系统展示武将名称最后一个字与该角色的武将名称最后一个字相同的所有武将牌,你选择其中一张武将牌切换或变为你的副将,你变为主将.该角色随机记录5张牌名,当其使用已记录的牌后,其移去记录的牌名,令你摸X张牌(X为那张牌的名称字数).<br><b>成功:</b>因〖交易〗而记录了牌名的角色移去了所有记录的牌名时/击杀一名角色时,你获得一枚<杀神>标记/该角色移去所有记录的牌名,你获得与那些牌名相同牌名的牌各一张.该角色增加一点体力上限,将体力回复至体力上限并获得技能〖英姿〗和〖马术〗.<br><b>失败:</b>因〖交易〗而记录了牌名的角色死亡时,其移去所有记录的牌名,你获得与那些牌名相同牌名的牌各一张
						lqtq_jiaoyi: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								global: ['phaseBefore', 'useCardEnd'],
								player: 'enterGame',
							},
							derivation: ['reyingzi', 'mashu'],
							intro: {
								content: '已记录牌名:$',
							},
							forced: true,
							dutySkill: true,
							filter(event, player, name) {
								if (name == 'useCardEnd') {
									return event.player.getStorage('lqtq_jiaoyi').includes(event.card.name);
								} else {
									return event.name != 'phase' || game.phaseNumber == 0;
								}
							},
							async content(event, trigger, player) {
								//QQQ
								if (trigger.name == 'useCard') {
									trigger.player.unmarkAuto('lqtq_jiaoyi', [trigger.card.name]);
									game.log(trigger.player, '移除了', '#y' + get.translation(trigger.card.name));
									player.draw(get.translation(trigger.card.name).length);
									if (!trigger.player.storage.lqtq_jiaoyi || !trigger.player.storage.lqtq_jiaoyi.length) {
										player.storage.lqtq_jiaoyi3 = trigger.player;
									} //QQQ
									event.finish();
								} else {
									const result = await player
										.chooseTarget(true, '将一名角色变为你的副将')
										.set('ai', (target) => get.attitude(player, target))
										.forResult();
									if (result.targets?.length) {
										const target = result.targets[0],
											chaii = [],
											a = get.translation(target.name),
											aa = a[a.length - 1];
										target.storage.lqtq_jiaoyi = lib.inpile.randomGets(5);
										for (const i in lib.character) {
											const c = lib.translate[i];
											if (c) {
												if (c[c.length - 1] == aa) {
													chaii.push(i);
												}
											} else {
												alert('没有翻译' + i);
											}
										}
										if (chaii.length) {
											const result1 = await player
												.chooseButton(true)
												.set('ai', (button) => lib.character[button.link][2])
												.set('createDialog', ['获得一张武将牌的所有技能', [chaii, 'character']])
												.forResult();
											if (result1.links && result1.links[0]) {
												player.name2 = result1.links[0];
												player.classList.add('fullskin2');
												player.node.avatar2.classList.remove('hidden');
												player.node.avatar2.setBackground(result1.links[0], 'character');
												player.node.name2.innerHTML = get.slimName(result1.links[0]);
												if (player == game.me && ui.fakeme) {
													ui.fakeme.style.backgroundImage = player.node.avatar.style.backgroundImage;
												}
												player.addSkills(lib.character[result1.links[0]][3]);
												game.log(player, '将副将切换为', '#g' + get.translation(result1.links[0]));
											}
										}
									}
								}
							},
							group: ['lqtq_jiaoyi_achieve', 'lqtq_jiaoyi_fail'],
							subSkill: {
								achieve: {
									audio: 'lqtq_jiaoyi',
									trigger: {
										global: 'die',
										player: 'lqtq_jiaoyiEnd',
									},
									forced: true,
									filter(event, player, name) {
										if (name == 'die') {
											return event.source && event.source.getStorage('lqtq_jiaoyi').length;
										}
										return player.storage.lqtq_jiaoyi3;
									},
									content() {
										'step 0';
										game.log(player, '使命成功');
										player.awakenSkill('lqtq_jiaoyi');
										if (trigger.name == 'die') {
											const list = [],
												list1 = [];
											get.cardPile(function (card) {
												if (trigger.source.getStorage('lqtq_jiaoyi').includes(card.name) && !list1.includes(card.name)) {
													list1.add(card.name);
													list.push(card);
												}
											});
											trigger.source.unmarkSkill('lqtq_jiaoyi');
											if (list.length) {
												player.gain(list, 'draw2');
											}
										} else if (player.hasSkill('lqtqj_shashen')) {
											player.addMark('lqtqj_shashen');
										}
										const target = player.storage.lqtq_jiaoyi3 || trigger.source;
										target.gainMaxHp();
										target.hp = target.maxHp;
										target.addSkill('reyingzi');
										target.addSkill('mashu');
										if (player.storage.lqtq_jiaoyi3) {
											delete player.storage.lqtq_jiaoyi3;
										}
									},
								},
								fail: {
									audio: 'lqtq_jiaoyi',
									trigger: {
										global: 'die',
									},
									forced: true,
									filter(event, player) {
										return event.player.getStorage('lqtq_jiaoyi').length;
									},
									content() {
										game.log(player, '使命失败');
										player.awakenSkill('lqtq_jiaoyi');
										const list = [],
											list1 = [];
										get.cardPile(function (card) {
											if (trigger.player.getStorage('lqtq_jiaoyi').includes(card.name) && !list1.includes(card.name)) {
												list1.add(card.name);
												list.push(card);
											}
										});
										trigger.player.unmarkSkill('lqtq_jiaoyi');
										if (list.length) {
											player.gain(list, 'draw2');
										}
									},
								},
							},
						},
						lqtq_basu: {
							trigger: {
								player: 'phaseDrawBegin2',
							},
							forced: true,
							filter(event, player, name) {
								return (
									(!event.numFixed && player.hasMark('lqtqj_shashen')) ||
									game.hasPlayer(function (current) {
										return current.hasSex('female');
									})
								);
							},
							content() {
								'step 0';
								const list = game.filterPlayer(function (current) {
									return current.hasSex('female');
								});
								if (list.length) {
									const target = list.randomGet();
									player.line(target, 'green');
									target.damage();
									player.addMark('lqtqj_shashen');
								}
								('step 1');
								if (player.hasMark('lqtqj_shashen')) {
									trigger.num += player.countMark('lqtqj_shashen');
								}
							},
							audio: 'ext:浪琴天阙/audio:2',
						},
						lqtq_wuyi: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							filter(event, player) {
								return player.countCards('h', { type: 'equip' }) > 0 || player.countCards('e') > 0 || player.countCards('j') > 0;
							},
							content() {
								'step 0';
								((event.a = player.getCards('h')), (event.b = player.getCards('e')), (event.c = player.getCards('j')));
								const list = ['是否发动【武夷】？'];
								if (player.countCards('h')) {
									list.push('将手牌一张装备牌置于一名角色的装备区.');
									list.push(player.getCards('h'));
								}
								if (player.countCards('e')) {
									list.push('将装备区一张牌当做【闪电】置于一名角色的判定区.');
									list.push(player.getCards('e'));
								}
								if (player.countCards('j')) {
									list.push('弃置你判定区一张牌并摸一张牌.');
									list.push(player.getCards('j'));
								}
								const u = player.chooseButton(list);
								u.set('ai', function (button) {
									if (event.c.includes(button.link)) {
										return 100;
									}
									if (event.b.includes(button.link)) {
										return 100 - get.value(button.link);
									}
									return 10 - get.value(button.link);
								});
								u.set('filterButton', function (button) {
									if (event.a.includes(button.link)) {
										return (
											get.type(button.link) == 'equip' &&
											game.hasPlayer(function (current) {
												return !current.isDisabled(get.subtype(button.link));
											})
										);
									}
									if (event.b.includes(button.link)) {
										return game.hasPlayer(function (current) {
											return !current.storage._disableJudge && !current.hasJudge('shandian');
										});
									}
									return true;
								});
								('step 1');
								if (result.links?.length) {
									event.link = result.links[0];
									let a = '将' + get.translation(event.link) + '置入一名角色的装备区.';
									if (event.b.includes(event.link)) {
										a = '将' + get.translation(event.link) + '当做【闪电】置入一名角色的判定区.';
									}
									if (event.c.includes(event.link)) {
										player.discard(event.link);
										player.draw();
									} else {
										player
											.chooseTarget(true, a, function (card, player, target) {
												if (event.a.includes(event.link)) {
													return !target.isDisabled(get.subtype(event.link));
												}
												if (event.b.includes(event.link)) {
													return !target.storage._disableJudge && !target.hasJudge('shandian');
												}
												return false;
											})
											.set('ai', function (target) {
												if (target == player) {
													return 100;
												}
												return get.attitude(_status.event.player, target);
											});
									}
								} else {
									event.finish();
								}
								('step 2');
								if (event.a.includes(event.link)) {
									result.targets[0].equip(event.link);
									player.chooseUseTarget('视为使用一张【火攻】', { name: 'huogong' }, true);
								} else if (event.b.includes(event.link)) {
									player.useCard({ name: 'shandian' }, result.targets[0], [event.link]).animate = false;
									player.chooseUseTarget('视为使用一张【决斗】', { name: 'juedou' }, true);
								} else {
									player.chooseUseTarget('视为使用一张【水淹七军】', { name: 'shuiyanqijunx' }, true);
								}
							},
							group: 'lqtq_wuyi_damage',
							subSkill: {
								damage: {
									trigger: {
										source: 'damageBegin2',
									},
									forced: true,
									filter(event, player) {
										return event.card;
									},
									usable: 1,
									content() {
										'step 0';
										((event.d = Math.max(1, player.maxHp - player.hp)), (event.e = Math.min(player.maxHp, 4)));
										player
											.chooseControl('选项一', '选项二', '选项三', '背水!', '破釜!', '血战!', 'cancel2')
											.set('choiceList', ['伤害加' + get.translation(event.d) + '.', '获得' + get.translation(trigger.player) + '的一个技能.', '你摸' + get.translation(event.e) + '张牌,并可以弃置' + get.translation(trigger.player) + get.translation(event.d) + '张牌.', '背水!依次执行①②两项,你失去一点体力.', '破釜!依次执行②③两项,你减少一点体力上限.', '血战!依次执行①②③三项,你翻面.'])
											.set('prompt', '【武夷】')
											.set('ai', function () {
												const player = _status.event.player,
													a = player.maxHp,
													b = player.hp,
													c = player.maxHp - player.hp;
												if (a > 3) {
													return '血战!';
												}
												if (b > 1) {
													return '背水!';
												}
												if (c > 1) {
													return '破釜!';
												}
												return '选项一';
											});
										('step 1');
										event.count = result.control;
										if (result.control == 'cancel2') {
											player.getStat('triggerSkill').lqtq_wuyi_damage--;
										}
										if (['选项一', '背水!', '血战!'].includes(event.count)) {
											trigger.num += event.d;
											game.log(trigger.player, '受到的伤害加', event.d);
										}
										if (['选项二', '背水!', '破釜!', '血战!'].includes(event.count)) {
											let list = trigger.player.getSkills(true, false).filter(function (skill) {
												const info = get.info(skill);
												return info && !info.hiddenSkill && !info.zhuSkill && !info.charlotte && lib.translate[skill + '_info'] && !player.hasSkill(skill);
											});
											if (!list.length) {
												list = trigger.player.getStockSkills().filter(function (skill) {
													const info = get.info(skill);
													return info && !info.hiddenSkill && !info.zhuSkill && !info.charlotte && lib.translate[skill + '_info'] && !player.hasSkill(skill);
												});
											}
											if (!list.length) {
												event.goto(3);
											} else if (list.length == 1) {
												event._result = { control: list[0] };
											} else {
												player
													.chooseControl(list)
													.set('prompt', '选择一个技能获得')
													.set('forceDie', true)
													.set('ai', function () {
														return list.randomGet();
													});
											}
										} else {
											event.goto(3);
										}
										('step 2');
										player.addTempSkill(result.control, { player: 'phaseAfter' });
										player.popup(result.control);
										game.log(player, '获得了技能', '#g【', '#g' + result.control, '#g】');
										('step 3');
										if (['选项三', '破釜!', '血战!'].includes(event.count)) {
											player.draw(event.e);
											if (trigger.player.countCards('he')) {
												player.discardPlayerCard([1, event.d], trigger.player, 'he');
											}
										}
										('step 4');
										if (event.count == '背水!') {
											player.loseHp();
										}
										if (event.count == '破釜!') {
											player.loseMaxHp();
										}
										if (event.count == '血战!') {
											player.turnOver();
										}
									},
								},
							},
							audio: 'ext:浪琴天阙/audio:3',
						},
						lqtq_yinpo: {
							mod: {
								maxHandcard(player, num) {
									return (
										num +
										player.hujia +
										player.getCards('h', function (card) {
											return card.hasGaintag('lqtq_yinpo');
										}).length
									);
								},
								aiOrder(player, card, num) {
									if (get.itemtype(card) == 'card' && card.hasGaintag('lqtq_yinpo')) {
										return 0;
									}
								},
							},
							trigger: {
								player: ['loseHpEnd', 'loseMaxHpEnd', 'phaseBegin', 'turnOverEnd'],
							},
							forced: true,
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player, name) {
								if (name == 'turnOverEnd') {
									return event.getParent(1).name != 'lqtq_wuyi_damage';
								}
								if (name == 'phaseBegin') {
									return (
										player.hujia > 0 ||
										player.countCards('h', function (card) {
											return card.hasGaintag('lqtq_yinpo');
										})
									);
								}
								return true;
							},
							content() {
								const a = event.triggername,
									c = player.hujia,
									b = player.getCards('h', function (card) {
										return card.hasGaintag('lqtq_yinpo');
									});
								if (a == 'loseHpEnd') {
									player.changeHujia(2);
								}
								if (a == 'loseMaxHpEnd') {
									player.draw(2).gaintag = ['lqtq_yinpo'];
								}
								if (a == 'phaseBegin') {
									if (b.length) {
										player.discard(b);
										player.gainMaxHp(b.length);
									}
									if (c) {
										player.changeHujia(-player.hujia);
										player.recover(c);
									}
								}
								if (a == 'turnOverEnd') {
									const next = player.phaseZhunbei();
									event.next.remove(next);
									trigger.parent.next.push(next);
								}
							},
						},
						lqtq_xingchen: {
							audio: 'ext:浪琴天阙/audio:2',
							enable: 'phaseUse',
							usable: 1,
							content() {
								'step 0';
								event.card = [];
								event.count = 4;
								('step 1');
								let a = [],
									b = [],
									c = [],
									d = [],
									e = [],
									f = [],
									g = [],
									h = ['sha', 'sha', 'sha', 'shan', 'shan', 'tao', 'jiu'].randomGet(),
									k = [],
									l = [];
								d.push(get.translation(h));
								for (const i of ['杀', '闪', '桃', '酒']) {
									if (i != h) {
										k.push(i);
									}
								}
								for (const i of lib.inpile) {
									if (typeof lib.translate[i] == 'string') {
										for (const j of lib.translate[i]) {
											if (k.includes(j)) {
												l.add(i);
											}
										}
										if (lib.translate[i].length == 4 && !l.includes(i)) {
											a.add(i);
										}
										if (get.type2(i) != 'basic' && lib.translate[i].length != 4 && !l.includes(i)) {
											b.add(i);
										}
									}
								}
								a = a.randomGet();
								b = b.randomGet();
								for (const i of get.translation(a)) {
									if (!d.includes(i)) {
										d.push(i);
										e.push(i);
									}
								}
								for (const i of get.translation(b)) {
									if (!d.includes(i)) {
										d.push(i);
										f.push(i);
									}
								}
								dialog.add('<b><font color=#F08080>按卡牌名称顺序选择文字组合成卡牌(提示:</font><font color=#eeee00>' + e.randomGet() + '</font><font color=#F08080>/</font><font color=#eeee00>' + f.randomGet() + '</font><font color=#F08080>)</font>');
								for (const i of lib.inpile) {
									if (get.type(i) != 'basic') {
										g.push(lib.translate[i]);
									}
								}
								for (const l of g) {
									if (l) {
										for (const j of l) {
											if (!d.includes(j)) {
												c.add(j);
											}
										}
									}
								}
								c = c.randomGets(50 - d.length);

								for (const i of c) {
									d.push(i);
								}

								d.randomSort(); //打乱
								event.list = [];
							},
						},

						lqtq_xuelin: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								target: 'useCardToTarget',
								player: 'damageEnd',
							},
							forced: true, //自动
							filter(event, player, name) {
								if (name == 'useCardToTarget') {
									return !player.getStorage('lqtq_xuelin').includes(event.card.name);
								}
								if (name == 'damageEnd') {
									return event.num > 0;
								}
							},
							content() {
								if (event.triggername == 'damageEnd') {
									player.draw();
								} else {
									player.markAuto('lqtq_xuelin', [trigger.card.name]);
								}
							},
							intro: { content: '已记录牌名:$' },
							group: 'lqtq_xuelin_add',
							subSkill: {
								add: {
									trigger: {
										player: 'gainAfter',
									},
									forced: true,
									usable: 1,
									filter(event, player) {
										const list = player.getStorage('lqtq_xuelin');
										if (Array.isArray(event.cards)) {
											for (const i of event.cards) {
												//QQQ
												if (list.length && list.includes(i.name)) {
													return true;
												}
											}
										}
										return false;
									},
									content() {
										'step 0';
										const list = [],
											list1 = [],
											list2 = player.getStorage('lqtq_xuelin');
										if (Array.isArray(trigger.cards)) {
											for (const i of trigger.cards) {
												if (list2.includes(i.name)) {
													list.push(i);
												}
											}
										}
										player.chooseCardTarget({
											position: 'h',
											filterTarget(card, player, target) {
												return player != target;
											},
											selectCard: 1,
											filterCard(card, player, event) {
												return list.includes(card);
											},
											ai1(card) {
												return 20 - get.useful(card);
											},
											ai2(target) {
												return get.attitude(_status.event.player, target);
											},
											prompt: '【血麟】',
											prompt2: '将一张获得的且已记录的牌交给一名其他角色',
										});
										('step 1');
										if (result.cards?.length) {
											const name = result.cards[0].name;
											if (player.getStorage('lqtq_xuelin').includes(name)) {
												player.unmarkAuto('lqtq_xuelin', [name]);
												game.log(player, '从', '#g【血麟】', '记录中移除了', '#y' + get.translation(name));
											}
											result.targets[0].gain(result.cards[0], 'gain2');
											player.recover();
											player.draw(2);
										} else {
											player.getStat('triggerSkill').lqtq_xuelin_add--;
										}
									},
								},
							},
						},
						lqtq_xiutian: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								player: 'phaseDiscardEnd',
								global: ['recoverEnd', 'loseHpEnd'],
							},
							forced: true,
							notemp: true,
							filter(event, player) {
								if (event.name == 'loseHp' || event.name == 'recover') {
									return event.num > 0;
								}
								if (!event.cards) {
									return false;
								}
								const list = [];
								for (const l of player.getExpansions('lqtq_xiutian')) {
									list.add(get.type2(l));
								} //var
								if (!list.length) {
									return true;
								}
								if (Array.isArray(event.cards)) {
									for (const i of event.cards) {
										//QQQ
										if (list.includes(get.type2(i))) {
											return false;
										}
									}
								}
								return true;
							},
							content() {
								'step 0';
								event.num = 2;
								event.cards = get.cards(3);
								game.cardsGotoOrdering(cards);
								('step 1');
								const prompt = '帝摄:将两张牌置于牌堆顶,并将一张牌置于你的武将牌上';
								const next = player.chooseToMove(prompt, true);
								next.set('list', [['按任意循序置于牌堆顶', cards], ['置于你的武将牌上']]);
								next.set('filterMove', function (from, to, moved) {
									if (to == 1 && moved[0].length <= _status.event.num) {
										return false;
									}
									return true;
								});
								next.set('filterOk', function (moved) {
									return moved[0].length == _status.event.num;
								});
								next.set('num', num);
								next.set('processAI', function (list) {
									const check = function (card) {
										const player = _status.event.player;
										const next = player.next;
										const att = get.attitude(player, next);
										const judge = next.getCards('j')[tops.length];
										if (judge) {
											return get.judge(judge)(card) * att;
										}
										return next.getUseValue(card) * att;
									};
									var cards = list[0][1].slice(0),
										tops = [];
									while (tops.length < _status.event.num) {
										cards.sort((a, b) => check(b) - check(a)); //QQQ
										tops.push(cards.shift());
									}
									return [tops, cards];
								});
								('step 2');
								if (result.bool) {
									const list = result.moved[0];
									var num = list.length - 1;
									for (let i = 0; i < list.length; i++) {
										event.cards.remove(list[num - i]);
										ui.cardPile.insertBefore(list[num - i], ui.cardPile.firstChild);
									}
								}
								('step 3');
								game.updateRoundNumber();
								if (event.cards.length) {
									player.addToExpansion(event.cards, player, 'giveAuto').gaintag.add('lqtq_xiutian');
								}
							},
							intro: {
								content: 'expansion',
								markcount: 'expansion',
							},
							onremove(player, skill) {
								const cards = player.getExpansions(skill);
								if (cards.length) {
									player.loseToDiscardpile(cards);
								}
							},
							mod: {
								cardUsable(card, player, num) {
									if (card.name == 'sha') {
										return num + player.getExpansions('lqtq_xiutian').length;
									}
								},
							},
						},
						lqtq_dilin: {
							audio: 'ext:浪琴天阙/audio:2',
							juexingji: true,
							trigger: { player: 'phaseZhunbeiBegin' },
							forced: true,
							filter(event, player) {
								return player.getExpansions('lqtq_xiutian').length > 2;
							},
							content() {
								player.loseMaxHp();
								player.chooseDrawRecover(2, true, function (event, player) {
									if (player.hp == 1 && player.isDamaged()) {
										return 'recover_hp';
									}
									return 'draw_card';
								});
								player.addSkill('lqtq_dishe');
								player.awakenSkill('lqtq_dilin');
							},
							derivation: 'lqtq_dishe',
						},
						lqtq_dishe: {
							enable: 'phaseUse',
							usable: 1,
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player) {
								return player.getExpansions('lqtq_xiutian').length;
							},
							chooseButton: {
								dialog(event, player) {
									return ui.create.dialog('帝摄', player.getExpansions('lqtq_xiutian'), 'hidden');
								},
								backup(links, player) {
									return {
										audio: 'lqtq_dishe',
										filterTarget(card, player, target) {
											return player != target && target.countCards('h');
										},
										filterCard() {
											return false;
										},
										selectCard: -1,
										card: links[0],
										delay: false,
										content: lib.skill.lqtq_dishe.contentx,
										ai: {
											order: 20,
											result: {
												target: -1,
											},
										},
									};
								},
								prompt() {
									return '获得一名其他角色所有手牌';
								},
							},
							contentx() {
								const card = lib.skill.lqtq_dishe_backup.card;
								player.loseToDiscardpile(card);
								player.gain(target.getCards('h'), target, 'giveAuto');
							},
							ai: {
								order: 20,
								combo: 'lqtq_xiutian',
								result: {
									player: 1,
								},
							},
						},
						lqtq_mieyan: {
							enable: 'phaseUse',
							usable: 1,
							filterTarget(card, player, target) {
								return target.countCards('h') && target != player;
							},
							content() {
								'step 0';
								player.discardPlayerCard(target, true, 'h');
								('step 1');
								if (result.cards?.length) {
									target.damage(result.cards[0].number);
								}
								('step 2');
								if (!target.isDead()) {
									player.draw(2);
								}
							},
							ai: {
								order: 30,
								result: {
									target: -2,
								},
							},
						},
						//一名其他角色发动技能或因技能而使用牌时,你可以取消之并摸一张牌
						lqtq_quan: {
							trigger: {
								global: ['logSkillBegin'],
							},
							popup: false,
							filter(event, player) {
								return event.player != player;
							},
							check: (event, player) => event.player.isEnemiesOf(player),
							prompt(event, player) {
								return `终止${get.translation(event.skill)}的发动`;
							},
							async content(event, trigger, player) {
								player.draw();
								const name = trigger.skill;
								const info = lib.skill[name];
								const arr = trigger.parent.next;
								for (let i = arr.length - 1; i >= 0; i--) {
									if (arr[i].name === name) {
										arr.splice(i, 1);
									}
								}
								game.log(player, `终止${get.translation(name)}的发动`);
								if (info.limited || info.juexingji) {
									trigger.player.awakenSkill(name);
								}
							},
							group: 'lqtq_quan_card',
							subSkill: {
								card: {
									trigger: {
										global: 'useCard',
									},
									filter(event, player) {
										if (event.player == player) {
											return false;
										}
										for (const j in lib.skill) {
											if (event.skill == j + '_backup' || event.skill == j || event.getParent(j).name == j) {
												return true;
											}
										} //QQQ
										return false;
									},
									check(event, player) {
										return get.attitude(player, event.player) < 0;
									},
									logTarget(event, player) {
										return event.player;
									},
									prompt2(event) {
										return '令' + get.translation(event.player) + '使用的' + get.translation(event.card) + '无效.';
									},
									content() {
										trigger.all_excluded = true;
										trigger.targets.length = 0;
										player.draw();
									},
								},
							},
						},
						lqtq_dudan: {
							enable: 'phaseUse',
							audio: 'ext:浪琴天阙/audio:1',
							filter(event, player) {
								return player.countCards('hej') > 0;
							},
							content() {
								'step 0';
								player
									.chooseControl('正面', '反面')
									.set('prompt', '赌胆:猜测正反面,并投一次硬币.')
									.set('ai', function () {
										return Math.random();
									});
								('step 1');
								event.a = result.control;
								game.throwCoin();
								('step 2');
								if ((result.bool && event.a == '正面') || (!result.bool && event.a == '反面')) {
									player.draw(player.countCards('hej'));
								} else {
									player.discard(player.getCards('hej'));
								}
							},
							ai: {
								order: 0.1,
								result: {
									player: 1,
								},
							},
						},
						lqtq_nifan: {
							trigger: {
								target: 'useCardToTargeted',
							},
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player) {
								return event.player.countCards('h') > player.countCards('h');
							},
							check(event, player) {
								return get.attitude(player, event.player) < 0;
							},
							logTarget(event, player) {
								return event.player;
							},
							content() {
								'step 0';
								game.throwCoin(false);
								('step 1');
								if (result.bool) {
									player.draw(trigger.player.countCards('h'));
								} else {
									trigger.player.damage(trigger.player.countCards('h'));
								}
							},
						},
						lqtq_xiaoge: {
							mod: {
								aiOrder(player, card) {
									if (player.storage.lqtq_xiaoge) {
										const a = [];
										for (const i of player.storage.lqtq_xiaoge) {
											a.add(get.type2(i));
											a.add(i.suit);
										}
										if (!a.includes(card.suit) && !a.includes(get.type2(card))) {
											return 20;
										}
										if (!a.includes(get.type2(card))) {
											return 19;
										}
										if (!a.includes(card.suit)) {
											return 19;
										}
									}
								},
							},
							audio: 'ext:浪琴天阙/audio:2',
							group: 'lqtq_xiaoge_count',
							trigger: {
								player: ['useCard', 'respond'],
							},
							intro: {
								mark(dialog, content, player) {
									let a = player.storage.lqtq_xiaoge,
										b = [a[0], a[1]],
										c = '上上张牌/上一张牌';
									if (a.length == 1) {
										((b = [a[0]]), (c = '上一张牌'));
									}
									dialog.addText(c);
									dialog.add(b);
								},
								markcount(storage, player) {
									let a = '<font size=2><font color=#ffff33><b>',
										b = player.storage.lqtq_xiaoge,
										c = b[0],
										d = b[1];
									if (b.length == 1) {
										if (lib.suit.includes(c.suit)) {
											a += get.translation(c.suit);
										}
										a += '&nbsp;' + get.translation(get.type2(c))[0];
									} else {
										if (lib.suit.includes(c.suit)) {
											a += get.translation(c.suit);
										}
										if (lib.suit.includes(d.suit)) {
											a += '&nbsp;' + get.translation(d.suit);
										}
										a += '&nbsp;' + get.translation(get.type2(c))[0];
										a += '&nbsp;' + get.translation(get.type2(d))[0];
									}
									return a;
								},
							},
							forced: true, //自动
							filter(event, player) {
								const a = player.storage.lqtq_xiaoge;
								if (!a || a.length < 2) {
									return false;
								} else {
									const b = event.card.suit || '',
										c = get.type2(event.card),
										d = a[0].suit || '',
										e = a[1].suit || '',
										f = get.type2(a[0]),
										g = get.type2(a[1]);
									return (b != d && b != e && d != e) || (c != f && c != g && f != g);
								}
								return false;
							},
							content() {
								let a = player.storage.lqtq_xiaoge,
									b = trigger.card.suit || '',
									c = get.type2(trigger.card),
									dd = 0,
									d = a[0].suit || '',
									e = a[1].suit || '',
									f = get.type2(a[0]),
									g = get.type2(a[1]);
								if (b != d && b != e && d != e) {
									dd++;
								}
								if (c != f && c != g && f != g) {
									dd++;
								}
								player.draw(dd);
							},
						},
						lqtq_xiaoge_count: {
							trigger: {
								player: ['useCard', 'respond'],
							},
							forced: true,
							popup: false,
							_priority: -1,
							content() {
								if (!player.storage.lqtq_xiaoge) {
									player.storage.lqtq_xiaoge = [];
								}
								const a = game.createCard(trigger.card.name, trigger.card.suit || '', trigger.card.number || '');
								player.storage.lqtq_xiaoge.push(a);
								if (player.storage.lqtq_xiaoge.length > 2) {
									player.storage.lqtq_xiaoge.remove(player.storage.lqtq_xiaoge[0]);
								}
								player.markSkill('lqtq_xiaoge');
							},
						},
						lqtq_yuanshen: {
							enable: 'phaseUse',
							audio: 'ext:浪琴天阙/audio:2',
							filterCard(card, player) {
								const a = [];
								player.getCards('e', function (card1) {
									if (!player.getStorage('lqtq_yuanshen').includes(get.subtype(card1))) {
										a.push(card1);
									}
								});
								if (a.length == 1 && !ui.selected.cards.length) {
									return !a.includes(card);
								}
								return true;
							},
							selectCard() {
								const player = _status.event.player,
									a = [];
								if (
									player.countCards('e', function (card) {
										return !player.getStorage('lqtq_yuanshen').includes(get.subtype(card));
									})
								) {
									return [1, 3];
								}
								return [2, 3];
							},
							check(card, player, event) {
								var player = _status.event.player;
								if (player.storage.lqtq_xiaoge) {
									const a = [];
									for (const i of player.storage.lqtq_xiaoge) {
										a.add(get.type2(i));
										a.add(i.suit);
									}
									if (
										player.countCards('e', function (card1) {
											return !player.getStorage('lqtq_yuanshen').includes(get.subtype(card1)) && (!a.includes(card1.suit) || !a.includes(get.type2(card1)));
										}) &&
										ui.selected.cards.length
									) {
										return false;
									}
								}
								if (player.countCards('h') < 5) {
									return false;
								}
								if (ui.selected.cards.length == 1 && get.type2(card) != get.type2(ui.selected.cards[0]) && get.type2(ui.selected.cards[0]) != 'basic') {
									return 30 - get.value(card);
								}
								if (ui.selected.cards.length == 2 && get.type2(ui.selected.cards[1]) != get.type2(ui.selected.cards[0])) {
									return false;
								}
								return 10 - get.value(card);
							},
							complexCard: true,
							filter(event, player) {
								let a = 0;
								if (
									!player.countCards('e', function (card) {
										return !player.getStorage('lqtq_yuanshen').includes(get.subtype(card));
									})
								) {
									a++;
								}
								return player.countCards('he') > a;
							},
							intro: {
								content: '已获得过的副类别:$',
							},
							position: 'he',
							content() {
								'step 0';
								if (cards.length == 2) {
									const c = [];
									for (const i of cards) {
										c.push(i.suit);
										c.push(get.type2(i));
									}
									const g = get.cardPile(function (card) {
										return !c.includes(card.suit) && !c.includes(get.type2(card));
									});
									if (g) {
										player.gain(g, 'gain2');
									}
								} else if (cards.length == 3) {
									player.draw();
								} else if (
									player.countCards('e', function (card) {
										return !player.getStorage('lqtq_yuanshen').includes(get.subtype(card));
									})
								) {
									const u = player.chooseButton(true, ['获得一张装备牌', player.getCards('e')]);
									u.ai = function (button) {
										if (player.storage.lqtq_xiaoge) {
											const a = [];
											for (const i of player.storage.lqtq_xiaoge) {
												a.add(get.type2(i));
												a.add(i.suit);
											}
											if (!a.includes(button.link.suit) && !a.includes(get.type2(button.link))) {
												return 20;
											}
											if (!a.includes(get.type2(button.link))) {
												return 19;
											}
											if (!a.includes(button.link.suit)) {
												return 19;
											}
										}
										return 1;
									};
									u.set('filterButton', function (button) {
										const player = _status.event.player;
										return !player.getStorage('lqtq_yuanshen').includes(get.subtype(button.link));
									});
								}
								('step 1');
								if (result.bool && cards.length == 1) {
									const a = result.links[0];
									player.gain(a, 'gain2');
									player.markAuto('lqtq_yuanshen', [get.subtype(a)]);
									player.addTempSkill('lqtq_yuanshen_count');
								} else if (
									Array.isArray(result) &&
									result.length &&
									cards.length == 3 &&
									((lib.filter.cardUsable({ name: 'sha' }, player) && result[0].name == 'sha') ||
										!game.hasPlayer(function (current) {
											return player.canUse(result[0].name, current);
										}))
								) {
									player.draw();
								}
							},
							ai: {
								order: 1,
								result: {
									player: 1,
								},
							},
						},
						lqtq_yuanshen_count: {
							onremove(player) {
								player.unmarkSkill('lqtq_yuanshen');
							},
							forced: true,
							charlotte: true, //夏洛特
						},
						//每回合对每个技能各限一次,其他角色发动技能时,该角色获得一枚对应该技能的标记,若该技能不存在标记,则你创建一个该技能的标记令该角色获得.
						//②你成为本回合未成为过与该牌类型相同的牌的目标时,你获得一枚<淫趴>标记,若该角色所有技能的标记数合计大于你所有技能的标记数合计,你摸一张牌
						lqtq_yinpa: {
							trigger: {
								global: 'logSkillBegin', //QQQ
								target: 'useCardToTargeted',
							},
							silent: true,
							logTarget: 'player',
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player, name) {
								if (name == 'useCardToTargeted') {
									return !player.getStorage('lqtq_yinpa_count').includes(get.type(event.card));
								} else {
									if (event.getParent('lqtq_yinpa').name == 'lqtq_yinpa') {
										return false;
									}
									const skills = event.player.getSkills(true, false);
									game.expandSkills(skills);
									return skills.includes(event.skill) && event.player != player; //QQQ
								}
								return false;
							},
							content() {
								if (!player.storage.lqtq_yinpa_count) {
									player.storage.lqtq_yinpa_count = [];
								}
								if (trigger.name == 'logSkill') {
									trigger.player.addMark(trigger.skill);
								} else {
									player.storage.lqtq_yinpa_count.push(get.type(trigger.card));
									player.addMark('lqtq_yinpa');
									let a = 0,
										b = 0,
										c = trigger.player,
										d = player;
									for (const i in lib.skill) {
										if (c.countMark(i) > 0 && lib.skill[i].intro) {
											a += c.countMark(i);
										}
										if (d.countMark(i) > 0 && lib.skill[i].intro) {
											b += d.countMark(i);
										}
									}
									if (a > b) {
										d.draw();
									}
								}
							},
							intro: {
								content: 'mark',
							},
						},
						lqtq_yinpa_count: {
							charlotte: true,
						},
						lqtq_yuzu: {
							audio: 'ext:浪琴天阙/audio:2',
							group: 'lqtq_yuzu_count',
							enable: ['chooseToUse', 'chooseToRespond'],
							filter(event, player) {
								let a = 0,
									b = player.getStat('skill').lqtq_yuzu_backup || 0;
								game.countPlayer(function (current) {
									for (const i in lib.skill) {
										if (current.countMark(i) > 0 && lib.skill[i].intro) {
											a += current.countMark(i);
										}
									}
								});
								for (const i of lib.inpile) {
									if (['equip', 'trick', 'basic'].includes(get.type2(i)) && event.filterCard && event.filterCard({ name: i }, player, event) && a > b) {
										return true;
									}
								}
								return false;
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (name == 'sha') {
											if (event.filterCard && event.filterCard({ name: name }, player, event)) {
												list.push(['基本', '', 'sha']);
											}
											for (const j of lib.inpile_nature) {
												if (event.filterCard && event.filterCard({ name: name, nature: j }, player, event)) {
													list.push(['基本', '', 'sha', j]);
												}
											}
										} else if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['基本', '', name]);
										} else if (get.type2(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										} else if (get.type(name) == 'equip' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['装备', '', name]);
										}
									}

									return ui.create.dialog('玉足', [list, 'vcard']);
								},
								check(button) {
									return _status.event.player.getUseValue({ name: button.link[2] });
								},
								backup(links, player) {
									return {
										viewAs: {
											name: links[0][2],
										},
										filterCard: () => false,
										card: links[0],
										selectCard: -1,
										popname: true,
										onuse(result, player) {
											if (['delay', 'equip'].includes(get.type(result.card))) {
												player.storage.lqtq_yuzu_card = result.card.name;
											}
										},
										precontent() {
											'step 0';
											var a = [],
												b = [],
												e = [],
												bbb = player.getStat('skill').lqtq_yuzu_backup + 1 || 1;
											game.countPlayer(function (current) {
												for (const i in lib.skill) {
													if (current.countMark(i) > 0 && lib.skill[i].intro && !b.includes(current)) {
														for (let r = 0; r < Math.min(current.countMark(i), bbb); r++) {
															a.push([current, i]);
															b.push(i);
														}
													}
												}
											});
											for (const i of a) {
												var c = i[0],
													d = i[1],
													f = 'lqtq_' + c.name + '_' + d;
												lib.card[f] = {
													fullimage: true,
													image: 'character:' + c.name,
												};
												if (lib.skill[d].intro && lib.skill[d].intro.name) {
													lib.translate[f] = lib.skill[d].intro.name;
												} else {
													lib.translate[f] = get.translation(d);
												}
												lib.translate[f + '_info'] = '标记持有者:' + get.translation(c.name);
												e.push([c, '', f]);
											}
											player.chooseButton(['移去场上合计' + bbb + '枚任意标记,相同的标记选择多枚代表要移去此标记的数量.', [e, 'vcard']], true, bbb).set('ai', function (button) {
												//QQQ
												for (const i of e) {
													if (get.attitude(_status.event.player, i[0]) < 0 && button.link == i) {
														return 20;
													}
												}
												return Math.random();
											});
											('step 1');
											for (const i of result.links) {
												i[0].removeMark(i[2].slice(i[0].name.length + 6));
											}
											var a = [],
												b = [];
											for (const i of game.players) {
												if (i == player) {
													continue;
												}
												for (const j of i.getSkills(true, false)) {
													var info = get.info(j);
													if (info && !info.charlotte && j.indexOf('rewrite') == -1 && lib.translate[j] && lib.translate[j + '_info'] && !b.includes(j)) {
														var f = 'lqtq_skill_' + j;
														lib.card[f] = {
															fullimage: true,
															image: 'character:' + i.name,
														};
														lib.translate[f] = lib.translate[j];
														lib.translate[f + '_info'] = lib.translate[j + '_info'];
														a.push([i, '', f]);
														b.push(j);
													}
												}
											}
											if (a.length) {
												var c = a.length < 2 ? 1 : 2;
												player.chooseButton(['选择以下' + c + '个技能展示其的描述', [a, 'vcard']], true, c).set('ai', function () {
													return Math.random();
												});
											} else {
												player.say('场上没有角色有合适的技能啊宝');
												event.finish();
											}
											('step 2');
											((event.bb = []), (event.cc = ''), (event.dd = ''));
											for (const i of get.skillInfoTranslation(result.links[0][2].slice(11))) {
												event.bb.push(i);
											}
											if (result.links[1]) {
												for (const i of get.skillInfoTranslation(result.links[1][2].slice(11))) {
													event.bb.push(i);
												}
											}
											var a = [];
											for (const i of result.links) {
												a.push(i[2].slice(11));
											}
											game.log(player, '选择了', a);
											('step 3');
											var dialog = ui.create.dialog('<b>选择任意个文字与符合组合成一句技能描述', 'hidden');
											if (event.cc != '') {
												dialog.add('条件一(<font color=#eeee00>' + event.cc + '</font>)');
											}
											var yi = document.createElement('div');
											yi.classList.add('add-setting');
											yi.style.margin = '0';
											yi.style.width = '100%';
											yi.style.position = 'relative';

											for (const i of event.bb) {
												var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
												td.innerHTML = '<font size=5><b><font color=#eeee00>' + i + '</b></font>';
												td.link = i;
												td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
												Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
												yi.appendChild(td);
												dialog.buttons.add(td);
											}

											dialog.content.appendChild(yi);
											var chooseButton = player.chooseButton([1, Infinity], dialog, true);
											chooseButton.set('ai', function (button) {
												if (ui.selected.buttons.length) {
													return false;
												}
												if (['摸', '伤', '获', '标'].includes(button.link)) {
													return 20;
												}
												return Math.random();
											});
											('step 4');
											event.ee = [];
											for (const i of result.links) {
												event.cc += i;
											}
											for (const i in lib.character) {
												var d = lib.character[i][4];
												if ((game.me == player && lib.config.extension_浪琴天阙_lqtq_shaiji_kuochong) || !lib.config.forbidai.includes(i)) {
													event.ee.push(i);
												}
											}
											if (!event.ee.length) {
												player.say('没有武将牌,请开启「武将包」或本扩展的「筛技扩充」功能');
												event.finish();
											}
											('step 5');
											event.ff = [];
											for (const i of event.ee) {
												for (const j of lib.character[i][3]) {
													if (j.includes('rewrite') || !lib.translate[j] || !lib.translate[j + '_info']) {
														continue;
													}
													if (
														game.hasPlayer(function (current) {
															return current.hasSkill(j);
														})
													) {
														continue;
													}
													var info = get.info(j);
													var str = lib.translate[j + '_info'];
													for (var l = 0; l < str.length; l++) {
														if (str[l] == event.cc[0] && str.substring(l, l + event.cc.length) == event.cc && info && !info.juexingji && !info.hiddenSkill && !info.zhuSkill && !info.charlotte && !info.dutySkill && !info.unique && !event.ff.includes(j)) {
															event.ff.push(j);
														}
													}
												}
											}
											('step 6');
											if (!event.ff.length) {
												player.say('选的啥啊,啥也不是');
												player.chooseBool('未检测到技能描述符合(' + event.cc + ')的技能,请重新选择.');
												event.cc = '';
												event.goto(3);
											} else {
												player
													.chooseControl('续选条件一', '另选条件二', '不想磨蹭辣')
													.set('choiceList', ['续选条件一:你可以再次选择任意文字符号续在已选择的(' + event.cc + ')后面组成一句新的条件一', '另选条件二:你另外选择任意文字符号组合成一句新的条件二,最后筛选符合的技能时会筛选出描述中同时包含条件一和条件二的技能', '不想磨蹭辣:不再进行条件选择,直接利用目前的条件筛选出技能获得'])
													.set('prompt', '系统目前已经检测出' + event.ff.length + '个符合条件的技能.')
													.set('ai', function () {
														return '不想磨蹭辣';
													});
											}
											('step 7');
											if (result.control == '续选条件一') {
												event.goto(3);
											}
											if (result.control == '不想磨蹭辣') {
												event.gg = event.ff;
												event.goto(15);
											}
											('step 8');
											var dialog = ui.create.dialog('<b>选择任意个文字与符合组合成一句技能描述', 'hidden');
											if (event.cc != '') {
												dialog.add('条件一:(<font color=#eeee00>' + event.cc + '</font>)');
											}
											if (event.dd != '') {
												dialog.add('条件二:(<font color=#eeee00>' + event.dd + '</font>)');
											}
											var yi = document.createElement('div');
											yi.classList.add('add-setting');
											yi.style.margin = '0';
											yi.style.width = '100%';
											yi.style.position = 'relative';

											for (const i of event.bb) {
												var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
												td.innerHTML = '<font size=5><b><font color=#eeee00>' + i + '</b></font>';
												td.link = i;
												td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
												Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
												yi.appendChild(td);
												dialog.buttons.add(td);
											}

											dialog.content.appendChild(yi);
											var chooseButton = player.chooseButton([1, Infinity], dialog, true);
											chooseButton.set('ai', function (button) {
												return Math.random();
											});
											('step 9');
											for (const i of result.links) {
												event.dd += i;
											}
											('step 10');
											event.gg = [];
											for (const i of event.ff) {
												var str = lib.translate[i + '_info'];
												for (var l = 0; l < str.length; l++) {
													if (str[l] == event.dd[0] && str.substring(l, l + event.dd.length) == event.dd && !event.gg.includes(i)) {
														event.gg.push(i);
													}
												}
											}
											('step 11');
											if (!event.gg.length) {
												player.say('选的啥啊,啥也不是');
												player
													.chooseControl('重选条件一', '重选条件二')
													.set('choiceList', ['重选条件一:重新选择条件一和条件二', '重选条件二:条件一保留,重新选择条件二'])
													.set('prompt', '未检测到技能描述同时符合(' + event.cc + ')以及(' + event.dd + ')的技能请重新选择.');
											} else {
												event.goto(13);
											}
											('step 12');
											if (result.control == '重选条件一') {
												event.cc = '';
												event.dd = '';
												event.goto(3);
											} else {
												event.dd = '';
												event.goto(8);
											}
											('step 13');
											player
												.chooseControl('续选条件二', '不想磨蹭辣')
												.set('choiceList', ['续选条件二:你可以再次选择任意文字符号续在已选择的(' + event.dd + ')后面组成一句新的筛选条件二', '不想磨蹭辣:不再进行条件选择,直接利用目前的条件筛选出技能获得'])
												.set('prompt', '系统目前已经检测出' + event.gg.length + '个符合条件的技能.')
												.set('ai', function () {
													return Math.random();
												});
											('step 14');
											if (result.control == '续选条件二') {
												event.goto(8);
											}
											('step 15');
											let z = '';
											if (event.dd) {
												z = '#g「' + event.dd + '」';
											}
											game.log(player, '选择的条件为', '#g「' + event.cc + '」', z);
											event.gg = event.gg.randomGets(4);
											var dialog = ui.create.dialog('<b>选择一个技能获得', 'hidden');
											var yi = document.createElement('div');
											yi.classList.add('add-setting');
											yi.style.margin = '0';
											yi.style.width = '100%';
											yi.style.position = 'relative';

											for (const i of event.gg) {
												var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
												td.innerHTML = '<font size=4>【' + get.translation(i) + '】<br><P align=left>' + get.skillInfoTranslation(i);
												td.link = i;
												td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
												Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
												yi.appendChild(td);
												dialog.buttons.add(td);
											}

											dialog.content.appendChild(yi);
											var chooseButton = player.chooseButton(dialog, true);
											chooseButton.set('ai', function (button) {
												return Math.random();
											});
											('step 16');
											player.addSkillLog(result.links);
										},
									};
								},
								prompt(links, player) {
									return '请选择' + get.translation(links[0][2]) + '的目标';
								},
							},
							hiddenCard(player, name) {
								let a = 0,
									b = player.getStat('skill').lqtq_yuzu_backup || 0;
								game.countPlayer(function (current) {
									for (const i in lib.skill) {
										if (current.countMark(i) > 0 && lib.skill[i].intro) {
											a += current.countMark(i);
										}
									}
								});
								if (b >= a) {
									return false;
								}
								return ['equip', 'trick', 'basic'].includes(get.type2(name));
							},
							ai: {
								fireAttack: true,
								respondSha: true,
								respondShan: true,
								skillTagFilter(player) {
									let a = 0,
										b = player.getStat('skill').lqtq_yuzu_backup || 0;
									game.countPlayer(function (current) {
										for (const i in lib.skill) {
											if (current.countMark(i) > 0 && lib.skill[i].intro) {
												a++;
											}
										}
									});
									if (b > a) {
										return false;
									}
									return true;
								},
								order: 9,
								result: {
									player(player) {
										if (_status.event.dying) {
											return get.attitude(player, _status.event.dying);
										}
										return 1;
									},
								},
							},
							subSkill: {
								count: {
									trigger: {
										player: 'useCardToBegin',
									},
									forced: true,
									_priority: 21,
									popup: false,
									filter(event, player) {
										return event.skill == 'lqtq_yuzu_backup' && player.storage.lqtq_yuzu_card && event.card.name == player.storage.lqtq_yuzu_card;
									},
									content() {
										const card = game.createCard(trigger.card.name, '', '');
										trigger.card = card;
										trigger.cards = [card];
										trigger.card.cards = trigger.cards;
										game.cardsGotoOrdering(card).relatedEvent = trigger;
										delete player.storage.lqtq_yuzu_card;
									},
								},
							},
						},
						//浪琴荟萃
						lqhc_hanguang: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								target: 'useCardToBefore',
							},
							usable: 1,
							filter(event, player) {
								return event.targets.includes(player) && event.player != player;
							},
							content() {
								'step 0';
								player.draw();
								('step 1');
								if (!player.countCards('h')) {
									event.finish();
								} else {
									player
										.chooseToDiscard('h', true, '选择一张手牌弃置')
										.set('ai', function (card) {
											if (get.type(card) == 'equip') {
												return 5 - get.value(card);
											}
											return -get.value(card);
										})
										.set('filterCard', lib.filter.cardDiscardable);
								}
								('step 2');
								if (result.cards?.length) {
									event.a = result.cards[0];
									if (get.type(event.a) == 'basic') {
										player.changeHujia();
									} else if (get.type(event.a, 'trick') == 'trick') {
										trigger.cancel();
									} else if (get.type(event.a) == 'equip') {
										if (
											!player.isDisabled(get.subtype(event.a)) &&
											game.hasPlayer(function (current) {
												return current.countCards('e');
											})
										) {
											player
												.chooseControl('装备' + get.translation(event.a), '转移场上一张牌')
												.set('ai', function () {
													const player = _status.event.player;
													if (
														player.countCards('e', function (card) {
															return get.subtype(card) == get.subtype(event.a) && get.equipValue(card, player) > get.equipValue(event.a, player);
														})
													) {
														return '转移场上一张牌';
													}
													return '装备' + get.translation(event.a);
												})
												.set('请选择一项');
										} else if (player.isDisabled(get.subtype(event.a))) {
											event._result = { control: '转移场上一张牌' };
										} else {
											event._result = { control: '装备' + get.translation(event.a) };
										}
									}
								}
								('step 3');
								if (result.control == '转移场上一张牌') {
									player.moveCard();
								}
								if (result.control == '装备' + get.translation(event.a)) {
									player.chooseUseTarget(event.a, true, 'nopopup');
								}
							},
							ai: {
								result: {
									player: 1,
								},
							},
						},
						lqhc_chuqiao: {
							audio: 'ext:浪琴天阙/audio:1',
							trigger: {
								player: 'phaseUseBegin',
							},
							forced: true,
							content() {
								'step 0';
								if (player.countCards('h') < 5) {
									player.chooseBool('###是否发动【出鞘】？###将手牌摸至5张.').set('ai', function () {
										return true;
									});
								} else {
									player.chooseToDiscard([1, Infinity], 'he', '###是否发动【出鞘】？###弃置任意张牌,并摸等量的牌.').set('ai', function (card) {
										return 6 - get.value(card);
									});
								}
								('step 1');
								if (result.bool) {
									player.addTempSkill('lqhc_chuqiao_use', ['dieEnd', 'phaseAfter']);
									if (result.cards?.length) {
										//QQQ
										player.draw(result.cards.length);
										player
											.chooseTarget('可以弃置最多' + get.translation(result.cards.length) + '名角色各一张牌.', [1, result.cards.length], function (card, player, target) {
												return player != target && target.countDiscardableCards(player, 'he');
											})
											.set('ai', function (target) {
												return -get.attitude(_status.event.player, target);
											});
									} else {
										player.drawTo(5);
									}
								} else {
									event.finish();
								}
								('step 2');
								if (result.targets?.length) {
									for (const i of result.targets) {
										player.discardPlayerCard(i, 'he', true);
									}
								}
							},
							subSkill: {
								use: {
									trigger: {
										player: 'useCard',
									},
									charlotte: true,
									prompt: '是否发动【出鞘】？',
									prompt2(event) {
										return '令' + get.translation(event.card) + '无视防具,不能被其他角色响应,回复/伤害值+1,可额外指定任意个目标(无距离限制)并可以进行两次结算.';
									},
									filter(event, player) {
										return (event.targets && event.targets.length == 1 && get.type(event.card) == 'trick') || ['sha', 'tao'].includes(event.card.name);
									},
									content() {
										'step 0';
										trigger.effectCount++;
										player.addTempSkill('unequip', { player: 'useCardEnd' });
										trigger.directHit.addArray(
											game.filterPlayer(function (current) {
												return current != player;
											}),
										);
										trigger.baseDamage++;
										let goon = false;
										const info = get.info(trigger.card);
										if (trigger.targets && !info.multitarget) {
											const players = game.filterPlayer();
											for (const i of players) {
												if (lib.card[trigger.card.name].enable && lib.filter.targetEnabled2(trigger.card, player, i) && !trigger.targets.includes(i)) {
													goon = true;
													break; //循环
												}
											}
										}
										if (goon) {
											player
												.chooseTarget(1, '为' + get.translation(trigger.card) + '增加任意个额外目标', function (card, player, target) {
													const trigger = _status.event;
													if (trigger.targets.includes(target)) {
														return false;
													}
													return lib.filter.targetEnabled2(trigger.card, player, target, false);
												})
												.set('autodelay', true)
												.set('ai', function (target) {
													const trigger = _status.event.getTrigger(),
														player = _status.event.player;
													return get.effect(target, trigger.card, player, player);
												})
												.set('targets', trigger.targets)
												.set('card', trigger.card);
										}
										('step 1');
										if (result.targets?.length) {
											for (const i of result.targets) {
												trigger.targets.push(i);
											}
										}
									},
								},
							},
						},
						lqhc_jiuxian: {
							subSkill: {
								a: {
									filter(event, player) {
										if (!player.storage.lqhc_jiuxian || !player.storage.lqhc_jiuxian.length) {
											return false;
										}
										for (const i of player.storage.lqhc_jiuxian) {
											if (event.filterCard && event.filterCard({ name: i }, player, event) && !player.storage.jiux.includes(i)) {
												return true;
											}
										}
										return false;
									},
									hiddenCard(player, name) {
										if (!player.storage.lqhc_jiuxian || !player.storage.lqhc_jiuxian.length) {
											return false;
										}
										return !player.storage.jiux.includes(name) && player.storage.lqhc_jiuxian.includes(name);
									},
									enable: ['chooseToUse', 'chooseToRespond'],
									chooseButton: {
										dialog(event, player) {
											const list = [];

											for (const i of lib.inpile) {
												const name = i;
												if (player.storage.jiux.includes(name)) {
													continue;
												}
												if (!player.storage.lqhc_jiuxian.includes(name)) {
													continue;
												}
												if (name == 'sha') {
													if (event.filterCard && event.filterCard({ name: name }, player, event)) {
														list.push(['基本', '', 'sha']);
													}
													if (event.filterCard && event.filterCard({ name: name, nature: 'fire' }, player, event)) {
														list.push(['基本', '', 'sha', 'fire']);
													}
													if (event.filterCard && event.filterCard({ name: name, nature: 'thunder' }, player, event)) {
														list.push(['基本', '', 'sha', 'thunder']);
													}
													if (event.filterCard && event.filterCard({ name: name, nature: 'ice' }, player, event)) {
														list.push(['基本', '', 'sha', 'ice']);
													}
													if (event.filterCard && event.filterCard({ name: name, nature: 'kami' }, player, event)) {
														list.push(['基本', '', 'sha', 'kami']);
													}
													if (event.filterCard && event.filterCard({ name: name, nature: 'stab' }, player, event)) {
														list.push(['基本', '', 'sha', 'stab']);
													}
												} else if (event.filterCard && event.filterCard({ name: name }, player, event)) {
													list.push([get.translation(get.type({ name: name })), '', name]);
												}
											}

											return ui.create.dialog('九弦', [list, 'vcard']);
										},
										filter(button, player) {
											return _status.event.parent.filterCard(
												{
													name: button.link[2],
												},
												player,
												_status.event.parent,
											);
										},
										check(button) {
											if (_status.event.parent.type != 'phase') {
												return 1;
											}
											const player = _status.event.player;
											if (['wugu', 'zhulu_card', 'yiyi', 'lulitongxin', 'lianjunshengyan', 'diaohulishan'].includes(button.link[2])) {
												return 0;
											}
											return player.getUseValue({
												name: button.link[2],
												nature: button.link[3],
											});
										},
										backup(links, player) {
											return {
												filterCard() {
													return false;
												},
												audio: 'lqhc_jiuxian',
												line: false,
												selectCard: -1,
												popname: true,
												viewAs: {
													name: links[0][2],
													nature: links[0][3],
												},
												onuse(result, player) {
													player.storage.jiux.add(result.card.name);
													if (['delay', 'equip'].includes(get.type(result.card))) {
														player.storage.jiuequipd = result.card.name;
													}
													player.addTempSkill('lqhc_jiuxian_b');
												},
												onrespond(result, player) {
													player.storage.jiux.add(result.card.name);
													player.addTempSkill('lqhc_jiuxian_b');
												},
											};
										},
										prompt(links, player) {
											return '视为使用或打出' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]);
										},
									},
									ai: {
										fireAttack: true,
										respondSha: true,
										respondShan: true,
										skillTagFilter(player) {
											return ['sha', 'shan'].includes(player.storage.jiux) && player.storage.lqhc_jiuxian && ['sha', 'shan'].includes(player.storage.lqhc_jiuxian);
										},
										order: 1,
										result: {
											player(player) {
												if (_status.event.dying) {
													return get.attitude(player, _status.event.dying);
												}
												return 1;
											},
										},
									},
								},
								b: {
									onremove(player) {
										player.storage.jiux = [];
									},
								},
								c: {
									trigger: {
										player: 'useCardToBegin',
									},
									forced: true,
									_priority: 21,
									popup: false,
									filter(event, player) {
										return event.skill == 'lqhc_jiuxian_a_backup' && player.storage.jiuequipd && event.card.name == player.storage.jiuequipd;
									},
									content() {
										const card = game.createCard(trigger.card.name, '', '');
										trigger.card = card;
										trigger.cards = [card];
										trigger.card.cards = trigger.cards;
										game.cardsGotoOrdering(card).relatedEvent = trigger;
										delete player.storage.jiuequipd;
									},
								},
							},
							trigger: {
								player: ['enterGame', 'drawBefore', 'gainBegin', 'gainAfter', 'useCardAfter'],
								global: 'phaseBefore',
							},
							forced: true,
							filter(event, player, name) {
								if (event.name == 'useCard' || name == 'gainAfter') {
									return player.countCards('h');
								}
								if (name == 'phaseBefore') {
									return game.phaseNumber == 0;
								}
								return true;
							},
							content() {
								'step 0';
								if (player.countCards('h') > 0) {
									const hs = player.getCards('h');
									game.addVideo('lose', player, [get.cardsInfo(hs), [], [], []]);

									for (const i of hs) {
										i.discard(false);
									}

									player.update();
									game.log(player, '不会拥有手牌');
								}
								if (['drawBefore', 'gainBegin'].includes(event.triggername)) {
									trigger.cancel();
									game.log(player, '不会拥有手牌');
								}
								if (['phaseBefore', 'enterGame'].includes(event.triggername)) {
									const basic = [],
										trick = [],
										delay = [],
										equip = [],
										card = [];
									for (const i of lib.inpile) {
										if (get.type(i) == 'basic') {
											basic.push(i);
										} else if (get.type(i) == 'trick') {
											trick.push(i);
										} else if (get.type(i) == 'delay') {
											delay.push(i);
										} else if (get.type(i) == 'equip') {
											equip.push(i);
										} else {
											card.push(i);
										}
									}
									const dialog = ui.create.dialog('九弦', '声明九个牌名', 'hidden');
									if (basic.length) {
										dialog.addText('基本牌');
										dialog.add([basic, 'vcard']);
									}
									if (trick.length) {
										dialog.addText('锦囊牌');
										dialog.add([trick, 'vcard']);
									}
									if (delay.length) {
										dialog.addText('延时性锦囊牌');
										dialog.add([delay, 'vcard']);
									}
									if (equip.length) {
										dialog.addText('装备牌');
										dialog.add([equip, 'vcard']);
									}
									if (card.length) {
										dialog.addText('其他');
										dialog.add([card, 'vcard']);
									}
									const chooseButton = player.chooseButton(dialog, true, 9);
									chooseButton.set('ai', function (button) {
										if (['wuzhong', 'zengbin', 'dongzhu', 'shunshou', 'huogong', 'shenenshu'].includes(button.link[2]) || get.tag({ name: button.link[2] }, 'draw') || get.tag({ name: button.link[2] }, 'gain')) {
											return 0;
										}
										if (['shan', 'sha', 'tao', 'jiu', 'lebu', 'wuxie', 'guohe', 'baiyin', 'hanbing'].includes(button.link[2])) {
											return 20;
										}
										const value = get.value({ name: button.link[2] });
										return value;
									});
								}
								('step 1');
								if (result.bool) {
									const zhuan = [];
									for (let n = 0; n < result.links.length; n++) {
										zhuan.push(result.buttons[n].link[2]);
									}
									game.log(player, '声明了', '#y' + get.translation(zhuan));
									player.storage.lqhc_jiuxian = zhuan;
									player.markSkill('lqhc_jiuxian');
								}
							},
							onremove(player) {
								delete player.storage.lqhc_jiuxian;
							},
							init(player) {
								if (!player.storage.jiux) {
									player.storage.jiux = [];
								}
								const hs = player.getCards('h');
								game.addVideo('lose', player, [get.cardsInfo(hs), [], [], []]);

								for (const i of hs) {
									i.discard(false);
								}

								player.update();
							},
							marktext: '九',
							intro: {
								content(storage, player) {
									return get.translation(storage);
								},
							},
							ai: {
								noh: true,
							},
							group: ['lqhc_jiuxian_a', 'lqhc_jiuxian_c'],
							ai: {
								effect: {
									target(card, player, target, current) {
										if (card.name == 'jiu') {
											return [1, 1];
										}
									},
								},
							},
						},
						lqhc_shiyuan: {
							trigger: {
								player: 'useCardAfter',
							},
							intro: {
								content: 'mark',
							},
							filter(event, player) {
								return player.isPhaseUsing();
							},
							forced: true,
							usable: 3,
							content() {
								'step 0';
								player
									.chooseTarget('###是否发动【峙援】？###令一名其他角色从牌堆获得一张' + get.translation(get.type2(trigger.card)), function (card, player, target) {
										return target != player;
									})
									.set('ai', function (target) {
										return get.attitude(_status.event.player, target);
									});
								('step 1');
								if (result.targets?.length) {
									player.line(result.targets[0], 'fire');
									const type1 = get.type2(trigger.card);
									const delay1 = 0;
									if (
										result.targets[0].countCards('h', function (cardx) {
											return get.type2(cardx) == type1;
										}) > 0
									) {
										if (result.targets[0].countMark('lqhc_shiyuan') < 2) {
											result.targets[0].addMark('lqhc_shiyuan', 1);
										} else {
											return;
										}
									} else {
										const card = get.cardPile(function (card) {
											return get.type2(card) == type1;
										});
										if (card) {
											result.targets[0].gain(card, 'gain2');
										}
									}
								} else {
									player.getStat('triggerSkill').lqhc_shiyuan--;
								}
							},
							group: 'lqhc_shiyuan_draw',
							subSkill: {
								draw: {
									trigger: {
										global: 'phaseAfter',
									},
									forced: true,
									filter(event, player) {
										return event.player.countMark('lqhc_shiyuan') > 0;
									},
									content() {
										trigger.player.draw(trigger.player.countMark('lqhc_shiyuan'));
										trigger.player.removeMark('lqhc_shiyuan', trigger.player.countMark('lqhc_shiyuan'));
									},
								},
							},
						},
						lqhc_zhaocai: {
							enable: 'phaseUse',
							prompt: '获得四张花色各不相同的牌',
							audio: 'ext:浪琴天阙/audio:2',
							usable: 2,
							filter(event, player) {
								if (player.getStat().skill.lqhc_zhaocai > 0 && !player.hasSkill('lqhc_qunchong_zc')) {
									return false;
								}
								return true;
							},
							content() {
								'step 0';
								const spade = [],
									heart = [],
									club = [],
									diamond = [];
								get.cardPile(function (card) {
									if (card.suit == 'spade') {
										spade.push(card);
									}
									if (card.suit == 'heart') {
										heart.push(card);
									}
									if (card.suit == 'club') {
										club.push(card);
									}
									if (card.suit == 'diamond') {
										diamond.push(card);
									}
								});
								const list = [spade.randomGet(), heart.randomGet(), club.randomGet(), diamond.randomGet()];
								player.gain(list, 'draw2');
								player.addTempSkill('lqhc_zhaocai_losehp');
							},
							ai: {
								basic: {
									order: 8,
								},
								result: {
									player: 1,
								},
							},
							subSkill: {
								losehp: {
									trigger: {
										player: 'phaseEnd',
									},
									forced: true,
									audio: 'lqhc_zhaocai',
									filter(event, player) {
										const history = player.getHistory('useCard').concat(player.getHistory('respond'));
										const suit = [];

										for (const i of history) {
											suit.add(i.card.suit);
										}

										return suit.length < 4;
									},
									content() {
										player.loseHp();
									},
								},
							},
						},
						lqhc_qunchong: {
							trigger: {
								player: 'qunchongthrowEmotionEnd',
								target: 'qunchongthrowEmotionEnd',
							},
							forced: true,
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player) {
								if (player.storage.qunchongthrow && player.storage.qunchongthrow.includes(event.namex)) {
									return false;
								}
								if (event.player == player) {
									if (event.target == player) {
										return false;
									}
									if (event.namex == 'egg') {
										return event.target.countCards('he');
									}
									return ['shoe', 'jiasuo'].includes(event.namex);
								}
								if (event.target == player) {
									if (event.player == player) {
										return false;
									}
									if (['yuxisx', 'wine'].includes(event.namex)) {
										return player.isDamaged() || !player.hasSkill('lqhc_qunchong_zc');
									}
									return event.namex == 'flower';
								}
							},
							logTarget(event, player) {
								if (event.target == player) {
									return false;
								}
								return [event.target];
							},
							content() {
								if (!player.storage.qunchongthrow) {
									player.storage.qunchongthrow = [];
								}
								player.storage.qunchongthrow.push(trigger.namex);
								if (trigger.player == player) {
									if (trigger.namex == 'egg') {
										trigger.target.chooseToDiscard(true, 'he');
									} else {
										trigger.target.damage();
									}
								}
								if (trigger.target == player) {
									if (trigger.namex == 'flower') {
										player.draw();
									} else {
										if (player.isDamaged()) {
											player.recover();
										} else {
											player.addTempSkill('lqhc_qunchong_zc', { player: 'phaseAfter' });
											game.log(player, '下次使用【招财】次数', '#y+1');
										}
									}
								}
							},
							global: ['lqhc_qunchong_global', 'lqhc_qunchong_global0', 'lqhc_qunchong_global1', 'lqhc_qunchong_globalx'],
							group: 'lqhc_qunchong_thro',
							subSkill: {
								zc: {},
								thro: {
									trigger: {
										global: 'phaseAfter',
									},
									forced: true,
									content() {
										delete player.storage.qunchongthrow;
									},
								},
							},
						},
						lqhc_qunchong_global: {
							trigger: {
								global: 'phaseUseBegin',
							},
							forced: true,
							filter(event, player) {
								return (_status.auto || !player.isUnderControl(true)) && player.hasSkill('lqhc_qunchong') && (!player.storage.qunchongthrow || !player.storage.qunchongthrow.includes('egg') || !player.storage.qunchongthrow.includes('shoe'));
							},
							content() {
								'step 0';
								player.chooseTarget(
									function (card, player, target) {
										return target.isAlive() && target != player && target.countCards('he') && (!player.storage.qunchongthrow || !player.storage.qunchongthrow.includes('egg'));
									},
									function (target) {
										return -get.attitude(player, target);
									},
								);
								('step 1');
								if (result.targets?.length) {
									player.$throwEmotion(result.targets[0], 'egg');
								}
								('step 2');
								player.chooseTarget(
									function (card, player, target) {
										return target.isAlive() && target != player && (!player.storage.qunchongthrow || !player.storage.qunchongthrow.includes('shoe'));
									},
									function (target) {
										return get.damageEffect(target, player, player);
									},
								);
								('step 3');
								if (result.targets?.length) {
									player.$throwEmotion(result.targets[0], 'shoe');
								}
							},
						},
						lqhc_qunchong_global0: {
							trigger: {
								global: 'damageAfter',
							},
							forced: true,
							filter(event, player) {
								return (_status.auto || !player.isUnderControl(true)) && event.player.hasSkill('lqhc_qunchong') && (!event.player.storage.qunchongthrow || !event.player.storage.qunchongthrow.includes('wine'));
							},
							content() {
								if (get.attitude(player, trigger.player) > 0) {
									player.$throwEmotion(trigger.player, 'wine');
								}
							},
						},
						lqhc_qunchong_global1: {
							trigger: {
								global: 'phaseUseBegin',
							},
							forced: true,
							_priority: -1,
							filter(event, player) {
								return _status.auto || !player.isUnderControl(true);
							},
							content() {
								'step 0';
								player.chooseTarget(
									function (card, player, target) {
										return target.isAlive() && target != player && target.hasSkill('lqhc_qunchong') && (!target.storage.qunchongthrow || !target.storage.qunchongthrow.includes('wine')) && (target.isDamaged() || (!target.hasSkill('lqhc_qunchong_zc') && target == trigger.player));
									},
									function (target) {
										return get.attitude(player, target);
									},
								);
								('step 1');
								if (result.targets?.length) {
									player.$throwEmotion(result.targets[0], 'wine');
								}
							},
						},
						lqhc_qunchong_globalx: {
							trigger: {
								global: 'phaseBegin',
							},
							forced: true,
							_priority: -1,
							filter(event, player) {
								return _status.auto || !player.isUnderControl(true);
							},
							content() {
								'step 0';
								player.chooseTarget(
									function (card, player, target) {
										return target.isAlive() && target != player && target.hasSkill('lqhc_qunchong') && (!target.storage.qunchongthrow || !target.storage.qunchongthrow.includes('flower'));
									},
									function (target) {
										return get.attitude(player, target);
									},
								);
								('step 1');
								if (result.targets?.length) {
									player.$throwEmotion(result.targets[0], 'flower');
								}
							},
						},
						lqhc_lianyue: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								player: 'phaseBegin',
							},
							forced: true,
							content() {
								'step 0';
								player.judge(function (card) {
									return card.number == 13 ? 4 : 1;
								});
								('step 1');
								if (result.bool) {
									if (result.number == 13) {
										player.recover(3 - player.hp);
										if (player.hasSkill('lqhc_lianyue_jue')) {
											player.removeSkill('lqhc_lianyue_jue');
										}
										player.addSkill('lqhc_lianyue_ying');
									} else {
										if (player.hasSkill('lqhc_lianyue_ying')) {
											player.removeSkill('lqhc_lianyue_ying');
										}
										player.addSkill('lqhc_lianyue_jue');
									}
								}
							},
							subSkill: {
								ying: {
									trigger: {
										player: 'loseAfter',
										global: ['equipAfter', 'addJudgeAfter', 'gainAfter', 'loseAsyncAfter', 'addToExpansionAfter'],
									},
									name: '憐月',
									marktext: '盈',
									mark: true,
									intro: {
										content: '当你获得牌/失去手牌后,若你的手牌数大于3/小于3,则你将手牌摸至3张/弃置至3张.',
									},
									forced: true,
									filter(event, player) {
										if (event.name == 'gain' && event.player == player) {
											return player.countCards('h') > 3;
										}
										var evt = event.getl(player);
										if (!evt || !evt.hs || evt.hs.length == 0 || player.countCards('h') >= 3) {
											return false;
										}
										var evt = event;
										for (let i = 0; i < 3; i++) {
											evt = evt.getParent('lqhc_lianyue_ying');
											if (evt.name != 'lqhc_lianyue_ying') {
												return true;
											}
										}
										return false;
									},
									content() {
										const num = 3 - player.countCards('h');
										if (num > 0) {
											player.draw(num);
										} else {
											player.chooseToDiscard('h', true, -num);
										}
									},
								},
								jue: {
									trigger: {
										player: ['loseAfter', 'dying'],
										global: ['equipAfter', 'addJudgeAfter', 'gainAfter', 'loseAsyncAfter', 'addToExpansionAfter'],
									},
									name: '憐月',
									marktext: '玦',
									mark: true,
									intro: {
										content: '当你获得牌/失去手牌后,若你的手牌数大于1/小于1,则你将手牌摸至1张/弃置至1张.当你进入濒死状态时,你回复一点体力.',
									},
									forced: true,
									filter(event, player, name) {
										if (name == 'dying') {
											return true;
										}
										if (name != 'dying') {
											if (event.name == 'gain' && event.player == player) {
												return player.countCards('h') > 1;
											}
											var evt = event.getl(player);
											if (!evt || !evt.hs || evt.hs.length == 0 || player.countCards('h') >= 1) {
												return false;
											}
											var evt = event;
											for (let i = 0; i < 1; i++) {
												evt = evt.getParent('lqhc_lianyue_jue');
												if (evt.name != 'lqhc_lianyue_jue') {
													return true;
												}
											}
										}
										return false;
									},
									content() {
										if (event.triggername == 'dying') {
											player.recover();
										} else {
											const num = 1 - player.countCards('h');
											if (num > 0) {
												player.draw(num);
											} else {
												player.chooseToDiscard('h', true, -num);
											}
										}
									},
								},
							},
						},
						lqhc_wenqing: {
							trigger: {
								global: ['turnOverBefore', 'damageBegin4'],
							},
							_priority: 20,
							usable: 1,
							check(event, player) {
								return get.attitude(player, event.player) > 0 && player.hp > 1;
							},
							logTarget(event, player) {
								return event.player;
							},
							prompt2(event, player) {
								return get.translation(event.player) + '即将' + (event.name == 'damage' ? '受到伤害' : '翻面') + ',你可以失去一点体力取消之';
							},
							logTarget(event, player) {
								return event.player;
							},
							content() {
								player.loseHp();
								trigger.cancel();
								game.log(trigger.name == 'damage' ? get.translation(trigger.player) + '即将受到的伤害被取消' : '取消了翻面');
							},
						},
						lqhc_xidie: {
							trigger: {
								global: ['phaseBefore', 'dieEnd'],
								player: 'enterGame',
							},
							intro: {
								content: '你的判定牌点数+#',
							},
							forced: true,
							filter(event, player) {
								if (event.name == 'die') {
									return event.player.hasSkill('lqhc_wenqing');
								}
								if (event.name == 'phase') {
									return game.phaseNumber == 0;
								}
								return true;
							},
							content() {
								'step 0';
								player
									.chooseTarget('###是否发动【牺蝶】?###可以令男性一名男性角色获得技能【温卿】.', function (card, player, target) {
										return target.hasSex('male');
									})
									.set('ai', function (target) {
										return get.attitude(_status.event.player, target);
									});
								('step 1');
								if (result.targets?.length) {
									result.targets[0].addSkill('lqhc_wenqing');
								}
							},
							group: ['lqhc_xidie_judge', 'lqhc_xidie_die'],
							subSkill: {
								die: {
									trigger: {
										global: 'dieEnd',
									},
									forced: true,
									_priority: -1,
									filter(event, player) {
										return event.player.hasSkill('lqhc_wenqing');
									},
									content() {
										player.addMark('lqhc_xidie', 3);
									},
								},
								judge: {
									trigger: {
										global: 'judge',
									},
									forced: true,
									lastDo: true,
									filter(event, player) {
										return player.countMark('lqhc_xidie') > 0 && event.player == player;
									},
									content() {
										const card = trigger.player.judging[0];
										let num = card.number + player.countMark('lqhc_xidie');
										if (num > 13) {
											num = 13;
										}
										if (!trigger.fixedResult) {
											trigger.fixedResult = {};
										}
										trigger.fixedResult.number = num;
										game.log(trigger.player, '判定结果点数改为', '#g' + num);
									},
								},
							},
						},
						lqtqx_jinfan: {
							audio: 'gnjinfan',
							enable: 'phaseUse',
							usable: 1,
							filterTarget(card, player, target) {
								return target != player && (target.countCards('h') > 0 || player.countCards('h') > 0);
							},
							content() {
								'step 0';
								if (player.countCards('h') < 1) {
									var u = player.chooseButton([1, 4], [get.translation(target) + '的手牌', target.getCards('h')]);
								} else if (target.countCards('h') < 1) {
									var u = player.chooseButton([1, 4], ['你的手牌', player.getCards('h')]);
								} else {
									var u = player.chooseButton([1, 4], ['你的手牌', player.getCards('h'), get.translation(target) + '的手牌', target.getCards('h')]);
								}
								u.set('target', target);
								u.set('ai', function (button) {
									const player = _status.event.player;
									const target = _status.event.target;
									for (const i of ui.selected.buttons) {
										if (target.getCards('h').includes(i)) {
											return 1000 - get.value(button.link);
										}
									}
									return get.value(button.link);
								});
								u.set('filterButton', function (button) {
									for (const i of ui.selected.buttons) {
										if (button.link.suit == i.link.suit) {
											return false;
										}
									}

									return true;
								});
								('step 1');
								if (result.links?.length) {
									game.log(player, '将', result.links, '放到了武将牌上');
									const list = [];
									for (const i of result.links) {
										if (get.owner(i) != player) {
											list.push(i);
										}
									}
									if (list.length) {
										target.$give(list.length, player, false);
										target.lose(list, player);
									}
									player.loseToSpecial(result.links, 'lqtqx_jinfan').visible = true;
								}
								('step 2');
								target.update();
								player.markSkill('lqtqx_jinfan');
							},
							group: ['lqtqx_jinfan_gain'],
							marktext: '铃',
							intro: {
								mark(dialog, storage, player) {
									dialog.addAuto(
										player.getCards('s', function (card) {
											return card.hasGaintag('lqtqx_jinfan');
										}),
									);
								},
								markcount(storage, player) {
									return player.getCards('s', function (card) {
										return card.hasGaintag('lqtqx_jinfan');
									}).length;
								},
								onunmark(storage, player) {
									const cards = player.getCards('s', function (card) {
										return card.hasGaintag('lqtqx_jinfan');
									});
									if (cards.length) {
										player.lose(cards, ui.discardPile);
										player.$throw(cards, 1000);
										game.log(cards, '进入了弃牌堆');
									}
								},
							},
							mod: {
								aiOrder(player, card, num) {
									if (get.itemtype(card) == 'card' && card.hasGaintag('lqtqx_jinfan')) {
										return num + 0.5;
									}
								},
							},
							ai: {
								order: 13,
								result: {
									target: -1,
									player: 1,
								},
							},
							subSkill: {
								gain: {
									trigger: {
										player: 'loseAfter',
									},
									forced: true,
									filter(event, player) {
										if (!event.ss || !event.ss.length) {
											return false;
										}
										for (const i in event.gaintag_map) {
											if (event.gaintag_map[i].includes('lqtqx_jinfan')) {
												return true;
											}
											return false;
										}
									},
									content() {
										'step 0';
										const cards = [];
										for (const i of trigger.ss) {
											if (!trigger.gaintag_map[i.cardid] || !trigger.gaintag_map[i.cardid].includes('lqtqx_jinfan')) {
												continue;
											}
											var suit = i.suit;
											const card = get.cardPile2(function (card) {
												return !cards.includes(card) && card.suit == suit;
											});
											if (card) {
												cards.push(card);
											}
										}
										if (cards.length) {
											player.gain(cards, 'gain2');
										}
										const num = player.getCards('s', function (card) {
											return card.hasGaintag('lqtqx_jinfan');
										}).length;
										if (num > 0) {
											player.markSkill('lqtqx_jinfan');
										} else {
											player.unmarkSkill('lqtqx_jinfan');
										}
										('step 1');
										game.updateRoundNumber();
									},
								},
							},
						},
						lqtqx_qixi: {
							trigger: {
								global: 'phaseZhunbeiBegin',
							},
							filter(event, player) {
								const cards = player.getCards('hs', function (card) {
									return card.hasGaintag('lqtqx_jieying') || card.hasGaintag('lqtqx_jinfan');
								});
								for (const i of cards) {
									if (player.canUse(i, event.player) && event.player != player) {
										return true;
									}
								}
								return false;
							},
							audio: 'gnsheque',
							forced: true,
							content() {
								'step 0';
								player
									.chooseCard('hs', '###是否发动【奇袭】？###你可以对' + get.translation(trigger.player) + '使用一张<铃>或「劫营」牌.', function (card, player, target) {
										return (card.hasGaintag('lqtqx_jieying') || card.hasGaintag('lqtqx_jinfan')) && player.canUse(card, trigger.player);
									})
									.set('ai', function (card) {
										if (get.attitude(_status.event.player, trigger.player) < 0) {
											return get.value(card);
										}
										return 0;
									});
								('step 1');
								if (result.cards?.length) {
									player.useCard(result.cards[0], trigger.player);
								}
							},
							group: 'lqtqx_qixi_use',
							subSkill: {
								use: {
									audio: 'qixi',
									usable: 1,
									enable: 'chooseToUse',
									filterCard(card, player) {
										return card.hasGaintag('lqtqx_jinfan') || card.hasGaintag('lqtqx_jieying');
									},
									position: 'hs',
									viewAs: {
										name: 'guohe',
									},
									viewAsFilter(player) {
										if (
											!player.getCards('s', function (card) {
												return card.hasGaintag('lqtqx_jinfan');
											}).length
										) {
											return false;
										}
									},
									prompt: '将一张<铃>或「劫营」牌当过河拆桥使用',
									check(card) {
										return 4 - get.value(card);
									},
								},
							},
						},
						lqtqx_jieying: {
							mod: {
								cardUsable(card, player, num) {
									if (!card.cards) {
										return;
									}
									for (const i of card.cards) {
										if (!i.hasGaintag('lqtqx_jieying')) {
											return;
										}
									}
									return Infinity;
								},
								targetInRange(card, player) {
									if (!card.cards) {
										return;
									}
									for (const i of card.cards) {
										if (!i.hasGaintag('lqtqx_jieying')) {
											return;
										}
									}
									return true;
								},
							},
							trigger: {
								player: 'phaseDiscardBegin',
								global: 'phaseEnd',
							},
							forced: true,
							audio: 'drlt_jieying',
							filter(event, player, name) {
								if (name == 'phaseDiscardBegin') {
									return (
										player.countCards('h') > 0 &&
										!game.hasPlayer(function (current) {
											return current.getCards('s', function (card) {
												return card.hasGaintag('lqtqx_jieying_mark');
											}).length;
										})
									);
								}
								return (
									event.player.getCards('s', function (card) {
										return card.hasGaintag('lqtqx_jieying_mark');
									}).length && event.player.isAlive()
								);
							},
							content() {
								'step 0';
								if (event.triggername == 'phaseEnd') {
									if (trigger.player.countCards('h') > 0) {
										player.gain(trigger.player.getCards('h'), 'draw2').gaintag.add('lqtqx_jieying');
									}
									const cards = trigger.player.getCards('s', function (card) {
										return card.hasGaintag('lqtqx_jieying_mark');
									});
									trigger.player.gain(cards, 'draw2');
									event.finish();
								} else {
									player.chooseCardTarget({
										position: 'h',
										filterTarget: true,
										selectCard: [1, 4],
										complexCard: true,
										filterCard(card, player, event) {
											if (Array.isArray(ui.selected.cards)) {
												for (const i of ui.selected.cards) {
													if (i.suit == card.suit) {
														return false;
													}
												}
											}
											return true;
										},
										ai1(card) {
											if (ui.selected.cards.length > 1) {
												return false;
											}
											return -get.useful(card);
										},
										ai2(target) {
											return -get.attitude(_status.event.player, target);
										},
										prompt: '是否发动【劫营】？',
										prompt2: '将任意张花色各不同的牌置于一名其他角色的武将牌上称为<营>.',
									});
								}
								('step 1');
								if (result.targets?.length) {
									game.log(player, '将', result.cards, '放去了', result.targets[0], '的武将牌上');
									player.$give(result.cards.length, result.targets[0], false);
									player.lose(result.cards, result.targets[0]);
									result.targets[0].addTempSkill('lqtqx_jieying_mark', { player: 'phaseAfter' });
									result.targets[0].loseToSpecial(result.cards, 'lqtqx_jieying_mark').visible = true;
									result.targets[0].markSkill('lqtqx_jieying_mark');
								}
							},
							subSkill: {
								mark: {
									marktext: '营',
									intro: {
										mark(dialog, storage, player) {
											dialog.addAuto(
												player.getCards('s', function (card) {
													return card.hasGaintag('lqtqx_jieying_mark');
												}),
											);
										},
										markcount(storage, player) {
											return player.getCards('s', function (card) {
												return card.hasGaintag('lqtqx_jieying_mark');
											}).length;
										},
										onunmark(storage, player) {
											const cards = player.getCards('s', function (card) {
												return card.hasGaintag('lqtqx_jieying_mark');
											});
											if (cards.length) {
												player.lose(cards, ui.discardPile);
												player.$throw(cards, 1000);
												game.log(cards, '进入了弃牌堆');
											}
										},
									},
									mod: {
										aiOrder(player, card, num) {
											if (get.itemtype(card) == 'card' && card.hasGaintag('lqtqx_jieying_mark')) {
												return num + 0.5;
											}
										},
										maxHandcard(player, num) {
											return (
												num +
												player.getCards('s', function (card) {
													return card.hasGaintag('lqtqx_jieying_mark');
												}).length
											);
										},
									},
									trigger: {
										player: 'loseAfter',
									},
									forced: true,
									popup: false,
									filter(event, player) {
										if (!event.ss || !event.ss.length) {
											return false;
										}
										for (const i in event.gaintag_map) {
											return (
												!player.getCards('s', function (card) {
													return card.hasGaintag('lqtqx_jieying_mark');
												}).length && event.gaintag_map[i].includes('lqtqx_jieying_mark')
											);
										}
									},
									content() {
										player.unmarkSkill('lqtqx_jieying_mark');
									},
								},
							},
						},
						lqhc_gaishi: {
							trigger: {
								player: 'phaseDrawBegin',
								global: 'useCardEnd',
							},
							check(event, player) {
								if (event.name == 'useCard') {
									return get.attitude(player, event.player) < 0;
								}
								return true;
							},
							filter(event, player, name) {
								if (name == 'phaseDrawBegin') {
									return !event.numFixed;
								}
								return (event.card.name == 'jiedao' && event.target == player) || (event.card.name == 'sha' && event.targets.includes(player) && player.canUse('sha', event.player, false, false));
							},
							logTarget(event, player) {
								return event.player;
							},
							prompt2(event, player) {
								if (event.name == 'useCard') {
									return '可以视为对' + get.translation(event.player) + '使用一张火属性【杀】.';
								}
								return '摸牌阶段,你可以多摸一张牌.';
							},
							content() {
								if (event.triggername == 'phaseDrawBegin') {
									trigger.num++;
								} else {
									player.useCard({ name: 'sha', nature: 'fire' }, trigger.player, false);
								}
							},
						},
						lqhc_jingji: {
							mod: {
								canBeDiscarded(card) {
									if (get.position(card) == 'e' && (get.subtype(card) == 'equip1' || get.subtype(card) == 'equip4')) {
										return false;
									}
								},
								canBeGained(card) {
									if (get.position(card) == 'e' && (get.subtype(card) == 'equip1' || get.subtype(card) == 'equip4')) {
										return false;
									}
								},
							},
							trigger: {
								source: 'damageBegin2',
							},
							forced: true,
							content() {
								trigger.num += Math.max(1, player.countCards('e'));
							},
						},
						lqhc_yiwen: {
							trigger: {
								player: 'damageBegin4',
							},
							forced: true,
							filter(event, player) {
								return get.type2(event.card) == 'trick';
							},
							content() {
								trigger.num++;
							},
						},
						lqhc_shuizhan: {
							enable: 'phaseUse',
							filterCard: true,
							selectCard: [1, Infinity],
							usable: 1,
							position: 'hs',
							viewAs: { name: 'shuiyanqijunx' },
							prompt: '将任意张手牌当做一张【水淹七军】使用.',
							viewAsFilter(player) {
								return player.countCards('hs') > 0;
							},
							check(card) {
								return 7 - get.useful(card);
							},
							ai: {
								order: 6.9,
							},
							group: 'lqhc_shuizhan_draw',
							subSkill: {
								draw: {
									trigger: { player: 'useCardAfter' },
									forced: true,
									filter(event, player) {
										return event.skill == 'lqhc_shuizhan' && event.cards && event.cards.length;
									},
									content() {
										if (
											player.hasHistory('sourceDamage', function (evt) {
												return evt.card == trigger.card;
											})
										) {
											player.draw(2 * trigger.cards.length);
										} else {
											player.draw(trigger.cards.length);
										}
									},
								},
							},
						},
						lqhc_yibing: {
							trigger: {
								player: ['dieBegin', 'phaseBegin', 'damageEnd'],
							},
							filter(event, player, name) {
								if (name == 'damageEnd') {
									return event.num > 0;
								}
								return game.hasPlayer(function (current) {
									return current.hasSkill('lqhc_yibing_add') || current.hasSkill('mad');
								});
							},
							forced: true,
							_priority: -1,
							content() {
								'step 0';
								if (trigger.name != 'damage') {
									game.countPlayer(function (current) {
										if (trigger.name == 'die') {
											current.removeSkill('lqhc_yibing_add');
										}
										current.removeSkill('mad');
									});
								} else {
									player
										.chooseTarget(get.prompt('lqhc_yibing'), '令一名其他角色于其的回合开始时进入混乱状态.', function (card, player, target) {
											return target != player && !target.hasSkill('lqhc_yibing_add');
										})
										.set('ai', function (target) {
											return -get.attitude(_status.event.player, target);
										});
								}
								('step 1');
								if (result.targets?.length) {
									result.targets[0].addSkill('lqhc_yibing_add');
								}
							},
							subSkill: {
								add: {
									mark: true,
									intro: {
										content: '你于回合开始时进入混乱状态',
									},
									trigger: {
										player: 'phaseBegin',
									},
									forced: true,
									charlotte: true,
									content() {
										player.removeSkill('lqhc_yibing_add');
										player.goMad();
									},
								},
							},
						},
						lqhc_kunlin: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							audio: 'tianren',
							content() {
								'step 0';
								player.loseMaxHp();
								('step 1');
								const i = player.maxHp;
								player.draw(i);
								player
									.chooseTarget([1, i], '对最多' + get.translation(i) + '名其他角色造成一点伤害.', function (card, player, target) {
										return target != player;
									})
									.set('ai', function (target) {
										return -get.attitude(_status.event.player, target);
									});
								('step 2');
								if (result.targets?.length) {
									for (const i of result.targets) {
										i.damage();
									}
								}
							},
						},
						lqhc_wuzhi: {
							trigger: {
								player: 'dieBegin',
							},
							audio: 'pingxiang',
							forced: true,
							juexingji: true,
							content() {
								'step 0';
								player.awakenSkill('lqhc_wuzhi');
								trigger.cancel();
								player.gainMaxHp(9);
								player.recover(6 - player.hp);
								const skills = [],
									characters = [];
								for (const i in lib.character) {
									const d = lib.character[i][4];
									if (d.includes('boss') || d.includes('hiddenboss') || d.includes('bossallowed') || d.includes('minskin')) {
										continue;
									}
									if (lib.character[i][1] != 'shu') {
										continue;
									}
									if ((game.me == player && lib.config.extension_浪琴天阙_lqtq_shaiji_kuochong) || !lib.config.forbidai.includes(i)) {
										characters.push(i);
									}
								}

								for (const i of characters) {
									const name = i;
									if (!lib.character[name]) {
										continue;
									}
									const skillsx = lib.character[name][3].slice(0);
									const list = skillsx.slice(0);

									for (const j of list) {
										if (j.includes('rewrite') || skills.includes(j)) {
											continue;
										}
										if (!lib.translate[j] || !lib.translate[j + '_info']) {
											continue;
										}
										const info = get.info(j);
										if (!info || info.zhuSkill || info.charlotte) {
											continue;
										}
										if (
											game.hasPlayer(function (current) {
												return current.hasSkill(j);
											})
										) {
											continue;
										}
										skills.push(j);
										lib.card['skillCard_' + j] = {
											fullimage: true,
											image: 'character:' + name,
										};
										lib.translate['skillCard_' + j] = lib.translate[j];
										lib.translate['skillCard_' + j + '_info'] = lib.translate[j + '_info'];
									}
								}

								event.list = skills.randomGets(9);
								for (let i = 0; i < event.list.length; i++) {
									event.list[i] = ['', '', 'skillCard_' + event.list[i]];
								}
								if (!event.list.length) {
									return;
								} else {
									player.chooseButton(['选择要获得的技能', [event.list, 'vcard']], true, 3);
								}
								('step 1');
								event.list = result.links;
								game.log('g' + event.list);
								player
									.chooseTarget('可以令一名其他角色与你分配选择的技能,否则你获得这些所有技能.', function (card, player, target) {
										return target != player;
									})
									.set('ai', function (target) {
										return get.attitude(_status.event.player, target);
									});
								('step 2');
								if (result.targets?.length) {
									event.target = result.targets[0];
									player.chooseButton(['选择你要获得的技能,' + get.translation(event.target) + '获得剩下的技能', [event.list, 'vcard']], true, [1, 2]);
								} else {
									for (const i of event.list) {
										player.addSkill(i[2].slice(10));
									}
									event.finish();
								}
								('step 3');
								for (const i of event.list) {
									if (result.links.includes(i)) {
										player.addSkill(i[2].slice(10));
									} else {
										event.target.addSkill(i[2].slice(10));
									}
								}
							},
						},
						//回合结束后,你可以观看牌堆顶6张牌,获得其中一张牌并将剩余的牌按任意顺序至于牌堆
						lqhc_yixiang: {
							trigger: {
								player: 'phaseEnd',
							},
							forced: true,
							audio: 'jiufa',
							content() {
								'step 0';
								event.cards = get.cards(6);
								game.cardsGotoOrdering(cards);
								('step 1');
								const prompt = '憶相:获得其中一张牌,将其他牌按任意顺序至于牌堆顶';
								const next = player.chooseToMove(prompt, true);
								next.set('list', [['按任意循序置于牌堆顶', cards], ['获得一张牌']]);
								next.set('filterMove', function (from, to, moved) {
									if (to == 1 && moved[0].length <= 5) {
										return false;
									}
									return true;
								});
								next.set('filterOk', function (moved) {
									return moved[0].length == 5;
								});
								next.set('num', 5);
								next.set('processAI', function (list) {
									const check = function (card) {
										const player = _status.event.player;
										const next = player.next;
										const att = get.attitude(player, next);
										const judge = next.getCards('j')[tops.length];
										if (judge) {
											return get.judge(judge)(card) * att;
										}
										return next.getUseValue(card) * att;
									};
									var cards = list[0][1].slice(0),
										tops = [];
									while (tops.length < 5) {
										cards.sort((a, b) => check(b) - check(a)); //QQQ
										tops.push(cards.shift());
									}
									return [tops, cards];
								});
								('step 2');
								if (result.bool) {
									const list = result.moved[0];
									const num = list.length - 1;
									for (let i = 0; i < list.length; i++) {
										event.cards.remove(list[num - i]);
										ui.cardPile.insertBefore(list[num - i], ui.cardPile.firstChild);
									}
								}
								('step 3');
								game.updateRoundNumber();
								if (event.cards.length) {
									player.gain(event.cards, 'gain2');
								}
							},
						},
						lqhc_shelie: {
							audio: 'shelie',
							trigger: {
								player: 'phaseDrawBegin1',
							},
							filter(event, player) {
								return !event.numFixed;
							},
							content() {
								'step 0';
								trigger.changeToZero();
								event.num = [5, 6, 7, 8, 9].randomGet();
								event.cards = get.cards(event.num);
								game.cardsGotoOrdering(event.cards);
								event.videoId = lib.status.videoId++;
								game.broadcastAll(
									function (player, id, cards) {
										let str;
										if (player == game.me && !_status.auto) {
											str = '涉猎:获取点数各不相同的牌';
										} else {
											str = '涉猎';
										}
										const dialog = ui.create.dialog(str, cards);
										dialog.videoId = id;
									},
									player,
									event.videoId,
									event.cards,
								);
								event.time = get.utc();
								game.addVideo('showCards', player, ['涉猎', get.cardsInfo(event.cards)]);
								game.addVideo('delay', null, 2);
								('step 1');
								const next = player.chooseButton([0, event.num], true);
								next.set('dialog', event.videoId);
								next.set('filterButton', function (button) {
									for (const i of ui.selected.buttons) {
										if (i.link.number == button.link.number) {
											return false;
										}
									}

									return true;
								});
								next.set('ai', function (button) {
									return get.value(button.link, _status.event.player);
								});
								('step 2');
								if (result.links?.length) {
									event.cards2 = result.links;
								} else {
									event.finish();
								}
								const time = 1000 - (get.utc() - event.time);
								if (time > 0) {
								}
								('step 3');
								game.broadcastAll('closeDialog', event.videoId);
								const cards2 = event.cards2;
								player.gain(cards2, 'log', 'gain2');
							},
						},
						lqhc_gongxin: {
							audio: 'gongxin',
							enable: 'phaseUse',
							intro: {
								content: '已记录花色:$',
							},
							filterTarget(card, player, target) {
								return target != player && target.countCards('h');
							},
							filter(event, player) {
								return game.hasPlayer(function (current) {
									return current != player && current.countCards('h') && player.getStorage('lqhc_gongxin').length < 4;
								});
							},
							content() {
								'step 0';
								event.videoId = lib.status.videoId++;
								const cards = target.getCards('h');
								if (player.isOnline2()) {
									player.send(
										function (cards, id) {
											ui.create.dialog('攻心', cards).videoId = id;
										},
										cards,
										event.videoId,
									);
								}
								event.dialog = ui.create.dialog('攻心', cards);
								event.dialog.videoId = event.videoId;
								if (!event.isMine()) {
									event.dialog.style.display = 'none';
								}
								player
									.chooseButton()
									.set('filterButton', function (button) {
										if (player.getStorage('lqhc_gongxin').length) {
											return !player.getStorage('lqhc_gongxin').includes(button.link.suit);
										}
										return get.value(button.link);
									})
									.set('dialog', event.videoId);
								('step 1');
								if (result.links?.length) {
									event.card = result.links[0];
									const func = function (card, id) {
										const dialog = get.idDialog(id);
										if (dialog) {
											for (const i of dialog.buttons) {
												if (i.link == card) {
													i.classList.add('selectedx');
												} else {
													i.classList.add('unselectable');
												}
											}
										}
									};
									if (player.isOnline2()) {
										player.send(func, event.card, event.videoId);
									} else if (event.isMine()) {
										func(event.card, event.videoId);
									}
									player.chooseControl('gongxin_discard', 'gongxin_top');
								} else {
									if (player.isOnline2()) {
										player.send('closeDialog', event.videoId);
									}
									event.dialog.close();
									event.finish();
								}
								('step 2');
								if (player.isOnline2()) {
									player.send('closeDialog', event.videoId);
								}
								event.dialog.close();
								const card = event.card;
								if (result.control == 'gongxin_top') {
									player.showCards(card, '置于牌堆顶');
									target.lose(card, ui.cardPile, 'insert', 'visible');
									game.log(player, '将', event.card, '置于牌堆顶');
								} else {
									target.discard(card);
								}
								player.addTempSkill('lqhc_gongxin_remove');
								player.markAuto('lqhc_gongxin', [card.suit]);
							},
							ai: {
								threaten: 1.5,
								result: {
									target(player, target) {
										for (const i of target.getCards('h')) {
											if (!player.getStorage('lqhc_gongxin').includes(i.suit)) {
												return -1;
												break;
											}
										}
										return 0;
									},
								},
								order: 10,
								expose: 0.4,
							},
							subSkill: {
								remove: {
									onremove(player, skill) {
										player.unmarkSkill('lqhc_gongxin');
									},
								},
							},
						},
						lqhc_xuepi: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							audio: 'qinxue',
							forced: true,
							filter(event, player) {
								return player.countCards('h') < player.maxHp;
							},
							juexingji: true,
							content() {
								player.awakenSkill('lqhc_xuepi');
								player.loseMaxHp();
								player.addSkill('lqhc_keji');
							},
							derivation: 'lqhc_keji',
						},
						lqhc_keji: {
							trigger: {
								player: 'phaseUseEnd',
							},
							audio: 'keji',
							forced: true,
							filter(event, player) {
								return (
									!player.hasHistory('sourceDamage', function (evt) {
										return get.type2(evt.card) == 'basic';
									}) ||
									!player.hasHistory('sourceDamage', function (evt) {
										return get.type2(evt.card) == 'trick';
									})
								);
							},
							content() {
								if (
									!player.hasHistory('sourceDamage', function (evt) {
										return get.type2(evt.card) == 'basic';
									})
								) {
									player.gainMaxHp();
									player.recover();
								}
								if (
									!player.hasHistory('sourceDamage', function (evt) {
										return get.type2(evt.card) == 'trick';
									})
								) {
									player.changeHujia(Math.floor(player.maxHp / 2));
								} //向下
							},
						},
						lqhc_longwei: {
							mod: {
								maxHandcard(player, num) {
									return 1 + num;
								},
							},
							trigger: {
								player: ['useCard', 'changeHp', 'loseMaxHpEnd', 'gainMaxHpEnd'],
							},
							audio: 'lqtz_juechou',
							forced: true,
							filter(event, player, name) {
								if (name == 'useCard') {
									return event.card && event.card.name == 'sha' && !player.getEquip(1);
								}
								return true;
							},
							content() {
								if (event.triggername == 'useCard') {
									player.addTempSkill('unequip', { player: 'useCardAfter' });
								} else {
									player.draw(Math.max(player.hp, player.maxHp - player.hp));
								}
							},
						},
						lqhc_longhun: {
							mod: {
								globalFrom(from, to, distance) {
									if (from.countMark('lqhc_longhun') > 0) {
										return distance - 1;
									}
								},
							},
							intro: {
								content: 'mark',
							},
							audio: 'lqtz_longce',
							inherit: 'relonghun',
							group: 'lqhc_longhun_num',
							subSkill: {
								num: {
									trigger: { player: ['useCard1', 'respond1'] },
									forced: true,
									_priority: 2,
									popup: false,
									filter(event, player) {
										return event.skill == 'lqhc_longhun';
									},
									content() {
										'step 0';
										player.addMark('lqhc_longhun');
										if (trigger.cards.length != 2) {
											return;
										}
										if (['sha', 'tao'].includes(trigger.card.name)) {
											trigger.baseDamage++;
											event.finish();
										} else {
											player
												.chooseControl(lib.suit)
												.set('ai', function () {
													return 'heart';
												})
												.set('prompt', '从牌堆中获得一张你选择的花色牌.');
										}
										('step 1');
										const list = [];
										get.cardPile(function (card) {
											if (card.suit == result.control) {
												list.push(card);
											}
										});
										if (list.length) {
											player.gain(list.randomGet(), 'draw2');
										}
									},
								},
							},
						},
						lqhc_gaoda: {
							intro: {
								mark(dialog, content, player) {
									if (player.countMark('lqhc_gaoda') > 0) {
										dialog.addText('代替成为【杀】的次数:' + player.storage.lqhc_gaoda);
									}
									dialog.addText('选择的角色');
									dialog.addSmall([player.storage.lqhc_gaoda2]);
								},
							},
							trigger: {
								player: 'phaseUseEnd',
								global: ['useCardToPlayer', 'dying'],
							},
							forced: true,
							dutySkill: true,
							filter(event, player, name) {
								if (name == 'phaseUseEnd') {
									return player.phaseNumber == 2 && !player.storage.lqhc_gaoda2;
								}
								if (name == 'dying') {
									return event.player == player.storage.lqhc_gaoda2;
								}
								return !event.targets.includes(player) && event.player != player && event.targets.includes(player.storage.lqhc_gaoda2) && (get.type(event.card) == 'trick' || event.card.name == 'sha');
							},
							content() {
								'step 0';
								const name = event.triggername,
									target = player.storage.lqhc_gaoda2;
								if (name == 'dying') {
									target.recover();
									game.log(player, '使命失败');
									player.awakenSkill('lqhc_gaoda');
								} else if (name == 'useCardToPlayer') {
									trigger.parent.targets.remove(player.storage.lqhc_gaoda2);
									trigger.parent.targets.push(player);
									if (trigger.card.name == 'sha') {
										player.addMark('lqhc_gaoda');
									}
									if (player.countMark('lqhc_gaoda') > 6) {
										game.log(player, '使命成功');
										player.awakenSkill('lqhc_gaoda');
										player.addSkill('lqhc_yingrui');
									}
								} else {
									player
										.chooseTarget('###【高达】###选择一名其他角色,该角色成为你以外的角色使用的【杀】或普通锦囊牌的目标时,目标改为你.', true, function (card, player, target) {
											return target != player;
										})
										.set('ai', function (target) {
											return 10 + get.attitude(_status.event.player, target);
										});
								}
								('step 1');
								if (result.bool) {
									player.markSkill('lqhc_gaoda');
									player.storage.lqhc_gaoda2 = result.targets[0];
								}
							},
							derivation: 'lqhc_yingrui',
						},
						lqhc_yingrui: {
							trigger: {
								player: 'phaseEnd',
							},
							filter(event, player, name) {
								return player.countMark('lqhc_longhun') > 0;
							},
							forced: true,
							content() {
								'step 0';
								event.count2 = player.maxHp;
								event.count1 = player.hp;
								if (event.isMine()) {
									if (!event.num) {
										event.num = 0;
									}
									if (!event.arr) {
										event.arr = [0, player.countMark('lqhc_longhun')];
									}
									if (!event.str) {
										event.str = '英锐:移去任意枚<龙>标记,数值为0时视为不发动.';
									}
									event.dialog = ui.create.dialog(event.str);
									event.count = ui.create.control('-', event.num, '+');
									const newcount = event.count.cloneNode(true);
									const list = newcount.childNodes;
									list[0].style.width = '20px';
									list[0].onclick = function () {
										event.num--;
										if (event.num < event.arr[0]) {
											event.num = event.arr[1];
										}
										list[1].innerHTML = event.num;
									};
									list[1].style.width = '60px';
									list[1].onclick = function () {
										newcount.parentNode.removeChild(newcount);
										event.dialog.parentNode.removeChild(event.dialog);
										game.resume();
									};
									list[2].style.width = '20px';
									list[2].onclick = function () {
										event.num++;
										if (event.num > event.arr[1]) {
											event.num = event.arr[0];
										}
										list[1].innerHTML = event.num;
									};
									event.count.parentNode.replaceChild(newcount, event.count);
									ui.updatec();
									game.pause();
								} else {
									event.num = player.countMark('lqhc_longhun');
								}
								('step 1');
								player.removeMark('lqhc_longhun', event.num);
								if (event.num > 1) {
									event.num1 = event.num - 1;
								} else {
									event.num1 = 1;
								}
								('step 2');
								event.num1--;
								player.judge();
								('step 3');
								if (result.color == 'red') {
									player.gainMaxHp();
								} else if (player.maxHp == 1) {
									player.loseHp();
								} else {
									player.loseMaxHp();
								}
								if (event.num1 > 0) {
									event.goto(2);
								}
								('step 4');
								if (player.maxHp != event.count2 || player.hp != event.count1) {
									player.recover();
									player
										.chooseTarget('对一名其他角色造成一点火焰伤害.', function (card, player, target) {
											return target != player;
										})
										.set('ai', function (target) {
											return -get.attitude(_status.event.player, target);
										});
								} else {
									event.finish();
								}
								('step 5');
								if (result.targets?.length) {
									result.targets[0].damage('fire');
								}
							},
						},
						lqhc_jiyuan: {
							intro: {
								content: 'mark',
							},
							trigger: {
								player: 'useCardToPlayered',
								source: 'damageSource',
								global: 'phaseUseBegin',
							},
							filter(event, player, name) {
								if (name == 'damageSource') {
									return event.num > 0;
								}
								if (name == 'useCardToPlayered') {
									return event.target && event.target.hasMark('lqhc_jiyuan');
								}
								return event.player.hasMark('lqhc_jiyuan');
							},
							forced: true,
							content() {
								const name = event.triggername;
								if (name == 'damageSource') {
									trigger.player.addMark('lqhc_jiyuan');
								} else if (name == 'useCardToPlayered') {
									trigger.directHit.add(trigger.target);
								} else {
									const num = trigger.player.countMark('lqhc_jiyuan');
									player.damage(num, trigger.player);
									trigger.player.removeMark('lqhc_jiyuan', num);
								}
							},
						},
						lqhc_shenxiao: {
							intro: {
								content: 'mark',
							},
							audio: 'paoxiao',
							trigger: {
								player: ['useCard1', 'shaMiss'],
								source: 'damageBegin',
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'damageBegin') {
									return event.card && event.card.name == 'sha' && player.hasMark('lqhc_shenxiao');
								}
								if (name == 'useCard1') {
									return event.card && event.card.name == 'sha';
								}
								return true;
							},
							content() {
								const name = event.triggername;
								if (name == 'shaMiss') {
									player.addMark('lqhc_shenxiao');
								} else if (name == 'damageBegin') {
									trigger.num += player.countMark('lqhc_shenxiao');
									player.removeMark('lqhc_shenxiao', player.countMark('lqhc_shenxiao'));
								} else {
									if (!player.hasSkill('lqhc_shenxiao_1')) {
										player.addTempSkill('lqhc_shenxiao_1');
									} else {
										player.draw();
									}
								}
							},
							mod: {
								cardUsable(card, player, num) {
									if (card.name == 'sha') {
										return Infinity;
									}
								},
							},
							ai: {
								unequip: true,
								skillTagFilter(player, tag, arg) {
									if (!get.zhu(player, 'shouyue')) {
										return false;
									}
									if (arg && arg.name == 'sha') {
										return true;
									}
									return false;
								},
							},
							subSkill: {
								1: {
									charlotte: true,
									mod: {
										targetInRange(card, player) {
											if (card.name == 'sha') {
												return true;
											}
										},
									},
								},
							},
						},
						lqhc_xingtian: {
							juexingji: true,
							audio: 'retishen',
							derivation: 'lqhc_zhannu',
							trigger: {
								player: 'dying',
							},
							forced: true,
							content() {
								player.awakenSkill('lqhc_xingtian');
								player.removeSkill('lqhc_jiyuan');
								player.loseMaxHp();
								if (player.hp < 2) {
									player.recover(2 - player.hp);
								}
								game.countPlayer(function (current) {
									if (current.hasMark('lqhc_jiyuan')) {
										current.removeMark('lqhc_jiyuan', current.countMark('lqhc_jiyuan'));
										current.damage();
									}
								});
								player.addSkill('lqhc_zhannu');
							},
						},
						lqhc_zhannu: {
							mod: {
								cardname(card, player, name) {
									if (card.suit == 'spade' || card.name == 'taoyuan') {
										return 'juedou';
									}
								},
							},
							audio: 'retishen',
							trigger: {
								global: ['useCardToPlayer', 'damageBegin1'],
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'damageBegin1') {
									return event.card && event.card.name == 'juedou' && (event.player == player || event.source == player);
								}
								return event.card && event.card.name == 'juedou' && (event.targets.includes(player) || event.player == player);
							},
							content() {
								if (event.triggername == 'damageBegin1') {
									trigger.num++;
								} else {
									trigger.directHit.addArray(game.players);
								}
							},
						},
						lqtqx_quzuo: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								global: 'phaseBefore',
								player: 'enterGame',
							},
							forced: true,
							filter(event, player) {
								return (event.name != 'phase' || game.phaseNumber == 0) && !lib.inpile.includes('qizhengxiangsheng');
							},
							content() {
								game.addGlobalSkill('tianzuo_global');
								for (let i = 2; i < 10; i++) {
									const card = game.createCard2('qizhengxiangsheng', i % 2 ? 'club' : 'spade', i);
									ui.cardPile.insertBefore(card, ui.cardPile.childNodes[get.rand(0, ui.cardPile.childNodes.length)]);
								}
								game.broadcastAll(function () {
									lib.inpile.add('qizhengxiangsheng');
								});
								game.updateRoundNumber();
							},
							group: 'lqtqx_quzuo_compare',
							subSkill: {
								compare: {
									audio: 'lqtqx_quzuo',
									enable: 'phaseUse',
									usable: 1,
									filter(event, player) {
										return (
											player.countCards('he') > 0 &&
											game.hasPlayer(function (current) {
												return current.countCards('he') > 0 && player.canCompare(current);
											})
										);
									},
									filterTarget(card, target, player) {
										return player.canCompare(target);
									},
									content() {
										'step 0';
										player.chooseToCompare(target);
										('step 1');
										let t = target;
										if (result.bool) {
											t = player;
										}
										t.chooseUseTarget('qizhengxiangsheng', 'nopopup', true);
									},
									ai: {
										order: 5,
										result: {
											player: 1,
											target(player, target) {
												const num = target.countCards('h');
												if (num == 1) {
													return -1;
												}
												if (num == 2) {
													return -0.7;
												}
												return -0.5;
											},
										},
									},
								},
							},
						},
						lqtqx_jiece: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: { global: 'useCard' },
							forced: true,
							filter(event, player) {
								return (event.card && event.card.name == 'qizhengxiangsheng') || get.zhinangs().includes(event.card.name) || player.getStorage('lqtqx_dinghan').includes(event.card.name);
							},
							content() {
								'step 0';
								let num = 0;
								event.count = player.getStorage('lqtqx_dinghan').length;
								if (trigger.card.name == 'qizhengxiangsheng') {
									num++;
								}
								if (get.zhinangs().includes(trigger.card.name)) {
									num++;
								}
								if (player.getStorage('lqtqx_dinghan').includes(trigger.card.name)) {
									num++;
								}
								if (num > 0) {
									player.draw();
								}
								if (num > 1) {
									player.chooseTarget(true, '节策:令一名角色摸牌.').set('ai', function (target) {
										return get.attitude(_status.event.player, target);
									});
								}
								('step 1');
								if (result.targets?.length) {
									if (result.targets[0].countCards('h') < event.count) {
										result.targets[0].drawTo(event.count);
									} else {
										result.targets[0].draw();
									}
								}
							},
						},
						lqtqx_dinghan: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: { target: 'useCardToTarget' },
							forced: true,
							filter(event, player) {
								return get.type2(event.card) == 'trick' && !player.getStorage('lqtqx_dinghan').includes(event.card.name);
							},
							content() {
								player.markAuto('lqtqx_dinghan', [trigger.card.name]);
								game.log(player, '向定汉记录中添加了', '#y' + get.translation(trigger.card.name));
								trigger.targets.remove(player);
								trigger.parent.triggeredTargets2.remove(player);
								trigger.untrigger();
							},
							intro: { content: '已记录牌名:$' },
							group: ['lqtqx_dinghan_add', 'lqtqx_dinghan_count'],
							subSkill: {
								add: {
									enable: 'chooseToUse',
									audio: 'ext:浪琴天阙/audio:2',
									filter(event, player) {
										if (player.getStat('skill').lqtqx_dinghan_add_backup > 0 || !player.countCards('hes') || _status.currentPhase != player) {
											return false;
										}
										for (const i of lib.inpile) {
											if (get.type2(i) == 'trick' && event.filterCard && event.filterCard({ name: i }, player, event)) {
												return true;
											}
										}
										return false;
									},
									hiddenCard(player, name) {
										if (player.getStat('skill').lqtqx_dinghan_add_backup > 0) {
											return false;
										}
										return _status.currentPhase == player && get.type(name) == 'trick' && player.countCards('hes');
									},
									chooseButton: {
										dialog(event, player) {
											const list = [],
												list1 = [];

											for (const i of lib.inpile) {
												const name = i;
												if (get.type2(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
													if (player.getStorage('lqtqx_dinghan').includes(name)) {
														list.push(['锦囊', '', name]);
													} else {
														list1.push(['锦囊', '', name]);
													}
												}
											}

											const dialog = ui.create.dialog('<font size=6>选择一张牌使用', '<font size=3>', 'hidden');
											if (list.length) {
												dialog.add('已记录的牌');
												dialog.add([list, 'vcard']);
											}
											if (list1.length) {
												dialog.add('未记录的牌');
												dialog.add([list1, 'vcard']);
											}
											return dialog;
										},
										filter(button, player) {
											return _status.event.parent.filterCard({ name: button.link[2] }, player, _status.event.parent);
										},
										check(button) {
											if (_status.event.parent.type != 'phase') {
												return 1;
											}
											const player = _status.event.player;
											if (!player.getStorage('lqtqx_dinghan').includes(button.link[2])) {
												return 1;
											}
											return player.getUseValue({
												name: button.link[2],
												nature: button.link[3],
											});
										},
										backup(links, player) {
											return {
												filterCard: true,
												popname: true,
												selectCard: 1,
												check(card) {
													return 7 - get.value(card);
												},
												position: 'hes',
												viewAs: { name: links[0][2], nature: links[0][3] },
												onuse(result, player) {
													if (player.getStorage('lqtqx_dinghan').includes(result.card.name)) {
														player.unmarkAuto('lqtqx_dinghan', [result.card.name]);
														game.log(player, '从定汉记录中移除了', '#y' + get.translation(result.card.name));
													} else {
														player.storage.lqtqx_dinghan2 = result.card;
													}
												},
											};
										},
										prompt(links, player) {
											return '将一张牌当做' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]) + '使用';
										},
									},
									ai: {
										skillTagFilter(player) {
											if (!player.countCards('hes') || player.getStat('skill').lqtqx_dinghan_backup > 0) {
												return false;
											}
											return true;
										},
										order: 1,
										result: {
											player(player) {
												return 1;
											},
										},
									},
								},
								count: {
									trigger: {
										player: 'useCardAfter',
									},
									forced: true,
									popup: false,
									_priority: 200,
									filter(event, player) {
										return player.storage.lqtqx_dinghan2 == event.card;
									},
									content() {
										player.markAuto('lqtqx_dinghan', [trigger.card.name]);
										game.log(player, '向定汉记录中添加了', '#y' + get.translation(trigger.card.name));
										delete player.storage.lqtqx_dinghan2;
									},
								},
							},
						},
						lqhc_liaodi: {
							trigger: {
								player: ['loseAfter', 'gainAfter'],
							},
							filter(event, player, name) {
								if (name == 'loseAfter') {
									return event.type == 'discard' && event.getParent(2).name != 'lqhc_liaodi';
								}
								return event.parent.name != 'lqhc_liaodi';
							},
							forced: true,
							usable: 2,
							content() {
								'step 0';
								event.z = trigger.name == 'lose' ? '弃置' : '获得';
								player
									.chooseTarget(get.prompt('lqhc_liaodi'), '观看一名角色随机两张牌并' + event.z + '其中一张牌.', function (card, player, target) {
										return target.countCards('hej') > 0;
									})
									.set('ai', function (target) {
										return -get.attitude(_status.event.player, target);
									});
								('step 1');
								if (result.targets?.length) {
									const target = result.targets[0];
									event.target = target;
									player.line(target);
									const u = player.chooseButton(true, ['选择一张牌' + event.z, target.getCards('hej').randomGets(2)]);
									u.set('target', target);
									u.set('ai', function (button) {
										return get.value(button.link);
									});
								} else {
									event.finish();
								}
								('step 2');
								if (result.links?.length) {
									trigger.name == 'lose' ? event.target.discard(result.links) : player.gain(result.links, 'draw2');
								}
							},
						},
						lqhc_zhisheng: {
							trigger: {
								player: 'useCardToBefore',
							},
							forced: true,
							filter(event, player) {
								return event.target == player && _status.currentPhase == player;
							},
							content() {
								player.draw();
							},
							group: 'lqhc_zhisheng_discard',
							subSkill: {
								discard: {
									trigger: {
										player: 'useCardToBefore',
									},
									forced: true,
									filter(event, player) {
										return (
											_status.currentPhase == player &&
											event.target &&
											event.target != player &&
											game.hasPlayer(function (current) {
												return current != player && current.countCards('he');
											})
										);
									},
									content() {
										'step 0';
										player
											.chooseTarget(get.prompt('lqhc_zhisheng'), '弃置一名其他角色一张牌.', function (card, player, target) {
												return target != player && target.countCards('hej') > 0;
											})
											.set('ai', function (target) {
												return -get.attitude(_status.event.player, target);
											});
										('step 1');
										if (result.targets?.length) {
											player.discardPlayerCard(result.targets[0], true, 'he');
										}
									},
								},
							},
						},
						lqhc_guicai: {
							audio: 'reguicai',
							trigger: { global: 'judge' },
							forced: true,
							content() {
								'step 0';
								player
									.chooseControl('选项一', '选项二', '背水!', 'cancel2')
									.set('choiceList', [get.translation(trigger.player) + '的' + (trigger.judgestr || '') + '判定为' + get.translation(trigger.player.judging[0]) + ',你可用牌堆顶的牌或打出一张牌代替之', '令' + get.translation(trigger.player) + '的' + (trigger.judgestr || '') + '判定' + get.translation(trigger.player.judging[0]) + '不可被更改', '背水!依次执行上述所有选项后,选择一名角色对你造成一点伤害.'])
									.set('prompt', get.prompt('lqhc_guicai', _status.event.player.player))
									.set('ai', function () {
										return 0;
									});
								('step 1');
								event.count = result.control;
								if (event.count == '选项一' || event.count == '背水!') {
									const list = ['牌堆顶的牌', get.cards()];
									if (player.countCards('h')) {
										list.push('你的手牌');
										list.push(player.getCards('h'));
									}
									if (player.countCards('e')) {
										list.push('你的装备区');
										list.push(player.getCards('e'));
									}
									if (player.countCards('s')) {
										list.push('你的S区(例如牛马、锦帆)');
										list.push(player.getCards('s'));
									}
									const u = player.chooseButton(list);
									u.set('ai', function (button) {
										const card = button.link,
											trigger = _status.event.getTrigger(),
											player = _status.event.player,
											judging = _status.event.judging,
											result = trigger.judge(card) - trigger.judge(judging),
											attitude = get.attitude(player, trigger.player);
										return result * attitude;
									});
									u.set('filterButton', function (button) {
										const card = button.link,
											player = _status.event.player,
											mod = game.checkMod(card, player, 'unchanged', 'cardRespondable', player),
											mod2 = game.checkMod(card, player, 'unchanged', 'cardEnabled2', player);
										if (mod2 != 'unchanged') {
											return mod2;
										}
										if (mod != 'unchanged') {
											return mod;
										}
										return true;
									});
									u.set('judging', trigger.player.judging[0]);
								} else {
									event.goto(4);
								}
								('step 2');
								if (result.links?.length) {
									player.respond(result.links, 'lqhc_guicai', 'highlight', 'noOrdering');
									result.cards = result.links;
									event.card = result.cards[0];
								} else {
									event.x = 1;
									event.goto(4);
								}
								('step 3');
								if (result.bool) {
									if (trigger.player.judging[0].clone) {
										trigger.player.judging[0].clone.classList.remove('thrownhighlight');
										game.broadcast(function (card) {
											if (card.clone) {
												card.clone.classList.remove('thrownhighlight');
											}
										}, trigger.player.judging[0]);
										game.addVideo('deletenode', player, get.cardsInfo([trigger.player.judging[0].clone]));
									}
									game.cardsDiscard(trigger.player.judging[0]);
									trigger.player.judging[0] = result.cards[0];
									trigger.orderingCards.addArray(result.cards);
									game.log(trigger.player, '的判定牌改为', result.cards[0]);
								}
								('step 4');
								if (event.count == '选项二' || event.count == '背水!') {
									player.storage.lqhc_guicaiskill = [];
									player.storage.lqhc_guicaiskill2 = [];
									for (const i in lib.skill) {
										if (lib.skill[i].trigger) {
											if (lib.skill[i].trigger.global) {
												var j = lib.skill[i].trigger.global;
												if (typeof j == 'string') {
													if (j == 'judge') {
														player.storage.lqhc_guicaiskill.push(i);
														player.storage.lqhc_guicaiskill2.push(lib.skill[i].trigger);
														lib.skill[i].trigger = {};
														lib.skill[i].trigger.global = '';
													}
												} else {
													for (var item of j) {
														if (item == 'judge') {
															player.storage.lqhc_guicaiskill.push(i);
															player.storage.lqhc_guicaiskill2.push(lib.skill[i].trigger);
															lib.skill[i].trigger = {};
															lib.skill[i].trigger.global = '';
														}
													}
												}
											}
											if (lib.skill[i].trigger.player) {
												var j = lib.skill[i].trigger.player;
												if (typeof j == 'string') {
													if (j == 'judge') {
														player.storage.lqhc_guicaiskill.push(i);
														player.storage.lqhc_guicaiskill2.push(lib.skill[i].trigger);
														lib.skill[i].trigger = {};
														lib.skill[i].trigger.player = '';
													}
												} else {
													for (var item of j) {
														if (item == 'judge') {
															player.storage.lqhc_guicaiskill.push(i);
															player.storage.lqhc_guicaiskill2.push(lib.skill[i].trigger);
															lib.skill[i].trigger = {};
															lib.skill[i].trigger.player = '';
														}
													}
												}
											}
										}
									}
								} else {
									event.finish();
								}
								('step 5');
								if (event.count == '背水!') {
									player.chooseTarget(true, '令一名角色对你造成一点伤害.').set('ai', function (target) {
										return -get.attitude(_status.event.player, target);
									});
								}
								('step 6');
								if (result.targets?.length) {
									player.damage(result.targets[0]);
								}
							},
							group: 'lqhc_guicai_count',
							subSkill: {
								count: {
									trigger: { global: 'judgeBegin' },
									popup: false,
									forced: true,
									filter(event, player) {
										return player.storage.lqhc_guicaiskill && player.storage.lqhc_guicaiskill2;
									},
									content() {
										for (let i = 0; i < player.storage.lqhc_guicaiskill.length; i++) {
											if (player.storage.lqhc_guicaiskill[i]) {
												lib.skill[player.storage.lqhc_guicaiskill[i]].trigger = player.storage.lqhc_guicaiskill2[i];
											}
										}
										delete player.storage.lqhc_guicaiskill;
										delete player.storage.lqhc_guicaiskill2;
									},
								},
							},
							mark: true,
							intro: {
								mark(dialog, content, player) {
									if (player != game.me) {
										return get.translation(player) + '观看牌堆中...';
									}
									if (get.itemtype(_status.pileTop) != 'card') {
										return '牌堆顶无牌';
									}
									dialog.add([_status.pileTop]);
								},
							},
						},
						lqhc_langgu: {
							audio: 'refankui',
							trigger: {
								player: 'damageEnd',
								source: 'damageEnd',
							},
							check(event, player) {
								//触发技ai
								if (event.player == player) {
									return get.attitude(player, event.source) < 1;
								}
								return get.attitude(player, event.player) < 1;
							},
							content() {
								'step 0';
								event.count = Math.min(trigger.num, 9);
								('step 1');
								event.count--;
								player.judge(function (card) {
									if (card.suit == 'club') {
										if (trigger.source && (trigger.source != player || (trigger.source == player && trigger.player != player))) {
											return 2;
										} else {
											return 1;
										}
									}
									if (card.suit == 'spade' && trigger.source && (trigger.source != player || (trigger.source == player && trigger.player != player))) {
										return 1.7;
									}
									if (['heart', 'diamond'].includes(card.suit) && trigger.source && (trigger.source != player || (trigger.source == player && trigger.player != player))) {
										return 1.4;
									}
									return 0;
								});
								('step 2');
								event.ta = trigger.source;
								if (trigger.source == player) {
									event.ta = trigger.player;
								}
								if (result.suit == 'club') {
									player.draw();
									if (event.ta) {
										player.discardPlayerCard(event.ta, 'he');
									}
								}
								if (result.suit == 'spade' && event.ta) {
									event.ta.damage('thunder', 'nosource');
								}
								if (['heart', 'diamond'].includes(result.suit) && event.ta) {
									player.gainPlayerCard(true, event.ta, 'he');
								}
								('step 3');
								if (event.count > 0) {
									player.chooseBool(get.prompt2('lqhc_langgu'));
								} else {
									event.finish();
								}
								('step 4');
								if (result.bool) {
									event.goto(1);
								}
							},
						},
						lqhc_zhabing: {
							limited: true,
							trigger: {
								player: ['dying', 'phaseJieshuBegin'],
							},
							audio: 'lianpo',
							check(event, player) {
								if (event.name != 'dying') {
									return player.hp < 2;
								}
								return true;
							},
							content() {
								'step 0';
								player.awakenSkill('lqhc_zhabing');
								event.current = player.next;
								('step 1');
								if (!event.current.countCards('h')) {
									event.goto(3);
								} else {
									event.current.chooseCard('交给' + get.translation(player) + '一张牌', 'h', true).set('ai', function (card) {
										const evt = _status.event.parent;
										if (get.attitude(_status.event.player, evt.player) > 0) {
											if (card.name == 'jiu') {
												return 120;
											}
											if (card.name == 'tao') {
												return 110;
											}
										}
										return 100 - get.value(card);
									});
								}
								('step 2');
								if (result.cards?.length) {
									player.gain(result.cards, event.current, 'giveAuto');
								}
								('step 3');
								event.current = event.current.next;
								if (event.current != player) {
									event.goto(1);
								} else {
									player.addTempSkill('lqhc_zhabing_cancel', { player: 'phaseBefore' });
								}
							},
							subSkill: {
								cancel: {
									trigger: {
										player: ['damageBegin4', 'loseHpBegin'],
									},
									_priority: -Infinity,
									forced: true,
									charlotte: true,
									filter(event, player, name) {
										let num2 = event.num - player.hujia || 0;
										if (name == 'loseHpBegin') {
											num2 = event.num;
										}
										if (player.hp - num2 < 1) {
											return true;
										}
										return false;
									},
									content() {
										let num2 = player.hp + player.hujia || 0;
										if (trigger.name == 'loseHp') {
											num2 = player.hp;
										}
										trigger.num = num2 - 1;
									},
								},
							},
						},
						lqhc_polu: {
							trigger: {
								global: ['phaseJudgeSkipped', 'phaseDrawSkipped', 'phaseUseSkipped', 'phaseDiscardSkipped', 'phaseJudgeCancelled', 'phaseDrawCancelled', 'phaseUseCancelled', 'phaseDiscardCancelled'],
							},
							forced: true,
							audio: 'polu',
							content() {
								'step 0';
								player.draw();
								('step 1');
								event.card = result.cards[0];
								if (Array.isArray(result) && result.length && get.color(result[0]) == 'black') {
									player
										.chooseBool('是否展示' + get.translation(result) + '并对' + get.translation(trigger.player) + '造成一点伤害.')
										.set('ai', function () {
											return _status.event.goon;
										})
										.set('goon', get.attitude(player, trigger.player) < 1);
								}
								('step 2');
								if (result.bool) {
									player.showCards(event.card);
									trigger.player.damage();
								}
							},
						},
						lqhc_juezan: {
							audio: 'choulve',
							trigger: {
								player: 'damageEnd',
							},
							forced: true,
							filter(event, player) {
								return event.num > 0;
							},
							content() {
								'step 0';
								event.count = Math.min(trigger.num, 9);
								('step 1');
								event.count--;
								player.draw(2);
								('step 2');
								let list = [],
									list1 = 0;
								for (const i of player.getExpansions('lqhc_juezan')) {
									list.add(i.suit);
								}
								for (const i of player.getCards('he')) {
									if (!list.includes(i.suit)) {
										list1++;
									}
								}
								if (list1 > 0) {
									player
										.chooseCard('he', '将一张牌置于武将牌上作为<堑>', function (card, player, target) {
											const list = [];
											for (const i of player.getExpansions('lqhc_juezan')) {
												list.add(i.suit);
											}
											return !list.includes(card.suit);
										})
										.set('ai', function (card) {
											return 7 - get.value(card);
										});
								} else {
									event.goto(4);
								}
								('step 3');
								if (result.cards?.length) {
									player.addToExpansion(result.cards, player, 'giveAuto').gaintag.add('lqhc_juezan');
								}
								('step 4');
								if (event.count > 0) {
									player.chooseBool(get.prompt2('lqhc_juezan')).set('frequentSkill', 'lqhc_juezan');
								} else {
									event.finish();
								}
								('step 5');
								if (result.bool) {
									event.goto(1);
								}
							},
							intro: {
								content: 'expansion',
								markcount: 'expansion',
							},
							onremove(player, skill) {
								const cards = player.getExpansions('lqhc_juezan');
								if (cards.length) {
									player.loseToDiscardpile(cards);
								}
							},
							ai: {
								maixie: true,
								maixie_hp: true,
								threaten: 0.8,
								effect: {
									target(card, player, target) {
										if (get.tag(card, 'damage')) {
											if (player.hasSkillTag('jueqing', false, target)) {
												return [1, -2];
											}
											if (!target.hasFriend()) {
												return;
											}
											if (target.hp >= 4) {
												return [0.5, get.tag(card, 'damage') * 2];
											}
											if (target.hp == 3) {
												return [0.5, get.tag(card, 'damage') * 1.5];
											}
											if (target.hp == 2) {
												return [1, get.tag(card, 'damage') * 0.5];
											}
										}
									},
								},
							},
							group: 'lqhc_juezan_skipped',
							subSkill: {
								skipped: {
									trigger: {
										global: 'phaseBegin',
									},
									forced: true,
									filter(event, player) {
										return player.getExpansions('lqhc_juezan').length;
									},
									content() {
										'step 0';
										player.chooseCardButton([1, 4], '是否发动【倔堑】？<br><font size=4>弃置任意张<堑>,令' + get.translation(trigger.player) + '跳过对应阶段.</font><br><font size=3><b>♠️️️️:跳过判定//♣️️️️:跳过摸牌//♦️️️️:跳过出牌//♥️️️️:跳过弃牌', player.getExpansions('lqhc_juezan')).ai = function (button) {
											const att = get.attitude(_status.event.player, trigger.player),
												suit = button.link.suit;
											if (att > 0 && trigger.player.countCards('j') && trigger.player.countCards('h') - trigger.player.getHandcardLimit() > 2) {
												return suit == 'spade' || suit == 'heart';
											}
											if (att > 0 && trigger.player.countCards('j')) {
												return suit == 'spade';
											}
											if (att > 0 && trigger.player.countCards('h') - trigger.player.getHandcardLimit() > 2) {
												return suit == 'heart';
											}
											if (att < 1) {
												return suit == 'diamond' || suit == 'club';
											}
											return false;
										};
										('step 1');
										if (result.links?.length) {
											player.loseToDiscardpile(result.links);
											for (const i of result.links) {
												const suit = i.suit;
												if (suit == 'spade') {
													trigger.player.skip('phaseJudge');
												}
												if (suit == 'club') {
													trigger.player.skip('phaseDraw');
												}
												if (suit == 'diamond') {
													trigger.player.skip('phaseUse');
												}
												if (suit == 'heart') {
													trigger.player.skip('phaseDiscard');
												}
											}
										}
									},
								},
							},
						},
						lqhc_choulue: {
							audio: 'choulve',
							trigger: {
								global: 'phaseJieshuBegin',
							},
							round: 1,
							filter(event, player) {
								return event.player.countCards('h') > player.countCards('h');
							},
							logTarget(event, player) {
								return [event.player];
							},
							content() {
								'step 0';
								event.count = player;
								trigger.player
									.chooseCard('交给' + get.translation(player) + '一张牌,否则' + get.translation(player) + '摸一张牌.', 'he')
									.set('ai', function (card) {
										const list = [];
										for (const i of event.count.getExpansions('lqhc_juezan')) {
											list.add(i.suit);
										}
										if (list.includes(card.suit) && _status.event.goon) {
											return 1000 - get.value(card);
										}
										return false;
									})
									.set('goon', get.attitude(player, trigger.player) < 0);
								('step 1');
								if (result.cards?.length) {
									player.gain(result.cards, trigger.player, 'giveAuto');
									event.finish();
								} else {
									player.draw();
								}
								('step 2');
								let list = [],
									list1 = 0;
								for (const i of player.getExpansions('lqhc_juezan')) {
									list.add(i.suit);
								}
								for (const i of player.getCards('he')) {
									if (!list.includes(i.suit)) {
										list1++;
									}
								}
								if (list1 > 0) {
									player
										.chooseCard('he', '将一张牌置于武将牌上作为<堑>', function (card, player, target) {
											const list = [];
											for (const i of player.getExpansions('lqhc_juezan')) {
												list.add(i.suit);
											}
											return !list.includes(card.suit);
										})
										.set('ai', function (card) {
											return 7 - get.value(card);
										});
								} else {
									event.finish();
								}
								('step 3');
								if (result.cards?.length) {
									player.addToExpansion(result.cards, player, 'giveAuto').gaintag.add('lqhc_juezan');
								}
							},
						},
						lqhc_jueyuan: {
							trigger: {
								target: 'useCardToTargeted',
							},
							forced: true,
							filter(event, player) {
								return event.card && event.card.name == 'sha' && !event.card.nature;
							},
							content() {
								trigger.cancel();
							},
							group: 'lqhc_jueyuan_sha',
							subSkill: {
								sha: {
									audio: true,
									enable: ['chooseToUse', 'chooseToRespond'],
									prompt() {
										return '将一张【闪】当作【杀】使用或打出';
									},
									position: 'hs',
									check(card, event) {
										return 10 - get.value(card);
									},
									selectCard: 1,
									viewAs: { name: 'sha' },
									viewAsFilter(player) {
										return player.countCards('hes', 'shan') > 0;
									},
									filterCard(card, player) {
										return card.name == 'shan';
									},
								},
							},
						},
						lqhc_yaoli: {
							trigger: {
								player: 'useCard',
							},
							prompt: '是否发动【妖力】？',
							prompt2(event) {
								return '令' + get.translation(event.card) + '在本次的使用中结算两次.';
							},
							check(event, player) {
								if (event.card.name == 'tiesuo') {
									return false;
								}
								return true;
							},
							filter(event, player) {
								return get.type(event.card) == 'trick';
							},
							content() {
								trigger.effectCount++;
							},
						},
						lqhc_xinshang: {
							limited: true,
							enable: 'phaseUse',
							filterTarget: true,
							content() {
								'step 0';
								player.awakenSkill('lqhc_xinshang');
								player.turnOver();
								('step 1');
								if (target.name2 != undefined) {
									player.chooseControl(target.name1, target.name2).set('prompt', '选择一张武将牌变成「东方月初」.');
								} else {
									event._result = { control: target.name };
								}
								('step 2');
								const num = target.hp,
									num1 = target.maxHp;
								target.reinit(result.control, 'lqhc_dongfangyuechu');
								target.maxHp = num1;
								target.hp = num;
								target.update();
								game.log(target, '将「', '#g' + get.translation(result.control), '」切换为「东方月初」.');
								if (_status.characterlist) {
									_status.characterlist.add(result.control);
									_status.characterlist.remove('lqhc_dongfangyuechu');
								}
							},
							ai: {
								order: 10,
								result: {
									target: 1,
								},
							},
						},
						lqhc_yanleiH: {
							enable: 'phaseUse',
							filter(event, player) {
								return game.hasPlayer(function (current) {
									return current != player && [current.name, current.name1, current.name2].includes('lqhc_dongfangyuechu');
								});
							},
							filterTarget(card, player, target) {
								if (player == target) {
									return false;
								}
								if (!ui.selected.targets.length) {
									return [target.name, target.name1, target.name2].includes('lqhc_dongfangyuechu');
								}
								return true;
							},
							targetprompt: ['交换位置', '剥夺技能'],
							selectTarget: 2,
							usable: 1,
							multitarget: true,
							multiline: true,
							content() {
								game.broadcastAll(
									function (target1, target2) {
										game.swapSeat(target1, target2);
									},
									player,
									targets[0],
								);
								for (const i of targets[1].getSkills(true, false)) {
									player.addTempSkill(i);
									targets[0].addTempSkill(i, { player: 'phaseAfter' });
								}
								targets[1].addTempSkill('baiban');
							},
							ai: {
								order: 10,
								result: {
									player: 1,
									target(player, target) {
										if (!ui.selected.targets.length) {
											return 1;
										}
										return -1;
									},
								},
							},
						},
						lqhc_shenhuo: {
							enable: 'phaseUse',
							filterCard(card, player, target) {
								return get.subtype(card) == 'equip1' && !ui.selected.targets.length && !card.storage.lqhc_shenhuo;
							},
							usable: 1,
							selectCard(card, player, target) {
								if (ui.selected.targets.length) {
									return 0;
								}
								return [0, 1];
							},
							selectTarget(card, player, target) {
								if (ui.selected.cards.length) {
									return -1;
								} //已选择
								return 1;
							},
							discard: false,
							lose: false,
							delay: 0,
							position: 'he',
							filterTarget(card, player, target) {
								return player != target && !ui.selected.cards.length && !target.hasSkill('lqhc_shenhuo_fire');
							},
							prompt(event) {
								return '炼化一张武将牌或令一名角色进入烧伤状态.';
							},
							check(card) {
								if (get.position(card) == 'e') {
									return 100 - get.value(card);
								}
								return false;
							},
							content() {
								if (cards.length) {
									const name1 = cards[0].name,
										name = 'lqhc_' + name1;
									if (!lib.card[name]) {
										lib.card[name] = lib.card[name1];
										lib.card[name].skills.push('lqhc_shenhuo_direct');
										lib.translate[name + '_info'] = lib.translate[name1 + '_info'] + '你的【杀】不能被响应.';
										lib.card[name].cardimage = name1;
										let links = get.translation(cards[0].name),
											str = '炼';

										for (const i of links) {
											str += get.translation(i);
										}

										lib.translate[name] = str;
									}
									cards[0].init(game.createCard(name, cards[0].suit, cards[0].number));
									cards[0].storage.lqhc_shenhuo = true;
								}
								if (targets.length) {
									targets[0].addSkill('lqhc_shenhuo_fire');
								}
							},
							ai: {
								order: 5,
								result: {
									target: -1,
								},
							},
							subSkill: {
								fire: {
									trigger: { player: 'phaseUseBegin' },
									forced: true,
									charlotte: true,
									content() {
										'step 0';
										player.removeSkill('lqhc_shenhuo_fire');
										player.chooseToDiscard(3, 'he', '烧伤状态:弃置三张牌,否则失去一点体力.').set('ai', function (card) {
											return 7 - get.value(card);
										});
										('step 1');
										if (!result.bool) {
											player.loseHp();
										}
									},
									mark: true,
									intro: {
										content: '出牌阶段开始时,你选择三张牌弃置,否则你失去一点体力.',
									},
								},
								direct: {
									trigger: {
										player: 'useCard2',
									},
									forced: true,
									popup: false,
									filter(event, player) {
										return event.card && event.card.name == 'sha';
									},
									content() {
										trigger.directHit.addArray(
											game.filterPlayer(function (current) {
												return current != player;
											}),
										);
									},
								},
							},
						},
						lqhc_daofa: {
							enable: 'phaseUse',
							usable: 1,
							filterTarget(card, player, target) {
								return (target.countCards('h') > target.hp && target.isDamaged()) || target.countCards('h') < target.maxHp;
							},
							filter(event, player) {
								return game.hasPlayer(function (current) {
									return (current.countCards('h') > current.hp && current.isDamaged()) || current.countCards('h') < current.maxHp;
								});
							},
							content() {
								'step 0';
								if (target.countCards('h') > target.hp && target.countCards('h') < target.maxHp) {
									const num = target.countCards('h') - target.hp,
										num1 = target.maxHp - target.countCards('h');
									player.chooseControl('摸牌', '回复体力').set('prompt', '令' + get.translation(target) + '摸' + num1 + '张牌或回复' + num + '点体力.');
								} else if (target.countCards('h') > target.hp) {
									event._result = { control: '回复体力' };
								} else {
									event._result = { control: '摸牌' };
								}
								('step 1');
								if (result.control == '摸牌') {
									target.drawTo(target.maxHp);
								} else {
									target.recover(target.countCards('h') - target.hp);
								}
							},
							ai: {
								order: 10,
								result: {
									target: 1,
								},
							},
						},
						lqhc_jizhi: {
							intro: {
								content: '发动次数:#',
							},
							enable: 'chooseToUse',
							filter(event, player) {
								for (const i of lib.inpile) {
									if (get.type(i) == 'trick' && event.filterCard && event.filterCard({ name: i }, player, event) && player.countMark('lqhc_jizhi') < 2) {
										return true;
									}
								}
								return false;
							},
							hiddenCard(player, name) {
								return get.type(name) == 'trick' && player.countMark('lqhc_jizhi') < 2;
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (get.type(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										}
									}

									return ui.create.dialog('机智', [list, 'vcard']);
								},
								filter(button, player) {
									return _status.event.parent.filterCard({ name: button.link[2] }, player, _status.event.parent);
								},
								check(button) {
									if (_status.event.parent.type != 'phase') {
										return 1;
									}
									const player = _status.event.player;
									return player.getUseValue({
										name: button.link[2],
										nature: button.link[3],
									});
								},
								backup(links, player) {
									return {
										viewAs: {
											name: links[0][2],
										},
										filterCard: () => false,
										selectCard: -1,
										popname: true,
										onuse(result, player) {
											player.addMark('lqhc_jizhi');
										},
									};
								},
								prompt(links, player) {
									return '视为使用一张' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]);
								},
							},
							ai: {
								skillTagFilter(player) {
									if (player.countMark('lqhc_jizhi') > 1) {
										return false;
									}
									return true;
								},
								order: 1,
								result: {
									player: 1,
								},
							},
							group: 'lqhc_jizhi_count',
						},
						lqhc_jizhi_count: {
							trigger: {
								player: ['phaseAfter', 'phaseBegin'],
							},
							forced: true,
							popup: false,
							_priority: 200000,
							filter(event, player) {
								return player.hasMark('lqhc_jizhi');
							},
							content() {
								player.removeMark('lqhc_jizhi', player.countMark('lqhc_jizhi'));
							},
						},
						lqhc_yanleiC: {
							enable: 'phaseUse',
							usable: 1,
							filterTarget(card, player, target) {
								return target != player && ![target.name, target.name1, target.name2].includes('lqhc_tushanhonghong');
							},
							filter(event, player) {
								return (
									!game.hasPlayer(function (current) {
										return [current.name, current.name1, current.name2].includes('lqhc_tushanhonghong');
									}) || !player.hasSkill('lqhc_yanleiC_kong')
								);
							},
							prompt() {
								if (
									!game.hasPlayer(function (current) {
										return [current.name, current.name1, current.name2].includes('lqhc_tushanhonghong');
									}) &&
									!_status.event.player.hasSkill('lqhc_yanleiC_kong')
								) {
									return '令一名角色与你交换位置或变成「涂山红红」.';
								}
								if (_status.event.player.hasSkill('lqhc_yanleiC_kong')) {
									return '令一名角色变成「涂山红红」.';
								}
								return '与一名角色交换位置.';
							},
							content() {
								'step 0';
								if (
									!game.hasPlayer(function (current) {
										return [current.name, current.name1, current.name2].includes('lqhc_tushanhonghong');
									}) &&
									!player.hasSkill('lqhc_yanleiC_kong')
								) {
									player.chooseControl('交换位置', '切换武将牌').set('prompt', '令' + get.translation(target) + '与你交换位置或变成「涂山红红」.');
								} else if (player.hasSkill('lqhc_yanleiC_kong')) {
									event._result = { control: '切换武将牌' };
								} else {
									event._result = { control: '交换位置' };
								}
								('step 1');
								if (result.control == '交换位置') {
									game.broadcastAll(
										function (target1, target2) {
											game.swapSeat(target1, target2);
										},
										player,
										target,
									);
									target.addTempSkill('baiban', { player: 'phaseAfter' });
									player.addTempSkill('lqhc_yanleiC_kong');
									event.finish();
								} else {
									player.removeSkill('lqhc_yanleiC');
									if (target.name2 != undefined) {
										player.chooseControl(target.name1, target.name2).set('prompt', '选择一张武将牌变成「涂山红红」.');
									} else {
										event._result = { control: target.name };
									}
								}
								('step 2');
								const num = target.hp,
									num1 = target.maxHp;
								target.reinit(result.control, 'lqhc_tushanhonghong');
								target.maxHp = num1;
								target.hp = num;
								target.update();
								game.log(target, '将「', '#g' + get.translation(result.control), '」切换为「涂山红红」.');
								if (_status.characterlist) {
									_status.characterlist.add(result.control);
									_status.characterlist.remove('lqhc_tushanhonghong');
								}
							},
							ai: {
								order: 5,
								result: {
									target(player, target) {
										if (
											!game.hasPlayer(function (current) {
												return [current.name, current.name1, current.name2].includes('lqhc_tushanhonghong');
											}) &&
											!player.hasSkill('lqhc_yanleiC_kong')
										) {
											return -1;
										}
										if (player.hasSkill('lqhc_yanleiC_kong')) {
											return 1;
										}
										return -1;
									},
								},
							},
							subSkill: {
								kong: {},
							},
						},
						lqhc_tiaobo: {
							enable: 'phaseUse',
							filter(event, player) {
								return game.hasPlayer(function (current) {
									return current.countCards('hej') && current != player;
								});
							},
							filterTarget(card, player, target) {
								if (!ui.selected.targets.length) {
									return target.countCards('hej');
								}
								return ui.selected.targets[0].canUse({ name: 'juedou' }, target);
							},
							targetprompt: ['被获得财', '被决斗'],
							selectTarget: 2,
							usable: 1,
							multitarget: true,
							multiline: true,
							content() {
								'step 0';
								player.choosePlayerCard(targets[0], 'hej', true).ai = get.buttonValue;
								('step 1');
								if (result.links?.length) {
									player.addToExpansion(result.links[0], 'giveAuto', targets[0]).gaintag.add('lqhc_tiaobo');
								}
								const next = targets[0].useCard({ name: 'juedou' }, targets[1], 'noai');
								event.card = next.card;
								('step 2');
								game.countPlayer(function (current) {
									if (
										current.hasHistory('damage', function (evt) {
											return evt.card == event.card;
										}) &&
										current.countGainableCards(player, 'he')
									) {
										player.gainPlayerCard(2, 'he', current, true);
									}
								}); //可获得
								('step 3');
								if (result.cards?.length) {
									player
										.chooseCard('h', '将一张获得的牌置于武将牌上作为<财>', true, function (card, player, target) {
											return result.cards.includes(card);
										})
										.set('ai', function (card) {
											return 100 - get.value(card);
										});
								}
								('step 4');
								if (result.cards?.length) {
									player.addToExpansion(result.cards, player, 'giveAuto').gaintag.add('lqhc_tiaobo');
								}
							},
							intro: {
								content: 'expansion',
								markcount: 'expansion',
							},
							onremove(player, skill) {
								const cards = player.getExpansions('lqhc_tiaobo');
								if (cards.length) {
									player.loseToDiscardpile(cards);
								}
							},
							ai: {
								order: 10,
								result: {
									target: -1,
								},
							},
						},
						lqhc_liyou: {
							mod: {
								maxHandcard(player, num) {
									return num + player.getExpansions('lqhc_tiaobo').length;
								},
							},
							trigger: {
								global: 'damageBegin2',
							},
							forced: true,
							filter(event, player, name) {
								return event.num > 0 && player.getExpansions('lqhc_tiaobo').length;
							},
							content() {
								'step 0';
								if (trigger.source && trigger.source == player) {
									player.chooseControlList('是否发动【利诱】？', ['将一张<财>交给' + get.translation(trigger.player) + ',取消其即将受到的伤害.', '令伤害加' + player.getExpansions('lqhc_tiaobo').length + '.'], function () {
										if (get.attitude(_status.event.player, trigger.player) > 0) {
											return 0;
										}
										return 1;
									});
								} else {
									event.goto(3);
								}
								('step 1');
								if (result.index == 0) {
									player.chooseCardButton(true, '将一张<财>交给' + get.translation(trigger.player) + '并取消其即将受到的伤害.', player.getExpansions('lqhc_tiaobo')).ai = function (button) {
										return get.value(button.link);
									};
								} else if (result.index == 1) {
									trigger.num += player.getExpansions('lqhc_tiaobo').length;
									event.finish();
								} else {
									event.finish();
								}
								('step 2');
								if (result.links?.length) {
									trigger.player.gain(result.links[0], 'draw2');
									trigger.cancel();
									event.finish();
								}
								('step 3');
								const next = player.chooseCardButton('###是否发动【利诱】？###将一张<财>交给' + get.translation(trigger.player) + '并取消其即将受到的伤害.', player.getExpansions('lqhc_tiaobo'));
								next.set('goon', get.attitude(player, trigger.player) > 0);
								next.set('ai', function (button) {
									if (!_status.event.goon) {
										return 0;
									}
									return get.value(button.link);
								});
								('step 4');
								if (result.links?.length) {
									trigger.player.gain(result.links[0], 'draw2');
									trigger.cancel();
								}
							},
							group: ['lqhc_liyou_gain'],
							subSkill: {
								gain: {
									enable: 'phaseUse',
									filter(event, player) {
										return player.getExpansions('lqhc_tiaobo').length && player.countCards('h');
									},
									content() {
										'step 0';
										const cards = player.getExpansions('lqhc_tiaobo');
										const next = player.chooseToMove('利诱:交换<财>和手牌？');
										next.set('list', [
											[get.translation(player) + '(你)的<财>', cards],
											['手牌区', player.getCards('h')],
										]);
										next.set('filterMove', function (from, to) {
											return typeof to != 'number';
										});
										next.set('processAI', function (list) {
											const player = _status.event.player,
												cards = list[0][1].concat(list[1][1]).sort(function (a, b) {
													return get.value(a) - get.value(b);
												}),
												cards2 = cards.splice(0, player.getExpansions('lqhc_tiaobo').length);
											return [cards2, cards];
										});
										('step 1');
										if (result.bool) {
											const pushs = result.moved[0],
												gains = result.moved[1];
											pushs.removeArray(player.getExpansions('lqhc_tiaobo'));
											gains.removeArray(player.getCards('h'));
											if (!pushs.length || pushs.length != gains.length) {
												return;
											}
											player.addToExpansion(pushs).gaintag.add('lqhc_tiaobo');
											player.gain(gains, 'gain2', 'log');
										}
									},
								},
							},
							ai: {
								order: 10,
								result: {
									player(player) {
										const num = player.getStat('skill').lqhc_liyou_gain || 0;
										if (num > 0) {
											return -1;
										}
										return 1;
									},
								},
							},
						},
						lqhc_liuji: { global: 'lqhc_liuji2' },
						lqhc_liuji2: {
							enable: 'phaseUse',
							filter(event, player) {
								return (
									game.hasPlayer(function (current) {
										return current.hasSkill('lqhc_liuji');
									}) && player.countCards('h')
								);
							},
							filterTarget(card, player, target) {
								return target.hasSkill('lqhc_liuji');
							},
							selectTarget(card, player, target) {
								if (
									game.filterPlayer(function (current) {
										return current.hasSkill('lqhc_liuji');
									}).length > 1
								) {
									return 1;
								}
								return -1;
							},
							prompt() {
								const list = game.filterPlayer(function (current) {
									return current.hasSkill('lqhc_liuji');
								});
								if (list.length > 1) {
									return '将任意张手牌置于一名拥有【六计】的角色的武将牌上作为<财>.';
								}
								return '选择任意张手牌置于' + get.translation(list) + '的武将牌上作为<财>.';
							},
							filterCard: true,
							check(card) {
								return 7 - get.value(card);
							},
							discard: false,
							lose: false,
							position: 'h',
							usable: 1,
							selectCard: [2, Infinity],
							content() {
								'step 0';
								if (!target) {
									var target = game.filterPlayer(function (current) {
										return current.hasSkill('lqhc_liuji');
									});
								}
								target.addToExpansion(cards, 'giveAuto', player).gaintag.add('lqhc_tiaobo');
								if (!target.storage.lqhc_liuji) {
									target.storage.lqhc_liuji = 0;
								}
								target.storage.lqhc_liuji++;
								if (target.storage.lqhc_liuji > 5) {
									target.awakenSkill('lqhc_liuji');
								}
								player.chooseTarget(true, '选择一名角色,令' + get.translation(target) + '对该角色造成一点伤害.').set('ai', function (target) {
									return -get.attitude(_status.event.player, target);
								});
								event.target = target;
								('step 1');
								if (result.targets?.length) {
									result.targets[0].damage(target);
									if (target.getExpansions('lqhc_tiaobo').length > 1) {
										const num = Math.floor(target.getExpansions('lqhc_tiaobo').length / 2);
										target.chooseCardButton(num, true, '弃置' + num + '张<财>并摸' + num + '张牌.', target.getExpansions('lqhc_tiaobo')).ai = function (button) {
											return 100 - get.value(button.link);
										};
									}
								}
								('step 2');
								if (result.links?.length) {
									target.loseToDiscardpile(result.links);
									target.draw(result.links.length);
								}
							},
							ai: {
								order: 1,
								result: {
									player(player, target) {
										var target = game.findPlayer(function (current) {
											return current.hasSkill('lqhc_tiaobo');
										});
										if (target) {
											return get.attitude(player, target);
										}
									},
								},
							},
						},
						lqhc_jibing: {
							mod: {
								attackRange(player, num) {
									return (num += player.getExpansions('lqhc_jibing').length * 2);
								},
							},
							marktext: '兵',
							intro: {
								content: 'expansion',
								markcount: 'expansion',
							},
							onremove(player, skill) {
								const cards = player.getExpansions(skill);
								if (cards.length) {
									player.loseToDiscardpile(cards);
								}
							},
							audio: 'jibing',
							enable: ['chooseToUse', 'chooseToRespond'],
							filter(event, player) {
								for (const i of lib.inpile) {
									if (get.type(i) == 'basic' && event.filterCard && event.filterCard({ name: i }, player, event) && player.getExpansions('lqhc_jibing').length) {
										return true;
									}
								}
								return false;
							},
							hiddenCard(player, name) {
								return get.type(name) == 'basic' && player.getExpansions('lqhc_jibing').length;
							},
							//①摸牌阶段开始时,你可以改为将牌堆顶的3张牌置于你的武将牌上,称为<兵>.
							//②你可以将一张<兵>当做任意基本牌使用或打出.③你获得或失去<兵>时,你摸一张牌.④你的攻击距离加2X(X为你的<兵>数)
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (name == 'sha') {
											if (event.filterCard && event.filterCard({ name: name }, player, event)) {
												list.push(['基本', '', 'sha']);
											}
											for (const j of lib.inpile_nature) {
												if (event.filterCard && event.filterCard({ name: name, nature: j }, player, event)) {
													list.push(['基本', '', 'sha', j]);
												}
											}
										}
										if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['基本', '', name]);
										}
									}

									return ui.create.dialog('兵祸', [list, 'vcard'], 'hidden'); //QQQ
								},
								select: 2,
								filter(button) {
									if (ui.selected.buttons.length && get.itemtype(ui.selected.buttons[0].link) == get.itemtype(button.link)) {
										return false;
									}
									return true;
								},
								check(button) {
									const player = _status.event.player;
									if (player.hasSkill('lqhc_moucuan') && !ui.selected.buttons.length) {
										return player.getUseValue({
											name: button.link[2],
											nature: button.link[3],
										});
									}
									if (button.link[2] == 'tao' && player.hp < 3) {
										return 4;
									}
									if (button.link[2] == 'jiu' && player.getExpansions('lqhc_jibing').length > 1) {
										return 3;
									}
									if (button.link[2] == 'sha' && button.link[3] == 'thunder') {
										return 2;
									}
									return 1;
								},
								backup(links, player) {
									if (get.itemtype(links[1]) == 'card') {
										links.reverse();
									}
									const name = links[1][2],
										nature = links[1][3],
										skill = _status.event.buttoned;
									return {
										viewAs: { name: name, nature: nature },
										position: 'x',
										filterCard: (card) => card == lib.skill.lqhc_jibing_backup.card,
										selectCard: -1,
										popname: true,
										card: links[0],
										precontent() {
											player.draw();
										},
									};
								},
								prompt(links, player) {
									return '请选择' + get.translation(links[1][2]) + '的目标';
								},
							},
							ai: {
								skillTagFilter(player) {
									return player.getExpansions('lqhc_jibing').length;
								},
								order(item, player) {
									if (!_status.event.dying && player.hasSkill('lqhc_moucuan')) {
										return 1;
									}
									return 6;
								},
								result: {
									player(player) {
										if (_status.event.dying) {
											return get.attitude(player, _status.event.dying);
										}
										return 1;
									},
								},
							},
							group: 'lqhc_jibing_place',
							subSkill: {
								place: {
									audio: 'jibing',
									trigger: { player: 'phaseDrawBegin1' },
									prompt2: '摸牌阶段开始时,你可以改为将牌堆顶的3张牌置于你的武将牌上,称为<兵>.',
									filter(event, player) {
										return !event.numFixed;
									},
									content() {
										trigger.changeToZero();
										const cards = get.cards(3);
										player.addToExpansion(cards, player, 'giveAuto').gaintag.add('lqhc_jibing');
										player.draw();
									},
								},
							},
						},
						lqhc_moucuan: {
							audio: 'moucuan',
							derivation: ['lqhc_binghuo', 'lqhc_taiping'],
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							juexingji: true,
							filter(event, player) {
								return player.getExpansions('lqhc_jibing').length >= game.countGroup();
							},
							content() {
								player.awakenSkill('lqhc_moucuan');
								player.addSkill('lqhc_binghuo');
								player.addSkill('lqhc_taiping');
							},
							ai: { combo: 'lqhc_jibing' },
						},
						lqhc_binghuo: {
							audio: 'binghuo',
							trigger: { global: 'phaseJieshuBegin' },
							forced: true,
							filter(event, player) {
								return (
									player.hasHistory('useCard', function (evt) {
										return evt.skill == 'lqhc_jibing_backup';
									}) ||
									player.hasHistory('respond', function (evt) {
										return evt.skill == 'lqhc_jibing_backup';
									})
								);
							},
							content() {
								'step 0';
								player.chooseTarget(get.prompt2('lqhc_binghuo'), '对一名角色造成一点雷电伤害.').set('ai', function (target) {
									const player = _status.event.player;
									return get.damageEffect(target, player, player, 'thunder') * (target.hp == 1 ? 2 : 1); //伤害目标ai
								});
								('step 1');
								if (result.targets?.length) {
									event.target = result.targets[0];
									event.target.damage('thunder');
								}
							},
							ai: { combo: 'lqhc_jibing', expose: 0.2 },
							group: 'lqhc_binghuo_die',
							subSkill: {
								die: {
									audio: 'binghuo',
									trigger: { global: 'die' },
									forced: true,
									filter(event, player) {
										return event.player.countCards('he') > 0 && event.getParent(3).name == 'lqhc_binghuo';
									},
									content() {
										'step 0';
										event.card = trigger.player.getCards('he');
										player.chooseButton(['兵祸:选择任意张牌作为<兵>,剩余的牌作为<符>置于你的武将牌上.', event.card], [1, event.card.length]).set('ai', function (button) {
											if (!ui.selected.buttons || ui.selected.buttons.length < Math.floor(event.card.length / 2)) {
												return 1;
											}
											return 0;
										});
										('step 1');
										if (result.links?.length) {
											for (const i of result.links) {
												event.card.remove(i);
											}
											player.addToExpansion(result.links, player, 'giveAuto').gaintag.add('lqhc_jibing');
											player.draw();
										}
										if (event.card.length) {
											player.addToExpansion(event.card, player, 'giveAuto').gaintag.add('lqhc_taiping');
										}
									},
								},
							},
						},
						lqhc_taiping: {
							marktext: '符',
							intro: {
								content: 'expansion',
								markcount: 'expansion',
							},
							onremove(player, skill) {
								const cards = player.getExpansions(skill);
								if (cards.length) {
									player.loseToDiscardpile(cards);
								}
							},
							audio: 'moucuan',
							enable: 'phaseUse',
							filter(event, player) {
								for (const i of lib.inpile) {
									if (get.type2(i) == 'trick' && event.filterCard && event.filterCard({ name: i }, player, event) && player.getExpansions('lqhc_taiping').length) {
										return true;
									}
								}
								return false;
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (get.type2(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										}
									}

									return ui.create.dialog('<font size=6>太平', [list, 'vcard'], 'hidden');
								},
								select: 2,
								filter(button) {
									if (ui.selected.buttons.length && get.itemtype(ui.selected.buttons[0].link) == get.itemtype(button.link)) {
										return false;
									}
									return true;
								},
								check(button) {
									if (player.getExpansions('lqhc_taiping') > 1) {
										if (!ui.selected.buttons.length) {
											return _status.event.player.getUseValue({ name: button.link[2] });
										}
										return 10;
									}
									return 0;
								},
								backup(links, player) {
									if (get.itemtype(links[1]) == 'card') {
										links.reverse();
									}
									const name = links[1][2],
										skill = _status.event.buttoned;
									return {
										viewAs: {
											name: name,
										},
										position: 'x',
										filterCard: (card) => card == lib.skill.lqhc_taiping_backup.card,
										selectCard: -1,
										popname: true,
										card: links[0],
										precontent() { },
									};
								},
								prompt(links, player) {
									return '请选择' + get.translation(links[1][2]) + '的目标';
								},
							},
							ai: {
								order: 10,
								result: {
									player: 3,
								},
							},
							group: 'lqhc_taiping_place',
							subSkill: {
								place: {
									trigger: {
										player: ['useCard', 'respond'],
										global: 'damageBegin2',
									},
									forced: true,
									filter(event, player, name) {
										if (name == 'damageBegin2') {
											return event.num > 0 && event.source && event.nature == 'thunder' && player.getExpansions('lqhc_taiping').length;
										}
										return event.skill == 'lqhc_jibing_backup' && _status.currentPhase && _status.currentPhase.countCards('hej'); //QQQ
									},
									content() {
										'step 0';
										if (trigger.name != 'damage') {
											const next = player.choosePlayerCard(_status.currentPhase, 'hej', '###是否发动【太平】？###将' + get.translation(_status.currentPhase) + '区域内的一张牌置于你的武将牌上作为<符>.');
											next.set('ai', function (button) {
												if (!_status.event.goon) {
													return 0;
												}
												return get.value(button.link);
											});
											next.set('goon', get.attitude(player, _status.currentPhase) < 1);
											next.set('forceAuto', true);
										} else {
											player.chooseCardButton('###是否发动【太平】？###弃置一张<符>令伤害加1.', player.getExpansions('lqhc_taiping')).ai = function (button) {
												if (get.attitude(_status.event.player, trigger.player) > 0) {
													return false;
												}
												return 100 - get.value(button.link);
											};
										}
										('step 1');
										if (result.cards?.length) {
											if (trigger.name != 'damage') {
												player.addToExpansion(result.cards, player, 'giveAuto').gaintag.add('lqhc_taiping');
											} else {
												player.loseToDiscardpile(result.links);
												trigger.num++;
											}
										}
									},
								},
							},
						},
						lqhc_jinglve: {
							init(player) {
								if (!player.storage.lqhc_jinglve1) {
									player.storage.lqhc_jinglve1 = [];
								}
							},
							audio: 'jinglve',
							enable: 'phaseUse',
							usable: 2,
							filter(event, player) {
								if (
									game.hasPlayer(function (current) {
										if (current != player && current.countCards('h')) {
											for (const i of current.getCards('h')) {
												return !player.storage.lqhc_jinglve1.includes(i);
											}
										}
									})
								) {
									return true;
								}
								return false;
							},
							filterTarget(card, player, target) {
								for (const i of target.getCards('h')) {
									if (!player.storage.lqhc_jinglve1.includes(i) && target != player) {
										return true;
									}
								}
								return false;
							},
							content() {
								'step 0';
								const u = player.chooseButton(['选择一张牌作为「死士」', target.getCards('h')], true);
								u.set('ai', function (button) {
									const target = _status.event.parent.target;
									const card = button.link;
									const val = target.getUseValue(card);
									if (val > 0) {
										return val;
									}
									return get.value(card);
								});
								u.set('filterButton', function (button) {
									if (player.storage.lqhc_jinglve1 && player.storage.lqhc_jinglve1.includes(button.link)) {
										return false;
									}
									return true;
								});
								('step 1');
								if (result.bool) {
									player.storage.lqhc_jinglve1.push(target);
									player.storage.lqhc_jinglve1.push(result.links[0]);
									player.addSkill('lqhc_jinglve_lose');
								}
							},
							ai: {
								order: 12,
								result: {
									target: -1,
								},
							},
							subSkill: {
								lose: {
									mark: true,
									intro: {
										name: '死士',
										mark(dialog, content, player) {
											if (player.storage.lqhc_jinglve1.length) {
												if (player == game.me || player.isUnderControl()) {
													const a = player.storage.lqhc_jinglve1;
													for (let j = 0; j < a.length; j++) {
														if (j % 2 == 0) {
															dialog.addText('记录的目标和死士牌');
															dialog.add([a[j]]);
														} else {
															dialog.add([a[j]]);
														}
													}
												} else {
													dialog.addText('死士牌对你不可见');
												}
											}
										},
									},
									onremove(player) {
										player.storage.lqhc_jinglve1 = [];
									},
									trigger: { global: ['dieEnd', 'loseEnd', 'gainEnd'] },
									silent: true,
									lastDo: true,
									charlotte: true,
									filter(event, player) {
										if (event.name != 'gain' && !player.storage.lqhc_jinglve1.includes(event.player)) {
											return false;
										}
										for (const i of event.cards) {
											return event.name == 'die' || (player.storage.lqhc_jinglve1.includes(i) && (event.name == 'gain' || (event.position != ui.ordering && event.position != ui.discardPile)));
										}
									},
									content() {
										const a = player.storage.lqhc_jinglve1,
											b = [];
										if (trigger.name != 'die') {
											for (const i of trigger.cards) {
												for (var j = 0; j < a.length; j++) {
													if (a[j] == i) {
														b.push(player.storage.lqhc_jinglve1[j]);
														b.push(player.storage.lqhc_jinglve1[j - 1]);
													}
												}
											}
										} else {
											for (var j = 0; j < a.length; j++) {
												if (a[j] == trigger.player) {
													b.push(player.storage.lqhc_jinglve1[j]);
													b.push(player.storage.lqhc_jinglve1[j + 1]);
												}
											}
										}
										if (b.length) {
											for (const i of b) {
												player.storage.lqhc_jinglve1.remove(i);
											}
										}
										if (player.storage.lqhc_jinglve1.length < 2) {
											player.removeSkill('lqhc_jinglve_lose');
										}
									},
									group: 'lqhc_jinglve_gain',
								},
								gain: {
									audio: 'jinglve',
									trigger: { global: ['loseAfter', 'useCard', 'phaseAfter', 'cardsDiscardAfter'] },
									filter(event, player) {
										if (event.player && !player.storage.lqhc_jinglve1.includes(event.player)) {
											return false;
										}
										if (event.name == 'phase') {
											return event.player.countCards('hej', function (card) {
												return player.storage.lqhc_jinglve1.includes(card);
											});
										}
										for (const i of event.cards) {
											return player.storage.lqhc_jinglve1.includes(i) && (event.name == 'useCard' || get.position(i, true) == 'd' || event.position == ui.discardPile);
										}
									},
									forced: true,
									lastDo: true,
									forceDie: true,
									charlotte: true,
									logTarget: 'player',
									content() {
										'step 0';
										event.l = [];
										const a = player.storage.lqhc_jinglve1,
											b = [];
										if (trigger.name == 'useCard') {
											trigger.all_excluded = true;
											trigger.targets.length = 0;
											for (const i of trigger.cards) {
												if (a.includes(i)) {
													event.l.push(i);
												}
											}
											player.gain(event.l, 'draw2');
										} else if (trigger.name == 'phase') {
											for (const i of trigger.player.getCards('hej')) {
												if (a.includes(i)) {
													event.l.push(i);
												}
											}
										} else {
											for (const i of trigger.cards) {
												if (a.includes(i)) {
													event.l.push(i);
												}
											}
										}
										if (event.l.length && trigger.name != 'useCard') {
											player.chooseTarget(true, '令一名角色获得' + get.translation(event.l) + '.').set('ai', function (target) {
												return get.attitude(_status.event.player, target);
											});
										}
										('step 1');
										if (result.targets?.length) {
											result.targets[0].addSkill('lqhc_jinglve_draw');
											result.targets[0].gain(event.l, 'gain2');
										}
									},
								},
								draw: {
									mark: true,
									intro: {
										content: '你的下次摸牌阶段摸牌数+1.',
									},
									trigger: {
										player: 'phaseDrawBegin2',
									},
									forced: true,
									charlotte: true,
									audio: 'jinglve',
									filter(event, player) {
										return !event.numFixed;
									},
									content() {
										player.removeSkill('lqhc_jinglve_draw');
										trigger.num++;
										game.log(player, '的摸牌数+1');
									},
								},
							},
						},
						lqhc_baiyi: {
							audio: 'baiyi',
							enable: 'phaseUse',
							usable: 1,
							limited: true,
							filter(event, player) {
								return player.isDamaged() && game.players.length > 2;
							},
							multitarget: true,
							multiline: true,
							contentBefore() {
								player.$fullscreenpop('败移', 'thunder');
							},
							content() {
								'step 0';
								player.awakenSkill('lqhc_baiyi');
								player.storage.lqhc_baiyi2 = true;
								('step 1');
								player.chooseTarget(2, '###调整座位###选择两名角色交换位置.').set('ai', function (target) {
									return false;
								});
								('step 2');
								if (result.bool) {
									game.broadcastAll(
										function (target1, target2) {
											game.swapSeat(target1, target2);
										},
										result.targets[0],
										result.targets[1],
									);
									event.goto(1);
								}
							},
						},
						lqhc_shanli: {
							trigger: {
								player: ['phaseZhunbeiBegin', 'useCard'],
							},
							forced: true,
							dutySkill: true,
							audio: 'shanli',
							filter(event, player, name) {
								if (name == 'useCard') {
									return game.hasPlayer(function (current) {
										return current.hasSkill('lqhc_shanli_zhu');
									});
								}
								return (
									player.storage.lqhc_baiyi2 &&
									!game.hasPlayer(function (current) {
										return current.hasSkill('lqhc_shanli_zhu');
									})
								);
							},
							content() {
								'step 0';
								if (trigger.name == 'useCard') {
									trigger.directHit.addArray(
										game.filterPlayer(function (current) {
											return current != player;
										}),
									);
									game.log(trigger.card, '不能被响应');
									event.finish();
								} else {
									player.chooseTarget(true, '###【擅立】###我司马氏权势滔天,擅自立个君王又有何妨!').set('ai', function (target) {
										if (get.attitude(_status.event.player, target) > 0) {
											return 2;
										}
										if (target == _status.event.player) {
											return 1;
										}
										return 0;
									});
								}
								('step 1');
								((event.target = result.targets[0]), (event.a = []), (event.b = []), (event.d = lib.group));
								('step 2');
								event.e = event.d[0];
								const f = [];
								if (!_status.characterlist) {
									if (_status.connectMode) {
										var list = get.charactersOL();
									} else {
										var list = [];
										for (const i in lib.character) {
											if (lib.filter.characterDisabled2(i) || lib.filter.characterDisabled(i)) {
												continue;
											}
											list.push(i);
										}
									}
									_status.characterlist = list;
								}
								_status.characterlist.randomSort();
								for (const i of _status.characterlist) {
									if (i == 'key_yuri') {
										continue;
									}
									const character = lib.character[i];
									if (character && character[3] && character[1] == event.e) {
										for (const j of character[3]) {
											if (event.b.includes(j) || j == 'yuri_wangxi' || event.target.hasSkill('j')) {
												continue;
											}
											const info = get.info(j);
											if (info && info.zhuSkill) {
												event.b.add(j);
												event.a.add(i);
												f.add(i);
												continue;
											}
										}
									}
									if (f.length > 1) {
										break;
									}
								}
								if (!event.b.length) {
									event.finish();
									return;
								}
								('step 3');
								event.d.remove(event.e);
								if (event.d.length) {
									event.goto(2);
								}
								('step 4');
								player.chooseControl(event.b).set('dialog', ['选择令' + get.translation(event.target) + '获得一个技能', [event.a, 'character']]);
								('step 5');
								event.target.addSkillLog(result.control);
								event.target.addSkill('lqhc_shanli_zhu');
								event.target.storage.zhuSkill_shanli = [result.control];
								event.target.setAvatarQueue(target.name1 || target.name, [event.a[event.b.indexOf(result.control)]]);
							},
							group: 'lqhc_shanli_achieve',
							subSkill: {
								zhu: {
									mark: true,
									marktext: '君',
									intro: {
										content: '君王',
									},
									charlotte: true,
									forced: true,
								},
								achieve: {
									trigger: {
										global: 'dieEnd',
									},
									forced: true,
									audio: 'shanli',
									filter(event, player) {
										return event.player.hasSkill('lqhc_shanli_zhu');
									},
									content() {
										game.log(player, '成功完成使命');
										player.awakenSkill('lqhc_shanli');
										player.changeGroup('jin');
										player.addSkillLog('lqhc_tairan');
									},
								},
							},
							derivation: 'lqhc_tairan',
						},
						lqhc_tairan: {
							trigger: {
								player: ['phaseEnd', 'phaseUseBegin'],
							},
							forced: true,
							audio: 'tairan',
							filter(event, player, name) {
								if (name != 'phaseEnd') {
									return player.storage.lqhc_tairan && player.storage.lqhc_tairan > 0;
								}
								return player.hp < player.maxHp || player.countCards('h') < player.maxHp;
							},
							content() {
								if (event.triggername != 'phaseEnd') {
									player.loseHp(player.storage.lqhc_tairan);
									delete player.storage.lqhc_tairan;
								} else {
									const num = Math.min(player.maxHp - player.hp, 5); //最小
									if (num > 0) {
										if (!player.storage.lqhc_tairan) {
											player.storage.lqhc_tairan = num - 1;
										}
										player.recover(num);
									}
									if (player.countCards('h') < player.maxHp) {
										player.drawTo(player.maxHp);
									}
								}
							},
						},
						lqhc_tiaozhan: {
							trigger: { player: 'phaseZhunbeiBegin' },
							forced: true,
							content() {
								'step 0';
								const list = [];

								for (const i of lib.inpile) {
									const name = i,
										card = { name: name };
									if (!get.tag(card, 'damage')) {
										continue;
									}
									if (name == 'sha') {
										if (lib.filter.cardEnabled({ name: name }, player)) {
											list.push(['基本', '', 'sha']);
										}
										for (const j of lib.inpile_nature) {
											if (lib.filter.cardEnabled({ name: name, nature: j }, player)) {
												list.push(['基本', '', 'sha', j]);
											}
										}
									} else if (get.type(name) == 'basic' && lib.filter.cardEnabled({ name: name }, player)) {
										list.push(['基本', '', name]);
									} else if (get.type(name) == 'trick' && lib.filter.cardEnabled({ name: name }, player)) {
										list.push(['锦囊', '', name]);
									}
								}

								const dialog = ui.create.dialog('###是否发动【挑战】？###你可以视为使用一张带有「伤害」标签的牌.', [list, 'vcard'], 'hidden');
								player.chooseButton(dialog).ai = function (button) {
									const player = _status.event.player;
									return player.getUseValue({
										name: button.link[2],
										nature: button.link[3],
									});
								};
								('step 1');
								if (result.links?.length) {
									if (result.links[0][2] != 'sha') {
										player.chooseUseTarget(false, true, result.links[0][2]);
									} else {
										player.chooseUseTarget(false, 'nodistance', true, { name: result.links[0][2], nature: result.links[0][3] });
									}
								}
							},
						},
						lqhc_zhonger: {
							enable: 'phaseUse',
							usable: 1,
							dutySkill: true,
							filterTarget: true,
							selectTarget: -1,
							content() {
								'step 0';
								const list = ['爆裂吧,现实,粉碎吧,精神,放逐这个世界!', '出生于光明的人,永远痴迷黑暗,渴望堕落.', '三十年河东,三十年河西,莫欺少年穷!', '我若成佛,天下无魔,我若成魔,佛奈我何.', '抱歉呢,其实当你准备和我战斗时,你已经败了.', '脚踩生灵,手掌生死,传说不朽,王者不败.', '这天,为孤而哭泣!这地,为孤而颤栗!你说天下若无孤？何人有资格与孤相提并论？', '逆天,尚有例外.逆吾,绝无生机!', '有朝一日权在手,杀尽天下负我人.', '一点寒光万丈芒,屠尽天下又何妨.深埋不改凌云志,一聚风云便是皇.', '切身感受,何谓最强,这便是最强!I...AM...Atomic', '凡王之血,必以剑终.'].randomGet();
								player.chat(list);
								target.chooseControlList(true, get.translation(player) + '邀请你一起前往异世界', ['太中二了,断然拒绝.<br>你姹紫嫣红的掉了两斤HP.', '稍加思索,一起堕落.<br>让' + get.translation(player) + '摸一下腿并获得一件JK.', '通往异世界的大门突然爆炸,当场吃席.<br>你抑扬顿挫的没了两颗宝贝.', '左搂右抱,酒池肉林,并试图报答.<br>让' + get.translation(player) + '贴贴并抱抱.'], function () {
									if (get.attitude(_status.event.player, player) < 0 && _status.event.player.hp > 4) {
										return 0;
									}
									return 1;
								});
								('step 1');
								if (result.index == 0 || result.index == 2) {
									target.loseHp(2);
								} else {
									player.draw();
									player.changeHujia();
								}
							},
							group: 'lqhc_zhonger_achieve',
							subSkill: {
								achieve: {
									trigger: {
										player: 'phaseZhunbeiBegin',
									},
									forced: true,
									filter(event, player) {
										return player.hujia > 3;
									},
									content() {
										'step 0';
										game.log(player, '成功完成使命');
										player.awakenSkill('lqhc_zhonger');
										player.loseMaxHp();
										player.drawTo(8);
										let skills = [],
											chaii = [];
										for (const i in lib.character) {
											if (lib.character[i][1] != 'shen') {
												continue;
											}
											const d = lib.character[i][4];
											if (d.includes('boss') || d.includes('hiddenboss') || d.includes('bossallowed') || d.includes('minskin')) {
												continue;
											}
											if ((game.me == player && lib.config.extension_浪琴天阙_lqtq_shaiji_kuochong) || !lib.config.forbidai.includes(i)) {
												chaii.push(i);
											}
										}
										if (chaii.length) {
											chaii = chaii.randomGets(3);
											player
												.chooseButton(true)
												.set('ai', function (button) {
													return get.rank(button.link, true) - lib.character[button.link][2];
												})
												.set('createDialog', ['获得一张武将牌的所有技能', [chaii, 'character']]);
										} else {
											event.finish();
										}
										('step 1');
										for (const i of lib.character[result.links[0]][3]) {
											player.addSkillLog(i);
										}
									},
								},
							},
						},
						lqhc_guibi: {
							audio: 'ext:浪琴天阙/audio:1',
							trigger: {
								player: ['useCardAfter', 'respondAfter'],
							},
							forced: true,
							filter(event, player) {
								for (const i of event.cards) {
									return (
										event.card.name == 'shan' &&
										([1, 11, 12, 13].includes(i.number) ||
											player.getCards('e', function (card) {
												return card.number == i.number;
											}).length)
									);
								}
							},
							content() {
								player.draw();
							},
							group: ['lqhc_guibi_shan', 'lqhc_guibi_bi'],
							subSkill: {
								shan: {
									mod: {
										aiValue(player, card, num) {
											const cards = player.getCards('hs');
											cards.sort(function (a, b) {
												return (b.name == 'shan' ? 1 : 2) - (a.name == 'shan' ? 1 : 2);
											});
											const geti = function () {
												if (cards.includes(card)) {
													return cards.indexOf(card);
												}
												return cards.length;
											};
											if (card.name == 'shan') {
												return Math.min(num, [6, 4, 3][Math.min(geti(), 2)]) * 0.6;
											}
											return Math.max(num, [6.5, 4, 3][Math.min(geti(), 2)]);
										},
										aiUseful() {
											return lib.skill.lqhc_guibi_shan.mod.aiValue.apply(this, arguments);
										},
									},
									audio: 2,
									enable: ['chooseToRespond', 'chooseToUse'],
									filterCard: true,
									viewAs: { name: 'shan' },
									viewAsFilter(player) {
										if (!player.countCards('hs')) {
											return false;
										}
									},
									position: 'hs',
									prompt: '将一张手牌当做【闪】使用或打出',
									check() {
										return 1;
									},
									ai: {
										order: 3,
										respondShan: true,
										skillTagFilter(player) {
											if (player.countCards('hs') < 1) {
												return false;
											}
										},
										effect: {
											target(card, player, target, current) {
												if (get.tag(card, 'respondShan') && current < 0) {
													return 0.6;
												}
											},
										},
									},
								},
								bi: {
									trigger: { global: ['chooseToRespondBefore', 'chooseToUseBefore'] },
									filter(event, player) {
										if (event.responded) {
											return false;
										}
										return event.filterCard && event.filterCard({ name: 'shan' }, player, event) && event.player != player;
									},
									forced: true,
									content() {
										'step 0';
										const next = player.chooseToRespond('###是否发动【规避】？###你可以替' + get.translation(trigger.player) + '打出一张闪？', { name: 'shan' });
										next.set('ai', function () {
											return get.attitude(_status.event.player, trigger.player);
										});
										('step 1');
										if (result.bool) {
											trigger.result = { bool: true, card: { name: 'shan' } };
											trigger.responded = true;
											trigger.animate = false;
											if (typeof player.ai.shown == 'number' && player.ai.shown < 0.95) {
												player.ai.shown += 0.3;
												if (player.ai.shown > 0.95) {
													player.ai.shown = 0.95;
												}
											}
										}
									},
									ai: {
										respondShan: true,
									},
								},
							},
						},
						lqhc_liangjiang: {
							intro: {
								mark(dialog, storage, player) {
									dialog.addAuto(
										player.getCards('s', function (card) {
											return card.hasGaintag('lqhc_liangjiang');
										}),
									);
								},
								markcount(storage, player) {
									return player.getCards('s', function (card) {
										return card.hasGaintag('lqhc_liangjiang');
									}).length;
								},
								onunmark(storage, player) {
									const cards = player.getCards('s', function (card) {
										return card.hasGaintag('lqhc_liangjiang');
									});
									if (cards.length) {
										player.lose(cards, ui.discardPile);
										player.$throw(cards, 1000);
										game.log(cards, '进入了弃牌堆');
									}
								},
							},
							mod: {
								aiOrder(player, card, num) {
									if (get.itemtype(card) == 'card' && card.hasGaintag('lqhc_liangjiang')) {
										return num + 0.5;
									}
								},
							},
							trigger: {
								player: ['gainEnd', 'gainAfter'],
								global: 'gameDrawEnd',
							},
							audio: 'ext:浪琴天阙/audio:1',
							forced: true,
							_priority: Infinity,
							filter(event, player) {
								return player.countCards('h');
							},
							content() {
								player.loseToSpecial(player.getCards('h'), 'lqhc_liangjiang').visible = true;
							},
						},
						lqhc_tiaoxin: {
							audio: 'sbtiaoxin',
							enable: 'phaseUse',
							usable: 1,
							chargeSkill: true,
							filter(event, player) {
								return game.hasPlayer(function (current) {
									return current.countCards('hej') && current != player;
								});
							},
							filterTarget(card, player, target) {
								return target.countCards('hej') && target != player;
							},
							selectTarget: [1, 4],
							multiline: true,
							content() {
								'step 0';
								target
									.chooseToUse(
										function (card, player, event) {
											if (card.name != 'sha') {
												return false;
											}
											return lib.filter.filterCard.apply(this, arguments);
										},
										'挑衅:对' + get.translation(player) + '使用一张杀,或令' + get.translation(player) + '获得你每个区域内的各一张牌.',
									)
									.set('targetRequired', true)
									.set('complexSelect', true)
									.set('filterTarget', function (card, player, target) {
										if (target != _status.event.sourcex && !ui.selected.targets.includes(_status.event.sourcex)) {
											return false;
										}
										return lib.filter.filterTarget.apply(this, arguments);
									})
									.set('sourcex', player);
								('step 1');
								if (!result.bool && target.countGainableCards(player, 'hej')) {
									let num = 0,
										a = target;
									if (a.countGainableCards(player, 'h')) {
										num++;
									}
									if (a.countGainableCards(player, 'e')) {
										num++;
									}
									if (a.countGainableCards(player, 'j')) {
										num++;
									}
									if (num > 0) {
										player.gainPlayerCard(a, num, 'hej', true).set('filterButton', function (button) {
											for (const i of ui.selected.buttons) {
												if (get.position(button.link) == get.position(i.link)) {
													return false;
												}
											}

											return true;
										});
									}
								}
							},
							ai: {
								threaten: 1.2,
								order: 4,
								expose: 0.2,
								result: {
									target(player, target) {
										if (target.countGainableCards(player, 'hej') == 0) {
											return 0;
										}
										return -1;
									},
									player(player, target) {
										if (!target.canUse('sha', player)) {
											return 0;
										}
										if (target.countCards('h') == 0) {
											return 0;
										}
										if (target.countCards('h') == 1) {
											return -0.1;
										}
										if (player.hp <= 2) {
											return -2;
										}
										if (player.countCards('h', 'shan') == 0) {
											return -1;
										}
										return -0.5;
									},
								},
							},
						},
						lqhc_zhiji: {
							audio: 'sbzhiji',
							trigger: {
								player: 'lqhc_tiaoxinAfter',
							},
							juexingji: true,
							forced: true,
							derivation: ['reguanxing', 'tianren'],
							filter(event, player) {
								let len = 0;
								player.getAllHistory('useSkill', (evt) => {
									if (evt.skill != 'lqhc_tiaoxin') {
										return false;
									}
									len += evt.targets.length;
								});
								return len >= 6;
							},
							content() {
								'step 0';
								player.awakenSkill('lqhc_zhiji');
								player.addSkill('reguanxing');
								player.addSkill('tianren');
								player
									.chooseTarget('志继:是否令一名其他角色获得<北伐>标记？', function (card, player, target) {
										return target != player;
									})
									.set('ai', (target) => -get.attitude(player, target));
								('step 1');
								if (result.targets?.length) {
									result.targets.forEach((target) => {
										target.addAdditionalSkill('lqhc_zhiji_' + player.playerid, 'lqhc_zhiji_beifa');
										target.markAuto('lqhc_zhiji_beifa', [player]);
									});
									player.addTempSkill('lqhc_zhiji_clear', { player: 'die' });
								}
							},
							subSkill: {
								beifa: {
									charlotte: true,
									mark: true,
									marktext: '伐',
									intro: {
										content: '你使用的牌只能指定$为目标',
									},
									mod: {
										playerEnabled(card, player, target) {
											if (!player.getStorage('lqhc_zhiji_beifa').includes(target)) {
												return false;
											}
										},
									},
								},
								clear: {
									forced: true,
									popup: false,
									charlotte: true,
									onremove(player) {
										game.countPlayer(function (current) {
											current.removeAdditionalSkill('lqhc_zhiji_' + player.playerid);
										});
									},
									trigger: {
										global: 'dieBegin',
									},
									filter(event, player) {
										return event.player.getStorage('lqhc_zhiji_beifa').includes(player);
									},
									content() {
										game.countPlayer(function (current) {
											current.removeAdditionalSkill('lqhc_zhiji_' + player.playerid);
										});
									},
								},
							},
						},
						lqhc_shence: {
							enable: 'phaseUse',
							usable: 1,
							position: 'he',
							filterCard: true,
							filter(event, player) {
								return (
									game.hasPlayer(function (current) {
										return current.countCards('ej');
									}) && player.countCards('he')
								);
							},
							content() {
								player.moveCard();
								player.drawTo(player.maxHp);
							},
							group: 'lqhc_shence_damage',
							subSkill: {
								damage: {
									trigger: {
										player: 'damageEnd',
									},
									filter(event, player) {
										return (
											game.hasPlayer(function (current) {
												return current.countCards('ej');
											}) &&
											player.countCards('he') &&
											player.storage.lqhc_shence == true
										);
									},
									forced: true,
									content() {
										'step 0';
										player.chooseToDiscard('he', '###是否发动【神策】？###弃置一张牌并转移场上一张牌,你摸至' + player.maxHp + '张牌.').set('ai', function (card) {
											if (_status.event.player.countCards('h') < 5) {
												return 10 - get.value(card);
											}
											return 6 - get.value(card);
										});
										('step 1');
										if (result.bool) {
											player.moveCard();
											player.drawTo(player.maxHp);
										}
									},
								},
							},
						},
						lqhc_liangzhi: {
							trigger: {
								global: 'useCard',
							},
							filter(event, player) {
								let a = 0;
								if (player.storage.lqhc_liangzhi) {
									a = player.storage.lqhc_liangzhi;
								}
								return (
									event.player != player &&
									player.countCards('he') &&
									_status.currentPhase == event.player &&
									a < game.countPlayer() &&
									event.player.getHistory('lose', function (evt) {
										return evt.parent == event && evt.hs && evt.hs.length == event.cards.length;
									}).length
								);
							},
							forced: true,
							content() {
								'step 0';
								let effect = 0;
								if (trigger.card.name == 'wuxie' || trigger.card.name == 'shan') {
									if (get.attitude(player, trigger.player) < -1) {
										effect = -1;
									}
								} else if (trigger.targets && trigger.targets.length) {
									for (const i of trigger.targets) {
										effect += get.effect(i, trigger.card, trigger.player, player);
									}
								}
								let str = '###是否发动【亮智】？###弃置一张牌令' + get.translation(trigger.player);
								if (trigger.targets && trigger.targets.length) {
									str += '对' + get.translation(trigger.targets);
								}
								str += '使用的' + get.translation(trigger.card) + '失效？';
								player
									.chooseToDiscard('he', str)
									.set('ai', function (card) {
										if (_status.event.effect < 0) {
											return 8 - get.value(card);
										}
										return 0;
									})
									.set(
										'effect',
										effect,
									)('step 1');
								if (result.bool) {
									player.addTempSkill('lqhc_liangzhi_usable');
									player.storage.lqhc_liangzhi++;
									trigger.targets.length = 0;
									trigger.all_excluded = true;
									game.log(trigger.player, '使用的', trigger.card, '被无效.');
								}
							},
							subSkill: {
								usable: {
									forced: true,
									charlotte: true, //夏洛特
									init(player, skill) {
										if (!player.storage.lqhc_liangzhi) {
											player.storage.lqhc_liangzhi = 0;
										}
									},
									onremove(player) {
										delete player.storage.lqhc_liangzhi;
									},
								},
							},
						},
						lqhc_renwang: {
							trigger: {
								global: 'recoverBegin',
							},
							forced: true,
							filter(event, player) {
								return !player.storage.lqhc_liangzhi && event.player != player;
							},
							content() {
								player.gainMaxHp();
							},
						},
						lqhc_nitian: {
							juexingji: true,
							forced: true,
							derivation: 'lqhc_shence_zhu',
							trigger: {
								global: 'phaseEnd',
							},
							filter(event, player) {
								return player.maxHp - player.hp > player.countCards('h');
							},
							content() {
								player.awakenSkill('lqhc_nitian');
								player.storage.lqhc_shence = true;
							},
						},
						lqhc_yishi: {
							mark: true,
							zhuanhuanji: true,
							marktext: '☯',
							intro: {
								content(storage, player, skill) {
									if (player.storage.lqhc_yishi == true) {
										return '你可以视为使用一张【闪】.';
									}
									return '你可以视为使用一张【杀】.';
								},
							},
							group: ['lqhc_yishi_sha', 'lqhc_yishi_shan'],
							subSkill: {
								sha: {
									enable: 'chooseToUse',
									viewAs: { name: 'sha' },
									filterCard() {
										return false;
									},
									viewAsFilter(player) {
										return !player.storage.lqhc_yishi;
									},
									selectCard: -1,
									precontent() {
										player.changeZhuanhuanji('lqhc_yishi');
									},
									prompt: '视为使用一张杀',
								},
								shan: {
									enable: 'chooseToUse',
									viewAs: { name: 'shan' },
									filterCard() {
										return false;
									},
									viewAsFilter(player) {
										return player.storage.lqhc_yishi == true;
									},
									onuse(event, player) {
										player.changeZhuanhuanji('lqhc_yishi');
									},
									selectCard: -1,
									prompt: '视为使用一张闪',
								},
							},
						},
						lqhc_qiuxian: {
							trigger: {
								target: 'useCardToTarget',
								global: 'phaseBefore',
								player: 'enterGame',
							},
							derivation: 'lqhc_yishi',
							filter(event, player, name) {
								if (name != 'phaseBefore' && name != 'enterGame') {
									let a = 0,
										b = 0,
										c = 0,
										d = 0;
									game.countPlayer(function (current) {
										if (!event.targets.includes(current)) {
											if (event.player.canUse({ name: event.card.name }, current)) {
												a++;
											} else {
												b++;
											}
										}
										if (current.hasSkill('lqhc_yishi')) {
											if (current.storage.lqhc_yishi == true) {
												c++;
											} else {
												d++;
											}
										}
									});
									if (d > c) {
										return a > 1;
									}
									if (d < c) {
										return b > 1;
									}
									return false;
								}
								return event.name != 'phase' || game.phaseNumber == 0;
							},
							forced: true,
							content() {
								const name = event.triggername;
								if (name == 'phaseBefore' || name == 'enterGame') {
									game.countPlayer(function (current) {
										if (!current.hasSkill('lqhc_yishi')) {
											current.addSkill('lqhc_yishi');
										}
									});
								} else {
									trigger.parent.excluded.add(player);
								}
							},
						},
						lqhc_tizi: {
							trigger: {
								player: 'useCardEnd',
							},
							filter(event, player) {
								return (
									(event.skill == 'lqhc_yishi_sha' || event.skill == 'lqhc_yishi_shan') &&
									game.hasPlayer(function (current) {
										return current.hasSkill('lqhc_yishi') && current.storage.lqhc_yishi != player.storage.lqhc_yishi;
									})
								);
							},
							logTarget(event, player) {
								const a = game.filterPlayer(function (current) {
									return current.hasSkill('lqhc_yishi') && current.storage.lqhc_yishi != _status.event.player.storage.lqhc_yishi;
								});
								return a;
							},
							prompt: '是否发动【提子】？',
							prompt2(player) {
								const a = game.filterPlayer(function (current) {
									return current.hasSkill('lqhc_yishi') && current.storage.lqhc_yishi != _status.event.player.storage.lqhc_yishi;
								});
								return '可以将' + get.translation(a) + '的〖弈式〗转化状态转换为与你一致.';
							},
							content() {
								const a = game.filterPlayer(function (current) {
									return current.hasSkill('lqhc_yishi') && current.storage.lqhc_yishi != player.storage.lqhc_yishi;
								});
								a.sort(lib.sort.seat);
								player.line(a, 'green');
								game.countPlayer(function (current) {
									if (a.includes(current)) {
										current.changeZhuanhuanji('lqhc_yishi');
									}
								});
							},
							group: ['lqhc_tizi_ena'],
							subSkill: {
								ena: {
									trigger: { player: 'useCardToPlayered' },
									filter(event, player) {
										return event.card && event.card.name == 'sha';
									},
									prompt2(event, player) {
										return '令' + get.translation(event.target) + '不能使用' + (!event.cards.length ? '实体' : '虚拟') + '的牌响应' + get.translation(event.card);
									},
									logTarget: 'target',
									check(event, player) {
										return get.attitude(player, event.target) < 0;
									},
									content() {
										if (trigger.cards.length) {
											player.draw();
											trigger.target.storage.lqhc_tizi = true;
										}
										trigger.target.addTempSkill('lqhc_tizi_block');
									},
								},
								block: {
									mod: {
										cardEnabled2(card, player) {
											let evt = _status.event,
												a = [];
											if (evt.name != 'chooseToUse') {
												evt = evt.getParent('chooseToUse');
											}
											if (!evt || !evt.respondTo || evt.respondTo[1].name != 'sha') {
												return;
											}
											for (const i in lib.skill) {
												if (evt.skill == i && lib.skill[i].selectCard == -1) {
													a.push(i);
												}
												if (evt.skill == i + '_backup' && lib.skill[i + '_backup'].selectCard == -1) {
													a.push(i + '_backup');
												}
											}
											if (player.storage.lqhc_tizi && !a.includes(evt.skill)) {
												return false;
											}
										},
										cardEnabled(card, player) {
											let evt = _status.event,
												a = [];
											if (evt.name != 'chooseToUse') {
												evt = evt.getParent('chooseToUse');
											}
											if (!evt || !evt.respondTo || evt.respondTo[1].name != 'sha') {
												return;
											}
											for (const i in lib.skill) {
												if (evt.skill == i && lib.skill[i].selectCard == -1) {
													a.push(i);
												}
												if (evt.skill == i + '_backup' && lib.skill[i + '_backup'].selectCard == -1) {
													a.push(i + '_backup');
												}
											}
											if (!player.storage.lqhc_tizi && a.includes(evt.skill)) {
												return false;
											}
										},
									},
									trigger: {
										player: ['damageBefore', 'damageCancelled', 'damageZero'],
										target: ['shaMiss', 'useCardToExcluded', 'useCardToEnd'],
										global: ['useCardEnd'],
									},
									forced: true,
									firstDo: true,
									charlotte: true,
									filter(event, player) {
										return player.hasSkill('lqhc_tizi_block');
									},
									onremove(player) {
										delete player.storage.lqhc_tizi;
									},
									content() {
										player.removeSkill('lqhc_tizi_block');
									},
								},
							},
						},
						lqhc_nanci: {
							enable: 'phaseUse',
							usable: 1,
							filter(event, player) {
								for (const i of lib.inpile) {
									if (get.type(i) == 'delay' && player.canAddJudge({ name: i })) {
										return true;
									}
								}
								return false;
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (get.type(name) == 'delay' && player.canAddJudge({ name: name })) {
											list.push(['锦囊', '', name]);
										}
									}

									return ui.create.dialog('难辞:选择一张牌置于你的判定区.', [list, 'vcard']);
								},
								check(button) {
									if (button.link[2] == 'lebu') {
										return 20;
									}
									return 10;
								},
								backup(links, player) {
									return {
										forced: true,
										filterCard() {
											return false;
										},
										selectCard: -1,
										card: links[0],
										delay: false,
										content: lib.skill.lqhc_nanci.contentx,
									};
								},
							},
							contentx() {
								if (player.countCards('j')) {
									player.recover();
								}
								const a = game.createCard(lib.skill.lqhc_nanci_backup.card[2]);
								if (player.canAddJudge(a)) {
									player.addJudge(a);
								}
							},
							ai: {
								order: 1.1,
								result: {
									player: 1,
								},
							},
						},
						lqhc_jiexin: {
							trigger: {
								source: 'damageSource',
							},
							check(event, player) {
								return get.attitude(player, event.player) < 0;
							},
							prompt2(event) {
								return '弃置' + get.translation(event.player) + '装备区与判定区的所有牌.';
							},
							logTarget(event, player) {
								return event.player;
							},
							filter(event, player) {
								return event.player && event.player.isAlive() && event.player.countCards('ej') && event.card && event.card.name == 'sha';
							},
							content() {
								const a = trigger.player.countCards('ej');
								trigger.player.discard(trigger.player.getCards('ej'));
								if (player.countCards('j')) {
									player.discardPlayerCard('h', trigger.player, [1, a]);
								}
							},
						},
						lqhc_huaishang: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							filter(event, player) {
								for (const i of player.getCards('j')) {
									if (
										(game.hasPlayer(function (current) {
											return current.canAddJudge(i) && current != player;
										}) ||
											player.isDamaged()) &&
										player.countCards('j')
									) {
										return true;
									}
								}
								return false;
							},
							content() {
								'step 0';
								for (const i of player.getCards('j')) {
									if (
										game.hasPlayer(function (current) {
											return current.canAddJudge(i);
										})
									) {
										var c = true;
									}
								}
								if (c) {
									player.chooseTarget('【怀殇】', '你可以将判定区的牌转移给一名其他角色.', function (card, player, target) {
										for (const i of player.getCards('j')) {
											if (target != player && target.canAddJudge(i)) {
												return true;
											}
										}
										return false;
									}).ai = function (target) {
										return -get.attitude(player, target);
									};
								}
								('step 1');
								if (result.targets?.length) {
									const b = result.targets[0];
									player.recover();
									event.finish();
									for (const i of player.getCards('j')) {
										if (b.canAddJudge(i)) {
											if (i.viewAs) {
												b.addJudge({ name: i.viewAs }, [i]);
											} else {
												b.addJudge(i);
											}
											player.$give(i, b, false);
											game.log(player, '的', i, '被移动给了', b);
										}
									}
								} else if (player.isDamaged()) {
									player.chooseBool(get.prompt2(event.name), '回复一点体力.').set('frequentSkill', event.name);
								}
								('step 2');
								if (result.bool) {
									player.recover();
								}
							},
						},
						lqhc_gengxin: {
							enable: 'phaseUse',
							usable: 1,
							prompt(event, player) {
								const a = _status.event.player.countCards('j');
								return '创建与你判定区同名的牌各一张置入' + (a == 1 ? '一' : '最多' + a) + '名角色的判定区.';
							},
							filter(event, player) {
								for (const i of player.getCards('j')) {
									if (
										game.hasPlayer(function (current) {
											return current.canAddJudge(i) && current != player;
										}) &&
										player.countCards('j')
									) {
										return true;
									}
								}
								return false;
							},
							filterTarget(card, player, target) {
								for (const i of player.getCards('j')) {
									if (target != player && target.canAddJudge(i)) {
										return true;
									}
								}
								return false;
							},
							selectTarget() {
								return [1, _status.event.player.countCards('j')];
							},
							content() {
								for (const i of player.getCards('j')) {
									const a = game.createCard(i);
									if (target.canAddJudge(a)) {
										target.addJudge(a);
									}
								}
							},
							ai: {
								order: 1,
								result: {
									target: -1,
								},
							},
						},
						lqhc_chaoneng: {
							init(player) {
								lib.card.lebu.filterTarget = lib.skill.lqhc_chaoneng.lebuTarget;
								lib.card.lebu.content = lib.skill.lqhc_chaoneng.lebucontent;
								lib.card.bingliang.filterTarget = lib.skill.lqhc_chaoneng.lebuTarget;
								lib.card.bingliang.content = lib.skill.lqhc_chaoneng.lebucontent;
								lib.card.guiyoujie.filterTarget = lib.skill.lqhc_chaoneng.lebuTarget;
								lib.card.guiyoujie.content = lib.skill.lqhc_chaoneng.lebucontent;
								lib.card.caomu.filterTarget = lib.skill.lqhc_chaoneng.lebuTarget;
								lib.card.caomu.content = lib.skill.lqhc_chaoneng.lebucontent;
								lib.card.shandian.filterTarget = lib.skill.lqhc_chaoneng.shandianTarget;
								lib.card.shandian.content = lib.skill.lqhc_chaoneng.lebucontent;
								lib.card.shandian.modTarget = lib.skill.lqhc_chaoneng.fuleimodTarget;
								lib.card.fulei.filterTarget = lib.skill.lqhc_chaoneng.shandianTarget;
								lib.card.fulei.content = lib.skill.lqhc_chaoneng.lebucontent;
								lib.card.fulei.modTarget = lib.skill.lqhc_chaoneng.fuleimodTarget;
								lib.card.hongshui.filterTarget = lib.skill.lqhc_chaoneng.shandianTarget;
								lib.card.hongshui.content = lib.skill.lqhc_chaoneng.lebucontent;
								lib.card.hongshui.modTarget = lib.skill.lqhc_chaoneng.fuleimodTarget;
								lib.card.huoshan.filterTarget = lib.skill.lqhc_chaoneng.shandianTarget;
								lib.card.huoshan.content = lib.skill.lqhc_chaoneng.lebucontent;
								lib.card.huoshan.modTarget = lib.skill.lqhc_chaoneng.fuleimodTarget;
							},
							lebuTarget(card, player, target) {
								if (
									!game.hasPlayer(function (current) {
										return current.hasSkill('lqhc_chaoneng');
									})
								) {
									return lib.filter.judge(card, player, target) && player != target;
								}
								return player != target;
							},
							shandianTarget(card, player, target) {
								if (
									!game.hasPlayer(function (current) {
										return current.hasSkill('lqhc_chaoneng');
									})
								) {
									return lib.filter.judge(card, player, target) && player == target;
								}
								return player == target;
							},
							lebucontent() {
								if (
									(game.hasPlayer(function (current) {
										return current.hasSkill('lqhc_chaoneng');
									}) ||
										lib.filter.judge(card, player, target)) &&
									cards.length &&
									get.position(cards[0], true) == 'o'
								) {
									target.addJudge(card, cards);
								}
							},
							fuleimodTarget(card, player, target) {
								if (
									!game.hasPlayer(function (current) {
										return current.hasSkill('lqhc_chaoneng');
									})
								) {
									return lib.filter.judge(card, player, target);
								}
								return true;
							},
							forced: true,
						},
						lqhc_yiyou: {
							trigger: {
								global: 'phaseBefore',
								player: 'enterGame',
								target: 'useCardToTarget',
							},
							audio: 'ext:浪琴天阙/audio:2',
							forced: true,
							filter(event, player, name) {
								if (name == 'useCardToTarget') {
									return event.targets.length == 1;
								} else {
									return event.name != 'phase' || game.phaseNumber == 0;
								}
							},
							content() {
								if (event.triggername == 'useCardToTarget') {
									if (['equip', 'trick', 'basic'].includes(get.type2(trigger.card))) {
										player.addMark('lqhc_yiyou_' + get.type2(trigger.card));
									} //QQQ
								} else {
									for (const i of ['equip', 'trick', 'basic']) {
										player.addMark('lqhc_yiyou_' + i, 2);
									}
								}
							},
							subSkill: {
								equip: {
									marktext: '兽',
									intro: {
										name: '怪兽',
										content: 'mark',
									},
								},
								trick: {
									marktext: '魔',
									intro: {
										name: '魔法',
										content: 'mark',
									},
								},
								basic: {
									marktext: '陷',
									intro: {
										name: '陷阱',
										content: 'mark',
									},
								},
							},
						},
						lqhc_chouka: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								player: 'phaseDrawBegin1',
							},
							prompt: '是否发动【抽卡】？',
							prompt2(event, player) {
								return '摸牌阶段,你可以少摸一张牌,展示牌堆顶三张牌来获得标记.';
							},
							filter(event, player) {
								return !event.numFixed && event.num > 0;
							},
							content() {
								trigger.num--;
								event.cards = get.cards(game.players.length);
								game.cardsGotoOrdering(event.cards);
								player.showCards(event.cards);
								for (const i of event.cards) {
									if (['equip', 'trick', 'basic'].includes(get.type2(event.card))) {
										player.addMark('lqhc_yiyou_' + get.type2(event.card));
									} //QQQ
								}
							},
						},
						lqhc_anjue: {
							audio: 'ext:浪琴天阙/audio:8',
							group: ['lqhc_anjue_shou', 'lqhc_anjue_mo', 'lqhc_anjue_count', 'lqhc_anjue_xian'],
							subSkill: {
								shou: {
									audio: 'ext:浪琴天阙/audio:2',
									trigger: {
										global: ['roundStart', 'phaseBefore'],
										player: 'enterGame',
									},
									usable: 1,
									_priority: -1,
									prompt: '是否发动【暗决】？',
									prompt2(event, player) {
										return '移去两枚<怪兽>标记,展示「浪琴游漫」武将栏中的随机一张武将牌并获得其所有技能.';
									},
									filter(event, player) {
										if (event.name == 'phase') {
											return game.phaseNumber == 0 && player.countMark('lqhc_yiyou_equip') > 1;
										}
										return player.countMark('lqhc_yiyou_equip') > 1;
									},
									content() {
										'step 0';
										player.removeMark('lqhc_yiyou_equip', 2);
										let b = [];
										for (const i in lib.characterPack.langqinyouman) {
											if (!lib.character[i][4].includes('minskin')) {
												b.push(i);
											}
										}
										b = b.randomGet();
										game.log(player, '展示了', '#g' + b);
										for (const i of lib.character[b][3]) {
											player.addSkillLog(i);
										}
										event.dialog = ui.create.dialog('<div class="text center">' + get.translation(player) + '展示了', [[b], 'character']);
										('step 1');
										event.dialog.close();
									},
								},
								mo: {
									enable: 'phaseUse',
									audio: 'ext:浪琴天阙/audio:2',
									filter(event, player) {
										const a = player.getStat('skill').lqhc_anjue_mo_backup || 0;
										for (const i of lib.inpile) {
											if (['equip', 'trick', 'basic'].includes(get.type2(i)) && event.filterCard && event.filterCard({ name: i }, player, event) && a < player.hp && player.countMark('lqhc_yiyou_trick') > 1) {
												return true;
											}
										}
										return false;
									},
									chooseButton: {
										dialog(event, player) {
											const list = [];

											for (const i of lib.inpile) {
												const name = i;
												if (name == 'sha') {
													if (event.filterCard && event.filterCard({ name: name }, player, event)) {
														list.push(['基本', '', 'sha']);
													}
													for (const j of lib.inpile_nature) {
														if (event.filterCard && event.filterCard({ name: name, nature: j }, player, event)) {
															list.push(['基本', '', 'sha', j]);
														}
													}
												} else if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
													list.push(['基本', '', name]);
												} else if (get.type2(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
													list.push(['锦囊', '', name]);
												} else if (get.type(name) == 'equip' && event.filterCard && event.filterCard({ name: name }, player, event)) {
													list.push(['装备', '', name]);
												}
											}

											return ui.create.dialog('暗决', [list, 'vcard']);
										},
										check(button) {
											return _status.event.player.getUseValue({ name: button.link[2] });
										},
										backup(links, player) {
											return {
												viewAs: {
													name: links[0][2],
												},
												filterCard: () => false,
												selectCard: -1,
												popname: true,
												onuse(result, player) {
													player.removeMark('lqhc_yiyou_trick', 2);
													if (['delay', 'equip'].includes(get.type(result.card))) {
														player.storage.lqhc_anjue_mo = result.card.name;
													}
												},
											};
										},
										prompt(links, player) {
											return '请选择' + get.translation(links[0][2]) + '的目标';
										},
									},
									ai: {
										order: 7,
										result: {
											player: 1,
										},
									},
								},
								count: {
									trigger: {
										player: 'useCardToBegin',
									},
									forced: true,
									_priority: 21,
									popup: false,
									filter(event, player) {
										return event.skill == 'lqhc_anjue_mo_backup' && player.storage.lqhc_anjue_mo && event.card.name == player.storage.lqhc_anjue_mo;
									},
									content() {
										const card = game.createCard(trigger.card.name, '', '');
										trigger.card = card;
										trigger.cards = [card];
										trigger.card.cards = trigger.cards;
										game.cardsGotoOrdering(card).relatedEvent = trigger;
										delete player.storage.lqhc_anjue_mo;
									},
								},
								xian: {
									trigger: {
										player: 'phaseJieshuBegin',
									},
									forced: true,
									filter(event, player) {
										return player.countMark('lqhc_yiyou_basic') > 1;
									},
									audio: 'ext:浪琴天阙/audio:2',
									content() {
										'step 0';
										player
											.chooseControl('便乘', '无限泡影', '废铁稻草人', 'cancel2')
											.set('choiceList', ['每个回合对每名角色其他角色各限一次,当这些角色获得牌时,你摸一张牌.', '每轮限一次,一名角色的回合开始时,你可以令其所有技能失效直到回合结束.', '每回合限一次,你即将受到【杀】造成的伤害时,可以取消之.'])
											.set('prompt', get.prompt('lqhc_anjue_xian'), '移去两枚<陷阱>标记,获得一个效果直到你的下个回合开始.')
											.set('ai', function () {
												return 0;
											});
										('step 1');
										const a = result.control;
										if (a != 'cancel2') {
											player.removeMark('lqhc_yiyou_basic', 2);
										}
										player.addTempSkill(a == '便乘' ? 'lqhc_anjue_mft' : a == '无限泡影' ? 'lqhc_anjue_wxpy' : 'lqhc_anjue_ftdcr', { player: 'phaseBefore' });
									},
								},
								mft: {
									trigger: {
										global: 'gainEnd',
									},
									audio: 'ext:浪琴天阙/audio:2',
									forced: true,
									logTarget: 'player',
									charlotte: true,
									filter(event, player) {
										return !event.player.hasSkill('lqhc_anjue_bc');
									},
									content() {
										trigger.player.addTempSkill('lqhc_anjue_bc');
										player.draw();
									},
								},
								wxpy: {
									trigger: {
										global: 'phaseBegin',
									},
									audio: 'lqhc_anjue_mft',
									logTarget: 'player',
									charlotte: true,
									prompt2(event, player) {
										return '令' + get.translation(event.player) + '的所有技能失效直到本回合结束.';
									},
									logTarget(event, player) {
										return event.player;
									},
									content() {
										player.removeSkill('lqhc_anjue_wxpy');
										trigger.player.addTempSkill('baiban');
									},
								},
								ftdcr: {
									trigger: {
										player: 'damageBegin4',
									},
									_priority: -Infinity,
									audio: 'lqhc_anjue_mft',
									usable: 1,
									charlotte: true,
									prompt: '是否发动【暗决】？',
									prompt2(event, player) {
										return '取消' + get.translation(event.card) + '即将对你造成的伤害.';
									},
									content() {
										trigger.cancel();
									},
								},
								bc: { charlotte: true },
							},
						},
						lqhc_longdan: {
							intro: {
								content: '已记录牌名:$',
							},
							audio: 'longdan_sha',
							enable: ['chooseToUse', 'chooseToRespond'],
							filter(event, player) {
								let a = 0,
									b = player.getStorage('lqhc_longdan');
								for (const i of lib.inpile) {
									if (get.type(i) == 'basic' && event.filterCard && event.filterCard({ name: i }, player, event) && !b.includes(i)) {
										a++;
									}
									if (((i == 'sha' && player.countCards('hs', 'shan')) || (i == 'shan' && player.countCards('hs', 'sha'))) && event.filterCard && event.filterCard({ name: i }, player, event)) {
										a++;
									}
									if (b.includes(i) && !player.hasSkill('lqhc_longdan_count') && event.type == 'phase') {
										a++;
									}
								}
								return a > 0;
							},
							hiddenCard(player, name) {
								if (name == 'sha') {
									return player.countCards('hs', 'shan');
								}
								if (name == 'shan') {
									return player.countCards('hs', 'sha');
								}
								return get.type(name) == 'basic' && !player.getStorage('lqhc_longdan').includes(name);
							},
							chooseButton: {
								dialog(event, player) {
									const a = [],
										b = [],
										c = [],
										d = player.getStorage('lqhc_longdan');

									for (const i of lib.inpile) {
										const name = i;
										if (d.includes(name) && event.type == 'phase' && !player.hasSkill('lqhc_longdan_count')) {
											c.push(['基本', '', name]);
										}
										if (name == 'sha') {
											if (event.filterCard && event.filterCard({ name: name }, player, event)) {
												if (player.countCards('hs', 'shan')) {
													a.push(['basic', '', 'sha']);
												}
												if (!d.includes(name)) {
													b.push(['基本', '', 'sha']);
												}
											}
											for (const j of lib.inpile_nature) {
												if (event.filterCard && event.filterCard({ name: name, nature: j }, player, event)) {
													if (player.countCards('hs', 'shan')) {
														a.push(['basic', '', 'sha', j]);
													}
													if (!d.includes(name)) {
														b.push(['基本', '', 'sha', j]);
													}
												}
											}
										} else if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											if (!d.includes(name)) {
												b.push(['基本', '', name]);
											}
											if (name == 'shan' && player.countCards('hs', 'sha')) {
												a.push(['basic', '', 'shan']);
											}
										}
									}

									const dialog = ui.create.dialog('<font size=5>龙胆', '<font size=3>', 'hidden');
									if (a.length) {
										dialog.add('闪当闪,闪当杀');
										dialog.add([a, 'vcard']);
									}
									if (b.length) {
										dialog.add('视为使用一张牌');
										dialog.add([b, 'vcard']);
									}
									if (c.length) {
										dialog.add('重置一张〖龙胆②〗使用过的牌');
										dialog.add([c, 'vcard']);
									}
									return dialog;
								},
								filter(button, player) {
									if (button.link[0] == '基本' && _status.event.parent.type == 'phase' && player.getStorage('lqhc_longdan').includes(button.link[2])) {
										return true;
									}
									return _status.event.parent.filterCard({ name: button.link[2] }, player, _status.event.parent);
								},
								check(button) {
									const player = _status.event.player;
									if (button.link[0] == '基本' && _status.event.type == 'phase' && player.getStorage('lqhc_longdan').includes(button.link[2]) && player.hp < 4) {
										return 0;
									}
									if (_status.event.parent.type != 'phase') {
										return 1;
									}
									return player.getUseValue({ name: button.link[2], nature: button.link[3] });
								},
								backup(links, player) {
									if (player.getStorage('lqhc_longdan').includes(links[0][2]) && links[0][0] == '基本') {
										return {
											filterCard() {
												return false;
											},
											selectCard: -1,
											card: links[0][2],
											delay: false,
											content: lib.skill.lqhc_longdan.contentx,
										};
									} else {
										return {
											filterCard(card, player) {
												if (links[0][0] == '基本') {
													return false;
												}
												if (links[0][2] == 'sha') {
													return card.name == 'shan';
												}
												if (links[0][2] == 'shan') {
													return card.name == 'sha';
												}
											},
											viewAs: { name: links[0][2], nature: links[0][3] },
											selectCard() {
												if (links[0][0] == 'basic') {
													return 1;
												}
												return -1;
											},
											popname: true,
											onuse(result, player) {
												if (links[0][0] == '基本') {
													player.markAuto('lqhc_longdan', [result.card.name]);
												}
											},
											onrespond(result, player) {
												if (links[0][0] == '基本') {
													player.markAuto('lqhc_longdan', [result.card.name]);
												}
											},
										};
									}
								},
								prompt(links, player) {
									return '视为使用一张' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]);
								},
							},
							contentx() {
								player.loseHp();
								player.unmarkAuto('lqhc_longdan', [lib.skill.lqhc_longdan_backup.card]);
								game.log(player, '从〖龙胆②〗中移除了', '#y' + get.translation(lib.skill.lqhc_longdan_backup.card));
								player.addTempSkill('lqhc_longdan_count');
							},
							ai: {
								fireAttack: true,
								respondSha: true,
								respondShan: true,
								skillTagFilter(player) {
									return true;
								},
								order: 9,
								result: {
									player(player) {
										if (_status.event.dying) {
											return get.attitude(player, _status.event.dying);
										}
										return 1;
									},
								},
							},
						},
						lqhc_longdan_count: {
							forced: true,
							charlotte: true,
						},
						lqhc_qinggang: {
							audio: 'chongzhen',
							trigger: {
								player: 'useCardToPlayered',
								target: 'useCardToTargeted',
							},
							shaRelated: true,
							logTarget: 'target',
							forced: true,
							filter(event, player) {
								return ['sha', 'juedou'].includes(event.card.name);
							},
							content() {
								'step 0';
								let a = '令' + get.translation(player) + '获得你一张牌',
									b = '令' + get.translation(player) + '摸一张牌且' + get.translation(trigger.card) + '无视防具';
								((event.c = trigger.player), (event.d = trigger.target));
								if (trigger.player == player) {
									((a = '你获得' + get.translation(trigger.target) + '一张牌'), (b = '你摸一张牌且' + get.translation(trigger.card) + '无视防具'));
									((event.c = trigger.target), (event.d = trigger.player));
								}
								const list = [],
									list2 = [a, b, '你失去一点体力令' + get.translation(trigger.card) + '伤害基数+1'];
								if (event.c.countGainableCards(event.d, 'he')) {
									list.push('选项一');
								}
								list.push('选项二');
								list.push('选项三');
								trigger.player.chooseControl(list).set('choiceList', list2).set('prompt', '【青钢】').ai = function (target) {
									if (trigger.player == player) {
										if (get.attitude(player, event.c) > 0) {
											return '选项二';
										} else if (list.includes('选项一')) {
											return '选项一';
										}
									}
									return '选项二';
								};
								('step 1');
								if (result.control == '选项一') {
									event.d.gainPlayerCard('he', event.c, true);
								}
								if (result.control == '选项二') {
									event.d.draw();
									trigger.player.storage.lqhc_qinggang = trigger.card;
									trigger.player.addTempSkill('lqhc_qinggang_unequip');
									game.log(trigger.card, '无视防具');
								}
								if (result.control == '选项三') {
									trigger.player.loseHp();
									const e = trigger.parent;
									if (typeof e.baseDamage != 'number') {
										e.baseDamage = 1;
									}
									e.baseDamage++;
									game.log(trigger.card, '伤害基数+1');
								}
							},
						},
						lqhc_qinggang_unequip: {
							trigger: {
								player: 'useCardAfter',
							},
							popup: false,
							forced: true,
							charlotte: true,
							filter(event, player) {
								return player.storage.lqhc_qinggang && player.storage.lqhc_qinggang == event.card;
							},
							content() {
								player.removeSkill('lqhc_qinggang_unequip');
								delete player.storage.lqhc_qinggang;
							},
							ai: {
								unequip: true,
								unequip: true,
								skillTagFilter(player, tag, arg) {
									if (tag == 'unequip') {
										if (arg && player.storage.lqhc_qinggang && player.storage.lqhc_qinggang == arg.card) {
											return true;
										}
										return false;
									}
								},
							},
						},
						lqhc_lingqiao: {
							mod: {
								cardUsable(card, player, num) {
									if (card.name == 'jiu') {
										return Infinity;
									}
								},
							},
							trigger: {
								player: 'useCard',
							},
							forced: true,
							filter(event, player) {
								return event.card && event.card.name == 'jiu';
							},
							content() {
								'step 0';
								trigger.cancel();
								const list = [];
								if (
									game.hasPlayer(function (current) {
										return player.canUse('juedou', current);
									})
								) {
									list.push('来决斗吧');
								}
								if (
									game.hasPlayer(function (current) {
										return current.isDamaged();
									})
								) {
									list.push('回复体力');
								}
								if (!list.length) {
									player.chooseBool('场上没有角色可成为决斗的目标且没有角色受伤.');
									event.finish();
								} else {
									player.chooseControl(list).set('choiceList', ['视为使用一张决斗', '失去一点体力令一名角色回复两点体力']).set('prompt', '【凌峭】').ai = function (target) {
										if (player.hp < 3) {
											return '回复体力';
										}
										if (list.includes('来决斗吧')) {
											return '来决斗吧';
										}
										return '回复体力';
									};
								}
								('step 1');
								if (result.control == '来决斗吧') {
									player.chooseUseTarget('【凌峭】', '视为使用一张决斗.', { name: 'juedou' }, true);
									event.finish();
								} else {
									player.loseHp();
								}
								('step 2');
								if (
									game.hasPlayer(function (current) {
										return current.isDamaged();
									})
								) {
									player.chooseTarget(true, '【凌峭】', '失去一点体力令一名角色回复两点体力', function (card, player, target) {
										return target.isDamaged();
									}).ai = function (target) {
										if ((target = player)) {
											return 20;
										}
										return get.recoverEffect(_status.event.player, target);
									};
								}
								('step 3');
								if (result.targets?.length) {
									result.targets[0].recover(2);
								}
							},
						},
						lqhc_shuiqun: {
							trigger: {
								global: 'useCardEnd',
							},
							intro: {
								content: 'expansion',
								markcount: 'expansion',
							},
							onremove(player, skill) {
								const cards = player.getExpansions('lqhc_shuiqun');
								if (cards.length) {
									player.loseToDiscardpile(cards);
								}
							},
							filter(event, player) {
								return event.targets && event.targets.includes(player);
							},
							forced: true,
							content() {
								'step 0';
								player.draw();
								('step 1');
								if (player.countCards('he')) {
									player.chooseCard('将一张手牌置于武将牌上作为<水>', true).set('ai', function (card) {
										return _status.event.player.getUseValue(card);
									});
								} else {
									event.goto(3);
								}
								('step 2');
								if (result.cards?.length) {
									player.addToExpansion(result.cards, player, 'giveAuto').gaintag.add('lqhc_shuiqun');
								}
								('step 3');
								if (player.getExpansions('lqhc_shuiqun').length) {
									player.loseToSpecial(player.getExpansions('lqhc_shuiqun'), 'lqhc_shuiqun_s').visible = true;
								}
								('step 4');
								player
									.chooseToUse(function (card, player, event) {
										return lib.filter.filterCard.apply(this, arguments);
									}, '水群:是否使用一张牌？')
									.set('complexSelect', true)
									.set('filterTarget', function (card, player, target) {
										return lib.filter.targetEnabled.apply(this, arguments);
									});
								('step 5');
								if (
									player.countCards('s', function (card) {
										return card.hasGaintag('lqhc_shuiqun_s');
									})
								) {
									player
										.addToExpansion(
											player.getCards('s', function (card) {
												return card.hasGaintag('lqhc_shuiqun_s');
											}),
											player,
											'give',
										)
										.gaintag.add('lqhc_shuiqun');
								}
							},
							group: ['lqhc_shuiqun_s', 'lqhc_shuiqun_dying'],
							subSkill: {
								s: {
									trigger: {
										global: ['useCardBefore', 'dyingEnd', 'dieBefore'],
									},
									forced: true,
									popup: false,
									_priority: Infinity,
									filter(event, player) {
										if (event.name == 'useCard') {
											return !_status.dying.length;
										}
										return player.countCards('s', function (card) {
											return card.hasGaintag('lqhc_shuiqun_s');
										});
									},
									content() {
										player
											.addToExpansion(
												player.getCards('s', function (card) {
													return card.hasGaintag('lqhc_shuiqun_s');
												}),
												player,
												'give',
											)
											.gaintag.add('lqhc_shuiqun');
									},
									marktext: '水',
									intro: {
										mark(dialog, storage, player) {
											dialog.addAuto(
												player.getCards('s', function (card) {
													return card.hasGaintag('lqhc_shuiqun_s');
												}),
											);
										},
										markcount(storage, player) {
											return player.getCards('s', function (card) {
												return card.hasGaintag('lqhc_shuiqun_s');
											}).length;
										},
										onunmark(storage, player) {
											const cards = player.getCards('s', function (card) {
												return card.hasGaintag('lqhc_shuiqun_s');
											});
											if (cards.length) {
												player.lose(cards, ui.discardPile);
												player.$throw(cards, 1000);
												game.log(cards, '进入了弃牌堆');
											}
										},
									},
								},
								dying: {
									trigger: {
										global: 'dying',
									},
									forced: true,
									_priority: 101,
									popup: false,
									filter(event, player) {
										return _status.dying.length && player.getExpansions('lqhc_shuiqun').length;
									},
									content() {
										player.loseToSpecial(player.getExpansions('lqhc_shuiqun'), 'lqhc_shuiqun_s').visible = true;
									},
								},
							},
						},
						lqhc_qianshui: {
							trigger: {
								global: 'phaseUseEnd',
								player: 'dyingAfter',
							},
							filter(event, player) {
								return player.hasHistory('useSkill', function (evt) {
									return evt.skill == 'lqhc_shuiqun';
								});
							},
							prompt2(event, player) {
								if (player != _status.currentPhase) {
									return '可以令你或' + get.translation(_status.currentPhase) + '进入隐匿状态.';
								}
								return '你进入隐匿状态.';
							},
							logTarget(event, player) {
								return _status.currentPhase;
							},
							check(event, player) {
								const a = _status.currentPhase;
								if (player == a && player.hp > 1) {
									return false;
								}
								return true;
							},
							content() {
								'step 0';
								const a = player.getExpansions('lqhc_shuiqun');
								player.loseToDiscardpile(a);
								player.draw(a.length);
								('step 1');
								if (_status.currentPhase != player) {
									player.chooseControlList(true, ['你进入隐匿状态', get.translation(_status.currentPhase) + '进入隐匿状态'], function () {
										const a = _status.currentPhase,
											b = get.attitude(_status.event.player, a);
										if (player.hp < 2) {
											return 0;
										}
										if ((b < 0 && a.hp > 1 && a.countCards('h') > 1) || (b > 0 && a.hp < 2)) {
											return 1;
										}
										return 0;
									});
								} else {
									event._result = { index: 0 };
								}
								('step 2');
								const playerx = result.index == 0 ? player : _status.currentPhase;
								playerx.storage.rawHp = playerx.hp;
								playerx.storage.rawMaxHp = playerx.maxHp;
								if (playerx.skills.length) {
									if (!playerx.hiddenSkills) {
										playerx.hiddenSkills = [];
									}
									for (const i of playerx.skills.slice()) {
										playerx.removeSkill(i);
										playerx.hiddenSkills.add(i);
									}
								}
								playerx.classList.add('unseen');
								playerx.name = 'unknown';
								playerx.sex = 'male';
								playerx.storage.nohp = true;
								playerx.node.hp.hide();
								playerx.node.avatar.setBackground('unknown', 'character');
								playerx.addSkill('g_hidden_ai');
								playerx.hp = 1;
								playerx.maxHp = 1;
								playerx.update();
							},
						},
						lqhc_maopao: {
							trigger: {
								player: 'showCharacterAfter',
							},
							forced: true,
							hiddenSkill: true,
							filter(event, player) {
								return player.countCards('h');
							},
							content() {
								'step 0';
								player.chooseCard('【冒泡】', '将一张手牌置于武将牌上作为<水>.').set('ai', function (card) {
									if (_status.event.player.getUseValue(card) > 0) {
										return _status.event.player.getUseValue(card);
									}
									return true;
								});
								('step 1');
								if (result.cards?.length) {
									player.addToExpansion(result.cards, player, 'giveAuto').gaintag.add('lqhc_shuiqun');
								}
							},
						},
						//浪琴游漫
						lqym_heiyan: {
							limited: true,
							derivation: 'lqym_fulianzhenhongya',
							enable: 'phaseUse',
							selectCard: 2,
							complexCard: true,
							filterCard(card, player, event) {
								if (ui.selected.cards.length) {
									return get.color(card) != get.color(ui.selected.cards[0]);
								} //已选择长度
								return true;
							},
							filter(event, player) {
								return player.countCards('he', { color: 'red' }) && player.countCards('he', { color: 'black' });
							},
							position: 'he',
							check(card) {
								return 8 - get.value(card);
							},
							filterTarget: true,
							content() {
								'step 0';
								player.awakenSkill('lqym_heiyan');
								const dialog = ui.create.dialog('选择来源', 'hidden');
								dialog.add('<font size=1>选择失去体力选项时不需选择其他选项');
								const sex = ['失去体力', '无来源', '来源为你'];
								event.s = sex;
								const yi = document.createElement('div');
								yi.classList.add('add-setting');
								yi.style.margin = '0';
								yi.style.width = '100%';
								yi.style.position = 'relative';

								for (const i of sex) {
									var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
									td.innerHTML = get.translation(i);
									td.link = i;
									td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
									Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
									yi.appendChild(td);
									dialog.buttons.add(td);
								}

								dialog.content.appendChild(yi);
								dialog.add('选择伤害');
								const group = ['普通', 'fire', 'thunder', 'ice', 'kami'];
								const er = document.createElement('div');
								er.classList.add('add-setting');
								er.style.margin = '0';
								er.style.width = '100%';
								er.style.position = 'relative';

								for (const i of group) {
									var td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
									td.innerHTML = get.translation(i);
									td.link = i;
									td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
									Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
									er.appendChild(td);
									dialog.buttons.add(td);
								}

								dialog.content.appendChild(er);
								var a = player.chooseButton(dialog, true);
								a.set('selectButton', function () {
									if (ui.selected.buttons.length && ui.selected.buttons[0].link == '失去体力') {
										return [1, 2];
									}
									return 2;
								});
								a.set('ai', function (button) {
									if (target.hasSkillTag('maixie') || target.hasSkillTag('maixie_hp')) {
										if (!target.isDamaged()) {
											return button.link == '来源为你' || button.link == 'kami';
										} else if (button.link == '失去体力') {
											return 19;
										}
									}
									if (get.damageEffect(target, player, player, 'fire') > 0 && (button.link == '来源为你' || button.link == 'fire')) {
										return 20;
									}
									if (get.damageEffect(target, player, player, 'thunder') > 0 && (button.link == '来源为你' || button.link == 'thunder')) {
										return 20;
									}
									if (target.getEquip(2) && (button.link == '来源为你' || button.link == 'ice')) {
										return 20;
									}
									return button.link == '来源为你' || button.link == 'thunder';
								}).set('filterButton', function (button) {
									if (ui.selected.buttons.length) {
										const a = ui.selected.buttons[0].link,
											b = button.link;
										if (a == '失去体力' || (a != '失去体力' && b == '失去体力')) {
											return false;
										}
										return (sex.includes(a) && !sex.includes(b)) || (!sex.includes(a) && sex.includes(b));
									}
									return true;
								});
								('step 1');
								if (result.links?.length) {
									for (const i of result.links) {
										if (event.s.includes(i)) {
											var a = i;
										} else {
											var b = i;
										}
									}
									let c = '',
										d = '';
									if (a == '失去体力') {
										target.loseHp();
									} else {
										if (a == '无来源') {
											c = 'nosource';
										}
										if (b != '普通') {
											d = b;
										}
										target.damage(c, d);
									}
								}
								('step 2');
								var b = 'lqym_fulianzhenhongya';
								if (!lib.card[b]) {
									lib.card[b] = get.copy(lib.skill.lqym_heiyan[b]);
									lib.translate[b] = get.translation(b);
									lib.translate[b + '_info'] = get.translation(b + '_info');
									game.broadcastAll(function () {
										lib.inpile.add(b);
									});
								}
								player.gain(game.createCard(b, 'spade', 1), 'gain');
							},
							ai: {
								order: 5,
								result: {
									target(player, target) {
										if (target.hasSkillTag('maixie') || target.hasSkillTag('maixie_hp')) {
											return -2;
										}
										if (get.damageEffect(target, player, player, 'fire') > 0 && target.hp == 1 && target.countCards('e', 'tengjia')) {
											return -2;
										}
										if (target.hp == 1) {
											return -1;
										}
										return 0;
									},
								},
							},
							lqym_fulianzhenhongya: {
								audio: true,
								fullskin: true,
								type: 'delay',
								derivation: 'lqym_zhenhongyanheilong',
								image: 'ext:浪琴天阙/image/卡牌图/lqym_fulianzhenhongya.png',
								filterTarget(card, player, target) {
									return lib.filter.judge(card, player, target) && player != target;
								},
								judge(card) {
									if (card.suit == 'heart' || card.suit == 'spade') {
										return -2;
									}
									return 1;
								},
								judge2(result) {
									if (result.bool == false) {
										return true;
									}
									return false;
								},
								effect() {
									if (result.bool == false) {
										player.damage('fire', 'nosource');
									}
									const a = game.filterPlayer(function (current) {
										return [current.name, current.name1, current.name2].includes('lqym_zhenhongyanheilong');
									});
									if (a.length) {
										for (const i of a) {
											i.gain(game.createCard('lqym_fulianzhenhongya', 'spade', 1), 'gain');
										}
									}
								},
								cancel() {
									const a = game.filterPlayer(function (current) {
										return [current.name, current.name1, current.name2].includes('lqym_zhenhongyanheilong');
									});
									if (a.length) {
										for (const i of a) {
											i.gain(game.createCard('lqym_fulianzhenhongya', 'spade', 1), 'gain');
										}
									}
								},
								ai: {
									basic: {
										order: 1,
										useful: 1,
										value: 8,
									},
									result: {
										target: -1,
									},
									tag: {
										damage: 1,
									},
								},
								selectTarget: 1,
								enable: true,
								content() {
									if (lib.filter.judge(card, player, target) && cards.length && get.position(cards[0], true) == 'o') {
										target.addJudge(card, cards);
									}
								},
								allowMultiple: false,
							},
						},
						lqym_honghun: {
							enable: 'phaseUse',
							filterCard: true,
							filter(event, player) {
								return player.countCards('he');
							},
							position: 'he',
							check(card) {
								if (get.type(card) == 'delay' || get.translation(card.name).length > 4) {
									return 12 - get.value(card);
								}
								if (card.number == 1) {
									10 - get.value(card);
								}
								return 8 - get.value(card);
							},
							usable: 1,
							content() {
								'step 0';
								event.count = 1;
								if (get.type(cards[0]) == 'delay') {
									event.count++;
								}
								if (get.translation(cards[0].name).length > 4) {
									event.count++;
								}
								if (cards[0].number == 1) {
									event.count++;
								}
								event.videoId = lib.status.videoId++;
								const func = function (id) {
									const list1 = ['令一名角色摸两张牌', '令一名角色回复一点体力', '对一名其他角色造成一点火焰伤害', '获得一名其他角色一张牌'];
									const choiceList = ui.create.dialog('红玉:请选择' + (event.count > 1 ? '1～' + event.count + '项' : '一项'), 'forcebutton');
									choiceList.videoId = id;
									for (let i = 0; i < list1.length; i++) {
										let str = '<div class="popup text" style="width:calc(100% - 10px);display:inline-block">';
										if (
											(i == 1 &&
												!game.hasPlayer(function (current) {
													return current.isDamaged();
												})) ||
											(i == 3 &&
												!game.hasPlayer(function (current) {
													return current != player && current.countGainableCards(player, 'he');
												}))
										) {
											str += '<div style="opacity:0.5">';
										}
										str += list1[i];
										if (
											(i == 1 &&
												!game.hasPlayer(function (current) {
													return current.isDamaged();
												})) ||
											(i == 3 &&
												!game.hasPlayer(function (current) {
													return current != player && current.countGainableCards(player, 'he');
												}))
										) {
											str += '</div>';
										}
										str += '</div>';
										const next = choiceList.add(str);
										next.firstChild.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
										next.firstChild.link = i;
										Object.setPrototypeOf(next, lib.element.Button.prototype); //QQQ
										choiceList.buttons.add(next.firstChild);
									}
									return choiceList;
								};
								if (player.isOnline2()) {
									player.send(func, event.videoId);
								}
								event.dialog = func(event.videoId);
								if (player != game.me || _status.auto) {
									event.dialog.style.display = 'none';
								}
								const next = player.chooseButton(true);
								next.set('dialog', event.videoId);
								next.set('forced', true);
								next.set('selectButton', [1, event.count]);
								next.set('filterButton', function (button) {
									return button.link == 1
										? game.hasPlayer(function (current) {
											return current.isDamaged();
										})
										: button.link == 3
											? game.hasPlayer(function (current) {
												return current != _status.event.player && current.countGainableCards(_status.event.player, 'he');
											})
											: true;
								});
								next.set('ai', function (button) {
									const player = _status.event.player;
									switch (button.link) {
										case 0: {
											if (event.count > 1) {
												return 3;
											}
											return 1;
										}
										case 1: {
											if (
												game.hasPlayer(function (current) {
													return get.attitude(player, current) > 0 && current.hp < 3 && current.isDamaged();
												})
											) {
												return 3;
											}
											return 0;
										}
										case 2: {
											if (
												game.hasPlayer(function (current) {
													return get.damageEffect(current, player, player, 'fire') > 0;
												})
											) {
												return 2;
											}
											return 1.5;
										}
										case 3: {
											return 1;
										}
									}
								});
								('step 1');
								if (player.isOnline2()) {
									player.send('closeDialog', event.videoId);
								}
								event.dialog.close();
								const map = [
									function (trigger, player, event) {
										event.i1 = true;
									},
									function (trigger, player, event) {
										event.i2 = true;
									},
									function (trigger, player, event) {
										event.i3 = true;
									},
									function (trigger, player, event) {
										event.i4 = true;
									},
								];
								for (const i of result.links) {
									map[i](trigger, player, event);
								}
								('step 2');
								if (event.i1 == true) {
									player.chooseTarget('令一名角色摸两张牌.').set('ai', function (target) {
										return get.attitude(_status.event.player, target);
									});
								} else {
									event.goto(4);
								}
								('step 3');
								if (result.targets?.length) {
									result.targets[0].draw(2);
								}
								('step 4');
								if (event.i2 == true) {
									player
										.chooseTarget('令一名角色回复一点体力.', function (card, player, target) {
											return target.isDamaged();
										})
										.set('ai', function (target) {
											return get.recoverEffect(_status.event.player, target);
										});
								} else {
									event.goto(6);
								}
								('step 5');
								if (result.targets?.length) {
									result.targets[0].recover();
								}
								('step 6');
								if (event.i3 == true) {
									player.chooseTarget('对一个其他角色造成一点火焰伤害.', function (card, player, target) {
										return target != player;
									}).ai = function (target) {
										return get.damageEffect(target, player, player, 'fire');
									};
								} else {
									event.goto(8);
								}
								('step 7');
								if (result.targets?.length) {
									result.targets[0].damage('fire');
								}
								('step 8');
								if (event.i4 == true) {
									player
										.chooseTarget('获得一名其他角色一张牌.', function (card, player, target) {
											return target != player && target.countGainableCards(player, 'he');
										})
										.set('ai', function (target) {
											return -get.attitude(_status.event.player, target);
										});
								} else {
									event.finish();
								}
								('step 9');
								if (result.targets?.length) {
									player.gainPlayerCard(true, result.targets[0], 'he');
								}
							},
							ai: {
								order: 4.9,
								result: {
									player: 2,
								},
							},
						},
						lqym_modao: {
							derivation: ['lqym_modao1', 'lqym_mozhen', 'lqym_qianbadao', 'lqym_moshuzhouwenshu', 'lqym_siwangmoshuxiang', 'lqym_moshulimao'],
							position: 'he',
							enable: 'phaseUse',
							usable: 1,
							intro: { content: '已记录花色:$' },
							filterCard: true,
							selectCard: 1,
							check(card, player) {
								var player = _status.event.player;
								if (player.getStorage('lqym_modao').includes(card.suit)) {
									return 0;
								} else if (card.suit == 'club') {
									return 8 - get.value(card) + card.number;
								}
								return 8 - get.value(card);
							},
							prompt() {
								return '♠️️️️:千把刀/♥️️️️:魔术咒文书/♣️️️️:死亡魔术箱/♦️️️️:魔术礼帽';
							},
							filter(event, player) {
								return player.countCards('he');
							},
							content() {
								'step 0';
								const a = cards[0].suit,
									b = a == 'spade' ? 'lqym_qianbadao' : a == 'heart' ? 'lqym_moshuzhouwenshu' : a == 'club' ? 'lqym_siwangmoshuxiang' : 'lqym_moshulimao';
								if (!lib.card[b]) {
									lib.card[b] = get.copy(lib.skill.lqym_modao[b]);
									lib.translate[b] = get.translation(b);
									lib.translate[b + '_info'] = get.translation(b + '_info');
									game.broadcastAll(function () {
										lib.inpile.add(b);
									});
								}
								player.gain(game.createCard(b, a, cards[0].number), 'gain2');
								('step 1');
								if (player.hasSkill('lqym_modao') && !player.storage.lqym_modao1) {
									player.markAuto('lqym_modao', [cards[0].suit]);
								}
							},
							ai: {
								order: 10,
								result: {
									player(player) {
										for (const i of player.getCards('he')) {
											if (!player.getStorage('lqym_modao').includes(i.suit) && get.value(i) <= 8) {
												return 1;
											}
										}
										return -1;
									},
								},
							},
							group: ['lqym_modao_achieve', 'lqym_modao_fail'],
							global: ['lqym_moshulimaoskill', 'lqym_siwangmoshuxiangskill'],
							subSkill: {
								hand: {
									marktext: '魔',
									intro: {
										name: '魔术咒文书',
										content: '你本回合手牌上限+#',
									},
									charlotte: true,
									mod: {
										maxHandcard(player, num) {
											return num + player.countMark('lqym_modao_hand');
										},
									},
								},
								achieve: {
									trigger: {
										player: 'phaseZhunbeiBegin',
									},
									forced: true,
									filter(event, player, name) {
										return player.getStorage('lqym_modao').length > 2 && !player.storage.lqym_modao1;
									},
									content() {
										'step 0';
										game.log(player, '使命成功');
										player.awakenSkill('lqym_modao');
										for (const i of lib.suit) {
											if (!player.getStorage('lqym_modao').includes(i)) {
												var a = i;
											}
										}
										if (a) {
											const b = a == 'spade' ? 'lqym_qianbadao' : a == 'heart' ? 'lqym_moshuzhouwenshu' : a == 'club' ? 'lqym_siwangmoshuxiang' : 'lqym_moshulimao';
											if (!lib.card[b]) {
												lib.card[b] = get.copy(lib.skill.lqym_modao[b]);
												lib.translate[b] = get.translation(b);
												lib.translate[b + '_info'] = get.translation(b + '_info');
												game.broadcastAll(function () {
													lib.inpile.add(b);
												});
											}
											player.gain(game.createCard(b, a, 13), 'gain2');
										}
										player.addSkill('lqym_mozhen');
									},
								},
								fail: {
									trigger: {
										player: 'gainBegin',
									},
									forced: true,
									filter(event, player) {
										return event.parent.name == 'lqym_modao' && player.getStorage('lqym_modao').includes(event.cards[0].suit) && !player.storage.lqym_modao1;
									},
									content() {
										player.storage.lqym_modao1 = true;
										game.log(trigger.parent.name);
										player.unmarkSkill('lqym_modao');
										game.log(player, '使命失败');
									},
								},
							},
							lqym_qianbadao: {
								enable: true,
								type: 'trick',
								fullskin: true,
								derivation: 'lqym_heimodao',
								filterTarget: lib.filter.notMe,
								content() {
									'step 0';
									target.damage();
									('step 1');
									const hs = target.getCards('he');
									if (hs.length) {
										target.discard(hs.randomGet());
									}
								},
								ai: {
									order: 5,
									useful: 5,
									value: 9,
									result: {
										target: -1,
									},
									tag: {
										damage: 1,
										loseCard: 1,
										discard: 1,
									},
								},
								selectTarget: 1,
								image: 'ext:浪琴天阙/image/卡牌图/lqym_qianbadao.png',
							},
							lqym_moshuzhouwenshu: {
								enable: true,
								type: 'trick',
								fullskin: true,
								derivation: 'lqym_heimodao',
								filterTarget: true,
								content() {
									target.recover();
									target.draw();
									target.addTempSkill('lqym_modao_hand');
									target.addMark('lqym_modao_hand', 1, false);
								},
								ai: {
									order: 5,
									useful: 6,
									value: 8,
									result: {
										target: 1,
									},
									tag: {
										recover: 1,
										draw: 1,
									},
								},
								selectTarget: 1,
								image: 'ext:浪琴天阙/image/卡牌图/lqym_moshuzhouwenshu.png',
							},
							lqym_siwangmoshuxiang: {
								fullskin: true,
								type: 'trick',
								derivation: 'lqym_heimodao',
								filterTarget: lib.filter.notMe,
								content() {
									'step 0';
									var list = '死亡魔术箱:将一张牌交给' + get.translation(player);
									if (card && cards[0]) {
										list = '死亡魔术箱:将一张点数大于' + get.translation(cards[0].number) + '的' + get.translation(cards[0].suit) + '牌交给' + get.translation(player);
									}
									target.chooseCard('h', list, function (card) {
										if (card && cards[0]) {
											return card.suit == cards[0].suit && card.number > cards[0].number;
										}
										return true;
									}).ai = function (card) {
										if (card.name != 'tao') {
											return 20 - get.value(card);
										}
										return 8 - get.value(card);
									};
									('step 1');
									if (result.cards?.length) {
										player.gain(result.cards[0], 'draw2');
									} else {
										const info = event.getParent(2).lqym_siwangmoshuxianginfo || event.getParent(3).lqym_siwangmoshuxianginfo;
										info.evt.cancel();
										var list = info.source;
										if (!info.source) {
											list = 'nosource';
										}
										target.damage(info.evt.num, list);
									}
								},
								image: 'ext:浪琴天阙/image/卡牌图/lqym_siwangmoshuxiang.png',
								ai: {
									order: 4,
									useful: [7, 4],
									value: [10, 4],
									result: {
										target: -1,
									},
									tag: {
										damage: 1,
										loseCard: 1,
										gain: 1,
									},
								},
								selectTarget: 1,
							},
							lqym_moshulimao: {
								fullskin: true,
								type: 'trick',
								derivation: 'lqym_heimodao',
								notarget: true,
								wuxieable: true,
								content() {
									'step 0';
									event.info = event.getParent(2).lqym_moshulimaoinfo || event.getParent(3).lqym_moshulimaoinfo;
									event.getParent(3)._trigger.neutralize();
									let list = [],
										list1 = [];
									get.cardPile(function (card) {
										if (!list1.includes(card.name) && get.type(card) == 'trick' && event.info.source.canUse(card, player)) {
											list.add(card);
											list1.add(card.name);
										}
									});
									list = list.randomGets(3);
									player.chooseButton(true, ['选择一张牌令' + get.translation(event.info.source) + '对你使用.', [list, 'vcard']]).ai = function (button) {
										return -get.value(card);
									};
									('step 1');
									if (result.links?.length) {
										event.a = result.links[0];
										event.info.source.useCard(event.a, player);
									}
									('step 2');
									player.gain(event.a, 'gain2');
								},
								image: 'ext:浪琴天阙/image/卡牌图/lqym_moshulimao.png',
								ai: {
									basic: {
										useful: [7, 4],
										value: [8, 4],
									},
									result: { player: 1 },
									tag: {
										loseCard: 1,
										gain: 1,
									},
								},
							},
						},
						lqym_mozhen: {
							trigger: { global: 'phaseDrawBegin' },
							filter(event, player) {
								return player.hasMark('lqym_mozhen') && !event.numFixed;
							},
							logTarget(event, player) {
								return event.player;
							},
							check(event, player) {
								return get.attitude(player, event.player) > 0 && player.countMark('lqym_mozhen') > 3;
							},
							prompt2(event, player) {
								return '可以移去一枚<魔力>标记,令' + get.translation(event.player) + '多摸一张牌.';
							},
							intro: {
								name: '魔力',
								content: 'mark',
							},
							content() {
								player.removeMark('lqym_mozhen');
								trigger.num++;
							},
							group: ['lqym_mozhen_gain', 'lqym_mozhen_add'],
							global: ['lqym_moshulimaoskill', 'lqym_siwangmoshuxiangskill'],
							subSkill: {
								gain: {
									enable: 'phaseUse',
									usable: 1,
									filter(event, player) {
										return player.countMark('lqym_mozhen') > 1;
									},
									ai: {
										order: 10,
										result: {
											player: 1,
										},
									},
									content() {
										'step 0';
										player.removeMark('lqym_mozhen', 2);
										const a = [],
											b = ['lqym_qianbadao', 'lqym_moshuzhouwenshu', 'lqym_siwangmoshuxiang', 'lqym_moshulimao'];
										for (const i of b) {
											const c = get.cardPile(function (card) {
												return card.name == i;
											});
											if (c) {
												a.add(c);
											} else {
												if (!lib.card[i]) {
													lib.card[i] = get.copy(lib.skill.lqym_modao[i]);
													lib.translate[i] = get.translation(i);
													lib.translate[i + '_info'] = get.translation(i + '_info');
													game.broadcastAll(function () {
														lib.inpile.add(i);
													});
												}
												a.add(game.createCard(i, i == 'lqym_qianbadao' ? 'spade' : i == 'lqym_moshuzhouwenshu' ? 'heart' : i == 'lqym_siwangmoshuxiang' ? 'club' : 'diamond', [10, 11, 12, 13].randomGet()));
											}
										}
										const dialog = ui.create.dialog('魔阵:请选择一张牌获得', a, 'hidden');
										player.chooseButton(dialog, true).set('ai', function (button) {
											return _status.event.player.getUseValue(button.link[2]);
										});
										('step 1');
										if (result.links?.length) {
											player.gain(result.links, 'gain2');
										}
									},
								},
								add: {
									trigger: { global: 'equipEnd' },
									forced: true,
									content() {
										player.addMark('lqym_mozhen');
									},
								},
							},
						},
						lqym_siwangmoshuxiangskill: {
							trigger: { player: 'damageBefore' },
							forced: true,
							filter(event, player) {
								return player.hasUsableCard('lqym_siwangmoshuxiang');
							},
							content() {
								event.lqym_siwangmoshuxianginfo = {
									source: trigger.source,
									evt: trigger,
								};
								player
									.chooseToUse(function (card, player) {
										if (card.name != 'lqym_siwangmoshuxiang') {
											return false;
										}
										return lib.filter.cardEnabled(card, player, 'forceEnable');
									}, '是否使用【死亡魔术箱】？')
									.set('source', trigger.source);
							},
						},
						lqym_moshulimaoskill: {
							trigger: { target: 'useCardToBegin' },
							forced: true,
							_priority: 6,
							filter(event, player) {
								return event.targets.length == 1 && ['basic', 'trick'].includes(get.type(event.card)) && player.hasUsableCard('lqym_moshulimao') && !event.directHit;
							},
							content() {
								event.lqym_moshulimaoinfo = {
									source: trigger.player,
									evt: trigger,
								};
								const next = player.chooseToUse();
								next.set('prompt', '是否使用【魔术礼帽】响应' + get.translation(trigger.player) + '使用的' + get.translation(trigger.card) + '？');
								next.set('filterCard', function (card, player) {
									if (card.name != 'lqym_moshulimao') {
										return false;
									}
									return lib.filter.cardEnabled(card, player, 'forceEnable');
								});
								next.set('respondTo', [trigger.player, trigger.card]);
								next.set('goon', -get.effect(player, trigger.card, trigger.player, player));
								next.set('ai1', function (card) {
									return _status.event.goon;
								});
							},
						},
						lqym_mieyan: {
							mark: true,
							zhuanhuanji: true,
							marktext: '☯',
							intro: {
								content(storage, player, skill) {
									if (player.storage.lqym_mieyan == true) {
										return '出牌阶段限一次,弃置一名其他角色一张手牌,若那张牌的名称字数大于2,你对该角色造成一点雷电伤害,否则,你摸两张牌.';
									}
									return '出牌阶段限一次,对一名其他角色造成一点火焰伤害,该角色随机弃置一张牌';
								},
							},
							enable: 'phaseUse',
							usable: 1,
							filterTarget(card, player, target) {
								if (target == player) {
									return false;
								}
								if (player.storage.lqym_mieyan == true) {
									return target.countCards('h');
								}
								return true;
							},
							content() {
								'step 0';
								player.changeZhuanhuanji('lqym_mieyan');
								if (player.storage.lqym_mieyan == true) {
									target.damage('fire');
									const hs = target.getCards('he');
									if (hs.length) {
										target.discard(hs.randomGet());
									}
									event.finish();
								} else {
									player.discardPlayerCard(target, true, 'h');
								}
								('step 1');
								if (result.links?.length) {
									const num = get.translation(result.links[0].name).length;
									if (num > 2) {
										target.damage('thunder');
									} else {
										player.draw(2);
									}
								}
							},
							ai: {
								order: 10,
								result: {
									target: -2,
								},
							},
						},
						lqym_quan: {
							trigger: { target: 'useCardToTarget' },
							forced: true,
							filter(event, player) {
								return player.countCards('he') && event.player != player;
							},
							usable: 1,
							content() {
								'step 0';
								player.chooseToDiscard('he', '###是否发动【驱黯】？###弃置一张牌令' + get.translation(trigger.player) + '使用的' + get.translation(trigger.card) + '对你无效.').set('ai', function (card) {
									if (get.effect(_status.event.player, trigger.card, trigger.player, _status.event.player) < 0) {
										return 8 - get.value(card);
									}
									return 0;
								});
								('step 1');
								if (result.cards?.length) {
									trigger.parent.excluded.add(player);
									let fool = true;
									if (!trigger.cards.length) {
										fool = false;
									} else {
										for (const i of trigger.cards) {
											if (get.color(i) != get.color(result.cards[0])) {
												fool = false;
											}
										}
									}
									if (fool == true) {
										player.recover();
									} else {
										if (trigger.cards.length) {
											player.gain(trigger.cards, 'gain2');
										} else {
											player.draw(2);
										}
									}
								} else {
									player.getStat('triggerSkill').lqym_quan--;
								}
							},
						},
						lqym_qiangren: {
							forced: true,
							trigger: {
								global: 'gameStart',
								player: ['enterGame', 'phaseUseBegin'],
							},
							content() {
								if (event.triggername == 'phaseUseBegin') {
									player.gainMaxHp();
								} else {
									player.gainMaxHp(
										game.countPlayer(function (current) {
											return current.isEnemiesOf(player);
										}),
									);
								}
							},
						},
						lqym_wudi: {
							enable: 'phaseUse',
							position: 'h',
							filterTarget: true,
							filterCard(card, player, event) {
								return card.name == 'sha';
							},
							filterTarget: lib.filter.notMe,
							check(card) {
								return 8 - get.value(card);
							},
							usable: 1,
							filter(event, player) {
								return player.countCards('h', 'sha');
							},
							content() {
								target.damage('thunder');
							},
						},
						lqym_zuiqiang: {
							trigger: {
								source: 'damageSource',
								player: 'phaseBegin',
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'phaseBegin') {
									return game.hasPlayer(function (current) {
										return current.countMark('lqym_zuiqiang') > 0;
									});
								}
								return !event.player.countMark('lqym_zuiqiang') && event.player != player && player.maxHp > 0 && event.player.isAlive();
							},
							marktext: '裂',
							intro: {
								name: '裂风',
								content: 'mark',
							},
							content() {
								'step 0';
								if (event.triggername == 'phaseBegin') {
									game.countPlayer(function (current) {
										if (current.countMark('lqym_zuiqiang') > 0) {
											current.removeMark('lqym_zuiqiang', current.countMark('lqym_zuiqiang'));
										}
									});
									event.finish();
								} else {
									const list = [1, 2, 3, 'cancel2'],
										i = player.maxHp;
									if (i < 3) {
										list.remove(3);
									}
									if (i < 2) {
										list.remove(2);
									}
									player
										.chooseControl(list)
										.set('ai', function () {
											let num = 1,
												num1 = _status.event.player.maxHp;
											if (num1 - 3 >= 2) {
												num = 2;
											}
											return num;
										})
										.set('prompt', '###是否发动【最强】？###失去最多3点体力上限令' + get.translation(trigger.player) + '获得两倍的<裂风>标记.');
								}
								('step 1');
								if (result.control != 'cancel2') {
									game.log(player, '选择了', result.control);
									player.loseMaxHp(result.control);
									trigger.player.addMark('lqym_zuiqiang', 2 * result.control);
								}
							},
							group: 'lqym_zuiqiang_damage',
							subSkill: {
								damage: {
									trigger: {
										global: 'damageBegin',
									},
									forced: true,
									filter(event, player) {
										return event.player.countMark('lqym_zuiqiang') > 0;
									},
									content() {
										trigger.player.removeMark('lqym_zuiqiang');
										trigger.num++;
									},
								},
							},
						},
						lqym_zhanmo: {
							trigger: {
								global: ['damageEnd', 'useCardAfter', 'gainEnd'],
							},
							forced: true,
							usable: 5, //QQQ
							filter(event, player, name) {
								if (name == 'damageEnd') {
									return event.num > 0;
								}
								return true;
							},
							content() {
								'step 0';
								if (!player.storage.lqym_zhanmo1) {
									player.storage.lqym_zhanmo1 = 0;
								}
								if (!player.storage.lqym_zhanmo2) {
									player.storage.lqym_zhanmo2 = 0;
								}
								if (!player.storage.lqym_zhanmo3) {
									player.storage.lqym_zhanmo3 = 0;
								}
								((event.u = 1), (event.n = event.triggername));
								if (event.n == 'damageEnd') {
									event.u = trigger.num;
								}
								if (event.n == 'gainEnd') {
									event.u = trigger.cards.length;
								}
								('step 1');
								event.u--;
								if (event.n == 'damageEnd') {
									player.storage.lqym_zhanmo1++;
									if (player.storage.lqym_zhanmo1 % 5 == 0) {
										player
											.chooseTarget('###【斩魔】###对一个其他角色造成一点伤害.', function (card, player, target) {
												return target != player;
											})
											.set('ai', function (target) {
												return -get.attitude(_status.event.player, target);
											});
									}
								} else if (event.n == 'useCardAfter') {
									player.storage.lqym_zhanmo2++;
									if (player.storage.lqym_zhanmo2 % 7 == 0) {
										player
											.chooseTarget('###【斩魔】###弃置一名其他角色一张牌.', function (card, player, target) {
												return target != player && target.countCards('he');
											})
											.set('ai', function (target) {
												return -get.attitude(_status.event.player, target);
											});
									}
								} else {
									player.storage.lqym_zhanmo3++;
									if (player.storage.lqym_zhanmo3 % 9 == 0) {
										player.draw(2);
									}
									event.goto(3);
								}
								player.markSkill('lqym_zhanmo');
								('step 2');
								if (result.targets?.length) {
									if (event.n == 'damageEnd') {
										result.targets[0].damage();
									} else {
										player.discardPlayerCard(result.targets[0], true, 'he');
									}
								}
								('step 3');
								if (event.u > 0) {
									event.goto(1);
								}
							},
							intro: {
								mark(dialog, content, player) {
									if (player.storage.lqym_zhanmo1 > 0) {
										dialog.addText('伤害数合计值' + player.storage.lqym_zhanmo1);
									}
									if (player.storage.lqym_zhanmo2 > 0) {
										dialog.addText('使用牌合计值' + player.storage.lqym_zhanmo2);
									}
									if (player.storage.lqym_zhanmo3 > 0) {
										dialog.addText('获得牌合计值' + player.storage.lqym_zhanmo3);
									}
								},
							},
						},
						lqym_ganghua: {
							enable: 'phaseUse',
							filter(event, player) {
								return (
									game.hasPlayer(function (current) {
										return current != player && current.countCards('h') > 0;
									}) &&
									!game.hasPlayer(function (current1) {
										return current1.countCards('h', function (card) {
											return card.hasGaintag('lqym_ganghua');
										});
									})
								);
							},
							filterTarget(card, player, target) {
								return target != player && target.countCards('h') > 0;
							},
							usable: 1,
							content() {
								'step 0';
								player.chooseButton(['选择一张牌「钢化」', target.getCards('h')], true).set('ai', function (button) {
									const target = _status.event.parent.target;
									const card = button.link;
									const val = target.getUseValue(card);
									if (val > 0) {
										return val;
									}
									return get.value(card);
								});
								('step 1');
								if (result.links?.length) {
									target.showCards(result.links);
									target.storage.lqym_ganghua_remove = result.links[0];
									target.addGaintag(result.links[0], 'lqym_ganghua');
									target.addSkill('lqym_ganghua_remove');
								}
							},
							ai: {
								order: 30,
								result: {
									target: -2,
								},
							},
							group: 'lqym_ganghua_gain',
							subSkill: {
								gain: {
									trigger: {
										global: 'phaseAfter',
									},
									filter(event, player) {
										return event.player.countCards('h', function (card) {
											return card.hasGaintag('lqym_ganghua');
										});
									},
									forced: true,
									content() {
										'step 0';
										const list = [],
											list1 = trigger.player.getCards('h');
										for (const i of list1) {
											if (i.hasGaintag('lqym_ganghua')) {
												list.push(i);
											}
										}
										trigger.player.discard(list);
										('step 1');
										if (!trigger.player.countCards('he')) {
											player.draw(2);
										} else {
											player.chooseControlList(true, ['获得' + get.translation(trigger.player) + '最多两张牌', '摸两张牌'], function () {
												if (trigger.player.countCards('he') > 1 && get.attitude(_status.event.player, trigger.player) < 0) {
													return 0;
												}
												return 1;
											});
										}
										('step 2');
										if (result.index == 0) {
											player.gainPlayerCard([1, 2], true, trigger.player, 'he');
										} else {
											player.draw(2);
										}
									},
								},
								remove: {
									mark: true,
									mod: {
										cardEnabled2(card, player) {
											const list1 = player.getCards('h');
											for (const i of list1) {
												if (i.hasGaintag('lqym_ganghua') && (get.type2(i) == get.type2(card) || i.suit == card.suit || i.number == card.number) && get.position(card) == 'h') {
													return false;
												}
											}
										},
									},
									init(player, skill) {
										player.addSkillBlocker(skill);
									},
									onremove(player, skill) {
										player.removeSkillBlocker(skill);
									},
									charlotte: true,
									forced: true,
									popup: false,
									_priority: 21,
									skillBlocker(skill, player) {
										return (
											skill != 'lqym_ganghua_remove' &&
											player.countCards('h', function (card) {
												return card.hasGaintag('lqym_ganghua');
											})
										);
									},
									mark: true,
									intro: {
										mark(dialog, content, player) {
											dialog.add([player.storage.lqym_ganghua_remove]);
											const list = player.getSkills(null, false, false).filter(function (i) {
												return lib.skill.lqym_ganghua_remove.skillBlocker(i, player);
											});
											let str = '<li>你不能使用或打出与「钢化」牌相同类型/花色/点数的牌.';
											if (list.length) {
												str += '<br><li>失效技能:' + get.translation(list);
											}
											dialog.addText(str);
										},
									},
									trigger: {
										player: ['loseEnd', 'turnOverBefore', 'linkBegin'],
										global: 'dieEnd',
									},
									filter(event, player, name) {
										if (
											name == 'dieEnd' &&
											game.hasPlayer(function (current) {
												return current.hasSkill('lqym_ganghua');
											})
										) {
											return false;
										}
										if (name == 'loseEnd') {
											return !player.countCards('h', function (card) {
												return card.hasGaintag('lqym_ganghua');
											});
										}
										return true;
									},
									content() {
										const n = event.triggername;
										if (n == 'loseEnd' || n == 'dieEnd') {
											delete player.storage.lqym_ganghua_remove;
											player.removeGaintag('lqym_ganghua');
											player.removeSkill('lqym_ganghua_remove');
										} else {
											trigger.cancel();
										}
									},
								},
							},
						},
						lqym_jingti: {
							init(player) {
								if (!player.storage.lqym_jingti1) {
									player.storage.lqym_jingti1 = [];
								}
							},
							intro: {
								mark(dialog, content, player) {
									if (player.countMark('lqym_jingti') > 0) {
										dialog.addText('当前标记数:' + player.storage.lqym_jingti);
									}
									const list = [];
									for (const i of ['类型', '花色', '点数']) {
										if (player.storage.lqym_jingti1.includes(i)) {
											list.push(i);
										}
									}
									if (list.length) {
										dialog.addText('你已因' + list + '而获得过<晶体>标记');
									}
								},
							},
							trigger: {
								global: 'useCardToPlayered',
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'phaseZhunbeiBegin') {
									return player.countMark('lqym_jingti') > 0;
								}
								const g = player.storage.lqym_jingti1;
								for (const i of event.target.getCards('h')) {
									if (i.hasGaintag('lqym_ganghua')) {
										if (get.type2(i) == get.type2(event.card) && !g.includes('类型')) {
											return true;
										}
										if (i.suit == event.card.suit && !g.includes('花色')) {
											return true;
										}
										if (i.number == event.card.number && !g.includes('点数')) {
											return true;
										}
									}
								}
								return false;
							},
							content() {
								if (event.triggername == 'phaseZhunbeiBegin') {
									const num = player.countMark('lqym_jingti');
									if (num > 0) {
										player.draw();
									}
									if (num > 1) {
										player.skip('phaseJudge');
									}
									if (num > 2) {
										player.gainMaxHp();
										player.recover();
										player.changeHujia();
										player.removeMark('lqym_jingti', num);
										player.storage.lqym_jingti1 = [];
									}
								} else {
									const g = player.storage.lqym_jingti1,
										t = get.type2(trigger.card),
										s = trigger.card.suit,
										n = trigger.card.number,
										list = trigger.target.getCards('h');
									for (const i of list) {
										if (i.hasGaintag('lqym_ganghua')) {
											if (get.type2(i) == t && !g.includes('类型')) {
												player.addMark('lqym_jingti');
												g.push('类型');
											}
											if (i.suit == s && !g.includes('花色')) {
												player.addMark('lqym_jingti');
												g.push('花色');
											}
											if (i.number == n && !g.includes('点数')) {
												player.addMark('lqym_jingti');
												g.push('点数');
											}
										}
									}
								}
							},
						},
						lqym_cundang: {
							dangka(name, skill) {
								const name0 = 'lqym_' + skill + 'card';
								lib.translate[name0] = lib.translate[skill];
								lib.translate[name0 + '_info'] = lib.translate[skill + '_info'];
								lib.card[name0] = lib.card[name0] || {
									image: 'character:' + name,
									enable: true,
									type: 'trick',
									lqym: true,
									fullimage: true,
									selectTarget: -1,
									filterTarget(card, player, target) {
										return target == player;
									},
									derivation: 'lqym_No.78',
								};
								if (lib.skill[skill].selectTarget || lib.skill[skill].filterTarget) {
									lib.card[name0].selectTarget = lib.skill[skill].selectTarget;
									lib.card[name0].filterTarget = lib.skill[skill].filterTarget;
								}
								lib.card[name0].content = lib.skill[skill].content;
								if (lib.skill[skill].ai) {
									lib.card[name0].ai = lib.skill[skill].ai;
								}
							},
							trigger: {
								global: ['phaseBefore', 'useCardEnd'],
								player: 'enterGame',
							},
							forced: true,
							filter(event, player) {
								if (event.name == 'useCard') {
									if (event.cards && event.cards.length) {
										for (const i of event.cards) {
											if (i.storage.dangka && get.position(i) == 'd' && event.player != player) {
												return true;
											}
										}
									}
								} else {
									return event.name != 'phase' || game.phaseNumber == 0;
								}
							},
							content() {
								'step 0';
								if (trigger.name == 'useCard') {
									const g = [];
									for (const i of trigger.cards) {
										if (i.storage.dangka && get.position(i) == 'd') {
											g.push(i);
										}
									}
									player.gain(g, 'gain2');
									event.finish();
								} else {
									const a = [];
									((event.b = []), (event.d = []), (event.e = Math.max(9, Math.ceil(ui.cardPile.childElementCount / 20))));
									for (const i in lib.character) {
										const d = lib.character[i][4];
										if (d.includes('boss') || d.includes('hiddenboss') || d.includes('bossallowed') || d.includes('minskin')) {
											continue;
										}
										if ((game.me == player && lib.config.extension_浪琴天阙_lqtq_shaiji_kuochong) || !lib.config.forbidai.includes(i)) {
											a.push(i);
										}
									}
									for (const i of a) {
										if (!lib.character[i]) {
											continue;
										}
										for (var j of lib.character[i][3].slice(0).slice(0)) {
											if (j.includes('rewrite') || event.b.includes(j) || !lib.translate[j] || !lib.translate[j + '_info']) {
												continue;
											}
											if (lib.skill[j].trigger || lib.skill[j].filter || !lib.skill[j].content || lib.skill[j].filterCard || lib.skill[j].selectCard || (lib.skill[j].selectTarget && lib.skill[j].selectTarget != 1)) {
												continue;
											}
											event.b.add(i);
										}
									}
								}
								('step 1');
								event.e--;
								const name = event.b.randomGet();
								const c = [],
									f = [];
								for (const i of event.b) {
									if (i != name) {
										f.add(i);
									}
								}
								event.b = f;
								for (var j of lib.character[name][3].slice(0).slice(0)) {
									if (j.includes('rewrite') || event.b.includes(j) || !lib.translate[j] || !lib.translate[j + '_info']) {
										continue;
									}
									if (lib.skill[j].trigger || lib.skill[j].filter || !lib.skill[j].content || lib.skill[j].filterCard || lib.skill[j].selectCard || (lib.skill[j].selectTarget && lib.skill[j].selectTarget != 1)) {
										continue;
									}
									c.add(j);
								}
								const skill = c.randomGet();
								lib.skill.lqym_cundang.dangka(name, skill);
								const name1 = 'lqym_' + skill + 'card';
								lib.inpile.add(name1); //QQQ
								const card = game.createCard(name1);
								card.storage.dangka = true;
								ui.cardPile.insertBefore(card, ui.cardPile.childNodes[get.rand(0, ui.cardPile.childNodes.length)]);
								game.updateRoundNumber();
								('step 2');
								if (event.e > 0) {
									event.goto(1);
								}
							},
						},
						lqym_bianma: {
							enable: 'phaseUse',
							usable: 1,
							content() {
								const a = [],
									b = [],
									c = [];
								for (const i in lib.character) {
									const d = lib.character[i][4];
									if (d.includes('boss') || d.includes('hiddenboss') || d.includes('bossallowed') || d.includes('minskin')) {
										continue;
									}
									if ((game.me == player && lib.config.extension_浪琴天阙_lqtq_shaiji_kuochong) || !lib.config.forbidai.includes(i)) {
										a.push(i);
									}
								}
								for (const i of a) {
									if (!lib.character[i]) {
										continue;
									}
									for (var j of lib.character[i][3].slice(0).slice(0)) {
										if (j.includes('rewrite') || b.includes(j) || !lib.translate[j] || !lib.translate[j + '_info']) {
											continue;
										}
										if (lib.skill[j].trigger || lib.skill[j].filter || !lib.skill[j].content || lib.skill[j].filterCard || lib.skill[j].selectCard || (lib.skill[j].selectTarget && lib.skill[j].selectTarget != 1)) {
											continue;
										}
										b.add(i);
									}
								}
								const name = b.randomGet();
								for (var j of lib.character[name][3].slice(0).slice(0)) {
									if (j.includes('rewrite') || b.includes(j) || !lib.translate[j] || !lib.translate[j + '_info']) {
										continue;
									}
									if (lib.skill[j].trigger || lib.skill[j].filter || !lib.skill[j].content || lib.skill[j].filterCard || lib.skill[j].selectCard || (lib.skill[j].selectTarget && lib.skill[j].selectTarget != 1)) {
										continue;
									}
									c.add(j);
								}
								const skill = c.randomGet();
								lib.skill.lqym_cundang.dangka(name, skill);
								const card = game.createCard('lqym_' + skill + 'card');
								card.storage.dangka = true;
								player.gain(card, 'gain');
								lib.inpile.add(card.name); //QQQ
							},
						},
						lqym_bajian: {
							mod: {
								aiOrder(player, card, num) {
									if (get.itemtype(card) == 'card' && card.hasGaintag('lqym_bajian')) {
										return num + 0.5;
									}
								},
								aiValue(player, card, num) {
									if (!player.hasUseTarget(card) && card.hasGaintag('lqym_bajian')) {
										return 0;
									}
								},
							},
							trigger: {
								global: ['phaseBefore', 'equipAfter', 'addJudgeAfter', 'gainAfter', 'loseAsyncAfter', 'addToExpansionAfter'],
								player: ['enterGame', 'loseAfter'],
							},
							forced: true,
							filter(event, player, name) {
								if (
									player.countCards('h', function (card) {
										return card.hasGaintag('lqym_bajian');
									})
								) {
									return false;
								}
								if (name == 'enterGame') {
									return true;
								}
								if (name == 'phaseBefore') {
									return game.phaseNumber == 0;
								}
								const evt = event.getl(player);
								return evt && evt.player == player && evt.hs && evt.hs.length;
							},
							content() {
								player.draw().gaintag = ['lqym_bajian'];
							},
						},
						lqym_tuijia: {
							audio: 2,
							enable: 'phaseUse',
							usable: 1,
							filter(event, player) {
								return player.countCards('he') > 1;
							},
							filterCard: true,
							selectCard: [1, 2],
							check(card) {
								if (card.hasGaintag('lqym_bajian')) {
									return 10;
								}
								return 7 - get.value(card);
							},
							filterTarget: true,
							content() {
								'step 0';
								if (cards.length > 1 && !player.isDamaged()) {
									player.draw(cards.length);
									event.finish();
								} else {
									target
										.chooseControl()
										.set('choiceList', ['摸' + cards.length + '张牌', cards.length > 1 ? '回复1点体力' : '获得一点护甲'])
										.set('ai', function () {
											if (_status.event.player.isHealthy()) {
												return 0;
											}
											return 1;
										});
								}
								('step 1');
								if (result.index == 0) {
									target.draw(cards.length);
								} else {
									cards.length > 1 ? target.recover() : target.changeHujia();
								}
							},
							ai: {
								order: 1,
								result: {
									target(player, target) {
										if (target.isDamaged()) {
											return 1.5;
										}
										return 1;
									},
								},
							},
						},
						lqym_xieyin: {
							trigger: {
								global: ['roundStart', 'useCardToPlayer'],
								player: 'damageEnd',
							},
							intro: {
								content: '已记录势力:$',
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'useCardToPlayer') {
									return get.type(event.card) != 'basic' && (event.target == player || event.target == event.player) && player.getStorage('lqym_xieyin').includes(event.player.group);
								}
								const a = lib.group.filter(function (group) {
									return (
										game.hasPlayer(function (current) {
											return current.group == group;
										}) && !player.getStorage('lqym_xieyin').includes(group)
									);
								});
								return a.length;
							},
							count(group) {
								const player = _status.event.player;
								return game.countPlayer(function (current) {
									return current != player && current.group == group && get.attitude(current, player) < 0;
								});
							},
							content() {
								'step 0';
								if (trigger.name == 'useCardToPlayer') {
									player.unmarkAuto('lqym_xieyin', [trigger.player.group]);
									player.chooseControlList(true, ['摸两张牌', '对' + get.translation(trigger.player) + '造成一点伤害.'], function () {
										if (get.attitude(_status.event.player, trigger.player) > 0) {
											return 0;
										}
										return 1;
									});
								} else {
									event.goto(2);
								}
								('step 1');
								if (result.index == 0) {
									player.draw(2);
								} else {
									trigger.player.damage();
								}
								event.finish();
								('step 2');
								const list = lib.group.filter(function (group) {
									return (
										game.hasPlayer(function (current) {
											return current.group == group;
										}) && !player.getStorage('lqym_xieyin').includes(group)
									);
								});
								const list2 = list.slice(0);
								list2.sort(function (a, b) {
									return lib.skill.lqym_xieyin.count(b) - lib.skill.lqym_xieyin.count(a);
								});
								player
									.chooseControl(list)
									.set('prompt', '记录一个势力')
									.set('ai', function () {
										return _status.event.group;
									})
									.set('group', list2[0]);
								('step 3');
								player.markAuto('lqym_xieyin', [result.control]);
								player.popup(result.control, get.groupnature(result.control));
								game.log(player, '选择了', '#g' + get.translation(result.control));
							},
						},
						lqym_weishi: {
							trigger: {
								global: 'phaseUseBegin',
							},
							forced: true,
							filter(event, player) {
								return _status.currentPhase != player;
							},
							content() {
								'step 0';
								player.viewHandcards(trigger.player);
								game.log(trigger.player, '的手牌(', trigger.player.getCards('h'), ')');
								('step 1');
								('step 2');
								event.num = 0;
								if (event.isMine()) {
									var a = [0, 999],
										c = ui.create.dialog('未视:记录一个数字'),
										d = ui.create.control('-', event.num, '+'),
										e = d.cloneNode(true),
										f = e.childNodes;
									f[0].style.width = '20px';
									f[0].onclick = function () {
										event.num--;
										if (event.num < a[0]) {
											event.num = a[1];
										}
										f[1].innerHTML = event.num;
									};
									f[1].style.width = '60px';
									f[1].onclick = function () {
										e.parentNode.removeChild(e);
										c.parentNode.removeChild(c);
										game.resume();
									};
									f[2].style.width = '20px';
									f[2].onclick = function () {
										event.num++;
										if (event.num > a[1]) {
											event.num = a[0];
										}
										f[1].innerHTML = event.num;
									};
									d.parentNode.replaceChild(e, d);
									ui.updatec();
									game.pause();
								} else {
									if (trigger.player.countCards('h') > 0) {
										for (const i of trigger.player.getCards('h')) {
											if (!player.hasUseTarget(i)) {
												event.num++;
											}
										}
										if (!trigger.player.countCards('he', 'zhuge') && trigger.player.countCards('h', 'sha') > 1) {
											event.num += trigger.player.countCards('h', 'sha') - 1;
										}
									}
								}
								('step 3');
								player.storage.lqym_weishi0 = event.num;
								if (!player.storage.lqym_weishi5) {
									player.storage.lqym_weishi5 = [];
								}
								event.f = 1;
								('step 4');
								var a = '受到伤害';
								if (event.f == 2) {
									a = '回复体力';
								}
								if (event.f == 3) {
									a = '获得牌';
								}
								if (event.f == 4) {
									a = '弃置牌';
								}
								player.chooseTarget(true, '未视:猜测一名可能会' + a + '的角色.', (card, player, target) => {
									return target != trigger.player;
								}).ai = function (target) {
									const a = get.attitude(trigger.player, target);
									if (a < 0 && event.f == 1 && trigger.player.inRange(target)) {
										return 2;
									}
									if (a < 0 && event.f == 4 && (target.getEquip(3) || target.getEquip(2))) {
										return 2;
									}
									if (a < 0 && (event.f == 1 || event.f == 4)) {
										return 1;
									}
									if (a > 0 && event.f == 2 && target.isDamaged()) {
										return 2;
									}
									if (a > 0 && (event.f == 2 || event.f == 3)) {
										return 1;
									}
									return 0.5;
								};
								('step 5');
								if (result.targets?.length) {
									player.storage[event.name + event.f] = result.targets[0];
								}
								event.f++;
								if (event.f < 5) {
									event.goto(4);
								} else {
									player.addTempSkill('lqym_weishi_mark');
								}
							},
							subSkill: {
								mark: {
									mark: true,
									intro: {
										name: '未视',
										mark(dialog, content, player) {
											if (player == game.me || player.isUnderControl()) {
												dialog.addText('记录的数字:' + player.storage.lqym_weishi0);
												if (player.storage.lqym_weishi5.includes('damage')) {
													dialog.addText('角色已受到过伤害');
												} else {
													dialog.addText('角色可能会受到伤害');
												}
												dialog.add([player.storage.lqym_weishi1]);
												if (player.storage.lqym_weishi5.includes('recover')) {
													dialog.addText('角色已回复过体力');
												} else {
													dialog.addText('角色可能会回复体力');
												}
												dialog.add([player.storage.lqym_weishi2]);
												if (player.storage.lqym_weishi5.includes('gain')) {
													dialog.addText('角色已获得过牌');
												} else {
													dialog.addText('角色可能会获得牌');
												}
												dialog.add([player.storage.lqym_weishi3]);
												if (player.storage.lqym_weishi5.includes('discard')) {
													dialog.addText('角色已弃置过牌');
												} else {
													dialog.addText('角色可能会弃置牌');
												}
												dialog.add([player.storage.lqym_weishi4]);
											} else {
												dialog.addText('就不给你看,寄蓝铜');
											}
										},
									},
									onremove(player) {
										for (let i = 0; i <= 5; i++) {
											delete player.storage['lqym_weishi' + i];
										}
									},
									group: 'lqym_weishi_count',
									trigger: {
										global: 'phaseUseEnd',
									},
									forced: true,
									charlotte: true,
									filter(event, player) {
										return event.player.countCards('h') == player.storage.lqym_weishi0 || player.storage.lqym_weishi5.length;
									},
									content() {
										'step 0';
										let a = 0,
											b = player.storage.lqym_weishi5;
										if (trigger.player.countCards('h') == player.storage.lqym_weishi0) {
											a++;
										}
										if (b.includes('damage')) {
											a++;
										}
										if (b.includes('recover')) {
											a++;
										}
										if (b.includes('gain')) {
											a++;
										}
										if (b.includes('discard')) {
											a++;
										}
										player.draw(a);
										if (_status.connectMode) {
											game.broadcastAll(function () {
												_status.noclearcountdown = true;
											});
										}
										event.given_map = {};
										event.num = 3;
										('step 1');
										player.chooseCardTarget({
											filterCard(card, player) {
												return get.itemtype(card) == 'card' && !card.hasGaintag('reyiji_tag');
											},
											filterTarget: lib.filter.notMe,
											selectCard: [1, event.num],
											prompt: '请选择要分配的卡牌和目标',
											ai1(card) {
												if (!ui.selected.cards.length) {
													return 1;
												}
												return 0;
											},
											position: 'he',
											ai2(target) {
												const player = _status.event.player,
													card = ui.selected.cards[0];
												const val = target.getUseValue(card);
												if (val > 0) {
													return val * get.attitude(player, target) * 2;
												}
												return get.value(card, target) * get.attitude(player, target);
											},
										});
										('step 2');
										if (result.cards?.length) {
											const res = result.cards,
												target = result.targets[0].playerid;
											player.addGaintag(res, 'reyiji_tag');
											event.num -= res.length;
											if (!event.given_map[target]) {
												event.given_map[target] = [];
											}
											event.given_map[target].addArray(res);
											if (event.num > 0) {
												event.goto(1);
											}
										} else if (event.num == 3) {
											if (_status.connectMode) {
												game.broadcastAll(function () {
													delete _status.noclearcountdown;
													game.stopCountChoose();
												});
											}
											event.goto(4);
										}
										('step 3');
										if (_status.connectMode) {
											game.broadcastAll(function () {
												delete _status.noclearcountdown;
												game.stopCountChoose();
											});
										}
										const map = [],
											cards = [];
										for (const i in event.given_map) {
											const source = (_status.connectMode ? lib.playerOL : game.playerMap)[i];
											player.line(source, 'green');
											map.push([source, event.given_map[i]]);
											cards.addArray(event.given_map[i]);
										}
										game.loseAsync({
											gain_list: map,
											player: player,
											cards: cards,
											giver: player,
											animate: 'giveAuto',
										}).setContent('gaincardMultiple');
										('srep 4');
										player.removeSkill('lqym_weishi_mark');
									},
								},
								count: {
									trigger: {
										global: ['damage', 'recoverBegin', 'discardBegin', 'gainBegin'],
									},
									forced: true,
									popup: false,
									filter(event, player) {
										let a = event.name;
										if ((a = 'damage')) {
											return event.player == player.storage.lqym_weishi1;
										}
										if ((a = 'recover')) {
											return event.player == player.storage.lqym_weishi2;
										}
										if ((a = 'gain')) {
											return event.player == player.storage.lqym_weishi3;
										}
										return event.player == player.storage.lqym_weishi4;
									},
									content() {
										player.storage.lqym_weishi5.push(trigger.name);
									},
								},
							},
						},
						lqym_jiaozhu: {
							trigger: {
								global: 'phaseZhunbeiBegin',
							},
							round: 3,
							logTarget(event, player) {
								return event.player;
							},
							prompt2(event) {
								return '令' + get.translation(event.player) + '本回合使用的牌全部无效.';
							},
							check(event, player) {
								return get.attitude(player, event.player) < 0;
							},
							filter(event, player) {
								return _status.currentPhase != player;
							},
							content() {
								trigger.player.addTempSkill('lqym_jiaozhu_add');
							},
							subSkill: {
								add: {
									trigger: {
										player: 'useCardToPlayered',
									},
									mark: true,
									intro: {
										content: '你使用的牌全部无效.',
									},
									forced: true,
									charlotte: true,
									content() {
										trigger.cancel();
										trigger.targets.length = 0;
										trigger.parent.triggeredTargets1.length = 0;
										game.log(player, '使用的', trigger.card, '无效.');
									},
								},
							},
						},
						lqym_shuanghuang: {
							trigger: {
								global: 'useCard',
								player: 'useCardToPlayered',
							},
							mark: true,
							zhuanhuanji: true,
							marktext: '☯',
							intro: {
								content(storage, player, skill) {
									if (player.storage.lqym_shuanghuang != true) {
										return '其他角色使用普通锦囊牌时,你可以取消那张牌的所有目标,若那张牌的点数不为数字,你获得那张牌.';
									}
									return '你使用【杀】指定目标时,你可以获得目标装备区一张牌并使用之.';
								},
							},
							filter(event, player, name) {
								if (name != 'useCardToPlayered') {
									return (
										player.storage.lqym_shuanghuang != true &&
										event.player != player &&
										get.type(event.card) == 'trick' &&
										event.player.getHistory('lose', function (evt) {
											return evt.parent == event && evt.hs && evt.hs.length == event.cards.length;
										}).length
									);
								}
								return event.target.countCards('e') > 0 && event.card.name == 'sha' && player.storage.lqym_shuanghuang == true;
							},
							forced: true,
							content() {
								'step 0';
								if (player.storage.lqym_shuanghuang == true) {
									player.gainPlayerCard('双皇:是否获得' + get.translation(trigger.target) + '一张装备牌？', trigger.target, 'e').set('ai', function (button) {
										return get.value(button.link);
									});
								} else {
									let effect = 0;
									if (trigger.card.name == 'wuxie' || trigger.card.name == 'shan') {
										if (get.attitude(player, trigger.player) < -1) {
											effect = -1;
										}
									} else if (trigger.targets && trigger.targets.length) {
										for (const i of trigger.targets) {
											effect += get.effect(i, trigger.card, trigger.player, player);
										}
									}
									let str = '双皇:是否令' + get.translation(trigger.player);
									if (trigger.targets && trigger.targets.length) {
										str += '对' + get.translation(trigger.targets);
									}
									str += '使用的' + get.translation(trigger.card) + '失效？';
									const next = player.chooseBool(str, function () {
										const player = _status.event.player;
										const trigger = _status.event.getTrigger();
										if (_status.event.effect < 0) {
											return true;
										}
										return false;
									});
									next.set('effect', effect);
								}
								('step 1');
								if (result.bool) {
									player.changeZhuanhuanji('lqym_shuanghuang');
									if (player.storage.lqym_shuanghuang != true) {
										const card = result.cards[0];
										if (player.getCards('h').includes(card) && get.type(card) == 'equip') {
											player.chooseUseTarget(card, true).nopopup = true;
										}
									} else {
										trigger.targets.length = 0;
										trigger.all_excluded = true;
										if ([1, 11, 12, 13].includes(trigger.card.number)) {
											player.gain(trigger.cards, 'gain2');
										}
									}
								}
							},
							ai: {
								threaten: 1.8,
								expose: 0.3,
							},
						},
						lqym_ganranx: {
							trigger: {
								global: 'roundStart',
								player: ['phaseDiscardEnd', 'gainEnd'],
							},
							intro: { content: '已记录牌名:$' },
							forced: true,
							filter(event, player) {
								if (event.name == 'gain') {
									return event.parent.name == 'lqym_ganranx' || event.getParent(2).name == 'lqym_ganranx_gain';
								}
								if (event.name == 'phaseDiscard') {
									return event.cards && event.cards.length && ui.discardPile.childNodes.length;
								}
								return true;
							},
							prompt: '是否发动【感染】？',
							prompt2(event, player) {
								if (event.name == 'gain') {
									if (player.getStorage('lqym_ganranx').includes(event.cards[0].name)) {
										return '可以移去你记录的' + get.translation(event.cards[0].name) + ',摸一张牌.';
									} else {
										return '你记录' + get.translation(event.cards[0]) + '的牌名.';
									}
								}
								if (event.name == 'phaseDiscard') {
									return '可以从弃牌堆获得一张最早进入弃牌堆的牌.';
								}
								return '可以观看牌堆底两张牌,获得其中一张牌.';
							},
							content() {
								'step 0';
								if (trigger.name == 'gain') {
									var a = player.getStorage('lqym_ganranx'),
										b = trigger.cards[0].name;
									if (!a.includes(b)) {
										player.markAuto('lqym_ganranx', [b]);
										game.log(player, '从', '#g【感染】', '记录中记录了', '#y' + get.translation(b));
									} else {
										player.draw();
										player.unmarkAuto('lqym_ganranx', [b]);
										game.log(player, '从', '#g【感染】', '记录中移除了', '#y' + get.translation(b));
									}
								} else if (trigger.name == 'phaseDiscard') {
									var a = get.discardPile(function (card) {
										return true;
									});
									if (a) {
										player.gain(a, 'gain2');
									}
								} else {
									event.card = get.bottomCards(2);
									player.chooseCardButton('感染:获得一张牌', true, event.card).ai = function (button) {
										return player.getUseValue(button.link);
									};
								}
								('step 1');
								if (result.links?.length) {
									player.gain(result.links, 'gain2');
									event.card.remove(result.links[0]);
									game.cardsGotoOrdering(event.card);
								}
							},
							group: 'lqym_ganranx_gain',
							subSkill: {
								gain: {
									enable: 'phaseUse',
									usable: 1,
									filter(event, player) {
										return (
											player.countCards('h') > 0 &&
											game.hasPlayer(function (current) {
												return current.countGainableCards(player, 'h') > 0 && current != player;
											})
										);
									},
									prompt: '可以弃置一张手牌,获得一名其他角色一张手牌.',
									filterCard: true,
									check(card) {
										return 7 - get.value(card);
									},
									filterTarget(card, player, target) {
										return target != player && target.countGainableCards(player, 'h') > 0;
									},
									content() {
										player.gainPlayerCard('h', target, true);
									},
								},
							},
						},
						lqym_huanjue: {
							trigger: {
								target: 'useCardToTargeted',
								global: 'phaseUseBegin',
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'useCardToTargeted') {
									return event.card && event.card.name == 'sha';
								}
								return event.player.hasSkill('lqym_huanjue_remove');
							},
							logTarget: 'source',
							content() {
								if (event.triggername == 'useCardToTargeted') {
									if (!trigger.player.hasMark('lqym_huanjue_remove')) {
										trigger.player.addSkill('lqym_huanjue_remove');
										const a = trigger.player.getSkills(null, false);
										for (let i = 0; i < a.length; i++) {
											if (get.info(a[i]).charlotte) {
												a.splice(i--, 1);
											}
										}
										if (a.length) {
											trigger.player.storage.lqym_huanjue.push(a.randomGet());
											trigger.player.disableSkill('lqym_huanjue_remove', trigger.player.storage.lqym_huanjue);
										}
									}
									trigger.player.addMark('lqym_huanjue_remove');
								} else {
									if (trigger.player.countMark('lqym_huanjue_remove') > 1) {
										trigger.player.loseHp();
									}
									trigger.player.removeSkill('lqym_huanjue_remove');
								}
							},
							global: 'lqym_huanjue_mod',
							group: ['lqym_huanjue_damage', 'lqym_huanjue_count'],
							subSkill: {
								mod: {
									mod: {
										targetEnabled(card, player, target) {
											const a = game.findPlayer(function (current) {
												return current.hasSkill('lqym_huanjue');
											});
											if (a && player.inRange(a) && !target.hasSkill('lqym_huanjue') && card.name == 'sha') {
												return false;
											}
										},
									},
								},
								remove: {
									init(player, skill) {
										if (!player.storage.lqym_huanjue) {
											player.storage.lqym_huanjue = [];
										}
									},
									onremove(player, skill) {
										player.enableSkill(skill);
										player.removeMark('lqym_huanjue_remove', player.countMark('lqym_huanjue_remove'));
										player.unmarkSkill('lqym_huanjue_remove');
										delete player.storage.lqym_huanjue;
									},
									charlotte: true,
									mark: true,
									intro: {
										content(storage, player, skill) {
											let str = '';
											if (player.hasMark('lqym_huanjue_remove')) {
												str += '当前标记数:' + player.countMark('lqym_huanjue_remove');
											}
											const list = Object.keys(player.disabledSkills);
											if (list.length) {
												str += '<br><br>失效技能:';

												for (const i of list) {
													if (lib.translate[i + '_info']) {
														str += get.translation(i) + '、';
													}
												}

												return str.slice(0, str.length - 1);
											}
										},
									},
								},
								damage: {
									trigger: {
										player: 'damageBegin',
									},
									forced: true,
									usable: 1,
									filter(event, player) {
										return event.card && event.card.name == 'sha';
									},
									content() {
										trigger.num--;
										game.log(trigger.card, '伤害减一');
									},
								},
								count: {
									trigger: {
										player: 'die',
									},
									forced: true,
									forceDie: true,
									popup: true,
									filter(event, player) {
										return game.hasPlayer(function (current) {
											return current.hasSkill('lqym_huanjue_remove');
										});
									},
									content() {
										game.countPlayer(function (current) {
											if (current.hasSkill('lqym_huanjue_remove')) {
												current.removeSkill('lqym_huanjue_remove');
											}
										});
									},
								},
							},
						},
						lqym_xunxin: {
							enable: 'phaseUse',
							usable: 1,
							filterTarget(card, player, target) {
								return target != player && (target.canUse('sha', player, false, false) || target.countGainableCards(player, 'he'));
							},
							content() {
								'step 0';
								event.a = target.hasMark('lqym_huanjue_remove') ? player : target;
								let b = [],
									c = ['视为对' + get.translation(player) + '使用一张【杀】.', '将一张牌交给' + get.translation(player) + '.', '令' + get.translation(player) + '获得你一张手牌.'];
								if (event.a == player) {
									c = ['令' + get.translation(target) + '视为对你使用一张【杀】.', '令' + get.translation(target) + '将一张牌交给你.', '获得' + get.translation(target) + '一张手牌.'];
								}
								if (target.canUse('sha', player, false, false)) {
									b.push('选项一');
								}
								if (target.countGainableCards(player, 'he')) {
									b.push('选项二');
								}
								if (target.countGainableCards(player, 'h')) {
									b.push('选项三');
								}
								event.a.chooseControl(b).set('choiceList', c).ai = function () {
									if (event.a == player) {
										if (target.countGainableCards(player, 'h')) {
											return '选项三';
										}
										if (target.countGainableCards(player, 'he')) {
											return '选项二';
										}
										return '选项一';
									} else {
										if (event.a.canUse({ name: 'sha' }, player, false, false)) {
											return '选项一';
										}
										return '选项二';
									}
								};
								('step 1');
								if (result.control == '选项三') {
									player.gainPlayerCard('h', target, true);
									event.finish();
								}
								if (result.control == '选项一') {
									target.addTempSkill('lqym_xunxin_range', { player: 'phaseAfter' });
									target.storage.lqym_xunxin_range = player;
									target.useCard({ name: 'sha' }, player);
									event.finish();
								}
								if (result.control == '选项二') {
									target.chooseCard('he', true, '寻衅:将一张牌交给' + get.translation(player));
								}
								('step 2');
								if (result.cards?.length) {
									target.give(result.cards, player, true);
								}
							},
							mod: {
								inRangeOf(from, to) {
									if (from.hasSkill('lqym_xunxin_range')) {
										return true;
									}
								},
								inRange(from, to) {
									if (to.hasSkill('lqym_xunxin_range')) {
										return true;
									}
								},
							},
							ai: {
								order: 20,
								result: {
									target: -1,
								},
							},
							subSkill: {
								range: {
									onremove(player) {
										delete player.storage.lqym_xunxin_range;
									},
									mark: true,
									charlotte: true,
									forced: true,
									intro: {
										content: '你与$视为在彼此的攻击范围内.',
									},
								},
							},
						},
						lqym_gongcan: {
							trigger: {
								global: ['turnOverEnd', 'linkEnd', 'phaseUseCancelled', 'phaseUseSkipped', 'phaseDrawCancelled', 'phaseDrawSkipped'],
							},
							forced: true,
							content() {
								'step 0';
								trigger.player.chooseBool('共蝉:是否令' + get.translation(player) + '获得一张手牌中较少的类型牌?').ai = function () {
									if (get.attitude(player, trigger.player) < 0) {
										return false;
									}
									return true;
								};
								('step 1');
								if (result.bool) {
									const b = [];
									for (const i of player.getCards('h')) {
										b.add(get.type2(i));
									}
									let a = get.cardPile(function (card) {
										return !b.includes(get.type2(card));
									});
									if (!a) {
										a = get.cardPile(function (card) {
											const c = [],
												d = [];
											for (const i of b) {
												const j = player.countCards('h', function (car) {
													return get.type2(car) == i;
												});
												c.push([i, j]); //type//num
											}
											for (let i = 1; i <= player.countCards('h'); i++) {
												for (const j of c) {
													if (j[1] == i) {
														return get.type2(card) == j[0];
														break;
													}
												}
											}
										});
									}
									if (a) {
										player.gain(a, 'gain2');
									}
								}
							},
						},
						lqym_chantui: {
							mod: {
								aiValue(player, card, num) {
									const cards = player.getCards('hs');
									cards.sort(function (a, b) {
										return (b.name == 'wuxie' ? 1 : 2) - (a.name == 'wuxie' ? 1 : 2);
									});
									const geti = function () {
										if (cards.includes(card)) {
											return cards.indexOf(card);
										}
										return cards.length;
									};
									if (card.name == 'wuxie') {
										return Math.min(num, [6, 4, 3][Math.min(geti(), 2)]) * 0.6;
									}
									return Math.max(num, [6, 4, 3][Math.min(geti(), 2)]);
								},
								aiUseful() {
									return lib.skill.lqym_chantui.mod.aiValue.apply(this, arguments);
								},
							},
							position: 'hs',
							enable: 'chooseToUse',
							filterCard: true,
							viewAsFilter(player) {
								return player.countCards('hs') > 0 && !player.hasSkill('lqym_chantui_link');
							},
							viewAs: {
								name: 'wuxie',
							},
							precontent() {
								player.addTempSkill('lqym_chantui_link');
							},
							prompt: '将一张手牌当无懈可击使用',
							check(card) {
								return 8 - get.value(card);
							},
							subSkill: {
								link: {
									trigger: {
										player: 'useCardAfter',
									},
									popup: false,
									forced: true,
									filter(event, player) {
										return event.skill == 'lqym_chantui';
									},
									content() {
										'step 0';
										player.chooseTarget('蝉蜕:是否横置一名角色？').ai = function (target) {
											if (target.isLinked()) {
												return get.attitude(player, target) > 0;
											}
											return target == player;
										};
										('step 1');
										if (result.targets?.length) {
											result.targets[0].link();
										}
									},
								},
							},
						},
						lqym_ganran: {
							trigger: {
								global: 'phaseBefore',
								player: ['enterGame', 'phaseBegin'],
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'phaseBefore') {
									return game.phaseNumber == 0;
								}
								return true;
							},
							content() {
								'step 0';
								if (event.triggername != 'phaseBegin') {
									player
										.chooseControl('wei', 'shu', 'wu', 'qun', 'jin')
										.set('prompt', '###【感染】###变换场上所有角色的势力')
										.set('ai', function () {
											return 'wei';
										});
								} else {
									player.chooseTarget(get.prompt(event.name), '选择一名角色.', function (card, player, target) {
										if (target.group == 'qun') {
											return target != player && target.countGainableCards(player, 'hej');
										}
										return true;
									}).ai = function (target) {
										const a = get.attitude(player, target),
											b = player.group;
										if (!['wei', 'shu', 'wu', 'qun', 'jin'].includes(target.group)) {
											if (['wei', 'wu', 'jin'].includes(player.group)) {
												return a;
											}
											if (player.group == 'qun' || player.group == 'shu') {
												return -a;
											}
										}
										if (target.group == 'shu') {
											return -a;
										}
										if (['wei', 'wu', 'jin'].includes(target.group)) {
											return a;
										}
										if (target.group == 'qun') {
											if (a > 0 && target.countGainableCards(player, 'j')) {
												return 3;
											}
											if (a < 0 && target.countGainableCards(player, 'e')) {
												return 2;
											}
											if (a < 0) {
												return 1;
											}
											return 0;
										}
									};
								}
								('step 1');
								if (event.triggername != 'phaseBegin') {
									var a = game.filterPlayer(function (current) {
										return current.group != result.control;
									});
									a.sort(lib.sort.seat);
									player.line(a, 'green');
									game.countPlayer(function (current) {
										const a = result.control;
										if (current.group != a) {
											current.changeGroup(a);
										}
									});
								} else {
									if (result.targets?.length) {
										var a = result.targets[0];
										if (!['shu', 'wei', 'qun', 'wu', 'jin'].includes(a.group)) {
											a.changeGroup(player.group);
										}
										const b = result.targets[0].group;
										if (b == 'wu') {
											a.changeHujia();
										}
										if (b == 'shu') {
											a.loseHp();
										}
										if (b == 'wei') {
											a.draw(2);
										}
										if (b == 'jin') {
											a.gainMaxHp();
										}
										if (b == 'qun') {
											let num = 0;
											if (a.countCards('h')) {
												num++;
											}
											if (a.countCards('e')) {
												num++;
											}
											if (a.countCards('j')) {
												num++;
											}
											if (num > 0) {
												player.gainPlayerCard(a, num, 'hej', true).set('filterButton', function (button) {
													for (const i of ui.selected.buttons) {
														if (get.position(button.link) == get.position(i.link)) {
															return false;
														}
													}

													return true;
												});
											}
										}
									}
								}
							},
						},
						lqym_yinzhuang: {
							trigger: {
								player: 'damage',
							},
							dutySkill: true,
							forced: true,
							intro: {
								content: 'mark',
							},
							content() {
								player.addMark('lqym_yinzhuang');
							},
							group: 'lqym_yinzhuang_achieve',
							subSkill: {
								achieve: {
									trigger: {
										player: 'dieBegin',
									},
									forced: true,
									filter(event, player) {
										return player.hasMark('lqym_yinzhuang');
									},
									content() {
										game.log(player, '成功完成使命');
										player.awakenSkill('lqym_yinzhuang');
										var a = player.countMark('lqym_yinzhuang'),
											b = player.group;
										trigger.cancel();
										var a = player.getCards('hej');
										game.addVideo('lose', player, [get.cardsInfo(a), [], []]);

										for (const i of a) {
											i.discard(false);
										}

										game.log(player, '退化为', '#g「碟状幼体」');
										player.uninit();
										player.init('lqym_diezhuangyouti');
										player.addMark('lqym_yinzhuang', a);
										player.changeGroup(b);
										player.directgain(get.cards(4));
									},
								},
							},
						},
						lqym_jinran: {
							enable: 'phaseUse',
							usable: 1,
							filter(event, player) {
								return player.hasMark('lqym_yinzhuang');
							},
							filterTarget(card, player, target) {
								return target.group == player.group;
							},
							content() {
								player.removeMark('lqym_yinzhuang');
								target.damage();
							},
							dutySkill: true,
							group: 'lqym_jinran_achieve',
							subSkill: {
								achieve: {
									trigger: {
										player: 'phaseBegin',
									},
									forced: true,
									filter(event, player) {
										return !player.hasMark('lqym_yinzhuang');
									},
									content() {
										'step 0';
										game.log(player, '成功完成使命');
										player.awakenSkill('lqym_jinran');
										var a = player.getCards('hej');
										game.addVideo('lose', player, [get.cardsInfo(a), [], []]);

										for (const i of a) {
											i.discard(false);
										}

										game.log(player, '化为', '#g「隐形水母怪」');
										player.uninit();
										player.init('lqym_No.4');
										('step 1');
										player.directgain(get.cards(4));
										player
											.chooseControl('wei', 'shu', 'wu', 'qun', 'jin')
											.set('prompt', '浸染:令所有角色变换势力')
											.set('ai', function () {
												return 'wei';
											});
										('step 2');
										var a = game.filterPlayer(function (current) {
											return current.group != result.control;
										});
										a.sort(lib.sort.seat);
										player.line(a, 'green');
										game.countPlayer(function (current) {
											const a = result.control;
											if (current.group != a) {
												current.changeGroup(a);
											}
										});
									},
								},
							},
							ai: {
								order: 10,
								result: {
									target: -1,
								},
							},
						},
						lqym_qianhe: {
							enable: 'phaseUse',
							filter(event, player) {
								let a = 0;
								game.countPlayer(function (current) {
									for (const i in lib.skill) {
										if (current.countMark(i) > 0 && lib.skill[i].intro) {
											a++;
										}
									}
								});
								return a > 0 && ui.discardPile.childNodes.length;
							},
							usable: 5, //QQQ
							filterTarget(card, player, target) {
								const a = [];
								game.countPlayer(function (current) {
									for (const i in lib.skill) {
										if (current.countMark(i) > 0 && lib.skill[i].intro) {
											a.push(current);
										}
									}
								});
								return a.includes(target);
							},
							intro: {
								content: 'mark',
							},
							content() {
								'step 0';
								var a = [];
								for (const i in lib.skill) {
									if (target.countMark(i) > 0 && lib.skill[i].intro) {
										a.push(i);
									}
								}
								if (a.length > 1) {
									player
										.chooseControl(a)
										.set('prompt', '嵌合:移去' + get.translation(target) + '任意技能的一个标记.')
										.set('ai', function () {
											return 'wei';
										});
								} else {
									event._result = { control: a[0] };
								} //单选
								('step 1');
								var a = [];
								target.removeMark(result.control);
								get.discardPile(function (card) {
									a.push(card);
								});
								if (a) {
									player.gain(a.randomGet(), 'gain2');
								}
							},
							ai: {
								order: 10,
								result: {
									target: -0.5,
									player: 1,
								},
							},
							group: 'lqym_qianhe_mark',
							subSkill: {
								mark: {
									trigger: {
										player: 'useCardEnd',
									},
									filter(event, player, includecard) {
										return event.targets && event.targets.length && ['sha', 'juedou'].includes(event.card.name);
									},
									forced: true,
									content() {
										player.addMark('lqym_qianhe');
									},
								},
							},
						},
						lqym_longtong: {
							mod: {
								cardUsableTarget(card, player, target) {
									if (
										!player.hasHistory('useCard', function (evt) {
											return evt.card.name == 'sha' && evt.targets && evt.targets.includes(target);
										}) &&
										player.canUse('sha', target) &&
										card.name == 'sha'
									) {
										return true;
									}
								},
								targetInRange(card, target, player) {
									if (card.name == 'sha') {
										return true;
									}
								},
							},
						},
						lqym_yanfen: {
							enable: 'phaseUse',
							filter(event, player) {
								return game.hasPlayer(function (current) {
									return current.countCards('he') && current != player;
								});
							},
							filterTarget(card, player, target) {
								return target.countCards('he') && target != player;
							},
							usable: 1,
							content() {
								'step 0';
								player.choosePlayerCard(targets[0], 'he', true).ai = get.buttonValue;
								('step 1');
								if (result.links?.length) {
									player.addToExpansion(result.links[0], 'giveAuto', targets[0]).gaintag.add('lqym_yanfen');
									player.draw();
								}
							},
							intro: {
								content: 'expansion',
								markcount: 'expansion',
							},
							onremove(player, skill) {
								const cards = player.getExpansions('lqym_yanfen');
								if (cards.length) {
									player.loseToDiscardpile(cards);
								}
							},
							ai: {
								order: 10,
								result: {
									target: -1,
								},
							},
							group: 'lqym_yanfen_remove',
							subSkill: {
								remove: {
									trigger: {
										player: ['damageBegin', 'phaseEnd'],
									},
									filter(event, player) {
										if (event.name == 'phase') {
											return player.getExpansions('lqym_yanfen').length > 1;
										}
										return player.getExpansions('lqym_yanfen').length && event.nature != 'fire';
									},
									forced: true,
									content() {
										'step 0';
										var a = trigger.name,
											b = a == 'damage' ? 1 : 2;
										player.chooseCardButton(a == 'damage' ? 1 : [2, 3], '###是否发动【焱焚】？###弃置' + (a == 'damage' ? '一' : '2-3') + '张<符>' + (a == 'damage' ? '令伤害变成火属性.' : '并对一名角色造成伤害.'), player.getExpansions('lqym_yanfen')).ai = function (button) {
											return true;
										};
										('step 1');
										if (result.links?.length) {
											event.a = result.links.length;
											player.loseToDiscardpile(result.links);
											if (trigger.name == 'damage') {
												trigger.nature = 'fire';
												event.finish();
											} else {
												player.chooseTarget(true, '对一名角色造成伤害.').ai = function (target) {
													return get.damageEffect(target, player, player, 'fire') * target.hp;
												};
											}
										}
										('step 2');
										if (result.targets?.length) {
											var b = result.targets[0],
												c = event.a == 2 ? Math.floor(b.hp / 2) : b.hp - 1;
											if (c < 1) {
												c = 1;
											}
											b.damage(c, 'fire');
										}
									},
								},
							},
						},
						lqym_yanhua: {
							trigger: {
								player: 'damageBegin',
							},
							filter(event, player) {
								return event.nature == 'fire';
							},
							forced: true,
							content() {
								trigger.cancel();
								player.draw(trigger.num);
							},
						},
						lqym_xingyun: {
							enable: 'phaseUse',
							usable: 1,
							content() {
								'step 0';
								if (!player.storage.lqym_xingyun1) {
									game.me.chooseBool('是否跳过' + get.translation(player) + '本局游戏此技能的掷骰子过程？').ai = function () {
										return false;
									};
								} else {
									event.goto(2);
								}
								('step 1');
								if (result.bool) {
									player.storage.lqym_xingyun1 = 1;
								} else {
									player.storage.lqym_xingyun1 = 2;
								}
								('step 2');
								event.a = player.storage.lqym_xingyun1;
								if (event.a == 2) {
									player.throwDice();
								}
								('step 3');
								event.b = event.a == 2 ? num : [1, 2, 3, 4, 5, 6].randomGet();
								('step 4');
								if (event.a == 2) {
									player.throwDice();
								}
								('step 5');
								event.c = event.a == 2 ? num : [1, 2, 3, 4, 5, 6].randomGet();
								player.chat('骰子数:' + event.b + '/' + event.c);
								game.log('骰子数分别为:' + event.b + '/' + event.c);
								player.popup(event.b + '/' + event.c);
								const a = get.cards(Math.max(event.b, event.c)); //最大
								player.discard(
									player.getCards('s', function (card) {
										return card.hasGaintag('lqym_xingyun');
									}),
								);
								player.unmarkSkill('lqym_xingyun');
								player.loseToSpecial(a, 'lqym_xingyun').visible = true;
								('step 6');
								player.markSkill('lqym_xingyun');
								game.updateRoundNumber(); //刷新
								if (event.c + event.b == 7) {
									const d = player.isDamaged() ? 0 : 1;
									const next = player.chooseTarget();
									next.set('filterTarget', function (card, player, target) {
										if (ui.selected.targets.length && ui.selected.targets[0].countCards('e') > 0) {
											return target.countCards('e');
										}
										if (!player.countCards('e')) {
											return target != player;
										}
										return true;
									});
									next.set('ai', function (target) {
										const e = get.attitude(_status.event.player, target);
										if (target.countCards('e')) {
											return -e * 2;
										}
										return e;
									});
									next.set('complexTarget', true);
									next.set('selectTarget', function () {
										if (ui.selected.targets.length && ui.selected.targets[0].countCards('e') > 0) {
											return [d, Infinity];
										}
										return [d, 1];
									});
									next.set('prompt', (_status.event.player.isDamaged() ? '直接点确实你回复一点体力或' : '') + '令一名其他角色获得一张<智囊>或弃置任意名角色装备区内所有牌.');
								} else {
									event.finish();
								}
								('step 7');
								if (result.targets?.length) {
									const f = result.targets;
									event.f = f[0];
									if (!f.length) {
										player.recover();
										event.finish();
									} else if (f.length > 1) {
										for (const i of f) {
											i.discard(i.getCards('e'));
										}
										event.finish();
									} else {
										if (f[0] == player) {
											player.discard(player.getCards('e'));
											event.finish();
										} else if (f[0].countCards('e')) {
											player.chooseControlList(true, ['令' + get.translation(f[0]) + '获得一张<智囊>', '令' + get.translation(f[0]) + '弃置装备区所有牌'], function () {
												if (get.attitude(_status.event.player, f[0]) > 0) {
													return 0;
												}
												return 1;
											});
										} else {
											event._result = { index: 0 };
										}
									}
								}
								('step 8');
								if (result.index == 1) {
									event.f.discard(event.f.getCards('e'));
									event.finish();
								}
								if (result.index == 0) {
									event.f.chooseButton(['幸运:获得一张智囊.', [get.zhinangs(), 'vcard']]).set('ai', function (card) {
										return (Math.random() + 0.5) * get.value({ name: card.link[2] }, _status.event.player);
									});
								}
								('step 9');
								if (result.links?.length) {
									const g = get.cardPile(function (card) {
										return card.name == result.links[0][2];
									});
									if (g) {
										event.f.gain(g, 'gain2');
									}
								}
							},
							ai: {
								order: 10,
								result: {
									player: 3,
								},
							},
							intro: {
								mark(dialog, storage, player) {
									dialog.addAuto(
										player.getCards('s', function (card) {
											return card.hasGaintag('lqym_xingyun');
										}),
									);
								},
								markcount(storage, player) {
									return player.getCards('s', function (card) {
										return card.hasGaintag('lqym_xingyun');
									}).length;
								},
								onunmark(storage, player) {
									const cards = player.getCards('s', function (card) {
										return card.hasGaintag('lqym_xingyun');
									});
									if (cards.length) {
										player.lose(cards, ui.discardPile);
										player.$throw(cards, 1000);
										game.log(cards, '进入了弃牌堆');
									}
								},
							},
							mod: {
								aiOrder(player, card, num) {
									if (get.itemtype(card) == 'card' && card.hasGaintag('lqym_xingyun')) {
										return num + 0.5;
									}
								},
							},
							group: 'lqym_xingyun_count',
						},
						lqym_xingyun_count: {
							trigger: {
								player: 'loseAfter',
							},
							forced: true,
							popup: false,
							filter(event, player) {
								if (!event.ss || !event.ss.length) {
									return false;
								}
								for (const i in event.gaintag_map) {
									if (event.gaintag_map[i].includes('lqym_xingyun')) {
										return true;
									}
									return false;
								}
							},
							content() {
								const num = player.getCards('s', function (card) {
									return card.hasGaintag('lqym_xingyun');
								}).length;
								if (num > 0) {
									player.markSkill('lqym_xingyun');
								} else {
									player.unmarkSkill('lqym_xingyun');
								}
							},
						},
						lqym_jizu: {
							enable: 'phaseUse',
							usable: 1,
							filterTarget: lib.filter.notMe,
							content() {
								for (const i of target.getSkills(true, false)) {
									if (i.indexOf('rewrite') == -1 && lib.translate[i] && lib.translate[i + '_info'] && i != player.getSkills(true, false).slice(0)) {
										player.addTempSkill(i);
										player.popup(i);
									}
								}
								target.addTempSkill('baiban');
							},
							subSkill: {
								count: {
									charlotte: true,
									forced: true,
									onremove(player) {
										if (player.storage.lqym_jizu) {
											const a = player.storage.lqym_jizu;
											player.maxHp = player.maxHp + a[0];
											player.hp = player.hp + a[1];
											player.update();
											delete player.storage.lqym_jizu;
										}
									},
								},
							},
						},
						lqym_yaoxing: {
							trigger: {
								source: 'damageSource',
							},
							filter(event, player) {
								return !player.getExpansions('lqym_yaoxing').length && event.card;
							},
							prompt: '是否发动【妖星】？',
							prompt2: '可以将牌堆顶两张牌置于你的武将牌上作为<星>.',
							content() {
								player.addToExpansion(get.cards(2), player, 'giveAuto').gaintag.add('lqym_yaoxing');
							},
							intro: {
								content: 'expansion',
								markcount: 'expansion',
							},
							onremove(player, skill) {
								const cards = player.getExpansions('lqym_yaoxing');
								if (cards.length) {
									player.loseToDiscardpile(cards);
								}
							},
							group: 'lqym_yaoxing_damage',
							subSkill: {
								damage: {
									trigger: {
										player: 'damageEnd',
									},
									filter(event, player) {
										return event.source && event.source.countCards('he');
									},
									forced: true,
									content() {
										'step 0';
										const a = player.choosePlayerCard(trigger.source, 'he');
										a.ai = function (button) {
											return -get.attitude(player, trigger.source);
										};
										('step 1');
										if (result.links?.length) {
											player.addToExpansion(result.links[0], 'giveAuto', trigger.source).gaintag.add('lqym_yaoxing');
										}
									},
								},
							},
						},
						lqym_jianmie: {
							enable: 'phaseUse',
							usable: 1,
							filter(event, player) {
								return player.getExpansions('lqym_yaoxing').length;
							},
							chooseButton: {
								dialog(event, player) {
									return ui.create.dialog('歼灭', player.getExpansions('lqym_yaoxing'), 'hidden');
								},
								select() {
									return [1, _status.event.player.getExpansions('lqym_yaoxing').length];
								},
								backup(links, player) {
									return {
										filterTarget: true,
										selectTarget: links.length,
										filterCard() {
											return false;
										},
										selectCard: -1,
										card: links,
										delay: false,
										content: lib.skill.lqym_jianmie.contentx,
										ai: {
											order: 10,
											result: {
												target: -1,
											},
										},
									};
								},
								prompt() {
									return '请选择〖歼灭〗的目标';
								},
							},
							contentx() {
								const card = lib.skill.lqym_jianmie_backup.card;
								player.loseToDiscardpile(card);
								target.damage();
							},
							ai: {
								order: 10,
								combo: 'lqym_yaoxing',
								result: {
									player: 1,
								},
							},
						},
						lqym_tiangai: {
							enable: ['chooseToUse', 'chooseToRespond'],
							filter(event, player) {
								if (player.getStat('skill').lqym_tiangai_backup > 0) {
									return false;
								}
								return player.getExpansions('lqym_yaoxing').length && ((event.filterCard && event.filterCard({ name: 'wuxie' }, player, event)) || (event.filterCard && event.filterCard({ name: 'shan' }, player, event)));
							},
							hiddenCard(player, name) {
								if (player.getStat('skill').lqym_tiangai_backup > 0) {
									return false;
								}
								return ['wuxie', 'shan'].includes(name) && player.getExpansions('lqym_yaoxing').length;
							},
							chooseButton: {
								dialog(event, player) {
									const dialog = ui.create.dialog('天盖', 'hidden');
									dialog._cardName = event.filterCard && event.filterCard({ name: 'wuxie' }, player, event) ? 'wuxie' : 'shan';
									dialog.add(player.getExpansions('lqym_yaoxing'));
									return dialog;
								},
								filter(button) {
									const evt = _status.event,
										player = _status.event.player;
									if (!evt.dialog._chooseButton) {
										const evt2 = _status.event.parent;
										return evt2.filterCard({ name: evt.dialog._cardName }, player, evt2);
									}
									return typeof button.link == 'string';
								},
								backup(links, player) {
									let card, name;
									if (links.length == 2) {
										name = links[0];
										card = links[1];
									} else {
										card = links[0];
										const event = _status.event;
										name = event.filterCard({ name: 'wuxie' }, player, event) ? 'wuxie' : 'shan';
									}
									return {
										filterCard(card, player) {
											return card == lib.skill.lqym_tiangai_backup.card;
										},
										selectCard: -1,
										position: 'x',
										viewAs: { name: name },
										card: card,
									};
								},
							},
							ai: {
								respondShan: true,
								skillTagFilter(player, tag, arg) {
									if (player.getStat('skill').lqym_tiangai_backup > 0) {
										return false;
									}
									return player.getExpansions('lqym_yaoxing').length;
								},
								result: {
									player: 1,
								},
							},
						},
						lqym_baihui: {
							enable: 'phaseUse',
							usable: 1,
							filter(event, player) {
								return player.countDiscardableCards(player, 'h') > 0;
							},
							filterCard: true,
							content() {
								player.draw();
							},
						},
						lqym_tianqi: {
							enable: 'phaseUse',
							usable: 1,
							filterTarget: lib.filter.notMe,
							selectTarget() {
								return [1, _status.event.player.countMark('charge')];
							},
							chargeSkill: true,
							filter(event, player) {
								return player.hasMark('charge');
							},
							prompt(event, player) {
								return '与至多' + _status.event.player.countMark('charge') + '名角色进行谋弈.';
							},
							content() {
								'step 0';
								const mouyi = '启明';
								player.chooseToLqtqMouyi(target, mouyi);
								('step 1');
								if (result.bool) {
									if (result.mouyi == 'lqtq_pohuaizhilun') {
										target.damage();
									}
									if (result.mouyi == 'lqtq_hejuzhilun') {
										if (target.countGainableCards(player, 'he')) {
											player.gainPlayerCard(target, 'he', true);
										}
										player.draw();
									}
								}
							},
							contentAfter() {
								player.removeMark('charge', targets.length);
							},
							ai: {
								order: 10,
								result: {
									player: 2,
									target: -1,
								},
							},
							group: 'lqym_tianqi_backflow',
							subSkill: {
								backflow: {
									trigger: {
										player: ['gainAfter', 'enterGame'],
										global: ['loseAsyncAfter', 'phaseBefore'],
									},
									forced: true,
									filter(event, player, name) {
										if (name == 'phaseBefore') {
											return game.phaseNumber == 0;
										}
										if (name == 'enterGame') {
											return true;
										}
										const a = [],
											b = event.getParent(2).name;
										if (player.countMark('charge') >= 4 || b == 'lqym_tianqi' || b == '_chongzhu' || event.getg(player).length == 0) {
											return false;
										}
										for (const i in lib.skill) {
											a.push(i);
										}
										if (a.includes(event.getParent(2).name) || a.includes(b)) {
											return true;
										}
										return false;
									},
									content() {
										player.addMark('charge', ['gain', 'loseAsync'].includes(trigger.name) ? 1 : 2);
									},
								},
							},
						},
						lqym_kanpo: {
							enable: 'phaseUse',
							filter(event, player) {
								return game.hasPlayer(function (current) {
									return current.countCards('h') && current != player;
								});
							},
							filterTarget(card, player, target) {
								if (!ui.selected.targets.length) {
									return true;
								}
								return target.countCards('h') && target != player;
							},
							targetprompt: ['被使用牌', '被观看牌'],
							selectTarget() {
								if (ui.selected.targets.length && ui.selected.targets[0] == _status.event.player) {
									return [2, 2];
								}
								return [1, 2];
							},
							usable: 1,
							multitarget: true,
							multiline: true,
							content() {
								'step 0';
								event.b = targets[1] || targets[0];
								game.log(player, '观看了', event.b, '的手牌');
								player.chooseControl('ok').set('dialog', [get.translation(event.b) + '的手牌', event.b.getCards('h')]);
								('step 1');
								const a = event.b.getCards('h').randomGet();
								game.log('系统随机选择了', a);
								event.b.showCards(a);
								if (event.b.canUse(a, targets[0]) && a.name != 'shengdong') {
									event.b.useCard(a, targets[0]);
								} else {
									event.b.discard(a, true);
								}
							},
						},
						lqym_guiying: {
							trigger: { global: 'useCardAfter' },
							filter(event, player) {
								if (_status.currentPhase != event.player) {
									return false;
								}
								return event.cards && event.cards.filterInD().length && player.hasMark('charge'); //QQQ
							},
							check(event, player) {
								return get.attitude(player, event.player) > 0;
							},
							logTarget(event, player) {
								return event.player;
							},
							prompt2(event) {
								return '令' + get.translation(event.player) + '获得' + get.translation(event.cards);
							},
							chargeSkill: true,
							content() {
								player.removeMark('charge');
								trigger.player.gain(trigger.cards.filterInD(), 'gain2');
							},
							group: 'lqym_guiying_charge',
							subSkill: {
								charge: {
									trigger: {
										global: ['phaseAfter', 'phaseBefore'],
										player: ['damageEnd', 'enterGame'],
									},
									filter(event, player, name) {
										if (player.countMark('charge') > 2) {
											return false;
										}
										return name != 'phaseBefore' || game.phaseNumber == 0;
									},
									forced: true,
									content() {
										player.addMark('charge');
									},
								},
							},
						},
						lqym_gaiyin: {
							trigger: {
								player: 'damageBegin4',
							},
							forced: true,
							content() {
								if (trigger.nature) {
									trigger.nature = false;
								}
								if (trigger.num > 1) {
									trigger.num--;
									game.log('伤害减一');
								}
								player.draw();
							},
						},
						lqym_xianren: {
							trigger: {
								global: 'phaseZhunbeiBegin',
							},
							filter(event, player) {
								return (player.countCards('h') < event.player.countCards('h') || player.hp < event.player.hp) && player.canUse('juedou', event.player) && event.player != player;
							},
							check(event, player) {
								return get.attitude(player, event.player) < 0 && (player.countCards('h', 'sha') || player.hp > 2);
							},
							logTarget(event, player) {
								return event.player;
							},
							prompt2(event, player) {
								return '视为对' + get.translation(event.player) + '使用一张决斗.';
							},
							content() {
								player.useCard({ name: 'juedou' }, trigger.player);
							},
						},
						lqym_yabo: {
							trigger: {
								source: 'damageBegin1',
							},
							forced: true,
							filter(event, player) {
								return player.countDiscardableCards(player, 'he') > 1;
							},
							content() {
								'step 0';
								player.chooseToDiscard(2, 'he', '是否发动【亚伯】？', '弃置两张牌令对' + get.translation(trigger.player) + '造成的伤害加一.').set('ai', function (card) {
									if (get.attitude(_status.event.player, trigger.player) < 0) {
										return 8 - get.value(card);
									}
									return 0;
								})('step 1');
								if (result.bool) {
									trigger.num++;
								}
							},
						},
						lqym_shuzui: {
							trigger: {
								global: 'phaseJieshuBegin',
							},
							filter(event, player) {
								return event.player.countGainableCards(player, 'h') > 2 || (event.player.hp > 2 && event.player != player);
							},
							forced: true,
							content() {
								'step 0';
								player.gainPlayerCard([1, 2], '是否发动【赎罪】？', '可获得' + get.translation(trigger.player) + '最多两张牌,受到等量的雷属性伤害.', trigger.player, 'he').set('ai', function (button) {
									if (get.attitude(_status.event.player, trigger.player) < 0 && _status.event.player.hp > 1) {
										return get.value(button.link);
									}
									return 0;
								})('step 1');
								if (result.links?.length) {
									player.damage(result.links.length, 'thunder');
								}
							},
						},
						lqym_shuangan: {
							trigger: {
								player: 'damageBegin',
								source: 'damageBegin',
								global: ['phaseZhunbeiBegin', 'phaseJieshuBegin'],
							},
							_priority: 1, //QQQ
							marktext: '黯',
							filter(event, player, name) {
								const a = lib.skill.lqym_shuangan.qiehuan(player);
								if (!a || event.isMine()) {
									return false;
								}
								if (name == 'damageBegin') {
									return (event.player == player && a == 'lqym_No.31') || (event.source && event.source == player && a == 'lqym_No.13' && event.player != player);
								}
								if (name == 'phaseZhunbeiBegin') {
									return a == 'lqym_No.31';
								}
								return a == 'lqym_No.13';
							},
							popup: false,
							forced: true,
							content() {
								lib.skill.lqym_shuangan.lqtq(player);
							},
							qiehuan(target) {
								const a = [],
									b = [];
								if (target.name) {
									a.push(target.name);
								}
								if (target.name1) {
									a.push(target.name1);
								}
								if (target.name2) {
									a.push(target.name2);
								}
								for (const i of a) {
									if ((i == 'lqym_No.13' || i == 'lqym_No.31') && !b.includes(i)) {
										b.add(i);
									}
								}
								if (b.length == 1) {
									return b[0];
								}
								if (b.length == 2) {
									return true;
								}
								return false;
							},
							lqtq(player) {
								const a = lib.skill.lqym_shuangan.qiehuan(player);
								if (a) {
									const b = a == 'lqym_No.13' ? 'lqym_No.31' : 'lqym_No.13';
									player.reinit(a, b);
								}
							},
							mark: true,
							init(player) {
								if (_status.characterlist) {
									const a = lib.skill.lqym_shuangan.qiehuan(player);
									if (a) {
										const b = a == 'lqym_No.13' ? 'lqym_No.31' : 'lqym_No.13';
										_status.characterlist.add(b);
										_status.characterlist.remove(b);
									}
								}
							},
							intro: {
								mark(dialog, content, player) {
									const a = lib.skill.lqym_shuangan.qiehuan(player);
									if (a != false && player.isUnderControl(true) && _status.gameStarted) {
										if (a == true) {
											dialog.addText('你的主将与副将分别为No.13 该隐恶魔与No.31 亚伯恶魔,无需切换');
										} else {
											const b = a == 'lqym_No.13' ? 'lqym_No.31' : 'lqym_No.13';
											dialog.addText('点击<切换武将>将武将牌' + get.translation(a) + '切换为' + get.translation(b));
											dialog.add(
												ui.create.div('.menubutton.pointerdiv', '切换武将', function () {
													if (!this.disabled) {
														this.disabled = true;
														this.classList.add('disabled');
														this.style.opacity = 0.5;
														lib.skill.lqym_shuangan.lqtq(player);
													}
												}),
											);
										}
									} else {
										dialog.addText('标记里空空如也');
									}
								},
							},
						},
						lqtm_qiangfei: {
							trigger: {
								player: 'damageBegin',
							},
							forced: true,
							filter(event, player) {
								return event.card && get.type2(event.card) == 'trick' && event.source != player;
							},
							content() {
								'step 0';
								trigger.cancel();
								if (trigger.source) {
									if (trigger.source.countGainableCards(player, 'h')) {
										trigger.source.chooseControlList(true, ['受到一点伤害', '令' + get.translation(player) + '获得你一张手牌'], function () {
											return Math.random();
										});
									} else {
										event._result = { index: 0 };
									}
								} else {
									event.finish();
								}
								('step 1');
								if (result.index == 0) {
									trigger.source.damage();
								} else {
									player.gainPlayerCard(true, trigger.source);
								}
							},
							ai: {
								notrick: true,
								effect: {
									target(card, player, target, current) {
										if (get.type(card) == 'trick' && get.tag(card, 'damage')) {
											return 'zeroplayer';
										}
									},
								},
							},
						},
						lqym_yushou: {
							trigger: {
								player: 'gainEnd',
								source: 'damageSource',
							},
							filter(event, player) {
								if (event.name == 'damage') {
									return !event.card;
								}
								return event.source && event.source != player;
							},
							forced: true,
							content() {
								player.draw();
							},
						},
						lqtm_lianhuan: {
							enable: 'phaseUse',
							derivation: ['lqym_wudi', 'lqtq_sanchen', 'lqtq_anhong'],
							usable: 1,
							filterTarget(card, player, target) {
								return target.countDiscardableCards(player, 'he');
							},
							content() {
								'step 0';
								player.discardPlayerCard(target, 'he', true);
								('step 1');
								if (result.cards?.length) {
									const a = get.translation(result.cards[0].name).length;
									if (a > 1) {
										player.addTempSkill('lqym_wudi', { player: 'phaseBefore' });
									}
									if (a > 2) {
										player.addTempSkill('lqtq_sanchen', { player: 'phaseBefore' });
									}
									if (a > 3) {
										player.addTempSkill('lqtq_anhong', { player: 'phaseBefore' });
									}
								}
							},
							ai: {
								order: 9,
								result: {
									target: -1,
								},
							},
						},
						lqym_sese: {
							forced: true,
							trigger: { global: 'roundStart' },
							filter(event, player) {
								return player.hasMark('lqym_sese_basic') || player.hasMark('lqym_sese_trick') || player.hasMark('lqym_sese_equip');
							},
							content() {
								'step 0';
								const a = [];
								if (player.hasMark('lqym_sese_basic')) {
									a.push('色色「基本牌」');
								}
								if (player.hasMark('lqym_sese_trick')) {
									a.push('瑟瑟「锦囊牌」');
								}
								if (player.hasMark('lqym_sese_equip')) {
									a.push('涩涩「装备牌」');
								}
								a.push('cancel2');
								player
									.chooseControl(a)
									.set('prompt', '###是否发动【涩涩】###移一个任意标记,获得对应效果.')
									.set('ai', function () {
										return Math.random();
									});
								('step 1');
								if (result.control != 'cancel2') {
									if (result.control == '色色「基本牌」') {
										player.removeMark('lqym_sese_basic');
										player.storage.lqym_sese = 'basic';
									}
									if (result.control == '瑟瑟「锦囊牌」') {
										player.removeMark('lqym_sese_trick');
										player.storage.lqym_sese = 'trick';
									}
									if (result.control == '涩涩「装备牌」') {
										player.removeMark('lqym_sese_equip');
										player.storage.lqym_sese = 'equip';
									}
									player.addTempSkill('lqym_sese_gian', 'roundStart');
									if (!player.hasMark('lqym_sese_basic') && !player.hasMark('lqym_sese_trick') && !player.hasMark('lqym_sese_equip')) {
										player.addMark('lqym_sese_basic');
										player.addMark('lqym_sese_trick');
										player.addMark('lqym_sese_equip');
									}
								}
							},
							group: 'lqym_sese_add',
							subSkill: {
								add: {
									trigger: {
										global: 'roundStart',
										player: 'enterGame',
									},
									filter(event, player, name) {
										return name == 'enterGame' || game.phaseNumber == 0;
									},
									forced: true,
									content() {
										player.addMark('lqym_sese_basic');
										player.addMark('lqym_sese_trick');
										player.addMark('lqym_sese_equip');
									},
								},
								gian: {
									onremove(player, skill) {
										delete player.storage.lqym_sese;
									},
									trigger: {
										global: ['loseAfter', 'cardsDiscardAfter', 'loseAsyncAfter', 'equipAfter'],
									},
									forced: true,
									filter(event, player) {
										const type = player.storage.lqym_sese,
											cards = event.getd();
										game.log(type);
										if (Array.isArray(cards)) {
											for (const i of cards) {
												if (_status.currentPhase != player && !player.countCards('he', i.name) && get.type(i, 'trick') == type && get.position(i, true) == 'd') {
													return true;
												}
											}
										}
										return false;
									},
									content() {
										'step 0';
										event.cards = [];
										const type = player.storage.lqym_sese,
											cards = trigger.getd();
										if (Array.isArray(cards)) {
											for (const i of cards) {
												if (!player.countCards('he', i.name) && get.type(i, 'trick') == type && get.position(i, true) == 'd') {
													event.cards.push(i);
												}
											}
										}
										('step 1');
										player.chooseCardButton([1, event.cards.length], event.cards, '是否发动【涩涩】？:获得其中任意张牌.').ai = function (card) {
											if (card.name == 'du') {
												return 0;
											}
											return get.value(card);
										};
										('step 2');
										if (result.links?.length) {
											player.gain(result.links, 'gain2');
										}
									},
								},
								basic: {
									marktext: '色',
									intro: {
										name: '色色',
										content: 'mark',
									},
								},
								trick: {
									marktext: '瑟',
									intro: {
										name: '瑟瑟',
										content: 'mark',
									},
								},
								equip: {
									marktext: '涩',
									intro: {
										name: '涩涩',
										content: 'mark',
									},
								},
							},
						},
						lqym_shie: {
							trigger: {
								global: 'phaseBefore',
								player: ['enterGame', 'phaseEnd', 'phaseZhunbeiBegin'],
								source: 'damageBegin',
							},
							marktext: '恶',
							intro: {
								content: 'mark',
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'phaseBefore') {
									return game.phaseNumber == 0;
								}
								if (name == 'phaseEnd') {
									return player.hasMark('lqym_shie') && player.getStat().damage > 0;
								}
								if (name == 'damageBegin') {
									return player.countMark('lqym_shie') < 2;
								}
								return true;
							},
							content() {
								'step 0';
								const a = event.triggername;
								if (a == 'phaseBefore' || a == 'enterGame') {
									player.addMark('lqym_shie', 2);
								}
								if (a == 'damageBegin') {
									trigger.num += 2 - player.countMark('lqym_shie');
								}
								if (a == 'phaseEnd') {
									player.removeMark('lqym_shie');
									if (player.countCards('h') < 5) {
										player.chooseControlList(true, ['获得一点护甲', '将手牌摸至5张'], function () {
											return 1;
										});
									} else {
										event._result = { index: 0 };
									}
								}
								('step 1');
								if (result.index == 0) {
									player.changeHujia();
								}
								if (result.index == 1) {
									player.drawTo(5);
								}
							},
						},
						lqym_juetong: {
							trigger: {
								player: 'phaseUseBegin',
							},
							filter(event, player) {
								return (
									player.countCards('he') > 0 &&
									game.hasPlayer(function (current) {
										return current.countCards('he') && current != player;
									})
								);
							},
							forced: true,
							content() {
								'step 0';
								player.chooseCardTarget({
									prompt: '是否发动【绝同】？',
									prompt2: '弃置一张牌并选择一名其他角色.',
									filterCard: true,
									position: 'he',
									filterTarget: lib.filter.notMe,
									ai1(card) {
										return 7 - get.useful(card);
									},
									ai2(target) {
										return -get.attitude(_status.event.player, target) * target.countCards('h');
									},
								});
								('step 1');
								if (result.cards?.length) {
									event.a = result.cards[0];
									event.d = result.targets[0];
									event.b = event.d.getCards('he', function (card) {
										return get.type2(card) == get.type2(event.a);
									});
									player.discard(result.cards, true);
									if (event.b.length) {
										event.d.discard(event.b, 'notBySelf').set('discarder', player);
									} else {
										event.finish();
									}
								} else {
									event.finish();
								}
								('step 2');
								event.c = 0;
								for (const i of event.b) {
									if (i.name == event.a.name) {
										event.c++;
									}
								}
								if (event.c > 0) {
									player.chooseControlList(true, ['摸两张牌', '对' + get.translation(event.d) + '造成一点雷电伤害'], function () {
										return 1;
									});
								} else {
									player.chooseCardButton('你可以获得一张' + get.translation(event.d) + '弃置的牌', event.b).ai = function (button) {
										return get.value(button.link);
									};
								}
								('step 3');
								if (result.index == 0) {
									player.draw(2);
								}
								if (result.index == 1) {
									event.d.damage('thunder');
								}
								if (event.c == 0 && result.bool) {
									player.gain(result.links, 'gain2');
								}
							},
						},
						lqym_jihui: {
							trigger: {
								player: 'dying',
							},
							forced: true,
							content() {
								'step 0';
								player
									.chooseTarget('是否发动【急徽】？', '你可以获得一名其他角色一张牌', function (card, player, target) {
										return target != player && target.countGainableCards(player, 'he');
									})
									.set('ai', function (target) {
										return -get.attitude(_status.event.player, target);
									});
								('step 1');
								if (result.targets?.length) {
									player.gainPlayerCard('he', result.targets[0], true);
								}
							},
						},
						lqym_bingying: {
							trigger: {
								global: 'useCardToPlayer',
							},
							logTarget(event, player) {
								return event.player;
							},
							filter(event, player) {
								const info = get.info(event.card);
								return player.hasMark('charge') && event.targets && !info.multitarget && (get.type(event.card) == 'trick' || (event.card.name == 'sha' && event.cards && get.color(event.cards) == 'black')) && event.player != player;
							},
							prompt2(event) {
								return '取消' + get.translation(event.player) + '使用的' + get.translation(event.card) + '的所有目标.';
							},
							usable: 1,
							check(event, player) {
								return get.attitude(player, event.player) < 0;
							},
							content() {
								'step 0';
								player.removeMark('charge');
								trigger.targets.length = 0;
								trigger.parent.triggeredTargets1.length = 0;
								('step 1');
								player.chooseTarget('冰映:选择一名角色成为' + get.translation(trigger.card) + '的目标,你成为' + get.translation(trigger.card) + '的使用者', function (card, player, target) {
									const trigger = _status.event.getTrigger();
									return lib.filter.targetEnabled2(trigger.card, player, target);
								}).ai = function (target) {
									const trigger = _status.event.getTrigger();
									if (get.attitude(player, event.player) > 0) {
										return get.effect(target, trigger.card, player, player);
									}
									return -get.effect(target, trigger.card, player, player);
								};
								('step 2');
								if (result.targets?.length) {
									trigger.parent.targets.push(result.targets[0]);
									trigger.untrigger();
									trigger.parent.player = player;
								}
							},
							ai: {
								order: 2,
								result: {
									target: 3,
								},
							},
							group: 'lqym_bingying_charge',
							subSkill: {
								charge: {
									trigger: {
										global: 'roundStart',
									},
									filter(event, player, name) {
										return player.countMark('charge') < 2;
									},
									forced: true,
									content() {
										player.addMark('charge', 2 - player.countMark('charge'));
									},
								},
							},
						},
						lqym_huiang: {
							enable: 'phaseUse',
							usable: 1,
							filterTarget: true,
							selectTarget: [1, Infinity],
							content() {
								if (targets.length > 1) {
									target.draw();
								} else {
									target.draw(2);
								}
								target.addTempSkill('lqym_huiang_sha', { player: 'phaseAfter' });
								target.addMark('lqym_huiang_sha');
							},
							ai: {
								order: 9,
								result: {
									target: 3,
								},
							},
							subSkill: {
								sha: {
									charlotte: true,
									nopop: true,
									mod: {
										cardUsable(card, player, num) {
											if (card.name == 'sha') {
												return num + player.countMark('lqym_huiang_sha');
											}
										},
									},
									mark: true,
									marktext: '辉',
									intro: {
										content: '出【杀】数+#',
									},
								},
							},
						},
						lqym_bingjie: {
							enable: 'phaseUse',
							filter(event, player) {
								return player.countCards('h') < Math.min(player.maxHp, 5);
							},
							content() {
								'step 0';
								player.drawTo(6);
								('step 1');
								if (player.hasCard((card) => lib.filter.cardDiscardable(card, player, 'lqym_bingjie'), 'he')) {
									player.chooseToDiscard('he', true).set('ai', function (card) {
										return card.number;
									});
								}
								('step 2');
								if (result.bool && typeof result.cards[0].number == 'number') {
									event.b = result.cards[0];
									player
										.chooseTarget('【冰结】', '令一名其他角色本回合不能使用或打出转化牌以及点数小于' + event.b.number + '的牌.', function (card, player, target) {
											return player != target;
										})
										.set('ai', function (target) {
											return -get.attitude(_status.event.player, target);
										});
								} else {
									event.finish();
								}
								('step 3');
								if (result.targets?.length) {
									const a = result.targets[0];
									a.storage.lqym_bingjie2 = event.b;
									a.addTempSkill('lqym_bingjie2');
								}
							},
							ai: {
								directHit_ai: true,
								skillTagFilter(player, tag, arg) {
									if (!arg.target.hasSkill('lqym_bingjie2')) {
										return false;
									}
									return arg.target.storage.lqym_bingjie2.number > 9;
								},
							},
						},
						lqym_bingjie2: {
							forced: true,
							mark: true,
							charlotte: true,
							mod: {
								cardUsable(card, player) {
									if (card.number < player.storage.lqym_bingjie2.number) {
										return false;
									}
								},
								cardRespondable(card, player) {
									if (card.number < player.storage.lqym_bingjie2.number) {
										return false;
									}
								},
								cardSavable(card, player) {
									if (card.number < player.storage.lqym_bingjie2.number) {
										return false;
									}
								},
								cardEnabled(card, player) {
									if (card.number < player.storage.lqym_bingjie2.number) {
										return false;
									}
								},
							},
							marktext: '冰',
							intro: {
								markcount(storage, player) {
									return storage.number;
								},
								name: '冰结',
								content(storage, player, skill) {
									return '不能使用或打出转化的牌以及点数小于' + storage.number + '的手牌';
								},
							},
						},
						lqym_wugen: {
							trigger: {
								player: ['loseHpEnd', 'phaseDrawBegin1'],
							},
							marktext: '艮',
							intro: {
								content: 'mark',
							},
							forced: true,
							filter(event, player) {
								return event.name == 'loseHp' ? event.num > 0 : !event.numFixed && player.hasMark('lqym_wugen');
							},
							content() {
								trigger.name == 'loseHp' ? player.addMark('lqym_wugen', trigger.num) : (trigger.num += player.countMark('lqym_wugen'));
							},
						},
						//其他角色出牌阶段开始时,你可以弃置一张手牌并失去一点体力,将你与该角色中未横置的角色横置,其获得技能〖魂火〗,失去〖魂火〗以外的所有技能,不能使用黑色基本牌且锦囊牌只能指定你或其为目标直到回合结束
						lqym_luanjian: {
							derivation: 'lqym_hunhuo',
							trigger: {
								global: 'phaseZhunbeiBegin',
							},
							forced: true,
							filter(event, player) {
								return !player.hasSkill('lqym_luanjian3') && event.player != player && player.hasCard((card) => lib.filter.cardDiscardable(card, player, 'lqym_luanjian'), 'he');
							},
							content() {
								'step 0';
								let a = '',
									b = [];
								game.countPlayer(function (current) {
									b.addArray(game.expandSkills(current.getSkills(true, false)));
								}); //QQQ
								if (!player.isLinked() && !trigger.player.isLinked()) {
									a = '将你与' + get.translation(trigger.player) + '横置,';
								} else if (!trigger.player.isLinked()) {
									a = '将' + get.translation(trigger.player) + '横置,';
								} else if (!player.isLinked()) {
									a = '将你横置,';
								}
								player.chooseToDiscard('he', '是否对' + get.translation(trigger.player) + '发动【乱健】？', '弃置一张牌并失去一点体力,' + a + '' + get.translation(trigger.player) + '获得技能〖魂火〗,失去〖魂火〗以外的所有技能,不能使用黑色基本牌且锦囊牌只能指定你与其为目标.').ai = function (card) {
									if (
										game.hasPlayer(function (current) {
											return (player.getStorage('lqym_luanjian').includes(current) || b.includes(current)) && get.attitude(player, current) < 0;
										}) &&
										!player.getStorage('lqym_luanjian').includes(trigger.player)
									) {
										return false;
									} else if (get.attitude(player, trigger.player) < 0 && player.hp > 1) {
										return 7 - get.value(card);
									}
									return false;
								};
								('step 1');
								if (result.bool) {
									player.loseHp();
								} else {
									event.finish();
								}
								('step 2');
								player.line(trigger.player, 'fire'); //指示线
								if (!player.isLinked()) {
									player.link();
								}
								player.addTempSkill('lqym_luanjian3', 'roundStart');
								if (!trigger.player.isLinked()) {
									trigger.player.link();
								}
								trigger.player.addTempSkill('lqym_hunhuo');
								trigger.player.storage.lqym_luanjian3 = player;
								trigger.player.addTempSkill('lqym_luanjian2');
							},
							group: 'lqym_luanjian_count',
						},
						lqym_luanjian_count: {
							trigger: {
								global: 'logSkillBegin', //QQQ
							},
							silent: true,
							_priority: 1,
							filter(event, player) {
								if (event.getParent('lqym_luanjian_count').name == 'lqym_luanjian_count') {
									return false;
								}
								const skills = event.player.getSkills(true, false);
								game.expandSkills(skills);
								return skills.includes(event.skill) && event.player == _status.currentPhase && !player.getStorage('lqym_luanjian').includes(event.player);
							},
							content() {
								player.markAuto('lqym_luanjian', [trigger.player]);
							},
						},
						lqym_luanjian2: {
							init(player, skill) {
								const skills = player.getSkills(true, false);
								for (let i = 0; i < skills.length; i++) {
									if (get.info(skills[i]).charlotte || skills[i] == 'lqym_hunhuo') {
										skills.splice(i--, 1);
									}
								}
								player.disableSkill(skill, skills);
							},
							onremove(player, skill) {
								delete player.storage.lqym_luanjian3;
								player.enableSkill(skill);
							},
							mark: true,
							mod: {
								cardEnabled2(card) {
									if (get.color(card) == 'black' && get.type(card) == 'basic') {
										return false;
									}
								},
								playerEnabled(card, player, target) {
									if (target != player && player.storage.lqym_luanjian3 != target && get.type2(card) == 'trick') {
										return false;
									}
								},
							},
							marktext: '乱',
							intro: {
								name: '乱健',
								content(storage, player, skill) {
									let str = '本回合,你不能使用黑色基本牌,你使用的锦囊牌不能指定你或' + get.translation(player.storage.lqym_luanjian3) + '以外的角色为目标.';
									const list = Object.keys(player.disabledSkills);
									if (list.length) {
										str += '<br>封印技能:';

										for (const i of list) {
											if (lib.translate[i + '_info']) {
												str += get.translation(i) + '/';
											}
										}

										return str.slice(0, str.length - 1);
									}
								},
							},
							charlotte: true,
							forced: true,
							popup: false,
						},
						lqym_luanjian3: {
							marktext: '乱',
							mark: true,
							intro: {
								name: '乱健',
								content: '本轮已发动',
								charlotte: true,
								forced: true,
							},
						},
						lqym_hunhuo: {
							position: 'hes',
							enable: 'chooseToUse',
							filterCard(card, player) {
								return get.color(card) == 'red';
							},
							usable: 1,
							viewAs: {
								name: 'linghunzhihuo',
								nature: 'fire',
							},
							viewAsFilter(player) {
								if (!player.countCards('hes', { color: 'red' })) {
									return false;
								}
							},
							prompt: '将一张红色牌当灵魂之火使用',
							check(card) {
								return 6 - get.value(card);
							},
							ai: {
								fireAttack: true,
							},
						},
						lqym_lingji: {
							forced: true,
							usable: 1,
							trigger: {
								player: 'useCard',
							},
							filter(event, player) {
								return event.card && event.card.name == 'sha' && player == _status.currentPhase;
							},
							logTarget: 'target',
							content() {
								if (!trigger.card.storage) {
									trigger.card.storage = {};
								}
								trigger.card.storage.lqym_lingji = true;
								trigger.directHit.addArray(game.players);
								game.log(trigger.player, '使用的', trigger.card, '无视防具且不能被响应');
							},
							ai: {
								unequip: true,
								unequip: true,
								skillTagFilter(player, tag, arg) {
									if (tag == 'unequip' && (!arg || !arg.card || !arg.card.storage || !arg.card.storage.lqym_lingji)) {
										return false;
									}
									if (tag == 'unequip' && (!arg || arg.name != 'sha')) {
										return false;
									}
								},
							},
						},
						lqym_lingyuan: {
							trigger: {
								source: 'damageSource',
							},
							forced: true,
							filter(event, player) {
								return game.players.length > 2;
							},
							usable: 1,
							content() {
								'step 0';
								player.chooseTarget('是否发动【灵刺】？', '对一名不为' + get.translation(trigger.player) + '的其他角色造成一点伤害.', function (card, player, target) {
									return player != target && target != trigger.player;
								}).ai = function (target) {
									return get.damageEffect(target, player, player);
								};
								('step 1');
								if (result.targets?.length) {
									result.targets[0].damage();
								} else {
									player.getStat('triggerSkill').lqym_lingyuan--;
								}
							},
							group: 'lqym_lingyuan_gain',
							subSkill: {
								gain: {
									trigger: {
										player: 'damageEnd',
									},
									forced: true,
									filter(event, player) {
										return game.hasPlayer(function (current) {
											return (!event.source || (event.source && event.source != current)) && current.countGainableCards(player, 'he') && current != player;
										});
									},
									usable: 1,
									content() {
										'step 0';
										player.chooseTarget('是否发动【灵刺】？', '获得一名' + (!trigger.source ? '其' : '不为' + get.translation(trigger.source) + '的其') + '他角色一张牌.', function (card, player, target) {
											return player != target && trigger.source != target;
										}).ai = function (target) {
											return -get.attitude(player, target);
										};
										('step 1');
										if (result.targets?.length) {
											player.gainPlayerCard('he', result.targets[0], true);
										} else {
											player.getStat('triggerSkill').lqym_lingyuan_gain--;
										}
									},
								},
							},
						},
						lqym_mingyu: {
							forced: true,
							usable: 1,
							trigger: {
								global: 'useCard',
							},
							filter(event, player) {
								return event.player != player && get.type2(event.card) == 'trick' && event.player == _status.currentPhase;
							},
							logTarget: 'player',
							content() {
								trigger.targets.length = 0;
								trigger.all_excluded = true;
								game.log(trigger.player, '使用的', trigger.card, '被无效');
							},
						},
						//浪琴超越
						lqcy_jianxiong: {
							audio: 'rejianxiong',
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								global: 'damageEnd',
							},
							filter(event, player) {
								return player == event.player ? event.num > 0 : !player.hasSkill('lqcy_jianxiong2');
							},
							content() {
								'step 0';
								event.a = trigger.num;
								if (trigger.player != player) {
									player.addTempSkill('lqcy_jianxiong2');
									event.a = 1;
								}
								('step 1');
								event.a--;
								if (get.itemtype(trigger.cards) == 'cards' && get.position(trigger.cards[0], true) == 'o') {
									player.gain(trigger.cards, 'gain2');
								} else if (trigger.card) {
									card = get.cardPile(function (card) {
										return card.name == trigger.card.name;
									});
									if (card) {
										player.gain(card, 'draw2');
									}
								}
								player.draw(2);
								('step 2');
								if (event.a > 0) {
									player.chooseBool(get.prompt2('lqcy_jianxiong'));
								} else {
									event.finish();
								}
								('step 3');
								if (result.bool) {
									event.goto(1);
								}
							},
							ai: {
								maixie: true,
								maixie_hp: true,
								effect: {
									target(card, player, target) {
										if (player.hasSkillTag('jueqing', false, target)) {
											return [1, -1];
										}
										if (get.tag(card, 'damage') && player != target) {
											let cards = card.cards,
												evt = _status.event;
											if (evt.player == target && card.name == 'damage' && evt.parent.type == 'card') {
												cards = evt.parent.cards.filterInD();
											}
											if (target.hp <= 1) {
												return;
											}
											if (get.itemtype(cards) != 'cards') {
												return;
											}
											for (const i of cards) {
												if (i.name == 'tao') {
													return [1, 5];
												}
											}
											if (get.value(cards, target) >= 7 + target.getDamagedHp()) {
												return [1, 3];
											}
											return [1, 0.6];
										}
									},
								},
							},
						},
						lqcy_jianxiong2: {
							forced: true,
							charlotte: true, //夏洛特
						},
						lqcy_cidong: {
							audio: 'ext:浪琴天阙/audio:2',
							enable: 'phaseUse',
							usable: 1,
							content() {
								'step 0';
								game.throwCoin();
								('step 1');
								event.card = get.cardPile('qibaodao', 'field');
								if (!event.card) {
									if (!lib.inpile.includes('qibaodao')) {
										lib.inpile.push('qibaodao');
									}
									event.card = game.createCard2('qibaodao', 'spade', 6);
								}
								if (result.bool) {
									player.chooseUseTarget(true, event.card, 'nothrow', 'nopopup');
									const next = player.chooseUseTarget('sha', '刺董:你可以视为使用一张不能被闪避的【杀】', false, 'nodistance');
									next.set('oncard', function () {
										_status.event.directHit.addArray(game.filterPlayer());
									});
									event.finish();
								} else {
									player.chooseTarget(true, '刺董:令一名其他角色视为对你使用牌.', function (card, player, target) {
										return player != target;
									}).ai = function (target) {
										return get.damageEffect(player, target, target);
									};
								}
								('step 2');
								if (result.targets?.length) {
									let a = result.targets[0],
										b = ['nanman', 'wanjian', 'juedou', 'sha'],
										c = game.players.slice(0);
									c.remove(a);
									for (let i = 0; i < b.length; i++) {
										if (!a.canUse(b[i], player, false)) {
											b.splice(i--, 1);
										}
									}
									if (!b.length) {
										return;
									}
									const name = b.randomGet();
									if (name == 'nanman' || name == 'wanjian') {
										for (let i = 0; i < c.length; i++) {
											if (!a.canUse(name, c[i], false)) {
												c.splice(i--, 1);
											}
										}
									} else {
										c = player;
									}
									const card = a.useCard({ name: name }, c, 'noai');
									event.card = card.card;
								}
							},
						},
						lqcy_hujia: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: { player: 'damageEnd' },
							zhuSkill: true,
							forced: true,
							filter(event, player) {
								return (
									!player.hasSkill('lqcy_hujia_used') &&
									game.hasPlayer((current) => {
										return current != player && current.group == 'wei' && player.hasZhuSkill('lqcy_hujia', current);
									})
								);
							},
							content() {
								'step 0';
								player
									.chooseTarget(get.prompt('lqcy_hujia'), '对一名其他魏势力角色造成一点伤害,你回复一点体力.', (card, player, target) => {
										return target != player && target.group == 'wei' && player.hasZhuSkill('lqcy_hujia', target);
									})
									.set('ai', (target) => {
										const player = _status.event.player,
											evt = _status.event.getTrigger();
										return get.damageEffect(target, evt.source, player, evt.nature);
									});
								('step 1');
								if (result.targets?.length) {
									const target = result.targets[0];
									player.addTempSkill('lqcy_hujia_used', 'roundStart');
									target.damage(trigger.source, trigger.nature);
									player.recover();
								}
							},
							ai: {
								maixie_defend: true,
								effect: {
									target(card, player, target) {
										if (player.hasSkillTag('jueqing', false, target)) {
											return;
										}
										if (
											get.tag(card, 'damage') &&
											!target.hasSkill('lqcy_hujia_used') &&
											game.hasPlayer((current) => {
												return current != target && current.group == 'wei' && target.hasZhuSkill('lqcy_hujia', current);
											})
										) {
											return 0.8;
										}
									},
								},
								threaten(player, target) {
									if (target.countCards('h') == 0) {
										return 2;
									}
								},
							},
							subSkill: {
								used: { charlotte: true },
							},
						},
						lqcy_tiandu: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: { global: 'judgeEnd' },
							inherit: 'tiandu',
						},
						lqcy_yiji: {
							inherit: 'new_reyiji',
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								global: 'damageEnd',
							},
							filter(event, player) {
								return player == event.player ? event.num > 0 : !player.hasSkill('lqcy_yiji2');
							},
							content() {
								'step 0';
								event.count = Math.min(trigger.num, 9);
								if (trigger.player != player) {
									player.addTempSkill('lqcy_yiji2');
									event.count = 1;
								}
								('step 1');
								player.draw(Math.max(2, player.maxHp - player.hp)); //最小
								('step 2');
								if (Array.isArray(result) && result.length) {
									const a = [];
									for (const i of result) {
										a.add(get.type2(i));
									}
									player.chooseTarget([1, a.length], '遗计:令最多' + a.length + '名角色各摸一张牌.').ai = function (target) {
										return get.attitude(player, target);
									};
								} else {
									event.goto(4);
								}
								('step 3');
								if (result.targets?.length) {
									for (const i of result.targets) {
										i.draw();
									}
								}
								('step 4');
								event.count--;
								player.chooseCardTarget({
									filterCard(card, player) {
										return get.itemtype(card) == 'card';
									},
									filterTarget: lib.filter.notMe,
									selectCard: [1, 2],
									selectTarget(card) {
										if (ui.selected.cards.length > 1) {
											return [1, 2];
										}
										return 1;
									},
									prompt: '请选择要分配的卡牌和目标',
									prompt2: '选择一至二张牌并选择一名角色,则选择的角色获得你选择的牌.选择两张牌并选择两名角色,则选择的第一名角色获得你选择的第一张牌,选择的第二名角色获得你选择的第二张牌.',
									ai1(card) {
										if (!ui.selected.cards.length) {
											return 1;
										}
										return [0, 1].randomGet();
									},
									ai2(target) {
										const player = _status.event.player,
											card = ui.selected.cards[0];
										const val = target.getUseValue(card);
										if (val > 0) {
											return val * get.attitude(player, target) * 2;
										}
										return get.value(card, target) * get.attitude(player, target);
									},
								});
								('step 5');
								if (result.targets?.length) {
									if (result.targets[1]) {
										result.targets[0].gain(result.cards[0], 'gain2');
										result.targets[1].gain(result.cards[1], 'gain2');
									} else {
										result.targets[0].gain(result.cards, 'gain2');
									}
								}
								('step 6');
								if (event.count > 0) {
									player.chooseBool(get.prompt2('lqcy_yiji'));
								} else {
									event.finish();
								}
								('step 7');
								if (result.bool) {
									event.goto(1);
								}
							},
						},
						lqcy_yiji2: {
							forced: true,
							charlotte: true, //夏洛特
						},
						lqcy_yingcai: {
							ai: {
								order: 20,
								result: {
									player: 1,
								},
							},
							enable: 'phaseUse',
							usable: 1,
							audio: 'ext:浪琴天阙/audio:2',
							content() {
								'step 0';
								game.throwCoin();
								('step 1');
								if (result.bool) {
									const a = [],
										b = [],
										c = [],
										d = [];
									for (const i in lib.character) {
										if ((game.me == player && lib.config.extension_浪琴天阙_lqtq_shaiji_kuochong) || !lib.config.forbidai.includes(i)) {
											a.push(i);
										}
									}
									a.randomSort();
									for (const l of a) {
										for (const j of lib.character[l][3].slice(0).slice(0)) {
											if (
												['xinfu_zhenyi', 'lqtq_zongling', 'jilue', 'shiroha_jiezhao', 'midao'].includes(j) ||
												j.includes('rewrite') ||
												b.includes(j) ||
												!lib.translate[j] ||
												!lib.translate[j + '_info'] ||
												game.hasPlayer(function (current) {
													return current.hasSkill(j);
												})
											) {
												continue;
											}
											const str = lib.translate[j + '_info'];
											for (let i = 0; i < str.length; i++) {
												if (str[i] == '判' && str.substring(i, i + 6) == '判定牌生效前') {
													c.push(j);
												}
												if ((str[i] == '一' && str.substring(i, i + 4) == '一次判定') || (str[i] == '进' && str.substring(i, i + 4) == '进行判定')) {
													d.push(j);
												}
											}
										}
									}
									if (c.length) {
										player.addTempSkill(c[0], { player: 'phaseBefore' });
										player.popup(c[0]);
									}
									if (d.length) {
										player.addTempSkill(d[0], { player: 'phaseBefore' });
										player.popup(d[0]);
									}
								} else if (player.maxHp < 6) {
									player.gainMaxHp();
								} else {
									player.loseMaxHp();
								}
							},
						},
						lqcy_xunxun: {
							audio: 'xunxun',
							trigger: { player: 'phaseBegin' },
							preHidden: true,
							content() {
								'step 0';
								const cards = get.bottomCards(6);
								game.cardsGotoOrdering(cards);
								const next = player.chooseToMove('恂恂:选择两张牌获得', true);
								next.set('list', [['牌堆顶', cards], ['获得']]);
								next.set('filterMove', function (from, to, moved) {
									if (to == 1 && moved[1].length >= 2) {
										return false;
									}
									return true;
								});
								next.set('filterOk', function (moved) {
									return moved[1].length == 2;
								});
								next.set('processAI', function (list) {
									const cards = list[0][1].slice(0).sort(function (a, b) {
										return get.value(b) - get.value(a);
									});
									return [cards, cards.splice(2)];
								});
								('step 1');
								const top = result.moved[0];
								top.reverse();

								for (const i of top) {
									ui.cardPile.insertBefore(i, ui.cardPile.firstChild);
								}

								game.updateRoundNumber();
								player.gain(result.moved[1], 'gain2');
							},
							audio: 'ext:浪琴天阙/audio:2',
						},
						lqcy_wangxi: {
							audio: 'wangxi',
							trigger: { global: 'damageEnd' },
							preHidden: true,
							content() {
								'step 0';
								event.count = Math.min(trigger.num, 9);
								('step 1');
								player.draw(3);
								event.count--;
								('step 2');
								event.target = trigger.player;
								var cards = player.getCards('he');
								if (event.target && event.target != player && cards.length && event.target.isAlive()) {
									if (cards.length == 1) {
										event._result = { bool: true, cards: cards };
									} else {
										player.chooseCard('he', '忘隙:交给事件玩家' + get.translation(event.target) + '一张牌', true);
									}
								} else {
									event.goto(4);
								}
								('step 3');
								if (result.cards?.length) {
									player.give(result.cards, event.target);
								}
								('step 4');
								event.target = trigger.source;
								var cards = player.getCards('he');
								if (event.target && event.target != player && cards.length && event.target.isAlive()) {
									if (cards.length == 1) {
										event._result = { bool: true, cards: cards };
									} else {
										player.chooseCard('he', '忘隙:交给事件来源' + get.translation(event.target) + '一张牌', true);
									}
								} else {
									event.goto(6);
								}
								('step 5');
								if (result.cards?.length) {
									player.give(result.cards, event.target);
								}
								('step 6');
								if (event.count) {
									player.chooseBool(get.prompt2('lqcy_wangxi'));
								} else {
									event.finish();
								}
								('step 7');
								if (result.bool) {
									event.goto(1);
								}
							},
							audio: 'ext:浪琴天阙/audio:2',
							ai: {
								maixie: true,
								maixie_hp: true,
							},
						},
						//出牌阶段限一次,你可以投一次硬币,若为正面,你直到下次你的回合开始前获得技能〖伪伤〗,否则你视为受到一点伤害
						lqcy_shengquan: {
							derivation: ['lqtq_weishang', 'lqtq_weishang_zhu'],
							enable: 'phaseUse',
							usable: 1,
							audio: 'ext:浪琴天阙/audio:2',
							content() {
								'step 0';
								game.throwCoin();
								('step 1');
								if (result.bool) {
									player.addTempSkill('lqtq_weishang', { player: 'phaseBefore' });
								} else {
									player.damage(player).unreal = true;
								} //QQQ
							},
							ai: {
								order: 19,
								result: {
									player: 1,
								},
							},
						},
						lqcy_guose: {
							audio: 'ext:浪琴天阙/audio:2',
							enable: 'phaseUse',
							usable: 1,
							filterTarget(card, player, target) {
								return target != player && target.countGainableCards(player, 'hej') > 0;
							},
							selectCard: [1, Infinity],
							filterCard: true,
							position: 'he',
							complexSelect: true,
							complexCard: true,
							complexTarget: true,
							selectTarget() {
								return [ui.selected.cards.length, ui.selected.cards.length];
							},
							line: {
								color: [239, 204, 96],
							},
							content() {
								if (target.countGainableCards(player, 'hej') > 0) {
									player.gainPlayerCard(true, target, 'hej');
								}
							},
							ai: {
								order: 6,
								result: {
									target(player, target) {
										return lib.card.shunshou.ai.result.target.apply(this, arguments);
									},
									player(player, target) {
										if (target.sameSexAs(player)) {
											return 0;
										}

										for (const i of ui.selected.targets) {
											if (i.differentSexFrom(player)) {
												return 0;
											}
										}

										return get.attitude(player, target) < 0 && target.countCards('h', 'tao') > 0 ? 1 : -2;
									},
								},
							},
							group: 'lqcy_guose_le',
							subSkill: {
								le: {
									trigger: {
										player: 'loseAfter',
										global: ['equipAfter', 'addJudgeAfter', 'gainAfter', 'loseAsyncAfter', 'addToExpansionAfter'],
									},
									filter(event, player) {
										if (event.parent.name == 'useCard') {
											return false;
										}
										const evt = event.getl(player);

										for (const i of evt.cards2) {
											if (get.color(i, evt.hs.includes(i) ? evt.player : false) == 'red' && get.position(i) == 'd') {
												return true;
											}
										}

										return false;
									},
									audio: 'lqcy_guose',
									forced: true,
									content() {
										'step 0';
										event.b = [];
										const evt = trigger.getl(player);

										for (const i of evt.cards2) {
											if (get.color(i, evt.hs.includes(i) ? evt.player : false) == 'red' && get.position(i) == 'd') {
												event.b.push(i);
											}
										}

										if (!event.b.length) {
											event.finish();
										}
										game.log(event.b);
										('step 1');
										if (event.b.length) {
											player
												.chooseTarget(get.prompt('lqcy_guose'), '将' + get.translation(event.b) + (event.b.length > 1 ? '中的一张牌' : '') + '当做【乐不思蜀】对一名其他角色使用.', function (card, player, target) {
													for (const i of event.b) {
														if (player.canUse({ name: 'lebu', cards: [i] }, target, false)) {
															return true;
														}
													}
													return false;
												})
												.set('ai', function (target) {
													const player = _status.event.player;
													return get.effect(target, { name: 'lebu' }, player, player);
												})
												.set('cards', cards);
										} else {
											event.finish();
										}
										('step 2');
										if (result.targets?.length) {
											event.a = result.targets[0];
											if (event.b.length == 1) {
												player.line(event.a);
												player.useCard({ name: 'lebu' }, event.a, [event.b[0]], 'lqcy_guose_le').animate = false;
												event.finish();
											} else {
												player
													.chooseCardButton('选择一张牌当作乐不思蜀使用', event.b, true)
													.set('filterButton', function (button) {
														return player.canUse({ name: 'lebu', cards: [button.link] }, _status.event.target, false);
													})
													.set('target', event.a);
											}
										} else {
											event.finish();
										}
										('step 3');
										if (result.links?.length) {
											const card = result.links[0];
											event.b.remove(card);
											player.line(event.a);
											player.useCard({ name: 'lebu' }, event.a, [card], 'lqcy_guose_le').animate = false;
											event.goto(1);
										}
									},
								},
							},
						},
						lqcy_liuli: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: { target: 'useCardToTarget' },
							forced: true,
							filter(event, player) {
								return (
									player.countCards('he') > 0 &&
									game.hasPlayer(function (current) {
										return current != event.player && current != player && lib.filter.targetEnabled(event.card, event.player, current);
									})
								);
							},
							content() {
								'step 0';
								const next = player
									.chooseCardTarget({
										position: 'he',
										filterCard: lib.filter.cardDiscardable,
										filterTarget(card, player, target) {
											const trigger = _status.event;
											return target != player && lib.filter.targetEnabled(trigger.card, trigger.source, target);
										},
										ai1(card) {
											const player = _status.event.player,
												trigger = _status.event.getTrigger();
											if (get.effect(player, trigger.card, trigger.player, player) >= 0) {
												return false;
											}
											return get.unuseful(card) + 9;
										},
										ai2(target) {
											const player = _status.event.player,
												trigger = _status.event.getTrigger();
											if (get.effect(player, trigger.card, trigger.player, player) >= 0) {
												return false;
											}
											return -get.attitude(target, player);
										},
										prompt: get.prompt('lqcy_liuli'),
										prompt2: '弃置一张牌,将' + get.translation(trigger.card) + '转移给一名其他角色.',
										source: trigger.player,
										card: trigger.card,
									})
									.setHiddenSkill(event.name);
								('step 1');
								if (result.targets?.length) {
									const target = result.targets[0];
									player.discard(result.cards);
									const evt = trigger.parent;
									evt.triggeredTargets2.remove(player);
									evt.targets.remove(player);
									evt.targets.push(target);
								}
							},
						},
						lqcy_yanxiao: {
							enable: 'phaseUse',
							usable: 1,
							audio: 'ext:浪琴天阙/audio:2',
							content() {
								'step 0';
								game.throwCoin();
								('step 1');
								event.a = 0;
								for (const i of game.players) {
									event.a += i.countCards('j');
								}
								if (result.bool) {
									player.addTempSkill('lqcy_yanxiao2', { player: 'phaseBefore' });
									event.finish();
								} else if (event.a > 0) {
									player.chooseTarget('言笑:可以令一名角色各摸' + event.a + '张牌.').ai = function (target) {
										return get.attitude(player, target);
									};
								} else {
									event.finish();
								}
								('step 2');
								if (result.targets?.length) {
									result.targets[0].draw(event.a);
								}
							},
							ai: {
								order: 1,
								result: {
									player: 1,
								},
							},
						},
						lqcy_yanxiao2: {
							trigger: {
								global: ['phaseUseCancelled', 'phaseUseSkipped'],
							},
							forced: true,
							filter(event, player) {
								return player.hasSkill('lqcy_yanxiao') && event.player != player && event.player.countCards('h') > event.player.getHandcardLimit() && event.player.countGainableCards(player, 'h') > 0;
							},
							content() {
								const a = trigger.player.countCards('h') - trigger.player.getHandcardLimit();
								player.gainPlayerCard('言笑:获得' + get.translation(trigger.player) + '最多' + a + '张手牌.', trigger.player, get.buttonValue, 'h', [1, a]);
							},
						},
						lqtqx_liegong: {
							mod: {
								targetInRange(card, player, target) {
									if (card.name == 'sha' && typeof card.number == 'number') {
										if (get.distance(player, target) <= card.number) {
											return true;
										}
									}
								},
							},
							intro: {
								content: '已记录花色:$',
							},
							trigger: {
								source: 'damageBegin2',
							},
							filter(event, player) {
								return event.player != player && player.getStorage('lqtqx_liegong').length;
							},
							check(event, player) {
								return get.attitude(player, event.player) < 0 && player.getStorage('lqtqx_liegong').length > 2;
							},
							audio: 'ext:浪琴天阙/audio:1',
							content() {
								'step 0';
								let count = player.getStorage('lqtqx_liegong').length;
								if (count > 3) {
									count = 3;
								}
								trigger.num += count;
								game.log(trigger.player, '此次受到的伤害+', count);
								event.cards = get.cards(player.getStorage('lqtqx_liegong').length);
								const next = player.chooseCardButton(event.cards, '选择要获得的牌', [0, player.getStorage('lqtqx_liegong').length], true).set('filterButton', function (button) {
									for (const i of _status.event.player.getCards('h')) {
										if (i.suit == button.link.suit) {
											return false;
										}
									}
									return true;
								});
								player.unmarkSkill('lqtqx_liegong');
								('step 1');
								if (result.links?.length) {
									player.gain(result.links, 'log', 'gain2');
								}
							},
							group: ['lqtqx_liegong_mark'],
							subSkill: {
								mark: {
									trigger: {
										player: ['useCard1', 'respond'],
										target: 'useCardToTargeted',
									},
									filter(event, player, name) {
										return event.cards && event.cards.length == 1 && event.card.suit && !player.getStorage('lqtqx_liegong').includes(event.card.suit);
									}, //QQQ
									forced: true,
									audio: 'lqtqx_liegong',
									content() {
										if (trigger.cards.length == 1 && trigger.card.suit && !player.getStorage('lqtqx_liegong').includes(trigger.card.suit)) {
											player.markAuto('lqtqx_liegong', [trigger.card.suit]);
											game.log(player, '记录了', trigger.card.suit);
										}
										if (event.triggername == 'useCard1' && player.getStorage('lqtqx_liegong').length > 3) {
											trigger.directHit.addArray(
												game.filterPlayer(function (current) {
													return current != player;
												}),
											);
											player.addTempSkill('unequip', { player: 'useCardAfter' });
											game.log(trigger.card, '不能被其他角色响应,且无视防具');
										}
									},
								},
							},
						},
						_lqtqx_liegong_nocard: {
							charlotte: true,
							mod: {
								cardEnabled(card, player) {
									if (
										game.hasPlayer(function (current) {
											return current == _status.currentPhase && current != player && current.hasSkill('lqtqx_liegong') && current.getStorage('lqtqx_liegong').includes(card.suit);
										})
									) {
										return false;
									}
								},
								cardRespondable(card, player) {
									if (
										game.hasPlayer(function (current) {
											return current == _status.currentPhase && current != player && current.hasSkill('lqtqx_liegong') && current.getStorage('lqtqx_liegong').includes(card.suit);
										})
									) {
										return false;
									}
								},
							},
						},
						lqtqx_yingba: {
							audio: 'yingba',
							enable: 'phaseUse',
							usable: 1,
							filter: (event, player) => game.hasPlayer((current) => current != player),
							filterTarget: (card, player, target) => target != player,
							selectTarget: [1, 3],
							content() {
								'step 0';
								target.addMark('yingba_mark');
								target.loseMaxHp();
								('step 1');
								if (!target.isIn()) {
									player.loseMaxHp(2);
								} else if (target.countMark('yingba_mark') > 1) {
									player.draw();
								}
							},
							mod: {
								targetInRange(card, player, target) {
									if (target.hasMark('yingba_mark')) {
										return true;
									}
								},
							},
							ai: {
								combo: 'lqtqx_fuhai',
								threaten: 3,
								order: 12,
								result: {
									target(player, target) {
										if (target.isHealthy()) {
											return -2;
										}
										return -1;
									},
								},
							},
						},
						lqtqx_fuhai: {
							audio: 'scfuhai',
							forced: true,
							trigger: { player: 'useCard' },
							filter(event, player) {
								return (
									event.card &&
									(get.type(event.card) == 'trick' || event.card.name == 'sha') &&
									game.hasPlayer(function (current) {
										return current.hasMark('yingba_mark');
									})
								);
							},
							content() {
								trigger.directHit.addArray(
									game.filterPlayer(function (current) {
										return current.hasMark('yingba_mark');
									}),
								);
							},
							group: ['lqtqx_fuhai_die', 'lqtqx_fuhai_draw'],
							ai: {
								directHit_ai: true,
								skillTagFilter(player, tag, arg) {
									return arg && arg.target && arg.target.hasMark('yingba_mark');
								},
							},
							subSkill: {
								draw: {
									trigger: { player: 'useCardToPlayered' },
									forced: true,
									filter(event, player) {
										return event.target.hasMark('yingba_mark') && event.card && (get.type2(event.card) == 'trick' || event.card.name == 'sha');
									},
									logTarget: 'target',
									content() {
										'step 0';
										if (trigger.card.name == 'sha') {
											trigger.addCount = false;
											if (player.stat[player.stat.length - 1].card.sha > 0) {
												player.stat[player.stat.length - 1].card.sha--;
											}
										} else {
											player.draw();
										}
									},
								},
								die: {
									trigger: { global: 'die' },
									forced: true,
									audio: 'scfuhai',
									filter(event, player) {
										return event.player.countMark('yingba_mark') > 0;
									},
									content() {
										player.gainMaxHp(trigger.player.countMark('yingba_mark'));
										player.draw(trigger.player.countMark('yingba_mark'));
									},
								},
							},
						},
						lqtqx_pinghe: {
							audio: 'pinghe',
							mod: {
								maxHandcardBase(player) {
									return player.getDamagedHp();
								},
							},
							trigger: { player: 'damageBegin2' },
							forced: true,
							filter(event, player) {
								return player.maxHp > event.num || player.countCards('h') > 0;
							},
							content() {
								'step 0';
								if (player.countCards('h') > 0) {
									player.chooseCardTarget({
										prompt: '请选择【冯河】的牌和目标',
										prompt2: '将一张手牌交给一名其他角色或弃置并防止伤害' + (trigger.source ? ',令伤害来源获得一个<平定>标记' : ''),
										filterCard: true,
										selectTarget: [0, 1],
										forced: true,
										filterTarget: lib.filter.notMe, //不等于玩家
										ai1(card) {
											return -get.value(card);
										},
										ai2(target) {
											return -get.attitude(_status.event.player, target);
										},
									});
								}
								('step 1');
								if (result.targets?.length) {
									const target = result.targets[0];
									if (target) {
										player.line(target, 'green');
										target.gain(result.cards, player, 'giveAuto');
									} else {
										player.discard(result.cards);
									}
									trigger.cancel();
									if (player.maxHp > 1) {
										player.loseMaxHp();
									}
									if (player.hasSkill('lqtqx_yingba') && trigger.source && trigger.source != player) {
										trigger.source.addMark('yingba_mark', 1);
									}
								} else if (player.maxHp > trigger.num) {
									player.loseMaxHp(trigger.num);
									trigger.cancel();
									if (player.hasSkill('lqtqx_yingba') && trigger.source && trigger.source != player) {
										trigger.source.addMark('yingba_mark', 1);
									}
								}
							},
							group: 'lqtqx_pinghe_recover',
							subSkill: {
								recover: {
									trigger: { player: 'recoverBegin' },
									audio: 'pinghe',
									forced: true,
									filter(event, player) {
										return player.hp > 0;
									},
									content() {
										trigger.cancel();
										player.gainMaxHp(trigger.num);
									},
								},
							},
						},
						lqtqx_huishi: {
							audio: 'shuishi',
							trigger: {
								player: 'damageEnd',
							},
							forced: true,
							content() {
								'step 0';
								event.cards = [];
								event.suits = [];
								('step 1');
								player
									.judge(function (result) {
										const evt = _status.event.getParent('lqtqx_huishi');
										if (evt && evt.suits && evt.suits.includes(result.suit)) {
											return 0;
										}
										return 1;
									})
									.set('callback', function () {
										event.parent.orderingCards.remove(event.judgeResult.card);
									}).judge2 = function (result) {
										return result.bool ? true : false;
									};
								('step 2');
								event.cards.push(result.card);
								if (result.bool) {
									event.suits.push(result.suit);
									player.gainMaxHp();
									event.goto(1);
								} else {
									cards = cards.filterInD();
									if (cards.length) {
										player.chooseTarget('将' + get.translation(cards) + '交给一名角色', true).set('ai', function (target) {
											const player = _status.event.player;
											let att = get.attitude(player, target) / Math.sqrt(1 + target.countCards('h'));
											if (target.hasSkillTag('nogain')) {
												att /= 10;
											}
											return att;
										});
									} else {
										event.finish();
									}
								}
								('step 3');
								if (result.targets?.length) {
									const target = result.targets[0];
									event.target = target;
									player.line(target, 'green');
									target.gain(cards, 'gain2');
								}
							},
							group: 'lqtqx_huishi_phase',
							subSkill: {
								phase: {
									audio: 'shuishi',
									enable: 'phaseUse',
									usable: 1,
									content() {
										'step 0';
										event.cards = [];
										event.suits = [];
										('step 1');
										player
											.judge(function (result) {
												const evt = _status.event.getParent('lqtqx_huishi_phase');
												if (evt && evt.suits && evt.suits.includes(result.suit)) {
													return 0;
												}
												return 1;
											})
											.set('callback', function () {
												event.parent.orderingCards.remove(event.judgeResult.card);
											}).judge2 = function (result) {
												return result.bool ? true : false;
											};
										('step 2');
										event.cards.push(result.card);
										if (result.bool) {
											event.suits.push(result.suit);
											player.gainMaxHp();
											event.goto(1);
										} else {
											cards = cards.filterInD();
											if (cards.length) {
												player.chooseTarget('将' + get.translation(cards) + '交给一名角色', true).set('ai', function (target) {
													const player = _status.event.player;
													let att = get.attitude(player, target) / Math.sqrt(1 + target.countCards('h'));
													if (target.hasSkillTag('nogain')) {
														att /= 10;
													}
													return att;
												});
											} else {
												event.finish();
											}
										}
										('step 3');
										if (result.targets?.length) {
											const target = result.targets[0];
											event.target = target;
											player.line(target, 'green');
											target.gain(cards, 'gain2');
										}
									},
									ai: {
										order: 1,
										result: {
											player: 1,
										},
									},
								},
							},
						},
						lqtqx_tianyi: {
							audio: 'stianyi',
							trigger: { player: 'phaseZhunbeiBegin' },
							forced: true,
							juexingji: true,
							filter(event, player) {
								return !game.hasPlayer(function (current) {
									return current.getAllHistory('damage').length == 0;
								});
							},
							content() {
								'step 0';
								player.awakenSkill('lqtqx_tianyi');
								player.gainMaxHp(2);
								player.recover();
								('step 1');
								player
									.chooseTarget(true, '令一名角色获得技能〖佐幸〗', function (card, player, target) {
										return !target.hasSkill('lqtqx_zuoxing');
									})
									.set('ai', function (target) {
										return get.attitude(_status.event.player, target);
									});
								('step 2');
								if (result.targets?.length) {
									const target = result.targets[0];
									player.line(target, 'green');
									target.addSkill('lqtqx_zuoxing');
								}
							},
						},
						lqtqx_zuoxing: {
							enable: 'phaseUse',
							audio: 'zuoxing',
							usable: 1,
							filter(event, player) {
								for (const i of lib.inpile) {
									if (['trick', 'basic'].includes(get.type(i)) && event.filterCard && event.filterCard({ name: i }, player, event)) {
										return true;
									}
								}
								return false;
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (name == 'sha') {
											if (event.filterCard && event.filterCard({ name: name }, player, event)) {
												list.push(['基本', '', 'sha']);
											}
											for (const j of lib.inpile_nature) {
												if (event.filterCard && event.filterCard({ name: name, nature: j }, player, event)) {
													list.push(['基本', '', 'sha', j]);
												}
											}
										} else if (get.type(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										} else if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['基本', '', name]);
										}
									}

									return ui.create.dialog('佐幸', [list, 'vcard']);
								},
								check(button) {
									return _status.event.player.getUseValue({ name: button.link[2] });
								},
								backup(links, player) {
									return {
										viewAs: {
											name: links[0][2],
										},
										filterCard: () => false,
										selectCard: -1,
										popname: true,
										precontent() { },
									};
								},
								prompt(links, player) {
									return '请选择' + get.translation(links[0][2]) + '的目标';
								},
							},
							ai: {
								order: 1,
								result: {
									player: 1,
								},
							},
						},
						lqtqx_shuishi: {
							audio: 'sghuishi',
							enable: 'phaseUse',
							limited: true,
							filterTarget: true,
							content() {
								'step 0';
								player.awakenSkill('lqtqx_shuishi');
								const list = target.getSkills(null, false, false).filter(function (skill) {
									const info = lib.skill[skill];
									return info && info.juexingji;
								});
								if (list.length) {
									target.addMark('lqtqx_shuishi', 1, false);
									for (const i of list) {
										const info = lib.skill[i];
										if (info.filter && !info.charlotte && !info.lqtqx_shuishi_filter) {
											info.lqtqx_shuishi_filter = info.filter;
											info.filter = function (event, player) {
												if (player.hasMark('lqtqx_shuishi')) {
													return true;
												}
												return this.lqtqx_shuishi_filter.apply(this, arguments);
											};
										}
									}
								}
								target.draw(4);
							},
							intro: { content: '发动觉醒技时无视条件' },
							ai: {
								order: 0.1,
								expose: 0.2,
								result: {
									target(player, target) {
										if (player.hasUnknown() || player.maxHp < 5) {
											return 0;
										}
										const list = target.getSkills(null, false, false).filter(function (skill) {
											const info = lib.skill[skill];
											return info && info.juexingji;
										});
										if (list.length || target.hasJudge('lebu') || target.hasSkillTag('nogain')) {
											return 0;
										}
										return 4;
									},
								},
							},
						},
						lqtqx_quanji: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								global: 'gainAfter',
								player: ['damageEnd', 'phaseUseEnd'],
								source: 'damageSource',
							},
							forced: true,
							notemp: true,
							filter(event, player, name) {
								if (name == 'damageEnd') {
									return event.num > 0;
								}
								if (name == 'phaseUseEnd') {
									return player.countCards('h') > player.hp;
								}
								var evt = event.parent;
								if (name == 'damageSource') {
									return event.card && evt && evt.card == event.card && evt.type == 'card' && evt.targets && evt.targets.length == 1;
								}
								if (player == event.player) {
									return false;
								}
								var evt = event.getl(player);
								return evt && evt.cards2 && evt.cards2.length;
							},
							content() {
								'step 0';
								event.count = trigger.num || 1;
								('step 1');
								event.count--;
								player.draw(2);
								('step 2');
								if (player.countCards('he')) {
									player.chooseCard([1, 2], 'he', '将一至两张牌置于武将牌上作为<权>', true);
								} else {
									event.goto(4);
								}
								('step 3');
								if (result.cards?.length) {
									player.addToExpansion(result.cards, player, 'giveAuto').gaintag.add('quanji');
								}
								('step 4');
								if (event.count > 0) {
									player.chooseBool(get.prompt2('lqtqx_quanji')).set('frequentSkill', 'lqtqx_quanji');
								} else {
									event.finish();
								}
								('step 5');
								if (result.bool) {
									event.goto(1);
								}
							},
							mod: {
								maxHandcard(player, num) {
									return num + player.getExpansions('quanji').length;
								},
							},
							onremove(player, skill) {
								const cards = player.getExpansions('quanji');
								if (cards.length) {
									player.loseToDiscardpile(cards);
								}
							},
							ai: {
								maixie: true,
								maixie_hp: true,
								threaten: 0.8,
								effect: {
									target(card, player, target) {
										if (get.tag(card, 'damage')) {
											if (player.hasSkillTag('jueqing', false, target)) {
												return [1, -2];
											}
											if (!target.hasFriend()) {
												return;
											}
											if (target.hp >= 4) {
												return [0.5, get.tag(card, 'damage') * 2];
											}
											if (!target.hasSkill('lqtqx_paiyi') && target.hp > 1) {
												return [0.5, get.tag(card, 'damage') * 1.5];
											}
											if (target.hp == 3) {
												return [0.5, get.tag(card, 'damage') * 1.5];
											}
											if (target.hp == 2) {
												return [1, get.tag(card, 'damage') * 0.5];
											}
										}
									},
								},
							},
						},
						lqtqx_zili: {
							audio: 'ext:浪琴天阙/audio:2',
							derivation: 'lqtqx_paiyi',
							trigger: { player: 'phaseZhunbeiBegin' },
							forced: true,
							juexingji: true,
							filter(event, player) {
								return player.getExpansions('quanji').length > 4;
							},
							content() {
								player.awakenSkill('lqtqx_zili');
								player.recover();
								player.draw(2);
								player.addSkill('lqtqx_paiyi');
							},
						},
						lqtqx_paiyi: {
							audio: 'ext:浪琴天阙/audio:2',
							enable: 'phaseUse',
							init(player) {
								if (!player.storage.lqtqx_paiyi) {
									player.storage.lqtqx_paiyi1 = [];
								}
							},
							filter(event, player) {
								return player.getExpansions('quanji').length && player.storage.lqtqx_paiyi1.length < 4;
							},
							chooseButton: {
								check(button) {
									if (typeof button.link == 'object') {
										return 1;
									}
									const player = _status.event.player,
										num = player.getExpansions('quanji').length - 1;
									if (button.link == 3) {
										if (
											game.countPlayer(function (current) {
												return get.damageEffect(current, player, player) > 0;
											}) < num
										) {
											return 0.5;
										}
										return 2;
									}
									return 1;
								},
								dialog(event, player) {
									const dialog = ui.create.dialog('权计', 'hidden');
									const table = document.createElement('div');
									table.classList.add('add-setting');
									table.style.margin = '0';
									table.style.width = '100%';
									table.style.position = 'relative';
									const list = ['摸牌', '弃置牌', '获得牌', '造成伤害'];
									for (let i = 0; i < list.length; i++) {
										if (player.storage.lqtqx_paiyi1.includes(i)) {
											continue;
										}
										const td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
										td.innerHTML = '<span>' + list[i] + '</span>';
										td.link = i;
										td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
										Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
										table.appendChild(td);
										dialog.buttons.add(td);
									}
									dialog.content.appendChild(table);
									dialog.add(player.getExpansions('quanji'));
									return dialog;
								},
								select: 2,
								filter(button, player) {
									if (ui.selected.buttons.length) {
										return typeof ui.selected.buttons[0].link != typeof button.link;
									}
									return true;
								},
								backup(links) {
									if (typeof links[0] == 'object') {
										links.reverse();
									}
									const next = get.copy(lib.skill['lqtqx_paiyi_backup' + links[0]]);
									next.card = links[1];
									return next;
								},
								prompt(links, player) {
									if (typeof links[0] == 'object') {
										links.reverse();
									}
									let num = get.cnNumber(Math.max(1, player.getExpansions('quanji').length - 1)),
										card = get.translation(links[1]);
									if (links[0] == 0) {
										((num = get.cnNumber(Math.max(2, player.getExpansions('quanji').length - 1))), (card = get.translation(links[1])));
									}
									if (links[0] == 0) {
										return '移去' + card + '并令一名角色摸' + num + '张牌';
									}
									if (links[0] == 1) {
										return '移去' + card + '并弃置一名角色' + num + '张牌';
									}
									if (links[0] == 2) {
										return '移去' + card + '并令' + num + '名角色分别交给你一张牌';
									}
									return '移去' + card + '并对至多' + num + '名角色造成1点伤害';
								},
							},
							ai: {
								order: 11,
								result: { player: 1 },
							},
							group: 'lqtqx_paiyi_count',
							subSkill: {
								count: {
									trigger: {
										global: ['phaseAfter', 'phaseBefore'],
									},
									forced: true,
									popup: false,
									_priority: 29,
									filter(event, player) {
										return player.storage.lqtqx_paiyi1 && player.storage.lqtqx_paiyi1.length;
									},
									content() {
										player.storage.lqtqx_paiyi1 = [];
									},
								},
								backup0: {
									audio: 'lqtqx_paiyi',
									filterCard: () => false,
									selectCard: -1,
									filterTarget: true,
									content() {
										player.storage.lqtqx_paiyi1.push(0);
										const card = lib.skill.lqtqx_paiyi_backup.card;
										player.loseToDiscardpile(card);
										target.draw(Math.max(2, player.getExpansions('quanji').length - 1));
									},
									ai: {
										result: {
											target(player, target) {
												if (target.hasSkill('nogain')) {
													return 0;
												}
												if (player == target && !player.needsToDiscard()) {
													return 3;
												}
												return 1;
											},
										},
									},
								},
								backup1: {
									audio: 'lqtqx_paiyi',
									filterCard: () => false,
									selectCard: -1,
									filterTarget(card, player, target) {
										return target.countCards('he');
									},
									content() {
										player.storage.lqtqx_paiyi1.push(1);
										const card = lib.skill.lqtqx_paiyi_backup.card,
											num = Math.max(2, player.getExpansions('quanji').length - 1);
										player.loseToDiscardpile(card);
										player.discardPlayerCard(target, 'he', true, num);
									},
									ai: {
										result: {
											target: -1.5,
										},
									},
								},
								backup2: {
									audio: 'lqtqx_paiyi',
									filterCard: () => false,
									selectCard: -1,
									filterTarget(card, player, target) {
										return target.countCards('he');
									},
									delay: false,
									multitarget: true,
									multiline: true,
									selectTarget() {
										return [1, Math.max(1, _status.event.player.getExpansions('quanji').length - 1)];
									},
									content() {
										'step 0';
										targets.sortBySeat();
										player.storage.lqtqx_paiyi1.push(2);
										const card = lib.skill.lqtqx_paiyi_backup.card;
										player.loseToDiscardpile(card);
										event.current = player.next;
										('step 1');
										if (targets.includes(event.current)) {
											event.current.chooseCard('he', '交给' + get.translation(player) + '一张牌', true);
										} else {
											event.goto(3);
										}
										('step 2');
										player.gain(result.cards, event.current, 'giveAuto');
										('step 3');
										event.current = event.current.next;
										if (event.current != player) {
											event.goto(1);
										}
									},
									ai: {
										result: {
											target: -1.5,
										},
									},
								},
								backup3: {
									audio: 'lqtqx_paiyi',
									filterCard: () => false,
									selectCard: -1,
									filterTarget: true,
									delay: false,
									multitarget: true,
									multiline: true,
									selectTarget() {
										return [1, Math.max(1, _status.event.player.getExpansions('quanji').length - 1)];
									},
									content() {
										'step 0';
										targets.sortBySeat();
										player.storage.lqtqx_paiyi1.push(3);
										const card = lib.skill.lqtqx_paiyi_backup.card;
										player.loseToDiscardpile(card);
										('step 1');
										for (const i of targets) {
											i.damage();
										}
									},
									ai: {
										tag: {
											damage: 1,
										},
										result: {
											target: -1.5,
										},
									},
								},
							},
						},
						lqtqx_tuntian: {
							audio: 'ext:浪琴天阙/audio:2',
							init(player) {
								if (!player.storage.tuntian) {
									player.storage.tuntian = [];
								}
							},
							trigger: {
								player: ['loseAfter', 'judgeEnd'],
								global: ['equipAfter', 'addJudgeAfter', 'gainAfter', 'loseAsyncAfter', 'addToExpansionAfter'],
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'judgeEnd') {
									return get.position(event.result.card, true) == 'o';
								}
								if (player == _status.currentPhase) {
									return (
										event.name == 'lose' &&
										event.cards2.filter(function (card) {
											return get.name(card, event.hs.includes(card) ? player : false) == 'sha';
										}).length
									);
								}
								if (event.name == 'gain' && event.player == player) {
									return false;
								}
								const evt = event.getl(player);
								return evt && evt.cards2 && evt.cards2.length;
							},
							content() {
								'step 0';
								if (event.triggername == 'judgeEnd') {
									player.addToExpansion(trigger.result.card, player, 'giveAuto').gaintag.add('tuntian');
									event.finish();
								} else {
									player.judge(function (card) {
										if (card.suit == 'heart') {
											return 2;
										}
										return 1;
									});
								}
								('step 1');
								if (result.card.suit == 'heart') {
									player.draw();
								}
							},
							group: 'tuntian_dist',
						},
						lqtqx_zaoxian: {
							audio: 'ext:浪琴天阙/audio:2',
							juexingji: true,
							trigger: { player: 'phaseZhunbeiBegin' },
							forced: true,
							filter(event, player) {
								return player.getExpansions('tuntian').length > 2;
							},
							derivation: ['lqtqx_jixi'],
							content() {
								player.awakenSkill('lqtqx_zaoxian');
								player.recover();
								player.draw(2);
								player.addSkill('lqtqx_jixi');
								player.phase('nodelay');
							},
						},
						lqtqx_ziliang: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								global: 'damageEnd',
							},
							filter(event, player) {
								return event.num > 0 && player.getExpansions('tuntian').length && event.player.isAlive();
							},
							forced: true,
							content() {
								'step 0';
								player.chooseCardButton('###是否发动【资粮】？###将一张<田>交给' + get.translation(trigger.player), player.getExpansions('tuntian')).ai = function (button) {
									if (get.attitude(_status.event.player, trigger.player) < 0) {
										return false;
									}
									if (player.getExpansions('tuntian').length < 4) {
										return false;
									}
									return get.value(button.link);
								};
								('step 1');
								if (result.links?.length) {
									trigger.player.gain(result.links[0], 'draw2');
									trigger.player.draw();
								} else {
									event.finish();
								}
								('step 2');
								((event.count = player), (event.player = trigger.player));
								if (player.countCards('h') < trigger.player.countCards('h')) {
									((event.count = trigger.player), (event.player = player));
								}
								if (event.player != event.count) {
									event.count.chooseCard('he', '你可以将一张牌交给' + get.translation(event.player)).ai = function (card) {
										return true;
									};
								} else {
									event.finish();
								}
								('step 3');
								if (result.cards?.length) {
									event.player.gain(result.cards[0], 'draw2');
								}
							},
						},
						lqtqx_jixi: {
							enable: 'phaseUse',
							audio: 'ext:浪琴天阙/audio:2',
							filter(event, player) {
								for (const i of lib.inpile) {
									if (player.getExpansions('tuntian').length && get.type(i) == 'trick' && event.filterCard && event.filterCard({ name: i }, player, event)) {
										return true;
									}
								}
								return false;
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (get.type(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										}
									}

									return ui.create.dialog('急袭:选择一张牌使用.', [list, 'vcard']);
								},
								check(button) {
									if (_status.event.parent.type != 'phase') {
										return 1;
									}
									const player = _status.event.player;
									return player.getUseValue({
										name: button.link[2],
										nature: button.link[3],
									});
								},
								backup(links, player) {
									return {
										forced: true,
										filterCard() {
											return false;
										},
										selectCard: -1,
										card: links[0],
										delay: false,
										content: lib.skill.lqtqx_jixi.contentx,
										ai: {
											order: 20,
											result: {
												player: 1,
											},
										},
									};
								},
							},
							contentx() {
								'step 0';
								event.a = lib.skill.lqtqx_jixi_backup.card[2];
								const next = player.chooseCardButton('将一张<田>当做' + get.translation(event.a) + '使用.', player.getExpansions('tuntian')).set('filterButton', function (button) {
									return game.hasPlayer(function (current) {
										return _status.event.player.canUse({ name: event.a, cards: [button.link] }, current);
									});
								});
								next.set('ai', function (button) {
									return 20 - get.value(button.link);
								});
								('step 1');
								if (result.links?.length) {
									player.chooseUseTarget({ name: event.a }, result.links, true);
								}
							},
							ai: {
								order: 20,
								combo: 'tuntian',
								result: {
									player: 1,
								},
							},
						},
						lqtqx_pojun: {
							shaRelated: true,
							audio: 'ext:浪琴天阙/audio:2',
							group: 'lqtqx_pojun_1',
							trigger: {
								player: ['useCardToPlayered', 'phaseUseBegin'],
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'phaseUseBegin') {
									return (
										player.countCards('h', function (card) {
											return get.type2(card) == 'trick';
										}) > 0 &&
										game.hasPlayer(function (current) {
											return current != player && current.countCards('he');
										})
									);
								}
								return ['sha', 'juedou'].includes(event.card.name) && event.target.hp > 0 && event.target.countCards('he') > 0;
							},
							content() {
								'step 0';
								if (event.triggername == 'phaseUseBegin') {
									player.chooseCardTarget({
										prompt: '是否发动【破军】？',
										prompt2: '弃置一张锦囊牌,并选择一名目标角色.',
										filterCard(card, player) {
											return get.type2(card) == 'trick';
										},
										filterTarget: lib.filter.notMe,
										ai1(card) {
											return 7 - get.useful(card);
										},
										ai2(target) {
											return -get.attitude(_status.event.player, target);
										},
									});
								} else {
									event.goto(2);
								}
								('step 1');
								if (result.cards?.length) {
									player.discard(result.cards, true);
									event.target = result.targets[0];
									player.line(event.target, 'green');
								} else {
									event.finish();
								}
								('step 2');
								let tru = true;
								if (event.triggername != 'phaseUseBegin') {
									tru = '';
									event.target = trigger.target;
								}
								const next = player.choosePlayerCard(tru, event.target, 'he', [1, Math.min(event.target.hp, event.target.countCards('he'))], get.prompt('lqtqx_pojun', event.target));
								next.set('ai', function (button) {
									if (!_status.event.goon) {
										return 0;
									}
									const val = get.value(button.link);
									if (button.link == _status.event.target.getEquip(2)) {
										return 2 * (val + 3);
									}
									return val;
								});
								next.set('goon', get.attitude(player, event.target) <= 0);
								next.set('forceAuto', true);
								('step 3');
								if (result.bool) {
									target.addSkill('lqtqx_pojun_2');
									target.addToExpansion(result.cards, 'giveAuto', event.target).gaintag.add('lqtqx_pojun_2');
								}
							},
							ai: {
								unequip: true,
								directHit_ai: true,
								skillTagFilter(player, tag, arg) {
									if (get.attitude(player, arg.target) > 0) {
										return false;
									}
									if (tag == 'directHit_ai') {
										return arg.target.hp >= Math.max(1, arg.target.countCards('h') - 1);
									}
									if (arg && arg.name == 'sha' && arg.target.getEquip(2)) {
										return true;
									}
									return false;
								},
							},
							subSkill: {
								1: {
									audio: 'lqtqx_pojun',
									trigger: { source: 'damageBegin4' },
									forced: true,
									logTarget: 'player',
									_priority: -Infinity,
									filter(event, player) {
										const target = event.player;
										return event.card && (event.parent.name == 'sha' || event.card.name == 'juedou') && player.countCards('h') >= target.countCards('h') && player.countCards('e') >= target.countCards('e');
									},
									content() {
										trigger.num += trigger.num;
										game.log(trigger.card, '伤害翻倍');
									},
								},
								2: {
									trigger: { global: 'phaseEnd' },
									forced: true,
									popup: false,
									charlotte: true,
									filter(event, player) {
										return player.getExpansions('lqtqx_pojun_2').length;
									},
									content() {
										'step 0';
										const cards = player.getExpansions('lqtqx_pojun_2');
										player.gain(cards, 'draw');
										game.log(player, '收回了' + get.cnNumber(cards.length) + '张<破军>牌');
										('step 1');
										player.removeSkill('lqtqx_pojun_2');
									},
									intro: {
										markcount: 'expansion',
										mark(dialog, storage, player) {
											const cards = player.getExpansions('lqtqx_pojun_2');
											if (player.isUnderControl(true)) {
												dialog.addAuto(cards);
											} else {
												return '共有' + get.cnNumber(cards.length) + '张牌';
											}
										},
									},
								},
							},
						},
						lqtqx_yicheng: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								target: 'useCardToTargeted',
							},
							forced: true,
							filter(event, player) {
								return ['sha', 'juedou'].includes(event.card.name) && event.target.hp > 0 && event.player.countCards('he') > 0;
							},
							content() {
								'step 0';
								const next = player.choosePlayerCard(trigger.player, 'he', [1, Math.min(trigger.player.hp, trigger.player.countCards('he'))], '疑城:' + get.prompt('lqtqx_pojun', trigger.player));
								next.set('ai', function (button) {
									if (!_status.event.goon) {
										return 0;
									}
									const val = get.value(button.link);
									if (button.link == _status.event.player.getEquip(2)) {
										return 2 * (val + 3);
									}
									return val;
								});
								next.set('goon', get.attitude(player, trigger.player) <= 0);
								next.set('forceAuto', true);
								('step 1');
								if (result.bool) {
									trigger.player.addSkill('lqtqx_pojun_2');
									trigger.player.addToExpansion(result.cards, 'giveAuto', trigger.player).gaintag.add('lqtqx_pojun_2');
								}
							},
							group: 'lqtqx_yicheng_gain',
							subSkill: {
								gain: {
									audio: 'lqtqx_yicheng',
									trigger: {
										player: 'damageEnd',
										source: 'damageSource',
									},
									prompt: '是否发动【疑城】？',
									prompt2(event, player) {
										const list = event.player == player ? event.source : event.player;
										return '获得' + get.translation(list) + '的武将牌上随机两张破军牌.';
									},
									filter(event, player, name) {
										if (!event.card || !['sha', 'juedou'].includes(event.card.name) || event.num < 1) {
											return false;
										}
										if (name == 'damageEnd') {
											return event.source && event.source.getExpansions('lqtqx_pojun_2').length && event.source.isAlive();
										} //QQQ
										return event.player.getExpansions('lqtqx_pojun_2').length && event.player.isAlive();
									},
									content() {
										let list = trigger.source.getExpansions('lqtqx_pojun_2');
										if (event.triggername == 'damageSource') {
											list = trigger.player.getExpansions('lqtqx_pojun_2');
										}
										if (list.length) {
											player.gain(list.randomGets(2), 'draw2');
										}
									},
								},
							},
						},
						lqtq_huoluan: {
							trigger: {
								global: 'logSkillBegin',
							},
							audio: 'lqtq_yuanjiao',
							logTarget: 'player',
							silent: true,
							filter(event, player) {
								if (event.getParent('lqtq_huoluan').name == 'lqtq_huoluan') {
									return false;
								}
								if (event.player == player) {
									return false;
								}
								const skills = event.player.getSkills(true, false);
								game.expandSkills(skills);
								return skills.includes(event.skill); //QQQ
							},
							content() {
								trigger.player.storage.hsk = [lib.skill[trigger.skill].content, trigger.skill, player];
								lib.skill[trigger.skill].content = lib.skill.lqtq_huoluan.contentx;
							},
							contentx() {
								player.draw();
								player.storage.hsk[2].draw();
								lib.skill[player.storage.hsk[1]].content = player.storage.hsk[0];
							},
						},
						lqymm: {
							enable: 'phaseUse',
							content() {
								const a = [],
									b = [];
								for (const i in lib.skill) {
									const info = get.info(i);
									if (!info || !info.groupSkill) {
										continue;
									}
									b.push(i);
								}
								const dialog = ui.create.dialog('<b>选择一个技能获得', 'hidden');
								const skills = b;
								const yi = document.createElement('div');
								yi.classList.add('add-setting');
								yi.style.margin = '0';
								yi.style.width = '100%';
								yi.style.position = 'relative';

								for (const i of skills) {
									const td = ui.create.div('.shadowed.reduce_radius.pointerdiv.tdnode');
									td.innerHTML = '<font size=4>【' + get.translation(i) + '】<br><P align=left>' + get.skillInfoTranslation(i);
									td.link = i;
									td.addEventListener(lib.config.touchscreen ? 'touchend' : 'click', ui.click.button);
									Object.setPrototypeOf(td, lib.element.Button.prototype); //QQQ
									yi.appendChild(td);
									dialog.buttons.add(td);
								}

								dialog.content.appendChild(yi);
								const chooseButton = player.chooseButton(dialog, true);
								chooseButton.set('ai', function (button) {
									return Math.random();
								});
							},
						},
						qi_qi: {
							enable: 'phaseUse',
							content() {
								player.draw(17);
								player.discard(player.getCards('h'));
							},
						},
						//浪琴挑战
						_lqtz_shouqi: {
							//手气卡
							trigger: {
								global: 'gameDrawEnd',
							},
							forced: true,
							popup: false,
							_priority: 27,
							filter(event, player) {
								if (get.mode() != 'boss') {
									return false;
								}
								if (lib.config.extension_浪琴天阙_lqtq_shouqi_ka == false) {
									return false;
								}
								return true;
							},
							content() {
								'step 0';
								player.chooseBool('是否使用手气卡?').set('ai', function () {
									return false;
								});
								('step 1');
								if (result.bool) {
									const hs = player.getCards('h');
									game.addVideo('lose', player, [get.cardsInfo(hs), [], []]);

									for (const i of hs) {
										i.discard(false);
									}

									player.directgain(get.cards(hs.length));
									event.goto(0);
								}
							},
						},
						_lqtz_zhuanshutiaozhan: {
							//专属挑战开始
							trigger: {
								global: 'gameStart',
							},
							forced: true,
							popup: false,
							_priority: 21,
							filter(event, player) {
								return player == game.boss && (lib.config.extension_浪琴天阙_lqtq_zhuanshu_moshi != '0' || game.boss.name == 'lqtz_shanhaizhiyi');
							},
							content() {
								'step 0';
								event.m = lib.config.extension_浪琴天阙_lqtq_zhuanshu_moshi;
								if (game.boss.name == 'lqtz_shanhaizhiyi') {
									//位置
									var pian = lib.config.extension_浪琴天阙_lqtq_shanhaizhiyi_pian,
										xuan = lib.config.extension_浪琴天阙_lqtq_xuanyuan_tiaozhan;
									game.countPlayer(function (current) {
										if (current.name == 'lqtz_dahu') {
											current.init('lqtz_xiaohu');
										}
									});
									if (
										(pian == 'lqtz_ruilinjiangshi' &&
											!game.hasPlayer(function (current) {
												return current.name == 'lqtz_qilin';
											})) ||
										(pian == 'lqtz_huhushengwei' &&
											!game.hasPlayer(function (current) {
												return current.name == 'lqtz_xiaohu';
											}))
									) {
										if (xuan == 1) {
											player.addFellow('liubei');
										}
										player.addFellow(pian == 'lqtz_ruilinjiangshi' ? 'lqtz_qilin' : 'lqtz_xiaohu');
									}
									if (game.boss.next.next == game.boss) {
										player.addFellow('sunce');
									}
								}
								game.countPlayer(function (current) {
									//血量
									if (current.side != game.boss.side && !['lqtz_qilin', 'lqtz_xiaohu', 'lqtz_dahu'].includes(current.name)) {
										if (event.m != '4' || game.boss.name == 'lqtz_shanhaizhiyi') {
											current.clearSkills();
											if (!current.storage.zhuanshums3) {
												current.storage.zhuanshums3 = [];
											}
											for (const i of current.getSkills(true, false)) {
												if (lib.skill[i].fixed == true) {
													lib.skill[i].fixed = false;
													current.removeSkill(i);
													current.storage.zhuanshums3.push(i);
												}
											}
											for (const i of current.storage.zhuanshums3) {
												lib.skill[i].fixed = true;
											}
											lib.character[current.name][3] = [];
											current.maxHp = 4;
											current.hp = 4;
											if (
												game.boss.name != 'lqtz_shanhaizhiyi' ||
												game.hasPlayer(function (current1) {
													return current1.name == 'lqtz_xiaohu';
												})
											) {
												current.maxHp = 6;
												current.hp = 6;
											}
											current.update();
										} else {
											game.addGlobalSkill('lqtz_zhuanshutiaozhan4');
											if (!current.storage.zhuanshums1) {
												current.storage.zhuanshums1 = [];
											}
											if (!current.storage.zhuanshums2) {
												current.storage.zhuanshums2 = [];
											}
											event.finish();
										}
									}
								});
								if (game.boss.name == 'lqtz_shanhaizhiyi') {
									if (pian == 'lqtz_huhushengwei') {
										game.changeZhuGuoZi(0);
										game.addGlobalSkill('lqtz_zhuanshutiaozhan2');
									} else if (pian == 'lqtz_qingqiunongqin') {
										game.changeDuoMo(0);
										game.addGlobalSkill('lqtz_zhuanshutiaozhan3');
									} else {
										game.addGlobalSkill('lqtz_zhuanshutiaozhan1');
									}
								} else {
									game.changeMoShi(1);
									if (event.m == '2') {
										game.changeZhuGuoZi(0);
										game.addGlobalSkill('lqtz_zhuanshutiaozhan2');
									} else if (event.m == '3') {
										game.changeDuoMo(0);
										game.addGlobalSkill('lqtz_zhuanshutiaozhan3');
									} else if (event.m == '1') {
										game.addGlobalSkill('lqtz_zhuanshutiaozhan1');
									}
								}
								('step 1');
								event.count = player.next;
								('step 2');
								if (event.count.side == game.boss.side || ['lqtz_qilin', 'lqtz_xiaohu', 'lqtz_dahu'].includes(event.count.name)) {
									event.goto(4);
								} else {
									const b = lib.skill.lqtz_zhuanshutiaozhan1.skilla();
									if (b.length) {
										const list = b.randomGets(12);
										for (let i = 0; i < list.length; i++) {
											list[i] = ['', '', 'skillCard_' + list[i]];
										}
										event.count.chooseButton(['选择要获得的技能', [list, 'vcard']], true);
									} else {
										player.say('没有符合的技能,请开启武将包或关闭AI禁用武将');
										event.finish();
									}
								}
								('step 3');
								if (!event.count.storage.zhuanshums1) {
									event.count.storage.zhuanshums1 = [];
								}
								event.count.storage.zhuanshums1.push(result.links[0][2].slice(10));
								event.count.addSkillLog(result.links[0][2].slice(10));
								('step 4');
								event.count = event.count.next;
								if (event.count.side != game.boss.side) {
									event.goto(2);
								}
							},
						},
						lqtz_zhuanshutiaozhan1: {
							//专属挑战Ⅰ
							skilla() {
								const b = [];
								for (const name in lib.character) {
									for (const j of lib.character[name].skills) {
										b.push(j);
										lib.card['skillCard_' + j] = {
											fullimage: true,
											image: 'character:' + name,
										};
										lib.translate['skillCard_' + j] = lib.translate[j];
										lib.translate['skillCard_' + j + '_info'] = lib.translate[j + '_info'];
									}
								}
								return b;
							},
							trigger: {
								global: 'dieEnd',
							},
							forced: true,
							popup: false,
							_priority: 21,
							filter(event, player) {
								if (get.mode() != 'boss' || ['lqtz_qilin', 'lqtz_xiaohu', 'lqtz_dahu'].includes(player.name) || player.side == game.boss.side) {
									return false;
								}
								return event.player == game.boss;
							},
							content() {
								'step 0';
								const b = lib.skill.lqtz_zhuanshutiaozhan1.skilla();
								if (b.length) {
									const list = b.randomGets(12);
									for (let i = 0; i < list.length; i++) {
										list[i] = ['', '', 'skillCard_' + list[i]];
									}
									player.chooseButton(['选择要获得的技能', [list, 'vcard']], true);
								} else {
									player.say('没有符合的技能,请开启武将包或关闭AI禁用武将');
									event.finish();
								}
								('step 1');
								if (!player.storage.zhuanshums1) {
									player.storage.zhuanshums1 = [];
								}
								player.storage.zhuanshums1.push(result.links[0][2].slice(10));
								player.addSkillLog(result.links[0][2].slice(10));
								('step 2');
								const d = [],
									a = player.getSkills(true, false).slice(0),
									c = game.moshi > 0 ? 4 : 3;
								for (const i of player.storage.zhuanshums1) {
									if (i.name == a.name) {
										d.push(i);
									}
								}
								if (d.length > c) {
									player
										.chooseControl(d)
										.set('prompt', '请选择一个要失去的技能')
										.set('ai', function () {
											return 0;
										});
								} else {
									event.finish();
								}
								('step 3');
								player.removeSkill(result.control);
								player.storage.zhuanshums1.remove(result.control);
							},
						},
						lqtz_zhuanshutiaozhan2: {
							//专属模式Ⅱ
							enable: 'phaseUse',
							popup: false,
							filter(event, player) {
								if (get.mode() != 'boss' || player.side == game.boss.side || ['lqtz_qilin', 'lqtz_xiaohu', 'lqtz_dahu'].includes(player.name)) {
									return false;
								}
								return game.zhuguozi > 2;
							},
							content() {
								'step 0';
								game.changeZhuGuoZi(-3);
								const b = lib.skill.lqtz_zhuanshutiaozhan1.skilla();
								if (b.length) {
									const list = b.randomGets(12);
									for (let i = 0; i < list.length; i++) {
										list[i] = ['', '', 'skillCard_' + list[i]];
									}
									player.chooseButton(['选择要获得的技能', [list, 'vcard']], true);
								} else {
									player.say('没有符合的技能,请开启武将包或关闭AI禁用武将');
									event.finish();
								}
								('step 1');
								if (!player.storage.zhuanshums1) {
									player.storage.zhuanshums1 = [];
								}
								player.addSkill(result.links[0][2].slice(10));
								player.popup(result.links[0][2].slice(10));
								player.storage.zhuanshums1.push(result.links[0][2].slice(10));
								('step 2');
								const d = [],
									a = player.getSkills(true, false).slice(0),
									c = game.moshi > 0 ? 4 : 3;
								for (const i of player.storage.zhuanshums1) {
									if (i.name == a.name) {
										d.push(i);
									}
								}
								if (d.length > c) {
									player
										.chooseControl(d)
										.set('prompt', '请选择一个要失去的技能')
										.set('ai', function () {
											return 0;
										});
								} else {
									event.finish();
								}
								('step 3');
								player.removeSkill(result.control);
								player.storage.zhuanshums1.remove(result.control);
							},
							ai: {
								order: 30,
								result: {
									player: 2,
								},
							},
						},
						lqtz_zhuanshutiaozhan3: {
							//专属挑战Ⅲ
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							popup: false,
							_priority: 21,
							filter(event, player, name) {
								if (get.mode() != 'boss' || ['lqtz_qilin', 'lqtz_xiaohu', 'lqtz_dahu'].includes(player.name) || player.side == game.boss.side) {
									return false;
								}
								return game.duomo >= 0;
							},
							content() {
								'step 0';
								player.chooseBool('###【祁邪】###可以从数个随机技能中选择一个技能获得.').set('ai', function () {
									return true;
								});
								('step 1');
								if (result.bool) {
									const b = lib.skill.lqtz_zhuanshutiaozhan1.skilla();
									if (b.length) {
										const list = b.randomGets(12);
										for (let i = 0; i < list.length; i++) {
											list[i] = ['', '', 'skillCard_' + list[i]];
										}
										player.chooseButton(['选择要获得的技能', [list, 'vcard']], true);
									} else {
										player.say('没有符合的技能,请开启武将包或关闭AI禁用武将');
										event.finish();
									}
								} else {
									event.finish();
								}
								('step 2');
								if (!player.storage.zhuanshums1) {
									player.storage.zhuanshums1 = [];
								}
								player.storage.zhuanshums1.push(result.links[0][2].slice(10));
								player.addSkill(result.links[0][2].slice(10));
								player.popup(result.links[0][2].slice(10));
								if (game.duomo >= 0) {
									game.changeDuoMo(1);
								}
								if (game.duomo > 6 && game.boss.name == 'lqtz_qingnu') {
									game.boss.uninit();
									game.boss.init('lqtz_hunu');
								}
								if (num > 11 && game.boss.name == 'lqtz_hunu') {
									game.boss.uninit();
									game.boss.init('lqtz_jiuweihu');
								}
								('step 3');
								const d = [],
									a = player.getSkills(true, false).slice(0),
									c = game.moshi > 0 ? 4 : 3;
								for (const i of player.storage.zhuanshums1) {
									if (i.name == a.name) {
										d.push(i);
									}
								}
								if (d.length > c) {
									player
										.chooseControl(d)
										.set('prompt', '请选择一个要失去的技能')
										.set('ai', function () {
											return 0;
										});
								} else {
									event.finish();
								}
								('step 4');
								player.removeSkill(result.control);
								player.storage.zhuanshums1.remove(result.control);
							},
						},
						lqtz_zhuanshutiaozhan4: {
							//专属挑战中
							trigger: {
								global: 'dieEnd',
							},
							forced: true,
							popup: false,
							_priority: 21,
							filter(event, player, name) {
								if (get.mode() != 'boss' || ['lqtz_qilin', 'lqtz_xiaohu', 'lqtz_dahu'].includes(player.name) || player.side == game.boss.side) {
									return false;
								}
								return event.player == game.boss;
							},
							content() {
								'step 0';
								if (player.isDamaged()) {
									player
										.chooseControl('摸两张牌', '回复一点体力')
										.set('prompt', '请选择一项')
										.set('ai', function () {
											return 0;
										});
								}
								('step 1');
								if (!result.control || result.control == '摸两张牌') {
									player.draw(2);
								} else {
									player.recover();
								}
								const b = lib.skill.lqtz_zhuanshutiaozhan1.skilla();
								if (b.length) {
									const list = b.randomGets(12);
									for (let i = 0; i < list.length; i++) {
										list[i] = ['', '', 'skillCard_' + list[i]];
									}
									player.chooseButton(['选择要获得的技能', [list, 'vcard']], true);
								} else {
									player.say('没有符合的技能,请开启武将包或关闭AI禁用武将');
									event.finish();
								}
								('step 2');
								if (!player.storage.zhuanshums1) {
									player.storage.zhuanshums1 = [];
								}
								if (!player.storage.zhuanshums2) {
									player.storage.zhuanshums2 = [];
								}
								let group = lib.skill[result.links[0][2].slice(10)].group || [],
									num = 0;
								if (lib.skill[result.links[0][2].slice(10)].enable == 'phaseUse') {
									num++;
								}
								if (typeof group == 'string') {
									if (lib.skill[group].enable == 'phaseUse') {
										num++;
									}
								} else {
									for (const item of group) {
										if (lib.skill[item].enable == 'phaseUse') {
											num++;
										}
									}
								}
								player.addSkill('lqtz_baizhan_make');
								if (num > 0) {
									if (player.storage.zhuanshums1.length) {
										player.removeSkill(player.storage.zhuanshums1[0]);
										player.storage.zhuanshums1 = [];
									}
									player.storage.zhuanshums1.push(result.links[0][2].slice(10));
								} else {
									player.storage.zhuanshums2.push(result.links[0][2].slice(10));
								}
								player.addSkill(result.links[0][2].slice(10));
								player.popup(result.links[0][2].slice(10));
							},
						},
						lqtz_baizhan_make: {
							charlotte: true,
							fixed: true,
							mark: true,
							marktext: 'Ⅳ',
							intro: {
								name: '模式Ⅳ',
								mark(dialog, content, player) {
									if (player.storage.zhuanshums1 && player.storage.zhuanshums1.length) {
										dialog.addText('主动技');
										dialog.add(get.translation(player.storage.zhuanshums1));
									}
									if (player.storage.zhuanshums2 && player.storage.zhuanshums2.length) {
										dialog.addText('被动技');
										dialog.add(get.translation(player.storage.zhuanshums2));
									}
								},
							},
						},
						_lqtz_xiuzheng: {
							//进入修整
							trigger: {
								global: 'dieEnd',
							},
							filter(event, player) {
								return ['lqtz_qingnu', 'lqtz_hunu', 'lqtz_zhuquejiangling', 'lqtz_xuanwujiangling', 'lqtz_zhuquefaxiang', 'lqtz_xuanwufaxiang', 'lqtz_zhuquezhenshen', 'lqtz_xuanwuzhenshen'].includes(event.player.name);
							},
							forced: true,
							popup: false,
							_priority: Infinity,
							content() {
								game.addGlobalSkill('lqtz_xiuzheng2');
								const dead = game.dead.slice(0);

								for (const i of dead) {
									if (i.name == trigger.player.name) {
										i.revive(i.maxHp);
										i.addSkill('lqtz_xiuzheng');
										i.classList.add('out');
									}
								}
							},
						},
						lqtz_xiuzheng: {
							mark: true,
							marktext: '修',
							intro: {
								name: '修整中',
								mark(dialog, content, player) {
									dialog.addText('你正处于修整状态,到你的回合开始时,你结束修整重新加入游戏.');
								},
							},
						},
						lqtz_xiuzheng2: {
							//脱离修整
							trigger: {
								global: 'phaseBefore',
							},
							forced: true,
							popup: false,
							_priority: 12,
							filter(event, player) {
								return event.player.classList.contains('out') && event.player.hasSkill('lqtz_xiuzheng');
							},
							content() {
								trigger.player.classList.remove('out');
								trigger.player.removeSkill('lqtz_xiuzheng');
								if (!['lqtz_zhuquezhenshen', 'lqtz_xuanwuzhenshen'].includes(trigger.player.name)) {
									let list = 'lqtz_zhuquefaxiang';
									if (trigger.player.name == 'lqtz_zhuquefaxiang') {
										list = 'lqtz_zhuquezhenshen';
									}
									if (trigger.player.name == 'lqtz_xuanwujiangling') {
										list = 'lqtz_xuanwufaxiang';
									}
									if (trigger.player.name == 'lqtz_xuanwufaxiang') {
										list = 'lqtz_xuanwuzhenshen';
									}
									if (trigger.player.name == 'lqtz_qingnu') {
										list = 'lqtz_hunu';
									}
									if (trigger.player.name == 'lqtz_hunu') {
										list = 'lqtz_jiuweihu';
									}
									trigger.player.init(list);
									game.playAudio('../extension/浪琴天阙/audio/lqtq_xiuzhengyin.mp3');
									if (list == 'lqtz_xuanwuzhenshen' || list == 'lqtz_zhuquezhenshen') {
										game.addGlobalSkill(list + 'x');
									}
								}
								trigger.player.directgain(get.cards(4));
							},
						},
						_lqtz_zhuguozi2: {
							//获得朱果
							trigger: {
								global: ['dieBegin', 'damageEnd'],
							},
							forceDie: true,
							filter(event, player, name) {
								if (get.mode() != 'boss') {
									return false;
								}
								if (name == 'damageEnd') {
									return event.source && event.source == player && game.zhuguozi >= 0 && event.player.side == game.boss.side && player.side != game.boss.side && event.source && event.source.side != game.boss.side;
								}
								return event.player.side == game.boss.side && game.zhuguozi >= 0 && event.player == player;
							},
							forced: true,
							popup: false,
							_priority: 21,
							content() {
								game.changeZhuGuoZi(trigger.num || 5);
							},
						},
						_lqtz_baibei: {
							//助阵死亡败北
							trigger: { global: 'dieEnd' },
							forced: true,
							_priority: -20,
							popup: false,
							filter(event, player) {
								if (lib.config.mode != 'boss' || player.side == game.boss.side) {
									return false;
								}
								return (
									['lqtz_qilin', 'lqtz_xiaohu', 'lqtz_dahu'].includes(event.player.name) ||
									game.hasPlayer(function (current) {
										return (
											['lqtz_qilin', 'lqtz_xiaohu', 'lqtz_dahu'].includes(current.name) &&
											!game.hasPlayer(function (current1) {
												return current.getFriends().includes(current1);
											})
										);
									})
								);
							},
							content() {
								if (game.me == game.boss) {
									game.over(true);
								}
								game.over(false);
							},
						},
						_lqtz_shuangzimopai: {
							//朱雀玄武摸牌
							trigger: {
								global: 'damageBegin',
							},
							filter(event, player) {
								return (['lqtz_zhuquejiangling', 'lqtz_zhuquefaxiang', 'lqtz_zhuquezhenshen'].includes(player.name) && ['lqtz_xuanwujiangling', 'lqtz_xuanwufaxiang', 'lqtz_xuanwuzhenshen'].includes(event.player.name)) || (['lqtz_zhuquejiangling', 'lqtz_zhuquefaxiang', 'lqtz_zhuquezhenshen'].includes(event.player.name) && ['lqtz_xuanwujiangling', 'lqtz_xuanwufaxiang', 'lqtz_xuanwuzhenshen'].includes(player.name));
							},
							forced: true,
							popup: false,
							_priority: 21,
							content() {
								player.draw();
							},
						},
						lqtz_shanhai: {
							nobracket: true,
							trigger: {
								global: 'gameStart',
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								return player == game.boss;
							},
							content() {
								let pian = lib.config.extension_浪琴天阙_lqtq_shanhaizhiyi_pian,
									xuan = lib.config.extension_浪琴天阙_lqtq_xuanyuan_tiaozhan,
									list = ['lqtz_chi', 'lqtz_mei', 'lqtz_wang', 'lqtz_liang'];
								if (trigger.name == 'die') {
									game.removeGlobalSkill('lqtz_shanhai');
									if (pian != 'lqtz_qingqiunongqin' && pian != 'lqtz_huhushengwei') {
										game.addGlobalSkill('lqtz_shanhai2');
									}
								} else {
									if (pian == 'lqtz_jianghunjuexing') {
										list = ['lqtzhun_xuzhu', 'lqtzhun_ganning', 'lqtzhun_guanyu', 'lqtzhun_diaochan'];
									}
									if (pian == 'lqtz_ruilinjiangshi') {
										list = ['lqtz_qiongqi', 'lqtz_hundun'];
									}
									if (pian == 'lqtz_qingqiunongqin') {
										list = ['lqtz_qingnu'];
									}
									if (pian == 'lqtz_huhushengwei') {
										game.bossinfo.checkResult = function (player) {
											if (player == game.boss) {
												const list = ['lqtz_zhuquezhenshen', 'lqtz_xuanwuzhenshen'];
												return list.includes(player.name) && game.players.some((q) => !q.isIn() && list.includes(q.name));
											}
											return true;
										}; //QQQ
										list = ['lqtz_xuanwujiangling'];
										player.addFellow('lqtz_zhuquejiangling');
									}
									player.smoothAvatar();
									player.init(list.randomGet());
									game.addVideo('reinit2', player, player.name);
								}
							},
						},
						lqtz_shanhai2: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								return event.player == game.boss;
							},
							content() {
								game.removeGlobalSkill('lqtz_shanhai2');
								game.addGlobalSkill('lqtz_shanhai3');
								let pian = lib.config.extension_浪琴天阙_lqtq_shanhaizhiyi_pian,
									list = ['lqtz_mamian', 'lqtz_niutou'];
								if (pian == 'lqtz_jianghunjuexing') {
									list = ['lqtzhun_simayi', 'lqtzhun_zhouyu', 'lqtzhun_zhugeliang'];
								}
								if (pian == 'lqtz_ruilinjiangshi') {
									list = ['lqtz_taowu', 'lqtz_taotie'];
								}
								game.changeBossQ(list.randomGet());
							},
						},
						lqtz_shanhai3: {
							trigger: {
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								return player == game.boss;
							},
							content() {
								game.removeGlobalSkill('lqtz_shanhai3');
								game.addGlobalSkill('lqtz_shanhai4');
							},
						},
						lqtz_shanhai4: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								return event.player == game.boss;
							},
							content() {
								game.removeGlobalSkill('lqtz_shanhai4');
								let pian = lib.config.extension_浪琴天阙_lqtq_shanhaizhiyi_pian,
									list = ['lqtzhun_caocao', 'lqtzhun_sunquan', 'lqtzhun_liubei', 'lqtzhun_lubu'];
								if (pian == 'lqtz_quzhunianshou') {
									list = ['lqtz_nianshou'];
								}
								if (pian == 'lqtz_quguibixie') {
									list = ['lqtz_luocha', 'lqtz_yecha', 'lqtz_heiwuchang', 'lqtz_baiwuchang'];
								}
								if (pian == 'lqtz_ruilinjiangshi') {
									list = ['lqtz_zhuyin'];
								}
								game.changeBossQ(list.randomGet());
							},
						},
						lqtz_shanhaizx: { nobracket: true },
						lqtz_aogang: {
							nobracket: true,
							trigger: {
								global: 'gameStart',
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								return player == game.boss;
							},
							content() {
								if (trigger.name == 'die') {
									game.removeGlobalSkill('lqtz_aogang');
									game.addGlobalSkill('lqtz_aogang2');
								} else {
									player.smoothAvatar();
									player.init('lqtz_qidala');
									game.addVideo('reinit2', player, player.name);
								}
							},
						},
						lqtz_aogang2: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_aogang2');
								game.addGlobalSkill('lqtz_aogang3');
								('step 1');
								game.changeBossQ('lqtz_xiruonuosi');
								let num = trigger.player.countMark('lqtz_qida') || 0;
								if (num < 1) {
									num = Infinity;
								}
								game.boss.maxHp = num;
								game.boss.hp = num;
								game.boss.update();
								game.boss.directgain(get.cards(6));
								('step 2');
								game.boss.addFellow('lqtz_xiruonuosizuoshou');
								game.boss.addFellow('lqtz_xiruonuosiyoushou');
							},
						},
						lqtz_aogang3: {
							trigger: {
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								return player == game.boss;
							},
							async content(event, trigger, player) {
								game.removeGlobalSkill('lqtz_aogang3');
								game.addGlobalSkill('lqtz_aogang4');
							},
						},
						lqtz_aogang4: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_aogang4');
								game.addGlobalSkill('lqtz_aogang5');
								('step 1');
								game.changeBossQ('lqtz_dazi');
								game.boss.directgain(get.cards(6));
								('step 2');
								game.boss.addFellow('lqtz_sheshenyi');
							},
						},
						lqtz_aogang5: {
							trigger: {
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								return player == game.boss;
							},
							async content(event, trigger, player) {
								game.removeGlobalSkill('lqtz_aogang5');
								game.addGlobalSkill('lqtz_aogang6');
							},
						},
						lqtz_aogang6: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_aogang6');
								('step 1');
								game.changeBossQ('lqtz_zhishen');
								game.boss.directgain(get.cards(6));
							},
						},
						lqtz_zhuxie: {
							nobracket: true,
							trigger: {
								global: 'gameStart',
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								return player == game.boss;
							},
							content() {
								if (trigger.name == 'die') {
									game.removeGlobalSkill('lqtz_zhuxie');
									game.addGlobalSkill('lqtz_zhuxie2');
								} else {
									player.smoothAvatar();
									player.init('lqtz_mengpoa');
									game.addVideo('reinit2', player, player.name);
								}
							},
						},
						lqtz_zhuxie2: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_zhuxie2');
								game.addGlobalSkill('lqtz_zhuxie3');
								('step 1');
								game.changeBossQ(['lqtz_heibaiwuchang', 'lqtz_riyeyoushen', 'lqtz_niutoumamian'].randomGet());
								game.boss.directgain(get.cards(6));
								('step 2');
								game.boss.addFellow(['lqtz_niaozui', 'lqtz_huangfeng', 'lqtz_yvsai', 'lqtz_baowei'].randomGet());
								game.boss.next.directgain(get.cards(4));
							},
						},
						lqtz_zhuxie3: {
							_priority: -20,
							trigger: { player: 'dieBegin' },
							filter(event, player) {
								return (
									event.player.side == game.boss.side &&
									!game.hasPlayer(function (current) {
										return current != event.player && event.player.getFriends().includes(current);
									})
								);
							},
							async content(event, trigger, player) {
								game.removeGlobalSkill('lqtz_zhuxie3');
								game.addGlobalSkill('lqtz_zhuxie4');
							},
						},
						lqtz_zhuxie4: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								return (
									event.player.side == game.boss.side &&
									!game.hasPlayer(function (current) {
										return current != event.player && event.player.getFriends().includes(current);
									})
								);
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_zhuxie4');
								game.addGlobalSkill('lqtz_zhuxie5');
								('step 1');
								game.changeBossQ('lqtz_guiwang');
								game.boss.directgain(get.cards(8));
							},
						},
						lqtz_zhuxie5: {
							trigger: {
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								return player == game.boss;
							},
							content() {
								game.removeGlobalSkill('lqtz_zhuxie5');
								game.addGlobalSkill('lqtz_zhuxie6');
							},
						},
						lqtz_zhuxie6: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_zhuxie6');
								game.addGlobalSkill('lqtz_shenglipanduan');
								('step 1');
								game.changeBossQ('lqtz_yanluowang');
								game.boss.directgain(get.cards(8));
							},
						},
						lqtz_zijin: {
							nobracket: true,
							trigger: { global: 'gameStart' },
							popup: false,
							forced: true,
							filter(event, player) {
								return player == game.boss;
							},
							content() {
								player.smoothAvatar();
								const qqzj = ['lqtzqqzj_caocao', 'lqtzqqzj_simayi', 'lqtzqqzj_lubu', 'lqtzqqzj_dongzhuo', 'lqtzqqzj_zhangjiao', 'lqtzqqzj_yuanshu'];
								qqzj.remove(player.nextSeat.name);
								player.init(qqzj.randomGet());
								game.addVideo('reinit2', player, player.name);
							},
						},
						lqtz_shenglipanduan: {
							//胜利判定
							trigger: { global: 'dieEnd' },
							forced: true,
							_priority: -20,
							popup: false,
							filter(event, player) {
								return !game.hasPlayer(function (current) {
									return current.side == game.boss.side;
								});
							},
							content() {
								if (game.me == game.boss) {
									game.over(false);
								}
								game.over(true);
							},
						},
						lqtz_shibaipanduan: {
							//失败判定
							trigger: { global: 'dieEnd' },
							forced: true,
							_priority: -20,
							popup: false,
							filter(event, player) {
								return !game.hasPlayer(function (current) {
									return current.side != game.boss.side;
								});
							},
							content() {
								if (game.me == game.boss) {
									game.over(true);
								}
								game.over(false);
							},
						},
						lqtz_shenjia1: {
							nobracket: true,
							trigger: {
								global: 'gameStart',
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								return player == game.boss;
							},
							content() {
								if (trigger.name == 'die') {
									game.removeGlobalSkill('lqtz_shenjia1');
									game.addGlobalSkill('lqtz_shenjia2');
								} else {
									player.smoothAvatar();
									player.init('lqtz_maerxiusiren');
									game.addVideo('reinit2', player, player.name);
								}
							},
						},
						lqtz_shenjia2: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_shenjia2');
								game.addGlobalSkill('lqtz_shenjia3');
								('step 1');
								game.changeBossQ('lqtz_maerxiusiyi');
								if (trigger.player.hasMark('lqtz_jiajia')) {
									game.boss.addMark('lqtz_jiajia', trigger.player.countMark('lqtz_jiajia'));
								}
								game.boss.directgain(get.cards(6));
								('step 2');
								game.boss.addFellow('lqtz_yi');
								game.boss.addFellow('lqtz_yi');
							},
						},
						lqtz_shenjia3: {
							trigger: {
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								return player == game.boss;
							},
							content() {
								game.removeGlobalSkill('lqtz_shenjia3');
								game.addGlobalSkill('lqtz_shenjia4');
							},
						},
						lqtz_shenjia4: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_shenjia4');
								game.addGlobalSkill('lqtz_shenjia5');
								('step 1');
								game.changeBossQ('lqtz_maerxiusidun');
								if (trigger.player.hasMark('lqtz_jiajia')) {
									game.boss.addMark('lqtz_jiajia', trigger.player.countMark('lqtz_jiajia'));
								}
								game.boss.directgain(get.cards(6));
								('step 2');
								game.boss.addFellow('lqtz_dun');
								game.boss.addFellow('lqtz_dun');
							},
						},
						lqtz_shenjia5: {
							trigger: {
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								return player == game.boss;
							},
							content() {
								game.removeGlobalSkill('lqtz_shenjia5');
								game.addGlobalSkill('lqtz_shenjia6');
							},
						},
						lqtz_shenjia6: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_shenjia6');
								('step 1');
								game.changeBossQ('lqtz_maerxiusishen');
								if (trigger.player.hasMark('lqtz_jiajia')) {
									game.boss.addMark('lqtz_jiajia', trigger.player.countMark('lqtz_jiajia'));
								}
								game.boss.directgain(get.cards(6));
								('step 2');
								game.boss.addFellow('lqtz_shenbing');
								game.boss.addFellow('lqtz_shenbing');
							},
						},
						lqtz_tianque: {
							nobracket: true,
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								return player == game.boss;
							},
							trigger: { global: 'gameStart' },
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_tianque');
								game.addGlobalSkill('lqtz_tianque2');
								const b = [],
									c = [];
								for (const i in lib.characterPack.langqintianque) {
									if (['lqtqt', 'lqtqx'].includes(i[0] + i[1] + i[2] + i[3] + i[4])) {
										b.push(i);
									}
									c.push(i);
								}
								event.b = b.randomGets(3);
								player.smoothAvatar();
								if (lib.config.extension_浪琴天阙_lqtq_langqintianque_pian == '1') {
									player.init(c.randomGet());
									game.addVideo('reinit2', player, player.name);
									event.finish();
								} else {
									player.init(event.b[0]);
									game.addVideo('reinit2', player, player.name);
								}
								('step 1');
								player.addFellow(event.b[1]);
								player.addFellow(event.b[2]);
							},
						},
						lqtz_tianque2: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return !game.hasPlayer(function (current) {
									return current.side == game.boss.side;
								});
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_tianque2');
								game.addGlobalSkill('lqtz_tianque3');
								event.a = lib.config.extension_浪琴天阙_lqtq_langqintianque_pian;
								event.b = [];
								('step 1');
								const c = [];
								for (const i in lib.characterPack.langqintianque) {
									if (i.includes('lqtq_') && i.name != 'lqtq_abiao') {
										event.b.push(i);
									}
									c.push(i);
								}
								event.c = c.randomGets(2);
								if (event.a == '0') {
									game.changeBossQ('lqtq_abiao');
								} else {
									game.changeBossQ(event.c[0]);
								}
								game.boss.directgain(get.cards(event.a == '0' ? 6 : 2));
								('step 2');
								if (event.a == '0') {
									game.boss.addFellow(event.b.randomGet());
								} else {
									game.boss.addFellow(event.c[1]);
								}
							},
						},
						lqtz_tianque3: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return !game.hasPlayer(function (current) {
									return current.side == game.boss.side;
								});
							},
							content() {
								'step 0';
								event.d = lib.config.extension_浪琴天阙_lqtq_langqintianque_pian;
								event.b = [];
								game.removeGlobalSkill('lqtz_tianque3');
								if (event.d == '0') {
									game.addGlobalSkill('lqtz_shenglipanduan');
								} else {
									game.addGlobalSkill('lqtz_tianque4');
								}
								('step 1');
								const c = [];
								for (const i in lib.characterPack.langqintianque) {
									if (i.includes('lqtqj')) {
										event.b.push(i);
									}
									c.push(i);
								}
								event.c = c.randomGets(3);
								game.changeBossQ(event.d == '0' ? event.b.randomGet() : event.c[0]);
								game.boss.directgain(get.cards(event.d == '0' ? 6 : 4));
								if (event.d == '1' && !game.boss.storage.lqtz_tianque) {
									game.boss.storage.lqtz_tianque = 1;
								}
								('step 2');
								if (event.d == '1') {
									game.changeBossQ(event.c[1]);
									game.boss.addFellow(event.c[2]);
								}
							},
						},
						lqtz_tianque4: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return !game.hasPlayer(function (current) {
									return current.side == game.boss.side;
								});
							},
							content() {
								'step 0';
								event.hp = game.boss.storage.lqtz_tianque || 3; //QQQ
								('step 1');
								('step 2');
								const d = [];
								for (const i in lib.characterPack.langqintianque) {
									d.push(i);
								}
								event.d = d.randomGets(3);
								game.changeBossQ(event.d[0]);
								game.boss.directgain(get.cards(6));
								game.boss.maxHp = game.boss.maxHp + event.hp;
								game.boss.hp = game.boss.maxHp + event.hp;
								game.boss.update();
								('step 3');
								const b = game.changeBossQ(event.d[1]);
								const c = game.changeBossQ(event.d[2]);
								b.directgain(get.cards(6));
								b.maxHp = b.maxHp + event.hp;
								b.hp = b.maxHp + event.hp;
								b.update();
								c.directgain(get.cards(6));
								c.maxHp = c.maxHp + event.hp;
								c.hp = c.maxHp + event.hp;
								c.update();
								if (!game.boss.storage.lqtz_tianque) {
									game.boss.storage.lqtz_tianque = event.hp + 1;
								}
								game.boss.markSkill('lqtz_tianque5');
							},
						},
						lqtz_tianque5: {
							marktext: '阙',
							intro: {
								name: '开摆',
								mark(dialog, content, player) {
									dialog.add(
										ui.create.div('.menubutton.pointerdiv', '不响丸辣', function () {
											if (!this.disabled) {
												this.disabled = true;
												this.classList.add('disabled');
												this.style.opacity = 0.5;
												lib.skill.lqtz_tianque5.lqtq(player);
											}
										}),
									);
								},
							},
							lqtq(player) {
								player.unmarkSkill('lqtz_tianque5');
								game.removeGlobalSkill('lqtz_tianque4');
								game.addGlobalSkill('lqtz_shenglipanduan');
							},
							clickableFilter(player) {
								return true;
							},
						},
						lqtz_RR: {
							nobracket: true,
							trigger: {
								global: 'gameStart',
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return player == game.boss;
							},
							content() {
								const a = event.triggername;
								if (a == 'gameStart') {
									game.boss.smoothAvatar();
									game.boss.init('lqtz_qiyilieying');
									game.addVideo('reinit2', game.boss, game.boss.name);
								} else {
									game.removeGlobalSkill('lqtz_RR');
									game.addGlobalSkill('lqtz_RR2');
								}
							},
						},
						lqtz_RR2: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_RR2');
								game.addGlobalSkill('lqtz_RR3');
								('step 1');
								game.changeBossQ('lqtz_lieyanlieying');
								game.boss.directgain(get.cards(4));
								('step 2');
								game.boss.addFellow('lqtz_emodiao');
								game.boss.addFellow('lqtz_emodiao');
							},
						},
						lqtz_RR3: {
							trigger: {
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return player == game.boss;
							},
							content() {
								game.removeGlobalSkill('lqtz_RR3');
								game.addGlobalSkill('lqtz_RR4');
							},
						},
						lqtz_RR4: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_RR4');
								game.addGlobalSkill('lqtz_RR5');
								('step 1');
								game.changeBossQ('lqtz_geminglieying');
								game.boss.directgain(get.cards(4));
								('step 2');
								game.boss.addFellow('lqtz_wulilinxiao');
								game.boss.addFellow('lqtz_wulilinxiao');
							},
						},
						lqtz_RR5: {
							trigger: {
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return player == game.boss;
							},
							content() {
								game.removeGlobalSkill('lqtz_RR5');
								game.addGlobalSkill('lqtz_RR6');
							},
						},
						lqtz_RR6: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_RR6');
								game.addGlobalSkill('lqtz_RR7');
								('step 1');
								game.changeBossQ('lqtz_weixingpaolieying');
								game.boss.directgain(get.cards(4));
								('step 2');
								game.boss.addFellow('lqtz_huorenfenshaolieying');
								game.boss.addFellow('lqtz_huorenfenshaolieying');
							},
						},
						lqtz_RR7: {
							trigger: {
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return player == game.boss;
							},
							content() {
								game.removeGlobalSkill('lqtz_RR7');
								game.addGlobalSkill('lqtz_RR8');
							},
						},
						lqtz_RR8: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_RR8');
								game.addGlobalSkill('lqtz_RR9');
								('step 1');
								game.changeBossQ('lqtz_jiujilieying');
								game.boss.directgain(get.cards(6));
								('step 2');
								game.boss.addFellow('lqtz_yibanglieying');
								game.boss.addFellow('lqtz_yibanglieying');
							},
						},
						lqtz_RR9: {
							trigger: {
								player: 'dieBegin',
							},
							forced: true,
							popup: false,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return player == game.boss;
							},
							content() {
								game.removeGlobalSkill('lqtz_RR9');
								game.addGlobalSkill('lqtz_RRX');
							},
						},
						lqtz_RRX: {
							trigger: { global: 'dieAfter' },
							forced: true,
							_priority: -20,
							filter(event, player) {
								if (lib.config.mode != 'boss') {
									return false;
								}
								return event.player == game.boss;
							},
							content() {
								'step 0';
								game.removeGlobalSkill('lqtz_RRX');
								('step 1');
								game.changeBossQ('lqtz_zuizhongbaoleilieying');
								game.boss.directgain(get.cards(6));
								('step 2');
								game.boss.addFellow('lqtz_wukulieying');
								game.boss.addFellow('lqtz_zhihuilinxiao');
							},
						},
						lqtz_fanshi: {
							audio: 'renjie2',
							group: 'lqtz_fanshib',
							trigger: { player: 'phaseEnd' },
							forced: true,
							content() {
								player.loseHp();
							},
						},
						lqtz_fanshib: {
							trigger: { global: 'phaseBefore' },
							forced: true,
							popup: false,
							filter(event, player) {
								return player.hp < 1;
							},
							content() {
								player.dying({});
							},
						},
						lqtz_mojian: {
							trigger: {
								player: 'phaseBegin',
							},
							audio: 'olluanji',
							forced: true,
							content() {
								const list = game.filterPlayer(function (current) {
									return player.canUse('wanjian', current) && current.getEnemies().includes(player);
								});
								list.sort(lib.sort.seat);
								player.useCard({ name: 'wanjian' }, list);
							},
						},
						lqtz_qushou: {
							trigger: {
								player: 'phaseBegin',
							},
							audio: 'manyi',
							forced: true,
							content() {
								const list = game.filterPlayer(function (current) {
									return player.canUse('nanman', current) && current.getEnemies().includes(player);
								});
								list.sort(lib.sort.seat);
								player.useCard({ name: 'nanman' }, list);
							},
						},
						lqtz_guihuo: {
							trigger: {
								player: 'phaseJieshuBegin',
							},
							audio: 'xintan',
							forced: true,
							content() {
								'step 0';
								player.chooseTarget(get.prompt(event.name), function (card, player, target) {
									return player != target;
								}).ai = function (target) {
									return get.damageEffect(target, player, player, 'fire');
								};
								('step 1');
								if (result.targets?.length) {
									result.targets[0].damage('fire');
								}
							},
						},
						lqtz_modao: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							content() {
								player.draw(2);
							},
						},
						lqtz_juhuo: {
							audio: 'ranshang',
							trigger: {
								source: 'damageBegin1',
								player: 'damageBegin1',
							},
							filter(event, player) {
								return event.nature == 'fire';
							},
							forced: true,
							content() {
								if (trigger.source == player) {
									trigger.num++;
								} else {
									trigger.num += 2;
									player.draw(2);
								}
							},
						},
						lqtz_zhennu: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							content() {
								game.countPlayer(function (current) {
									if (player.getEnemies().includes(current)) {
										if (Math.random() > 1 / 3) {
											current.damage();
										} else {
											current.damage('fire');
										}
									}
								});
							},
						},
						lqtz_hanyong: {
							trigger: { player: 'useCard' },
							filter(event, player) {
								return event.card && ['nanman', 'wanjian', 'sha'].includes(event.card.name);
							},
							content() {
								trigger.baseDamage++;
							},
						},
						lqtz_aotang: {
							trigger: {
								player: 'phaseBegin',
							},
							audio: 2, //QQQ
							forced: true,
							content() {
								const list = game.players.slice(0);
								list.remove(player);
								const target = list.randomGet();
								player.line(target);
								target.addSkill('lqtz_aotang_2');
							},
							group: 'lqtz_aotang_1',
							subSkill: {
								1: {
									trigger: {
										player: ['phaseBegin', 'dieBegin'],
									},
									forced: true,
									_priority: 20,
									forced: true,
									content() {
										game.countPlayer(function (current) {
											if (current.hasSkill('lqtz_aotang_2')) {
												current.removeSkill('lqtz_aotang_2');
											}
										});
									},
								},
								2: {
									init(player, skill) {
										const skills = player.getSkills(true, false);
										for (let i = 0; i < skills.length; i++) {
											if (get.info(skills[i]).charlotte) {
												skills.splice(i--, 1);
											}
										}
										player.disableSkill(skill, skills);
									},
									onremove(player, skill) {
										player.enableSkill(skill);
									},
									mark: true,
									marktext: '遗',
									intro: {
										name: '遗忘',
										content(storage, player, skill) {
											const list = Object.keys(player.disabledSkills);
											if (list.length) {
												let str = '遗忘技能:';

												for (const i of list) {
													if (lib.translate[i + '_info']) {
														str += get.translation(i) + '/';
													}
												}

												return str.slice(0, str.length - 1);
											}
										},
									},
									forced: true,
									popup: false,
									charlotte: true,
								},
							},
						},
						lqtz_yunju: {
							trigger: {
								global: 'phaseAfter',
							},
							audio: 'lqtz_shiyou',
							forced: true,
							filter(event, player, target) {
								return player.getEnemies().includes(event.player);
							},
							content() {
								const hs = trigger.player.getCards('h');
								if (hs.length) {
									trigger.player.discard(hs.randomGet());
								} else {
									trigger.player.loseHp();
								}
							},
						},
						lqtz_guimei2: {
							mod: {
								maxHandcardBase(player, num) {
									return (num += 4);
								},
							},
						},
						lqtz_guimei: {
							mod: {
								maxHandcardBase(player, num) {
									return (num += 4);
								},
							},
							trigger: {
								player: ['turnOverBefore', 'phaseDrawSkipped', 'phaseDrawCancelled', 'phaseUseSkipped', 'phaseUseCancelled'],
							},
							_priority: 20,
							forced: true,
							filter(event, player, name) {
								if (name == 'turnOverBefore') {
									return !player.isTurnedOver();
								}
								return true;
							},
							content() {
								'step 0';
								event.a = event.triggername;
								if (event.a == 'turnOverBefore') {
									trigger.cancel();
									game.log(player, '取消了翻面');
								}
								if (event.a == 'phaseDrawSkipped' || event.a == 'phaseDrawCancelled') {
									player.draw(3);
								}
								('step 1');
								if (event.a == 'phaseUseSkipped' || event.a == 'phaseUseCancelled') {
									player.chooseToUse(function (card) {
										return game.hasPlayer(function (current) {
											return player.canUse(card, current);
										});
									}, '是否使用一张牌？');
								} else {
									event.finish();
								}
								('step 2');
								if (result.bool) {
									event.goto(1);
								}
							},
						},
						lqtz_xixing: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							audio: 'xinleiji',
							forceDie: true,
							forced: true,
							content() {
								game.countPlayer(function (current2) {
									if (player.getEnemies().includes(current2)) {
										current2.damage('thunder');
										player.recover();
									}
								});
							},
						},
						lqtz_taiping: {
							trigger: {
								player: 'damageEnd',
							},
							forced: true,
							filter(event, player) {
								return event.source && event.num > 0 && event.source != player;
							},
							content() {
								'step 0';
								event.count = Math.min(trigger.num, 9);
								('step 1');
								event.count--;
								const next = trigger.source
									.chooseToDiscard('h', 2, '弃置两张花色不同的手牌', function (card) {
										if (ui.selected.cards.length) {
											return card.suit != ui.selected.cards[0].suit;
										}
										return true;
									})
									.set('complexCard', true);
								next.ai = function (card) {
									return 7 - get.value(card);
								};
								('step 2');
								if (!result.bool) {
									trigger.source.loseHp();
								}
								if (event.count) {
									event.goto(1);
								}
							},
						},
						lqtz_mizui: {
							trigger: {
								source: 'damageEnd',
							},
							audio: 'oljiuchi',
							check(event, player) {
								return get.attitude(player, event.player) < 0;
							},
							filter(event, player) {
								return event.card;
							},
							content() {
								player.discardPlayerCard('he', trigger.player, [1, 2], true);
							},
						},
						lqtz_qiangzheng: {
							audio: 'hengzheng',
							trigger: {
								player: 'phaseJieshuBegin',
							},
							filter(event, player) {
								return game.hasPlayer(function (current) {
									return player.getEnemies().includes(current) && current.countCards('h');
								});
							},
							forced: true,
							content() {
								game.countPlayer(function (current) {
									if (current.countGainableCards(player, 'h') && player.getEnemies().includes(current)) {
										player.gainPlayerCard(current, 'he', true);
									}
								});
							},
						},
						lqtz_xiaoshou: {
							audio: 'liewei',
							inherit: 'lqtz_langxi',
						},
						lqtz_manji: {
							shaRelated: true,
							trigger: {
								player: 'useCardToBefore',
							},
							audio: 'cuorui',
							filter(event, player) {
								return ['sha', 'juedou'].includes(event.card.name) && event.target.countDiscardableCards(player, 'he') > 0; //可弃置
							},
							forced: true,
							content() {
								'step 0';
								player
									.discardPlayerCard('he', trigger.target, get.prompt('lqtz_manji', trigger.target))
									.set('ai', function (button) {
										if (!_status.event.att) {
											return 0;
										}
										return 1;
									})
									.set('att', get.attitude(player, trigger.target) <= 0);
								('step 1');
								if (result.links?.length) {
									if (result.links[0].name == 'sha') {
										trigger.baseDamage++;
									} else {
										player.gain(result.links[0], 'gain2', 'log');
									}
								}
							},
						},
						lqtz_shiyu: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							content() {
								const cards = [],
									list = [];
								get.cardPile(function (card) {
									if (!list.includes(card.suit)) {
										list.push(card.suit);
										cards.push(card);
									}
								});
								player.gain(cards, 'gain2');
							},
						},
						lqtz_guizhao: {
							trigger: {
								player: ['useCard1', 'respond'],
							},
							forced: true,
							filter(event, player) {
								return (
									!player.hasHistory('useCard', function (evt) {
										return evt.card != event.card && get.type2(evt.card) == get.type2(event.card);
									}) &&
									!player.hasHistory('respond', function (evt2) {
										return evt2.card != event.card && get.type2(evt2.card) == get.type2(event.card);
									})
								);
							},
							content() {
								player.draw();
							},
						},
						lqtz_zhoucha: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							content() {
								'step 0';
								player.judge(function (card) {
									return get.color(card) == 'red' ? 2 : 1;
								});
								('step 1');
								player.gain(result.card, 'draw2');
								if (result.color == 'red') {
									player.draw(2);
									player.addTempSkill('lqtz_zhoucha_1');
									event.finish();
								} else {
									player.chooseTarget('昼刹:对一名角色造成一点火焰伤害.', lib.filter.notMe).ai = function (target) {
										return get.damageEffect(target, player, player, 'fire');
									};
								}
								('step 2');
								if (result.targets?.length) {
									result.targets[0].damage('fire');
								}
							},
							subSkill: {
								1: {
									mod: {
										cardUsable(card, player, num) {
											if (card.name == 'sha') {
												return (num += 2);
											}
										},
									},
								},
							},
						},
						lqtz_yezhong: {
							trigger: {
								player: 'phaseJieshuBegin',
							},
							forced: true,
							content() {
								'step 0';
								player.judge(function (card) {
									return get.color(card) == 'black' ? 1 : 0;
								});
								('step 1');
								player.gain(result.card, 'draw2');
								if (result.color == 'black') {
									game.countPlayer(function (current) {
										if (current.countGainableCards(player, 'h') && player.getEnemies().includes(current)) {
											player.gainPlayerCard(current, 'h', true);
										}
									});
									event.finish();
								} else {
									player.chooseTarget('夜冢:对一名角色造成一点雷电伤害.', lib.filter.notMe).ai = function (target) {
										return get.damageEffect(target, player, player, 'thunder');
									};
								}
								('step 2');
								if (result.targets?.length) {
									result.targets[0].damage('thunder');
								}
							},
						},
						lqtz_duane: {
							trigger: {
								global: 'phaseDiscardAfter',
							},
							filter(event, player) {
								if (!event.cards) {
									return false;
								}
								return player.getEnemies().includes(event.player);
							},
							forced: true,
							content() {
								trigger.player.loseHp();
							},
						},
						lqtz_huiyun: {
							enable: 'phaseUse',
							usable: 1,
							filterTarget(card, player, target) {
								return player != target && target.countCards('h');
							},
							content() {
								'step 0';
								player.chooseButton([target + '的手牌', target.getCards('h')], true, [1, 2]).set('ai', function (card) {
									return get.value(card);
								});
								('step 1');
								if (result.links?.length) {
									target.discard(result.links);
									player
										.chooseToDiscard('你可弃置一张与' + get.translation(result.links) + '中有相同类型的牌,对' + get.translation(target) + '造成两点伤害', function (card) {
											const a = [];
											for (const i of result.links) {
												a.push(get.type2(i));
											}
											return a.includes(get.type2(card));
										})
										.set('ai', function (card) {
											return 10 - get.value(card);
										});
								} else {
									event.finish();
								}
								('step 2');
								if (result.bool) {
									target.damage(2);
								}
							},
							ai: {
								order: 11,
								result: {
									target(player, target) {
										return -target.countCards('h');
									},
								},
								threaten: 1.1,
							},
						},
						lqtz_yinsha: {
							trigger: {
								global: 'phaseUseBegin',
							},
							forced: true,
							filter(event, player, target) {
								return event.player.countCards('h') > event.player.maxHp;
							},
							content() {
								player.addTempSkill('lqtz_yinsha_1');
							},
							subSkill: {
								1: {
									mod: {
										targetEnabled(card, player, target, now) {
											if (card.name == 'sha' || card.name == 'juedou') {
												return false;
											}
										},
									},
								},
							},
						},
						lqtz_eli: {
							trigger: {
								source: 'damageBegin1',
							},
							filter(event, player) {
								return player.getEnemies().includes(event.player);
							},
							forced: true,
							derivation: ['xinkuanggu', 'rewansha'],
							content() {
								'step 0';
								player.judge(function (card) {
									return get.color(card) == 'red' ? 1 : 0;
								});
								('step 1');
								if (result.color == 'red') {
									trigger.num++;
									player.addTempSkill('xinkuanggu');
								} else {
									player.draw();
									player.addTempSkill('rewansha');
								}
							},
						},
						lqtz_bingyi: {
							trigger: {
								player: 'loseAfter',
								global: ['equipAfter', 'addJudgeAfter', 'gainAfter', 'loseAsyncAfter'],
							},
							usable: 1,
							forced: true,
							filter(event, player) {
								if (player.countCards('h') > 2) {
									return false;
								}
								const evt = event.getl(player);
								return evt && evt.player == player && evt.hs && evt.hs.length;
							},
							content() {
								player.draw(6);
							},
						},
						lqtz_suoxue: {
							shaRelated: true,
							trigger: {
								player: 'useCardToPlayered',
							},
							forced: true,
							filter(event, player) {
								return ['sha', 'juedou'].includes(event.card.name);
							},
							content() {
								'step 0';
								if (player.countCards('h') < trigger.target.countCards('h')) {
									player.chooseBool('###是否发动【索穴】？###将手牌摸至' + get.translation(trigger.target) + '的手牌数.').set('ai', function () {
										return true;
									});
								} else {
									player.chooseToDiscard('###是否发动【索穴】？###弃置一张牌令' + get.translation(trigger.target) + '不可闪避你使用的' + get.translation(trigger.card) + '.').set('ai', function (card) {
										if (get.attitude(_status.event.player, trigger.target) < 1) {
											return 7 - get.value(card);
										}
										return 0;
									});
								}
								('step 1');
								if (result.cards?.length) {
									if (!result.cards[0]) {
										player.drawTo(trigger.target.countCards('h'));
									} else {
										trigger.directHit.add(trigger.target);
									}
								}
							},
						},
						lqtz_duzhen: {
							trigger: {
								player: 'useCardToPlayered',
							},
							forced: true,
							filter(event, player) {
								return player.getEnemies().includes(event.target) && event.target.countDiscardableCards(event.target, 'he'); //QQQ
							},
							content() {
								const hs = trigger.target.getCards(trigger.target.countCards('e') ? 'e' : 'h');
								if (hs.length) {
									trigger.target.discard(hs.randomGet());
								}
							},
						},
						lqtz_mingchong: {
							trigger: {
								player: 'dieBegin',
							},
							forced: true,
							filter(event, player, target) {
								return game.hasPlayer(function (current) {
									return current != player && player.getFriends().includes(current);
								});
							},
							content() {
								game.countPlayer(function (current1) {
									if (current1 != player && player.getFriends().includes(current1)) {
										current1.addSkill('lqtz_duzhen');
									}
								});
							},
						},
						lqtz_guixi: {
							trigger: {
								player: 'damageEnd',
							},
							forced: true,
							content() {
								'step 0';
								player.judge(function (card) {
									return card.suit == 'heart' ? 2 : 1;
								});
								('step 1');
								if (result.suit == 'heart') {
									player.recover();
								} else {
									player.draw();
								}
							},
						},
						lqtz_anchao: {
							intro: {
								content: 'mark',
							},
							trigger: {
								global: ['damageBegin3', 'phaseDrawBegin2', 'phaseAfter'],
								player: 'dieBegin',
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'phaseAfter') {
									return player.getFriends(true).includes(event.player) && (!event.player.getStat().damage || (event.player.getStat().damage > 0 && event.player.countMark('lqtz_anchao') > 0));
								}
								if (name == 'damageBegin3') {
									return event.source && event.source.countMark('lqtz_anchao') > 0;
								}
								if (name == 'phaseDrawBegin2') {
									return event.player.countMark('lqtz_anchao') > 0 && !event.numFixed;
								}
								return game.hasPlayer(function (current) {
									return current != player && current.countMark('lqtz_anchao') > 0;
								});
							},
							content() {
								const mark = trigger.player.countMark('lqtz_anchao'),
									a = event.triggername;
								if (a == 'phaseAfter') {
									if (!trigger.player.getStat().damage) {
										trigger.player.addMark('lqtz_anchao', 1);
									} else {
										trigger.player.removeMark('lqtz_anchao', mark);
									}
								} else if (a == 'damageBegin3') {
									trigger.num += trigger.source.countMark('lqtz_anchao');
								} else if (a == 'phaseDrawBegin2') {
									trigger.num += mark;
								} else {
									game.countPlayer(function (current) {
										if (current.countMark('lqtz_anchao') > 0) {
											current.removeMark('lqtz_anchao', current.countMark('lqtz_anchao'));
										}
									});
								}
							},
						},
						lqtz_tiemian: {
							trigger: {
								target: 'useCardToTargeted',
							},
							audio: 'yizhong',
							forced: true,
							filter(event, player) {
								return event.card && event.card.name == 'sha' && get.color(event.card) == 'red';
							},
							content() {
								trigger.parent.excluded.add(player);
							},
						},
						lqtz_jizhou: {
							marktext: '噬',
							intro: {
								content: 'mark',
							},
							trigger: {
								global: 'phaseUseEnd',
							},
							forced: true,
							filter(event, player, target) {
								return player.getEnemies().includes(event.player);
							},
							content() {
								'step 0';
								player.judge();
								('step 1');
								const num = result.number;
								const next = trigger.player.chooseToDiscard(
									'弃置任意张点数之和大于' + get.cnNumber(num) + '的牌',
									function (card) {
										let num = 0;
										if (Array.isArray(ui.selected.cards)) {
											for (const i of ui.selected.cards) {
												num += i.number;
											}
										}
										return card.number + num < Infinity;
									},
									'he',
								);
								next.set('num', num);
								next.set('complexCard', true);
								next.set('selectCard', function () {
									let num = 0;
									if (Array.isArray(ui.selected.cards)) {
										for (const i of ui.selected.cards) {
											num += i.number;
										}
									}
									if (num > _status.event.num) {
										return ui.selected.cards.length;
									}
									return ui.selected.cards.length + 2;
								});
								next.set(
									'cardResult',
									(function () {
										const cards = trigger.player.getCards('he');
										const l = cards.length;
										const all = Math.pow(l, 2);
										const list = [];
										for (let i = 1; i < all; i++) {
											const array = [];
											for (let j = 0; j < l; j++) {
												if (Math.floor((i % Math.pow(2, j + 1)) / Math.pow(2, j)) > 0) {
													array.push(cards[j]);
												}
											}
											let numx = 0;
											for (const k of array) {
												numx += k.number;
											}
											if (numx > num) {
												list.push(array);
											}
										}
										if (list.length) {
											list.sort(function (a, b) {
												return get.value(a) - get.value(b);
											});
											return list[0];
										}
										return list;
									})(),
								);
								next.set('ai', function (card) {
									if (!_status.event.cardResult.includes(card)) {
										return 0;
									}
									return 6 - get.value(card);
								});
								('step 2');
								if (result.cards?.length) {
									player.addMark('lqtz_jizhou', result.cards.length);
								} else {
									trigger.player.loseHp();
								}
							},
						},
						lqtz_danshi: {
							trigger: {
								source: 'damageBegin2',
							},
							forced: true,
							filter(event, player, target) {
								return player.countMark('lqtz_jizhou') > 0;
							},
							content() {
								trigger.num += player.countMark('lqtz_jizhou');
								player.removeMark('lqtz_jizhou');
							},
						},
						lqtz_chihu: {
							trigger: {
								player: 'phaseDrawBegin2',
								source: 'damageBegin',
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'phaseDrawBegin2') {
									return !event.numFixed;
								}
								return true;
							},
							content() {
								if (trigger.name == 'phaseDraw') {
									trigger.num += 2;
								} else {
									trigger.num++;
								}
							},
						},
						lqtz_difu: {
							trigger: {
								global: 'phaseUseBegin',
							},
							filter(event, player, target) {
								return player.getEnemies().includes(event.player) && event.player.countCards('h') > event.player.maxHp;
							},
							forced: true,
							content() {
								trigger.player.chooseToDiscard('h', trigger.player.countCards('h') - trigger.player.maxHp, true);
							},
						},
						lqtz_zhennub: {
							trigger: {
								player: 'changeHp',
							},
							audio: 'lqtz_baonu',
							forced: true,
							filter(event, player) {
								return player.hp <= 8 && !player.storage.lqtz_zhennub;
							},
							content() {
								player.draw(4);
								if (player.hp < 8) {
									player.recover(8 - player.hp);
								}
								player.storage.lqtz_zhennub = true;
							},
						},
						lqtz_xingpan: {
							trigger: {
								player: 'phaseUseBegin',
							},
							forced: true,
							content() {
								'step 0';
								player.judge();
								('step 1');
								const list = game.filterPlayer(function (current) {
									return (
										player.getEnemies().includes(current) &&
										!game.hasPlayer(function (target) {
											return player.getEnemies().includes(target) && target.countCards('h') > current.countCards('h');
										})
									);
								});
								const list1 = game.filterPlayer(function (current1) {
									return (
										player.getEnemies().includes(current1) &&
										!game.hasPlayer(function (target1) {
											return player.getEnemies().includes(target1) && target1.hp > current1.hp;
										})
									);
								});
								if (result.color == 'red' && list.length) {
									const target = list.randomGet();
									player.line(target, 'green');
									target.give(target.getCards('h'), player, true);
								} else if (list1.length) {
									const targets = list1.randomGet();
									player.line(targets, 'green');
									targets.loseHp(targets.hp);
								}
							},
						},
						lqtz_dianwei: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							content() {
								game.countPlayer(function (current) {
									if (player.getEnemies().includes(current) && !current.countCards('e')) {
										player.useCard({ name: 'sha' }, current, false);
									}
									if (player.getEnemies().includes(current) && current.countCards('e')) {
										const eq = current.getCards('e');
										if (eq.length) {
											current.discard(eq.randomGet());
										}
									}
								});
							},
						},
						lqtz_xuanpan: {
							init(player) {
								if (!player.storage.lqtz_xuanpan1) {
									player.storage.lqtz_xuanpan1 = [];
								}
							},
							marktext: '宣',
							intro: {
								name: '宣判',
								mark(dialog, content, player) {
									let num = 0,
										num2 = 0,
										num3 = 0,
										num4 = 0;
									for (const i of player.storage.lqtz_xuanpan1) {
										if (i == 'damage') {
											num++;
										}
										if (i == 'discard') {
											num2++;
										}
										if (i == 'draw') {
											num3++;
										}
										if (i == 'recover') {
											num4++;
										}
									}
									if (num > 0) {
										dialog.addText(get.translation(player) + '已累计受到了' + num + '点伤害');
									}
									if (num2 > 0) {
										dialog.addText(get.translation(player) + '已累计弃置了' + num2 + '张牌');
									}
									if (num3 > 0) {
										dialog.addText(get.translation(_status.currentPhase) + '已累计摸了' + num3 + '张牌');
									}
									if (num4 > 0) {
										dialog.addText(get.translation(_status.currentPhase) + '已累计回复了' + num4 + '点体力');
									}
								},
							},
							trigger: {
								player: ['damageEnd', 'discardEnd'],
								global: ['drawEnd', 'recoverEnd', 'phaseAfter'],
							},
							_priority: 2021,
							filter(event, player, name) {
								if (player.getFriends(true).includes(_status.currentPhase)) {
									return false;
								}
								if (name == 'phaseAfter') {
									return true;
								}
								if (name == 'drawEnd' || name == 'recoverEnd') {
									return event.player == _status.currentPhase && event.num > 0;
								}
								return event.num > 0 || event.cards.length;
							},
							forced: true,
							content() {
								if (event.triggername != 'phaseAfter') {
									var num = trigger.num || trigger.cards.length;
									for (let i = 0; i < num; i++) {
										player.storage.lqtz_xuanpan1.push(trigger.name);
									}
									player.markSkill('lqtz_xuanpan');
								} else {
									var num = 0,
										num2 = 0,
										num3 = 0,
										num4 = 0;
									for (const i of player.storage.lqtz_xuanpan1) {
										if (i == 'damage') {
											num++;
										}
										if (i == 'discard') {
											num2++;
										}
										if (i == 'draw') {
											num3++;
										}
										if (i == 'recover') {
											num4++;
										}
									}
									if (num > 3) {
										trigger.player.damage([1, 2, 3, 4].randomGet());
									}
									if (num2 > 3) {
										trigger.player.discard(trigger.player.getCards('he').randomGets([1, 2, 3, 4].randomGet()));
									}
									if (num3 > 7) {
										player.draw([1, 2, 3, 4].randomGet());
									}
									if (num4 > 2) {
										player.recover([1, 2, 3, 4].randomGet());
									}
									player.storage.lqtz_xuanpan1 = [];
									player.unmarkSkill('lqtz_xuanpan');
									game.log(player, '清空所有记录的次数');
								}
							},
						},
						lqtz_beiming: {
							trigger: {
								player: 'dieBegin',
							},
							forced: true,
							filter(event, player) {
								return event.source != undefined;
							},
							content() {
								trigger.source.discard(trigger.source.getCards('h'));
							},
							ai: {
								threaten: 0.7,
							},
						},
						lqtz_qida: {
							intro: {
								content: 'mark',
							},
							trigger: {
								global: 'damageBefore',
							},
							forced: true,
							filter(event, player) {
								return event.num > 0;
							},
							content() {
								'step 0';
								if (
									trigger.player == player &&
									game.hasPlayer(function (current) {
										return player.getFriends().includes(current);
									})
								) {
									player.draw(trigger.num);
									trigger.cancel();
									player
										.chooseTarget('将伤害转移给一名其他友方角色', true, function (card, player, target) {
											return target != player && player.getFriends().includes(target);
										})
										.set('ai', function (target) {
											if (target.name == 'lqtz_qijiasi') {
												return 10;
											}
											return 1;
										});
								} else if (player.getFriends(true).includes(trigger.player)) {
									player.addMark('lqtz_qida', trigger.num);
								}
								('step 1');
								if (result.targets?.length) {
									if (trigger.source) {
										result.targets[0].damage(trigger.num, trigger.source);
									} else {
										result.targets[0].damage(trigger.num, 'nosource');
									}
								}
							},
						},
						lqtz_qijia: {
							trigger: {
								player: 'phaseDrawBegin',
							},
							forced: true,
							content() {
								trigger.num = player.maxHp;
							},
							ai: {
								revertsave: true,
								effect: {
									target(card, player, target) {
										if (!game.boss) {
											return;
										}
										if (card.name == 'tiesuo') {
											if (_status.event.player == game.boss) {
												return 'zeroplayertarget';
											}
											return 0.5;
										}
										if (get.tag(card, 'damage') || get.tag(card, 'recover')) {
											if (game.boss.isLinked() && get.damageEffect(game.boss, player, game.boss, 'fire') < 0) {
												if (
													game.hasPlayer(function (current) {
														return current.isEnemiesOf(game.boss) && current.isLinked();
													})
												) {
													return;
												}
												if (get.tag(card, 'natureDamage') && target.isLinked()) {
													return;
												}
											}
											if (target.isDying()) {
												if (player.isEnemiesOf(target) && player.hp >= -1) {
													return [0, 0, 0, 1];
												}
												return 'zeroplayertarget';
											}
											return -0.5;
										}
									},
								},
							},
						},
						_lqtz_qijiafuhuo: {
							//奇甲斯复活
							trigger: {
								player: 'phaseAfter',
							},
							filter(event, player) {
								const dead = game.dead.slice(0);

								for (const i of dead) {
									if (i.name == 'lqtz_qijiasi') {
										return true;
									}
								}

								return false;
							},
							_priority: 20,
							forced: true,
							popup: false,
							content() {
								const dead = game.dead.slice(0);

								for (const i of dead) {
									if (i.parentNode == player.parentNode && i.name == 'lqtz_qijiasi') {
										i.revive(i.maxHp);
										i.draw(4, false);
										i.gainMaxHp();
										i.hp = 2;
										i.update();
									}
								}
							},
						},
						lqtz_jingying: {
							trigger: {
								player: 'damageEnd',
							},
							filter(event, player) {
								return event.num > 0;
							},
							forced: true,
							//round:1,//轮次
							usable: 1,
							content() {
								player.recover();
								player.draw();
								if (trigger.source && player.getEnemies().includes(trigger.source)) {
									let d = [];
									const b = trigger.source.getSkills(true, false);

									for (const l of b) {
										if (!lib.translate[l] || !lib.translate[l + '_info']) {
											continue;
										}
										d.push(l);
									}

									d = d.randomGet();
									player.addTempSkill(d, { player: 'phaseAfter' });
								}
							},
						},
						lqtz_zuidun: {
							global: 'lqtz_zuidun1',
							trigger: {
								player: 'damageBegin4',
							},
							forced: true,
							filter(event, player, target) {
								return event.num > 1;
							},
							content() {
								if (trigger.source && player.getEnemies().includes(trigger.source)) {
									trigger.source.damage(trigger.num - 1);
								}
								trigger.num = 1;
								game.log(player, '将伤害变成1');
							},
						},
						lqtz_zuidun1: {
							mod: {
								targetEnabled(card, player, target) {
									const boss = game.findPlayer(function (current) {
										return current.hasSkill('lqtz_zuidun');
									});
									if (boss && player.getEnemies().includes(boss) && player.getEnemies().includes(target) && !target.hasSkill('lqtz_zuidun')) {
										return false;
									}
								},
							},
						},
						lqtz_zuiren: {
							trigger: {
								player: 'damageEnd',
							},
							group: 'lqtz_zuiren_1',
							forced: true,
							filter(event, player) {
								return event.num > 0;
							},
							content() {
								'step 0';
								event.a = trigger.num;
								('step 1');
								event.a--;
								const list = get.inpile('trick').randomGets(3);
								const dialog = ui.create.dialog('使用一张锦囊牌', [list, 'vcard'], 'hidden');
								player.chooseButton(dialog, true).set('ai', function (button) {
									return get.value({ name: button.link[2] });
								});
								('step 2');
								if (result.bool) {
									event.card = result.buttons[0].link[2];
									if (
										game.findPlayer(function (target) {
											return player.canUse(event.card, target);
										})
									) {
										player.chooseUseTarget(event.card, true);
									} else {
										player.gain(game.createCard(event.card), 'draw2');
									}
								}
								('step 3');
								if (event.a > 0) {
									event.goto(1);
								}
							},
							subSkill: {
								1: {
									trigger: {
										source: 'damageBegin',
									},
									forced: true,
									filter(event, player) {
										return player.getEnemies().includes(event.player);
									},
									content() {
										'step 0';
										player.judge(function (card) {
											const type = get.type2(card);
											return type != 'basic';
											switch (type) {
												case 'basic':
													return 0;
												default:
													return 2;
											}
										});
										('step 1');
										if (result.bool === true) {
											trigger.num += 2;
										}
									},
								},
							},
						},
						lqtz_shenou: {
							trigger: {
								global: 'damageEnd',
							},
							forced: true,
							filter(event, player) {
								return event.num > 0 && event.source && (event.source.name == 'lqtz_xiruonuosiyoushou' || event.source.name == 'lqtz_xiruonuosizuoshou');
							},
							content() {
								player.damage(trigger.num);
							},
						},
						lqtz_baoxing: {
							trigger: {
								player: 'damageEnd',
							},
							filter(event, player) {
								return event.num > 0;
							},
							forced: true,
							content() {
								'step 0';
								player.draw(trigger.num);
								event.count = Math.min(trigger.num, 9);
								('step 1');
								event.count--;
								player.judge();
								('step 2');
								if (result.suit == 'heart') {
									player.draw();
									player.recover();
								} else if (result.suit == 'club') {
									game.countPlayer(function (current) {
										if (player.getEnemies().includes(current)) {
											if (!current.isLinked()) {
												current.link();
											} else {
												const hs = current.getCards('he');
												if (hs.length) {
													current.discard(hs.randomGet());
												}
											}
										}
									});
								} else {
									player.chooseTarget(true, result.suit == 'diamond' ? '获得一名敌方角色区域内各一张牌.' : '对一名敌方角色造成一点雷电伤害.', function (card, player, target) {
										if (result.suit == 'diamond' && !target.countGainableCards(player, 'hej')) {
											return false;
										}
										return player.getEnemies().includes(target);
									}).ai = function (target) {
										if (result.suit == 'diamond') {
											if (get.attitude(player, target) < 0) {
												return target.countCards('e');
											}
											return -get.attitude(player, target);
										}
										return get.damageEffect(target, player, player, 'thunder');
									};
								}
								event.a = result.suit;
								('step 3');
								if (result.targets?.length) {
									if (event.a == 'spade') {
										result.targets[0].damage('thunder');
									}
									if (event.a == 'diamond') {
										let num = 0;
										if (result.targets[0].countCards('h')) {
											num++;
										}
										if (result.targets[0].countCards('e')) {
											num++;
										}
										if (result.targets[0].countCards('j')) {
											num++;
										}
										if (num > 0) {
											player.gainPlayerCard(result.targets[0], num, 'hej', true).set('filterButton', function (button) {
												for (const i of ui.selected.buttons) {
													if (get.position(button.link) == get.position(i.link)) {
														return false;
													}
												}

												return true;
											});
										}
									}
								}
								('step 4');
								if (event.count > 0) {
									event.goto(1);
								}
							},
						},
						lqtz_shenhuang: {
							shaRelated: true,
							trigger: {
								player: 'useCardToBefore',
							},
							forced: true,
							filter(event, player) {
								return event.card && event.card.name == 'sha';
							},
							content() {
								'step 0';
								event.cards = get.cards(10);
								('step 1');
								game.log(player, '观看了牌堆顶10张牌');
								const chooseButton = player.chooseButton(['弃置一张杀', event.cards]);
								chooseButton.set('ai', function (button) {
									const card = button.link;
									return card.number;
								});
								chooseButton.set('filterButton', function (button) {
									const card = button.link;
									return card.name == 'sha';
								});
								('step 2');
								if (result.links?.length) {
									result.links[0].discard();
									game.log(player, '弃置了', result.links[0]);
									const card = result.links[0];
									trigger.baseDamage = card.number;
								} else {
									trigger.cancel();
									if (player.hasSkill('lqtz_pangqu')) {
										player.addMark('lqtz_pangqu');
									}
									if (player.countMark('lqtz_pangqu') > 3) {
										player.die();
									}
								}
								('step 3');
								while (event.cards.length) {
									ui.cardPile.insertBefore(event.cards.pop(), ui.cardPile.firstChild);
								}
							},
						},
						lqtz_pangqu: {
							marktext: '暗',
							intro: {
								name: '暗黑',
								content: 'mark',
							},
							trigger: {
								player: 'phaseAfter',
							},
							forced: true,
							filter(event, player, name) {
								return !player.getStat().damage;
							},
							content() {
								player.addMark('lqtz_pangqu');
								if (player.countMark('lqtz_pangqu') > 3) {
									player.die();
								}
							},
						},
						lqtz_anxi: {
							trigger: {
								player: ['damageEnd', 'loseHpEnd'],
							},
							filter(event, player) {
								return event.num > 0;
							},
							forced: true,
							async content(event, trigger, player) {
								const { cards } = await player.draw(trigger.num * 3).forResult();
								if (cards?.length) {
									for (const i of cards) {
										if (player.hasUseTarget(i)) {
											await player.chooseToUse((card) => card == i, '是否使用一张牌？');
										}
									}
								}
							},
						},
						lqtz_xiequ: {
							trigger: {
								player: 'dieBefore',
							},
							charlotte: true,
							fixed: true,
							forced: true,
							filter(event, player) {
								return player.countDiscardableCards(player, 'he') > 2;
							},
							content() {
								'step 0';
								player.chooseToDiscard(2, true, 'he');
								('step 1');
								if (result.bool) {
									trigger.cancel();
									if (player.maxHp < 2) {
										player.maxHp = 2;
									}
									if (player.hp < 2) {
										player.hp = 2;
									}
									player.update(); //刷新
									const evt = _status.event.getParent('phaseUse');
									if (evt && evt.name == 'phaseUse' && _status.currentPhase != player) {
										player.addSkill('lqtz_xiequ_count');
										evt.skipped = true;
									}
								}
							},
							subSkill: {
								count: {
									trigger: {
										global: 'phaseAfter',
									},
									forced: true,
									popup: false,
									charlotte: true,
									_priority: -80,
									content() {
										player.removeSkill('lqtz_xiequ_count');
									},
								},
							},
						},
						lqtz_shenzong: {
							enable: 'phaseUse',
							selectCard: 1,
							filterCard(card, player) {
								return ['wuzhong', 'shunshou', 'guohe', 'huogong', 'nanman', 'tiesuo', 'jiedao', 'juedou', 'taoyuan', 'wugu', 'wanjian'].includes(card.name);
							},
							prompt: '选择一张手牌弃置',
							position: 'h',
							filter(event, player) {
								return player.countCards('h') > 0;
							},
							check(card) {
								if (_status.event.player.countCards('h') > 0) {
									if (card.name == 'tiesuo') {
										return 200 - get.value(card);
									}
									return 100 - get.value(card);
								}
							},
							filterTarget(card, player, target) {
								const name = ui.selected.cards[0].name;
								if (name == 'shunshou') {
									return target.countCards('hej') && target != player;
								}
								if (name == 'guohe') {
									return player.getEnemies().includes(target) && target.countCards('hej');
								}
								if (
									name == 'tiesuo' &&
									game.hasPlayer(function (current) {
										return player.getEnemies().includes(current) && !current.isLinked();
									})
								) {
									return player.getEnemies().includes(target) && !target.isLinked();
								}
								if (name == 'jiedao') {
									return target.countCards('e') && target != player;
								}
								return true;
							},
							//出牌阶段,你可以弃置一张以下牌名的手牌,根据弃置的牌名发动效果:<br>【决斗】:你对一名角色造成一点雷电伤害.<br>【火攻】:你对一名角色造成一点火焰伤害.<br>【无中生有】:你摸4张牌.<br>【顺手牵羊】:你获得1~2名其他角色合计2张牌.<br>【过河拆桥】:你弃置所以敌方角色各一张牌.<br>【借刀杀人】:你获得一名其他角色装备区里的一张装备牌.<br>【桃园结义】:你回复2点体力,回复超出的值改为获得等量的护甲.<br>【五谷丰登】:你翻开牌堆顶4张牌,并获得其中两张牌.<br>【南蛮入侵】:你视为使用一张【惊雷闪】.<br>【万箭齐发】:你视为使用一张【炽羽袭】.<br>【铁索连环】:你令所以敌方角色横置,若敌方角色均已横置,则你改为摸两张牌,若你已横置,则你解除横置
							selectTarget() {
								if (ui.selected.cards && ui.selected.cards[0]) {
									//QQQ
									const name = ui.selected.cards[0].name;
									if (name == 'shunshou') {
										return [1, 2];
									}
									if (
										name == 'guohe' ||
										(name == 'tiesuo' &&
											game.hasPlayer(function (current) {
												return _status.event.player.getEnemies().includes(current) && !current.isLinked();
											}))
									) {
										return [-1, -1];
									}
									if (['jiedao', 'juedou', 'huogong'].includes(name)) {
										return [1, 1];
									}
								}
								return [0, 0];
							},
							content() {
								'step 0';
								if (cards[0].name == 'wugu') {
									event.car = get.cards(4);
									game.log(player, '观看了牌堆顶四张牌');
									player.chooseButton(['选择两张牌获得', event.car], 2, true).set('ai', function (button) {
										return get.value(button.link);
									});
								}
								('step 1');
								if (result.links?.length) {
									player.gain(result.links, 'gain2');
									for (const i of result.links) {
										event.car.remove(i);
									}
								}
								('step 2');
								const name = cards[0].name;
								while (event.car && event.car.length) {
									ui.cardPile.insertBefore(event.car.pop(), ui.cardPile.firstChild);
								}
								if (name == 'wuzhong') {
									player.draw(4);
								}
								if (name == 'nanman') {
									player.chooseUseTarget('视为使用一张【惊雷闪】', { name: 'jingleishan' }, true);
								}
								if (name == 'wanjian') {
									player.chooseUseTarget('视为使用一张【炽羽袭】', { name: 'chiyuxi' }, true);
								}
								if (name == 'jiedao') {
									player.gainPlayerCard(true, target, 'e');
								}
								if (name == 'juedou') {
									target.damage('thunder');
								}
								if (name == 'huogong') {
									target.damage('fire');
								}
								if (name == 'shunshou') {
									player.gainPlayerCard([1, 3 - targets.length], true, target, 'he');
								}
								if (name == 'guohe') {
									player.discardPlayerCard(target, true, 'he');
								}
								if (name == 'taoyuan') {
									const num = player.maxHp - player.hp;
									if (num == 0) {
										player.changeHujia(2);
									} else if (num == 2) {
										player.recover(2);
									} else {
										player.changeHujia();
										player.recover();
									}
								}
								if (name == 'tiesuo') {
									if (player.isLinked()) {
										player.link();
									}
									if (targets.length) {
										target.link();
									} else {
										player.draw(2);
									}
								}
							},
							ai: {
								order: 999,
								result: {
									target(player, target) {
										const name = ui.selected.cards[0].name;
										if (['shunshou', 'jiedao', 'juedou', 'huogong', 'tiesuo', 'guohe'].includes(name)) {
											return -1;
										}
									},
									player: 2,
								},
							},
						},
						lqtz_shenyi: {
							trigger: {
								global: ['useCard', 'respond'],
							},
							forced: true,
							filter(event, player) {
								if (player.getFriends().includes(event.player)) {
									return false;
								}
								if (player == event.player) {
									return false;
								}
								if (Math.random() > 97 / 100) {
									return false;
								}
								return (event.card && event.card.name == 'shan') || event.card.name == 'wuxie';
							},
							content() {
								trigger.player.damage('ice');
							},
						},
						lqtz_shenpo: {
							trigger: {
								player: ['turnOverBefore', 'phaseJudgeBegin'],
							},
							_priority: 20,
							forced: true,
							filter(event, player, name) {
								return name == 'turnOverBefore' ? !player.isTurnedOver() : true;
							},
							content() {
								trigger.cancel();
								player.draw(2);
							},
						},
						lqtz_shenyan: {
							trigger: {
								global: 'useCardAfter',
							},
							forced: true,
							filter(event, player) {
								if (Math.random() > 79 / 100) {
									return false;
								}
								return player != event.player && get.type(event.card) != 'equip';
							},
							content() {
								'step 0';
								const cards = game.createCard(trigger.card);
								player.gain(cards, 'gain2');
								('step 1');
								player.chooseToUse(function (card) {
									return game.hasPlayer(function (current) {
										return player.canUse(card, current);
									});
								}, '是否使用一张牌？');
							},
						},
						lqtz_zuijiu: {
							trigger: {
								source: 'damageBegin1',
							},
							audio: 'oljiuchi',
							forced: true,
							filter(event, player) {
								return event.parent.name == 'sha';
							},
							content() {
								trigger.num++;
							},
						},
						lqtz_guanshi: {
							trigger: {
								player: 'turnOverBefore',
							},
							_priority: 20,
							forced: true,
							filter(event, player) {
								return !player.isTurnedOver();
							},
							content() {
								'step 0';
								player.judge(function (card) {
									return card.suit == 'diamond' ? 0 : 1;
								});
								('step 1');
								if (result.color == 'black') {
									trigger.cancel();
									event.finish();
								}
								if (result.suit == 'heart') {
									player
										.chooseTarget('是否令一名其他角色翻面', function (card, player, target) {
											return target != player;
										})
										.set('ai', function (target) {
											return -get.attitude(_status.event.player, target);
										});
								}
								('step 2');
								if (result.targets?.length) {
									result.targets[0].turnOver();
								}
							},
							group: ['lqtz_guanshi_1'],
							subSkill: {
								1: {
									trigger: {
										target: 'useCardToTargeted',
									},
									_priority: 20,
									forced: true,
									filter(event, player) {
										return get.type(event.card) == 'delay';
									},
									content() {
										'step 0';
										player.judge(function (card) {
											return card.suit == 'diamond' ? 0 : 1;
										});
										('step 1');
										if (result.color == 'black') {
											trigger.cancel();
											event.finish();
										}
										if (result.suit == 'heart') {
											player
												.chooseTarget('是否将' + get.translation(event.card) + '转移给其他角色', function (card, player, target) {
													return target != player;
												})
												.set('ai', function (target) {
													return -get.attitude(_status.event.player, target);
												});
										}
										('step 2');
										if (result.targets?.length) {
											trigger.targets.push(result.targets[0]);
											trigger.parent.excluded.add(player);
										}
									},
								},
							},
						},
						lqtz_huoluan: {
							trigger: {
								player: 'phaseUseBegin',
							},
							forced: true,
							audio: 'taoluan',
							content() {
								'step 0';
								event.count = 4;
								('step 1');
								event.count--;
								const list = [];
								get.cardPile(function (card) {
									if (get.color(card) == 'black' && get.type(card) == 'trick' && card.name != 'wuxie') {
										list.push(card);
									}
								});
								const card = list.randomGet();
								player.gain(card, 'draw2');
								if (!player.hasUseTarget(card)) {
									player.discard(card);
									event.goto(4);
								} else {
									event.card = card;
								}
								('step 2');
								const next = player.chooseToUse();
								next.filterCard = function (car) {
									return car == event.card;
								};
								next.prompt = '是否使用' + get.translation(event.card) + '？';
								('step 3');
								if (!result.bool) {
									player.discard(event.card);
								}
								('step 4');
								if (event.count) {
									event.goto(1);
								}
							},
						},
						lqtz_xumou: {
							intro: {
								content: '你下次对敌方角色造成的伤害+#',
							},
							trigger: {
								player: 'damageEnd',
							},
							forced: true,
							filter(event, player) {
								return player.countMark('lqtz_xumou') < 4;
							},
							content() {
								player.addMark('lqtz_xumou', 1);
							},
							group: 'lqtz_xumou_1',
							subSkill: {
								1: {
									trigger: {
										source: 'damageBegin',
									},
									forced: true,
									filter(event, player) {
										return player.countMark('lqtz_xumou') && player.getEnemies().includes(event.player);
									},
									content() {
										trigger.num += player.storage.lqtz_xumou;
										player.removeMark('lqtz_xumou', player.storage.lqtz_xumou);
									},
								},
							},
						},
						lqtz_jiquan: {
							trigger: {
								player: 'phaseBegin',
							},
							forced: true,
							content() {
								player.drawTo(8);
							},
						},
						lqtz_luanzheng: {
							trigger: {
								global: 'damageEnd',
							},
							forced: true,
							filter(event, player) {
								if (player.getEnemies().includes(event.source) && event.source != player && event.source == _status.currentPhase && event.source.getStat().damage && event.source.getStat().damage > 5) {
									return true;
								}
								return false;
							},
							content() {
								trigger.source.addTempSkill('lqtz_luanzheng_2');
							},
							group: 'lqtz_luanzheng_1',
							subSkill: {
								1: {
									trigger: {
										global: 'gainEnd',
									},
									filter(event, player) {
										if (player.getEnemies().includes(event.player) && event.player != player && event.player == _status.currentPhase && event.player.countCards('h') > 10) {
											return true;
										}
										return false;
									},
									content() {
										trigger.player.addTempSkill('lqtz_luanzheng_2');
									},
								},
								2: {
									mark: true,
									mod: {
										cardEnabled2(card) {
											if (get.position(card) == 'h') {
												return false;
											}
										},
									},
									intro: {
										content: '不能使用或打出手牌',
									},
								},
							},
						},
						lqtz_tieji: {
							mod: {
								aiOrder(player, card, num) {
									if (card.name == 'sha') {
										return 20;
									}
								},
							},
							shaRelated: true,
							audio: 'retieji',
							trigger: {
								player: 'useCardToPlayered',
							},
							check(event, player) {
								return get.attitude(player, event.target) <= 0;
							},
							filter(event, player) {
								return event.card && event.card.name == 'sha';
							},
							logTarget: 'target',
							content() {
								'step 0';
								trigger.target.addTempSkill('lqtz_tieji2');
								player.judge();
								('step 1');
								if (result.color == 'red') {
									if (trigger.target.countCards('he', { color: 'red' })) {
										trigger.target.chooseToDiscard('he', true, '弃置一张红色牌', function (card) {
											return get.color(card) == 'red';
										});
									}
									trigger.parent.directHit.add(trigger.target);
								}
								if (result.color == 'black') {
									const target = trigger.target,
										num = target.countCards('h', 'shan');
									target
										.chooseToDiscard('he', 2, '弃置两张黑色牌,否则此杀你不能闪避', function (card) {
											return get.color(card) == 'black';
										})
										.set('ai', function (card) {
											if (num == 0) {
												return 0;
											}
											if (card.name == 'shan') {
												return num > 1 ? 2 : 0;
											}
											return 8 - get.value(card);
										})
										.set('num', num)
										.set('color', 'blsck');
								}
								('step 2');
								if (!result.bool) {
									trigger.parent.directHit.add(trigger.target);
								}
							},
						},
						lqtz_xiongshi: {
							enable: 'phaseUse',
							audio: 'tieji',
							discard: false,
							filter(event, player) {
								return player.countCards('h') && !player.countCards('h', { name: 'sha' });
							},
							prepare: 'throw',
							position: 'h',
							filterCard: true,
							filterTarget(card, player, target) {
								if (player == target) {
									return false;
								}
								return player.canUse({ name: 'sha', cards: ui.selected.cards }, target, false, false);
							},
							selectCard: -1,
							content() {
								const next = player.useCard({ name: 'sha' }, target, cards, false);
								next.animate = false;
								next.set('oncard', function () {
									_status.event.baseDamage++;
								});
							},
							ai: {
								result: {
									target(player, target) {
										return get.effect(target, { name: 'sha' }, player, target);
									},
								},
								order(card, player) {
									if (player.countCards('h') == 1) {
										return 20;
									}
									return 1;
								},
							},
						},
						lqtz_mashu: {
							mod: {
								globalFrom(from, to, distance) {
									return distance - 1;
								},
							},
							trigger: {
								player: 'drawBefore',
							},
							_priority: -100,
							forced: true,
							popup: false,
							filter(event, player) {
								return Math.random() < 4 / 4;
							},
							content() {
								let list = [];
								get.cardPile2(function (card) {
									if (card.name != 'sha') {
										list.push(card);
									}
								});
								list = list.randomGets(trigger.num);
								while (list.length) {
									ui.cardPile.insertBefore(list.pop(), ui.cardPile.firstChild);
								}
							},
							group: 'lqtz_mashu_1',
							subSkill: {
								1: {
									trigger: {
										player: 'phaseEnd',
									},
									forced: true,
									filter(event, player) {
										return !player.countCards('h', { name: 'sha' }) && player.getStat().damage && player.getStat().damage > 0;
									},
									content() {
										player.draw(player.getStat().damage);
									},
								},
							},
						},
						lqtz_qianji: {
							enable: 'phaseUse',
							audio: 'shichou',
							selectTarget: 2,
							filter(event, player) {
								return (
									game.hasPlayer(function (current) {
										return !current.hasSkill('lqtz_qianji_1') && current != player;
									}) &&
									game.hasPlayer(function (current1) {
										return current1.hasSkill('lqtz_tieji2') && current1 != player;
									}) &&
									game.hasPlayer(function (current2) {
										return !current2.hasSkill('lqtz_tieji2') && !current2.hasSkill('lqtz_qianji_1') && current2 != player;
									})
								);
							},
							filterTarget(card, player, target) {
								if (player == target) {
									return false;
								}
								if (ui.selected.targets.length) {
									if (ui.selected.targets[0].hasSkill('lqtz_tieji2')) {
										return !target.hasSkill('lqtz_tieji2') && !target.hasSkill('lqtz_qianji_1');
									} else {
										return target.hasSkill('lqtz_tieji2');
									}
								}
								return target.hasSkill('lqtz_tieji2') || !target.hasSkill('lqtz_qianji_1');
							},
							multitarget: true,
							multiline: true,
							complexSelect: true,
							content() {
								if (targets[0].hasSkill('lqtz_tieji2')) {
									targets[0].removeSkill('lqtz_tieji2');
									targets[0].addTempSkill('lqtz_qianji_1');
									targets[1].addTempSkill('lqtz_tieji2');
									player.useCard({ name: 'sha' }, targets[1]);
								} else if (targets[1].hasSkill('lqtz_tieji2')) {
									targets[1].removeSkill('lqtz_tieji2');
									targets[1].addTempSkill('lqtz_qianji_1');
									targets[0].addTempSkill('lqtz_tieji2');
									player.useCard({ name: 'sha' }, targets[0]);
								}
							},
							ai: {
								order: 20,
								threaten: 3,
								expose: 0.9,
								result: {
									target: -1,
								},
							},
							subSkill: {
								1: {
									forced: true,
									charlotte: true,
								},
							},
						},
						lqtz_tieji2: { inherit: 'baiban' },
						lqtz_yingying: {
							audio: 'shiren',
							fixed: true,
							charlotte: true,
						},
						_lqtz_yingying: {
							trigger: {
								global: 'useCardEnd',
							},
							forced: true,
							filter(event, player) {
								if (player != _status.currentPhase && !player.countDiscardableCards(player, 'he')) {
									return false;
								}
								return [player.name, player.name1, player.name2].includes('lqtz_langqinbiao') && event.cards.length && event.getParent(2).name != '_lqtz_abaaba' && get.type(event.card) != 'equip' && get.type(event.card) != 'delay';
							},
							check(event, player) {
								if (['sha', 'jiu', 'wuzhong', 'nanman', 'shunshou', 'guohe', 'tiesuo', 'wanjian', 'juedou', 'huogong'].includes(event.card.name)) {
									return true;
								}
								return false;
							},
							content() {
								'step 0';
								if (player != _status.currentPhase) {
									player.chooseToDiscard('he', '###是否发动【嘤嘤嘤】？###弃置一张牌,将' + get.translation(trigger.cards) + '置于牌堆底.').set('ai', function (card) {
										if (['sha', 'jiu', 'wuzhong', 'nanman', 'shunshou', 'guohe', 'tiesuo', 'wanjian', 'juedou', 'huogong'].includes(trigger.card.name)) {
											return 1000 - get.value(card);
										}
										return 0;
									});
								} else {
									player.chooseBool('###是否发动【嘤嘤嘤】？###将' + get.translation(trigger.cards) + '置于牌堆底.').set('ai', function () {
										if (['sha', 'jiu', 'wuzhong', 'nanman', 'shunshou', 'guohe', 'tiesuo', 'wanjian', 'juedou', 'huogong'].includes(trigger.card.name)) {
											return true;
										}
										return false;
									});
								}
								('step 1');
								if (result.bool) {
									event.cards = trigger.cards.filterInD();
									if (event.cards.length > 1) {
										player
											.chooseButton(true, event.cards.length, ['按顺序将卡牌置于牌堆底(先选择的在上)', event.cards])
											.set('ai', function (button) {
												const value = get.value(button.link);
												if (_status.event.reverse) {
													return value;
												}
												return -value;
											})
											.set('reverse', _status.currentPhase && _status.currentPhase.next ? get.attitude(player, _status.currentPhase.next) > 0 : false);
									}
								} else {
									event.finish();
								}
								('step 2');
								if (result.links?.length) {
									cards = result.links.slice(0);
								}
								while (cards.length) {
									const card = cards.pop();
									if (get.position(card, true) == 'o') {
										card.fix();
										ui.cardPile.appendChild(card, ui.cardPile.firstChild);
										game.log(player, '将', card, '置于牌堆底');
									}
								}
								game.updateRoundNumber();
							},
						},
						lqtz_abaaba: {
							charlotte: true,
							audio: 'zunwei',
							fixed: true,
						},
						_lqtz_abaaba: {
							trigger: {
								player: 'phaseAfter',
							},
							filter(event, player) {
								return [player.name, player.name1, player.name2].includes('lqtz_langqinbiao');
							},
							forced: true,
							content() {
								'step 0';
								('step 1');
								event.card = get.bottomCards()[0];
								player.showCards(event.card);
								if (!player.hasUseTarget(event.card)) {
									player.gain(event.card);
									event.finish();
								}
								('step 2');
								const next = player.chooseUseTarget(event.card, true, false);
								if (get.info(event.card).updateUsable == 'phaseUse') {
									next.addCount = false;
								}
								('step 3');
								if (result.bool) {
									event.goto(1);
								}
							},
						},
						lqtz_miaowu: {
							audio: 'rehongyan',
							charlotte: true,
							fixed: true,
						},
						_lqtz_miaowu: {
							trigger: {
								player: 'dieBefore',
							},
							forced: true,
							filter(event, player) {
								return [player.name, player.name1, player.name2].includes('lqtz_langqinbiao');
							},
							content() {
								'step 0';
								event.card = get.bottomCards()[0];
								player
									.chooseControl('basic', 'trick', 'equip', '其他')
									.set('ai', function () {
										if (get.type2(event.card) != '其他') {
											return get.type(event.card);
										}
										return '其他';
									})
									.set('prompt', '###【喵呜】###猜测一种牌类型并获得牌堆底一张牌,若猜测的类型与该牌类型相同则你不死亡');
								('step 1');
								game.log(player, '猜测的类型为', get.translation(result.control) || result.control, '牌');
								player.showCards(event.card);
								if ((result.control == '其他' && !['basic', 'trick', 'equip'].includes(get.type2(event.card))) || get.type2(event.card) == result.control) {
									player.popup('洗具', 'metal');
									trigger.cancel();
									player.maxHp = 2;
									player.update(); //刷新
									if (player.hp < 2) {
										player.recover(2 - player.hp);
									}
									player.draw();
								}
								player.gain(event.card, 'draw2');
							},
						},
						lqtz_chiliu: {
							audio: 'zhente',
							charlotte: true,
							fixed: true,
						},
						_lqtz_chiliu: {
							mod: {
								globalFrom(from, to, distance) {
									if (from.hasSkill('lqtz_chiliu')) {
										return distance - 1;
									}
								},
								maxHandcard(player, num) {
									if ([player.name, player.name1, player.name2].includes('lqtz_langqinbiao')) {
										return 2 + num;
									}
								},
							},
							trigger: {
								player: ['turnOverBefore', 'phaseBegin', 'dying'],
							},
							_priority: 20,
							forced: true,
							filter(event, player, name) {
								if (![player.name, player.name1, player.name2].includes('lqtz_langqinbiao')) {
									return false;
								}
								return name == 'turnOverBefore' ? !player.isTurnedOver() : true;
							},
							content() {
								if (event.triggername == 'turnOverBefore') {
									trigger.cancel();
								} else if (event.triggername == 'dying') {
									player.die();
								} else {
									player.damage('thunder');
									player.gain(player.getCards('j'), 'draw2');
								}
							},
						},
						lqtz_lingba: {
							trigger: {
								player: 'phaseBegin',
							},
							forced: true,
							filter(event, player) {
								return !game.hasPlayer(function (current) {
									return current.countCards('h') > player.countCards('h');
								});
							},
							content() {
								if (player.countCards('h') >= player.hp * 2) {
									game.countPlayer(function (current2) {
										if (current2 != player && player.getEnemies().includes(current2)) {
											current2.damage(2);
										}
									});
								} else {
									const list = game.filterPlayer(function (current) {
										return player.getEnemies().includes(current);
									});
									if (list.length) {
										const target = list.randomGet();
										player.line(target, 'green');
										target.damage(2);
									}
								}
							},
						},
						lqtz_yishen: {
							trigger: {
								player: 'recoverBegin',
							},
							check(event, player) {
								return _status.event.player.hp > 3;
							},
							filter(event, player) {
								return game.hasPlayer(function (current) {
									return player.getEnemies().includes(current) && current.countCards('e');
								});
							},
							content() {
								trigger.cancel();
								game.countPlayer(function (current) {
									if (current.countCards('e') && player.getEnemies().includes(current)) {
										const card = current.getCards('e').randomGet();
										player.gain(card, current);
										current.$giveAuto(card, player);
									}
								});
							},
						},
						lqtz_langgu: {
							trigger: {
								player: 'gainEnd',
							},
							usable: 1,
							filter(event, player) {
								if (!event.source || !event.source.countCards('h') || event.source == player || !event.source.isIn()) {
									return false;
								}
								const evt = event.getl(event.source);
								return evt && evt.cards2 && evt.cards2.length;
							},
							check(event, player) {
								return -get.attitude(player, event.source);
							},
							logTarget: 'source',
							forced: true,
							content() {
								'step 0';
								player.judge(function (card) {
									return get.color(card) == 'black' ? 1 : 0;
								});
								('step 1');
								if (result.color == 'black') {
									const hs = trigger.source.getCards('h');
									if (hs.length) {
										trigger.source.discard(hs.randomGet());
									}
									player.getStat('triggerSkill').lqtz_langgu--;
								}
							},
						},
						lqtz_yuanlu: {
							trigger: {
								source: 'damageBegin',
							},
							filter(event, player) {
								return player.getEnemies().includes(event.player) && event.card && get.type(event.card) == 'trick';
							},
							check(event, player) {
								return event.player.countCards('h') > 1 && event.player.hp > 1;
							},
							content() {
								trigger.cancel();
								player.draw();
								player.damage(trigger.player);
							},
						},
						lqtz_shenji: {
							mod: {
								selectTarget(card, player, range) {
									if (Array.isArray(range) && range[1] == -1) {
										return;
									}
									if (card.name == 'sha') {
										range[1] += 2;
									}
								},
								cardUsable(card, player, num) {
									if (card.name == 'sha') {
										return num + 2;
									}
								},
							},
							trigger: {
								player: ['phaseJudgeBegin', 'phaseDrawBegin2'],
							},
							audio: 'shenji',
							forced: true,
							filter(event, player, name) {
								return name == 'phaseDrawBegin2' ? !event.numFixed : player.countCards('j') > 0 && player.countCards('h') > 1;
							},
							content() {
								'step 0';
								if (event.triggername == 'phaseDrawBegin2') {
									trigger.num += 2;
								} else {
									player.chooseToDiscard(2, '是否弃置两张手牌来弃置判定区的牌？').set('ai', function (card) {
										return 1;
									})('step 1');
								}
								if (result.bool) {
									player.discard(player.getCards('j'));
								}
							},
						},
						lqtz_zhankai: {
							trigger: {
								player: 'damageBegin4',
							},
							filter(event, player) {
								return event.num > 2;
							},
							audio: 'shenqu',
							forced: true,
							content() {
								trigger.num = 2;
								game.log(player, '将伤害变成2');
								player.draw(2);
							},
						},
						lqtz_baonue: {
							trigger: {
								player: 'phaseBegin',
							},
							filter(event, player) {
								return player.hp < player.maxHp;
							},
							audio: 'olbaonue',
							forced: true,
							content() {
								'step 0';
								let num = player.maxHp - player.hp;
								if (num > 5) {
									num = 5;
								}
								player.draw(num);
								player
									.chooseTarget([1, num], '对最多' + get.translation(num) + '名敌方角色造成一点伤害.', function (card, player, target) {
										return true;
									})
									.set('ai', function (target) {
										return -get.attitude(_status.event.player, target);
									});
								('step 1');
								if (result.targets?.length) {
									event.targets = result.targets.slice(0).sortBySeat();
								} else {
									event.goto(3);
								}
								('step 2');
								if (event.targets && event.targets.length) {
									event.targets.shift().damage();
									event.redo();
								}
								('step 3');
								player.loseHp();
							},
						},
						lqtz_qubu: {
							trigger: {
								global: 'useCard',
							},
							audio: 'baonue2',
							filter(event, player) {
								return (event.player == player || player.getFriends().includes(event.player)) && event.card && event.card.name == 'sha';
							},
							forced: true,
							content() {
								'step 0';
								player.judge(function (card) {
									return get.color(card) == 'black' ? 1 : 0;
								});
								('step 1');
								if (result.color == 'black') {
									event.targets = trigger.targets.slice(0).sortBySeat();
								}
								('step 2');
								if (event.targets && event.targets.length) {
									event.targets.shift().damage();
									event.redo();
								}
							},
						},
						lqtz_jianzheng: {
							audio: 'jianzheng',
							trigger: { global: 'useCardToPlayer' },
							filter(event, player) {
								if (!player.countCards('h')) {
									return false;
								}
								return event.player != player && event.card.name == 'sha' && !event.targets.includes(player) && event.player.inRange(player);
							},
							forced: true,
							content() {
								'step 0';
								let effect = 0;

								for (const i of trigger.targets) {
									effect -= get.effect(i, trigger.card, trigger.player, player);
								}

								if (effect > 0) {
									if (player.countCards('h', 'shan')) {
										effect = 1;
									} else {
										effect = 0;
									}
									if (trigger.targets.length == 1) {
										if (trigger.targets[0].hp == 1) {
											effect++;
										}
										if (effect > 0 && player.countCards('h', { color: 'black' })) {
											effect++;
										}
									}
									if (effect > 0) {
										effect += 6;
									}
								}
								player
									.chooseCard('h', get.prompt2('lqtz_jianzheng', trigger.player))
									.set('ai', function (card) {
										if (_status.event.effect >= 0) {
											const val = get.value(card);
											if (val < 0) {
												return 10 - val;
											}
											return _status.event.effect - val;
										}
										return 0;
									})
									.set(
										'effect',
										effect,
									)('step 1');
								if (result.cards?.length) {
									event.card = result.cards[0];
									trigger.targets.length = 0;
									trigger.parent.triggeredTargets1.length = 0;
								} else {
									event.finish();
								}
								('step 2');
								('step 3');
								if (event.card) {
									player.lose(event.card, ui.cardPile, 'visible', 'insert');
									player.$throw(event.card, 1000);
									game.log(player, '将', card, '置于牌堆顶');
								}
								('step 4');
								trigger.parent.targets.push(player);
								trigger.player.line(player);
							},
							ai: {
								threaten: 1.1,
								expose: 0.25,
							},
						},
						lqtz_yinlei: {
							trigger: { player: 'loseBegin' },
							forced: true,
							content() {
								let list = [];
								for (const i of game.players) {
									if (player.getEnemies().includes(i) && !i.isLinked()) {
										list.push(i);
									}
								}
								if (list.length) {
									list = list.randomGet();
									player.line(list);
									list.link();
								}
							},
						},
						lqtz_wangzun: {
							trigger: { global: 'phaseEnd' },
							forced: true,
							audio: 'wangzun',
							filter(event, player) {
								return event.player != player && player.getEnemies().includes(event.player);
							},
							content() {
								let num = 0;
								trigger.player.getHistory('sourceDamage', function (evt) {
									if (evt.player == player) {
										num += evt.num;
									}
								});
								if (num > 0) {
									trigger.player.damage();
								} else {
									trigger.player.chooseToDiscard('he', 2, true);
								}
							},
						},
						lqtz_duoxi: {
							trigger: {
								global: 'phaseDrawBegin1',
							},
							audio: 'drlt_weidi',
							check(event, player) {
								return _status.event.player.hp > 1 && get.attitude(_status.event.player, event.player) <= 0;
							},
							filter(event, player) {
								return !event.numFixed && event.player != player;
							},
							content() {
								player.loseHp();
								trigger.cancel();
								player.draw(2);
							},
						},
						lqtz_shehun: { inherit: 'new_rejianxiong' },
						lqtz_lingsi: { inherit: 'lqtz_zhennu' },
						lqtz_taoyuan: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							audio: 'qingyi',
							content() {
								player.chooseUseTarget('###是否发动【饕怨】？###视为使用一张没有距离限制的【杀】', { name: 'sha' }, false, 'nodistance');
							},
							ai: {
								threaten(player, target) {
									return 1.6;
								},
							},
						},
						lqtz_lihuo: { inherit: 'lqtz_guihuo' },
						lqtz_jingxin: {
							audio: 'liyu',
							inherit: 'liyu',
							content() {
								'step 0';
								player.gainPlayerCard(get.prompt('lqtz_jingxin', trigger.player), trigger.player, 'hej', 'visibleMove').set('ai', function (button) {
									const player = _status.event.player;
									const evt = _status.event.target;
									if (get.attitude(player, evt) > 0 && get.position(button.link) == 'j') {
										return 4 + get.value(button.link);
									}
									if (get.type(button.link) == 'equip') {
										if (
											get.attitude(player, evt) > 0 &&
											game.hasPlayer(function (current) {
												return player.canUse({ name: 'juedou' }, current) && current != evt.target && get.effect(current, { name: 'juedou' }, player, player) > 2;
											})
										) {
											return 5;
										} else if (
											game.hasPlayer(function (current) {
												return player.canUse({ name: 'juedou' }, current) && current != evt && current != player && get.effect(current, { name: 'juedou' }, player, player) < 0;
											})
										) {
											return 1;
										} else {
											return 4;
										}
									}
									return 3;
								})('step 1');
								if (result.cards?.length) {
									if (get.type(result.cards[0]) != 'equip') {
										trigger.player.draw();
										event.finish();
									} else {
										if (
											!game.hasPlayer(function (current) {
												return current != player && current != trigger.player && player.canUse('juedou', current);
											})
										) {
											event.finish();
											return;
										}
										trigger.player
											.chooseTarget(
												true,
												function (card, player, target) {
													const evt = _status.event.parent;
													return evt.player.canUse({ name: 'juedou' }, target) && target != _status.event.player;
												},
												'请选择一名角色,视为' + get.translation(player) + '对其使用【决斗】',
											)
											.set('ai', function (target) {
												const evt = _status.event.parent;
												return get.effect(target, { name: 'juedou' }, evt.player, _status.event.player) - 2;
											});
									}
								} else {
									event.finish();
								}
								('step 2');
								if (result.targets?.length) {
									player.useCard({ name: 'juedou' }, result.targets[0], 'noai');
								}
							},
							ai: {
								halfneg: true,
							},
						},
						lqtz_xifan: {
							inherit: 'rezhiyi',
							audio: 'zhiyi',
							content() {
								'step 0';
								const list = [];
								player.getHistory('useCard', function (evt) {
									if (get.type(evt.card) != 'basic') {
										return;
									}
									let name = evt.card.name;
									if (name == 'sha') {
										const nature = evt.card.nature;
										switch (nature) {
											case 'fire':
												name = 'huosha';
												break;
											case 'thunder':
												name = 'leisha';
												break;
											case 'kami':
												name = 'kamisha';
												break;
											case 'ice':
												name = 'icesha';
												break;
											case 'stab':
												name = 'cisha';
												break;
										}
									}
									list.add(name);
								});
								player.getHistory('respond', function (evt) {
									if (get.type(evt.card) != 'basic') {
										return;
									}
									let name = evt.card.name;
									if (name == 'sha') {
										const nature = evt.card.nature;
										switch (nature) {
											case 'fire':
												name = 'huosha';
												break;
											case 'thunder':
												name = 'leisha';
												break;
											case 'kami':
												name = 'kamisha';
												break;
											case 'ice':
												name = 'icesha';
												break;
											case 'stab':
												name = 'cisha';
												break;
										}
									}
									list.add(name);
								});
								player.chooseButton(
									[
										'袭返:选择要使用的牌,或点取消摸一张牌',
										[
											list.map(function (name) {
												return ['基本', '', name];
											}),
											'vcard',
										],
									],
									function (button) {
										return _status.event.player.getUseValue({ name: button.link[2], nature: button.link[3] });
									},
									function (button) {
										return _status.event.player.hasUseTarget({ name: button.link[2], nature: button.link[3] });
									},
								);
								('step 1');
								if (!result.bool) {
									player.draw();
								} else {
									player.chooseUseTarget({ name: result.links[0][2], nature: result.links[0][3] });
								}
							},
						},
						lqtz_yutu: {
							group: ['lqtz_yutu_sha', 'lqtz_yutu_shan'],
							subSkill: {
								sha: {
									enable: 'chooseToUse',
									viewAs: { name: 'sha' },
									filterCard() {
										return false;
									},
									viewAsFilter(player) {
										if (player.hasSkill('lqtz_yutu_disable')) {
											return false;
										}
									},
									selectCard: -1,
									mark: false,
									precontent() {
										player.addTempSkill('lqtz_yutu_disable', 'roundStart');
									},
									prompt: '视为使用一张杀',
									ai: {
										order() {
											const player = _status.event.player;
											if (
												!player.hasShan() &&
												!game.hasPlayer(function (current) {
													return player.canUse('sha', current) && current.hp == 1 && get.effect(current, { name: 'sha' }, player, player) > 0;
												})
											) {
												return 0;
											}
											return 2.95;
										},
										skillTagFilter(player, tag, arg) {
											if (player.hasSkill('lqtz_yutu_disable')) {
												return false;
											}
											if (arg != 'use') {
												return false;
											}
										},
										respondSha: true,
									},
								},
								shan: {
									enable: 'chooseToUse',
									viewAs: { name: 'shan' },
									mark: false,
									filterCard() {
										return false;
									},
									viewAsFilter(player) {
										if (player.hasSkill('lqtz_yutu_disable')) {
											return false;
										}
										return true;
									},
									onuse(event, player) {
										player.addTempSkill('lqtz_yutu_disable', 'roundStart');
									},
									selectCard: -1,
									prompt: '视为使用一张闪',
									ai: {
										order() {
											const player = _status.event.player;
											if (player.hasSkill('qingzhongx_give')) {
												return 2.95;
											}
											return 3.15;
										},
										skillTagFilter(player) {
											if (player.hasSkill('lqtz_yutu_disable')) {
												return false;
											}
										},
										respondShan: true,
									},
								},
								disable: {
									mark: true,
									intro: {
										content: '本轮已发动',
									},
								},
							},
						},
						lqtz_leili: {
							trigger: { source: 'damageEnd' },
							forced: true,
							filter(event, player) {
								return event.card && event.card.name == 'sha';
							},
							content() {
								'step 0';
								player.chooseTarget(get.prompt('lqtz_leili'), function (card, player, target) {
									return target != player && target != trigger.player;
								}).ai = function (target) {
									return get.damageEffect(target, player, player, 'thunder');
								};
								('step 1');
								if (result.targets?.length) {
									result.targets[0].damage('thunder');
								}
							},
							ai: {
								expose: 0.2,
								threaten: 1.3,
							},
						},
						lqtz_fengxing: {
							trigger: { player: 'phaseBegin' },
							forced: true,
							content() {
								'step 0';
								player.chooseTarget(get.prompt('lqtz_fengxing'), function (card, player, target) {
									if (target.isFriendsOf(player)) {
										return false;
									}
									return lib.filter.targetEnabled({ name: 'sha' }, player, target);
								}).ai = function (target) {
									return get.effect(target, { name: 'sha' }, player);
								};
								('step 1');
								if (result.targets?.length) {
									player.useCard({ name: 'sha' }, result.targets, false);
								}
							},
							ai: {
								expose: 0.2,
								threaten: 1.3,
							},
						},
						lqtz_zhenlei: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							content() {
								'step 0';
								player.chooseTarget(get.prompt('lqtz_zhenlei'), function (card, player, target) {
									return player != target;
								}).ai = function (target) {
									return get.damageEffect(target, player, player, 'thunder');
								};
								('step 1');
								if (result.targets?.length) {
									result.targets[0].damage('thunder');
								}
							},
						},
						lqtz_lingsia: { global: 'lqtz_lingsia2' },
						lqtz_lingsia2: {
							trigger: { global: 'dieAfter' },
							forced: true,
							filter(event, player) {
								return event.player.hasSkill('lqtz_lingsia') && event.player.isDead();
							},
							content() {
								trigger.player.line(player, 'fire');
								player.damage('nosource', 'fire').animate = false;
								player.$damage(trigger.player);
								player.$damagepop(-1, 'fire');
								if (lib.config.animation && !lib.config.low_performance) {
									player.$fire();
								}
								if (!event.parent.parent.lqtz_lingsia_logv) {
									event.parent.parent.lqtz_lingsia_logv = true;
									game.logv(trigger.player, 'lqtz_lingsia', game.filterPlayer(), event.parent.parent);
								}
							},
						},
						lqtz_xiangrui: {
							trigger: {
								global: 'phaseUseEnd',
							},
							forced: true,
							filter(event, player) {
								return player.hp != player.maxHp && event.player != player;
							},
							content() {
								'step 0';
								trigger.player.chooseToDiscard('he', 2, '###【祥瑞】###是否弃置两张牌,令' + get.translation(player) + '回复一点血量？').set('ai', function (card) {
									if (get.attitude(player, trigger.player) < 0) {
										return 0;
									}
									return 1;
								});
								('step 1');
								if (result.bool) {
									player.recover();
								}
							},
						},
						_lqtz_xiangrui2: {
							//麒麟增加血量上限
							trigger: {
								global: 'dieEnd',
							},
							forced: true,
							popup: false,
							_priority: 999,
							filter(event, player) {
								if (get.mode() != 'boss' || player.name != 'lqtz_qilin') {
									return false;
								}
								return event.player == game.boss;
							},
							content() {
								player.gainMaxHp(2);
							},
						},
						lqtz_xiongqu: {
							mod: {
								targetEnabled(card, player, target, now) {
									if (card.name == 'toulianghuanzhu' || card.name == 'fudichouxin') {
										return false;
									}
								},
							},
						},
						lqtz_wuyou: {
							trigger: {
								player: ['phaseDrawBegin', 'phaseUseBegin'],
							},
							forced: true,
							content() {
								trigger.cancel();
								game.log('你没有摸牌与出牌阶段');
							},
						},
						lqtz_weihu: {
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							filter(event, player) {
								return (game.zhuguozi > 0 && player.hp != player.maxHp) || (game.zhuguozi > 3 && !player.hasSkill('lqtz_zuoji'));
							},
							forced: true,
							content() {
								'step 0';
								if (player.isDamaged()) {
									//受伤
									let num = player.maxHp - player.hp;
									if (game.zhuguozi < num) {
										num = game.zhuguozi;
									}
									game.changeZhuGuoZi(-num);
									player.recover(num);
								}
								('step 1');
								if (!player.hasSkill('lqtz_zuoji') && game.zhuguozi > 3) {
									game.changeZhuGuoZi(-4);
									player.init('lqtz_dahu');
								}
							},
						},
						lqtz_zuoji: {
							trigger: {
								global: 'damageEnd',
							},
							filter(event, player) {
								return event.card && event.card.name == 'sha' && player.getEnemies().includes(event.player) && player.getFriends().includes(event.source);
							},
							forced: true,
							content() {
								trigger.player.damage();
							},
						},
						lqtz_miewu: {
							audio: 'spmiewu',
							enable: ['chooseToUse', 'chooseToRespond'],
							filter(event, player) {
								if (!player.getExpansions('buqu').length || player.getExpansions('buqu').length < 1 + player.getStat('skill').lqtz_miewu_backup || !player.countCards('hes')) {
									return false;
								}
								for (const i of lib.inpile) {
									const type = get.type2(i);
									if ((type == 'basic' || type == 'trick') && event.filterCard && event.filterCard({ name: i }, player, event)) {
										return true;
									}
								}
								return false;
							},
							hiddenCard(player, name) {
								if (!player.getExpansions('buqu').length || player.getExpansions('buqu').length < 1 + player.getStat('skill').lqtz_miewu_backup || !player.countCards('hes')) {
									return false;
								}
								const type = get.type2(name);
								return type == 'basic' || type == 'trick';
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (name == 'sha') {
											if (event.filterCard && event.filterCard({ name: name }, player, event)) {
												list.push(['基本', '', 'sha']);
											}
											for (const j of lib.inpile_nature) {
												if (event.filterCard && event.filterCard({ name: name, nature: j }, player, event)) {
													list.push(['基本', '', 'sha', j]);
												}
											}
										} else if (get.type2(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										} else if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['基本', '', name]);
										}
									}

									return ui.create.dialog('灭吴', [list, 'vcard']);
								},
								filter(button, player) {
									return _status.event.parent.filterCard({ name: button.link[2] }, player, _status.event.parent);
								},
								check(button) {
									const player = _status.event.player;
									if (player.countCards('hs', button.link[2]) > 0) {
										return 0;
									}
									if ('lebu' == button.link[2]) {
										return 10;
									}
									return player.getUseValue({
										name: button.link[2],
										nature: button.link[3],
									});
								},
								backup(links, player) {
									return {
										filterCard: true,
										audio: 'spmiewu',
										popname: true,
										check(card) {
											return 8 - get.value(card);
										},
										position: 'hes',
										viewAs: { name: links[0][2], nature: links[0][3] },
										precontent() {
											let num = 1;
											if (player.getStat('skill').lqtz_miewu_backup > 0) {
												num = 1 + player.getStat('skill').lqtz_miewu_backup;
											}
											const card = player.getExpansions('buqu').randomGets(num);
											player.gain(card, 'draw2');
										},
									};
								},
								prompt(links, player) {
									return '将一张牌当做' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]) + '使用';
								},
							},
							ai: {
								fireAttack: true,
								respondSha: true,
								respondShan: true,
								skillTagFilter(player) {
									if (!player.getExpansions('buqu').length || player.getExpansions('buqu').length < 1 + player.getStat('skill').lqtz_miewu_backup || !player.countCards('hes')) {
										return false;
									}
								},
								order: 1,
								result: {
									player(player) {
										if (_status.event.dying) {
											return get.attitude(player, _status.event.dying);
										}
										return 1;
									},
								},
							},
						},
						yuezi: {
							trigger: { player: ['phaseBegin', 'phaseEnd', 'recoverEnd'] },
							forced: true,
							content() {
								if (!player.countCards('h')) {
									player.draw(2);
								} else {
									player.draw();
								}
							},
							mod: {
								maxHandcardBase(player, num) {
									var num = 0;
									const next = game.filterPlayer().sortBySeat();

									for (const i of next) {
										num += i.hp;
									}

									return num;
								},
							},
						},
						//锁定技,你进入濒死状态或即将死亡时,若你的体力大于0,则你取消之;当你处于濒死状态时,其他角色发动的技能全部无效
						lqtz_shenyu: {
							trigger: {
								global: ['logSkillBegin'],
							},
							popup: false,
							forced: true,
							filter(event, player) {
								return _status.dying.includes(player) && event.player != player;
							},
							async content(event, trigger, player) {
								const name = trigger.skill;
								const info = lib.skill[name];
								const arr = trigger.parent.next;
								for (let i = arr.length - 1; i >= 0; i--) {
									if (arr[i].name === name) {
										arr.splice(i, 1);
									}
								}
								game.log(player, `终止${get.translation(name)}的发动`);
								if (info.limited || info.juexingji) {
									trigger.player.awakenSkill(name);
								}
							},
							group: ['lqtz_shenyu_1'],
							subSkill: {
								1: {
									trigger: {
										player: ['dieBefore', 'dyingBefore'],
									},
									filter(event, player) {
										return player.hp > 0;
									},
									forced: true,
									content() {
										trigger.cancel();
										if (player.name == 'lqtz_langqinyu' || player.name2 == 'lqtz_langqinyu') {
											game.log('耍小聪明可杀不了啊羽喔');
										} else {
											game.log('耍小聪明可杀不了我');
										}
									},
								},
							},
						},
						lqtz_shenbingj: {
							trigger: {
								source: 'damageSource',
							},
							filter(event, player) {
								return event.num > 0;
							},
							forced: true,
							content() {
								player.gainMaxHp(trigger.num);
							},
						},
						lqtz_tongben: {
							trigger: {
								player: 'damageBegin',
								source: 'damageBegin',
							},
							forced: true,
							filter(event, player, name) {
								return (event.source == game.boss || event.player == game.boss) && event.source != event.player && event.num > 0;
							},
							content() {
								trigger.cancel();
							},
						},
						lqtz_jiajia: {
							intro: {
								content: 'mark',
							},
						},
						lqtx_xisheng: {
							trigger: {
								player: 'dieBegin',
							},
							filter(event, player, name) {
								return game.boss;
							},
							forced: true,
							content() {
								game.boss.addMark('lqtz_jiajia', 3);
							},
						},
						lqtx_xishengjia: {
							trigger: {
								global: 'dying',
							},
							filter(event, player, name) {
								return event.player == game.boss;
							},
							content() {
								trigger.player.recover(5 - trigger.player.hp);
								player.die();
							},
						},
						lqtz_gongpo: {
							mod: {
								cardUsable(card, player, num) {
									if (!card.cards) {
										return;
									}
									for (const i of card.cards) {
										if (!i.hasGaintag('lqtz_gongpo')) {
											return;
										}
									}
									return Infinity;
								},
								targetInRange(card) {
									if (!card.cards) {
										return;
									}
									for (const i of card.cards) {
										if (!i.hasGaintag('lqtz_gongpo')) {
											return;
										}
									}
									return true;
								},
							},
							trigger: {
								player: ['phaseUseBegin', 'useCard', 'useCardEnd'],
							},
							forced: true,
							filter(event, player, name) {
								if (name != 'phaseUseBegin') {
									return event.cards && event.cards.length == 1 && event.card.storage.lqtz_gongpo;
								}
								return true;
							},
							content() {
								const name = event.triggername;
								if (name == 'useCard' && trigger.card.storage.lqtz_gongpo) {
									trigger.directHit.addArray(game.players);
									trigger.addCount = false;
									if (player.stat[player.stat.length - 1].card.sha > 0) {
										player.stat[player.stat.length - 1].card.sha--;
									}
								} else if (name == 'useCardEnd' && trigger.card.storage.lqtz_gongpo) {
									if (
										player.hasHistory('sourceDamage', function (evt) {
											return evt.card == trigger.card;
										})
									) {
										player.recover();
									}
									delete trigger.card.storage.lqtz_gongpo;
								} else {
									const card = get.cardPile2(function (card) {
										return card.name == 'sha';
									});
									player.gain(card, 'gain2').gaintag.add('lqtz_gongpo');
									card.storage.lqtz_gongpo = true;
								}
							},
						},
						latz_fangyu: {
							trigger: {
								global: 'damageBegin',
							},
							forced: true,
							filter(event, player, name) {
								return event.player == game.boss && event.num > 0;
							},
							content() {
								trigger.cancel();
								trigger.source ? player.damage(trigger.num, trigger.source) : player.damage(trigger.num);
								trigger.player.addMark('lqtz_jiajia');
							},
						},
						lqtz_feiyue: {
							trigger: {
								global: 'phaseEnd',
							},
							forced: true,
							filter(event, player, name) {
								return event.player == game.boss && event.skill != 'lqtz_feiyue';
							},
							content() {
								trigger.player.phase('nodelay');
							},
						},
						lqtz_huimie: {
							trigger: {
								player: 'useCard',
								source: 'damageBegin4',
							},
							_priority: -Infinity,
							forced: true,
							filter(event, player, name) {
								if (name == 'damageBegin4') {
									return Math.random() <= 35 / 100;
								}
								return game.hasPlayer(function (current) {
									return current.hp / current.maxHp <= 0.35;
								});
							},
							content() {
								if (trigger.name == 'damage') {
									trigger.num *= 2;
								} else {
									trigger.directHit.addArray(
										game.filterPlayer(function (current) {
											return current.hp / current.maxHp <= 0.35;
										}),
									);
								}
							},
						},
						lqtz_fengbao: {
							enable: 'phaseUse',
							usable: 1,
							selectTarget: -1,
							filterTarget(card, player, target) {
								return player.getEnemies().includes(target);
							},
							content() {
								'step 0';
								target.damage('thunder');
								target
									.chooseToDiscard('he', [1, 3], '###【风暴离子电】###弃置最多3张类型各不同的牌.', function (card, player, target) {
										for (const i of ui.selected.cards) {
											if (get.type2(i) == get.type2(card)) {
												return false;
											}
										}
										return true;
									})
									.set('complexCard', true)
									.set('ai', function (card) {
										return 8 - get.value(card);
									});
								('step 1');
								const list = ['basic', 'trick', 'equip'];
								if (!result.bool || result.cards.length < 3) {
									target.addTempSkill('lqtz_fengbao_mark', { player: 'phaseAfter' });
									if (result.cards?.length) {
										for (const i of result.cards) {
											if (list.includes(get.type2(i))) {
												list.remove(get.type2(i));
											}
										}
									}
									target.storage.lqtz_fengbao = list;
								}
							},
							ai: {
								order: 20,
								result: {
									target: -1,
								},
							},
							subSkill: {
								mark: {
									mark: true,
									intro: {
										mark(dialog, content, player) {
											dialog.addText('你于出牌阶段不能使用或打出' + get.translation(player.storage.lqtz_fengbao));
										},
									},
									charlotte: true,
									onremove(player) {
										delete player.storage.lqtz_fengbao;
									},
									mod: {
										cardEnabled2(card, player) {
											if (player == _status.currentPhase && player.storage.lqtz_fengbao.includes(get.type(card, 'trick'))) {
												return false;
											}
										},
									},
								},
							},
						},
						lqtz_shuangren: {
							enable: 'phaseUse',
							filter(event, player) {
								let dead = game.dead.slice(0),
									list = 0;

								for (const i of dead) {
									if (i.name == 'lqtz_ren') {
										list += 5;
									}
								}

								return list > 0 && player.countMark('lqtz_jiajia') >= list;
							},
							forced: true,
							content() {
								let dead = game.dead.slice(0),
									num = 0;

								for (const i of dead) {
									if (i.name == 'lqtz_ren') {
										num += 5;
										i.revive(i.maxHp);
										i.draw(4, false);
										i.update();
									}
								}

								player.removeMark('lqtz_jiajia', num);
							},
							ai: {
								order: 20,
								result: {
									player: 1,
								},
							},
						},
						lqtz_shuangyi: {
							enable: 'phaseUse',
							filter(event, player) {
								let dead = game.dead.slice(0),
									list = 0;

								for (const i of dead) {
									if (i.name == 'lqtz_yi') {
										list += 5;
									}
								}

								return list > 0 && player.countMark('lqtz_jiajia') >= list;
							},
							forced: true,
							content() {
								let dead = game.dead.slice(0),
									num = 0;

								for (const i of dead) {
									if (i.name == 'lqtz_yi') {
										num += 5;
										i.revive(i.maxHp);
										i.draw(4, false);
										i.update();
									}
								}

								player.removeMark('lqtz_jiajia', num);
							},
							ai: {
								order: 20,
								result: {
									player: 1,
								},
							},
						},
						lqtz_shuangdun: {
							enable: 'phaseUse',
							filter(event, player) {
								let dead = game.dead.slice(0),
									list = 0;

								for (const i of dead) {
									if (i.name == 'lqtz_dun') {
										list += 5;
									}
								}

								return list > 0 && player.countMark('lqtz_jiajia') >= list;
							},
							forced: true,
							content() {
								let dead = game.dead.slice(0),
									num = 0;

								for (const i of dead) {
									if (i.name == 'lqtz_dun') {
										num += 5;
										i.revive(i.maxHp);
										i.draw(4, false);
										i.update();
									}
								}

								player.removeMark('lqtz_jiajia', num);
							},
							ai: {
								order: 20,
								result: {
									player: 1,
								},
							},
						},
						lqtz_shenbingx: {
							enable: 'phaseUse',
							filter(event, player) {
								let dead = game.dead.slice(0),
									list = 0;

								for (const i of dead) {
									if (i.name == 'lqtz_shenbing') {
										list += 5;
									}
								}

								return list > 0 && player.countMark('lqtz_jiajia') >= list;
							},
							forced: true,
							content() {
								let dead = game.dead.slice(0),
									num = 0;

								for (const i of dead) {
									if (i.name == 'lqtz_shenbing') {
										num += 5;
										i.revive(i.maxHp);
										i.draw(4, false);
										i.update();
									}
								}

								player.removeMark('lqtz_jiajia', num);
							},
							ai: {
								order: 20,
								result: {
									player: 1,
								},
							},
						},
						lqtz_shenjia: {
							trigger: {
								player: ['damageEnd', 'changeHp'],
								source: 'damageSource',
							},
							forced: true,
							filter(event, player, name) {
								if (name != 'changeHp') {
									return event.num > 0;
								}
								return player.hp < 11 && !player.storage.lqtz_shenjiaj;
							},
							content() {
								if (event.triggername != 'changeHp') {
									player.addMark('lqtz_jiajia');
									event.finish();
								} else {
									if (player.hp < 10) {
										player.recover(10 - player.hp);
									}
									player.storage.lqtz_shenjiaj = true;
								}
							},
						},
						lqtz_bilei: {
							trigger: {
								player: 'damageBegin4',
							},
							forced: true,
							_priority: -Infinity,
							filter(event, player, name) {
								if (event.num == 1) {
									return Math.random() <= 35 / 100;
								}
								return event.num > 0;
							},
							content() {
								if (trigger.num > 1) {
									trigger.cancel();
								} else {
									player.changeHujia();
								}
							},
						},
						lqtz_niliu: {
							intro: {
								content: '伤害合计:#',
							},
							enable: 'phaseUse',
							usable: 1,
							filterTarget(player, target) {
								return player != target;
							},
							filter(event, player) {
								return player.hasMark('lqtz_niliu');
							},
							content() {
								target.damage(Math.ceil(player.countMark('lqtz_niliu') / 2));
								player.removeMark('lqtz_niliu', player.countMark('lqtz_niliu'));
							},
							ai: {
								order: 7,
								result: {
									target: -2,
								},
							},
							group: 'lqtz_niliu_num',
							subSkill: {
								num: {
									trigger: {
										player: 'phaseAfter',
										global: 'damageEnd',
									},
									forced: true,
									_priority: -6,
									popup: false,
									filter(event, player, name) {
										if (name == 'phaseAfter') {
											return player.hasMark('lqtz_niliu');
										}
										return event.num > 0 && !event.player.getEnemies().includes(player);
									},
									content() {
										if (trigger.name == 'damage') {
											player.addMark('lqtz_niliu', trigger.num);
										} else {
											player.removeMark('lqtz_niliu', player.countMark('lqtz_niliu'));
										}
									},
								},
							},
						},
						lqtz_jixun: { group: ['mashu', 'feiying'] },
						lqtz_leiting: {
							trigger: {
								player: 'useCardToPlayered',
							},
							check(event, player) {
								return get.attitude(player, event.target) <= 0;
							},
							filter(event, player) {
								return event.targets.length && ['sha', 'juedou'].includes(event.card.name);
							},
							content() {
								'step 0';
								if (!trigger.target.hasSkill('baiban')) {
									trigger.target.addTempSkill('baiban');
								}
								event.count = trigger.target.countCards('h', { suit: 'diamond' });
								let cards = trigger.target.getCards('h'),
									zi = '没有♦️️️️牌';
								if (event.count > 0) {
									zi = '共' + get.translation(event.count) + '张♦️️️️牌';
								}
								const content = [get.translation(trigger.target) + '的手牌,' + zi, cards];
								game.log(player, '观看了', trigger.target, '的手牌');
								player
									.chooseControl('摸牌', '增加伤害')
									.set('ai', function () {
										if (event.count > 1) {
											return 1;
										}
										return 0;
									})
									.set('dialog', content);
								('step 1');
								if (result.index == 0) {
									player.draw(event.count);
								} else {
									const trigger2 = trigger.parent;
									if (typeof trigger2.baseDamage != 'number') {
										trigger2.baseDamage = 1;
									}
									trigger2.baseDamage += event.count;
								}
								player.judge(function () {
									return 0;
								});
								('step 2');
								let suit = result.suit,
									target = trigger.target,
									num = target.countCards('h', 'shan');
								if (trigger.card.name == 'juedou') {
									num = target.countCards('h', 'sha');
								}
								if (suit != 'heart') {
									player.storage.lqtz_leiting = trigger.card;
								}
								target
									.chooseToDiscard('请弃置一张' + get.translation(suit) + '牌,否则不能使用牌响应' + get.translation(trigger.card), 'he', function (card) {
										return card.suit == _status.event.suit;
									})
									.set('ai', function (card) {
										let num = _status.event.num,
											ca = 'shan';
										if (trigger.card.name == 'juedou') {
											ca = 'sha';
										}
										if (num == 0) {
											return 0;
										}
										if (card.name == ca) {
											return num > 1 ? 2 : 0;
										}
										return 8 - get.value(card);
									})
									.set('num', num)
									.set('suit', suit);
								('step 3');
								if (!result.bool) {
									trigger.parent.directHit.add(trigger.target);
								}
							},
							ai: {
								ignoreSkill: true,
								skillTagFilter(player, tag, arg) {
									if (tag == 'directHit_ai') {
										return get.attitude(player, arg.target) <= 0;
									}
									if (!arg || arg.isLink || !arg.card || arg.card.name != 'sha') {
										return false;
									}
									if (!arg.target || get.attitude(player, arg.target) >= 0) {
										return false;
									}
									if (!arg.skill || !lib.skill[arg.skill] || lib.skill[arg.skill].charlotte || get.is.locked(arg.skill) || !arg.target.getSkills(true, false).includes(arg.skill)) {
										return false;
									}
								},
								directHit_ai: true,
							},
							group: 'lqtz_leiting_damage',
							subSkill: {
								damage: {
									trigger: {
										source: 'damageBegin',
										player: 'useCardAfter',
									},
									forced: true,
									_priority: 46,
									filter(event, player, name) {
										if (name == 'useCard') {
											return event.card == player.storage.lqtz_leiting;
										}
										return event.num > 0 && event.card && event.card == player.storage.lqtz_leiting;
									},
									content() {
										'step 0';
										if (trigger.name == 'damage') {
											player.chooseBool('是否令伤害减1,然令' + get.translation(trigger.player) + '失去一点体力上限.').set('ai', function () {
												return true;
											});
										}
										('step 1');
										if (result.bool) {
											trigger.num--;
											trigger.player.loseMaxHp();
										}
										delete player.storage.lqtz_leiting;
									},
								},
							},
						},
						lqtz_tianwei: {
							derivation: ['relonghun', 'new_reyiji', 'new_repaoxiao', 'wushuang'],
							init(player) {
								if (!player.storage.lqtz_tianwei) {
									player.storage.lqtz_tianwei = 71;
								}
								player.removeAdditionalSkill('lqtz_tianwei');
								let list = ['new_repaoxiao', 'wushuang'];
								if (player.hp < 11) {
									list = ['relonghun', 'new_reyiji'];
								}
								player.addAdditionalSkill('lqtz_tianwei', list);
							},
							getInfo(player) {
								if (!player.storage.lqtz_tianwei) {
									player.storage.lqtz_tianwei = 71;
								}
								return player.storage.lqtz_tianwei - 1;
							},
							trigger: {
								global: 'phaseAfter',
								source: 'damageSource',
								player: ['latz_fangyuAfter', 'lqtx_xishengAfter', 'lqtz_shenjiaAfter', 'damageEnd', 'changeHp'],
							},
							filter(event, player, name) {
								if (name == 'phaseAfter') {
									return player.getStat().damage > 0;
								}
								if (name == 'damageSource') {
									return event.num > 0 && event.card && event.card;
								}
								if (name == 'damageEnd') {
									return event.num > 0;
								}
								if (name == 'changeHp') {
									return player.hp <= lib.skill.lqtz_tianwei.getInfo(player) && lib.skill.lqtz_tianwei.getInfo(player) > 9;
								}
								return true;
							},
							forced: true,
							content() {
								'step 0';
								if (event.triggername == 'phaseAfter' || trigger.name == 'damage') {
									var num = trigger.num || 1;
									player.addMark('lqtz_jiajia', num);
								}
								player.removeAdditionalSkill('lqtz_tianwei');
								let list = ['new_repaoxiao', 'wushuang'];
								if (player.hp < 11) {
									list = ['relonghun', 'new_reyiji'];
								}
								player.addAdditionalSkill('lqtz_tianwei', list);
								if (trigger.name == 'changeHp') {
									var num = player.storage.lqtz_tianwei - 1;
									if (player.hp < num) {
										player.recover(num - player.hp);
									}
									player.storage.lqtz_tianwei -= 10;
								}
							},
						},
						lqtz_shenchao: {
							enable: ['chooseToUse', 'chooseToRespond'],
							filter(event, player) {
								for (const i of lib.inpile) {
									if ((get.type(i) == 'basic' || get.type(i) == 'trick') && event.filterCard && event.filterCard({ name: i }, player, event) && !player.hasSkill('lqtz_shenchao2') && player.hasMark('lqtz_jiajia')) {
										return true;
									}
								}
								return false;
							},
							hiddenCard(player, name) {
								return get.type(name) == 'basic' || (get.type(name) == 'trick' && !player.hasSkill('lqtz_shenchao2') && player.hasMark('lqtz_jiajia'));
							},
							chooseButton: {
								dialog(event, player) {
									const list = [];

									for (const i of lib.inpile) {
										const name = i;
										if (name == 'sha') {
											if (event.filterCard && event.filterCard({ name: name }, player, event)) {
												list.push(['基本', '', 'sha']);
											}
											for (const j of lib.inpile_nature) {
												if (event.filterCard && event.filterCard({ name: name, nature: j }, player, event)) {
													list.push(['基本', '', 'sha', j]);
												}
											}
										} else if (get.type(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										} else if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['基本', '', name]);
										}
									}

									return ui.create.dialog('神潮', [list, 'vcard']);
								},
								filter(button, player) {
									return _status.event.parent.filterCard({ name: button.link[2] }, player, _status.event.parent);
								},
								check(button) {
									if (_status.event.parent.type != 'phase') {
										return 1;
									}
									const player = _status.event.player;
									//if(button.link[2]=='wanjian') return 30
									if (_status.currentPhase == player && button.link[2] != 'wanjian') {
										return 0;
									}
									return player.getUseValue({
										name: button.link[2],
										nature: button.link[3],
									});
								},
								backup(links, player) {
									return {
										filterCard() {
											return false;
										},
										viewAs: { name: links[0][2], nature: links[0][3] },
										selectCard: -1,
										popname: true,
										precontent() {
											player.removeMark('lqtz_jiajia');
										},
									};
								},
								prompt(links, player) {
									return '视为使用一张' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]);
								},
							},
							ai: {
								fireAttack: true,
								respondSha: true,
								respondShan: true,
								skillTagFilter(player) {
									if (player.hasSkill('lqtz_shenchao2') && !player.hasMark('lqtz_jiajia')) {
										return false;
									}
									return true;
								},
								order: 1,
								result: {
									player(player) {
										if (_status.event.dying) {
											return get.attitude(player, _status.event.dying);
										}
										if (_status.event.type == 'wuxie' && _status.currentPhase == player) {
											return 0;
										}
										return 1;
									},
								},
							},
							group: 'lqtz_shenchao_count',
						},
						lqtz_shenchao_count: {
							trigger: { player: ['useCard', 'respond'] },
							forced: true,
							popup: false,
							filter(evt, player) {
								if (evt.skill != 'lqtz_shenchao_backup') {
									return false;
								}
								if (
									evt.card.name == 'wanjian' &&
									player.getHistory('useCard', function (card) {
										return card.card.name == 'wanjian';
									}).length < 4
								) {
									return false;
								}
								return true;
							},
							content() {
								player.addTempSkill('lqtz_shenchao2');
							},
						},
						lqtz_shenchao2: {},
						lqtz_zhongzhuang: {
							enable: 'phaseUse',
							usable: 1,
							filterTarget(player, target) {
								if (_status.event.player.hujia > 0) {
									return player != target;
								}
								return true;
							},
							selectTarget() {
								if (_status.event.player.hujia > 0) {
									return [1, 1];
								}
								return [0, 0];
							},
							filter(event, player) {
								return player.hasMark('lqtz_jiajia') || player.hujia > 0;
							},
							content() {
								'step 0';
								if (player.hujia > 0) {
									target.damage(player.hujia);
									player.changeHujia(-player.hujia);
								}
								if (player.hasMark('lqtz_jiajia')) {
									const list = [];
									for (let i = 0; i < 4; i++) {
										if (list.length < player.countMark('lqtz_jiajia') + 1) {
											list.add(i);
										}
									}
									player
										.chooseControl(list)
										.set('ai', function () {
											return list.length - 1;
										})
										.set('prompt', '你可以移去最多3枚<甲>标记,获得两倍的护甲.');
								}
								('step 1');
								player.removeMark('lqtz_jiajia', result.control);
								player.changeHujia(2 * result.control);
							},
							ai: {
								order: 7,
								result: {
									target(player) {
										if (player.hujia > 0) {
											return -1;
										}
										return 0;
									},
									player: 2,
								},
							},
						},
						lqtz_jiying: {
							enable: 'phaseUse',
							usable: 1,
							filter(event, player) {
								return player.hasMark('lqtz_jiajia');
							},
							content() {
								'step 0';
								const list = [];
								for (let i = 1; i <= 3; i++) {
									if (list.length < player.countMark('lqtz_jiajia')) {
										list.add(i);
									}
								}
								player
									.chooseControl(list)
									.set('ai', function () {
										return list.length - 1;
									})
									.set('prompt', '你可以移去最多3枚<甲>标记,获得一个效果.');
								('step 1');
								player.addTempSkill('lqtz_jiying_dr');
								player.removeMark('lqtz_jiajia', result.control);
								player.addMark('lqtz_jiying_dr', result.control);
								player.storage.lqtz_jiying = result.control;
							},
							ai: {
								order: 70,
								result: {
									player: 1,
								},
							},
							subSkill: {
								dr: {
									trigger: {
										source: 'damageSource',
									},
									intro: {
										mark(dialog, content, player) {
											if (player.countMark('lqtz_jiying_dr') > 0) {
												dialog.addText('剩下使用次数:' + player.storage.lqtz_jiying_dr);
											}
											dialog.addText('你造成伤害后,可选择摸' + player.storage.lqtz_jiying + '张牌或回复' + player.storage.lqtz_jiying + '点体力.');
										},
									},
									onremove(player) {
										delete player.storage.lqtz_jiying;
										delete player.storage.lqtz_jiying_dr;
									},
									forced: true,
									filter(event, player) {
										return player.hasMark('lqtz_jiying_dr');
									},
									content() {
										'step 0';
										const list = ['摸牌', '回复体力'];
										if (!player.isDamaged()) {
											list.remove('回复体力');
										}
										player
											.chooseControl(list)
											.set('ai', function () {
												return '摸牌';
											})
											.set('prompt', '选择一项.');
										('step 1');
										player.removeMark('lqtz_jiying_dr');
										if (result.control == '摸牌') {
											player.draw(player.storage.lqtz_jiying);
										} else {
											player.recover(player.storage.lqtz_jiying);
										}
									},
								},
							},
						},
						_lqtz_duomo: {
							mod: {
								maxHandcardBase(player, num) {
									let a = 0;
									for (let i = 1; i <= game.duomo; i++) {
										if ([3, 6, 9, 12, 15].includes(i)) {
											a++;
										}
									}
									if (player == game.boss && a > 0) {
										return (num += a);
									}
								},
								cardUsable(card, player, num) {
									let a = 0;
									for (let i = 1; i <= game.duomo; i++) {
										if ([2, 5, 8, 11, 14].includes(i)) {
											a++;
										}
									}
									if (player == game.boss && a > 0 && card.name == 'sha') {
										return num + a;
									}
								},
							},
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							filter(event, player) {
								let a = 0;
								for (let i = 1; i <= game.duomo; i++) {
									if ([1, 4, 7, 10, 13].includes(i)) {
										a++;
									}
								}
								return player == game.boss && game.duomo >= 0;
							},
							forced: true,
							popup: false,
							_priority: Infinity,
							content() {
								'step 0';
								game.changeDuoMo(1);
								const num = game.duomo;
								if (num > 6 && player.name == 'lqtz_qingnu') {
									player.uninit();
									player.init('lqtz_hunu');
								}
								if (num > 11 && player.name == 'lqtz_hunu') {
									player.uninit();
									player.init('lqtz_jiuweihu');
								}
								('step 1');
								let a = 0;
								for (let i = 1; i <= game.duomo; i++) {
									if ([1, 4, 7, 10, 13].includes(i)) {
										a++;
									}
								}
								player.draw(a);
							},
						},
						lqtz_meihuo: {
							audio: 'rexianzhen',
							inherit: 'xianzhen',
							content() {
								'step 0';
								player.chooseToCompare(target);
								('step 1');
								if (result.bool) {
									player.storage.xianzhen = target;
									player.addTempSkill('xianzhen2');
								} else {
									player.addTempSkill('xianzhen3');
								}
							},
						},
						lqtz_qinyun: {
							audio: 'jijiu',
							inherit: 'jijiu',
						},
						lqtz_duanwei: {
							trigger: {
								player: 'phaseJudgeBegin',
							},
							forced: true,
							filter(event, player, name) {
								return player.countCards('j') > 0 && player.countCards('h') > 1;
							},
							content() {
								'step 0';
								player.chooseToDiscard(2, '是否弃置两张手牌来弃置判定区的牌？').set('ai', function (card) {
									return 1;
								})('step 1');
								if (result.bool) {
									player.discard(player.getCards('j'));
								}
							},
						},
						lqtz_yazi: {
							inherit: 'fankui',
							audio: 'refankui',
						},
						lqtz_duoling: {
							inherit: 'kuanggu',
							audio: 'kuanggu',
						},
						lqtz_danshu: {
							trigger: { player: 'loseEnd' },
							forced: true,
							filter(event, player) {
								return _status.currentPhase != player && player.isDamaged();
							},
							content() {
								'step 0';
								player.judge(function (card) {
									return get.color(card) == 'red' ? 1 : 0;
								});
								('step 1');
								if (result.color == 'red') {
									player.recover();
								}
							},
							ai: {
								effect: {
									target(card) {
										if (get.tag(card, 'loseCard')) {
											return [0.5, 1];
										}
									},
								},
							},
						},
						lqtz_xiemei: { inherit: 'mashu' },
						lqtz_juechou: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: {
								player: ['damage', 'recoverEnd'],
								global: ['dyingAfter', 'equipEnd', 'loseAfter', 'cardsDiscardAfter'],
							},
							forced: true,
							filter(event, player) {
								if (event.name == 'damage') {
									return event.source && event.num > 0;
								}
								if (event.name == 'dying') {
									return event.player != player && player.isDamaged();
								}
								if (event.name == 'equip' || event.name == 'recover') {
									return true;
								}
								if (event.name == 'lose' && event.position != ui.discardPile) {
									return false;
								}
								for (const i of event.cards) {
									if (get.type(i) == 'equip') {
										return true;
									}
									break;
								}
								return false;
							},
							logTarget(event, player) {
								if (event.name == 'damage') {
									return event.source;
								}
								if (['dying', 'equip', 'recover', 'lose'].includes(event.name)) {
									return event.player;
								}
								return false;
							},
							content() {
								if (trigger.name == 'damage') {
									trigger.source.damage(trigger.num, trigger.nature, 'nosource');
								} else if (trigger.name == 'dying') {
									player.recover();
								} else if (trigger.name == 'recover' || trigger.name == 'equip') {
									player.draw();
								} else {
									let num = 0;
									for (const i of trigger.cards) {
										if (get.type(i) == 'equip') {
											num++;
										}
									}
									player.draw(num);
								}
							},
						},
						lqtz_longce: {
							audio: 'ext:浪琴天阙/audio:2',
							init(player) {
								if (!player.storage.lqtz_longce) {
									player.storage.lqtz_longce = [];
								}
							},
							enable: ['chooseToUse', 'chooseToRespond'],
							filter(event, player) {
								let a = 0,
									b = [];
								for (const i of lib.inpile) {
									const type = get.type2(i);
									if (type == 'basic' && event.filterCard && event.filterCard({ name: i }, player, event)) {
										return player.countCards('hs', function (card) {
											return get.type2(card) == 'basic';
										});
									}
									a = 0;
									for (const l of player.storage.lqtz_longce) {
										if (l == i) {
											a++;
										}
										if (a > 1) {
											b.add(i);
										}
									}
									if (!b.includes(i) && type == 'trick' && event.filterCard && event.filterCard({ name: i }, player, event)) {
										return player.countCards('hs', function (card) {
											return get.type2(card) == 'trick';
										});
									}
								}
								return false;
							},
							hiddenCard(player, name) {
								let type = get.type2(name),
									a = 0,
									b = [];
								if (type == 'basic') {
									return player.countCards('hs', function (card) {
										return get.type2(card) == 'basic';
									});
								}
								for (const l of player.storage.lqtz_longce) {
									if (l == name) {
										a++;
									}
									if (a > 1) {
										b.add(name);
									}
								}
								if (!b.includes(name) && type == 'trick') {
									return player.countCards('hs', function (card) {
										return get.type2(card) == 'trick';
									});
								}
							},
							chooseButton: {
								dialog(event, player) {
									let list = [],
										a = 0,
										b = [];

									for (const i of lib.inpile) {
										var name = i;
										a = 0;
										for (const l of player.storage.lqtz_longce) {
											if (l == name) {
												a++;
											}
											if (a > 1) {
												b.add(name);
											}
										}
										if (b.includes(name)) {
											continue;
										}
										if (
											!player.countCards('hs', function (card) {
												return get.type2(card) == get.type2(name);
											})
										) {
											continue;
										}
										if (name == 'sha') {
											if (event.filterCard && event.filterCard({ name: name }, player, event)) {
												list.push(['基本', '', 'sha']);
											}
											for (const j of lib.inpile_nature) {
												if (event.filterCard && event.filterCard({ name: name, nature: j }, player, event)) {
													list.push(['基本', '', 'sha', j]);
												}
											}
										} else if (get.type2(name) == 'trick' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['锦囊', '', name]);
										} else if (get.type(name) == 'basic' && event.filterCard && event.filterCard({ name: name }, player, event)) {
											list.push(['基本', '', name]);
										}
									}

									return ui.create.dialog('龙策', [list, 'vcard']);
								},
								filter(button, player) {
									return _status.event.parent.filterCard({ name: button.link[2] }, player, _status.event.parent);
								},
								check(button) {
									if (_status.event.parent.type != 'phase') {
										return 1;
									}
									const player = _status.event.player;
									if ('lebu' == button.link[2]) {
										return 10;
									}
									return player.getUseValue({
										name: button.link[2],
										nature: button.link[3],
									});
								},
								backup(links, player) {
									return {
										filterCard(card, event) {
											return get.type2(card) == get.type2(links[0][2]);
										},
										popname: true,
										selectCard: 1,
										check(card) {
											return 7 - get.value(card);
										},
										position: 'hs',
										viewAs: { name: links[0][2], nature: links[0][3] },
										onuse(result, player) {
											if (_status.currentPhase == player) {
												player.addTempSkill('lqtz_longce_count');
												if (get.type2(result.card) == 'trick') {
													player.storage.lqtz_longce.push(result.card.name);
												}
											}
										},
									};
								},
								prompt(links, player) {
									return '将一张牌当做' + (get.translation(links[0][3]) || '') + get.translation(links[0][2]) + '使用';
								},
							},
							ai: {
								fireAttack: true,
								respondSha: true,
								respondShan: true,
								skillTagFilter(player) {
									if (!player.countCards('hs')) {
										return false;
									}
								},
								order: 1,
								result: {
									player(player) {
										if (_status.event.dying) {
											return get.attitude(player, _status.event.dying);
										}
										return 1;
									},
								},
							},
						},
						lqtz_longce_count: {
							forced: true,
							popup: false,
							charlotte: true,
							onremove(player) {
								player.storage.lqtz_longce = [];
							},
						},
						lqtz_hundu: {
							audio: 'ext:浪琴天阙/audio:2',
							trigger: { global: 'judgeEnd' },
							preHidden: true,
							forced: true,
							content() {
								'step 0';
								const list = [],
									list2 = ['获得那张判定牌', '对' + get.translation(trigger.player) + '造成一点伤害', '背水!弃置一张牌,并依次执行上述所有选项'];
								if (get.position(trigger.result.card, true) == 'o') {
									list.push('选项一');
								}
								list.push('选项二');
								if (player.countDiscardableCards(player, 'he')) {
									list.push('背水!');
								}
								list.push('cancel2');
								player.chooseControl(list).set('choiceList', list2).set('prompt', '是否发动【魂妒】？').ai = function (target) {
									if (get.attitude(player, trigger.player) > 0) {
										if (list.includes('选项一')) {
											return '选项一';
										}
										return 'cancel2';
									} else {
										if (list.includes('选项一') && list.includes('背水!')) {
											return '背水!';
										}
										return '选项二';
									}
								};
								('step 1');
								if (result.control == '背水!') {
									player.chooseToDiscard('he', true);
								}
								if (result.control == '选项一' || result.control == '背水!') {
									player.gain(trigger.result.card, 'gain2');
								}
								if (result.control == '选项二' || result.control == '背水!') {
									trigger.player.damage();
								}
							},
						},
						lqtz_qiyi: {
							trigger: {
								player: 'phaseBegin',
							},
							forced: true,
							charlotte: true,
							fixed: true,
							content() {
								let a = 0,
									b = game.filterPlayer(function (current) {
										return current.isEnemiesOf(player);
									});
								for (const i of b) {
									a += i.hp;
								}
								player.gainMaxHp(a);
								player.recover(a);
							},
						},
						lqtz_yongzhua: {
							audio: 2,
							charlotte: true,
							fixed: true,
							enable: 'phaseUse',
							filter(event, player) {
								return !game.hasPlayer(function (current) {
									return current.maxHp >= player.maxHp && current.isEnemiesOf(player);
								});
							},
							selectTarget: -1,
							filterTarget(card, player, target) {
								return player.canUse({ name: 'sha' }, target, false) && target.isEnemiesOf(player);
							},
							content() {
								player.useCard({ name: 'sha', nature: 'thunder' }, target, false);
							},
							contentAfter() {
								player.loseMaxHp();
							},
							ai: {
								order(item, player) {
									return get.order({ name: 'sha' }, player) - 0.5;
								},
								result: {
									target(player, target) {
										return get.effect(target, { name: 'sha', nature: 'thunder' }, player, target);
									},
								},
							},
						},
						lqtz_quzhu: {
							trigger: {
								global: 'phaseUseBegin',
							},
							charlotte: true,
							fixed: true,
							forced: true,
							content() {
								'step 0';
								const dialog = [get.prompt('lqtz_quzhu')];
								list2 = lib.inpile.filter(function (i) {
									return get.type(i, false) == 'trick';
								});
								dialog.push('<div class="text center">选择一个牌名</div>');
								dialog.push([list2, 'vcard']);
								player
									.chooseButton(dialog)
									.set('filterButton', function (button) {
										return !_status.event.player.countCards('h', button.link[2]);
									})
									.set('ai', function (button) {
										return get.value({ name: button.link[2] });
									});
								('step 1');
								if (result.links?.length) {
									const a = result.links[0][2],
										b = get.cardPile(function (card) {
											return card.name == a;
										});
									if (trigger.player.countCards('h', a)) {
										trigger.player.discard(trigger.player.getCards('h', a));
									} else if (b) {
										player.gain(b, 'gain2');
									}
								}
							},
						},
						lqtz_lieyan: {
							mod: {
								cardnature(card, player) {
									if (card.name == 'sha') {
										return 'fire';
									}
								},
							},
							charlotte: true,
							fixed: true,
							trigger: {
								player: ['shaMiss', 'eventNeutralized'],
								source: 'damageSource',
							},
							forced: true,
							audio: true,
							filter(event, player) {
								if (event.name == 'damage') {
									return event.player && event.player.isAlive() && event.player.countCards('e');
								}
								return event.type == 'card' && event.card.name == 'sha' && event.target.isAlive();
							},
							content() {
								if (trigger.name == 'damage') {
									trigger.player.discard(trigger.player.getCards('e'));
								} else if (event.triggername == 'shaMiss') {
									trigger.untrigger();
									trigger.trigger('shaHit');
									trigger._result.bool = false;
									trigger._result.result = null;
								} else {
									trigger.unneutralize();
								}
							},
							ai: {
								directHit_ai: true,
								skillTagFilter(player, tag, arg) {
									if (player._guanshi_temp) {
										return;
									}
									player._guanshi_temp = true;
									const bool = get.attitude(player, arg.target) < 0 && arg.card && arg.card.name == 'sha';
									delete player._guanshi_temp;
									return bool;
								},
							},
						},
						lqtz_xiongyan: {
							audio: 2,
							charlotte: true,
							fixed: true,
							enable: 'phaseUse',
							usable: 1,
							selectTarget: -1,
							filterTarget(card, player, target) {
								return target.isEnemiesOf(player);
							},
							multitarget: true,
							multiline: true,
							content() {
								let a = 0,
									b = game.filterPlayer(function (current) {
										return current.isEnemiesOf(player);
									});
								for (const i of b) {
									a += i.countCards('h');
								}
								const c = Math.ceil(a / targets.length);
								for (const i of targets) {
									i.discard(i.getCards('h').randomGets(c));
								}
								for (const i of targets) {
									i.loseHp(c);
								}
							},
							ai: {
								order: 10,
								result: {
									target: -2,
								},
							},
						},
						lqtz_ehui: {
							charlotte: true,
							fixed: true,
							trigger: {
								player: 'damageEnd',
								source: 'damageSource',
							},
							usable: 1,
							forced: true,
							content() {
								'step 0';
								const a = game.findPlayer(function (current) {
									return (
										current.isEnemiesOf(player) &&
										!game.hasPlayer(function (current1) {
											return current1.isEnemiesOf(player) && current1.hp < current.hp;
										})
									);
								});
								event.b = a.hp;
								player
									.chooseTarget('是否发动【恶喙】？', '令一体力最多的敌方角色失去' + get.translation(event.b) + '点体力.', function (card, player, target) {
										return (
											target.isEnemiesOf(player) &&
											!game.hasPlayer(function (current1) {
												return current1.isEnemiesOf(player) && current1.hp > target.hp;
											})
										);
									})
									.set('ai', function (target) {
										return target.isEnemiesOf(player);
									});
								('step 1');
								if (result.targets?.length) {
									result.targets[0].loseHp(event.b);
								} else {
									player.getStat('triggerSkill').lqtz_ehui--;
								}
							},
						},
						lqtz_geming: {
							trigger: {
								player: 'damage',
								source: 'damage',
							},
							charlotte: true,
							fixed: true,
							forced: true,
							filter(event, player) {
								return (event.source && event.source.isEnemiesOf(player)) || (event.player.isEnemiesOf(player) && event.player.hp > 0);
							},
							content() {
								const a = trigger.player == player ? trigger.source : trigger.player;
								a.loseHp(a.hp);
							},
						},
						lqtz_kongxi: {
							charlotte: true,
							fixed: true,
							filterTarget(card, player, target) {
								if (ui.selected.cards.length) {
									const a = get.color(ui.selected.cards[0]);
									if (a == 'black') {
										return player.canUse({ name: 'jingleishan' }, target);
									}
									if (a == 'red') {
										return player.canUse({ name: 'chiyuxi' }, target);
									}
								}
								return true;
							},
							selectTarget: [1, Infinity],
							enable: 'chooseToUse',
							prompt: '将黑色的杀当做惊雷闪,红色的杀当做炽羽袭使用.',
							viewAs(cards, player) {
								let name = false;
								if (get.color(cards[0]) == 'black') {
									name = 'jingleishan';
								}
								if (get.color(cards[0]) == 'red') {
									name = 'chiyuxi';
								}
								if (name) {
									return { name: name };
								}
								return null;
							},
							check(card) {
								return 20 - get.value(card);
							},
							position: 'hs',
							filterCard(card, player, event) {
								event = event || _status.event;
								const filter = event._backup.filterCard;
								const name = get.color(card, player),
									color = card.name;
								if (color == 'sha' && name == 'black' && filter({ name: 'jingleishan', cards: [card] }, player, event)) {
									return true;
								}
								if (color == 'sha' && name == 'red' && filter({ name: 'chiyuxi', cards: [card] }, player, event)) {
									return true;
								}
								return false;
							},
							filter(event, player) {
								const filter = event.filterCard;
								if (
									filter({ name: 'jingleishan' }, player, event) &&
									player.countCards('hs', function (card) {
										return get.color(card) == 'black' && card.name == 'sha';
									})
								) {
									return true;
								}
								if (
									filter({ name: 'chiyuxi' }, player, event) &&
									player.countCards('hs', function (card) {
										return get.color(card) == 'red' && card.name == 'sha';
									})
								) {
									return true;
								}
								return false;
							},
							ai: {
								order(item, player) {
									return get.order({ name: 'sha' }, player) + 0.5;
								},
							},
						},
						lqtz_yingyi: {
							charlotte: true,
							fixed: true,
							audio: 2,
							ai: {
								order: 70,
								result: {
									target(player, target) {
										return -target.countCards('he');
									},
								},
							},
							enable: 'phaseUse',
							usable: 1,
							filter(event, player) {
								return game.hasPlayer(function (current) {
									return player.getFriends().includes(current);
								});
							},
							filterTarget: lib.filter.notMe,
							content() {
								target.discard(target.getCards('he'));
								target.loseHp(target.hp);
							},
						},
						lqtz_wuxiao: {
							charlotte: true,
							fixed: true,
							trigger: {
								player: ['phaseBegin', 'damageEnd', 'loseHpEnd'],
							},
							forced: true,
							filter(event, player) {
								if (event.name == 'phase') {
									return player.countCards('e') < 5;
								}
								return true;
							},
							content() {
								'step 0';
								event.a = trigger.num || 1;
								('step 1');
								event.a--;
								event.b = get.cardPile(function (card) {
									return get.type(card) == 'equip' && player.isEmpty(get.subtype(card));
								});
								if (trigger.name != 'phase') {
									if (player.countCards('e') < 5) {
										player.chooseControlList(true, ['摸' + (2 + player.countCards('e')) + '张牌', (!event.b ? '<font color=#FF3333>(已无合适装备)</font>' : '') + '随机将一张装备牌置于你空置的装备栏内'], function () {
											if (_status.event.player.countCards('e') < 5 && event.b) {
												return 1;
											}
											return 0;
										});
									} else {
										event._result = { index: 0 };
									}
								}
								('step 2');
								if (result.index == 0) {
									player.draw(2 + player.countCards('e'));
								}
								if (event.b && (result.index == 1 || trigger.name == 'phase')) {
									player.$gain2(event.b);
									player.equip(event.b);
								}
								('step 3');
								if (event.a > 0) {
									event.goto(1);
								}
							},
						},
						lqtz_yingbi: {
							charlotte: true,
							fixed: true,
							trigger: {
								player: ['damageBegin4', 'loseHpBegin', 'turnOverBefore'],
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'turnOverBefore') {
									return !player.isTurnedOver();
								}
								return event.num > 2;
							},
							content() {
								if (trigger.name == 'turnOver') {
									trigger.cancel();
									game.log(player, '取消了翻面');
								} else {
									player.draw(trigger.num - 2);
									trigger.num = 2;
								}
							},
						},
						lqtz_guogan: {
							charlotte: true,
							fixed: true,
							trigger: {
								player: ['phaseBegin', 'phaseEnd'],
							},
							forced: true,
							filter(event, player) {
								return game.hasPlayer(function (current) {
									return current.isEnemiesOf(player) && current.countCards('h');
								});
							},
							content() {
								'step 0';
								const a = game.filterPlayer(function (current) {
									return current.isEnemiesOf(player) && current.countCards('h');
								});
								for (const i of a) {
									const f = [];
									if (i.countCards('h')) {
										var b = i.getCards('h'),
											c = b.randomGet();
										f.push(c);
										c.discard(false);
									}
									if (i.countCards('e')) {
										var b = i.getCards('e'),
											c = b.randomGet();
										f.push(c);
										c.discard(false);
									}
									player.line(i, 'green');
									i.update();
									i.give(f, player, true);
									game.log(player, '从', i, '获得了', f);
								}
							},
						},
						lqtz_jianong: {
							charlotte: true,
							fixed: true,
							logTarget: 'player',
							forced: true,
							trigger: {
								target: 'useCardToTargeted',
							},
							content() {
								player.draw();
								if (trigger.player.isEnemiesOf(player)) {
									trigger.player.loseHp();
								}
							},
						},
						lqtz_huoren: {
							charlotte: true,
							fixed: true,
							trigger: {
								player: 'phaseBegin',
							},
							forced: true,
							filter(event, player) {
								return game.hasPlayer(function (current) {
									return current.isEnemiesOf(player) && current.hp >= player.hp;
								});
							},
							content() {
								'step 0';
								let a = player.hp - 1,
									b = 0,
									c = game.filterPlayer(function (current) {
										return current.isEnemiesOf(player) && current.hp >= player.hp;
									});
								for (const i of c) {
									i.loseHp(i.hp - a);
									b += i.hp - a;
								}
								player.recover(b);
							},
						},
						lqtz_fenshao: {
							charlotte: true,
							fixed: true,
							trigger: {
								player: 'dieEnd',
							},
							forced: true,
							forceDie: true,
							content() {
								'step 0';
								a = game.filterPlayer(function (current) {
									return current.isEnemiesOf(player);
								});
								for (const i of a) {
									if (i.isDamaged()) {
										i.loseMaxHp(i.maxHp - i.hp);
									}
								}
								for (const i of a) {
									i.loseHp();
								}
								('step 1');
								if (game.boss) {
									const b = game.filterPlayer(function (current) {
										return current.isEnemiesOf(player);
									});
									const c = Math.ceil(game.boss.hp / b.length);
									for (const i of b) {
										i.discard(i.getCards('h').randomGets(c));
									}
								}
							},
						},
						lqtz_yingyou: {
							charlotte: true,
							fixed: true,
							mod: {
								targetEnabled(card, player, target, now) {
									if (get.type2(card) == 'trick') {
										return false;
									}
								},
							},
							trigger: {
								player: ['damageBegin4', 'loseHpBegin', 'turnOverBefore'],
							},
							forced: true,
							filter(event, player) {
								if (event.name == 'turnOver') {
									return !player.isTurnedOver();
								}
								return event.num > 2;
							},
							content() {
								if (trigger.name == 'turnOver') {
									trigger.cancel();
									game.log(player, '取消了翻面');
								} else {
									const a = trigger.num - 2;
									trigger.num = 2;
									player.draw(a);
									if (_status.currentPhase && _status.currentPhase.isEnemiesOf(player)) {
										_status.currentPhase.loseHp(a);
									} //QQQ
								}
							},
						},
						lqtz_rongguang: {
							charlotte: true,
							fixed: true,
							trigger: {
								global: 'phaseEnd',
							},
							forced: true,
							filter(event, player) {
								return event.player.isEnemiesOf(player) || event.player == player;
							},
							content() {
								const a = game.filterPlayer(function (current) {
									return current.isEnemiesOf(player) && current.countCards('h');
								});
								for (const i of a) {
									i.loseHp();
								}
							},
						},
						lqtz_aoxiang: {
							charlotte: true,
							fixed: true,
							enable: 'phaseUse',
							filter(event, player) {
								return player.countCards('he', function (card) {
									return get.type2(card) == 'trick';
								});
							},
							filterCard(card, event) {
								return get.type2(card) == 'trick';
							},
							check(card) {
								return 20 - get.value(card);
							},
							filterTarget: lib.filter.notMe,
							content() {
								target.addTempSkill('lqtz_aoxiang_f');
								target.loseHp();
							},
							ai: {
								order: 70,
								result: {
									target(player, target) {
										if (!target.hasSkill('lqtz_aoxiang_f')) {
											-target.countCards('he') * 2;
										}
										return -target.countCards('he');
									},
								},
							},
							subSkill: {
								f: {
									charlotte: true,
									forced: true,
									mark: true,
									mod: {
										cardEnabled2(card) {
											if (get.position(card) == 'h') {
												return false;
											}
										},
									},
									intro: {
										content: '不能使用或打出手牌',
									},
								},
							},
						},
						lqtz_yibang: {
							charlotte: true,
							fixed: true,
							trigger: {
								player: 'dieAfter',
							},
							forced: true,
							forceDie: true,
							content() {
								'step 0';
								player.revive(player.maxHp);
								('step 1');
								player.uninit();
								player.init(['lqtz_quzhubojue', 'lqtz_emodiao', 'lqtz_wulilinxiao', 'lqtz_huorenfenshaolieying'].randomGet());
								player.directgain(get.cards(4));
							},
						},
						lqtz_bileix: {
							charlotte: true,
							fixed: true,
							mod: {
								targetEnabled(card, player, target, now) {
									if (
										(player.isEnemiesOf(target) && get.type2(card) == 'trick') ||
										(game.hasPlayer(function (current) {
											return target.getFriends().includes(current);
										}) &&
											player.isEnemiesOf(target) &&
											get.type(card) == 'basic')
									) {
										return false;
									}
								},
							},
							trigger: {
								global: ['damageBegin4', 'loseHpBegin', 'turnOverBefore'],
							},
							forced: true,
							filter(event, player, name) {
								if (name == 'turnOverBefore') {
									return event.player.isFriendsOf(player) && !event.player.isTurnedOver();
								}
								return event.num > 2 && event.player.isFriendsOf(player); //友方包括自己
							},
							content() {
								if (trigger.name == 'turnOver') {
									trigger.cancel();
									game.log(trigger.player, '取消了翻面');
								} else {
									const a = trigger.num;
									trigger.cancel();
									trigger.player.draw(a);
									if (_status.currentPhase && _status.currentPhase.isEnemiesOf(player)) {
										_status.currentPhase.loseHp(a);
									}
								}
							},
						},
						lqtz_baolei: {
							charlotte: true,
							fixed: true,
							trigger: {
								global: 'phaseUseBegin',
							},
							mode: ['boss', 'identity', 'doudizhu', 'guozhan'],
							forced: true,
							filter(event, player) {
								return game.dead.some((q) => q.isFriendsOf(player, true));
							}, //QQQ
							content() {
								const list = game.dead.filter((q) => q.isFriendsOf(player, true));
								const target = list.randomGet();
								if (target) {
									target.revive(target.maxHp);
									target.draw(4, false);
								}
							},
						},
						lqtz_yaosai: {
							charlotte: true,
							fixed: true,
							trigger: {
								player: 'useCard',
							},
							forced: true,
							filter(event, player) {
								return get.type(event.card) == 'trick' || get.type(event.card) == 'basic';
							},
							content() {
								trigger.effectCount += game.countPlayer(function (current) {
									return current.isEnemiesOf(player);
								});
							},
						},
						lqtz_jiwu: {
							charlotte: true,
							fixed: true,
							mod: {
								cardUsable(card, player, num) {
									if (card.name == 'sha') {
										return num + player.countMark('spwuku');
									}
								},
								maxHandcard(player, num) {
									return player.countMark('spwuku') + num;
								},
								globalFrom(from, to, distance) {
									return distance - from.countMark('spwuku');
								},
								globalTo(from, to, distance) {
									return distance + to.countMark('spwuku');
								},
							},
							trigger: {
								player: ['phaseDrawBegin1', 'damageBegin4'],
								source: 'damageBegin',
							},
							forced: true,
							filter(event, player, name) {
								if (!player.hasMark('spwuku')) {
									return false;
								}
								if (name == 'phaseDrawBegin1') {
									return !event.numFixed;
								}
								if (name == 'damageBegin4') {
									return event.num >= player.countMark('spwuku');
								}
								return true;
							},
							content() {
								const a = player.countMark('spwuku');
								if (event.triggername == 'damageBegin4') {
									trigger.num -= a;
								} else if (trigger.name == 'phaseDraw') {
									trigger.num += a;
								} else {
									trigger.cancel();
									trigger.player.loseHp(a);
								}
							},
						},
						lqtz_bangwu: {
							charlotte: true,
							fixed: true,
							trigger: {
								player: 'dieAfter',
							},
							forced: true,
							forceDie: true,
							content() {
								'step 0';
								player.revive(player.maxHp);
								('step 1');
								player.uninit();
								player.init(['lqtz_qiyilieying', 'lqtz_lieyanlieying', 'lqtz_geminglieying', 'lqtz_weixingpaolieying', 'lqtz_jiujilieying'].randomGet());
								player.directgain(get.cards(4));
							},
						},
						lqtz_sanji: {
							charlotte: true,
							fixed: true,
							enable: 'phaseUse',
							usable: 3,
							filter(event, player) {
								return player.hasMark('spwuku');
							},
							content() {
								player.draw(player.countMark('spwuku'));
								player.removeMark('spwuku', player.countMark('spwuku'));
							},
							ai: {
								order() {
									const player = _status.event.player;
									if (player.countMark('spwuku') > 2) {
										return 10;
									}
									return 1;
								},
								result: {
									player: 1,
								},
								effect: {
									target(card, player, target, current) {
										if (get.type(card) == 'equip' && !get.cardtag(card, 'gifts') && player.countMark('spwuku') < 3) {
											return [1, 3];
										}
									},
								},
							},
						},
						lqtz_zhixiao: {
							charlotte: true,
							fixed: true,
							trigger: {
								player: 'phaseZhunbeiBegin',
							},
							forced: true,
							content() {
								'step 0';
								player.judge(function (card) {
									return card.number;
								});
								('step 1');
								const a = [];
								get.cardPile(function (card) {
									if (get.type2(card) == 'trick') {
										a.push(card);
									}
								});
								if (a.length) {
									player.gain(a.randomGets(result.number), 'gain2');
								}
							},
						},
						lqtz_shengjie: {
							derivation: ['lqtz_jixizhili', 'lqtz_mengqinzhili', 'lqtz_gemingzhili', 'lqtz_tiaoyuezhili', 'lqtz_xuehunzhili', 'lqtz_siwangshuangli'],
							charlotte: true,
							fixed: true,
							enable: 'phaseUse',
							usable: 1,
							content() {
								let b = [];
								for (const i of ['lqtz_jixizhili', 'lqtz_mengqinzhili', 'lqtz_gemingzhili', 'lqtz_tiaoyuezhili', 'lqtz_xuehunzhili', 'lqtz_siwangshuangli']) {
									if (!lib.card[i]) {
										b.push(i);
									}
								}
								game.log(b);
								if (b.length) {
									b = b.randomGet();
								} else {
									b = ['lqtz_jixizhili', 'lqtz_mengqinzhili', 'lqtz_gemingzhili', 'lqtz_tiaoyuezhili', 'lqtz_xuehunzhili', 'lqtz_siwangshuangli'].randomGet();
								}
								if (!lib.card[b]) {
									lib.card[b] = get.copy(lib.skill.lqtz_shengjie[b]);
									lib.translate[b] = get.translation(b);
									lib.translate[b + '_info'] = get.translation(b + '_info');
									game.broadcastAll(function () {
										lib.inpile.add(b);
									});
								}
								player.gain(game.createCard(b), 'gain');
							},
							ai: {
								order: 10,
								result: {
									player: 1,
								},
							},
							group: 'lqtz_shengjie_add',
							subSkill: {
								add: {
									trigger: {
										player: 'useCardEnd',
									},
									forced: true,
									filter(event, player) {
										const a = [];
										get.cardPile2(function (card) {
											if (get.tag(card, 'Ascending')) {
												a.push(card);
											}
										});
										return a.length < 8 && get.tag(event.cards[0], 'Ascending') && get.itemtype(event.cards) == 'cards' && get.position(event.cards[0], true) == 'o';
									},
									content() {
										ui.cardPile.insertBefore(trigger.cards[0], ui.cardPile.childNodes[get.rand(0, ui.cardPile.childNodes.length)]);
										game.updateRoundNumber();
										game.log(player, '将', trigger.cards[0], '置入了牌组中的随机位置');
									},
								},
							},
							lqtz_jixizhili: {
								enable: true,
								type: 'trick',
								fullskin: true,
								derivation: 'lqtz_zhihuilinxiao',
								filterTarget: true,
								content() {
									'step 0';
									target.addSkill('lqtz_shengjie2');
									if (!target.storage.lqtz_jixizhili) {
										target.storage.lqtz_jixizhili = 0;
									}
									target.storage.lqtz_jixizhili++;
									if (target.hasSkill('lqtz_shengjie')) {
										target.chooseToDiscard('he', '急袭之力:你可以弃置一张牌,获得一张升级魔法.').set('ai', function (card) {
											return -get.value(card);
										});
									}
									('step 1');
									if (result.bool) {
										const b = ['lqtz_mengqinzhili', 'lqtz_gemingzhili', 'lqtz_tiaoyuezhili', 'lqtz_xuehunzhili', 'lqtz_siwangshuangli'].randomGet();
										if (!lib.card[b]) {
											lib.card[b] = get.copy(lib.skill.lqtz_shengjie[b]);
											lib.translate[b] = get.translation(b);
											lib.translate[b + '_info'] = get.translation(b + '_info');
											game.broadcastAll(function () {
												lib.inpile.add(b);
											});
										}
										target.gain(game.createCard(b), 'gain');
									}
									player.draw();
								},
								ai: {
									order: 7.1,
									useful: 4.5,
									value: 9.2,
									result: {
										target(player, target) {
											if (target.hasSkill('lqtz_shengjie')) {
												return 2;
											}
											return 0.1;
										},
										player: 1,
									},
									tag: {
										Ascending: 1,
										draw: 1,
									},
								},
								selectTarget: 1,
								image: 'ext:浪琴天阙/image/卡牌图/lqtz_jixizhili.png',
							},
							lqtz_mengqinzhili: {
								enable: true,
								type: 'trick',
								fullskin: true,
								derivation: 'lqtz_zhihuilinxiao',
								filterTarget: true,
								content() {
									target.addSkill('lqtz_shengjie2');
									if (!target.storage.lqtz_mengqinzhili) {
										target.storage.lqtz_mengqinzhili = 0;
									}
									target.storage.lqtz_mengqinzhili++;
									if (target.hasSkill('lqtz_shengjie')) {
										target.draw(2);
									}
									player.draw();
								},
								ai: {
									order: 7.3,
									useful: 4.5,
									value: 9.2,
									result: {
										target(player, target) {
											if (target.hasSkill('lqtz_shengjie')) {
												return 2;
											}
											return 0.1;
										},
										player: 1,
									},
									tag: {
										Ascending: 1,
										draw: 1,
									},
								},
								selectTarget: 1,
								image: 'ext:浪琴天阙/image/卡牌图/lqtz_mengqinzhili.png',
							},
							lqtz_gemingzhili: {
								enable: true,
								type: 'trick',
								fullskin: true,
								derivation: 'lqtz_zhihuilinxiao',
								filterTarget: true,
								content() {
									'step 0';
									target.addSkill('lqtz_shengjie2');
									if (!target.storage.lqtz_gemingzhili) {
										target.storage.lqtz_gemingzhili = 0;
									}
									target.storage.lqtz_gemingzhili++;
									if (target.hasSkill('lqtz_shengjie')) {
										event.a = target;
										event.a
											.chooseTarget('革命之力:你可以获得一名其他角色一张牌', function (card, player, target) {
												return target != event.a && target.countGainableCards(event.a, 'he');
											})
											.set('ai', function (target) {
												return -get.attitude(event.a, target);
											});
									}
									('step 1');
									if (result.targets?.length) {
										event.a.gainPlayerCard('he', result.targets[0], true);
									}
									player
										.chooseControl('basic', 'trick', 'equip')
										.set('prompt', '选择获得一种类型的牌')
										.set('ai', function () {
											const player = _status.event.player;
											if (player.hp <= 3 && !player.countCards('h', { name: ['shan', 'tao'] })) {
												return 'basic';
											}
											if (player.countCards('he', { type: 'equip' }) < 2) {
												return 'equip';
											}
											return 'trick';
										});
									('step 2');
									const card = get.cardPile(function (card) {
										return get.type(card, 'trick') == result.control;
									});
									if (card) {
										player.gain(card, 'gain2', 'log');
									}
								},
								ai: {
									order: 7.5,
									useful: 4.5,
									value: 9.2,
									result: {
										target(player, target) {
											if (target.hasSkill('lqtz_shengjie')) {
												return 2;
											}
											return 0.1;
										},
										player: 1,
									},
									tag: {
										Ascending: 1,
										draw: 1,
									},
								},
								selectTarget: 1,
								image: 'ext:浪琴天阙/image/卡牌图/lqtz_gemingzhili.png',
							},
							lqtz_tiaoyuezhili: {
								enable: true,
								type: 'trick',
								fullskin: true,
								derivation: 'lqtz_zhihuilinxiao',
								filterTarget: true,
								content() {
									'step 0';
									target.addSkill('lqtz_shengjie2');
									if (!target.storage.lqtz_tiaoyuezhili) {
										target.storage.lqtz_tiaoyuezhili = 0;
									}
									target.storage.lqtz_tiaoyuezhili += 2;
									event.a = target;
									if (target.hasSkill('lqtz_shengjie') && get.position(cards[0]) == 'd') {
										if (target != player) {
											target.gain(cards[0], 'gain2');
										} else {
											event.a
												.chooseTarget('跳跃之力:你可以将' + get.translation(cards[0]) + '交给一名其他角色.', function (card, player, target) {
													return target != event.a;
												})
												.set('ai', function (target) {
													return get.attitude(event.a, target);
												});
										}
									}
									('step 1');
									if (result.targets?.length) {
										result.targets[0].gain(cards[0], 'gain2');
									}
									player.draw();
								},
								ai: {
									order: 7,
									useful: 5,
									value: 7,
									result: {
										target(player, target) {
											if (target.hasSkill('lqtz_shengjie')) {
												return 2;
											}
											return 0.1;
										},
										player: 1,
									},
									tag: {
										Ascending: 1,
										draw: 1,
									},
								},
								selectTarget: 1,
								image: 'ext:浪琴天阙/image/卡牌图/lqtz_tiaoyuezhili.png',
							},
							lqtz_xuehunzhili: {
								enable: true,
								type: 'trick',
								fullskin: true,
								derivation: 'lqtz_zhihuilinxiao',
								filterTarget: true,
								content() {
									'step 0';
									target.addSkill('lqtz_shengjie2');
									if (!target.storage.lqtz_xuehunzhili) {
										target.storage.lqtz_xuehunzhili = 0;
									}
									target.chooseBool('削魂之力:是否失去一点体力,令你下次的判定牌最终生效时,点数改为+4.').set('ai', function () {
										if (target.hasSkill('lqtz_shengjie')) {
											return true;
										}
										return false;
									});
									('step 1');
									if (result.bool) {
										target.loseHp();
										target.storage.lqtz_xuehunzhili += 4;
									} else {
										target.storage.lqtz_xuehunzhili += 2;
									}
									player.draw();
								},
								ai: {
									order: 7,
									useful: 4.5,
									value: 7,
									result: {
										target(player, target) {
											if (target.hasSkill('lqtz_shengjie')) {
												return 2;
											}
											return 0.1;
										},
										player: 1,
									},
									tag: {
										Ascending: 1,
										draw: 1,
									},
								},
								selectTarget: 1,
								image: 'ext:浪琴天阙/image/卡牌图/lqtz_xuehunzhili.png',
							},
							lqtz_siwangshuangli: {
								enable: true,
								type: 'trick',
								fullskin: true,
								derivation: 'lqtz_zhihuilinxiao',
								filterTarget: true,
								content() {
									if (!target.storage.lqtz_siwangshuangli) {
										target.storage.lqtz_siwangshuangli = 1;
									}
									if (target.storage.lqtz_siwangshuangli) {
										target.storage.lqtz_siwangshuangli++;
									}
									target.addSkill('lqtz_shengjie2');
									player.draw();
								},
								ai: {
									order: 7,
									useful: 4.5,
									value: 8,
									result: {
										target(player, target) {
											if (target.hasSkill('lqtz_shengjie')) {
												return 2;
											}
											return 0.1;
										},
										player: 1,
									},
									tag: {
										Ascending: 1,
										draw: 1,
									},
								},
								selectTarget: 1,
								image: 'ext:浪琴天阙/image/卡牌图/lqtz_siwangshuangli.png',
							},
						},
						lqtz_shengjie2: {
							trigger: {
								player: 'judge',
							},
							onremove(player) {
								for (const i of ['lqtz_jixizhili', 'lqtz_mengqinzhili', 'lqtz_gemingzhili', 'lqtz_tiaoyuezhili', 'lqtz_xuehunzhili', 'lqtz_siwangshuangli']) {
									if (player.storage[i]) {
										delete player.storage[i];
									}
								}
							},
							mark: true,
							marktext: '升',
							intro: {
								name: '升阶魔法',
								mark(dialog, content, player) {
									if (player.storage.lqtz_jixizhili) {
										dialog.addText('急袭之力:+' + player.storage.lqtz_jixizhili);
									}
									if (player.storage.lqtz_mengqinzhili) {
										dialog.addText('猛禽之力:+' + player.storage.lqtz_mengqinzhili);
									}
									if (player.storage.lqtz_gemingzhili) {
										dialog.addText('革命之力:+' + player.storage.lqtz_gemingzhili);
									}
									if (player.storage.lqtz_tiaoyuezhili) {
										dialog.addText('跳跃之力:+' + player.storage.lqtz_tiaoyuezhili);
									}
									if (player.storage.lqtz_xuehunzhili) {
										dialog.addText('削魂之力:+' + player.storage.lqtz_xuehunzhili);
									}
									if (player.storage.lqtz_siwangshuangli) {
										dialog.addText('死亡双力:×' + player.storage.lqtz_siwangshuangli);
									}
								},
							},
							forced: true,
							lastDo: true, //判定最后
							charlotte: true, //夏洛特
							content() {
								'step 0';
								const b = [];
								if (player.storage.lqtz_jixizhili) {
									b.push('lqtz_jixizhili');
								}
								if (player.storage.lqtz_mengqinzhili) {
									b.push('lqtz_mengqinzhili');
								}
								if (player.storage.lqtz_gemingzhili) {
									b.push('lqtz_gemingzhili');
								}
								if (player.storage.lqtz_tiaoyuezhili) {
									b.push('lqtz_tiaoyuezhili');
								}
								if (player.storage.lqtz_xuehunzhili) {
									b.push('lqtz_xuehunzhili');
								}
								if (player.storage.lqtz_siwangshuangli) {
									b.push('lqtz_siwangshuangli');
								}
								event.b = b;
								event.c = trigger.judge(player.judging[0]);
								('step 1');
								if (event.b.length > 1) {
									player
										.chooseControl(event.b)
										.set('ai', function () {
											return 0;
										})
										.set('prompt', '选择你要优先使用的升阶效果.');
								} else {
									event._result = { control: event.b[0] };
								}
								('step 2');
								if (result.control != 'lqtz_siwangshuangli') {
									event.c += player.storage[result.control];
								} else {
									event.c = event.c * player.storage[result.control];
								}
								delete player.storage[result.control];
								event.b.remove(result.control);
								if (event.b.length) {
									event.goto(1);
								} else {
									game.log(player, '判定结果点数为', '#g' + event.c);
									if (!trigger.fixedResult) {
										trigger.fixedResult = {};
									}
									trigger.fixedResult.number = event.c;
								}
								('step 3');
								player.removeSkill('lqtz_shengjie2');
							},
						},
						//暂时分界线
						lqtz_katong: {
							shaRelated: true,
							trigger: {
								player: 'useCardToPlayered',
							},
							check(event, player) {
								return get.attitude(player, event.target) <= 0;
							},
							filter(event, player) {
								return (event.card && event.card.name == 'sha') || event.card.name == 'juedou';
							},
							logTarget: 'target',
							preHidden: true,
							content() {
								player.addTempSkill('unequip', { player: 'useCardAfter' });
								trigger.parent.directHit.add(trigger.target);
								player.storage.lqtz_katong = trigger.card;
								player.addTempSkill('lqtz_katong_damage', { player: 'useCardAfter' });
							},
							subSkill: {
								damage: {
									trigger: {
										source: ['damageBegin2', 'damageEnd'],
									},
									filter(event, player) {
										return event.card && player.storage.lqtz_katong && player.storage.lqtz_katong == event.card;
									},
									forced: true,
									content() {
										if (event.triggername == 'damageBegin2') {
											trigger.num++;
										} else {
											player.draw();
										}
									},
								},
							},
						},
						lqtz_zuiyan: {
							trigger: { global: 'phaseEnd' },
							forced: true,
							filter(event, player) {
								return player.getEnemies().includes(event.player);
							},
							content() {
								if (trigger.player.getStat().damage > 0) {
									trigger.player.damage(2, 'fire');
								} else {
									trigger.player.loseHp();
									if (game.boss) {
										game.boss.draw();
									}
								}
							},
						},
						lqtz_heiya: {
							trigger: {
								source: 'damageSource',
								player: 'damageEnd',
							},
							filter(event, player) {
								return event.num > 0;
							},
							forced: true,
							usable: 1,
							content() {
								'step 0'; //是否发动
								player
									.chooseTarget(get.prompt('lqtz_heiya'), '令一名其他角色交给你一张牌.', function (card, player, target) {
										return target != player;
									})
									.set('ai', function (target) {
										return -get.attitude(_status.event.player, target);
									});
								('step 1');
								if (result.targets?.length) {
									event.target = result.targets[0];
									if (event.target.countCards('he', { color: 'black' })) {
										event.target.chooseCard('交给' + get.translation(player) + '一张黑色牌', true, function (card) {
											return get.color(card) == 'black';
										});
									} else {
										event.target.loseHp();
										event.finish();
									}
								} else {
									player.getStat('triggerSkill').lqtz_heiya--;
								}
								('step 2');
								if (result.cards?.length) {
									player.gain(result.cards, event.target, 'give');
									player.getStat('triggerSkill').lqtz_heiya--;
								}
							},
						},
						lqtz_heigang: {
							trigger: {
								global: 'useCard',
							},
							forced: true,
							filter(event, player) {
								return get.zhinangs().includes(event.card.name);
							},
							content() {
								player.changeHujia();
								if (trigger.respondTo && trigger.player != player && player == trigger.respondTo[0]) {
									player.draw();
								}
							},
						},
						lqtz_angang: {
							enable: 'phaseUse',
							usable: 1,
							content() {
								if (game.boss) {
									game.boss.draw();
								}
							},
							group: 'lqtz_angang_die',
							subSkill: {
								die: {
									trigger: {
										player: 'dieBegin',
									},
									forced: true,
									filter(event, player) {
										return game.hasPlayer(function (current) {
											return player.getFriends().includes(current) && current != game.boss;
										});
									},
									content() {
										'step 0';
										player.chooseTarget(get.prompt('lqtz_angang'), '选择一名友方角色', function (card, player, target) {
											return target.isFriendsOf(player);
										}).ai = function (target) {
											if (
												!game.hasPlayer(function (current) {
													return current.hp > target && target.isFriendsOf(_status.event.player);
												})
											) {
												return 2;
											}
											return 1;
										};
										('step 1');
										if (result.bool) {
											trigger.cancel();
											const num1 = result.targets[0].hp - 1,
												num = num1 + 1;
											if (num1 > 0) {
												result.targets[0].loseHp(num1);
											}
											player.recover(num);
										}
									},
								},
							},
						},
						lqtz_longquan: {
							trigger: {
								global: 'useCard',
							},
							//usable:2,
							filter(event, player) {
								return get.type2(event.card) != 'equip' && trigger.player != player;
							},
							check(event, player) {
								return get.attitude(player, event.player) < 0; //QQQ
							},
							content() {
								'step 0';
								trigger.player
									.chooseToDiscard('he', '###【龙拳】###弃置一张' + get.translation(get.type2(trigger.card)) + '牌.', function (card, player, target) {
										return get.type2(card) == get.type2(trigger.card);
									})
									.set('ai', function (card) {
										return 8 - get.value(card);
									});
								('step 1');
								if (!result.bool) {
									const num = [1, 2, 3].randomGet();
									if (num == 1) {
										trigger.targets.length = 0;
										trigger.all_excluded = true;
										game.log(player, '取消了', trigger.card, '的所有目标');
									} else if (num == 2) {
										player.gain(trigger.cards, 'draw2');
									} else {
										trigger.player.damage('fire');
									}
								}
							},
						},
						lqtz_annang: {
							trigger: {
								global: 'damageEnd',
							},
							forced: true,
							filter(event, player) {
								return trigger.player.getFriends().includes(player) && trigger.player != player && event.num > 0;
							},
							content() {
								if (player.isDamaged()) {
									player.recover();
								} else if (player.hujia < player.countCards('h')) {
									player.changeHujia();
								} else {
									player.draw();
								}
							},
						},
						lqtz_annang: {
							trigger: {
								player: 'gainBegin',
							},
							content() {
								player.init('liubei', 'caocao');
							},
						},
						// --------------------------------------武将/技能翻译------------------------------------------//
					},
					dynamicTranslate: {
						lqym_mieyan(player) {
							if (player.storage.lqym_mieyan == true) {
								return '转换技,阴:出牌阶段限一次,对一名其他角色造成一点火焰伤害,该角色随机弃置一张牌.<span class="bluetext">阳:出牌阶段限一次,弃置一名其他角色一张手牌,若那张牌的名称字数大于2,你对该角色造成一点雷电伤害,否则,你摸两张牌.</span>';
							}
							return '转换技,<span class="bluetext">阴:出牌阶段限一次,对一名其他角色造成一点火焰伤害,该角色随机弃置一张牌.</span>阳:出牌阶段限一次,弃置一名其他角色一张手牌,若那张牌的名称字数大于2,你对该角色造成一点雷电伤害,否则,你摸两张牌.';
						},
						lqym_shuanghuang(player) {
							if (player.storage.lqym_shuanghuang == true) {
								return '转换技,阴:其他角色使用普通锦囊牌时,你可以取消那张牌的所有目标,若那张牌的点数不为数字,你获得那张牌.<span class="bluetext">阳:你使用【杀】指定目标时,你可以获得目标装备区一张牌并使用之.</span>';
							}
							return '转换技,<span class="bluetext">阴:其他角色使用普通锦囊牌时,你可以取消那张牌的所有目标,若那张牌的点数不为数字,你获得那张牌..</span>阳:你使用【杀】指定目标时,你可以获得目标装备区一张牌并使用之.';
						},
						lqhc_yishi(player) {
							if (player.storage.lqhc_yishi == true) {
								return '转换技,阴:你可以视为使用一张【杀】.<span class="bluetext">阳:你可以视为使用一张【闪】.</span>';
							}
							return '转换技,<span class="bluetext">阴:你可以视为使用一张【杀】.</span>阳:你可以视为使用一张【闪】.';
						},
						lqhc_shence(player) {
							if (player.storage.lqhc_shence == true) {
								return '出牌阶段限一次<span class="bluetext">或你受到伤害后</span>,你可以弃置一张牌并转移场上一张牌,你将手牌摸至X(X为你的体力上限).';
							}
							return '出牌阶段限一次,你可以弃置一张牌并转移场上一张牌,你将手牌摸至X(X为你的体力上限).';
						},
						lqtz_tianwei(player) {
							const info = lib.skill.lqtz_tianwei.getInfo(player),
								zi = '锁定技,①一名角色的回合结束后,若你于回合内造成过伤害,你获得一枚<甲>标记.②你使用的非转化牌造成伤害或你受到伤害后,你获得X枚<甲>标记(X为伤害数).③你的体力大于10时,你视为拥有技能【咆哮】和【无双】,否则你视为拥有技能【龙魂】和【遗计】.';
							if (info < 10) {
								return zi + '<span style="text-decoration: line-through;">④你的体力不大于<span class=firetext>' + info + '</span>时,你将体力回复至<span class=firetext>' + info + '</span>并终止一切结算,立即开始你的回合,令【天威④】中的数值-10</span>.';
							}
							return zi + '④你的体力不大于<span class=firetext>' + info + '</span>时,你将体力回复至<span class=firetext>' + info + '</span>并终止一切结算,立即开始你的回合,令【天威④】中的数值-10.';
						},
						lqym_modao(player) {
							if (player.storage.lqym_modao1 == true) {
								return '出牌阶段限一次,你可以弃置一张牌,根那张牌的花色创建并获得一张与该牌相同花色和点数的「魔导牌」.<br>♠️️️️:【千把刀】<br>♥️️️️:【魔术咒文书】<br>♣️️️️:【死亡魔术箱】<br>♦️️️️:【魔术礼帽】';
							}
							return '使命技:出牌阶段限一次,你可以弃置一张牌,根那张牌的花色创建并获得一张与该牌相同花色和点数的「魔导牌」,记录此牌的花色.<br><b>成功:</b>准备阶段,若你记录的花色大于2,你创建并获得一张你未记录花色对应的「魔导牌」,并获得技能〖魔阵〗.<br><b>失败:</b>你因〖魔导〗而获得你已记录了相同花色的牌时,你不失去〖魔导〗改为修改〖魔导〗.<br>♠️️️️:【千把刀】<br>♥️️️️:【魔术咒文书】<br>♣️️️️:【死亡魔术箱】<br>♦️️️️:【魔术礼帽】';
						},
					},
					translate: {
						lqtq_luolijisi: '萝莉祭司',
						lqtq_yibiaotangtang: '亿婊堂堂',
						lqtq_tufaqixiang: '突发奇想',
						lqtq_xianzuogaizhi: '闲做改值',
						lqtq_xiyouzhuanshu: '稀有专属',
						lqtq_qunyingtougaojing: '群嘤投稿·精',
						lqtq_qunyingtougaozuo: '群嘤投稿·琢',
						lqtq_qunyingtougaogai: '群嘤投稿·改',
						lqtq_jiedouguaishou: '决斗怪兽',
						lqtq_Noxilie: 'No系列',
						lqtq_ciyuanshengwei: '超越界限 ·标',
						lqtz_shanhaizhiyihun: '山海志异·魂魂觉醒',
						lqtz_shanhaizhiyinian: '山海志异·驱逐年兽',
						lqtz_shanhaizhiyigui: '山海志异·驱鬼辟邪',
						lqtz_shanhaizhiyirui: '山海志异·瑞麟降世',
						lqtz_shanhaizhiyihu: '山海志异·虎虎生威',
						lqtz_shanhaizhiyiqing: '山海志异·青丘弄琴',
						lqtz_shanhaizhiyi: '山海志异',
						lqtz_quguizhuxie: '驱鬼逐邪',
						lqtz_aolihagang: '奥利哈刚',
						lqtz_danjiangboss: '单将BOSS',
						lqtz_tianweishenjia: '天威神甲',
						lqtz_langqintianque: '浪琴天阙',
						lqtz_luanzhantianque: '乱战天阙',
						lqtz_qingqingzijin: '青青子衿',
						lqtz_jiximengqin: '急袭猛禽',
						lqtq_abiao: '啊婊',
						lqtqre_abiao: '界啊婊',
						lqtq_miaoxiaoyu_ab: '喵小羽',
						lqtq_miaoxiaoyu: '千面猫&浪琴婊',
						lqtq_baibanbiao: '白板婊',
						lqtq_langqinjun: '浪琴君',
						lqtq_xiutianbiao: '修天婊',
						lqtq_yufengbiao: '御风婊',
						lqtq_dubiao: '赌婊',
						lqtqt_zuoci: '☄左慈',
						lqtqt_yuji: '☄于吉',
						lqtqt_caiwenji: '☄蔡文姬',
						lqtqt_majun: '☄马钧',
						lqtqt_guojia: '☄郭嘉',
						lqtqt_duyu: '☄杜预',
						lqtqt_huangyueying: '☄黄月英',
						lqtqt_zhugeguo: '☄诸葛果',
						lqtqt_baosanniang: '☄鲍三娘',
						lqtqt_zhouchu: '☄周处',
						lqtqt_daxiaoqiao: '☄大乔小乔',
						lqtqt_caojinyu: '☄曹金玉',
						lqtqt_sunhanhua: '☄孙寒华',
						lqtqt_guanyinping: '☄关银屏',
						lqtqt_sunshangxiang: '☄孙尚香',
						lqtqj_linglong: '灵龙',
						lqtqj_hongdie: '红爹',
						lqtqj_qixian: '琪仙',
						lqtqj_shashen: '杀神',
						lqtqx_zhonghui: '◎钟会',
						lqtqx_dengai: '◎邓艾',
						lqtqx_xusheng: '◎徐盛',
						lqtqx_huangzhong: '◎黄忠',
						lqtqxshen_sunce: '◎神孙策',
						lqtqxshen_guojia: '◎神郭嘉',
						lqtqxshen_ganning: '◎神甘宁',
						lqtqxshen_xunyu: '◎神荀彧',
						lqym_zhenhongyanheilong: '真红眼黑龙',
						lqym_modaolongqishi: '魔导龙骑士',
						lqym_zhanmolongqishi: '斩魔龙骑士',
						lqym_qingyanheilong: '青眼黑龙',
						lqym_heimodao: '黑魔导',
						lqym_qingyanbailong: '青眼白龙',
						'lqym_No.0_ab': '未来皇霍普',
						'lqym_No.0': 'No.0 未来皇 霍普',
						'lqym_No.1_ab': '巴力西卜',
						'lqym_No.1': 'No.1 感染蝇王 巴力·西卜',
						'lqym_No.2_ab': '影蚊',
						'lqym_No.2': 'No.2 蚊学忍者 影蚊',
						'lqym_No.3_ab': '地狱蝉王',
						'lqym_No.3': 'No.3 地狱蝉王',
						'lqym_No.4_ab': '隐形水母怪',
						'lqym_No.4': 'No.4 猛毒刺胞 隐形水母怪',
						lqym_diezhuangyouti_ab: '碟状幼体',
						lqym_diezhuangyouti: '隐形水母怪碟状幼体',
						'lqym_No.5_ab': '死亡嵌合龙',
						'lqym_No.5': 'No.5 亡胧龙 死亡嵌合龙',
						'lqym_No.6_ab': '大西洲巨人',
						'lqym_No.6': 'No.6 先史遗产 大西洲巨人',
						'lqym_No.7_ab': '幸运条纹人',
						'lqym_No.7': 'No.7 幸运条纹人',
						'lqym_No.8_ab': '基因组继承者',
						'lqym_No.8': 'No.8 纹章王 基因组继承者',
						'lqym_No.9_ab': '戴森球',
						'lqym_No.9': 'No.9 天盖星 戴森球',
						'lqym_No.10_ab': '启明者',
						'lqym_No.10': 'No.10 白辉士 启明者',
						'lqym_No.11_ab': '巨眼',
						'lqym_No.11': 'No.11 巨眼',
						'lqym_No.12_ab': '红影',
						'lqym_No.12': 'No.12 机甲忍者 红影',
						'lqym_No.13_ab': '该隐恶魔',
						'lqym_No.13': 'No.13 该隐恶魔',
						'lqym_No.14_ab': '强欲死神犬',
						'lqym_No.14': 'No.14 强欲死神犬',
						'lqym_No.15_ab': '巨人杀手',
						'lqym_No.15': 'No.15 机关傀儡 巨人杀手',
						'lqym_No.16_ab': '涩之支配者',
						'lqym_No.16': 'No.16 色之支配者',
						'lqym_No.17_ab': '海恶龙',
						'lqym_No.17': 'No.17 海恶龙',
						'lqym_No.18_ab': '素徽',
						'lqym_No.18': 'No.18 纹章祖 素徽',
						'lqym_No.19_ab': '冷冻龙',
						'lqym_No.19': 'No.19 冷冻龙',
						'lqym_No.20_ab': '辉蚁',
						'lqym_No.20': 'No.20 蚁岩土 辉蚁',
						'lqym_No.21_ab': '冰结正义女郎',
						'lqym_No.21': 'No.21 冰结正义女郎',
						'lqym_No.22_ab': '不乱健',
						'lqym_No.22': 'No.22 不乱健',
						'lqym_No.23_ab': '兰斯洛特',
						'lqym_No.23': 'No.23 冥界的灵骑士 兰斯洛特',
						'lqym_No.31_ab': '亚伯恶魔',
						'lqym_No.31': 'No.31 亚伯恶魔',
						'lqym_No.66_ab': '霸键甲虫',
						'lqym_No.66': 'No.66 霸键甲虫',
						'lqym_No.76_ab': '渐变天乐斗士',
						'lqym_No.76': 'No.76 谐调光师 渐变天乐斗士',
						'lqym_No.78_ab': '编号系档案馆',
						'lqym_No.78': 'No.78 编号系档案馆',
						lqhc_qin: '琴',
						lqhc_hanxin: '韩信',
						lqhc_xiangyu: '项羽',
						lqhc_chenping: '陈平',
						lqhc_yitianxing: '弈天行',
						lqhc_xinlianyue: '辛涟月',
						lqhc_hanmaojun: '憨猫君',
						lqhc_fengxuanjun: '风玄君',
						lqhc_tushanhonghong: '涂山红红',
						lqhc_dongfangyuechu: '东方月初',
						lqhc_xiangquyuanfang: '想去远方',
						lqhc_qijing_ab: '棋经',
						lqhc_qijing: '<棋经>',
						lqhc_shence: '神策',
						lqhc_yilite: '伊力特',
						lqhc_gujian: '古见',
						lqhc_yinbuniao: '银布鸟',
						lqhcgaishen_jiangwei: '❁神姜维',
						lqhcgai_lvmeng: '❁神吕蒙',
						lqhcgai_zhaoyun: '❁赵云',
						lqhcgaix_zhaoyun: '❁赵云',
						lqhcgai_zhangfei: '❁神张飞',
						lqhcgai_simashi: '❁司马师',
						lqhcgai_simayi: '❁司马懿',
						lqhcgai_liuye: '❁刘晔',
						lqhcgai_mayuanyi: '❁马元义',
						lqhcgai_xuhuang: '❁徐晃',
						lqhcgai_jiangwei: '❁姜维',
						lqcy_caocao: '罠曹操',
						lqcy_guojia: '罠郭嘉',
						lqcy_lidian: '罠李典',
						lqcy_daqiao: '罠大乔',
						lqtz_quzhunianshou2: '驱逐年兽',
						lqtz_jianghunjuexing2: '将魂觉醒',
						lqtz_quguibixie2: '驱鬼辟邪',
						lqtz_ruilinjiangshi2: '瑞麟降世',
						lqtz_huhushengwei2: '虎虎生威',
						lqtz_qingqiunongqin2: '青丘弄琴',
						lqtz_aolihagang: '奥利哈刚',
						lqtzhun_xuzhu: '魂·许褚',
						lqtzhun_simayi: '魂·司马懿',
						lqtzhun_caocao: '魂·曹操',
						lqtzhun_ganning: '魂·甘宁',
						lqtzhun_zhouyu: '魂·周瑜',
						lqtzhun_sunquan: '魂·孙权',
						lqtzhun_guanyu: '魂·关羽',
						lqtzhun_zhugeliang: '魂·诸葛亮',
						lqtzhun_liubei: '魂·刘备',
						lqtzhun_diaochan: '魂·貂蝉',
						lqtzhun_lubu: '魂·神吕布',
						lqtzhun_zhouyu: '魂·周瑜',
						lqtz_chi: '魑',
						lqtz_mei: '魅',
						lqtz_wang: '魍',
						lqtz_liang: '魉',
						lqtz_niutou: '牛头',
						lqtz_mamian: '马面',
						lqtz_nianshou: '年兽',
						lqtz_yanluowang: '阎罗王',
						lqtz_guiwang: '鬼王',
						lqtz_riyeyoushen: '日夜游神',
						lqtz_niutoumamian: '牛头马面',
						lqtz_heibaiwuchang: '黑白无常',
						lqtz_mengpoa: '孟婆',
						lqtz_yvsai: '鱼鳃',
						lqtz_niaozui: '鸟嘴',
						lqtz_huangfeng: '黄蜂',
						lqtz_baowei: '豹尾',
						lqtz_langqinbiao: '浪琴婊',
						lqtz_langqinyu: '浪琴羽',
						lqtz_qidala: '奇达拉',
						lqtz_jingzishibing: '镜子士兵',
						lqtz_qijiasi: '奇甲斯',
						lqtz_xiruonuosi: '西若诺斯',
						lqtz_xiruonuosizuoshou: '西若诺斯左手',
						lqtz_xiruonuosiyoushou: '西若诺斯右手',
						lqtz_shantongshenou: '山铜神偶',
						lqtz_sheshenyi: '蛇神藝',
						lqtz_dazi: '达姿',
						lqtz_zhishen: '奥利哈刚之神',
						lqtz_luocha: '罗刹',
						lqtz_yecha: '夜叉',
						lqtz_heiwuchang: '黑无常',
						lqtz_baiwuchang: '白无常',
						lqtz_zhangrang: '十常侍张让',
						lqtz_machao: '永远的神',
						lqtz_sanguoliemaren: '三国猎马人',
						lqtzqqzj_caocao: '曹操',
						lqtzqqzj_simayi: '司马懿',
						lqtzqqzj_lubu: '吕布',
						lqtzqqzj_dongzhuo: '董卓',
						lqtzqqzj_zhangjiao: '张角',
						lqtzqqzj_yuanshu: '袁术',
						lqtz_qiongqi: '穷奇',
						lqtz_hundun: '混沌',
						lqtz_taowu: '梼杌',
						lqtz_taotie: '饕餮',
						lqtz_zhuyin: '逐阴',
						lqtz_qilin: '麒麟',
						lqtz_zhuquejiangling: '朱雀降灵',
						lqtz_xuanwujiangling: '玄武降灵',
						lqtz_zhuquefaxiang: '朱雀法相',
						lqtz_xuanwufaxiang: '玄武法相',
						lqtz_zhuquezhenshen: '朱雀真身',
						lqtz_xuanwuzhenshen: '玄武真身',
						lqtz_xiaohu: '小虎',
						lqtz_dahu: '大虎',
						lqtz_ling: '靈',
						lqtz_ren: '刃',
						lqtz_dun: '盾',
						lqtz_yi: '翼',
						lqtz_shenbing: '神兵',
						lqtz_maerxiusiren: '马尔修斯',
						lqtz_maerxiusidun: '马尔修斯',
						lqtz_maerxiusiyi: '马尔修斯',
						lqtz_maerxiusishen: '天威神甲',
						lqtz_qingnu: '青女',
						lqtz_hunu: '狐女',
						lqtz_jiuweihu: '九尾狐',
						lqtz_yinianheyi: '意念合一',
						lqtz_qiyilieying: '起翼猎鹰',
						lqtz_quzhubojue: '驱逐伯爵',
						lqtz_lieyanlieying: '烈焰猎鹰',
						lqtz_emodiao: '恶魔雕',
						lqtz_geminglieying: '革命猎鹰',
						lqtz_wulilinxiao: '武力林鸮',
						lqtz_weixingpaolieying: '卫星炮猎鹰',
						lqtz_huorenfenshaolieying: '火刃焚烧猎鹰',
						lqtz_jiujilieying: '究极猎鹰',
						lqtz_yibanglieying: '异邦猎鹰',
						lqtz_zuizhongbaoleilieying: '最终堡垒猎鹰',
						lqtz_wukulieying: '武库猎鹰',
						lqtz_zhihuilinxiao: '智慧林鸮',
						lqtq_c1: '<img src="extension/浪琴天阙/image/辅助图/lqtq_card1.png" width="70" height="70"> ',
						lqtq_c2: '<img src="extension/浪琴天阙/image/辅助图/lqtq_card2.png" width="70" height="70"> ',
						lqtq_c3: '<img src="extension/浪琴天阙/image/辅助图/lqtq_card3.png" width="70" height="70"> ',
						lqtq_c4: '<img src="extension/浪琴天阙/image/辅助图/lqtq_card4.png" width="70" height="70"> ',
						lqtq_c5: '<img src="extension/浪琴天阙/image/辅助图/lqtq_card5.png" width="70" height="70"> ',
						lqtq_c6: '<img src="extension/浪琴天阙/image/辅助图/lqtq_card6.png" width="70" height="70"> ',
						lqtq_c7: '<img src="extension/浪琴天阙/image/辅助图/lqtq_card7.png" width="70" height="70"> ',
						lqtq_c8: '<img src="extension/浪琴天阙/image/辅助图/lqtq_card8.png" width="70" height="70"> ',
						lqtq_c9: '<img src="extension/浪琴天阙/image/辅助图/lqtq_card9.png" width="70" height="70"> ',
						lqtq_longling: '龙灵',
						lqtq_longling_info: '①出牌阶段,你可以将一张♦️️️牌当做任意一张基本牌或本回合未以此法使用过的普通锦囊牌使用;②你可以将一张♥️️️牌当做一张特定时机下才能使用的牌使用;③当你需要使用牌响应其他牌时,你可以将一张♠️️️牌当做那张牌使用;④当你需要打出牌响应其他牌时,你可以将一张♣️️️牌当做那张牌打出.',
						lqtq_longling_zhu: '龙灵·注',
						lqtq_longling_zhu_info: '①:出牌阶段可以直接使用的牌都可以以此法使用.<br>②:特定时机包含角色濒死/趁火打劫/诱敌深入等不可直接使用的锦囊.<br>③:可响应杀/锦囊牌触发技能使用草船借箭/无懈可击/闪.<br>④:可响应万箭齐发/南蛮入侵等触发技能打出杀/闪.',
						lqtq_juejing: '绝境',
						lqtq_juejing_info: '锁定技,你的手牌上限+3;当你扣减体力前/扣减体力后,你从牌堆获得一张你手牌上缺少的花色牌,否则你摸一张牌;你的回合开始/结束时,你从牌堆获得你手牌上缺少的花色牌各一张,你未因此法而获得牌时,你摸一张牌.',
						lqtq_huanshang: '缓伤',
						lqtq_huanshang_info: '锁定技,你即将受到/失去大于1的伤害/体力时,你改为执行X次受到一点同来源的同属性伤害/失去一点体力,你以此法执行的伤害/失去体力始终不会大于1.(X为你即将受到/失去的伤害/体力数)',
						lqtq_yabo: '鸭脖',
						lqtq_yabo_info: '<font color=#007799>抓鸭脖:</font>锁定技,①任何时候,🐀会以标记的形式几率性从你以外的任意角色武将牌上探头持续4秒,期间你打开标记,点击标记中的〔🐀🐀🐀〕即可成功抓捕一只🐀.②任何时候,你可以打开你的🐀笼子页面,在系统随机列出的6个技能中选择一个技能获得,同时你会消耗两只🐀(技能按钮任何时候都可以点击,但只有在抓捕的🐀不少于两只时点击技能才会获得),获得技能后退出页面,🐀笼子中可被获得的技能会随之刷新.③〖鸭脖〗以外的技能会显示在你的🐀笼子中,点击这些技能的按钮会失去对应的技能,并获得一只🐀.',
						lqtq_yabo_zhu: '鸭脖·注',
						lqtq_yabo_zhu_info: '<b>(图1)</b><br><span><img src="extension/浪琴天阙/image/辅助图/lqtq_tu1.jpg" width="80" height="80"></span><br>从(图1)中我们看到了一只🐀,这只🐀正是〖鸭脖①〗所述几率探头的🐀,这就要考察个人眼力了,当看到🐀出现时要迅速去点开,因为你只有四秒的时间.可能有的宝们就会问了,🐀那么小,还是随机出现在任何人身上,有没有什么技巧捕捉到它？答案是有的,那就是技能语音,众所周知,技能触发时是伴随语音的,技能〖鸭脖〗的语音是:<br>①欢迎来到神话的世界.<br>②山与海之间,有无穷无尽的奇遇.<br>③这天地间的万物,钟灵毓秀,一奇一幻.<br>④古老的山海之卷,守护着神秘光芒.<br>当你听到这几句技能语音时就要打起精神了,在接下来的4秒内找出场上的🐀并且点开.<br><br><b>(图2)</b><br><span><img src="extension/浪琴天阙/image/辅助图/lqtq_tu2.jpg" width="169" height="80"></span><br>打开标记后可看到(图2)中的按钮〔🐀🐀🐀〕,点击他,你就能成功抓捕一只🐀.<br><br><b>(图3)</b><br><span><img src="extension/浪琴天阙/image/辅助图/lqtq_tu3.jpg" width="170" height="350"></span><br>从(图3)中可以看到系统随机给出的6个可获得的技能以及这些技能的描述,点击按键可获得对应技能,若你抓捕的🐀不足两只时,随便你怎么点击也不会有反应,每次点击获得技能后,可获得的6个技能会自动刷新,技能发生变化后标记里是没法直接刷新的,需要你退出标记后重新进入才会刷新.<br><br><b>(图4)</b><br><span><img src="extension/浪琴天阙/image/辅助图/lqtq_tu4.jpg" width="170" height="350"></span><br>〖鸭脖〗以外的技能会显示在笼子里(图4),这个时候你也可以点击这些技能失去对应的技能,获得一只🐀.<br><br><b>(图5)</b><br><span><img src="extension/浪琴天阙/image/辅助图/lqtq_tu5.jpg" width="170" height="70"></span><br>小技巧,当你看到两个可组合的技能,如(图5)中的〖鬼才〗〖洛神〗,你想同时获得时,可以屯到四只🐀,再同时点击获得这两个技能.<br><br><b>(图6)</b><br><span><img src="extension/浪琴天阙/image/辅助图/lqtq_tu6.jpg" width="170" height="70"></span><br>',
						lqtq_guhuo: '蛊惑',
						lqtq_guhuo_info: '锁定技,任何时候,你几率性触发此技能,从4个卖血技能中选择一个技能获得,并回复一点体力,若你未受伤则先增加一个体力上限,你选择一名角色对你造成一点伤害,在此次伤害结算后,你失去以此法获得的技能,你发动技能〖蛊惑〗的期间,不会被翻面.',
						lqtq_xiqiao: '戏乔',
						lqtq_xiqiao_info: '出牌阶段限一次,你可以失去一点体力上限,令一名角色摸两张牌,你可以重复此步骤.',
						lqtq_beixian: '悲娴',
						lqtq_beixian_info: '当你受到伤害后,你可以弃置最多两张牌,若你弃置了两张点数相同的牌,你回复X点体力,你令一名角色摸1+Y张牌或弃置1+Y张牌.(X为你受到的伤害;Y为X与你弃置的牌数之和)',
						lqtq_huiyi: '慧宜',
						lqtq_huiyi_info: '①每回合限一次,你可以将一至两张牌当做任意一张普通锦囊牌使用.②一名角色的出牌阶段结束时,你可以将一至两张牌当做一张本回合内该角色使用过的普通锦囊牌使用(没有目标的牌默认选一张牌进行模仿).③你因〖慧宜〗而使用了两张牌来模仿牌时,那张牌结算两次.④你使用本回合内因〖慧宜〗而使用过的同名牌获得的牌不计入手牌上限.',
						lqtq_meizhi: '妹汁',
						lqtq_meizhi_info: '出牌阶段限一次,你展开「妹子戏汁图」,并喝光所有的<妹汁>,你猜测图中谁的身上藏有妹汁,一次可猜测4+X个(X为你喝光的<妹汁>杯数),没有猜中/猜中1个/猜中2个/猜中3个以上/猜中4个,你摸一张牌/你从牌堆获得三张类型不同/四张花色不同/五张牌名不同的牌/你获得一杯<妹汁>,若如此做,你于的出牌阶段再有一次,可以将任意张牌交给一名其他角色.若你以此法交出了4张或更多的牌,你获得一杯<妹汁>.',
						lqtq_jingqiao: '精巧',
						lqtq_jingqiao_info: '①出牌阶段,你可以选择一张未镶合技能的装备牌,从系统中随机展示的4个技能中选择一个技能镶合在此装备上融合成新的装备牌.②你失去装备区中镶合了技能的进攻/防御马时,你摸一张牌/获得一点护甲;③当你处于濒死状态时,你可以重铸一张装备牌,并获得一杯<妹汁>,将体力回复至1点,若你重铸的装备是你镶合过技能的武器/防具,你对令你进入濒死状态的角色造成一点伤害/你将体力回复至体力上限.④你的手牌上限+X(X为你的装备区中镶合了技能的装备牌数).',
						lqtq_tianhui: '天慧',
						lqtq_tianhui_info: '你可以将一张牌当做任意一张牌使用或打出,若如此做,你弃置一张牌,并获得一枚<天慧>标记;当你于回合内一次性获得两张或更多的牌后,你本回合发动〖天慧〗时,不能再模仿你本回合已模仿过的牌.',
						lqtq_tianzhi: '天智',
						lqtq_tianzhi_info: '①出牌阶段结束时,你可以移去任意枚<天慧>标记,摸等量的牌,若你以此法移去的标记不少于3,则你从牌堆随机获得一张【无懈可击】或黑色基本牌.②当你即将进入濒死状态时,你可以移去任意枚<天慧>标记,随机从等量的濒死状态下才能发动的技能中选择一个技能获得直到你脱离濒死.③你的手牌上限改为X+1(X为<天慧>的数量).',
						lqtq_sanchen: '三陈',
						lqtq_sanchen_info: '出牌阶段限一次,你可以令一名角色摸一张牌,其弃置一张牌,若其本回合以此法弃置的牌类型均不相同,则其重复此流程(最多3次),否则其摸X张牌.(X为目标弃置的牌的类型数)',
						lqtq_miewu: '灭吴',
						lqtq_miewu_info: '每名角色回合限一次,你需要使用或打出一张基本牌或锦囊牌时,你可以发动一次〖三陈〗视为使用或打出了那张牌.',
						lqtq_yuanjiao: '援交',
						lqtq_yuanjiao_info: '锁定技:①此技能不会被失去或失效.②游戏开始时,你随机记录两张未加入游戏的武将牌和两张牌堆中有的非装备牌.③一名角色死亡后,你记录该角色的所有你未记录的非装备牌和该角色的武将牌.④一名角色使用你未记录的非装备牌后,你记录那张牌.⑤每回合限一次,其他角色发动技能后,你记录一张随机武将牌.',
						lqtq_weiniang: '伪娘',
						lqtq_weiniang_info: '①此技能不会被失去或失效.②每名角色的回合内,你可以将一张本回合未以此法使用过的类型牌当做一张你记录的牌使用或打出,若如此做,你摸一张牌;③游戏开始时/回合开始时/出牌阶段结束后/你即将受到伤害时,你可以选择一张你已记录的武将牌置顶,若如此做,你获得该武将牌的所有技能直到取消该武将牌的置顶,若你置顶的武将牌超过三张,你需要选择一张武将牌取消置顶.',
						lqtq_baihe: '百合',
						lqtq_baihe_info: '锁定技,①你始终跳过出牌阶段和弃牌阶段.②其他角色获得牌时,若你的手牌少于20张,你摸一张牌.③其他角色使用装备牌/非装备牌时,你可以使用一张装备牌/同名牌.',
						lqtq_shuangxiu: '双修',
						lqtq_shuangxiu_info: '每种收益每轮限一次,一名角色获得一项收益时,你可以令一名角色获得一项相同的收益.若如此做,你可以弃置一张手牌.',
						lqtq_lingrang: '灵禳',
						lqtq_lingrang_info: '当你装备一张装备牌后,若你未因这个牌名的装备牌而发动过此技能,你可以选择性别/势力/体力各一项,系统随机展示X张符合你各项选项的武将牌,你获得其中一张武将牌上的所有技能,(因此技能展示时而选择过的武将牌不会再展示),若你展示的武将牌不足X,则你摸Y张牌.(X为该装备牌的名称字数,Y为X与展示的武将牌数之差)',
						lqtq_huayue: '化月',
						lqtq_huayue_info: '使命技,锁定技,摸牌阶段,你改为获得每种类型的牌各一张.<br><b>成功:</b>准备阶段开始时,若你装备区内的牌的名称字数合计大于8,则你获得技能〖凌念〗.<br><b>失败:</b>当你进入濒死状态时,你回复一点体力,并可以转移场上一张牌.',
						lqtq_lingnian: '凌念',
						lqtq_lingnian_info: '出牌阶段限一次,你可以将一张♦️️️牌当做【借刀杀人】使用.',
						lqtq_pingnan: '平南',
						lqtq_pingnan_info: '①你视为拥有技能〖征南〗.②当一名角色使用转化的【杀】结算后/失去体力后/即将受到的伤害被防止或伤害值发生过减少的事件结算后,你可以令该角色本回合出杀上限+1/令该角色摸两张牌/弃置一张牌令该角色受到一点无来源的伤害.',
						lqtq_jingzhi2_backup: '精智',
						lqtq_jingzhi2: '精智',
						lqtq_jingzhi: '精智',
						lqtq_jingzhi_info: '①出牌阶段开始时,你投一次骰子,若结果不为1/为1,你将牌堆中点数为X的倍数/为A、7、J、K的牌各一张置于你的武将牌上作为精智牌(X为骰子的数值).②回合结束后,你可以进行一次〖精智推演〗.③出牌阶段各限一次,你可以获得一张精智牌或弃置一张精智牌,获得场上一张牌.',
						lqtq_jingzhi_tuiyan: '精智推演',
						lqtq_jingzhi_tuiyan_info: '弃置两张精智牌并查看一名其他角色的一张手牌,若该牌点数的倍数中有数值等于X,你获得该角色区域内的所有牌,等于X+1/-1,你摸两张牌,否则你获得该角色的展示牌.(X为弃置精智牌的点数合计).',
						lqtq_xihai: '袭害',
						lqtq_xihai_info: '出牌阶段限一次,你可以弃置一名其他角色的一张手牌,若选择的目标没有手牌则改为令目标摸一张牌,并展示摸到的牌.若如此做,你根据那张牌的点数:1~6/7~12/13,对目标造成1/2/3点伤害,你从牌堆或弃牌堆获得与那张牌类型各不同的牌各一张.',
						lqtq_zhangquan: '彰权',
						lqtq_zhangquan_info: '每回合限一次,一名其他角色发动技能或触发技能对其的询问时,你可以弃置一张牌取消之,直到回合结束,该角色每次发动技能,你摸一张牌.',
						lqtq_huiling: '汇灵',
						lqtq_huiling_info: '出牌阶段各限一次:①你可以将一至二张牌置于武将牌上做为<灵>,视为使用一张本回合内你使用过的基本牌或普通锦囊牌(无次数和距离限制)并获得一张随机装备牌.;②与一名角色拼点,并将其的拼点牌置于你的武将牌上作为<灵>,若你赢,你从牌堆随机获得两张类型相同且与该角色的拼点牌类型不同的牌,否则,你摸一张牌.',
						lqtq_chiwu: '炽舞',
						lqtq_chiwu_info: '❶你使用牌时,获得一枚<炽>标记(最多3枚),若那张牌与你使用的上一张牌类型不同,则你需先移除所有标记.❷你使用牌后,可以移去3枚<炽>标记,选择一项发动:①你摸三张牌,并可以用任意张牌交换等量的<灵>.②令一名其他角色弃置1～3张牌,弃置的牌数少于3,其受到一点伤害,少于2,你获得其弃置的牌,你进行一次判定:红色:本回合手牌上限+1.点数为X,你移去所有<灵>,执行Y次对一名角色造成一点火焰伤害.(X为所有<灵>的点数合计,Y为你移去的<灵>数)',
						lqtq_zongling: '纵灵',
						lqtq_zongling_info: '一名角色的判定牌或拼点牌生效前,你可以打出一张<灵>代替其中一张牌.',
						lqtq_weishang: '伪伤',
						lqtq_weishang_info: '每回合限一次,一名其他角色受到伤害后,你可以将受到伤害的角色视为你.',
						lqtq_weishang_zhu: '伪伤·注',
						lqtq_weishang_zhu_info: '其他角色受到伤害后,你发动此技能将受伤角色视为你,则该角色相当于掉了血却无事发生,即便该角色有卖血类技能也不会触发,而你没有受到伤害,却相当于承受了该角色所受到的伤害值,如果你有卖血技则可以触发这些卖血技能,若伤害来源有狂骨之类的技能也是以你为受伤角色计算.',
						lqtq_anhong: '暗红',
						lqtq_anhong_info: '当你受到一点伤害后,若无伤害来源或伤害来源没有牌,你可以摸3张牌,否则你弃置伤害来源一张牌或令伤害来源摸一张牌,并弃置一张牌,你从牌堆随机获得两张与其弃置的牌不同颜色的牌.',
						lqtq_yujue: '欲绝',
						lqtq_yujue_info: '锁定技,你对其他角色造成的伤害不能触发技能.',
						lqtq_tiantuo: '天拓',
						lqtq_tiantuo_info: '锁定技,①回合开始时,你从「天拓之书」中选择一个未以此法获得过的技能获得直到你造成伤害.②一名角色使用转化的实体牌后,你记录这些牌的类型/花色.③你使用或打出非转化的牌后/受到伤害后,你移去一个记录的类型/花色,从牌堆获得一张与之相同的牌.',
						lqtq_fuyin: '复音',
						lqtq_fuyin_info: '你可以将一张手牌当做那张牌使用或打出.',
						lqtq_mieyan: '滅焰',
						lqtq_mieyan_info: '出牌阶段限一次,你可以弃置一名其他角色一张手牌,对该角色造成X点伤害(X为弃置的牌的点数),若该角色未因此而死亡,你摸两张牌.',
						lqtq_quan: '驱黯',
						lqtq_quan_info: '一名其他角色发动技能或触发技能对其的询问或因技能而使用牌时,你可以取消之并摸一张牌.',
						lqtq_xingchen: '星辰',
						lqtq_xingchen_info: '出牌阶段限一次,你可以进行一次拼字游戏,从系统给出的文字中选择任意个文字拼成一张牌名,连续进行4次,若游戏卡库中有与你拼出的牌名相同的牌,则你令一名角色获得这些牌.',
						lqtq_xuelin: '血麟',
						lqtq_xuelin_info: '①当你受到伤害后,你可以摸一张牌.②当你成为你未记录的牌的目标时,你可以记录那张牌.③每回合限一次,当你获得你已记录的同名牌时,你可以移去此记录牌,将那张牌交给一名其他角色,你回复一点体力,并摸两张牌.',
						lqtq_xiutian: '修天',
						lqtq_xiutian_info: '一名角色回复/失去体力后或你于弃牌阶段弃置了类型与<帝>的类型均不同的牌时,你可以观看牌堆顶三张牌,将其中一张牌置于你的武将牌上称为<帝>,将剩下的牌按任意循序置于牌堆顶.回合内你的出【杀】次数上限+X.(X为你的<帝>数)',
						lqtq_dilin: '帝临',
						lqtq_dilin_info: '觉醒技,准备阶段开始时,若你武将牌上的<帝>数量不小于3,你减1点体力上限,并选择回复1点体力或摸两张牌.你获得技能〖帝摄〗.',
						lqtq_dishe: '帝摄',
						lqtq_dishe_backup: '帝摄',
						lqtq_dishe_info: '出牌阶段限一次,你可以移去一张<帝>,获得一名其他角色的所有手牌.',
						lqtq_xuansha: '玄刹',
						lqtq_xuansha_info: '锁定技,①游戏中有牌进入弃牌堆时,你立即将这些牌置于你的武将牌上称为<玄刹>.②当你的<玄刹>牌不少于牌堆数时,你获得其中5张牌,将所有<玄刹>牌置于牌堆底.③回合结束后,你需将一张<玄刹>牌置于牌堆底并对一名角色造成一点雷电伤害,你可以重复此步骤,但之后选择的牌点数需为Y(Y为你上一张选择的牌的点数+1).④你可以将4张花色各不同的<玄刹>牌置于牌堆底,视为使用一张【桃】.',
						lqtq_buju: '御风',
						lqtq_buju2: '御风',
						lqtq_buju_info: '出牌阶段限一次,你可以表演「御风飞行」.若得分不为0,你可以选择至多X名其他角色获得<御风>效果,摸X-Y张牌(准备阶段开始时,你进行判定.若结果为:红色,你跳过摸牌阶段;黑色,你跳过出牌阶段.X为你的得分.Y为你选择的角色数).',
						lqtq_tianshu: '天书',
						lqtq_tianshu_info: '出牌阶段限一次,你可以弃置一张不为【太平要术】的牌并选择一名角色.该角色获得并使用【太平要术】.一名角色因【太平要术】失去体力时,你可以令其回复一点体力.',
						lqtqj_shashen_info: '锁定技,①一名角色进入濒死状态时,若你未对其发动过〖杀神①〗,你获得一枚<杀神>标记.②你使用牌时,若你的<杀神>标记不少于1/2/3/4时,此牌无距离限制/无数量限制/不能被响应/你摸一张牌.',
						lqtq_jiaoyi: '交易',
						lqtq_jiaoyi_info: '使命技,①游戏开始时,你选择一名其他角色,系统展示武将名称最后一个字与该角色的武将名称最后一个字相同的所有武将牌,你选择其中一张武将牌切换或变为你的副将,你变为主将.该角色随机记录5张牌名,当其使用已记录的牌后,其移去记录的牌名,令你摸X张牌(X为那张牌的名称字数).<br><b>成功:</b>因〖交易〗而记录了牌名的角色移去了所有记录的牌名时/击杀一名角色时,你获得一枚<杀神>标记/该角色移去所有记录的牌名,你获得与那些牌名相同牌名的牌各一张.该角色增加一点体力上限,将体力回复至体力上限并获得技能〖英姿〗和〖马术〗.<br><b>失败:</b>因〖交易〗而记录了牌名的角色死亡时,其移去所有记录的牌名,你获得与那些牌名相同牌名的牌各一张.',
						lqtq_basu: '拔速',
						lqtq_basu_info: '锁定技,①摸牌阶段开始时,你随机对一名女性角色造成一点伤害并获得一枚<杀神>标记.②摸牌阶段,你的摸牌数+X(X为你的<杀神>标记数).',
						lqtq_wuyi: '武夷',
						lqtq_wuyi_info: '❶准备阶段,你可以将手牌中的一张装备牌置入一名角色的装备区内/将装备区中的一张牌当做【闪电】置入一名角色的判定区内/弃置你判定区一张牌并摸一张牌,若如此做,你视为使用一张【火攻】/【决斗】/【水淹七军】.❷每回合限一次,你使用的牌造成伤害时,你可以选择:①令伤害加X.②获得目标角色的一个技能直到你的回合结束.③你摸Y张牌并可以弃置目标角色X张牌.④背水!:你依次执行①②两项,失去一点体力.⑤破釜!:你依次执行②③两项,减少一点体力上限.⑥血战!:你依次执行①②③选项,你翻面.(X为你已损失的体力,且最小为1.Y为你的体力上限,且最大为4).',
						lqtq_yinpo: '银魄',
						lqtq_yinpo_info: '锁定技,①当你失去体力/减少体力上限/不因〖武夷〗而翻面时,你获得两点护甲/摸两张牌/执行一个额准备阶段.②回合开始时,你弃置所有因〖银魄〗而获得的牌并失去所有护甲,增加X点体力上限并回复Y点体力.③你的手牌上限+Z(X为你弃置的牌数;Y为你失去的护甲数;Z为你的护甲数与手牌中的因〖银魄〗而获得的牌数之和).',
						lqtq_dudan: '赌胆',
						lqtq_dudan_info: '出牌阶段,你可以猜测正反面并投一次硬币,若猜对,你摸X张牌,否则你弃置区域内所有牌(X为你区域内的牌数).',
						lqtq_nifan: '逆翻',
						lqtq_nifan_info: '<font color=#FF3333>逆风翻盘:</font>你成为其他角色牌的目标时,若你的手牌数小于该角色的手牌数,你可以投一次硬币,若结果为正面,你摸X张牌,否则,该角色失去X点体力值(X为该角色的手牌数).',
						lqtq_xiaoge: '枭歌',
						lqtq_xiaoge_info: '你连续使用或打出3张花色/类型均不同的牌时,你可以摸一张牌.若同时符合两个条件,则改为摸两张牌.',
						lqtq_yuanshen: '缘神',
						lqtq_yuanshen_info: '出牌阶段,你可以弃置最多3张牌,根据你弃置的牌数执行以下选项:一张,你获得你装备区一张本回合未以此法获得过的相同副类别的牌.两张:你获得一张与你弃置的牌类型与花色均不同的牌.三张,你摸一张牌,若此时你不能使用那张牌则你摸一张牌.',
						lqtq_yinpa: '淫趴',
						lqtq_yinpa_info: '①每回合对每个技能各限一次,其他角色发动技能时,该角色获得一枚对应该技能的标记,若该技能不存在标记,则你创建一个该技能的标记令该角色获得.②你成为本回合未成为过与该牌类型相同的牌的目标时,你获得一枚<淫趴>标记,若该角色所有技能的标记数合计大于你所有技能的标记数合计,你摸一张牌.',
						lqtq_yuzu: '玉足',
						lqtq_yuzu_info: '你需要使用或打出一张牌时,你可以移去场上合计X枚任意标记并展示其他角色合计两个技能的技能描述,你从中选择任意文字和符号组成最多两句话作为条件,系统展示4个描述中有这些条件的技能,你选择其中一个技能获得,若如此做,你视为使用或打出了那张牌(X为本回合〖玉足〗发动的次数).',
						lqtqx_quzuo: '驱佐',
						lqtqx_quzuo_info: '锁定技,①游戏开始时,你将8张【奇正相生】加入牌堆.②出牌阶段限一次,你可以与一名其他角色拼点,若你赢,你视为使用一张【奇正相生】,否则,其视为使用一张【奇正相生】.',
						lqtqx_jiece: '节策',
						lqtqx_jiece_info: '锁定技,当一名角色使用①【奇正相生】/②智囊/③〖定汉〗记录的牌时,若该牌满足以上一项,你摸一张牌.满足两项,你令一名角色将手牌摸至X,若该角色的手牌数大于X则改为令该角色摸1张牌(X为〖定汉〗记录的牌名数).',
						lqtqx_dinghan: '定汉',
						lqtqx_dinghan_info: '锁定技,①当你成为未记录过的锦囊牌的目标时,你记录此牌名并取消之.②你的回合内限一次,你可以将一张牌当做任意一张锦囊牌使用,若你已记录那张牌的牌名,则你需先移去该牌名,否则,此牌结算后,你记录此牌的牌名.',
						lqtqx_quanji: '权计',
						lqtqx_quanji_info: '当你受到1点伤害后/使用单体性牌造成1点伤害后/你的牌被其他角色获得后/你的出牌阶段结束且你的手牌数大于你的体力时,你可以摸两张牌,将一至两张牌置于武将牌上,称为<权>;你的手牌上限+X.(X为<权>的数量)',
						lqtqx_zili: '自立',
						lqtqx_zili_info: '觉醒技.准备阶段,若你的<权>数大于4,则你回复1点体力并摸两张牌,获得技能〖排异〗.',
						lqtqx_paiyi: '排异',
						lqtqx_paiyi_backup: '排异',
						lqtqx_paiyi_info: '出牌阶段每项各限一次,你可移去一张<权>并选择一项:①令一名角色摸X张牌,且最少摸两张.②令一名角色弃置X张牌,且最少弃置两张.③令至多X名角色交给你一张牌.④对至多X名角色各造成1点伤害.(X为<权>的数量)',
						lqtqx_tuntian: '屯田',
						lqtqx_tuntian_info: '①当你于回合外失去牌或于回合内失去【杀】后,你可以进行一次判定.若判定结果为♥️️️,你摸一张牌.②你的判定牌生效后,你可以将该牌置于武将牌上称为<田>.③你计算与其他角色的距离时-X.(X为<田>的数量)',
						lqtqx_zaoxian: '凿陷',
						lqtqx_zaoxian_info: '觉醒技,准备阶段,若你的<田>数大于2,则你回复一点体力,并摸两张牌,获得技能〖急袭〗.你于当前回合结束后进行一个额外的回合.',
						lqtqx_jixi: '急袭',
						lqtqx_jixi_info: '出牌阶段,你可以将一张<田>当作一张任意普通锦囊牌使用.',
						lqtqx_ziliang: '资粮',
						lqtqx_ziliang_info: '当一名角色受到伤害后,你可以交给其一张<田>,并令其摸一张牌,若其的手牌数大于你,其可以交给你一张牌,否则,你可以交给其一张牌.',
						lqtqx_pojun: '破军',
						lqtqx_pojun_info: '①出牌阶段开始时,你可以弃置一张锦囊牌,并选择一名目标角色或当你使用【杀】或【决斗】指定角色后,你可以将该角色的至多X张牌置于其武将牌上,其于回合结束时获得这些牌.(X为其的体力).②当你使用【杀】或【决斗】对一名角色造成伤害时,若该角色的手牌数和装备区内的牌数均不大于你,你令此伤害翻倍.',
						lqtqx_yicheng: '疑城',
						lqtqx_yicheng_info: '①你成为其他角色【杀】或【决斗】的目标时,你可以对该角色发动一次〖破军〗.②你使用的【杀】或【决斗】或其他角色对你使用的【杀】或【决斗】造成伤害后,你可以随机获得对方武将牌上的两张破军牌.',
						lqtqx_yingba: '英霸',
						lqtqx_yingba_info: '①出牌阶段限一次,你可令最多3名其他角色减少一点体力上限并获得一枚<平定>标记,若其的<平定>标记大于1,你摸一张牌.若其因此而死亡,你减少两点体力上限.②你对拥有<平定>标记的角色使用牌没有距离限制.',
						lqtqx_fuhai: '覆海',
						lqtqx_fuhai_info: '锁定技.①拥有<平定>标记的角色不能响应你使用的牌.②你使用【杀】/锦囊牌指定拥有<平定>标记的角色为目标时,此【杀】不计入次数限制/你摸一张牌.③拥有<平定>标记的角色死亡时,你增加X点体力上限并摸X张牌.(X为其拥有的<平定>标记数).',
						lqtqx_pinghe: '冯河',
						lqtqx_pinghe_info: '锁定技.①你的手牌上限等于你已损失的体力.②你回复体力时,若你的体力大于0,则改为增加一点体力上限.③你受到伤害时,需将一张手牌交给一名其他角色或弃置并失去一点体力上限(体力上限不大于1时不失去),若你没有手牌且你的体力上限大于X,则改为减少X点体力上限(X为你受到的伤害数),防止此伤害.若伤害有来源不为你,伤害来源获得一枚<平定>标记.',
						lqtqx_huishi: '慧识',
						lqtqx_huishi_info: '出牌阶段限一次或当你受到伤害后,你可进行一次判定,若判定结果与此次进行判定的花色均不相同,你重复此步骤并增加1点体力上限.你可以将所有生效的判定牌交给任意一名角色.',
						lqtqx_tianyi: '天翊',
						lqtqx_tianyi_info: '觉醒技,准备阶段,若场上的角色均已受到过伤害,你增加两点体力上限并回复1点体力,令一名角色获得技能〖佐幸〗.',
						lqtqx_shuishi: '辉逝',
						lqtqx_shuishi_info: '限定技,出牌阶段,你可选择一名角色.若其有未发动的觉醒技,则你令其这些技能无视发动条件,其摸四张牌.',
						lqtqx_zuoxing: '佐幸',
						lqtqx_zuoxing_info: '出牌阶段限一次,你可以视为使用一张任意基本牌或普通锦囊牌.',
						lqtqx_liegong: '烈弓',
						lqtqx_liegong_info: '你使用【杀】可以选择你距离不大于此【杀】点数的角色为目标;你使用或打出牌或成为牌的目标时,若你未记录则你记录那张牌的花色.回合内,其他角色不能使用或打出与你记录花色相同的牌.你使用牌时,若你记录的花色已有4种,则此牌不能被其他角色响应,且无视防具.你对其他角色造成伤害时,可以移去所有记录的花色,令伤害+X(最多加3),并展示牌堆顶X张牌,获得任意张花色与手牌中花色均不同的牌.(X为你移去的花色数)',
						lqtqx_jinfan: '锦帆',
						lqtqx_jinfan_info: '出牌阶段限一次,你可以观看一名其他角色的手牌,可以将你与其的手牌中任意张花色各不同的牌置于你的武将牌上称为<铃>.你可以将<铃>当做手牌般使用或打出,你失去<铃>时可以立即从牌堆获得一张与此<铃>相同花色的牌.',
						lqtqx_qixi: '奇袭',
						lqtqx_qixi_info: '①每回合限一次,你可以将一张<铃>或因〖劫营〗而获得的牌当做【过河拆桥】使用.②你以外的角色准备阶段开始时,你可以对其使用一张<铃>或因〖劫营〗而获得的牌.',
						lqtqx_jieying: '劫营',
						lqtqx_jieying_info: '结束阶段,若场上没有角色武将牌上有<营>,你可以将任意张花色不同的手牌置于一名其他角色的武将牌上称为<营>,有<营>的角色手牌上限+X,且可以将<营>当做手牌般使用或打出(X为<营>数).有<营>的角色回合结束后,你获得该角色的所有手牌,你使用这些牌时无距离限制且不计入次数,该角色获得所有<营>.',
						lqhc_hanguang: '寒光',
						lqhc_hanguang_info: '其他角色的回合限一次,当你成为其他角色牌的目标时,你可以摸一张牌,并弃置一张牌,根据你弃置的牌获得效果:基本牌,你获得一点护甲;锦囊牌,你令该牌对你无效;装备牌,你可以改为使用之或转移场上一张牌.',
						lqhc_chuqiao: '出鞘',
						lqhc_chuqiao_info: '出牌阶段开始时,若你的手牌少于5张则你可以将手牌摸至5张,否则,你可以弃置任意张牌并摸等量的牌.若你以此法弃置了牌,你可以弃置最多X名其他角色各一张牌.(X为你弃置的牌数)若如此做,你获得以下效果直到回合结束或有角色死亡:你使用【杀】/【桃】/单体性锦囊牌时,可以令那张牌无视防具,不能被响应,伤害/回复基数+1,额外指定一个目标,且结算两次.',
						lqhc_jiuxian: '九弦',
						lqhc_jiuxian_info: '锁定技,你手牌始终为0.游戏开始时,你记录九张牌堆中有的牌.你需要使用或打出牌时,你可以视为使用或打出一张你记录的牌.(每个牌名每回合各限一次).',
						lqhc_shiyuan: '峙援',
						lqhc_shiyuan_info: '出牌阶段限3次,你使用的牌结算后,你可以令一名其他角色从牌堆随机获得一张与此牌相同类型的牌,若该角色的手牌中有与你使用的牌相同类型的牌,则改为令该角色获得一枚<峙援>标记(不能大于2),拥有<峙援>标记的角色回合结束后,其移去所有<峙援>标记,摸等量的牌.',
						lqhc_zhaocai: '招财',
						lqhc_zhaocai_info: '出牌阶段限一次,你获得四张花色各不相同的牌;发动过此技能的回合结束时,若你本回合内使用和打出过的牌花色不足四种,你失去一点体力.',
						lqhc_qunchong: '群宠',
						lqhc_qunchong_info: '锁定技,每回合每种交互表情限一次,你对其他角色使用:鸡蛋:目标弃置一张牌;拖鞋:目标受到一点伤害;其他角色对你使用:鲜花:你摸一张牌;酒杯:你回复一点体力,若无受伤则你下次可多使用一次【招财】(仅可多用一次).',
						lqhc_lianyue: '憐月',
						lqhc_lianyue_info: '锁定技,回合开始时,你进行一次判定,若判定结果的牌点数为K,你获得一枚<盈>标记,否则,你获得一枚<玦>标记.<br>盈:当你获得牌/失去手牌后,若你的手牌数大于3/小于3,则你将手牌摸至3张/弃置至3张.<br>玦:当你获得牌/失去手牌后,若你的手牌数大于1/小于1,则你将手牌摸至1张/弃置至1张.当你进入濒死状态时,你回复一点体力.',
						lqhc_wenqing: '温卿',
						lqhc_wenqing_info: '每回合限一次,当一名角色翻面时/受到伤害时,你可以失去一点体力取消之.',
						lqhc_xidie: '牺蝶',
						lqhc_xidie_info: '①游戏开始时或拥有〖温卿〗的角色死亡后,你可以令一名男性角色获得技能〖温卿〗.②拥有〖温卿〗的角色死亡后,你获得3枚<牺蝶>标记.③你的判定牌生效时,判定牌的点数+X(X为标记数).',
						lqhc_gaishi: '盖世',
						lqhc_gaishi_info: '摸牌阶段,你可以多摸一张牌.当你成为一名角色的【杀】或【借刀杀人】的目标结算后,你可以视为对该角色使用一张火属性【杀】.(无距离和次数限制)',
						lqhc_jingji: '精骑',
						lqhc_jingji_info: '锁定技,你装备区里的武器牌和进攻马不能被获得或弃置.你造成伤害时,伤害+X(X为你装备区里的装备牌数,至少为1).',
						lqhc_yiwen: '疑问',
						lqhc_yiwen_info: '锁定技,你受到锦囊牌造成的伤害+1.',
						lqhc_yibing: '疑兵',
						lqhc_yibing_info: '你受到伤害后,可以选择一名其他角色,令该角色于其的回合开始时进入混乱状态,直到你的下回合开始时.',
						lqhc_shuizhan: '水战',
						lqhc_shuizhan_info: '出牌阶段限一次,你可以将任意张手牌当做一张【水淹七军】使用,若你以此法使用的【水淹七军】未造成伤害,你摸X张牌,否则你摸2X张牌(X为转化【水淹七军】时使用的牌数).',
						lqhc_kunlin: '困麟',
						lqhc_kunlin_info: '准备阶段,你可以失去一点体力上限,摸X张牌并可以对最多X名角色造成一点伤害(X为你的体力上限).',
						lqhc_wuzhi: '武志',
						lqhc_wuzhi_info: '觉醒技,你死亡时,取消之,增加9点体力上限并将体力回复至6,你从系统列出的9个蜀势力武将牌的技能中选择3个技能,将这些技能分配给包含你在内的最多两名角色.',
						lqhc_yixiang: '憶相',
						lqhc_yixiang_info: '回合结束后,你可以观看牌堆顶6张牌,获得其中一张牌并将剩余的牌按任意顺序至于牌堆顶.',
						lqhc_shelie: '涉猎',
						lqhc_shelie_info: '摸牌阶段,你可以改为从牌堆顶亮出5～9张牌,获得点数不同的牌各一张.',
						lqhc_gongxin: '攻心',
						lqhc_gongxin_info: '出牌阶段,你可以观看一名其他角色的手牌,可以将其中一张花色与本回合以此法选择过的牌的花色各不同的牌弃置或置于牌堆顶.',
						lqhc_xuepi: '学屁',
						lqhc_xuepi_info: '准备阶段,若你的手牌数少于你的体力上限,你减少一点体力上限并获得技能〖克己〗.',
						lqhc_keji: '克己',
						lqhc_keji_info: '出牌阶段结束后,若你本回合未因基本牌/锦囊牌而造成过伤害,你可以增加一点体力上限并回复一点体力/获得X点护甲(X为你的体力上限的一半向下取整).',
						lqhc_longwei: '龙威',
						lqhc_longwei_info: '锁定技,①你的手牌上限+1,②你的装备区没有武器牌时,你使用的【杀】无视防具.③你的体力上限或体力发生变化时,摸X张牌(X为你的体力与损失的体力最大的一方).',
						lqhc_longhun: '龙魂',
						lqhc_longhun_info: '你可以将同花色的一至两张牌按下列规则使用或打出并获得一枚<龙>标记:♥️️️当【桃】,♦️️️当火【杀】,♣️️️当【闪】,♠️️️当【无懈可击】.若你以此法使用了两张红色牌,则此牌回复值或伤害值+1.若你以此法使用了两张黑色牌,则你获得一张任意花色的牌.你拥有<龙>标记时,你与其他角色计算的距离-1.',
						lqhc_gaoda: '高达',
						lqhc_gaoda_info: '使命技,你的第二个出牌阶段结束后,你选择一名其他角色,当该角色成为你以外的角色使用的【杀】或普通锦囊牌的目标时,将目标改为你.<br><b>成功:</b>你因此技能而成为了7次【杀】的目标,你获得技能〖英锐〗.<br><b>失败:</b>你因此技能而选择的角色进入濒死状态时,该角色将体力回复至1.',
						lqhc_yingrui: '英锐',
						lqhc_yingrui_info: '回合结束后,你可以移去任意枚<龙>标记,进行X次判定(X为你移去的标记数-1,且至少为1),若判定结果为红色,你增加一点体力上限,否则,你失去一点体力上限(体力上限为1时改为失去一点体力),若如此做,你的体力或体力上限与你发动此技能前的数值不同,你回复一点体力并对一名其他角色造成一点火焰伤害.',
						lqhc_jiyuan: '积怨',
						lqhc_jiyuan_info: '锁定技,你其他角色造成伤害后,伤害目标获得一枚<怨>标记,拥有<怨>标记的角色不能响应你对其使用的牌,拥有<怨>标记的角色出牌阶段开始时,其移去所有<怨>标记并对你造成X点伤害(X为移去的标记数).',
						lqhc_shenxiao: '神啸',
						lqhc_shenxiao_info: '锁定技,你的【杀】无次数限制.你于出牌阶段使用【杀】后,本回合你的【杀】无距离限制且使用【杀】时摸一张牌,你的【杀】被闪避后,你获得一枚<神啸>标记,你的【杀】造成伤害时,移去所有<神啸>标记令伤害+X(X为移去的标记数).',
						lqhc_xingtian: '刑天',
						lqhc_xingtian_info: '觉醒技,你进入濒死状态时,减少1点体力上限,将体力回复至2,对所有拥有<怨>标记的角色造成一点伤害并移去这些角色的所有标记,你失去技能〖积怨〗,获得技能〖战怒〗.',
						lqhc_zhannu: '战怒',
						lqhc_zhannu_info: '锁定技,你的♠️️️️牌与【桃园结义】视为【决斗】,你对其他角色或其他角色对你使用的【决斗】不能被【无懈可击】且造成的伤害+1.',
						lqhc_liaodi: '料敌',
						lqhc_liaodi_info: '每回合限两次,你不因〖料敌〗获得/弃置牌后,你可以观看一名角色区域内随机两张牌,获得/弃置其中一张牌.',
						lqhc_zhisheng: '制胜',
						lqhc_zhisheng_info: '回合内,你使用牌指定你/其他角色为目标时,你可以摸一张牌/弃置一名其他角色一张牌.',
						lqhc_guicai: '鬼才',
						lqhc_guicai_info: '牌堆顶的牌对你可见,一名角色的判定牌生效前,你可以选择一项:①打出一张牌或将牌堆顶的牌代替之.②令此次判定不可被更改.③背水:依次执行以上选项,令一名角色对你造成一点伤害.',
						lqhc_langgu: '狼顾',
						lqhc_langgu_info: '你造成/受到一点伤害后,可以进行一次判定,红色:你获得受伤害角色/伤害来源一张牌.♠️️️:你对受伤害角色/伤害来源造成一点无伤害来源的雷电伤害.♣️️️:你摸一张牌并可以弃置受伤害角色/伤害来源一张牌.',
						lqhc_zhabing: '诈病',
						lqhc_zhabing_info: '限定技,结束阶段或你进入濒死状态时,你可以令场上所以角色交给你一张手牌,你的体力直到下次回合开始前不会低于1.',
						lqhc_polu: '破橹',
						lqhc_polu_info: '一名角色跳过判定/摸牌/出牌/弃牌结束时,你可以摸一张牌,若摸到的牌为黑色,你可以展示那张牌并对该角色造成一点伤害.',
						lqhc_juezan: '倔堑',
						lqhc_juezan_info: '①当你受到一点伤害后,你可以摸两张牌并可以将一张牌置于武将牌上称为<堑>(每种花色的<堑>最多一张).②一名角色的回合开始时,你可以弃置任意张<堑>,根据你弃置的花色跳过该角色的对应阶段:♠️️️️判定阶段.♣️️️️摸牌阶段.♦️️️️出牌阶段.♥️️️️弃牌阶段.',
						lqhc_choulue: '筹略',
						lqhc_choulue_info: '每轮限一次,一名角色的结束阶段,若其的手牌数大于你的手牌数,则你可以令其选择一项:①交给你一张牌,你可以将那张牌当做<堑>置于你的武将牌上.②令你摸一张牌,你可以将一张牌当做<堑>置于你的武将牌上.',
						lqhc_jueyuan: '绝缘',
						lqhc_jueyuan_info: '锁定技,①普通杀对你无效.②你可以将【闪】当做【杀】使用或打出.',
						lqhc_yaoli: '妖力',
						lqhc_yaoli_info: '你可以令你的普通锦囊牌结算两次.',
						lqhc_xinshang: '心伤',
						lqhc_xinshang_info: '限定技,出牌阶段,你可以翻面,令一名其他角色将武将牌切换为「东方月初」.',
						lqhc_yanleiH: '眼泪',
						lqhc_yanleiH_info: '出牌阶段限一次,你可以与「东方月初」交换位置并选择一名其他角色,你与「东方月初」获得该角色所有技能直到各自的回合结束,该角色失去所有技能,直到本回合结束.',
						lqhc_shenhuo: '神火',
						lqhc_shenhuo_info: '出牌阶段限一次,你可以炼化一张武器牌或令一名角色进入烧伤状态.炼化:为武器牌增加使用【杀】时不能被响应的效果;烧伤状态:出牌阶段开始时,你需弃置三张牌或失去一点体力,你解除烧伤状态.',
						lqhc_daofa: '道法',
						lqhc_daofa_info: '出牌阶段限一次,你可以令一名角色将手牌摸着体力上限或将体力回复至手牌数.',
						lqhc_jizhi: '机智',
						lqhc_jizhi_info: '回合内与回合外各限两次.你可以视为使用一张任意普通锦囊牌.',
						lqhc_yanleiC: '眼泪',
						lqhc_yanleiC_info: '①出牌阶段限一次,你可以与一名其他角色交换位置,该角色失去所有技能直到其的回合结束.②出牌阶段,你可以失去此技能并令一名角色将一张武将牌切换为「涂山红红」.',
						lqhc_tiaobo: '挑拨',
						lqhc_tiaobo_info: '出牌阶段限一次,你可以将一名角色区域内的一张牌置于你的武将牌上作为<财>,该角色视为对你选择的另一名角色使用一张【决斗】,当以此法使用的【决斗】造成伤害后,你获得受伤害角色的两张牌并将其中一张置于你的武将牌上作为<财>.',
						lqhc_liyou: '利诱',
						lqhc_liyou_info: '①一名角色即将受到伤害时,你可以交给其一张<财>,取消之,若伤害来源为你,你可以改为令伤害+X.②你的手牌上限+X(X为你的<财>数).③出牌阶段,你可以用任意张手牌替换等量的<财>.',
						lqhc_liuji: '六计',
						lqhc_liuji2: '六计',
						lqhc_liuji_info: '一局游戏限六次,一名角色的出牌阶段限一次,该角色可以将至少两张手牌置于你的武将牌上作为<财>,该角色选择一名任意角色,令你对其选择的角色造成一点伤害,若如此做,你弃置X张<财>并摸等量的牌(X为你<财>的数量的一半向下取整).',
						lqhc_jibing: '集兵',
						lqhc_jibing_info: '①摸牌阶段开始时,你可以改为将牌堆顶的3张牌置于你的武将牌上,称为<兵>.②你可以将一张<兵>当做任意基本牌使用或打出.③你获得或失去<兵>时,你摸一张牌.④你的攻击距离加2X(X为你的<兵>数).',
						lqhc_moucuan: '谋篡',
						lqhc_moucuan_info: '觉醒技,准备阶段,若你的<兵>数不小于势力数,则你获得技能〖兵祸〗和〖太平〗.',
						lqhc_binghuo: '兵祸',
						lqhc_binghuo_info: '一名角色的结束阶段开始时,若你本回合内因〖集兵〗而使用或打出过牌,则你可以对一名角色造成1点雷电伤害,若该角色因此而死亡,你将该角色的所有牌分配至你武将牌上的<兵>和<符>中.',
						lqhc_taiping: '太平',
						lqhc_taiping_info: '①你因〖集兵〗而使用或打出牌时,你可以将当前回合角色区域内的一张牌置于你的武将牌才作为<符>.②出牌阶段,你可以将一张<符>当做任意锦囊牌使用.③一名角色造成雷属性伤害时,你可以弃置一张<符>,令伤害+1.',
						lqhc_jinglve: '景略',
						lqhc_jinglve_info: '出牌阶段限两次,你可以观看一名其他角色的手牌,将其中一张牌标记为「死士」.当其使用对应的实体牌中包含「死士」的牌时,你取消此牌的所有目标并获得此牌.当「死士」牌不因使用而进入弃牌堆,或其回合结束后,若「死士」牌仍在其区域内,则你选择一名任意角色,令该角色获得这些牌并于下次摸牌阶段多摸一张牌.',
						lqhc_baiyi: '败移',
						lqhc_baiyi_info: '限定技,出牌阶段,若你已受伤,你可以重新编排场上的座位号.',
						lqhc_shanli: '擅立',
						lqhc_shanli_info: '使命技,准备阶段,若你已发动过【败移】,则你立一名角色为<君王>,<君王>由你来操控.系统展示每个势力的主公技各两个,你选择其中一个技能令<君王>获得,只要<君王>存活,其他角色不能响应你使用的牌.<br><b>成功:</b><君王>死亡后,你获得技能〖泰然〗并修改势力为<晋>.',
						lqhc_tairan: '泰然',
						lqhc_tairan_info: '锁定技,回合结束后,你回复X点体力并摸Y张牌,出牌阶段开始时,你失去Y-1点体力(X为你已损失的体力数且最大为5,Y为你的体力上限).',
						lqhc_tiaozhan: '挑战',
						lqhc_tiaozhan_info: '准备阶段,你可以视为使用任意一张带有「伤害」标签的牌.',
						lqhc_zhonger: '中二',
						lqhc_zhonger_info: '使命技,出牌阶段限一次,你可以当场哔哔一句中二台词,令所以角色选择一项:①失去两点体力;②令你摸一张牌并获得一点护甲;③还是失去两点体力;④还是令你摸一张牌并获得一点护甲.<br><b>成功:</b>准备阶段,若你的护甲数大于3,你减少一点体力上限,摸至8张手牌,并获得技能〖真眼〗,系统随机展示3张神势力的武将牌,你选择并获得其中一张该武将牌的所有技能.',
						lqhc_guibi: '规避',
						lqhc_guibi_info: '①你可以将一张手牌当做【闪】使用或打出.②你使用或打出【闪】后,若对应那张牌的实体牌中有点数为字母或与你装备区中有牌点数相同的牌时,你可以摸一张牌.③一名角色需要使用或打出【闪】时,你可以打出一张【闪】,视为该角色使用或打出了一张【闪】.',
						lqhc_liangjiang: '良将',
						lqhc_liangjiang_info: '锁定技,你的手牌均置于武将牌上,你可以将武将牌上的牌如同手牌般使用或打出.',
						lqhc_tiaoxin: '挑衅',
						lqhc_tiaoxin_info: '出牌阶段限一次,你可以令至多4名其他角色,这些角色需对你使用一张【杀】,否则你获得未对你使用【杀】的角色每个区域内的各一张牌,你以此法获得的牌不计入手牌上限且无距离限制.',
						lqhc_zhiji: '志继',
						lqhc_zhiji_info: '当你〖挑衅〗选择的目标合计不少于6名时,你获得技能〖观星〗和〖天任〗,并令一名其他角色获得一枚<北伐>标记,拥有<北伐>标记的角色只能对你使用牌.',
						lqhc_yishi: '弈式',
						lqhc_yishi_info: '转换技,阴:你可以视为使用一张【杀】.阳:你可以视为使用一张【闪】.',
						lqhc_qiuxian: '求先',
						lqhc_qiuxian_info: '<font color=#FF7744>番局·求先:</font>锁定技,①游戏开始时,你令所有未拥有技能〖弈式〗的角色获得〖弈式〗.②你成为牌的目标时,若场上拥有〖弈式〗的角色中转换技的状态为阴/阳的角色居多,且场上不为此牌的目标但可以/不可以成为此牌目标的角色不少于2时,此牌对你无效.',
						lqhc_tizi: '提子',
						lqhc_tizi_info: '<font color=#FF7744>提子一瞬:</font>①当你发动技能〖弈式〗后,你可以令所以拥有〖弈式〗的角色将转换技状态变为与你〖弈式〗的转换技状态一致.②你使用的实体/虚拟的【杀】不能被虚拟/实体的牌响应.',
						lqhc_shence_info: '出牌阶段限一次,你可以弃置一张牌并转移场上一张牌,你将手牌摸至X(X为你的体力上限).',
						lqhc_shence_zhu: '神策·改',
						lqhc_shence_zhu_info: '出牌阶段限一次或你受到伤害后,你可以弃置一张牌并转移场上一张牌,你将手牌摸至X(X为你的体力上限).',
						lqhc_liangzhi: '亮智',
						lqhc_liangzhi_info: '出牌阶段限X次,其他角色于其的回合内使用牌时,你可以弃置一张牌取消那张牌的所有目标(X为存活的角色数).',
						lqhc_renwang: '仁望',
						lqhc_renwang_info: '锁定技,其他角色回复体力时,若你本回合未发动过技能〖亮智〗,则你增加一个体力上限.',
						lqhc_nitian: '逆天',
						lqhc_nitian_info: '觉醒技,一名角色的回合结束时,若你的手牌数小于X(X为你已损失的体力),你修改技能〖神策〗.',
						lqhc_nanci: '难辞',
						lqhc_nanci_info: '出牌阶段限一次,你可以创建一张任意延时性锦囊牌置于自己的判定区,若此时你的判定区已有牌,你回复一点体力.',
						lqhc_jiexin: '解心',
						lqhc_jiexin_info: '你的【杀】造成伤害后,可以弃置其装备区与判定区所有牌,若你的判定区有牌,你可以弃置其最多X张手牌(X为该角色弃置的牌数).',
						lqhc_huaishang: '怀殇',
						lqhc_huaishang_info: '准备阶段,若你的判定区有牌,你可以回复一点体力并可以将判定区的牌转移给一名其他角色.',
						lqhc_gengxin: '更心',
						lqhc_gengxin_info: '出牌阶段限一次,若你的判定区有牌,你可以选择最多X名其他角色,创建与你判定区相同牌名的延迟性锦囊牌各一张置于这些角色的判定区(X为你判定区的延迟性锦囊牌数).',
						lqhc_chaoneng: '超能',
						lqhc_chaoneng_info: '锁定技,所有角色的判定区可置入判定区内已有的延迟性锦囊牌(限制对【乐不思蜀】、【兵粮寸断】、【草木皆兵】、【鬼幽结】、【闪电】、【浮雷】、【火山】以及【洪水】生效).',
						lqhc_yiyou: '异游',
						lqhc_yiyou_info: '锁定技,①游戏开始时,你获得<怪兽>、<魔法>以及<陷阱>标记各两枚.②当一名角色使用的装备/锦囊/基本牌指定你为唯一目标时,你获得一枚<怪兽>/<魔法>/<陷阱>标记.',
						lqhc_chouka: '抽卡',
						lqhc_chouka_info: '摸牌阶段,你可以少摸一张牌并展示牌堆顶X张牌(X为场上的角色数),根据这些牌中的装备/锦囊/基本牌各自的数量获得等量的<怪兽>/<魔法>/<陷阱>标记,将这些牌置于弃牌堆.',
						lqhc_anjue: '暗决',
						lqhc_anjue_info: '❶一轮游戏开始时,你可以移去两枚<怪兽>标记,随机展示一张「浪琴游漫」武将栏中的一张武将牌,视为拥有该武将牌上的所有技能.❷出牌阶段限X次,你可以移去两枚<魔法>标记,视为使用一张任意牌(X为你的体力).❸结束阶段,你可以移去两枚<陷阱>标记,获得以下一个效果直到你的下个回合开始:①便乘.②无限泡影.③废铁稻草人.<br><b>便乘:</b>每个回合对每名角色其他角色各限一次,当这些角色获得牌时,你摸一张牌.<br><b>无限泡影:</b>每轮限一次,一名角色的回合开始时,你可以令其所有技能失效直到回合结束.<br><b>废铁稻草人:</b>每回合限一次,你即将受到【杀】造成的伤害时,可以取消之.',
						lqhc_longdan: '龙胆',
						lqhc_longdan_backup: '龙胆',
						lqhc_longdan_info: '①你可以将一张【杀】当做【闪】,【闪】当做【杀】使用或打出.②当你需要使用或打出一张基本牌时,若你未记录那张牌,你可以视为使用或打出了那张牌,你记录那张牌.③出牌阶段限一次,你可以失去一点体力移去一张〖龙胆②〗记录的牌.',
						lqhc_qinggang: '青钢',
						lqhc_qinggang_info: '你使用【杀】或【决斗】指定目标时或成为【杀】或【决斗】的目标时,你可以令此牌的使用者选择一项:①令你获得对方一张牌.②令你摸一张牌且此牌无视防具.③失去一点体力令此牌伤害基数+1.',
						lqhc_lingqiao: '凌峭',
						lqhc_lingqiao_info: '锁定技,❶你的【酒】无数量限制.❷你使用【酒】时,取消之并选择一项:①你视为使用一张【决斗】.②你失去一点体力令一名角色回复两点体力.',
						lqhc_shuiqun: '水群',
						lqhc_shuiqun_info: '锁定技,一名角色对你使用牌后,你摸一张牌并将一张手牌置于武将牌上称为<水>,你可以使用一张牌(无距离限制),你以此法使用牌时或场上有角色处于濒死状态时,你可以将<水>如同手牌般使用.',
						lqhc_qianshui: '潜水',
						lqhc_qianshui_info: '一名角色的出牌阶段结束时或脱离濒死状态时,若你于此回合发动过〖水群〗,你可以弃置所有张<水>并摸等量的牌,选择你或当前回合角色进入隐匿状态.',
						lqhc_maopao: '冒泡',
						lqhc_maopao_info: '隐匿技,你登场时,可以将一张手牌置于武将牌上作为<水>.',
						lqym_heiyan: '黑炎',
						lqym_heiyan_info: '限定技,出牌阶段,你可以弃置两张颜色不同的牌,并选择一名目标角色,令该角色失去一点体力或受到一点无来源或来源为你的普通/火焰/雷电/寒冰/神圣伤害,你创建并获得一张延迟锦囊【附链真红牙】.',
						lqym_fulianzhenhongya: '附链真红牙',
						lqym_fulianzhenhongya_info: '出牌阶段,对一名其他角色使用.若判定结果为♥️️️️/♠️️️️,则其受到一点火焰伤害,场上的「真红眼黑龙」创建并获得一张延迟锦囊【附链真红牙】.',
						lqym_honghun: '红魂',
						lqym_honghun_info: '出牌阶段限一次,你可以弃置一张牌,执行以下X项(X为1,且该牌每满足以下一项,X+1:❶延迟性锦囊牌.❷点数为A的牌.❸名称字数大于4的牌):①令一名角色摸两张牌.②令一名角色回复一点体力.③对一名其他角色造成一点火焰伤害.④获得一名其他角色一张牌.',
						lqym_zhenhong: '真红',
						lqym_zhenhong_info: '融合技,游戏开始时,若你的副将是「青眼白龙」或「黑魔导」,则你可以与之融合,诞生出全新的未知武将.',
						lqym_modao: '魔导',
						lqym_modao_info: '使命技:出牌阶段限一次,你可以弃置一张牌,根那张牌的花色创建并获得一张与该牌相同花色和点数的「魔导牌」,记录此牌的花色.<br><b>成功:</b>准备阶段,若你记录的花色大于2,你创建并获得一张你未记录花色对应的「魔导牌」,并获得技能〖魔阵〗.<br><b>失败:</b>你因〖魔导〗而获得你已记录了相同花色的牌时,你不失去〖魔导〗改为修改〖魔导〗.<br>♠️️️️:【千把刀】<br>♥️️️️:【魔术咒文书】<br>♣️️️️:【死亡魔术箱】<br>♦️️️️:【魔术礼帽】',
						lqym_modao1: '魔导·改',
						lqym_modao1_info: '出牌阶段限一次,你可以弃置一张牌,根那张牌的花色创建并获得一张与该牌相同花色和点数的「魔导牌」.<br>♠️️️️:【千把刀】<br>♥️️️️:【魔术咒文书】<br>♣️️️️:【死亡魔术箱】<br>♦️️️️:【魔术礼帽】',
						lqym_mozhen: '魔阵',
						lqym_mozhen_info: '①一名角色装备装备牌时,你获得一枚<魔力>标记.②出牌阶段限一次,你可以移去两枚<魔力>标记,从「魔导牌」中选择一种牌获得.③一名角色的摸牌阶段开始时,你可以移去一枚<魔力>标记,令该角色多摸一张牌.',
						lqym_qianbadao: '千把刀',
						lqym_qianbadao_info: '对一名其他角色造成一点伤害,并随机弃置该角色一张牌.',
						lqym_moshuzhouwenshu: '魔术咒文书',
						lqym_moshuzhouwenshu_info: '令一名角色回复一点体力,并摸一张牌,该角色本回合手牌上限+1.',
						lqym_siwangmoshuxiang: '死亡魔术箱',
						lqym_siwangmoshuxiang_info: '你即将受到伤害时,可以选择一名其他角色,除非该角色交给你一张点数大于此牌且与此牌相同花色的牌,否则你将伤害转移给该角色.',
						lqym_moshulimao: '魔术礼帽',
						lqym_moshulimao_info: '你成为基本牌或普通锦囊牌的唯一目标时,你可以令该牌对你无效,你从系统随机展示的3张普通锦囊牌中选择一张牌令该牌的使用者对你使用你选择的牌,你获得你选择的牌.',
						lqym_mieyan: '滅焰',
						lqym_mieyan_info: '转换技,阴:出牌阶段限一次,对一名其他角色造成一点火焰伤害,该角色随机弃置一张牌.阳:出牌阶段限一次,弃置一名其他角色一张手牌,若那张牌的名称字数大于2,你对该角色造成一点雷电伤害,否则,你摸两张牌..',
						lqym_quan: '驱黯',
						lqym_quan_info: '每回合限一次,你成为一名其他角色使用的牌的目标时,你可以弃置一张牌,令该牌对你无效,若你弃置的牌与该牌颜色相同,你回复一点体力,否则你获得该角色使用的牌,若那张牌无对应的实体牌则你改为摸两张牌.',
						lqym_qiangren: '强韧',
						lqym_qiangren_info: '游戏开始时/出牌阶段开始时,你增加X点/1点体力上限(X为场上的敌方角色数).',
						lqym_wudi: '无敌',
						lqym_wudi_info: '出牌阶段限一次,你可以弃置一张【杀】,对一名其他角色造成一点雷电伤害.',
						lqym_zuiqiang: '最强',
						lqym_zuiqiang_info: '①你对一名角色造成伤害后,你可以失去最多3点体力上限令该角色获得X枚<裂风>标记(X为你失去的体力上限的两倍).②拥有<裂风>标记的就是受到伤害时需移去一枚<裂风>标记令伤害+1.③回合开始时,清空场上所有角色的<裂风>标记.',
						lqym_zhanmo: '斩魔',
						lqym_zhanmo_info: '锁定技,场上所有角色合计受到5点伤害/使用7张牌/获得9张牌后,你对一名其他角色造成一点伤害/弃置一名其他角色一张牌/摸两张牌.',
						lqym_jingti: '晶体',
						lqym_jingti_info: '锁定技,一局游戏中各限一次,当手牌中有「钢化」牌的角色成为牌的目标时,①若那张牌的类型与其的「钢化」牌类型相同/②花色与其的「钢化」牌花色相同/③点数与其的「钢化」牌点数相同,你获得一枚<晶体>标记.准备阶段开始时,若你的<晶体>标记数不少于1,你摸一张牌;不少于2,你跳过本回合的判定阶段;不少于3,你增加一点体力上限,回复一点体力,获得一点护甲并移去所有<晶体>标记,重置此技能发动过的选项.',
						lqym_ganghua: '钢化',
						lqym_ganghua_info: '出牌阶段限一次,若场上没有角色有「钢化」牌,则你可以选择一名其他角色,将该角色的一张手牌标记为「钢化」牌.拥有「钢化」牌的角色失去所有技能,不能使用或打出与「钢化」牌相同类型/花色/点数的牌,不能被翻面,不能被横置,且拥有「钢化」牌的角色回合结束后,你弃置其的「钢化」牌,获得其最多两张牌或摸两张牌.',
						lqym_cundang: '存档',
						lqym_cundang_info: '锁定技,①游戏开始时,你创建X张技能锦囊牌加入牌堆中(X为牌堆数除于20向上取整,且最小为9).②其他角色使用的技能锦囊牌即将进入弃牌堆时,你获得那张牌.',
						lqym_bianma: '编码',
						lqym_bianma_info: '出牌阶段限一次,你可以创建一张技能锦囊牌并获得.',
						lqym_bajian: '霸键',
						lqym_bajian_info: '游戏开始时/你失去牌后,若手牌中没有<霸键>牌,你可以摸一张牌并标记为<霸键>牌.',
						lqym_tuijia: '蜕甲',
						lqym_tuijia_info: '出牌阶段限一次,你可以弃置一至两张牌并令一名角色选择一项:①摸X张牌(X为你弃置的牌数).②回复一点体力,若你只弃置了一张牌则改为获得一点护甲.',
						lqym_xieyin: '谐音',
						lqym_xieyin_info: '锁定技,①每轮游戏开始时或你受到伤害后,你记录一个未记录的势力.②与你已记录势力相同的角色使用非基本牌指定自己或你为目标时,你移去该势力,选择摸两张或对该角色造成一点伤害.',
						lqym_weishi: '未视',
						lqym_weishi_info: '锁定技,其他角色的出牌阶段开始时,你观看该角色的手牌,猜测该角色于出牌阶段结束时的手牌数,并分别猜测在此期间可能会受到伤害/回复体力/获得牌/弃置牌的角色各一名(不能是该角色,且对其他角色不可见).若如此做,该角色的出牌阶段结束时,若X大于0,你摸X张牌(X为你猜对的数量),你可以将1～3张牌分配给其他角色.',
						lqym_jiaozhu: '教主',
						lqym_jiaozhu_info: '每三轮限一次,其他角色的准备阶段开始时,你可以令该角色本回合使用的牌全部无效.',
						lqym_shuanghuang: '双皇',
						lqym_shuanghuang_info: '转换技,阴:其他角色使用普通锦囊牌时,你可以取消那张牌的所有目标,若那张牌的点数不为数字,你获得那张牌.阳:你使用【杀】指定目标时,你可以获得目标装备区一张牌并使用之.',
						lqym_ganranx: '感染',
						lqym_ganranx_info: '①一轮游戏开始时,你可以观看牌堆底两张牌,获得其中一张牌.②出牌阶段限一次,你可以弃置一张手牌并获得一名其他角色一张手牌.③当你于弃牌阶段弃置牌后,你可以从弃牌堆获得一张最早进入弃牌堆的牌.④你因〖感染①②③〗而获得牌时,若你未记录则记录那张牌的牌名,否则,你可以摸一张牌并移去该牌名.',
						lqym_huanjue: '幻觉',
						lqym_huanjue_info: '锁定技,①你每回合首次受到【杀】造成的伤害时,伤害减一.②攻击范围内有你的角色不能对没有技能〖幻觉〗的角色使用【杀】.③你成为【杀】的目标时,使用者随机失去一个技能(有<幻觉>标记时不失去)并获得一枚<幻觉>标记.⑤拥有<幻觉>标记的角色出牌阶段开始时,若其的<幻觉>标记大于1,则其失去一点体力,其失去所有<幻觉>标记并回复失去的技能.',
						lqym_xunxin: '寻衅',
						lqym_xunxin_info: '出牌阶段限一次,你令一名其他角色选择一项(若该角色有<幻觉>标记则改为由你来选择):①直到该角色的回合结束时,其与你视为在彼此的攻击范围内,并视为对你使用一张【杀】.②交给你一张牌.③你获得其一张手牌.',
						lqym_gongcan: '共蝉',
						lqym_gongcan_info: '一名角色翻面/横置/重置后或跳过摸牌/出牌阶段时,该角色可以令你获得一张你手牌中缺少或较少的类型牌.',
						lqym_chantui: '蝉蜕',
						lqym_chantui_info: '每回合限一次,你可以将一张手牌当做【无懈可击】使用,若如此做,你可以横置一名角色.',
						lqym_ganran: '感染',
						lqym_ganran_info: '①游戏开始时,你选择一个势力(魏/蜀/吴/群/晋),场上所有角色的势力变成你选择的势力.②回合开始时,你选择一名角色,若其的势力不为魏/蜀/吴/群/晋,则其的势力变成与你的势力相同,若其的势力是:<br>魏:其摸两张牌.<br>蜀:其失去一点体力.<br>吴:其获得一点护甲.<br>群:你获得其每个区域内各一张牌.<br>晋:其增加一个体力上限.',
						lqym_yinzhuang: '隐状',
						lqym_yinzhuang_info: '使命技,你受到伤害后,获得一枚<隐>标记.<br><b>成功:</b>你即将阵亡时,若你有<隐>标记,则你退化为「碟状幼体」加入游戏.',
						lqym_jinran: '浸染',
						lqym_jinran_info: '使命技,出牌阶段限一次,你可以移去一枚<隐>标记,并对一名与你势力相同的角色造成一点伤害.<br><b>成功:</b>回合开始时,若你没有<隐>标记,则你进化为「隐形水母怪」,你选择一个势力(魏/蜀/吴/群/晋),场上所有角色的势力变成你选择的势力.',
						lqym_qianhe: '嵌合',
						lqym_qianhe_info: '①出牌阶段,你可以移去一名角色任意技能的一个标记,从弃牌堆获得一张牌.②你使用的【杀】或【决斗】结算后,你获得一枚<嵌合>标记.',
						lqym_longtong: '胧朣',
						lqym_longtong_info: '锁定技,你的【杀】无距离限制,且你对回合内未成为过你【杀】目标的角色使用的【杀】无数量限制.',
						lqym_yanfen: '焱焚',
						lqym_yanfen_info: '①出牌阶段限一次,你可以将一名其他角色的一张牌置于你的武将牌上称为<焚>,并摸一张牌.②你受到伤害时,可以移去一枚<焚>令伤害变成火属性.③回合结束时,你可以移去2-3张<焚>,对一名角色造成X点火焰伤害(移去的<焚>为2时,X为该角色的体力的一半向下取整;3时,X为该角色的体力减1,且X最小为1).',
						lqym_yanhua: '焱化',
						lqym_yanhua_info: '锁定技,你即将受到火焰伤害时,取消之,你摸X张牌(X为伤害数).',
						lqym_xingyun: '幸运',
						lqym_xingyun_info: '出牌阶段限一次,你可以掷两次骰子(若觉得掷骰子过程烧条局内可跳过),你弃置武将牌上所有<幸运>牌,并将牌堆顶X张牌置于你的武将牌上作为<幸运>牌.你可以将这些牌当做手牌般使用或打出(X为最大的骰子数).若两次骰子的结果合计为7,你可以选择一项:①你回复一点体力.②令一名其他角色获得一张<智囊>.③弃置任意名角色装备区所有牌.',
						lqym_jizu: '基组',
						lqym_jizu_info: '出牌阶段限一次,你可以获得一名其他角色的所有技能并令该角色失去所有技能直到回合结束.',
						lqym_yaoxing: '妖星',
						lqym_yaoxing_info: '①你因牌而造成伤害后,若武将牌上没有<星>,你可以将牌堆顶两张牌置于你的武将牌上称为<星>.②你受到伤害后,可以将伤害来源的一张牌置于你的武将牌上称为<星>.',
						lqym_jianmie: '歼灭',
						lqym_jianmie_backup: '歼灭',
						lqym_jianmie_info: '出牌阶段限一次,你可以弃置任意张<星>对等量的角色造成一点伤害.',
						lqym_tiangai: '天盖',
						lqym_tiangai_info: '每回合限一次,你可以将一张<星>当做【闪】或【无懈可击】使用或打出.',
						lqym_baihui: '白辉',
						lqym_baihui_info: '出牌阶段限一次,你可以弃置一张牌并摸一张牌.',
						lqym_tianqi: '天启',
						lqym_tianqi_info: '蓄力技(2/4).①出牌阶段限一次,你可以与至多X名角色进行谋弈(X为你的蓄力值),你消耗等同于你选择的目标数的蓄力值.②你因〖天启〗以外的技能获得牌时,你获得一点蓄力值.<br>破坏之轮:你对其造成一点伤害.<br>核聚之轮:你获得其一张牌并摸一张牌.',
						lqtq_pohuaizhilun: '破坏之轮',
						lqtq_fangyuzhilun: '防御之轮',
						lqtq_hejuzhilun: '核聚之轮',
						lqtq_guangfangzhilun: '光防之轮',
						lqym_kanpo: '看破',
						lqym_kanpo_info: '出牌阶段限一次,你可以选择一名任意角色A,观看一名不为你的角色B的手牌(选择角色A后若角色A不为你,你可以不再选择角色B,则角色B视为角色A),系统随机展示角色B的一张手牌,若此牌可以对角色A使用,则角色B对角色A使用此牌,否则角色B弃置此牌.',
						lqym_guiying: '归影',
						lqym_guiying_info: '蓄力技(1/3).①一名角色使用牌后,你可以消耗一点蓄力值令该角色获得那张牌.①你受到伤害后或一名角色的回合结束后,你获得一点蓄力值.',
						lqym_gaiyin: '归隐',
						lqym_gaiyin_info: '锁定技,你即将受到伤害时,若伤害大于1,则伤害减一,且变为无属性,你摸一张牌.',
						lqym_xianren: '显刃',
						lqym_xianren_info: '其他角色的准备阶段,若该角色的手牌数大于你的手牌数或体力大你的体力,你可以视为对其使用一张【决斗】.',
						lqym_yabo: '亚伯',
						lqym_yabo_info: '你造成伤害时,可以弃置两张牌令伤害+1.',
						lqym_shuzui: '赎罪',
						lqym_shuzui_info: '其他角色的结束阶段,若该角色的手牌数大于2或体力大于2,你可以获得该角色最多两张牌,你受到X点伤害(X为你获得的牌数).',
						lqym_shuangan: '双黯',
						lqym_shuangan_info: '锁定技,任何时候,若你的武将牌有〖该隐/亚伯恶魔〗,你可以打开武将牌上的<黯>标记,点击标记里的<切换武将>,将你的武将牌切换为〖亚伯/该隐恶魔〗.',
						lqtm_qiangfei: '强吠',
						lqtm_qiangfei_info: '锁定技,当你受到其角色锦囊牌造成的伤害时,取消之,伤害来源选择一项:①受到一点伤害.②令你获得其一张牌.',
						lqym_yushou: '欲首',
						lqym_yushou_info: '你获得其他角色的牌时或不因牌而造成伤害后,你可以摸一张牌.',
						lqtm_lianhuan: '连环',
						lqtm_lianhuan_info: '出牌阶段限一次,你弃置一名角色一张牌,若那张牌的名称字数不少于2/3/4,你直到下次你的回合开始前获得技能〖无敌〗/〖三陈〗/〖暗红〗.',
						lqym_sese: '涩涩',
						lqym_sese_info: '①游戏开始时,你获得「色色」「瑟瑟」「涩涩」标记各一枚.②一轮游戏开始时,你可以移去一枚「色色」/「瑟瑟」/「涩涩」标记,当三种标记均被移去时,你获得每种标记各一个,直到下一轮游戏开始前,当有基本牌/锦囊牌/装备牌于你的回合外被置入弃牌堆时,若你的手牌中没有那些牌的同名牌,你可以获得那些牌.',
						lqym_shie: '食恶',
						lqym_shie_info: '锁定技,①游戏开始时,你获得两枚<恶>标记.②回合结束时,若你本回合造成过伤害,你移去一枚<恶>标记,选择获得一点护甲或将手牌摸至5张.③你造成伤害时,伤害+X(X为2减<恶>标记数).',
						lqym_juetong: '绝同',
						lqym_juetong_info: '出牌阶段开始时,你可以弃置一张牌并令一名其他角色弃置所有与那张牌相同类型的牌,若该角色弃置的牌中有与那张牌相同牌名的牌,则你摸两张牌或对该角色造成一点雷电伤害,否则,你获得一张该角色弃置的牌.',
						lqym_jihui: '急徽',
						lqym_jihui_info: '当你进入濒死状态时,你可以获得一名其他角色的一张牌.',
						lqym_bingying: '冰映',
						lqym_bingying_info: '蓄力技(0/2).①每回合限一次,其他角色使用黑色【杀】或锦囊牌指定目标时,你可以消耗一点蓄力值取消那张牌的所有目标,你可以选择一名角色成为此牌的唯一目标,且你成为此牌的使用者.②一轮游戏开始时,你获得两点蓄力值.',
						lqym_huiang: '辉昂',
						lqym_huiang_info: '出牌阶段限一次,你可以令任意名角色各摸一张牌,只选择了一名角色时改为摸两张,并获得一枚<辉>标记直到其的回合结束,拥有<辉>标记的角色于出牌阶段使用【杀】的次数+X(X为标记数).',
						lqym_bingjie: '冰结',
						lqym_bingjie_info: '出牌阶段限一次,你可以将手牌摸至六张并弃置一张牌,你可以令一名其他角色本回合不能使用或打出转化的牌以及点数小于X的牌(X为你弃置的牌的点数).',
						lqym_wugen: '无艮',
						lqym_wugen_info: '锁定技,①你失去点体力后,获得等量的枚<艮>标记.②摸牌阶段,你多摸X张牌(X为你的标记数).',
						lqym_luanjian: '乱健',
						lqym_luanjian_info: '其他角色出牌阶段开始时,你可以弃置一张手牌并失去一点体力,将你与该角色中未横置的角色横置,其获得技能〖魂火〗,失去〖魂火〗以外的所有技能,不能使用黑色基本牌且锦囊牌只能指定你或其为目标直到回合结束.',
						lqym_hunhuo: '魂火',
						lqym_hunhuo_info: '每回合限一次,你可以将一张红色牌当做【灵魂之火】使用.',
						lqym_lingji: '灵骑',
						lqym_lingji_info: '锁定技,你于回合内首次使用的【杀】无视防具且不能被响应.',
						lqym_lingyuan: '灵渊',
						lqym_lingyuan_info: '每回合各限一次,①你造成伤害后,可以对另一名其他角色造成一点伤害.②你受到伤害后,可以获得不为伤害来源的一名其他角色一张牌.',
						lqym_mingyu: '冥域',
						lqym_mingyu_info: '锁定技,其他角色于其的回合内首次使用的锦囊牌无效.',
						lqcy_jianxiong: '奸雄',
						lqcy_jianxiong_info: '你受到一点伤害后或每回合限一次,其他角色受到伤害后,你可以获得造成伤害的牌并摸两张牌,没有可获得的牌时改为获得一张与那张牌相同牌名的牌.',
						lqcy_cidong: '刺董',
						lqcy_cidong_info: '出牌阶段限一次,你可以投一次硬币,若为正面,你使用一张【七宝刀】并可以视为使用一张无距离和数量限制且不能被闪避的【杀】,否则你需令一名其他角色视为对你使用【杀】、【决斗】、【南蛮入侵】或【万箭齐发】中的随机一张牌.',
						lqcy_hujia: '护驾',
						lqcy_hujia_info: '主公技,每轮限一次,你受到伤害后,可以对一名其他魏势力角色造成一点同伤害来源的同属性伤害,你回复一点体力.',
						lqcy_tiandu: '天妒',
						lqcy_tiandu_info: '一名角色的判定牌生效后,你可以获得之.',
						lqcy_yiji: '遗计',
						lqcy_yiji_info: '你受到一点伤害后或每回合限一次,其他角色受到伤害后,你可以摸X张牌,并令Y名角色各摸一张牌(X为你已损失的体力,且最小为2,Y为你摸到的牌中的类型数),你可以将最多两张手牌分配给其他角色.',
						lqcy_yingcai: '英才',
						lqcy_yingcai_info: '出牌阶段限一次,你可以投一次硬币,若为正面,你获得一个会进行判定的技能和一个改判的技能直到下次你的回合开始,否则,若你增加一个体力上限,若你的体力上限大于5则改为你减少一个体力上限.',
						lqcy_xunxun: '恂恂',
						lqcy_xunxun_info: '回合开始时,你可以观看牌堆底的六张牌,获得其中的两张牌,并将其余的牌以任意顺序置于牌堆顶.',
						lqcy_wangxi: '忘隙',
						lqcy_wangxi_info: '当一名角色受到一点伤害后,你可以摸三张牌,分别交给该角色和伤害来源各一张牌.',
						lqcy_shengquan: '胜全',
						lqcy_shengquan_info: '出牌阶段限一次,你可以投一次硬币,若为正面,你直到下次你的回合开始前获得技能〖伪伤〗,否则你视为受到一点伤害.',
						lqcy_guose: '国色',
						lqcy_guose_info: '①出牌阶段限一次,你可以弃置任意张牌并选择等量的其他角色,获得这些角色区域内各一张牌.②你不因使用而失去红色牌时,你可以将那些牌当做【乐不思蜀】使用.',
						lqcy_liuli: '流离',
						lqcy_liuli_info: '你成为其他角色牌的目标时,可以弃置一张牌将此牌转移给其他角色.',
						lqcy_yanxiao: '言笑',
						lqcy_yanxiao_info: '出牌阶段限一次,你可以投一次硬币,若为正面,你获得以下效果直到下个你的回合开始:其他角色跳过出牌阶段时,你可以获得该角色最多X张牌.若为反面,你令一名角色摸Y张牌(X为该角色的手牌数减手牌上限,Y为场上的延迟锦囊牌数).',
						lqtz_shanhaizx: '<span><img src="extension/浪琴天阙/image/辅助图/lqtq_gjdm.png" width="50" height="50"></span>',
						lqtz_shanhaizx_info: '此挑战自制作到完成后面临无人问津的局面,后来偶得这位好心群友<公爵的猫>在此扩展中测出了不少关于AI无法正常操作的bug,可以说天阙栏的武将从制作到后期完善,这位<公爵的猫>居功至伟,所以婊在此特此向<公爵的猫>致谢,好人一生平安,一胎十八.',
						lqtz_shanhai: '&nbsp;食用法',
						lqtz_shanhai_info: '游戏开始时自行调整为「双人挑战」,使用与手刹对应的「模式挑战」.本挑战共分6个模式,的可在<浪琴天阙>扩展页进行模式切换,如图:<br><span><img src="extension/浪琴天阙/image/辅助图/lqtq_moshiqiehuan.jpg" width="135" height="215"></span>',
						lqtz_zhuxie: '&nbsp;食用法',
						lqtz_zhuxie_info: '游戏由BOSS先手,BOSS方的起始手牌上调,BOSS阵亡后清空游戏轮数,切换新的BOSS出场,并立即开始BOSS的回合.<br><br>第一关:<br>挑战孟婆.<br><br>第二关:<br>挑战黑白无常、牛头马面、日夜游神随机一个和黄蜂、豹尾、鸟嘴、鱼鳃随机一个.<br><br>第三关:<br>挑战鬼王.<br><br>第四关:<br>挑战阎罗王.',
						lqtz_shenjia1: '&nbsp;食用法',
						lqtz_shenjia1_info: 'BOSS设计:<br>想去远方<br><br>BOSS与随从的起始手牌均为10,游戏由挑战方先手,当BOSS阵亡切换新的BOSS后,新BOSS会继承上一关BOSS的<甲>标记,且清空游戏轮数,立即开始BOSS的回合.<br><br>第一关:<br>挑战马尔修斯•攻击形态,随从双刃.<br><br>第二关:<br>挑战马尔修斯•速度形态,随从双翼.<br><br>第三关:<br>挑战马尔修斯•防御形态,随从双盾.<br><br>第四关:<br>挑战马尔修斯•合体形态,随从神兵.',
						lqtz_zijin: '&nbsp;食用法',
						lqtz_zijin_info: '挑战曹操、司马懿、董卓、吕布、袁术、张角中的随机两个,BOSS方的起始手牌均为10,游戏由BOSS方先手,击败所有敌人后获得胜利.',
						lqtz_aogang: '&nbsp;食用法',
						lqtz_aogang_info: '游戏由BOSS先手,BOSS的起始手牌为10,BOSS阵亡后清空游戏轮数,切换新的BOSS出场,并立即开始BOSS的回合.<br><br>第一关:<br>挑战奇达拉,随从奇甲斯、镜子士兵.<br><br>第二关:<br>挑战西若诺斯,随从西若诺斯左手、西若诺斯右手.<br><br>第三关:<br>挑战达姿,随从蛇神藝.<br><br>第四关:<br>挑战奥利哈刚之神.',
						lqtz_tianque: '&nbsp;食用法',
						lqtz_tianque_info: '本挑战的BOSS方出自于<浪琴天阙>扩展.本挑战共分两个模式,可在<浪琴天阙>扩展页进行模式切换,如图<span><img src="extension/浪琴天阙/image/辅助图/lqtq_moshiqiehuan2.jpg" width="135" height="230"></span>',
						lqtz_RR: '&nbsp;食用法',
						lqtz_RR_info: '游戏由BOSS先手,BOSS与随从的起始手牌为8,挑战方的起始手牌为5,BOSS阵亡后清空游戏轮数,切换新的BOSS出场,并立即开始BOSS的回合.<br><br>第一关:<br>挑战起翼猎鹰,随从两只驱逐伯爵.<br><br>第二关:<br>挑战烈焰猎鹰,随从两只恶魔雕.<br><br>第三关:<br>挑战革命猎鹰,随从两只武力林鸮.<br><br>第四关:<br>挑战卫星炮猎鹰,随从两只火刃焚烧猎鹰.<br><br>第五关:<br>挑战究极猎鹰,随从两只异邦猎鹰.<br><br>第六关:<br>挑战最终堡垒猎鹰,随从武库猎鹰、智慧林鸮.',
						lqtz_xiemei: '邪魅',
						lqtz_xiemei_info: '锁定技,你计算与其他角色的距离时-1.',
						lqtz_juhuo: '惧火',
						lqtz_juhuo_info: '锁定技,①你造成的火焰伤害+1.②你受到火焰伤害伤害+2,且你摸两张牌.',
						lqtz_zhennu: '震怒',
						lqtz_zhennu_info: '锁定技,准备阶段,你对所有敌方角色造成一点伤害,此法造成的伤害有几率变为火属性.',
						lqtz_hanyong: '悍勇',
						lqtz_hanyong_info: '你使用【南蛮入侵】、【万箭齐发】或【杀】时,你可以令此牌的伤害基数+1.',
						lqtz_aotang: '熬汤',
						lqtz_aotang_info: '锁定技,你的回合开始时,令随机一名其他角色遗忘所有技能,直到你的下回合开始.',
						lqtz_yunju: '云飓',
						lqtz_yunju_info: '锁定技,敌方角色的回合结束时,其随机弃置一张手牌,若没有弃置的牌,则其失去一点体力.',
						lqtz_guimei: '鬼魅',
						lqtz_guimei_info: '锁定技,①你的手牌上限+4.②你不会被翻面.③你的出牌阶段被跳过时,你使用一张牌,并可重复此步骤.④你的摸牌阶段被跳过时,你摸三张牌.',
						lqtz_guimei2: '鬼魅',
						lqtz_guimei2_info: '锁定技,你的手牌上限+4.',
						lqtz_xixing: '吸星',
						lqtz_xixing_info: '锁定技,准备阶段,你对所有敌方角色造成一点雷电伤害,且每造成一点,你回复一点体力.',
						lqtz_taiping: '太平',
						lqtz_taiping_info: '锁定技,当你受到1点伤害后,伤害来源需要弃置两张不同花色的手牌,否则其失去一点体力.',
						lqtz_mizui: '迷醉',
						lqtz_mizui_info: '你因牌而造成伤害后,可以弃置目标两张牌.',
						lqtz_qiangzheng: '强征',
						lqtz_qiangzheng_info: '锁定技,结束阶段,你获得所有敌方角色各一张手牌.',
						lqtz_xiaoshou: '枭首',
						lqtz_xiaoshou_info: '准备阶段,你可以对一名其他角色造成2～3点随机伤害.',
						lqtz_manji: '蛮击',
						lqtz_manji_info: '当你使用【杀】或【决斗】指定一名角色为目标后,你可以弃置其一张牌,若以此法弃置的牌为【杀】,则此【杀】造成的伤害+1,否则你获得那张弃置的牌.',
						lqtz_shiyu: '施狱',
						lqtz_shiyu_info: '锁定技,准备阶段,你从牌堆中获得4张花色各不同的牌.',
						lqtz_guizhao: '诡招',
						lqtz_guizhao_info: '锁定技,当你于同一回合中使用或打出一张本回合未使用或打出过的类型牌时,你摸一张牌.',
						lqtz_duane: '断恶',
						lqtz_duane_info: '锁定技,当敌方角色于其弃牌阶段弃置了牌,则其失去一点体力.',
						lqtz_zhoucha: '昼刹',
						lqtz_zhoucha_info: '锁定技,准备阶段,你进行一次判定并获得该判定牌,若结果为红色,你摸两张牌,本回合出杀上限+2,否则你对一名其他角色造成一点火焰伤害.',
						lqtz_yezhong: '夜冢',
						lqtz_yezhong_info: '锁定技,结束阶段,你进行一次判定并获得该判定牌,若结果为黑色,你获得所有敌方角色各一张手牌,否则你对一名其他角色造成一点雷电伤害.',
						lqtz_huiyun: '晦运',
						lqtz_huiyun_info: '出牌阶段限一次,你可以展示一名敌方角色的手牌,并弃置其中至多两张牌.你可以弃置一张与该角色弃置的牌中有类型相同的牌,并对其造成2点伤害.',
						lqtz_yinsha: '隐煞',
						lqtz_yinsha_info: '锁定技,敌方角色的出牌阶段开始时,若其手牌数大于其体力上限,你本回合不能成为【杀】或【决斗】的目标.',
						lqtz_eli: '恶力',
						lqtz_eli_info: '锁定技,你对敌方角色造成伤害时,你进行一次判定:若结果为红色,此伤害+1且你获得技能〖狂骨〗直到回合结束,否则你摸一张牌并获得技能〖完杀〗直到回合结束.',
						lqtz_bingyi: '病疑',
						lqtz_bingyi_info: '锁定技,每回合限一次,当你失去手牌时,若你的手牌不大于2,则你摸6张牌.',
						lqtz_suoxue: '索穴',
						lqtz_suoxue_info: '你使用【杀】或【决斗】指定目标后,若其手牌数大于你,你可将手牌摸至与该角色相同,否则,你可弃置一张牌令其不能响应此牌.',
						lqtz_duzhen: '毒针',
						lqtz_duzhen_info: '锁定技,你使用牌指定敌方角色时,该角色随机弃置一张牌(优先弃置装备区内的牌).',
						lqtz_mingchong: '冥虫',
						lqtz_mingchong_info: '锁定技,你死亡时,若有其他友方角色存活,那些角色获得技能〖毒针〗.',
						lqtz_guixi: '鬼吸',
						lqtz_guixi_info: '锁定技,当你受到伤害后,你进行一次判定:若结果为♥️️️,你回复一点体力,否则,你摸一张牌.',
						lqtz_anchao: '暗潮',
						lqtz_anchao_info: '锁定技,①友方角色回合结束时,若该角色本回合内未造成过伤害,其获得一枚<暗潮>标记,否则,其移去所有<暗潮>标记.②友方角色的摸牌阶段摸牌时/造成伤害时,摸牌数/伤害+X.(X为该角色拥有的<暗潮>标记数量)',
						lqtz_tiemian: '铁面',
						lqtz_tiemian_info: '锁定技,红色【杀】对你无效.',
						lqtz_jizhou: '疾咒',
						lqtz_jizhou_info: '锁定技,一名敌方角色的出牌阶段结束时,你进行一次判定,该角色需弃置任意张点数之和大于X牌,否则该角色失去1点体力,若该角色弃置了牌,你获得Y枚<噬>标记(X为判定牌的点数,Y为该角色弃置的牌数).',
						lqtz_danshi: '啖噬',
						lqtz_danshi_info: '锁定技,你造成伤害时,伤害+X,你失去一枚<噬>标记(X为你拥有的<噬>标记数量).',
						lqtz_chihu: '赤虎',
						lqtz_chihu_info: '锁定技,①摸牌阶段,你多摸两张牌.②你造成的伤害时,伤害+1.',
						lqtz_difu: '地府',
						lqtz_difu_info: '锁定技,一名敌方角色的出牌阶段开始时,若其手牌数大于其体力上限,则其将手牌弃置至体力上限.',
						lqtz_zhennub: '震怒',
						lqtz_zhennub_info: '锁定技,当你的体力首次降至8或更低时,你将体力回复至8并摸4张牌,终止一切结算,立即开始你的回合.',
						lqtz_xingpan: '刑判',
						lqtz_xingpan_info: '锁定技,出牌阶段开始时你进行一次判定,若结果为红色,你随机获得一名手牌最多的敌方角色所有手牌,否则,随机一名体力最多的敌方角色失去所有体力.',
						lqtz_dianwei: '殿威',
						lqtz_dianwei_info: '锁定技,准备阶段,你视为对所有装备区内没装备牌的敌方角色使用一张【杀】,且装备区内有牌的角色随机弃置一张装备牌.',
						lqtz_xuanpan: '宣判',
						lqtz_xuanpan_info: '锁定技,一名敌方角色回合结束时,若本回合该角色对你造成了4点以上的伤害/该角色摸牌数达到8张以上/该角色回复了3点以上的体力/你弃置了4张以上的牌,你随机对该角色造成1~4点伤害/你随机摸1~4张牌/你随机回复1~4点体力/该角色随机弃置1~4张牌.',
						lqtz_qushou: '驱兽',
						lqtz_qushou_info: '锁定技,回合开始时,你视为对所有敌方角色使用一张【南蛮入侵】.',
						lqtz_mojian: '魔箭',
						lqtz_mojian_info: '锁定技,回合开始时,你视为对所有敌方角色使用一张【万箭齐发】.',
						lqtz_beiming: '悲鸣',
						lqtz_beiming_info: '锁定技,当你死亡时,你令击杀你的角色弃置所有手牌.',
						lqtz_yingying: '嘤嘤',
						_lqtz_yingying: '嘤嘤',
						lqtz_yingying_info: '当一名角色使用非装备牌后,你可以将此牌置于牌堆底,若当前回合不为你的回合,则你需要先弃置一张牌.',
						lqtz_abaaba: '阿巴',
						_lqtz_abaaba: '阿巴',
						lqtz_abaaba_info: '锁定技,你的回合结束时,你展示牌堆底的一张牌并使用之.若如此做,你重复此流程,直到你以此法展示的牌无法使用为止,你获得那张牌.',
						lqtz_miaowu: '喵呜',
						_lqtz_miaowu: '喵呜',
						lqtz_miaowu_info: '锁定技,当你死亡时,你选择一种类型(基本/锦囊/装备/其他),你翻开牌堆底的一张牌确认并获得之,若你选择的类型与该牌类型相同,则你取消死亡,将体力上限变成2,体力回复至2,并摸一张牌.',
						lqtz_chiliu: '哧溜',
						_lqtz_chiliu: '哧溜',
						lqtz_chiliu_info: '锁定技,你进入濒死状态时,立即死亡,你不会被翻面,与其他角色计算的距离-1,手牌上限+2;你的回合开始时,你受到一点雷属性伤害.并获得判定区里的所有牌.',
						lqtz_guihuo: '鬼火',
						lqtz_guihuo_info: '结束阶段,你可以对一名其他角色造成1点火焰伤害.',
						lqtz_modao: '魔道',
						lqtz_modao_info: '锁定技,准备阶段,你摸两张牌.',
						lqtz_qida: '奇达',
						lqtz_qida_info: '锁定技,①你受到伤害前,你摸X张牌,将伤害转移给其他友方角色.②当己方角色受到伤害时,你获得X枚<奇达>标记.(X为受的的伤害数值)',
						lqtz_qijia: '奇甲',
						lqtz_qijia_info: '锁定技,①一名角色回合结束时,若你已经死亡,则你复活,并摸4张牌,增加一点体力上限.②你的摸牌阶段改为摸X张牌.(X为你的体力上限)',
						lqtz_jingying: '镜映',
						lqtz_jingying_info: '锁定技,每回合限一次,当你受到伤害后,你回复一点体力,并摸一张牌,若伤害来源为敌方角色,你随机获得其一个技能直到你的回合结束.',
						lqtz_zuidun: '最盾',
						lqtz_zuidun_info: '<b><font color=#ff0000>西若诺斯的最强之盾:</font></b>锁定技,①没有技能〖最盾〗的友方角色不能成为敌方牌的目标.②你受的大于1的伤害时,伤害减至1,若伤害来源是敌方角色,则该角色受到超出的伤害.',
						lqtz_zuiren: '最刃',
						lqtz_zuiren_info: '<b><font color=#ff0000>西若诺斯的最强之刃:</font></b>锁定技,①你受到伤害后,你发现一张普通锦囊牌,并可以使用那张牌,否则你获得之.②你对敌方角色造成伤害时,你进行一次判定,若判定结果不为基本牌,你令伤害+2.',
						lqtz_shenou: '神偶',
						lqtz_shenou_info: '锁定技,①你作为BOSS出场时,你的体力变成<奇达拉>吸收的<奇达>标记数,若无标记则变成无限.②当你的双臂造成伤害后,你受到等量的伤害.',
						lqtz_baoxing: '暴行',
						lqtz_baoxing_info: '锁定技,当你受的伤害后,你摸X张牌,(X为受到的伤害数),伤害每有1点,你进行一次判定:♠️️️,你对一名敌方角色造成一点雷电伤害;♥️️️,你摸一张牌并回复一点体力;♣️️️,你令所有敌方角色进入铁索连环状态,若已被铁索连环的角色改为随机弃置一张牌;♦️️️,你获得一名角色区域内的各一张牌.',
						lqtz_shenhuang: '神荒',
						lqtz_shenhuang_info: '锁定技,你使用【杀】指定目标时,翻开牌堆顶10张牌,弃置其中一张【杀】,令你使用的【杀】伤害基数变成那张牌的点数,否则你令此【杀】无效且你有技能〖庞躯〗时,你获得1枚<暗黑>标记.',
						lqtz_pangqu: '庞躯',
						lqtz_pangqu_info: '锁定技,你的回合结束后,若你于回合内没有造成过伤害,你获得1枚<暗黑>标记,你的<暗黑>标记达到4枚时,你立即死亡.',
						lqtz_anxi: '暗袭',
						lqtz_anxi_info: '锁定技,当你体力减少后,你摸X张牌,并且可以使用这些牌(X为此次减少的值).',
						lqtz_xiequ: '邪躯',
						lqtz_xiequ_info: '锁定技,①此技能不会被失去或失效.②你死亡时,需弃置两张牌,取消死亡,体力小于2时回复至2,若当前回合角色不为你,你令当前回合直接结束,且当前回合结束后,你立即开始你的回合.',
						lqtz_shenzong: '神踪',
						lqtz_shenzong_info: '出牌阶段,你可以弃置一张以下牌名的手牌,根据弃置的牌名发动效果:<br>【决斗】:你对一名角色造成一点雷电伤害.<br>【火攻】:你对一名角色造成一点火焰伤害.<br>【无中生有】:你摸4张牌.<br>【顺手牵羊】:你获得1~2名其他角色合计2张牌.<br>【过河拆桥】:你弃置所以敌方角色各一张牌.<br>【借刀杀人】:你获得一名其他角色装备区里的一张装备牌.<br>【桃园结义】:你回复2点体力,回复超出的值改为获得等量的护甲.<br>【五谷丰登】:你翻开牌堆顶4张牌,并获得其中两张牌.<br>【南蛮入侵】:你视为使用一张【惊雷闪】.<br>【万箭齐发】:你视为使用一张【炽羽袭】.<br>【铁索连环】:你令所以敌方角色横置,若敌方角色均已横置,则你改为摸两张牌,若你已横置,则你解除横置.',
						lqtz_shenyi: '神意',
						lqtz_shenyi_info: '锁定技,敌方角色使用或打出【无懈可击】或【闪】时,你有97%的几率对该角色造成一点寒冰伤害.',
						lqtz_shenpo: '神魄',
						lqtz_shenpo_info: '锁定技,你跳过你的判定阶段改为摸两张牌;你翻面时,取消之,摸两张牌.',
						lqtz_shenyan: '神焉',
						lqtz_shenyan_info: '锁定技,其他角色使用牌后,你有79%的几率获得一张同名牌,你可以使用一张牌.',
						lqtz_zuijiu: '醉酒',
						lqtz_zuijiu_info: '锁定技,你使用的【杀】造成的伤害+1.',
						lqtz_guanshi: '官势',
						lqtz_guanshi_info: '锁定技,当你成为延迟性锦囊牌的目标时/当你翻面时,你进行一次判定,若结果为黑色,你取消之.若结果为♥️️️,你可以将该牌转移给其他角色/你可以令一名其他角色翻面.',
						lqtz_huoluan: '祸乱',
						lqtz_huoluan_info: '锁定技,出牌阶段开始时,你从牌堆随机获得的一张黑色的普通锦囊牌,并使用那张牌,否则你弃置那张牌.你重复此步骤直到发动了4次.',
						lqtz_xumou: '蓄谋',
						lqtz_xumou_info: '锁定技,当你受到伤害后,你下次对敌方角色造成的伤害基数+1.(最多加4)',
						lqtz_jiquan: '集权',
						lqtz_jiquan_info: '锁定技,回合开始时,你将手牌摸至8张.',
						lqtz_luanzheng: '乱政',
						lqtz_luanzheng_info: '锁定技,当一名敌方角色于其的回合内获得牌时/对你造成伤害时,若该角色此时的手牌数大于10张/若该角色本回合对你造成的伤害值合计大于5,你令该角色本回合不能使用或打出牌.',
						lqtz_tieji: '铁骑',
						lqtz_tieji2: '铁骑',
						lqtz_tieji_info: '当你使用【杀】指定目标后,你可以进行判定并令该角色所有技能失效直到回合结束.若结果为红色,目标弃置一张红色牌,且此【杀】不可被闪避.若结果为黑色,目标需弃置两张黑色牌,否则此【杀】不可被闪避.',
						lqtz_xiongshi: '雄师',
						lqtz_xiongshi_info: '<font color=#FF3333>西凉雄师:</font>出牌阶段,若你手牌上没有【杀】,你可以将所有手牌(最少一张)当做一张无距离与数量限制,且伤害基数+1的【杀】使用.',
						lqtz_mashu: '马术',
						lqtz_mashu_info: '<font color=#FF3333>马氏断杀术:</font>锁定技,你计算与其他角色的距离时-1.你摸牌时有较大的几率断【杀】.你的回合结束后,若你手牌上没有【杀】,则你摸X张牌.(X为你本回合造成的伤害数)',
						lqtz_qianji: '千骑',
						lqtz_qianji_info: '<font color=#FF3333>一骑当千:</font>出牌阶段,你可以将一名已被〖铁骑〗的角色的〖铁骑〗印转移到一名本回合内未被〖铁骑〗过的角色上,视为对该角色使用一张【杀】.',
						lqtz_lingba: '霸凌',
						lqtz_lingba_info: '锁定技,你的回合开始时,若你手牌数为全场最多,则对一名随机敌人造成2点伤害.若你手牌数大于等于你体力的两倍,则改为对所有敌人造成伤害.',
						lqtz_yishen: '疑神',
						lqtz_yishen_info: '当你回复体力时,可以改为获得所有敌人各一张随机装备.',
						lqtz_langgu: '狼顾',
						lqtz_langgu_info: '锁定技,每回合限一次,当你获得其他角色的牌时,进行一次判定:若结果为黑色,随机弃置其1张手牌,且视为此技能本回合未发动过.',
						lqtz_yuanlu: '远虑',
						lqtz_yuanlu_info: '当你使用锦囊牌对敌方角色造成伤害时,你可以防止该伤害,改为摸一张牌且该敌方角色对你造成1点伤害.',
						lqtz_shenji: '神戟',
						lqtz_shenji_info: '判定阶段,你可以弃置两张手牌,弃置你判定区里的牌;摸牌阶段,你多摸两张牌;出牌阶段,你可以多使用两张【杀】,你的【杀】可以多指定两名角色为目标.',
						lqtz_zhankai: '战铠',
						lqtz_zhankai_info: '锁定技,每回合限一次,当你受到大于2点的伤害时,将此伤害减至2点,摸两张牌.',
						lqtz_baonue: '暴虐',
						lqtz_baonue_info: '锁定技,回合开始时,你摸X张牌并对至多X名角色造成1点伤害,你失去1点体力.(X为你已损失体力且最大为5)',
						lqtz_qubu: '驭布',
						lqtz_qubu_info: '锁定技,当友方角色使用【杀】指定目标时,你进行一次判定:若结果为黑色,你对此【杀】的所有目标造成1点伤害.',
						lqtz_jianzheng: '谏征',
						lqtz_jianzheng_info: '当一名其他角色使用【杀】指定目标时,若你在其攻击范围内且你不是目标,则你可以将一张手牌置于牌堆顶,取消所有目标,你成为目标.',
						lqtz_yinlei: '引雷',
						lqtz_yinlei_info: '锁定技,当你失去牌时,你随机横置一名未横置的敌方角色.',
						lqtz_wangzun: '妄尊',
						lqtz_wangzun_info: '锁定技,其他敌方角色的结束阶段,若其本回合:1.没有对你造成伤害,则其弃置2张牌;2.对你造成过伤害,则你对其造成1点伤害.',
						lqtz_duoxi: '夺玺',
						lqtz_duoxi_info: '其他角色的摸牌阶段,你可以失去1点体力改为你摸两张牌.',
						lqtz_shehun: '摄魂',
						lqtz_shehun_info: '每当你受到伤害后,你可以获得对你造成伤害的牌并摸一张牌.',
						lqtz_lingsi: '灵嘶',
						lqtz_lingsi_info: '锁定技,准备阶段,你对所有敌方角色造成一点伤害,此法造成的伤害有几率变为火属性..',
						lqtz_jingxin: '惊心',
						lqtz_jingxin_info: '当你使用【杀】对一名其他角色造成伤害后,你可以获得其区域内的一张牌.若此牌不为装备牌,则其摸一张牌.若此牌为装备牌,则视为你对其选择的另一名角色使用一张【决斗】.',
						lqtz_xifan: '袭返',
						lqtz_xifan_info: '锁定技,一名角色的结束阶段开始时,若你本回合内使用或打出过基本牌,则你选择一项:1.摸一张牌.2.视为使用一张本回合内使用或打出过的基本牌.',
						lqtz_xiangrui: '祥瑞',
						lqtz_xiangrui_info: '其他角色的出牌阶段结束时,其可以弃置两张牌,令你回复一点体力.',
						lqtz_lihuo: '离火',
						lqtz_lihuo_info: '结束阶段,你可以对一名其他角色造成1点火焰伤害.',
						lqtz_leili: '雷厉',
						lqtz_leili_info: '你的【杀】造成伤害后,你可以对另一名其他角色造成1点雷电伤害.',
						lqtz_fengxing: '风行',
						lqtz_fengxing_info: '回合开始时,你可以选择一名敌方角色,若如此做,视为对其使用了一张【杀】.',
						lqtz_zhenlei: '震雷',
						lqtz_zhenlei_info: '准备阶段,你可以对一名其他角色造成1点雷电伤害.',
						lqtz_lingsia: '灵嘶',
						lqtz_lingsia_info: '锁定技,当你死亡时,对所有角色造成1点火焰伤害.',
						lqtz_xiongqu: '雄躯',
						lqtz_xiongqu_info: '锁定技,你不会成为【偷梁换柱】和【釜底抽薪】的目标.',
						lqtz_yutu: '御兔',
						lqtz_yutu_info: '每轮限一次,你可以视为使用一张【杀】或【闪】.',
						lqtz_wuyou: '无忧',
						lqtz_wuyou_info: '锁定技,你始终跳过摸牌阶段和出牌阶段.',
						lqtz_zhuanshutiaozhan2: '假威',
						lqtz_weihu: '威虎',
						lqtz_weihu_info: '锁定技,准备阶段,你消耗X颗朱果(X为你已损失的体力,若不足则消耗全部朱果),并回复等量的体力.若此时你未升级,且拥有的朱果数量不少于4,则你消耗4颗朱果进行升级.',
						lqtz_zuoji: '佐击',
						lqtz_zuoji_info: '锁定技,友方角色的【杀】对敌方角色造成伤害后,你对该敌方角色造成一点伤害.',
						lqtz_fanshi: '反噬',
						lqtz_fanshi_info: '锁定技,结束阶段,你失去一点体力.',
						lqtz_miewu: '灭吴',
						lqtz_miewu_info: '当你需要使用或打出一张基本牌或锦囊牌时,若你的<创>不少于X,你可以将一张牌当做那张牌使用或打出,并随机获得武将牌上X张<创>(X为同一回合内此技能发动的次数).',
						_lqtz_wuxian: '无限',
						_lqtz_wuxian_info: '挑战模式中,你每一次被击败,都将会变得更加强大.',
						yuezi: '月姿',
						yuezi_info: '锁定技,你回复体力时/回合开始时/回合结束时,你摸一张牌,若此时你没有手牌,则改为摸两张牌;你的手牌上限等于场上所有角色的体力合计.',
						lqtz_shenyu: '神羽',
						lqtz_shenyu_info: '锁定技,你进入濒死状态或即将死亡时,若你的体力大于0,则你取消之;当你处于濒死状态时,其他角色发动的技能全部无效.',
						lqtz_shenbingj: '神兵',
						lqtz_shenbingj_info: '锁定技,你造成伤害后,增加X点体力上限(X为你造成的伤害数).',
						lqtz_tongben: '同本',
						lqtz_tongben_info: '锁定技,你/游戏BOSS对游戏BOSS/你造成伤害时,取消之.',
						lqtz_jiajia: '甲',
						lqtx_xisheng: '牺牲',
						lqtx_xisheng_info: '锁定技,你死亡时,游戏BOSS获得3枚<甲>标记.',
						lqtx_xishengjia: '牺牲',
						lqtx_xishengjia_info: '游戏BOSS进入濒死状态时,你可以令其将体力回复至5点,若如此做,你死亡.',
						lqtz_gongpo: '攻破',
						lqtz_gongpo_info: '锁定技,出牌阶段开始时,你从牌堆获得一张【杀】,你使用此【杀】时,无视距离,不计入次数,且不能被响应.当此【杀】造成伤害时,你回复一点体力.',
						latz_fangyu: '防御',
						latz_fangyu_info: '锁定技,游戏BOSS受到伤害时,你代替其受到等量的伤害,并令游戏BOSS获得一枚<甲>标记.',
						lqtz_feiyue: '飞越',
						lqtz_feiyue_info: '锁定技,游戏BOSS回合结束时,获得一个额外的回合.',
						lqtz_huimie: '毁灭',
						lqtz_huimie_info: '锁定技,已损失的体力小于35%的角色不能响应你使用的牌.你造成伤害时,有35%的几率令伤害翻倍.',
						lqtz_fengbao: '风暴',
						lqtz_fengbao_info: '<font color=#a500cc><b>风暴离子电:</b></font>出牌阶段限一次,你可以令所有敌方角色弃置每种类型(基本/锦囊/装备)的牌各一张并受到一点雷电伤害,若有角色未弃置某种类型的牌,则其于其下次的出牌阶段,不能使用或打出该类型的牌.',
						lqtz_shuangren: '双刃',
						lqtz_shuangren_info: '出牌阶段,你可以移去X枚<甲>标记,令所有「刃」随从满状态复活(X为已阵亡的「刃」随从数×5).',
						lqtz_shuangyi: '双翼',
						lqtz_shuangyi_info: '出牌阶段,你可以移去X枚<甲>标记,令所有「翼」随从满状态复活(X为已阵亡的「翼」随从数×5).',
						lqtz_shuangdun: '双盾',
						lqtz_shuangdun_info: '出牌阶段,你可以移去X枚<甲>标记,令所有「盾」随从满状态复活(X为已阵亡的「盾」随从数×5).',
						lqtz_shenbingx: '神兵',
						lqtz_shenbingx_info: '出牌阶段,你可以移去X枚<甲>标记,令所有「神兵」随从满状态复活(X为已阵亡的「神兵」随从数×5).',
						lqtz_shenjia: '神甲',
						lqtz_shenjia_info: '锁定技,你造成或受到伤害后,获得一枚<甲>标记.你的体力首次减少至10时,你将体力回复至10并终止一切结算,立即开始你的回合.',
						lqtz_bilei: '壁垒',
						lqtz_bilei_info: '锁定技,你受到伤害时,若伤害大于1,则取消之,否则,你有35%的几率获得一点护甲.',
						lqtz_niliu: '逆流',
						lqtz_niliu_info: '<b><font color=#ffaa33>逆流加农炮:</font></b>出牌阶段限一次,你可以对一名其他角色造成X点伤害(X为友方角色上一轮受到的伤害数的一半向上取整).',
						lqtz_jixun: '急迅',
						lqtz_jixun_info: '锁定技,你与其他角色/其他角色与你计算的距离-1/+1.',
						lqtz_leiting: '雷霆',
						lqtz_leiting_info: '<b><font color=#33ccff>雷光极云破:</font></b>你使用【杀】或【决斗】指定目标时,你可以令目标的所有技能失去直到回合结束并选择一项:①摸X张牌,②令此牌的伤害基数+X(X为目标手牌中的♦️️️数量牌),你进行一次判定,目标若不弃置一张与判定结果花色相同的牌则不能响应你使用的牌,若你判定的结果不为♥️️️️且此牌造成伤害时,你可以令伤害-1,目标失去一点体力上限.',
						lqtz_tianwei: '天威',
						lqtz_tianwei_info: '锁定技,①一名角色的回合结束后,若你于回合内造成过伤害,你获得一枚<甲>标记.②你使用的非转化牌造成伤害或你受到伤害后,你获得X枚<甲>标记(X为伤害数).③你的体力大于10时,你视为拥有技能【咆哮】和【无双】,否则你视为拥有技能【龙魂】和【遗计】.④你的体力不大于<span class=firetext>70</span>时,你将体力回复至<span class=firetext>70</span>并终止一切结算,立即开始你的回合,令【天威④】中的数值-10.',
						lqtz_shenchao: '神潮',
						lqtz_shenchao_info: '每回合限一次,你需要使用或打出一张牌时,可以移去一枚<甲>标记视为使用或打出了那张牌,若你于一回合内以此法使用的牌是【万箭齐发】且不超过3张,你令〖神潮〗的使用次数+1.',
						lqtz_zhongzhuang: '重装',
						lqtz_zhongzhuang_info: '出牌阶段限一次,你可以失去所有护甲,对一名其他角色造成X点伤害,可以移去最多3枚<甲>标记并获得2Y点护甲(X为失去的护甲数,Y为移去的标记数).',
						lqtz_jiying: '极影',
						lqtz_jiying_info: '出牌阶段限一次,你可以移去最多3枚<甲>标记,本回合获得以下效果:每回合限X次,你造成伤害后,可摸X张牌或回复X点体力(X为你移去的标记数).',
						_lqtz_duomo: '堕魔',
						_lqtz_duomo_info: '敌方角色不因游戏开始时而因「模式挑战」获得技能时或你的准备阶段,你按顺序获得以下一项效果或按顺序令以下一项效果的数值+1:①准备阶段摸一张牌;②出牌阶段多出一张杀;③手牌上限加一.当你发动7次/12次【堕魔】后,若你是「青女」/「狐女」,则你进化.',
						lqtz_meihuo: '魅惑',
						lqtz_meihuo_info: '出牌阶段限一次,你可以与一名角色拼点.若你赢,你获得以下效果直到回合结束:无视与该角色的距离;无视该角色的防具且对其使用【杀】没有次数限制.若你没赢,你不能使用【杀】直到回合结束.',
						lqtz_qinyun: '琴韵',
						lqtz_qinyun_info: '回合外,你可以将一张红色牌当做【桃】使用.',
						lqtz_duanwei: '断尾',
						lqtz_duanwei_info: '判定阶段开始时,你可以弃置两张手牌,弃置你的判定区所有牌.',
						lqtz_xiuzheng: '修整',
						lqtz_xiuzheng_info: '回合开始时,你结束修整重新加入游戏.',
						lqtz_yazi: '睚眦',
						lqtz_yazi_info: '当你受到伤害后,你可以获得伤害来源的一张牌.',
						lqtz_duoling: '夺灵',
						lqtz_duoling_info: '锁定技,当你造成一点伤害后,若受伤角色与你的距离不大于1,你回复一点体力.',
						lqtz_taoyuan: '饕怨',
						lqtz_taoyuan_info: '准备阶段开始时,你可以视为使用一张无距离限制的【杀】.',
						lqtz_moyan: '魔炎',
						lqtz_moyan_info: '你受到一点伤害后,你可以进行一次判定,若结果为红色,你对一名角色造成两点火焰伤害,否则,你对一名角色造成一点火焰伤害并弃置其一张牌.',
						lqtz_danshu: '丹术',
						lqtz_danshu_info: '每当你于回合外失去牌时,你可以进行一次判定,若结果为红色,你回复1点体力.',
						lqtz_juechou: '绝筹',
						lqtz_juechou_info: '锁定技,①你受到伤害时,伤害来源立即受到等量相同属性且无来源的伤害.②其他角色脱离濒死状态后,你回复一点体力.③当你回复体力后或有装备牌置入一名角色的装备区/进入弃牌堆时,你摸一/X张牌(X为进入弃牌堆的装备牌数).',
						lqtz_longce: '龙策',
						lqtz_longce_info: '你可以将一张牌基本/锦囊牌当做任意基本/锦囊牌使用或打出.若为回合内则每种锦囊牌限使用两次.',
						lqtz_hundu: '魂妒',
						lqtz_hundu_info: '一名角色的判定牌生效后,你可以选择一项,①获得那张牌.②对该角色造成一点伤害.③背水:你弃置一张牌,执行①②选项.',
						lqtz_qiyi: '起翼',
						lqtz_qiyi_info: '锁定技,①此技能不会被失去或失效.②回合开始时,你增加X点体力上限并回复X点体力(X为所有敌方角色的体力合计).',
						lqtz_yongzhua: '勇爪',
						lqtz_yongzhua_info: '①此技能不会被失去或失效.②出牌阶段,若敌方角色的体力上限均小于你,你可以依次对所有敌方角色视为使用一张雷【杀】,你减少一点体力上限.',
						lqtz_quzhu: '驱逐',
						lqtz_quzhu_info: '①此技能不会被失去或失效.②一名角色的出牌阶段开始时,你可以选择一个普通锦囊牌的牌名,若该角色的手牌中有你选择的同名牌,则该角色弃置那些牌,否则,你获得一张同名牌.',
						lqtz_lieyan: '烈焰',
						lqtz_lieyan_info: '锁定技,①此技能不会被失去或失效.②你手牌中的【杀】视为火【杀】.③你的【杀】被闪避后依然造成伤害.④你的【杀】造成伤害后,弃置目标装备区所有牌.',
						lqtz_xiongyan: '凶焰',
						lqtz_xiongyan_info: '①此技能不会被失去或失效.②出牌阶段限一次,你可以弃置所有敌方角色随机X张手牌,对这些角色造成X点火焰伤害(X为所有敌方角色的手牌数合计除于这些角色的数量向上取整).',
						lqtz_ehui: '恶喙',
						lqtz_ehui_info: '①此技能不会被失去或失效.②每回合限一次,你造成或受到伤害时,你可以令一个体力最多的敌方角色失去X点体力(X为体力最少的敌方角色的体力数).',
						lqtz_geming: '革命',
						lqtz_geming_info: '锁定技,①此技能不会被失去或失效.②你造成/受到伤害,目标角色/伤害来源立即失去所有体力.',
						lqtz_kongxi: '空袭',
						lqtz_kongxi_info: '①此技能不会被失去或失效.②你可以将一张黑色/红色【杀】当做一张【惊雷闪】/【炽羽袭】对任意名角色使用.',
						lqtz_yingyi: '英翼',
						lqtz_yingyi_info: '①此技能不会被失去或失效.②场上有其他友方角色存在时,出牌阶段限一次,你可以令一名其他角色弃置所有牌并失去所有体力.',
						lqtz_wuxiao: '武鸮',
						lqtz_wuxiao_info: '锁定技,❶此技能不会被失去或失效.❷回合开始时,你随机将一张装备牌置于你空置的装备栏内.❸你扣减一点体力后,选择一项:①随机将一张装备牌置于你空置的装备区.②摸2+X张牌(X为你装备区的牌数).',
						lqtz_yingbi: '鹰庇',
						lqtz_yingbi_info: '锁定技,①此技能不会被失去或失效.②你不会被翻面.③你一次性扣减大于2的体力时,此数值变成2,你摸X张牌(X为数值-2).',
						lqtz_guogan: '果敢',
						lqtz_guogan_info: '锁定技,①此技能不会被失去或失效.②你的回合开始/结束时,你获得所有敌方角色的手牌和装备区各一张牌,所有角色不能因此操作而触发技能.',
						lqtz_jianong: '加农',
						lqtz_jianong_info: '锁定技,①此技能不会被失去或失效.②你成为牌的目标时,你摸一张牌,若此牌的使用者为敌方角色,则该角色失去一点体力.',
						lqtz_huoren: '火刃',
						lqtz_huoren_info: '锁定技,①此技能不会被失去或失效.②回合开始时,体力不少于你的敌方角色将体力失去至X,你回复Y点体力并摸Y张牌(X为你的体力-1,Y为这些角色合计失去的值).',
						lqtz_fenshao: '焚烧',
						lqtz_fenshao_info: '锁定技,①此技能不会被失去或失效.②你死亡后,所有敌方角色将体力上限减少至X,并各失去一点体力,随机弃置Y张牌(X为该角色的体力,Y为BOSS的体力除于敌方角色数).',
						lqtz_yingyou: '鹰佑',
						lqtz_yingyou_info: '锁定技,①此技能不会被失去或失效.②你不会被翻面.③你不会成为锦囊牌是目标.④你一次性扣减大于2的体力时,此数值变成2,你摸X张牌,若当前回合角色是敌方,则该角色失去X点体力(X为此数值-2).',
						lqtz_rongguang: '荣光',
						lqtz_rongguang_info: '①此技能不会被失去或失效.②你或敌方角色的回合结束时,所有敌方角色失去一点体力.',
						lqtz_aoxiang: '翱翔',
						lqtz_aoxiang_info: '①此技能不会被失去或失效.②出牌阶段,你可以弃置一张锦囊牌,并选择一名其他角色,该角色本回合不能使用或打出手牌并失去一点体力.',
						lqtz_yibang: '异邦',
						lqtz_yibang_info: '锁定技,①此技能不会被失去或失效.②你死亡后,立即复活并将武将牌切换为随机一个前置关卡的随从.',
						lqtz_bileix: '壁垒',
						lqtz_bileix_info: '锁定技,①此技能不会被失去或失效.②你与友方角色不会被翻面.③你不会成为敌方角色锦囊牌的目标.④场上有其他友方角色时,你不会成为敌方角色基本牌的目标.⑤你或友方角色一次性扣减大于2的体力时,取消之,你摸X张牌,若当前回合角色是敌方,则该角色失去X点体力(X为此数值).',
						lqtz_baolei: '堡垒',
						lqtz_baolei_info: '①此技能不会被失去或失效.②出牌阶段限一次,你可以随机令一名友方角色复活.',
						lqtz_yaosai: '要塞',
						lqtz_yaosai_info: '锁定技,①此技能不会被失去或失效.②你使用的基本牌与普通锦囊牌额外结算X次.(X为敌方角色数).',
						lqtz_sanji: '三辑',
						lqtz_sanji_info: '①此技能不会被失去或失效.②出牌阶段限三次,你可以移去所以<武库>标记并摸等量的牌.',
						lqtz_jiwu: '辑武',
						lqtz_jiwu_info: '锁定技,①此技能不会被失去或失效.②摸牌阶段,你的摸牌数+X.③出牌阶段,你使用【杀】的数量+X.④你的手牌上限+X.⑤其他角色计算与你的距离+X.⑥你与其他角色计算的距离-X.⑦你造成伤害时,伤害变为该角色失去X点体力.⑧你受到不小于X的伤害时,伤害减X(X为你的<武库>标记数).',
						lqtz_bangwu: '邦武',
						lqtz_bangwu_info: '锁定技,①此技能不会被失去或失效.②你死亡后,立即复活并将武将牌切换为随机一个前置关卡的BOSS.',
						lqtz_zhixiao: '智鸮',
						lqtz_zhixiao_info: '锁定技,①此技能不会被失去或失效.②准备阶段,你进行一次判定,获得X张锦囊牌(X为判定牌的点数).',
						lqtz_shengjie: '升阶',
						lqtz_shengjie_info: '①此技能不会被失去或失效.②出牌阶段限一次,你可以创建并获得一张「升阶魔法」.③你使用的「升阶魔法」结算后,若牌堆中的「升阶魔法」少于8张,你将此牌置于牌堆的随机位置.',
						lqtz_jixizhili: '急袭之力',
						lqtz_jixizhili_info: '出牌阶段,对一名角色使用,该角色下次判定牌最终生效时,判定点数+1,若该角色有技能〖升阶〗,该角色可以弃置一张牌,获得一张「升阶魔法」,你摸一张.',
						lqtz_mengqinzhili: '猛禽之力',
						lqtz_mengqinzhili_info: '出牌阶段,对一名角色使用,该角色下次判定牌最终生效时,判定点数+1,若该角色有技能〖升阶〗,则其摸两张牌,你摸一张牌.',
						lqtz_gemingzhili: '革命之力',
						lqtz_gemingzhili_info: '出牌阶段,对一名角色使用,该角色下次判定牌最终生效时,判定点数+1.若该角色有技能〖升阶〗,则该角色可以获得一名其他角色一张牌.你获得一张你指定类型的牌.',
						lqtz_tiaoyuezhili: '跳跃之力',
						lqtz_tiaoyuezhili_info: '出牌阶段,对一名角色使用,该角色下次判定牌最终生效时,判定点数+2.若该角色有技能〖升阶〗,且此牌的使用者为其/不为其,则其可以将此牌交给一名其他角色/获得此牌,你摸一张牌.',
						lqtz_xuehunzhili: '削魂之力',
						lqtz_xuehunzhili_info: '出牌阶段,对一名角色使用,该角色下次判定牌最终生效时,判定点数+2,该角色可以选择失去一点体力改为+4,你摸一张牌.',
						lqtz_siwangshuangli: '死亡双力',
						lqtz_siwangshuangli_info: '出牌阶段,对一名角色使用,该角色下次判定牌最终生效时,判定点数翻倍,你摸一张牌.',
						lqtz_katong: '卡通',
						lqtz_katong_info: '卡通',
						lqtz_zuiyan: '罪炎',
						lqtz_zuiyan_info: '罪炎',
						lqtz_heiya: '黑亚',
						lqtz_heiya_info: '黑亚',
						lqtz_heigang: '黑钢',
						lqtz_heigang_info: '黑钢',
						lqtz_angang: '暗钢',
						lqtz_angang_info: '暗钢',
						lqtz_longquan: '龙拳',
						lqtz_longquan_info: '龙拳',
						lqtz_annang: '暗能',
						lqtz_annang_info: '暗能',
					},
					// --------------------------------------武将简介------------------------------------------//
					characterIntro: {
						lqtq_abiao: '<img src="extension/浪琴天阙/image/辅助图/lqtq_abiaozuoji.jpg" width="300" height="450"></b>',
						lqtq_baibanbiao: '没想到吧,婊只是个白板,但是婊自带BUFF喔,可在<浪琴天阙>扩展页面「无限婊读」功能中查看详情.',
						lqtq_miaoxiaoyu: '猫猫和啊婊.',
						lqtq_langqinjun: '浪琴君,原名浪琴婊,浪琴君的技能星辰是一个拼字游戏,这里啊婊和大家说句掏心窝子的话,这个技能啊婊做得摆烂了M次,最后终于在猫猫的帮助下完成了,所以说啊婊的厉害之处并不只是代码,还有万能的猫猫让我抱猫腿.',
						lqtq_yufengbiao: '随意飞行,畅通无阻,撞天花板？不存在的,想飞多久飞多久!',
						lqtqj_shashen: '随机创作,有感而发',
						lqtqj_hongdie: '抗性,破坏,伤害,康康,样样俱全,yyds,爹一样的存在,故而被称之为红爹,又称红狙(蛆),常有人言,希望大家以后吃🍎都能吃出蛆,半条,以此来比喻红蛆的恶心程度,刚上线没多久官方便因为过于恐怖如斯而对其进行限制,打压,最后妥妥的安排进了牢,寄!真可谓是ygo史上活得最短命的一个爹,真心疼那些一出就🌸血本买下三张实卡的淫.',
						lqtqj_qixian: '某<诛仙>动漫女主,<九天玄刹,化为神雷,煌煌天威,以剑引之!>主要是看她说这句话的时候感觉很受,就get进了扩展里,稀有专属,强度必然不用说啦.',
						lqhc_qin: '武将设计:<br>群昵称:有时候是摆烂婊,偶尔会变成勤更婊<br>QQ昵称:浪琴婊<P><b>琴:</b><br>某自走棋的一颗棋子,琴是一个单体攻击能力极高的角色,很多人都喜欢玩带琴的阵容,这类人被称之为<浪琴婊>,不错,这就是浪琴婊的由来,也是啊婊网名的由来,当啊婊第一次看到浪琴婊这个称乎的时候,便对其爱不释手.而后来,很多游戏的昵称都禁用了婊这个字,哎,生活不易,啊婊做妓,所以啊婊别名浪琴羽.',
						lqhc_hanmaojun: '武将设计:<br>群昵称:千面猫<br>QQ昵称:千面猫<P><b>憨猫君:</b><br>此武将技能其实是猫猫亲自写的,憨猫君的技能群宠特别有新意,像送花,丢拖鞋之类的就能触发收益,不过这个技能还是有点小问题的,例如你丢拖鞋或者砸蛋之后,需要进行下一步操作才会刷新并触发技能,毕竟交流不是技能,没有时点.',
						lqhc_fengxuanjun: '武将设计:<br>群昵称:靓仔<br>QQ昵称:凌风失翎羽,通玄复得道<P><b>风玄君:</b><br>风玄君的技能比较有趣,始终不会拥有手牌的一个武将,游戏中可进可退,强度与游戏开始时你的选择息息相关.',
						lqhc_xinlianyue: '武将设计:<br>群昵称:辛涟月<br>QQ昵称:辛涟月<P><b>辛涟月:</b><br>暂无武将介绍.',
						lqhc_hanxin: '武将设计:<br>群昵称:乔<br>QQ昵称:亻白幺勺~<P><b>韩信:</b><br>(？—公元前196年),生年不详,淮阴县人.西汉开国功臣、军事家,被刘邦评价为麾下三位人杰之一,后世以此称之<汉初三杰>,古代军事思想<兵权谋家>的代表人物,后人奉为<兵仙>、<神帅>.',
						lqhc_xiangyu: '武将设计:<br>群昵称:乔<br>QQ昵称:亻白幺勺~<P><b>项羽:</b><br>(公元前232年―公元前202年),唐宋典籍记载为周王族诸侯国项国后代,姬姓,项氏,名籍,字羽,泗水郡下相县人.秦朝末年政治家、军事家,楚国名将项燕的孙子.',
						lqhc_shence: '武将设计:<br>群昵称:胖乔<br>QQ昵称:想去远方<P><b>神策:</b><br>暂无武将介绍.',
						lqhc_qijing: '武将设计:<br>群昵称:大文豪<br>QQ昵称:大文豪<P><b><棋经>:</b><br>暂无武将介绍.',
						lqhc_yilite: '武将设计:<br>群昵称:四下皆是你<br>QQ昵称:四下皆是你<P><b>伊力特:</b><br>赛尔号精灵,魔法筑梦师.',
						lqhc_gujian: '武将设计:<br>群昵称:古见<br>QQ昵称:游梦<P><b>古见:</b><br>古见的本质等于涩涩,经常会在群里发一些擦边图片.',
						lqhc_yitianxing: '武将设计:<br>群昵称:靓仔<br>QQ昵称:凌风失翎羽,通玄复得道<P><b>弈天行:</b>暂无武将介绍.',
						lqhc_tushanhonghong: '武将设计:<br>群昵称:胖乔<br>QQ昵称:想去远方<P><b>涂山红红:</b>中国漫画<狐妖小红娘>及其衍生作品中的主要角色之一,亦是漫画中<月红篇>的女主角.东狐,在人类妖怪战力排行榜上位列传说四皇之一.涂山狐妖之王,亦是妖盟盟主.有温柔善良一面,也有霸气冷漠的一面.涂山红红有着强大到无与伦比的妖力,令众妖闻风丧胆,一直都是涂山第一人,深得妹妹涂山雅雅敬佩.幼年时曾经误杀了自己的救命恩人而一度陷入低潮.在救了东方月初后,内心有所放开,但没有接受东方月初的表白.东方月初为了她的梦想离开涂山五十年,最后因某件变故,涂山红红面对自己的内心,与东方月初在苦情巨树下许下了来世再见的愿望.',
						lqhc_dongfangyuechu: '武将设计:<br>群昵称:胖乔<br>QQ昵称:想去远方<P><b>东方月初:</b>中国漫画<狐妖小红娘>及其衍生作品中的主要人物之一.一气道盟中有名的后起之秀,后晋升盟主,成为道界最强之人.能力超群,能使用狐族法术.幼年时曾被涂山红红救下,一直称涂山红红为妖仙姐姐.原本打算在七夕向她表白却未能如愿,想要实现涂山红红的某个梦想.',
						lqhc_chenping: '武将设计:<br>群昵称:乔<br>QQ昵称:亻白幺勺~<P><b>陈平:</b><br>(？～前179年十月),汉族,阳武户牖乡(今河南原阳东南,一说兰考县,一说山东省东明县)人,西汉王朝开国功臣,<史记>称之为陈丞相.少时喜读书,有大志,曾为乡里分肉,甚均,父老赞之,他感慨地说:<使平得宰天下,亦如此肉矣!',
						lqhc_xiangquyuanfang: '武将设计:<br>群昵称:乔<br>QQ昵称:亻白幺勺~<P><b>想去远方:</b><br>群某位中二群友,性格极其自大,是个话唠,和他聊起稿件的时候,不切实际的废话一大堆,喜欢用高人一等的眼光看待群友,甚至曾在群里爆音大喊自己是神.',
						lqhc_yinbuniao: '武将设计:<br>群昵称:银之庭<br>QQ昵称:银之庭<P><b>银布鸟:</b>🐦暂无武将介绍.',
						lqhcgai_mayuanyi: '武将设计:<br>群昵称:乔<br>QQ昵称:亻白幺勺~<P><b>马元义:</b><br>(？—184年6月7日),东汉末年黄巾之乱的组织者之一,是黄巾大方首领.光和六年(183年),马元义奉黄巾军首领张角之命,筹划起事的准备工作.马元义聚集荆州、扬州信徒数万人约定于邺城相会,并收买中常侍封谞、徐奉等为内应,准备于次年(甲子年)三月初五(戊申日)内外同时起事.由于张角弟子济南人唐周的告密,马元义于于中平元年(184年)正月在河内郡山阳县被捕,送往洛阳车裂,同时被杀的还有教众近千人.由于马元义的被杀,迫使张角提前于二月起兵发动叛乱.',
						lqhcgai_simashi: '武将设计:<br>群昵称:乔<br>QQ昵称:亻白幺勺~<P><b>司马师:</b><br>(208年～255年3月23日),字子元,河内温县(今河南省温县)人.三国时期曹魏权臣,西晋王朝的奠基人之一,晋宣帝司马懿与宣穆皇后张春华的长子,晋文帝司马昭的同母兄,晋武帝司马炎的伯父.司马师沉着坚强,雄才大略,早年与夏侯玄、何晏齐名.高平陵政变后,以功封长平乡侯,旋加卫将军.司马懿死后,以抚军大将军辅政,独揽朝廷大权,次年升为大将军.掌权后,他制定选拔官吏的法规,命百官推荐贤才,整顿纲纪,使其各有职掌,朝野肃然.司马师也有卓越的军事才能,曾用计于新城之战击溃吴国诸葛恪的大军.嘉平六年(254年),魏帝曹芳与中书令李丰等人密谋除司马师,事情泄露,司马师击杀参与者,迫郭太后废曹芳,改立高贵乡公曹髦为帝.次年,亲自率兵平定毌丘俭、文钦之乱.回师途中病死,时年四十八岁,谥号<忠武>.后被追尊为晋景王.西晋建立后,被追尊为景皇帝,庙号世宗.',
						lqhcgai_simayi: '武将设计:<br>群昵称:秋雨叶落時<br>QQ昵称:秋雨叶落時<P><b>司马懿:</b>晋宣帝,字仲达,河内温人.曾任职过曹魏的大都督,太尉,太傅.少有奇节,聪明多大略,博学洽闻,伏膺儒教,世之鬼才也.',
						lqhcgai_liuye: '武将设计:<br>群昵称:秋雨叶落時<br>QQ昵称:秋雨叶落時<P><b>刘晔:</b>(？－234年),字子扬,淮南成德人,是光武帝刘秀之子阜陵王刘延的后代,三国时期魏国著名的战略家.刘晔年少知名,人称有佐世之才,是曹操手下举足轻重的谋士,他屡献妙计,对天下形势的发展往往一语中的.刘晔历仕数朝,是曹魏的三朝元老.',
						lqhcgai_xuhuang: '武将设计:<br>群昵称:白<br>QQ昵称:白<P><b>徐晃:</b><br>字公明,河东杨人.三国时期曹魏名将,本为杨奉帐下骑都尉,杨奉被曹操击败后转投曹操,在曹操手下多立功勋,参与官渡、赤壁、关中征伐、汉中征伐等几次重大战役.',
						lqhcgai_jiangwei: '武将设计:<br>群昵称:乔<br>QQ昵称:亻白幺勺~<P><b>姜维:</b><br>字伯约,天水冀人.三国时期蜀汉著名将领、军事统帅.原为曹魏天水郡的中郎将,后降蜀汉,官至凉州刺史、大将军.诸葛亮去世后继承诸葛亮的遗志,继续率领蜀汉军队北伐曹魏,与曹魏名将陈泰、郭淮、邓艾等多次交手.',
						lqhcgai_lvmeng: '武将设计:<br>群昵称:乔<br>QQ昵称:亻白幺勺~<P><b>吕蒙:</b><br>字子明,汝南富陂人.陈寿评曰:<吕蒙勇而有谋断,识军计,谲郝普,擒关羽,最其妙者.初虽轻果妄杀,终于克己,有国士之量,岂徒武将而已乎!>',
						lqhcgai_zhaoyun: '武将设计:<br>群昵称:Nokoriwan<br>QQ昵称:Nokoriwan<P><b>赵云:</b><br>字子龙,常山真定人.身长八尺,姿颜雄伟.长坂坡单骑救阿斗,先主云:<子龙一身都是胆也.>',
						lqhcgai_zhangfei: '武将设计:<br>群昵称:Nokoriwan<br>QQ昵称:Nokoriwan<P><b>张飞:</b>字翼德,涿郡人,燕颔虎须,豹头环眼.有诗云:<长坂坡头杀气生,横枪立马眼圆睁.一声好似轰雷震,独退曹家百万兵>.',
						lqhcgaishen_jiangwei: '武将设计:<br>群昵称:乔<br>QQ昵称:亻白幺勺~<P><b>姜维:</b><br>字伯约,天水冀人.三国时期蜀汉著名将领、军事统帅.原为曹魏天水郡的中郎将,后降蜀汉,官至凉州刺史、大将军.诸葛亮去世后继承诸葛亮的遗志,继续率领蜀汉军队北伐曹魏,与曹魏名将陈泰、郭淮、邓艾等多次交手.',
						lqhcgaix_zhaoyun: '武将设计:<br>群昵称:祈星丶雨落<br>QQ昵称:祈星丶雨落<P><b>赵云:</b><br>字子龙,常山真定人.身长八尺,姿颜雄伟.长坂坡单骑救阿斗,先主云:<子龙一身都是胆也.>',
						lqym_heimodao: '作为魔法师,攻击力·守备力都是最高级别.',
						lqym_zhenhongyanheilong: '拥有真红之眼的黑龙.愤怒的黑炎会把映入其眼者全部烧成灰烬.',
						lqym_qingyanbailong: '以高攻击力著称的传说之龙.任何对手都能粉碎,其破坏力不可估量.',
						lqtz_machao: '永远的神为某千面猫大佬遗弃的随笔作品,多处代码由我的喵腿千面猫大佬亲手完成.此BOSS技能紧密度极高,以【铁骑】为中心展开强力的输出与控场能力.',
						lqtz_langqinbiao: '作者的本命武将,偷偷告诉你,浪琴婊的技能是不会被抹去和失效的喔.',
						lqtz_langqinyu: '挑战模式中,你每一次被击败,都会进化变得更加强大,进化10次后,游戏开始时,会召唤随从助阵.',
						lqtz_zhuquejiangling: '朱雀,是中国古代神话中的天之四灵之一,于五行主火,象征四象中的老阳.朱雀玄武命脉相连,当一方受到伤害,另一方将会摸牌.两者拥有不死之身,其阵亡后,经历短暂休整便会复活并变得更加强大.只有让两者同时阵亡才能获得胜利.',
						lqtz_zhuquefaxiang: '朱雀,是中国古代神话中的天之四灵之一,于五行主火,象征四象中的老阳.朱雀玄武命脉相连,当一方受到伤害,另一方将会摸牌.两者拥有不死之身,其阵亡后,经历短暂休整便会复活并变得更加强大.只有让两者同时阵亡才能获得胜利.',
						lqtz_zhuquezhenshen: '朱雀,是中国古代神话中的天之四灵之一,于五行主火,象征四象中的老阳.朱雀玄武命脉相连,当一方受到伤害,另一方将会摸牌.两者拥有不死之身,其阵亡后,经历短暂休整便会复活并变得更加强大.只有让两者同时阵亡才能获得胜利.',
						lqtz_xuanwujiangling: '玄武,是中国古代神话中的天之四灵之一,于五行主水,象征四象中的老阴.朱雀玄武命脉相连,当一方受到伤害,另一方将会摸牌.两者拥有不死之身,其阵亡后,经历短暂休整便会复活并变得更加强大.只有让两者同时阵亡才能获得胜利.',
						lqtz_xuanwufaxiang: '玄武,是中国古代神话中的天之四灵之一,于五行主水,象征四象中的老阴.朱雀玄武命脉相连,当一方受到伤害,另一方将会摸牌.两者拥有不死之身,其阵亡后,经历短暂休整便会复活并变得更加强大.只有让两者同时阵亡才能获得胜利.',
						lqtz_xuanwuzhenshen: '玄武,是中国古代神话中的天之四灵之一,于五行主水,象征四象中的老阴.朱雀玄武命脉相连,当一方受到伤害,另一方将会摸牌.两者拥有不死之身,其阵亡后,经历短暂休整便会复活并变得更加强大.只有让两者同时阵亡才能获得胜利.',
						'lqym_No.6': '卡图用了混沌No.6的,因为No.6丑,没有问题.',
						'lqym_No.9': '卡图用了混沌No.9的,因为No.9丑,没有问题.',
					},
					// --------------------------------------武将称号------------------------------------------//
					characterTitle: {
						lqtq_baibanbiao: '#r白板婊',
						lqtq_abiao: '#r本体出战',
						lqtq_miaoxiaoyu: '#r千面猫&浪琴羽',
						lqtq_langqinjun: '浪琴婊',
						lqhc_hanmaojun: '千面猫',
						lqhc_fengxuanjun: '慕善',
						lqtz_langqinbiao: '#r偷偷告诉你,浪琴婊的技能是不会被抹去和失效的喔.',
						lqtz_langqinyu: '#r挑战模式中,你每一次被击败,都会进化变得更加强大,进化10次后,游戏开始时,会召唤随从助阵.',
						lqtz_machao: '#b千面猫设计',
						lqtq_yufengbiao: '#r随意飞行,畅通无阻,撞天花板？不存在的,想飞多久飞多久!',
						lqhc_yilite: '归亡节之魂',
						'lqym_No.76': '谐调光师',
						'lqym_No.0': '未来No.0',
						'lqym_No.1': '感染蝇王',
						'lqym_No.2': '蚊学忍者',
						'lqym_No.4': '猛毒刺胞',
						'lqym_No.5': '亡胧龙',
						'lqym_No.6': '先史遗产',
						'lqym_No.8': '纹章王',
						'lqym_No.9': '天盖星',
						'lqym_No.10': '白辉士',
						'lqym_No.12': '机甲忍者',
						'lqym_No.15': '机关傀儡',
						'lqym_No.18': '纹章祖',
						'lqym_No.20': '蚁岩土',
						'lqym_No.23': '冥界的灵骑士',
					},
					// --------------------------------------浪琴天阙------------------------------------------//
					characterSort: {
						langqintianque: {
							lqtq_luolijisi: ['lqtq_abiao', 'lqtqre_abiao'],
							lqtq_yibiaotangtang: ['lqtq_dubiao', 'lqtq_miaoxiaoyu', 'lqtq_baibanbiao', 'lqtq_yufengbiao', 'lqtq_langqinjun', 'lqtq_xiutianbiao'],
							lqtq_tufaqixiang: ['lqtqt_sunshangxiang', 'lqtqt_guanyinping', 'lqtqt_caojinyu', 'lqtqt_daxiaoqiao', 'lqtqt_zhouchu', 'lqtqt_huangyueying', 'lqtqt_baosanniang', 'lqtqt_zhugeguo', 'lqtqt_caiwenji', 'lqtqt_duyu', 'lqtqt_guojia', 'lqtqt_sunhanhua', 'lqtqt_majun', 'lqtqt_yuji', 'lqtqt_zuoci'],
							lqtq_xianzuogaizhi: ['lqtqxshen_xunyu', 'lqtqxshen_ganning', 'lqtqxshen_sunce', 'lqtqxshen_guojia', 'lqtqx_zhonghui', 'lqtqx_dengai', 'lqtqx_huangzhong', 'lqtqx_xusheng'],
							lqtq_xiyouzhuanshu: ['lqtqj_shashen', 'lqtqj_hongdie', 'lqtqj_qixian', 'lqtqj_linglong'],
						},
					},
					// --------------------------------------武将资料------------------------------------------//
					character: {
						lqtq_abiao: ['female', 'shen', '3/6', ['lqtq_yuanjiao', 'lqtq_weiniang'], ['shu']],
						lqtqre_abiao: ['female', 'shen', '3/6', ['lqtq_yinpa', 'lqtq_yuzu'], ['shu']],
						lqtq_xiutianbiao: ['female', 'qun', 4, ['lqtq_xiutian', 'lqtq_dilin'], ['qun']],
						lqtq_miaoxiaoyu: ['female', 'shen', 4, ['lqtq_baihe', 'lqtq_shuangxiu'], ['wu']],
						lqtq_baibanbiao: ['female', 'shen', 1, [], ['wei']],
						lqtq_langqinjun: ['female', 'shen', 3, ['lqtq_xingchen', 'lqtq_xuelin'], ['qun']],
						lqtq_yufengbiao: ['female', 'qun', 3, ['lqtq_buju', 'lqtq_tianshu'], ['qun']],
						lqtq_dubiao: ['female', 'qun', 4, ['lqtq_dudan', 'lqtq_nifan'], ['qun']],
						lqtqt_caiwenji: ['female', 'qun', 3, ['lqtq_beixian', 'lqtq_huiyi'], ['qun']],
						lqtqt_majun: ['male', 'wei', 3, ['lqtq_meizhi', 'lqtq_jingqiao'], ['wei']],
						lqtqt_zuoci: ['male', 'qun', 3, ['lqtq_yabo'], ['qun']],
						lqtqt_yuji: ['male', 'qun', 3, ['lqtq_guhuo', 'lqtq_xiqiao'], ['qun']],
						lqtqt_guojia: ['male', 'wei', 3, ['lqtq_tianhui', 'lqtq_tianzhi'], ['wei']],
						lqtqt_duyu: ['male', 'jin', 4, ['lqtq_sanchen', 'lqtq_miewu'], ['jin']],
						lqtqt_zhugeguo: ['female', 'shu', 3, ['lqtq_lingrang', 'lqtq_huayue'], ['shu']],
						lqtqt_baosanniang: ['female', 'shu', 3, ['lqtq_pingnan'], ['shu']],
						lqtqt_huangyueying: ['female', 'shu', 3, ['lqtq_jingzhi'], ['shu']],
						lqtqt_zhouchu: ['male', 'wu', 4, ['lqtq_zhangquan', 'lqtq_xihai'], ['wu']],
						lqtqt_daxiaoqiao: ['male', 'wu', 3, ['lqtq_huiling', 'lqtq_chiwu', 'lqtq_zongling'], ['wu']],
						lqtqt_caojinyu: ['female', 'qun', 3, ['lqtq_weishang', 'lqtq_anhong', 'lqtq_yujue'], ['qun']],
						lqtqt_sunhanhua: ['female', 'wu', 4, ['lqtq_tiantuo', 'lqtq_fuyin'], ['wu']],
						lqtqt_guanyinping: ['female', 'shu', 3, ['lqtq_wuyi', 'lqtq_yinpo'], ['shu']],
						lqtqt_sunshangxiang: ['female', 'wu', 3, ['lqtq_xiaoge', 'lqtq_yuanshen'], ['wu']],
						lqtqx_zhonghui: ['male', 'wei', 4, ['lqtqx_quanji', 'lqtqx_zili'], ['wei']],
						lqtqx_dengai: ['male', 'wei', 4, ['lqtqx_tuntian', 'lqtqx_ziliang', 'lqtqx_zaoxian'], ['wei']],
						lqtqx_xusheng: ['male', 'wu', 4, ['lqtqx_pojun', 'lqtqx_yicheng'], ['wu']],
						lqtqx_huangzhong: ['male', 'shu', 4, ['lqtqx_liegong'], ['shu']],
						lqtqxshen_sunce: ['male', 'shen', '1/6', ['lqtqx_yingba', 'lqtqx_fuhai', 'lqtqx_pinghe'], ['wu']],
						lqtqxshen_guojia: ['male', 'shen', 3, ['lqtqx_huishi', 'lqtqx_tianyi', 'lqtqx_shuishi', 'lqtqx_zuoxing'], ['wei']],
						lqtqxshen_ganning: ['male', 'shen', '3/4', ['lqtqx_jinfan', 'lqtqx_qixi', 'lqtqx_jieying'], ['wu']],
						lqtqxshen_xunyu: ['male', 'shen', 3, ['lqtqx_quzuo', 'lqtqx_jiece', 'lqtqx_dinghan'], ['wei']],
						lqtqj_hongdie: ['male', 'shen', 4, ['lqtq_quan', 'lqtq_mieyan'], ['jin']],
						lqtqj_qixian: ['female', 'shen', 4, ['lqtq_xuansha'], ['qun']],
						lqtqj_shashen: ['male', 'shen', 4, ['lqtqj_shashen', 'lqtq_basu', 'lqtq_jiaoyi'], ['qun']],
						lqtqj_linglong: ['male', 'shen', 2, ['lqtq_huanshang', 'lqtq_juejing', 'lqtq_longling'], ['shu']],
					},
				};
				for (const i in langqintianque.character) {
					langqintianque.character[i][4].push('ext:浪琴天阙/image/浪琴天阙/' + get.translation(i) + '.jpg');
				}
				lib.config.all.characters.add('langqintianque');
				lib.config.characters.add('langqintianque');
				lib.translate.langqintianque_character_config = '<font color=#ffffbb>浪琴</font><font color=#ffb7dd>天阙</font>';
				return langqintianque;
			});
			// ------------------------------浪琴荟萃-----------------------------------//
			game.import('character', function () {
				const langqinhuicui = {
					name: 'langqinhuicui',
					connect: true,
					characterSort: {
						// --------------------------------------武将分类------------------------------------------//
						langqinhuicui: {
							lqtq_qunyingtougaojing: ['lqhc_yinbuniao', 'lqhc_gujian', 'lqhc_qijing', 'lqhc_yitianxing', 'lqhc_xiangyu', 'lqhc_hanxin', 'lqhc_fengxuanjun', 'lqhc_hanmaojun', 'lqhc_xiangquyuanfang'],
							lqtq_qunyingtougaozuo: ['lqhc_yilite', 'lqhc_shence', 'lqhc_xinlianyue', 'lqhc_chenping', 'lqhc_qin', 'lqhc_tushanhonghong', 'lqhc_dongfangyuechu'],
							lqtq_qunyingtougaogai: ['lqhcgaix_zhaoyun', 'lqhcgai_jiangwei', 'lqhcgai_lvmeng', 'lqhcgaishen_jiangwei', 'lqhcgai_zhaoyun', 'lqhcgai_zhangfei', 'lqhcgai_simashi', 'lqhcgai_mayuanyi', 'lqhcgai_xuhuang', 'lqhcgai_liuye', 'lqhcgai_simayi'],
						},
					},
					// --------------------------------------武将资料------------------------------------------//
					character: {
						lqhc_fengxuanjun: ['male', 'shen', 3, ['lqhc_jiuxian', 'lqhc_shiyuan'], ['shu']],
						lqhc_hanmaojun: ['female', 'shen', 3, ['lqhc_zhaocai', 'lqhc_qunchong'], ['wei']],
						lqhc_yitianxing: ['male', 'wei', 4, ['lqhc_liaodi', 'lqhc_zhisheng'], ['wei']],
						lqhc_hanxin: ['male', 'shu', 4, ['lqhc_shuizhan', 'lqhc_yibing'], ['shu']],
						lqhc_xiangyu: ['male', 'qun', 6, ['lqhc_gaishi', 'lqhc_jingji', 'lqhc_yiwen'], ['qun']],
						lqhc_xiangquyuanfang: ['female', 'shen', 4, ['lqhc_tiaozhan', 'lqhc_zhonger'], ['qun']],
						lqhc_qijing: ['female', 'qun', 3, ['lqhc_qiuxian', 'lqhc_tizi'], ['qun']],
						lqhc_yinbuniao: ['female', 'qun', 3, ['lqhc_shuiqun', 'lqhc_qianshui', 'lqhc_maopao'], ['hiddenSkill', 'qun']],
						lqhc_xinlianyue: ['female', 'shen', 3, ['lqhc_lianyue', 'lqhc_wenqing', 'lqhc_xidie'], ['qun']],
						lqhc_tushanhonghong: ['female', 'shen', 5, ['lqhc_jueyuan', 'lqhc_yaoli', 'lqhc_xinshang', 'lqhc_yanleiH'], ['qun']],
						lqhc_dongfangyuechu: ['male', 'shen', 6, ['lqhc_shenhuo', 'lqhc_daofa', 'lqhc_jizhi', 'lqhc_yanleiC'], ['qun']],
						lqhc_chenping: ['male', 'shu', 3, ['lqhc_tiaobo', 'lqhc_liyou', 'lqhc_liuji'], ['shu']],
						lqhc_qin: ['female', 'qun', 3, ['lqhc_hanguang', 'lqhc_chuqiao'], ['qun']],
						lqhc_shence: ['male', 'shen', '3/5', ['lqhc_shence', 'lqhc_liangzhi', 'lqhc_renwang', 'lqhc_nitian'], ['qun']],
						lqhc_yilite: ['female', 'qun', 4, ['lqhc_nanci', 'lqhc_jiexin', 'lqhc_huaishang', 'lqhc_gengxin', 'lqhc_chaoneng'], ['qun']],
						lqhc_gujian: ['female', 'shen', 4, ['lqhc_yiyou', 'lqhc_chouka', 'lqhc_anjue'], ['qun']],
						lqhcgaishen_jiangwei: ['male', 'shen', 4, ['lqhc_kunlin', 'lqhc_wuzhi', 'lqhc_yixiang'], ['shu']],
						lqhcgai_lvmeng: ['male', 'shen', 4, ['lqhc_shelie', 'lqhc_gongxin', 'lqhc_xuepi'], ['wu']],
						lqhcgai_zhaoyun: ['male', 'shu', 3, ['lqhc_longwei', 'lqhc_longhun', 'lqhc_gaoda'], ['shu']],
						lqhcgai_zhangfei: ['male', 'shen', 5, ['lqhc_jiyuan', 'lqhc_shenxiao', 'lqhc_xingtian'], ['shu']],
						lqhcgai_simashi: ['male', 'wei', '4/5', ['lqhc_jinglve', 'lqhc_baiyi', 'lqhc_shanli'], ['wei']],
						lqhcgai_mayuanyi: ['male', 'qun', 4, ['lqhc_jibing', 'lqhc_moucuan'], ['qun']],
						lqhcgai_xuhuang: ['female', 'wei', 4, ['lqhc_guibi', 'lqhc_liangjiang'], ['wei']],
						lqhcgai_simayi: ['male', 'wei', 3, ['lqhc_guicai', 'lqhc_langgu', 'lqhc_zhabing'], ['wei']],
						lqhcgai_liuye: ['male', 'wei', 3, ['lqhc_polu', 'lqhc_juezan', 'lqhc_choulue'], ['wei']],
						lqhcgai_jiangwei: ['female', 'shu', 5, ['lqhc_tiaoxin', 'lqhc_zhiji'], ['shu']],
						lqhcgaix_zhaoyun: ['male', 'shu', 4, ['lqhc_longdan', 'lqhc_qinggang', 'lqhc_lingqiao'], ['shu']],
						//'lqfxshen_xunyllu':['male','shen',3,['lqhc_tiaobo','lqhc_liuji'],.wei],
					},
				};
				for (const i in langqinhuicui.character) {
					langqinhuicui.character[i][4].push('ext:浪琴天阙/image/浪琴荟萃/' + i + '.jpg');
				}
				lib.config.all.characters.add('langqinhuicui');
				lib.config.characters.add('langqinhuicui');
				lib.translate.langqinhuicui_character_config = '<font color=#ffff77>浪琴</font><font color=#ff78c2>荟萃</font>';
				return langqinhuicui;
			});
			// ------------------------------浪琴游漫-----------------------------------//
			game.import('character', function () {
				const langqinyouman = {
					name: 'langqinyouman',
					connect: true,
					characterSort: {
						// --------------------------------------武将分类------------------------------------------//
						langqinyouman: {
							lqtq_jiedouguaishou: ['lqym_heimodao', 'lqym_zhenhongyanheilong', 'lqym_qingyanbailong', 'lqym_modaolongqishi', 'lqym_zhanmolongqishi', 'lqym_qingyanheilong'],
							lqtq_Noxilie: ['lqym_No.0', 'lqym_No.1', 'lqym_No.2', 'lqym_No.3', 'lqym_No.4', 'lqym_No.5', 'lqym_No.6', 'lqym_No.7', 'lqym_No.8', 'lqym_No.9', 'lqym_No.10', 'lqym_No.11', 'lqym_No.12', 'lqym_No.13', 'lqym_No.14', 'lqym_No.15', 'lqym_No.16', 'lqym_No.17', 'lqym_No.18', 'lqym_No.19', 'lqym_No.20', 'lqym_No.21', 'lqym_No.22', 'lqym_No.23', 'lqym_No.31', 'lqym_No.66', 'lqym_No.76', 'lqym_No.78'],
						},
					},
					// --------------------------------------武将资料------------------------------------------//
					character: {
						lqym_modaolongqishi: ['male', 'jin', 3, ['lqym_mieyan', 'lqym_quan'], ['jin']],
						lqym_zhenhongyanheilong: ['male', 'jin', 4, ['lqym_heiyan', 'lqym_honghun'], ['jin']],
						lqym_heimodao: ['male', 'jin', 3, ['lqym_modao'], ['jin']],
						lqym_qingyanbailong: ['female', 'shen', 3, ['lqym_qiangren', 'lqym_wudi', 'lqym_zuiqiang'], ['shen']],
						lqym_zhanmolongqishi: ['male', 'jin', 0, ['lqym_zhanmo'], ['jin']],
						lqym_qingyanheilong: ['male', 'jin', 0, ['lqym_ganghua', 'lqym_jingti'], ['jin']],
						'lqym_No.0': ['male', 'shu', 4, ['lqym_weishi', 'lqym_jiaozhu', 'lqym_shuanghuang'], ['shu']],
						'lqym_No.1': ['male', 'qun', 4, ['lqym_ganranx'], ['qun']],
						'lqym_No.2': ['female', 'shu', 3, ['lqym_huanjue', 'lqym_xunxin'], ['qun']],
						'lqym_No.3': ['male', 'jin', 3, ['lqym_gongcan', 'lqym_chantui'], ['jin']],
						'lqym_No.4': ['male', 'jin', 4, ['lqym_ganran', 'lqym_yinzhuang'], ['jin']],
						lqym_diezhuangyouti: ['male', 'jin', '1/2/1', ['lqym_jinran'], ['minskin'], 'jin'],
						'lqym_No.5': ['male', 'jin', 4, ['lqym_qianhe', 'lqym_longtong'], ['jin']],
						'lqym_No.6': ['male', 'shu', 3, ['lqym_yanfen', 'lqym_yanhua'], ['shu']],
						'lqym_No.7': ['male', 'wu', 3, ['lqym_xingyun'], ['wu']],
						'lqym_No.8': ['female', 'qun', 3, ['lqym_jizu'], ['qun']],
						'lqym_No.9': ['male', 'jin', 3, ['lqym_yaoxing', 'lqym_jianmie', 'lqym_tiangai'], ['jin']],
						'lqym_No.10': ['male', 'wei', 4, ['lqym_baihui', 'lqym_tianqi'], ['wei']],
						'lqym_No.11': ['male', 'qun', '2/3/1', ['lqym_kanpo'], ['qun']],
						'lqym_No.12': ['male', 'shu', 3, ['lqym_guiying'], ['shu']],
						'lqym_No.13': ['male', 'qun', 3, ['lqym_gaiyin', 'lqym_xianren', 'lqym_shuangan'], ['qun']],
						'lqym_No.31': ['male', 'qun', 3, ['lqym_yabo', 'lqym_shuzui', 'lqym_shuangan'], ['qun']],
						'lqym_No.14': ['male', 'wei', 4, ['lqtm_qiangfei', 'lqym_yushou'], ['wei']],
						'lqym_No.15': ['male', 'qun', 3, ['lqtm_lianhuan'], ['qun']],
						'lqym_No.16': ['male', 'shu', 3, ['lqym_sese'], ['shu']],
						'lqym_No.17': ['male', 'wei', 4, ['lqym_shie'], ['wei']],
						'lqym_No.18': ['male', 'wei', 4, ['lqym_juetong', 'lqym_jihui'], ['wei']],
						'lqym_No.19': ['male', 'wei', 4, ['lqym_bingying'], ['wei']],
						'lqym_No.20': ['male', 'jin', 4, ['lqym_huiang'], ['jin']],
						'lqym_No.21': ['female', 'wei', 4, ['lqym_bingjie'], ['wei']],
						'lqym_No.22': ['male', 'wei', 4, ['lqym_wugen', 'lqym_luanjian'], ['wei']],
						'lqym_No.23': ['male', 'jin', 3, ['lqym_lingji', 'lqym_lingyuan', 'lqym_mingyu'], ['jin']],
						'lqym_No.66': ['male', 'qun', 3, ['lqym_bajian', 'lqym_tuijia'], ['qun']],
						'lqym_No.76': ['female', 'wei', 4, ['lqym_xieyin'], ['wei']],
						'lqym_No.78': ['male', 'wu', 4, ['lqym_cundang', 'lqym_bianma'], ['wu']],
					},
				};
				for (const i in langqinyouman.character) {
					langqinyouman.character[i][4].push('ext:浪琴天阙/image/浪琴游漫/' + i + '.jpg');
				}
				lib.config.all.characters.add('langqinyouman');
				lib.config.characters.add('langqinyouman');
				lib.translate.langqinyouman_character_config = '<font color=#ffff33>浪琴</font><font color=#ff44aa>游漫</font>';
				return langqinyouman;
			});
			// ------------------------------浪琴超越-----------------------------------//
			game.import('character', function () {
				const langqinlanhuo = {
					name: 'langqinlanhuo',
					connect: true,
					characterSort: {
						// --------------------------------------武将分类------------------------------------------//
						langqinlanhuo: {
							lqtq_ciyuanshengwei: ['lqcy_caocao', 'lqcy_guojia', 'lqcy_lidian', 'lqcy_daqiao'],
						},
					},
					// --------------------------------------武将资料------------------------------------------//
					character: {
						lqcy_caocao: ['male', 'wei', 4, ['lqcy_jianxiong', 'lqcy_cidong', 'lqcy_hujia'], ['wei']],
						lqcy_guojia: ['male', 'wei', 3, ['lqcy_tiandu', 'lqcy_yingcai', 'lqcy_yiji'], ['wei']],
						lqcy_lidian: ['male', 'wei', 3, ['lqcy_xunxun', 'lqcy_wangxi', 'lqcy_shengquan'], ['wei']],
						lqcy_daqiao: ['female', 'wu', 3, ['lqcy_guose', 'lqcy_liuli', 'lqcy_yanxiao'], ['wu']],
					},
				};
				for (const i in langqinlanhuo.character) {
					langqinlanhuo.character[i][4].push('ext:浪琴天阙/image/浪琴超越/' + i + '.jpg');
				}
				lib.config.all.characters.add('langqinlanhuo');
				lib.config.characters.add('langqinlanhuo');
				lib.translate.langqinlanhuo_character_config = '<font color=#ffff00>浪琴</font><font color=#ff0088>超越</font>';
				return langqinlanhuo;
			});
			// --------------------------------------挑战武将------------------------------------------//
			game.import('character', function () {
				const langqintiaozhan = {
					name: 'langqintiaozhan',
					connect: true,
					characterSort: {
						// --------------------------------------武将分类------------------------------------------//
						langqintiaozhan: {
							lqtq_luolijisi: ['lqtz_langqinbiao', 'lqtz_langqinyu'],
							lqtz_langqintianque: ['lqtz_langqintianque'],
							lqtz_shanhaizhiyihun: ['lqtz_shanhaizhiyi', 'lqtzhun_xuzhu', 'lqtzhun_simayi', 'lqtzhun_caocao', 'lqtzhun_sunquan', 'lqtzhun_zhouyu', 'lqtzhun_guanyu', 'lqtzhun_zhugeliang', 'lqtzhun_liubei', 'lqtzhun_diaochan', 'lqtzhun_lubu', 'lqtzhun_ganning'],
							lqtz_shanhaizhiyinian: ['lqtz_chi', 'lqtz_mei', 'lqtz_wang', 'lqtz_liang', 'lqtz_niutou', 'lqtz_mamian', 'lqtz_nianshou'],
							lqtz_shanhaizhiyigui: ['lqtz_luocha', 'lqtz_yecha', 'lqtz_heiwuchang', 'lqtz_baiwuchang'],
							lqtz_shanhaizhiyirui: ['lqtz_qiongqi', 'lqtz_hundun', 'lqtz_taowu', 'lqtz_taotie', 'lqtz_qilin', 'lqtz_zhuyin'],
							lqtz_shanhaizhiyihu: ['lqtz_zhuquezhenshen', 'lqtz_xuanwuzhenshen', 'lqtz_dahu', 'lqtz_xiaohu'],
							lqtz_shanhaizhiyiqing: ['lqtz_qingnu', 'lqtz_hunu', 'lqtz_jiuweihu'],
							lqtz_quguizhuxie: ['lqtz_quguizhuxie', 'lqtz_yanluowang', 'lqtz_guiwang', 'lqtz_riyeyoushen', 'lqtz_niutoumamian', 'lqtz_heibaiwuchang', 'lqtz_huangfeng', 'lqtz_baowei', 'lqtz_yvsai', 'lqtz_niaozui', 'lqtz_mengpoa'],
							lqtz_aolihagang: ['lqtz_aolihagang', 'lqtz_qidala', 'lqtz_qijiasi', 'lqtz_jingzishibing', 'lqtz_xiruonuosi', 'lqtz_xiruonuosizuoshou', 'lqtz_xiruonuosiyoushou', 'lqtz_sheshenyi', 'lqtz_dazi', 'lqtz_zhishen'],
							lqtz_qingqingzijin: ['lqtz_qingqingzijin', 'lqtzqqzj_caocao', 'lqtzqqzj_simayi', 'lqtzqqzj_lubu', 'lqtzqqzj_dongzhuo', 'lqtzqqzj_zhangjiao', 'lqtzqqzj_yuanshu'],
							lqtz_tianweishenjia: ['lqtz_tianweishenjia', 'lqtz_ren', 'lqtz_dun', 'lqtz_yi', 'lqtz_shenbing', 'lqtz_maerxiusiren', 'lqtz_maerxiusidun', 'lqtz_maerxiusiyi', 'lqtz_maerxiusishen'],
							lqtz_jiximengqin: ['lqtz_jiximengqin', 'lqtz_qiyilieying', 'lqtz_quzhubojue', 'lqtz_lieyanlieying', 'lqtz_emodiao', 'lqtz_geminglieying', 'lqtz_wulilinxiao', 'lqtz_weixingpaolieying', 'lqtz_huorenfenshaolieying', 'lqtz_jiujilieying', 'lqtz_yibanglieying', 'lqtz_zuizhongbaoleilieying', 'lqtz_wukulieying', 'lqtz_zhihuilinxiao'],
							lqtz_danjiangboss: ['lqtz_zhangrang', 'lqtz_machao', 'lqtz_ling', 'lqtz_yinianheyi'],
						},
					},
					// --------------------------------------武将资料-----------------------------------------//
					character: {
						lqtz_shanhaizhiyi: ['', '', 0, ['lqtz_shanhai'], ['boss'], 'qun'],
						lqtz_quguizhuxie: ['', '', 0, ['lqtz_zhuxie'], ['boss'], 'wei'],
						lqtz_aolihagang: ['', '', 0, ['lqtz_aogang'], ['boss'], 'shu'],
						lqtz_qingqingzijin: ['', '', 0, ['lqtz_zijin'], ['boss'], 'qun'],
						lqtz_tianweishenjia: ['', '', 0, ['lqtz_shenjia1'], ['boss'], 'qun'],
						lqtz_langqintianque: ['', '', 0, ['lqtz_shanhaizx', 'lqtz_tianque'], ['boss'], 'qun'],
						lqtz_jiximengqin: ['', '', 0, ['lqtz_RR'], ['boss'], 'qun'],
						lqtzhun_xuzhu: ['male', 'wei', 11, ['mengjin', 'mashu', 'lqtz_shanhai2'], ['wei', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzhun_simayi: ['male', 'wei', 12, ['mingzhe', 'xinleiji', 'lqtz_shanhai3'], ['wei', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzhun_caocao: ['male', 'wei', 13, ['shuimeng', 'tianbian', 'xinjyzongshi', 'regushe'], ['wei', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzhun_ganning: ['male', 'wu', 11, ['retiaoxin', 'qianxun', 'lqtz_shanhai2'], ['wu', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzhun_zhouyu: ['male', 'wu', 12, ['yingjian', 'xiangle', 'lqtz_shanhai3'], ['wu', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzhun_sunquan: ['male', 'wu', 13, ['xinfu_langxi', 'xiansi', 'yuce', 'reyingzi'], ['wu', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzhun_guanyu: ['male', 'shu', 11, ['lqtz_guihuo', 'feiying', 'lqtz_shanhai2'], ['shu', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzhun_zhugeliang: ['male', 'shu', 12, ['xinjushou', 'niluan', 'lqtz_shanhai3'], ['shu', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzhun_liubei: ['male', 'shu', 13, ['refankui', 'xinkuanggu', 'xiaoguo', 'luoying'], ['shu', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzhun_diaochan: ['female', 'qun', 11, ['oltianxiang', 'lqtz_guimei2', 'lqtz_shanhai2'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzhun_lubu: ['male', 'qun', 13, ['qingxi', 'rezhiyi', 'moukui', 'lqtz_modao'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_chi: ['male', 'qun', 11, ['lqtz_guimei2', 'junxing', 'lqtz_shanhai2'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_mei: ['female', 'qun', 11, ['lqtz_guimei2', 'enyuan', 'lqtz_shanhai2'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_wang: ['male', 'qun', 11, ['lqtz_guimei2', 'new_retuxi', 'lqtz_shanhai2'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_liang: ['female', 'qun', 11, ['lqtz_guimei2', 'qiangxix', 'lqtz_shanhai2'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_niutou: ['male', 'qun', 12, ['lqtz_guimei2', 'mingzhe', 'xiangle', 'lqtz_shanhai3'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_mamian: ['male', 'qun', 12, ['lqtz_guimei2', 'xinjuece', 'xiansi', 'lqtz_shanhai3'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_nianshou: ['male', 'qun', 15, ['lqtz_juhuo', 'lqtz_zhennu', 'hanyong', 'reyingzi'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_luocha: ['male', 'qun', 14, ['biyue', 'lqtz_qushou', 'reganglie', 'lqtz_guimei2'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_yecha: ['male', 'qun', 14, ['yingzi', 'lqtz_mojian', 'lqtz_danshu', 'lqtz_guimei2'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_heiwuchang: ['male', 'qun', 14, ['wushuang', 'lqtz_leili', 'xinkuanggu', 'lqtz_guimei2'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_baiwuchang: ['male', 'qun', 14, ['lqtz_modao', 'lqtz_zuijiu', 'xinshensu', 'lqtz_guimei2'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_qiongqi: ['male', 'qun', 11, ['lqtz_xiemei', 'new_retuxi', 'lqtz_shanhai2'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_hundun: ['male', 'qun', 11, ['lqtz_xiemei', 'lqtz_guihuo', 'lqtz_shanhai2'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_taowu: ['male', 'qun', 12, ['lqtz_xiemei', 'lqtz_shehun', 'lqtz_lingsi', 'lqtz_shanhai3'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_taotie: ['male', 'qun', 12, ['lqtz_xiemei', 'lqtz_taoyuan', 'lqtz_duoling', 'lqtz_shanhai3'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_zhuyin: ['male', 'qun', 15, ['zhuiji', 'lqtz_jingxin', 'lqtz_xifan', 'lqtz_yazi'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_qilin: ['male', 'qun', 3, ['lqtz_xiangrui'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_zhuquejiangling: ['male', 'qun', 8, ['lqtz_xiongqu'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_zhuquefaxiang: ['male', 'qun', 9, ['lqtz_xiongqu', 'lqtz_lihuo'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_zhuquezhenshen: ['male', 'qun', 10, ['lqtz_xiongqu', 'lqtz_lihuo', 'lqtz_fengxing', 'lqtz_beiming'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_xuanwujiangling: ['female', 'qun', 8, ['lqtz_xiongqu'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_xuanwufaxiang: ['female', 'qun', 9, ['lqtz_xiongqu', 'lqtz_zhenlei'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_xuanwuzhenshen: ['female', 'qun', 10, ['lqtz_xiongqu', 'lqtz_zhenlei', 'lqtz_leili', 'lqtz_lingsia'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_xiaohu: ['male', 'qun', 6, ['lqtz_yutu', 'lqtz_wuyou', 'lqtz_xiongqu', 'lqtz_weihu'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_dahu: ['male', 'qun', 6, ['lqtz_yutu', 'lqtz_wuyou', 'lqtz_xiongqu', 'lqtz_weihu', 'lqtz_zuoji'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_qingnu: ['female', 'qun', 9, ['_lqtz_duomo', 'lqtz_meihuo'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_hunu: ['female', 'qun', 10, ['_lqtz_duomo', 'lqtz_meihuo', 'lqtz_qushou'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_jiuweihu: ['female', 'qun', 11, ['_lqtz_duomo', 'lqtz_meihuo', 'lqtz_qushou', 'lqtz_duanwei', 'lqtz_qinyun'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_yanluowang: ['male', 'qun', 18, ['lqtz_tiemian', 'lqtz_difu', 'lqtz_zhennub', 'lqtz_xingpan', 'lqtz_dianwei', 'lqtz_xuanpan', 'lqtz_guimei'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_riyeyoushen: ['male', 'qun', 10, ['lqtz_zhoucha', 'lqtz_yezhong', 'lqtz_huiyun', 'lqtz_duane', 'lqtz_guimei'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_niutoumamian: ['male', 'qun', 12, ['lqtz_xiaoshou', 'lqtz_manji', 'lqtz_shiyu', 'lqtz_guizhao', 'lqtz_guimei'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_heibaiwuchang: ['male', 'qun', 12, ['lqtz_xixing', 'lqtz_taiping', 'lqtz_mizui', 'lqtz_qiangzheng', 'lqtz_guimei'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_mengpoa: ['female', 'qun', 12, ['lqtz_aotang', 'lqtz_yunju', 'lqtz_guimei'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_yvsai: ['female', 'qun', 12, ['lqtz_guixi', 'lqtz_anchao', 'lqtz_guimei'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_niaozui: ['male', 'qun', 10, ['lqtz_bingyi', 'lqtz_suoxue', 'lqtz_guimei'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_huangfeng: ['male', 'qun', 7, ['lqtz_duzhen', 'lqtz_mingchong', 'lqtz_guimei'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_baowei: ['male', 'qun', 9, ['lqtz_yinsha', 'lqtz_eli', 'lqtz_guimei'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_guiwang: ['male', 'qun', 30, ['lqtz_jizhou', 'lqtz_danshi', 'lqtz_tiemian', 'lqtz_chihu', 'lqtz_guimei'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_langqinbiao: ['female', 'shen', 2, ['lqtz_yingying', 'lqtz_abaaba', 'lqtz_miaowu', 'lqtz_chiliu'], ['qun', 'boss', 'bossallowed'], 'zhu'],
						//'lqtz_langqinyu':['female','shen',1,[],['shen','boss','bossallowed'],'zhu'],
						lqtz_zhangrang: ['male', 'qun', 80, ['lqtz_guanshi', 'lqtz_huoluan', 'lqtz_xumou', 'lqtz_jiquan', 'lqtz_luanzheng'], ['qun', 'boss', 'bossallowed'], 'qun'],
						lqtz_machao: ['male', 'shen', 25, ['lqtz_tieji', 'lqtz_xiongshi', 'lqtz_mashu', 'lqtz_qianji'], ['shu', 'boss', 'bossallowed'], 'zhu'],
						lqtz_ling: ['male', 'qun', '0/4', ['xinjuejing', 'buqu', 'lqtz_fanshi', 'lqtz_miewu'], ['shu', 'boss', 'bossallowed'], 'zhu'],
						lqtz_yinianheyi: ['male', 'shen', '20/25/5', ['lqtz_juechou', 'lqtz_longce', 'lqtz_hundu'], ['wei', 'boss', 'bossallowed'], 'zhu'],
						lqtz_qidala: ['male', 'qun', 6, ['lqtz_qida'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_jingzishibing: ['male', 'qun', 3, ['lqtz_jingying'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_qijiasi: ['male', 'qun', 2, ['lqtz_qijia'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_xiruonuosi: ['male', 'qun', 1, ['lqtz_shenou', 'lqtz_baoxing'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_xiruonuosizuoshou: ['male', 'qun', 5, ['lqtz_zuidun'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_xiruonuosiyoushou: ['male', 'qun', 5, ['lqtz_zuiren'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_sheshenyi: ['male', 'qun', Infinity, ['lqtz_shenhuang', 'lqtz_pangqu'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_dazi: ['male', 'qun', 10, ['lqtz_anxi', 'lqtz_xiequ'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_zhishen: ['male', 'qun', 18, ['lqtz_shenzong', 'lqtz_shenyi', 'lqtz_shenpo', 'lqtz_shenyan'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzqqzj_caocao: ['male', 'wei', 30, ['rejianxiong', 'lqtz_lingba', 'lqtz_yishen'], ['wei', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzqqzj_simayi: ['male', 'wei', 30, ['refankui', 'reguicai', 'lqtz_langgu', 'lqtz_yuanlu'], ['wei', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzqqzj_lubu: ['male', 'qun', 30, ['lqtz_jixun', 'wushuang', 'lqtz_shenji', 'lqtz_zhankai'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzqqzj_dongzhuo: ['male', 'qun', 30, ['jiuchi', 'roulin', 'lqtz_baonue', 'lqtz_qubu'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzqqzj_zhangjiao: ['male', 'qun', 24, ['guidao', 'releiji', 'lqtz_jianzheng', 'lqtz_yinlei'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtzqqzj_yuanshu: ['male', 'qun', 30, ['drlt_yongsi', 'lqtz_wangzun', 'lqtz_duoxi'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_ren: ['male', 'qun', 4, ['lqtz_gongpo', 'lqtz_tongben', 'lqtx_xisheng'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_dun: ['male', 'qun', 5, ['latz_fangyu', 'lqtz_tongben', 'lqtx_xisheng'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_yi: ['male', 'qun', 4, ['lqtz_feiyue', 'lqtz_tongben', 'lqtx_xisheng'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_shenbing: ['male', 'qun', 6, ['lqtz_shenbingj', 'yinghun', 'lqtz_tongben', 'lqtx_xishengjia'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_maerxiusiren: ['male', 'qun', 20, ['lqtz_huimie', 'lqtz_fengbao', 'lqtz_shuangren', 'lqtz_shenjia'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_maerxiusiyi: ['male', 'qun', 20, ['lqtz_jixun', 'lqtz_leiting', 'lqtz_shuangyi', 'lqtz_shenjia'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_maerxiusidun: ['male', 'qun', 20, ['lqtz_bilei', 'lqtz_niliu', 'lqtz_shuangdun', 'lqtz_shenjia'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_maerxiusishen: ['male', 'shen', 80, ['lqtz_tianwei', 'lqtz_shenbingx', 'lqtz_shenchao', 'lqtz_zhongzhuang', 'lqtz_jiying'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_qiyilieying: ['male', 'qun', 5, ['lqtz_qiyi', 'lqtz_yongzhua'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_quzhubojue: ['male', 'qun', 5, ['lqtz_quzhu'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_lieyanlieying: ['male', 'qun', 10, ['lqtz_lieyan', 'lqtz_xiongyan'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_emodiao: ['male', 'qun', 10, ['lqtz_ehui'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_geminglieying: ['male', 'qun', 15, ['lqtz_geming', 'lqtz_kongxi', 'lqtz_yingyi'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_wulilinxiao: ['male', 'qun', 15, ['lqtz_wuxiao'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_weixingpaolieying: ['male', 'qun', 20, ['lqtz_yingbi', 'lqtz_guogan', 'lqtz_jianong'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_huorenfenshaolieying: ['male', 'qun', '3/20/5', ['lqtz_huoren', 'lqtz_fenshao'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_jiujilieying: ['male', 'qun', 25, ['lqtz_yingyou', 'lqtz_rongguang', 'lqtz_aoxiang'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_yibanglieying: ['male', 'qun', 25, ['lqtz_yingyi', 'lqtz_yibang'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_zuizhongbaoleilieying: ['male', 'qun', 30, ['lqtz_bileix', 'lqtz_baolei', 'lqtz_yaosai'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_wukulieying: ['male', 'qun', 30, ['spwuku', 'lqtz_jiwu', 'lqtz_sanji', 'lqtz_bangwu'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
						lqtz_zhihuilinxiao: ['male', 'qun', 30, ['lqtz_zhixiao', 'lqtz_shengjie'], ['qun', 'hiddenboss', 'bossallowed'], 'qun'],
					},
				};
				for (const i in langqintiaozhan.character) {
					langqintiaozhan.character[i][4].push('ext:浪琴天阙/image/浪琴挑战/' + i + '.jpg');
				}
				lib.config.all.characters.add('langqintiaozhan');
				lib.config.characters.add('langqintiaozhan');
				lib.translate.langqintiaozhan_character_config = '<font color=#eeee00>浪琴</font><font color=#c10066>挑战</font>';
				return langqintiaozhan;
			});
		},
		config: {
			死亡移除: {
				name: '<span class="Qmenu">死亡移除</span>',
				intro: '死亡后移出游戏',
				init: true,
				onclick(result) {
					game.saveConfig('dieremove', result);
				},
			},
			LQTQA: {
				name: '<div class="langqintianque">「未读」扩展介绍<font size="4px" color="yellow">▶▶▶</font></div>',
				clear: true,
				onclick() {
					if (this.LQTQA == undefined) {
						const more = ui.create.div('.LQTQA', '<div style="border:2px solid gray"><P align=left>扩展作者:浪琴婊<br><br>代码编写:浪琴婊<br><br>扩展版本:3.0.3<br><br><b><li>浪琴天阙</b><br><P align=left>萝莉祭司:一婊三魂,三倍化身,三倍灭吴,还原你的梦想!<br><br>亿婊堂堂:本婊贝的DIY武将,都是有感而发之作,是贤是拙,一探便知.<br><br>突发奇想:虽是原有武将图,却也是DIY武将,设计思路是先做技能,再把技能配给某个心怡武将,技能与武将基本上是没有什么关联,扩内强度适中,以娱乐为主,对!娱乐!不要质疑!<br><br>闲做改值:单纯就是把原武将缝合改值后的产物,没什么营养,就是本婊贝想玩,随便改着玩的,阿巴阿巴阿巴......<br><br>稀有专属:当你掏出这类武将的时候,你已经稳了,具体强度本婊也说不准,嘤嘤嘤.<br><P align=left><li><b>浪琴荟萃</b><br><P align=left>群嘤投稿:投稿,顾名思义本栏目皆是一些收录了群里热心人士的投稿的作品武将.若你有想法,也可以加入我天阙进行投稿,但本婊贝只有在接稿期间才会接稿.<br><P align=left><li><b>浪琴游漫</b><br><P align=left>收录一些想写的游戏或动漫的模型武将,强度适中<br><P align=left><li><b>浪琴超越</b><br><P align=left>看了手刹的谋旧划新有感而发,写一些超越界限的武将.<br><P align=left><li><b>浪琴挑战</b><br><P align=left>顾名思义是个挑战模式扩展,以手刹挑战山海志异为主,以及作者自制或群友投稿的多个挑战,趣味十足.');
						this.parentNode.insertBefore(more, this.nextSibling);
						this.LQTQA = more;
						this.innerHTML = '<div class="langqintianque">「查阅」扩展介绍<font size="4px">▼▼▼</font></div>';
					} else {
						this.parentNode.removeChild(this.LQTQA);
						delete this.LQTQA;
						this.innerHTML = '<div class="langqintianque">「已读」扩展介绍<font size="4px">▶▶▶</font></div>';
					}
				},
			},
			LQTQB: {
				name: '<div class="langqintianque">「未读」扩展群聊<font size="4px" color="gold">▶▶▶</font></div>',
				clear: true,
				onclick() {
					if (this.LQTQB == undefined) {
						const more = ui.create.div('.LQTQB', '<div style="border:2px solid gray"><P align=left>浪琴婊群:822753575<br><br><img style=width:255px src=extension/浪琴天阙/image/辅助图/lqtq_kuozhanqunliao.jpg>');
						this.parentNode.insertBefore(more, this.nextSibling);
						this.LQTQB = more;
						this.innerHTML = '<div class="langqintianque">「查阅」扩展群聊<font size="4px">▼▼▼</font></div>';
					} else {
						this.parentNode.removeChild(this.LQTQB);
						delete this.LQTQB;
						this.innerHTML = '<div class="langqintianque">「已读」扩展群聊<font size="4px">▶▶▶</font></div>';
					}
				},
			},
			LQTQC: {
				name: '<div class="langqintianque">「未读」本次更新<font size="4px" color="orange">▶▶▶</font></div>',
				clear: true,
				onclick() {
					if (this.LQTQC == undefined) {
						const more = ui.create.div(
							'.LQTQC',
							'<div style="border:2px solid gray">' +
							//'<b><li>功能更新&nbsp;&nbsp;&nbsp;❀</b><br><P align=left>伤害音效:移步功能键长按查看详情,婊就不再多说一遍了.<br><br>体力调整:移步功能键长按查看详情.<br><br>筛技扩充:移步功能键长按查看详情.<br><br>无限婊读:移步功能键长按查看详情.<br><br>评级选将:移步功能键长按查看详情.'<br>+
							//'<P align=left><li><b>浪琴天阙更新&nbsp;&nbsp;&nbsp;❀</b><br><P align=left>☄孙尚香:东吴女歌王香香参上!快来与香香举案齐眉双剑共引吧!香香自带两款伴生皮肤(需安装了千幻聆音UI才可查阅).<br><br>☄周处:修复了开局跳屏的bug.<br><br>☄关银屏:修复了武夷AI无法正常判断牌价值的bug.<br><br>◎邓艾:修复了急袭AI卡顿一分钟后直接结束回合的bug.<br><br>◎神甘宁:修复了奇袭AI无法正常判定牌价值的bug.<br>'+
							'<P align=left><li><b>浪琴荟萃更新&nbsp;&nbsp;&nbsp;❀</b><br><P align=left>古见:此武将刚上线,我的老婆古见体验过后觉得前期乏力,所以进行了一些技能上的调整,二技能抽卡展示牌堆顶3张改为展示场上人数的牌数,三技能准备阶段学技能改为每轮游戏开始时学,陷阱部分将魔法筒替换为便乘:每个回合对每名角色其他角色各限一次,当这些角色获得牌时,你摸一张牌.<br>' +
							'<P align=left><li><b>浪琴游漫更新&nbsp;&nbsp;&nbsp;❀</b><br><P align=left><br>No.21 冰结正义女郎:前后两难的屑.<br><br>No.22 不乱健:强人锁难.<br><br>No.23 冥界的灵骑士 兰斯洛特:不分敌我的屑<br><br>扩内多个武将更换了武将图并添加了一至两款皮肤(皮肤需千幻)<br>' +
							'<P align=left><li><b>浪琴超越更新&nbsp;&nbsp;&nbsp;❀</b><br><P align=left>罠大乔:大乔突破界限后,全场自此不思蜀.<br>' +
							//'<P align=left><li><b>浪琴挑战更新&nbsp;&nbsp;&nbsp;❀</b><br><P align=left>bug修复:修复了某些关卡BOSS因缠怨之类的技能阵亡后无法切换下一关的bug.<br><br>急袭猛禽:闯关型挑战,前后共6个关卡,游戏由BOSS先手,BOSS与随从的起始手牌为8,挑战方的起始手牌为5,BOSS阵亡后清空游戏轮数,切换新的BOSS出场,并立即开始BOSS的回合.<br>第一关:挑战起翼猎鹰,随从两只驱逐伯爵.<br>第二关:挑战烈焰猎鹰,随从两只恶魔雕.<br>第三关:挑战革命猎鹰,随从两只武力林鸮.<br>第四关:挑战卫星炮猎鹰,随从两只火刃焚烧猎鹰.<br>第五关:挑战究极猎鹰,随从两只异邦猎鹰.<br>第六关:挑战最终堡垒猎鹰,随从武库猎鹰、智慧林鸮.'+
							'',
						);
						this.parentNode.insertBefore(more, this.nextSibling);
						this.LQTQC = more;
						this.innerHTML = '<div class="langqintianque">「查阅」更新历史<font size="4px">▼▼▼</font></div>';
					} else {
						this.parentNode.removeChild(this.LQTQC);
						delete this.LQTQC;
						this.innerHTML = '<div class="langqintianque">「已读」更新历史<font size="4px">▶▶▶</font></div>';
					}
				},
			},
			lqtq_gongneng: {
				name: "<font size='5' font color=#ffffbb>功能设置▼</font>",
				clear: true,
			},
			lqtq_shanghai_damage: {
				name: '伤害音效<font size=1>(长按此处看详情)</font>',
				intro: '开启并重启游戏后生效,伤害以及失去体力的音效改变为与手刹一致.(注:若你已开启其他扩展的换音设置可能会出现冲突,关闭此功能后重启即可).',
				init: false,
			},
			lqtq_tili_tiaozheng: {
				name: '体力调整<font size=1>(长按此处看详情)</font>',
				intro: '开启并重启游戏后生效,非挑战模式下,将BOSS武将的体力上限以及体力下调,让BOSS武将更好的融入其他模式.武将牌技能三个以上的武将变成3体力上限3体力,武将牌技能两个以下的武将变成4体力上限4体力.',
				init: false,
			},
			lqtq_shaiji_kuochong: {
				name: '筛技扩充<font size=1>(长按此处看详情)</font>',
				intro: '开启并重启游戏后生效,本扩展的技能内容过滤武将牌时,AI禁用的武将牌亦适用,此功能对AI不适用.<br><br>功能范围▼▼▼<br>啊婊的援交<br>☄马钧的精巧<br>☄诸葛果的灵禳<br>☄左慈的抓鸭脖<br>杀神的交易<br>❁神姜维的武志<br>想去远方的中二<br>No.78的存档和编码<br>维郭嘉的英才<br>模式挑战功能.',
				init: false,
			},
			lqtq_yinni_qianzou: {
				name: '隐藏前奏<font size=1>(长按此处看详情)</font>',
				init: false,
				intro: '开启并重启游戏后生效,隐藏本扩武将ID前奏「&nbsp;☄/◎/❁&nbsp;」.',
			},
			lqtq_wuxian_biaodu: {
				name: '无限婊读<font size=1>(长按此处看详情)</font>',
				intro: '开启并重启游戏后生效,此功能BOSS模式不可用.<br><br>精华版:game.me死亡时(可理解为不为AI操控的角色),变身白板婊.<br><br>乱轮版:游戏中角色首次死亡时,变身白板婊.<br><br>白板婊BUFF<br>【开摆开摆】:你濒死或死亡时,将所有牌交给其他角色纵观全局.<br><br>【纵观全局】:你死亡状态下角色的回合开始时,你观看并选择其一张手牌对一名角色使用.<br><br>【负隅顽抗】:友军死亡导致游戏即将结束时,你可选择负隅顽抗复活进入迷糊状态.<br><br>【迷糊状态】:你可令你使用或目标包含你的牌变成任意牌.',
				init: '0',
				item: {
					0: '关闭',
					1: '精华版',
					2: '乱伦版',
				},
			},
			lqtq_woyao_sese: {
				name: '我要涩涩<font size=1>(长按此处看详情)</font>',
				intro: '开启并重启游戏后生效.<br><br>可以涩涩:AI只会选择女性武将出战.<br><br>男同贴贴:AI只会选择男性武将出战.<br><br>(注:此功能为强制令不适合的武将AI禁用,若出现AI无武将可选,或扶汉之类的技能无武将牌或武将技能可展示时,更换此功能选项或关闭此功能并重启即可).',
				init: '0',
				item: {
					0: '不可以涩涩',
					1: '可以涩涩',
					2: '男同贴贴',
				},
			},
			lqtq_pingji_xuanjiang: {
				name: '评级选将<font size=1>(长按此处看详情)</font>',
				intro: '开启并重启游戏后生效,AI只会选择对应评级的武将出战,「其他」选项包含平凡以及未分类的武将.(注:此功能为强制令不适合的武将AI禁用,若出现AI无武将可选,或扶汉之类的技能无武将牌或武将技能可展示时,更换此功能选项或关闭此功能并重启即可).',
				init: '0',
				item: {
					0: '关闭',
					1: '传说',
					2: '史诗',
					3: '精品',
					4: '其他',
				},
			},
			lqtq_gongneng2: {
				name: "<font size='5' font color=#ffffbb>挑战模式功能▼</font>",
				clear: true,
			},
			lqtq_shouqi_ka: {
				name: '挑战手气卡<font size=1>(长按此处看详情)</font>',
				init: false,
				intro: '开启并重启游戏后生效,在挑战模式中添加手气卡.',
			},
			lqtq_xuanyuan_tiaozhan: {
				name: '选员挑战<font size=1>(长按此处看详情)</font>',
				intro: '开启并重启游戏后生效,挑战模式中,可改为双人或单人挑战.(注:此功能会替换挑战模式的选将页面,如与其他扩展冲突关闭此功能并重启即可).',
				init: '3',
				item: {
					3: '关闭',
					2: '双人',
					1: '单人',
				},
			},
			lqtq_zhuanshu_moshi: {
				name: '模式挑战<font size=1>(长按此处看详情)</font>',
				intro: '开启并重启游戏后生效<br><b>模式Ⅰ:</b><br>游戏开始时,清空挑战方所有技能,并将挑战方的体力调整为6,每人从12个系统随机给出的技能中选择1个技能获得.当BOSS阵亡时,挑战方每人从12个系统随机给出的技能中选择1个技能获得,当获得的技能超过4个时,需选择1个技能失去.<br><br><b>模式Ⅱ:</b><br>游戏开始时,清空挑战方所有技能,并将挑战方的体力调整为6,每人从12个系统随机给出的技能中选择1个技能获得.游戏内挑战方对敌方角色造成1点伤害后/BOSS阵亡后,挑战方可获得1枚/5枚<朱果>,<朱果>为友方共用,出牌阶段,挑战方可以移去三枚<朱果>,从12个系统随机给出的技能中选择1个技能获得,当获得的技能超过4个时,需选择1个技能失去.<br><br><b>模式Ⅲ:</b><br>游戏开始时,清空挑战方所有技能,并将挑战方的体力调整为6,每人从12个系统随机给出的技能中选择1个技能获得.BOSS视为拥有技能〖堕魔〗(技能详情请查看山海志异BOSS「九尾狐」).挑战方的准备阶段开始时,可以从12个系统随机给出的技能中选择1个技能获得,当获得的技能超过4个时,需选择1个技能失去.<br><br><b>模式Ⅳ:</b><br>当BOSS阵亡时,挑战方选择回复一点体力或摸两张牌并从12个系统随机给出的技能中选择1个技能获得,获得的主动技只能保留一个,获得新的主动技时自动替换旧的主动技.',
				init: '0',
				item: {
					0: '关闭',
					1: '模式Ⅰ',
					2: '模式Ⅱ',
					3: '模式Ⅲ',
					4: '模式Ⅳ',
				},
			},
			lqtq_shanhaizhiyi_pian: {
				name: '山海志异<font size=1>(长按此处看详情)</font>',
				intro: '开启并重启游戏后生效,可更改挑战模式中山海志异的关卡,其他详情请查看挑战模式中的山海志异.',
				init: 'lqtz_huhushengwei',
				item: {
					lqtz_jianghunjuexing: '将魂觉醒',
					lqtz_quzhunianshou: '驱逐年兽',
					lqtz_quguibixie: '驱鬼辟邪',
					lqtz_ruilinjiangshi: '瑞麟降世',
					lqtz_huhushengwei: '虎虎生威',
					lqtz_qingqiunongqin: '青丘弄琴',
				},
			},
			lqtq_langqintianque_pian: {
				name: '浪琴天阙<font size=1>(长按此处看详情)</font>',
				intro: '开启并重启游戏后生效,可更改挑战模式中浪琴天阙的关卡,其他详情请查看挑战模式中的浪琴天阙.',
				init: '1',
				item: {
					0: '浪琴天阙',
					1: '乱战天阙',
				},
			},
		},
		package: {
			intro: '<font color=#ffff33><b><font size=2>扩展须知:初次导入扩展需要移步〖武将〗手动开启〖浪琴〗的5个武将包并重启游戏,武将包关闭时不会显示武将卡图.<br>关注微信公众号「无名杀扩展交流」,及时获取<浪琴天阙>最新版本!</b></font>',
			version: '1.3',
		},
	};
});
